# Retired Paths

This file records paths that should not be revived without new evidence and, when appropriate, an ADR.

| Path/name | Status | Why retired | Replacement | Revisit condition |
| --- | --- | --- | --- | --- |
| External LLM/Cortex/ThoughtLoop runtime path | retired | Added external dependency and ambiguous cognition claims without being the living substrate. | Subcortex-owned runtime evidence, semantics/readout surfaces, replay, and Runtime Truth. | A future ADR proves a bounded dependency does not become the cognition substrate and preserves evidence gates. |
| Manager mixin compatibility aliases | retired | Preserved shallow ownership after the service split. | Explicit deep modules and RuntimeFacade/StatusReadModel ownership. | Only if a compatibility period is required by a released public API. |
| Divergent Column Runtime service projections | retired implementation shape | `StatusReadModel` carried the promoted route-bank probe-lane and cached-state Runtime Truth fields, but `RuntimeStatusCore` still projected an older smaller shape. That let active service surfaces drift on `probe_rows`, `score_rows`, `probe_cursor`, `probe_refresh_count`, wake-plan mode, cached transition count, and predictive cached-vote fraction even though neither service module owns scheduler decisions. | `service.column_runtime_projection.build_column_runtime_evidence` is the single read-only projection helper for both status paths. Training/core still owns wake/sleep, route candidates, cached-vote use, memory pressure, and transition scope. | Reintroduce a separate projection only if a new service surface has a different public contract and focused tests prove it still exposes the full scheduler truth without decision ownership. |
| Static CUDA intent as capability proof | retired | Configuration can hide CPU fallback. | Observed tensor/device evidence in Runtime Truth and gates. | Never as proof; static intent may remain diagnostic only. |
| Local NVIDIA NIM/Cortex dotenv configuration | retired | The external LLM/Cortex adapters and embedder are deleted, so keeping local NIM credentials implies a capability the runtime cannot and must not use. | Local Hugging Face source credentials only; MARULHO-owned Subcortex/SNN cognition remains local. | Only through a new ADR that explicitly reopens an external non-cognition source/client boundary. |
| Routing-index reconstruction-distance reuse with pending overlay | rejected experiment | Exact numerical parity was possible, but the complete 1024-column CUDA routing pipeline regressed by 20.6% median and 22.3% p95 because expanded search and overlay control cost exceeded the removed dense scan. | Keep the fresh dense reconstruction scan and the separately proven candidate-scoped competitive scoring path. | Revisit only as a fused device-side pipeline benchmarked against the complete routing step. |
| Raw Wikipedia as default Terminus background source | retired | HF source review found a drop-in open-textbook stream with direct `text` rows and denser instructional/worked-example content for continuous background learning. Raw full-text Wikipedia remains useful reference material but is no longer the best default stream. | `open_textbooks` (`izumi-lab/open-text-books`) plus S2ORC ArXiv abstracts and FineWeb-Edu in the explicit Terminus Source Bank. | Revisit through structured Wikipedia only after Runtime Sources owns a section/table-aware adapter and bounded sampling policy. |
| Hot-path dead-column auto-revival | retired | Routine competitive-column processing scanned all stale counters and could structurally revive columns during every tick, adding CUDA synchronization/scan cost and hiding structural mutation inside routing. | Hot-path ticks record stale counters and spike-health evidence only; explicit deep-sleep/maintenance paths call `force_revive_dead_columns` when revival is warranted. | Revisit only with a fused/event-driven maintenance mechanism that beats the current benchmark and preserves reversible mutation evidence. |
| Zero-pressure slow-memory replay after global score scan | retired | Sleep replay could fall back from a bucket-scoped attempt to the global slow-memory scorer and still rehearse an arbitrary entry when every replay score was zero. The 2026-06-17 bounded replay-window benchmark showed this path did not pass reconstruction quality and only added replay work without positive tag/PRP/consolidation pressure. | `DualMemoryStore.select_replay_window` now returns an empty `bounded_replay_window_selection.v1` report with `fallback_reason=no_positive_global_scores`; deep sleep records any bounded bucket attempt and applies no replay updates unless positive pressure exists. | Revisit only if a bounded quality benchmark proves zero-score replay improves reconstruction/grounding and a long hot-path run keeps throughput at or above the current 6k-ish band without live-tick scans. |
| Unanchored deep-sleep global replay mutation | retired | Clearing or missing column anchors let deep sleep select from the global slow-memory scorer and mutate replay state even though the replay window was not local to a column or anchored bucket. The 2026-06-17 benchmark showed the global-control arm did not pass recall or prototype quality, and after retirement it applies `0` updates with `global_fallback_blocked_reason=no_anchor_bucket_scope_for_deep_replay`. | Production deep sleep now passes an empty bucket scope when no anchors exist, records `unscoped_global_fallback_retired=true`, and refuses replay mutation. Explicit global scoring can still exist as a diagnostic slow-path surface, not a production deep-sleep mutation fallback. | Revisit only if a bounded selection policy replaces the missing anchor scope and passes recall plus prediction/grounding/reconstruction gates without full-memory live-tick scans or throughput regression. |
| Stored-bucket modulated deep replay mutation | retired | Positive-pressure deep sleep used the stored bucket as a forced winner and called the live competitive `process()` plasticity path. On the bounded replay-window benchmark, that path applied `64` updates but worsened Task-A reconstruction from `0.0058662693` after Task B to `0.0063572129` after consolidation, failing recovery and degradation gates. It also collapsed colliding replay traces into the same prototype instead of measuring whether a commit helped the selected replay window. | Deep replay now uses `bounded_reconstruction_gated_candidate_repair`: selected traces are de-duplicated, candidate columns are bounded route candidates plus explicit stored-bucket fallback candidates, and prototype repair commits only when local replay-window reconstruction improves. The replacement report at `reports/bounded_replay_window_20260617/synthetic-selection-candidate-repair.json` applies `6` commits, rejects `14`, passes recall and prototype gates, and keeps zero-pressure/no-anchor arms at `0` updates. | Revisit only if a replacement commit rule beats the candidate-repair quality gate, reports candidate/quality budgets, avoids full-memory live-tick work, and a long hot-path run preserves or beats the current `6306.507 tokens/sec` 262144-token evidence. |
| Unanchored emergency repair replay mutation | retired | Emergency `"repair"` sleep still used the global repair scorer when no column anchors existed. That let a drift-triggered slow path mutate prototypes from unscoped archival entries even though production deep replay had already retired unanchored global mutation. | `_sleep_replay_candidate_bucket_ids()` now scopes both `deep` and `repair` modes to column anchors. Anchored repair reports `bounded_repair_reanchor`; no-anchor repair records `global_fallback_blocked_reason=no_anchor_bucket_scope_for_repair_replay`, applies `0` updates, and leaves prototypes unchanged. Focused tests cover both paths, and the current-tree 131072-token hot-path rerun stayed in band at `6252.073 tokens/sec`. | Revisit only if emergency repair gets a bounded selection policy with quality evidence, no hidden global scorer, explicit Runtime Truth, and long-run hot-path evidence at or above the current 6k-ish band. |
| Unanchored micro-sleep global maintenance refresh | retired | Micro sleep still used the unscoped maintenance scorer and then called `CompetitiveColumnLayer.process(...)` with zero learning rates before refreshing CPU metadata. That left a full-memory slow scorer available to a cadenced tick and kept old replay machinery around even though micro refresh had no weight/plasticity quality gate. | `_sleep_replay_candidate_bucket_ids()` now scopes `micro`, `deep`, and `repair` modes to column anchors. Anchored micro refresh reports `bounded_micro_maintenance_refresh`, selects through `bucket_indexed_candidate_window`, bypasses `competitive.process(...)`, and updates CPU memory metadata only. No-anchor micro refresh records `global_fallback_blocked_reason=no_anchor_bucket_scope_for_micro_replay`, applies `0` updates, and leaves replay counters/tags/prototypes/input weights unchanged. The 262144-token hot-path rerun stayed in band at `6332.439 tokens/sec`. | Revisit only if micro maintenance gets an explicit bounded selection policy with replay-quality evidence, no hidden global score scan, and repeated long-run throughput at or above the current 6k-ish band. |
| Unguarded HF replay-consolidation commit | retired implementation shape | The HF-backed consolidation runner initially proved bounded stored-experience recall, but the broader reconstruction gate could still fail after replay commits because acceptance was based on the selected replay-window repair metric rather than Task-A reconstruction. That made a slow-path replay update useful locally while harmful to the benchmark target. | `memory_consolidation_runner` now runs boundary and post-Task-B replay through `reconstruction_guarded_replay_consolidation.v1`: each cycle snapshots model and memory state, attempts the bounded anchor-window replay, measures Task-A `mean_reconstruction_error`, commits only non-regressing cycles, and rolls back rejected cycles. The guarded HF report rejected `9/9` attempted repair updates, kept effective updates at `0`, passed stored-experience recall, and passed the memory-consolidation gate. | Revisit unguarded commits only if a stronger target-level quality metric proves replay improves prediction/grounding/reconstruction and the 262144-token hot-path gate remains at or above the current 6k-ish band without live-tick replay work. |
| Repeating identical rejected replay cycles inside one guarded window | retired implementation shape | After a rejected reconstruction-guarded replay cycle, the runner restored model and memory state. Retrying the same bounded selection in the same slow window repeated the same local repair and regression: the previous HF guarded report attempted `9` repairs over `3` cycles and rejected all `9`, costing about `1003.442 ms` of guard latency with no accepted update. | `reconstruction_guarded_replay_consolidation.v1` now records `cadence_strategy=skip_repeated_rejected_selection`. Once a bounded selection is rejected and rolled back, repeated cycles in that unchanged slow window are skipped and reported with `sleep_replay_rollback_reason=repeated_rejected_selection_skipped`. The cadenced HF report attempted `3` repairs, skipped `2` repeated rejected cycles, kept effective updates at `0`, and reduced guard latency to `559.694 ms`; the accepted 262144-token rerun stayed in band at `6199.988 tokens/sec`. | Revisit repeated retries only if new state, new anchors, or a different replay-strength/search policy changes the selection before retry and quality evidence proves a second attempt can improve prediction/grounding/reconstruction. |
| Low-tail replay-strength schedules as defaults | rejected experiment | The previous HF default `[0.1, 0.05, 0.02, 0.01]` accepted useful repairs but paid four trial strengths per cycle and rejected `4` post-Task-B attempts. The broader synthetic low-first escalation `[0.1, 0.05, 0.02, 0.01, 0.5, 1.0]` passed the prototype stress target, but the low-tail entries doubled slow-window latency without improving the measured recovery versus compact escalation. Keeping those tails as defaults leaves old implementation cost around without target-specific evidence. | HF consolidation presets now use `[0.1]` with `repair_strength_trial_budget=1`; `reports/bounded_replay_window_20260617/hf-recall-target-strength-budget-single-010-promoted/summary.json` accepted `6` post-Task-B repairs, rejected `0`, improved Task-A reconstruction from `0.0170305534` to `0.0149637708`, and cut post-B guard latency to `1040.506 ms` while preserving exact recall and the consolidation gate. The synthetic benchmark default now uses compact escalation `[0.1, 0.5, 1.0]`; `reports/bounded_replay_window_20260617/synthetic-target-strength-budget-compact-default.json` passes recall and prototype gates with `repair_strength_trial_budget=3` and `2585.941 ms` latency versus `5074.171 ms` for the full escalation control. | Revisit low-tail or stronger shared schedules only with target-specific quality evidence that they improve prediction/grounding/reconstruction beyond the compact budgets and a clean long-run hot-path check remains in the maintained 6k-ish band without moving replay into the live tick. |
| Raw-window eligibility trace inside sleep replay | retired implementation shape | The maintained deep replay path already used bounded candidate repair, and repair mode returned before the old `raw_window`-derived eligibility trace branch could run. Keeping that dead branch made the sleep replay boundary ambiguous and left a path that looked like hidden replay-text reasoning. | `DualMemoryStore.replay_entry(..., include_text_payload=False)` lets sleep replay load tensor payloads only. Deep candidate repair and anchored repair request entries without `raw_window`, `text`, or metadata payloads. Runtime Truth records `raw_text_payload_loaded=false`, `sleep_replay_text_payload_loaded=false`, `sleep_replay_language_reasoning=false`, and `sleep_replay_text_payload_policy=sleep_replay_uses_tensor_payloads_only`. The synthetic boundary report at `reports/bounded_replay_window_20260617/synthetic-replay-tensor-payload-boundary.json` keeps bounded recall/prototype gates passing, and the 262144-token hot-path check stayed in band at `6237.420 tokens/sec`. | Revisit text payload use in replay only for an explicit, non-language-reasoning operator with bounded input length, quality improvement, visible latency/device budget, and long-run proof that it stays out of the live tick. |
| Unconditional dense input assembly inside repair replay | retired implementation shape | Anchored repair replay already selected a bounded replay window with stored routing keys, but still called `assembly_from_input(...)` for every selected replay input before repair. That scored all columns after selection and kept a dense slow-window preparation path that would scale poorly for larger memories and column counts. | Repair replay now uses `prepare_input_for_candidate_routing(...)` when a selected entry has a stored routing key, clears stale dense caches when no input trace exists, and reports `sleep_replay_unconditional_dense_input_assembly_retired=true`, dense fallback count, bounded input-prepare count, and stored-routing-key count. `reports/bounded_replay_window_20260618/sleep-repair-replay-bounded-input-prepare.json` selected and repaired `32/32` anchored entries, improved mean anchor distance by `0.148684`, reduced selected input-prep latency from `61.351 ms` to `32.613 ms`, and made `0` dense assembly calls during repair; the paired 524288-token hot-path run stayed in band at `6302.207 tokens/sec` with no observed contention. | Revisit only for legacy entries without stored routing keys, and only with an explicit fallback report, quality target, device placement, and long-run proof that dense repair preparation remains outside the live tick. |
| Retained replay-context scan in SNN replay-priority queue | retired implementation shape | `snn_replay_consolidation_priority_queue.v1` capped returned candidates but still verified every retained Replay Controller evaluation context before ranking. Today that retention is small, but the policy would scale as "verify the whole context history" and left old scan-shaped code beside due-cycle replay selection. | Replay Controller now keeps a replay-evaluation-context ID index and builds `bounded_snn_replay_priority_source_window.v1` from a `16`-entry recent context window plus up to `16` readout-target context IDs. The queue reports source/verified counts, CPU archival/score placement, no global candidate/score scan, no raw text payload, no language reasoning, `runs_live_tick=false`, and `gpu_used=false`; Runtime Truth carries the due-cycle queue source-window policy/counts. `reports/bounded_replay_window_20260618/snn-replay-priority-source-window.json` selected an old readout-targeted context outside the recent window while verifying `17` instead of `64` contexts at `1.825268 ms` mean and `0.0 MiB` CUDA allocation; the 524288-token hot-path run stayed in band at `6298.310 tokens/sec`. | Revisit only if a stronger indexed priority selector improves replay-context grounding or prediction/reconstruction targets, reports an explicit source-window budget and device placement, and repeated long-run evidence proves no live-tick cost or GPU-resident archival metadata. |
| Full-buffer SFA correction sample after deep replay | retired implementation shape | Deep sleep with an abstraction layer sampled SFA correction examples by drawing from the whole slow buffer after replay. That is slow-path rather than live tick, but it is still unselected archival memory work and does not scale to future memory. | `DualMemoryStore.sample_for_sfa_with_report(...)` samples only selected replay-window indices and emits `bounded_sfa_sample.v1`. Deep sleep embeds that report while preserving `sleep_replay_sfa_correction_scope=selected_replay_window`, sample/candidate counts, CPU archival/sample placement, no global candidate scan, and no language reasoning. `reports/bounded_replay_window_20260618/sfa-sample-bounded-window.json` improved selected-window sample purity from `0.00439453125` to `1.0` and mean latency from `1.451 ms` to `0.656 ms` over a `65536`-entry archive. | Revisit full-buffer SFA only if an explicit slow-window benchmark proves global abstraction correction improves reconstruction/grounding beyond selected-window SFA and reports a bounded memory budget for future large archival storage. |
| Unreported replay and SFA helper APIs | retired and removed | `sample_replay_indices(...)` and `sample_for_sfa(...)` still let callers receive replay/SFA samples while discarding the bounded selection or SFA report. Even with safer defaults, list-only helper names preserved an attractive unbounded replay shape for future callers. | `sample_replay_indices(...)` is deleted; callers must use `select_replay_window(...)`. `sample_for_sfa(...)` is deleted; callers must use `sample_for_sfa_with_report(...)`. Focused tests assert both old helpers are absent, deep sleep consumes the reported sampler, and checkpoint reload preserves `bounded_sfa_sample.v1`. The accepted `524288`-token protection run stayed in band at `6127.490 tokens/sec`, with bounded `12/65536` route rows, GPU memory `1840->1861 MiB`, no observed contention, and zero graph/native/sequence failures. | Revisit only as an external compatibility adapter that carries the report with the return value and cannot run in production replay without explicit candidate windows. |
| Full-buffer replay priority score helper | retired implementation shape | `DualMemoryStore.replay_scores(...)` remained as a public helper that computed replay priority for every slow-memory entry, even after production replay, SFA, and query readout moved to bounded candidate windows. It was test-only, but keeping it around preserved an attractive archive-wide scorer. | The method is removed. Tests and query readout now use `replay_scores_for_indices(...)` with explicit candidate indices, preserving the ranking formula inside selected windows only. The synthetic report `reports/bounded_replay_window_20260617/synthetic-replay-score-helper-retired.json` kept recall/prototype gates passing, and the 262144-token active-pressure run stayed in band at `6211.859 tokens/sec` with bounded `12/65536` route rows, flat `1852 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | Revisit only as an explicit offline diagnostic with a named memory budget, diagnostic global-scan report, quality target, device placement, and repeated long-run proof that no archive-wide scorer enters the live tick. |
| Full-buffer replay score tensor helper family | retired implementation shape | After the public priority helper was deleted, `maintenance_scores(...)`, `consolidation_scores(...)`, `repair_scores(...)`, `fragility_scores(...)`, and unused capture/tag/PRP tensor helpers still exposed archive-wide score tensors. They were mostly convenience helpers, but they preserved old full-buffer implementation code beside the bounded selector. | The methods are removed. Production selection scores only the bounded candidate window through `_score_replay_index(...)`; the explicit global diagnostic branch now scores privately inside `select_replay_window(..., allow_global_score_scan=true)` and ignores zero-score entries. The synthetic report `reports/bounded_replay_window_20260617/synthetic-score-tensor-helpers-retired.json` kept recall/prototype gates passing with `2` bounded updates and `0` global fallback cycles, and the accepted 262144-token active-pressure rerun stayed in band at `6151.952 tokens/sec` with bounded `12/65536` route rows, flat `1805 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | Revisit only as an explicit offline diagnostic with a named memory budget, no public production helper API, quality target, device placement, and repeated long-run proof that archive-wide tensors cannot enter the live tick. |
| Uncapped hot-bucket replay scoring and unscoped random replay default | retired implementation shape | Bucket-scoped replay avoided a global memory scan, but a hot anchor bucket could still pass every attached entry into the replay scorer before ranking. Unscoped random replay also remained an attractive full-buffer candidate scan when called without anchors. Both shapes scale poorly for future LLM-size memory and blur the selected-window contract. | `DualMemoryStore` now stores bucket entry indices in recency order and collects a recent round-robin candidate pool across anchor buckets before scoring. `bounded_replay_window_selection.v1` reports `candidate_window_policy=recent_bucket_round_robin_candidate_pool`, `candidate_window_limit`, `candidate_index_available_count`, `candidate_index_count`, `score_count`, and global scan flags. The focused cap test proves `10` available hot-bucket entries are cut to `4` scored entries before ranking, and unscoped random replay returns an empty retired report unless `allow_global_score_scan=true` marks a diagnostic global candidate scan. The synthetic replay benchmark kept recall/prototype gates passing, and the 262144-token active-pressure run stayed in band at `6148.125 tokens/sec` with bounded `12/65536` route rows, `65526` cached transition rows, flat `1848 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | Revisit only if a larger indexed replay design beats the capped round-robin policy on prediction/grounding/reconstruction, reports an explicit candidate memory budget, keeps archival metadata CPU-resident unless active replay computation benefits from CUDA, and proves repeated long-run live-tick throughput remains in band without full-memory scans. |
| Linear HF replay query collection over `slow_bucket_ids` | retired implementation shape | The HF replay recall runner collected Task-A anchor queries by walking `store.slow_bucket_ids` until `max_queries`. That was slow-path, not live tick, but it kept the old archive-linear scan shape beside the new bounded replay selector and would scale poorly for future LLM-size memory. | `DualMemoryStore.collect_replay_query_indices(...)` now emits `bounded_replay_query_collection.v1`, collects recent bucket-indexed query indices up to `max_queries`, requires input patterns by default, reports available/collected counts, `score_count=0`, no global score/candidate scan, CPU archival placement, and `runs_live_tick=false`. The HF report `reports/bounded_replay_window_20260617/hf-recall-capped-query-collection/summary.json` kept recall and consolidation gates passing with `3` bounded queries, and the 262144-token active-pressure run stayed in band at `6221.949 tokens/sec` with bounded `12/65536` route rows, flat `1848 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | Revisit only if query collection moves to a stronger indexed selector with explicit memory budget, quality improvement, no global archive walk, and repeated long-run proof that the live tick remains protected. |
| Full slow-memory query readout scan | retired implementation shape | `query_runner.memory_matches(...)` computed similarity, replay priority, and semantic term evidence across every slow-memory entry for explicit query readout. That is not the live tick, but it is still an archive-wide recall shape that does not scale to future LLM-size memory and can look like hidden memory reasoning. | `query_runner.memory_matches_with_report(...)` now emits `bounded_query_memory_match.v1`: routing supplies candidate bucket ids, `DualMemoryStore.collect_query_memory_match_indices(...)` returns a capped bucket-indexed memory window, and query readout scores only those candidate entries. The query report `reports/bounded_replay_window_20260617/query-memory-match-bounded-window.json` used `candidate_window_limit=192`, scored `1` candidate, returned `1` memory match, reported no global score/candidate scan, and kept archival placement on CPU with `runs_live_tick=false`; the 262144-token active-pressure run stayed in band at `6137.185 tokens/sec`, flat `1848 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | Revisit only if an explicit offline diagnostic or stronger indexed recall mechanism reports its memory budget, quality target, device placement, and repeated long-run proof that no archive-wide scan enters the live tick. |
| Report-dropping context query-memory wrapper | retired and removed | After query readout became bounded, `query_runner.memory_matches(...)` still existed as a compatibility wrapper that discarded `bounded_query_memory_match.v1`. Context comparison used that wrapper, so a future caller could get memory matches without a visible candidate budget, device placement, global-scan flags, or cadence boundary. | The wrapper is deleted. `build_context_comparison(...)` calls `memory_matches_with_report(...)` for each context, shares one returned replay-entry cache, returns per-context reports, and emits `bounded_context_comparison_memory_match.v1`. `reports/bounded_replay_window_20260618/context-memory-match-bounded.json` preserved selected-index parity for both contexts, reduced raw payload reads from `16` to `8` with `8` cache hits, and reduced mean latency from `71.927 ms` to `70.550 ms`; the 524288-token protection run stayed in band at `6065.987 tokens/sec`, with bounded `12/65536` route rows, GPU memory `1839->1845 MiB`, no observed contention, and zero graph/native/sequence failures. | Revisit only if an external API genuinely needs a list-only adapter; it must be outside production recall, clearly diagnostic, and must not hide the bounded recall report from callers or docs. |
| Eager query-readout text payload construction | retired implementation shape | Similarity-only query readout used the bounded candidate window, but still built replay text payloads and `top_chars` for every candidate before sorting. That kept unnecessary text expansion inside explicit readout and scaled poorly for future LLM-size memory. | `query_runner.memory_matches_with_report(...)` now keeps similarity-only readout tensor-first, materializes replay text only for returned matches, and reports `raw_text_payload_policy=returned_similarity_matches_only`, `raw_text_payload_count`, and `language_reasoning=false`. `reports/bounded_replay_window_20260617/query-memory-payload-returned-only.json` preserved selected indices, reduced raw text payload loads from `192` to `5`, and reduced mean latency from `33.612 ms` to `25.881 ms`; the 524288-token active-pressure run stayed in band at `6152.079 tokens/sec`, flat GPU memory (`1874->1878 MiB`), no observed contention, and zero graph/native/sequence failures. | Revisit eager candidate text payload construction only if a query/focus ranking path explicitly needs candidate-window text, reports the bounded payload policy, and proves quality and long-run throughput remain protected. |
| Full slow-memory concept-frontier metric scan | retired implementation shape | `autonomy_runner.concept_frontier_metrics(...)` computed acquisition novelty, uncertainty, and support by iterating every stored `slow_routing_keys` entry. That is slow-path planning, not live tick, but it was still archive-wide recall adjacent to source acquisition and would scale poorly for future LLM-size memory. | `concept_frontier_metrics_with_report(...)` now derives routing-index candidate buckets from the probe-bank signature, calls `DualMemoryStore.collect_query_memory_match_indices(...)`, and emits `bounded_concept_frontier_memory_metrics.v1` while scoring only the capped candidate window. The benchmark `reports/bounded_replay_window_20260617/concept-frontier-bounded-scope.json` preserved the diagnostic full-scan top-1 and frontier metrics while scoring `64/8192` entries at `5.040 ms` mean versus `658.116 ms`; the 262144-token active-pressure run stayed in band at `6148.846 tokens/sec`, flat `1805 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | Revisit only if a stronger indexed frontier metric improves acquisition quality, reports memory budget and device placement, and repeated long-run evidence proves it remains outside the live tick without hidden archive scans. |
| Concept-frontier metrics report-dropping wrapper | retired and removed | After concept-frontier scoring moved to `bounded_concept_frontier_memory_metrics.v1`, the compatibility `concept_frontier_metrics(...)` wrapper still returned novelty/uncertainty/support while discarding the report. It was unreferenced, but keeping it invited source-acquisition callers to hide candidate budgets, device placement, and scan flags. | The wrapper is deleted. Callers use `concept_frontier_metrics_with_report(...)`, and focused tests assert the report-dropping wrapper is absent while the bounded metric report still exposes selected indices, candidate counts, no global scans, CPU archival placement, and `runs_live_tick=false`. | Revisit only if a public API requires a shorthand adapter that returns or attaches the bounded report and cannot be used inside production source selection without evidence fields. |
| Unreported source-bank per-probe duplicate replay payloads | retired implementation shape | Source-bank gap planning aggregated per-probe query-memory matches without a bank-level Runtime Truth report and could reload the same returned replay text payload for every sampled probe. That kept work bounded per probe, but left the bank-level candidate budget, duplicate payload cost, device placement, and no-live-tick boundary implicit. | `bank_memory_matches_with_report(...)` now emits `bounded_source_bank_memory_match.v1`: source-bank probes are sampled, each probe delegates to `bounded_query_memory_match.v1`, returned replay-entry payloads are shared across probes, and the report records candidate totals, unique candidate count, raw text payload loads/cache hits, CPU archival/score placement, no global scans, `runs_live_tick=false`, and `language_reasoning=false`. `reports/bounded_replay_window_20260618/source-bank-memory-match-bounded.json` preserved selected indices, reduced raw payload loads from `32` to `4`, and improved mean latency from `194.259 ms` to `179.366 ms`; the 524288-token protection rerun stayed in band at `6524.395 tokens/sec`, with bounded `12/65536` route rows, GPU memory `1833->1798 MiB`, no observed contention, and zero graph/native/sequence failures. | Revisit per-probe payload reloads only as an explicit diagnostic baseline. Production source-bank recall must keep a bank-level bounded report, shared returned-payload policy, device placement, quality target, and repeated long-run proof that no archive-wide or every-token work enters the live tick. |
| Source-bank memory list wrapper | retired and removed | After source-bank recall emitted `bounded_source_bank_memory_match.v1`, `bank_memory_matches(...)` still called the reported path and discarded the report before returning matches. That preserved a tempting report-dropping API beside the maintained source-bank gap plan. | `bank_memory_matches(...)` is deleted and removed from `frontier.__all__`. Callers use `bank_memory_matches_with_report(...)` or `bank_gap_plan(...)`; focused tests assert the list-only wrapper is absent. | Revisit only if a public API requires it and the adapter returns or attaches the bounded report instead of hiding candidate budgets, device placement, global-scan flags, or no-live-tick evidence. |
| Query memory episode list helper | retired and removed | `build_memory_episodes(...)` converted returned query memory matches into text episodes without carrying a separate bounded readout report. It did not scan the archive, but it let explicit replay-text readout look like an unmeasured reasoning surface. | `build_memory_episodes(...)` is deleted. Query result construction uses `build_memory_episodes_with_report(...)` and returns `bounded_query_memory_episode_readout.v1`, including selected match count, neighbor radius, direct neighbor-window payload budget, CPU placement, no global scans, no live tick, no every-token work, and no language reasoning. `reports/bounded_replay_window_20260618/query-episode-readout-bounded.json` recovered the target top episode from four selected fragments over a `65536`-entry archive while keeping the neighbor read budget explicit; the paired 524288-token protection run stayed in band at `6219.926 tokens/sec` with bounded `12/65536` route rows and zero graph/native/sequence failures. | Revisit only if a public API requires a text-only adapter; it must return or attach the readout report and may only expand text through selected-memory evidence windows. |
| Mixed-provenance query episode stitching | retired implementation shape | After bounded source episodes were admitted beside cadence fragments, index-adjacent slow-memory entries no longer implied source-adjacent text. The previous neighbor stitcher could concatenate cadence fragments with source-episode raw windows and produce false evidence such as cross-sentence animal fragments. | `build_memory_episodes_with_report(...)` now treats complete `explicit_feed_source_episode` entries as whole bounded episodes, filters zero-support episodes when query-supported evidence exists, and only stitches raw neighbor windows when character overlap proves adjacency. Focused tests assert admitted source text is preserved and source-admission boundaries are not crossed. The source-admission benchmark improved simple-animals grounding from `0.25` to `1.0`, and the 524288-token protection run stayed in band at `6702.362 tokens/sec`. | Revisit only with source-span metadata or a stronger provenance graph that proves adjacency without relying on memory index order, plus quality and hot-path evidence. |
| Dense source-admission assembly after source selection | retired implementation shape | The first bounded source-admission pass selected at most `32` source episodes, but then called `assembly_for_pattern(...)` from the admission path, preserving a dense all-column assembly helper after selection. That is explicit slow-path work, not live tick, but it scales poorly for future LLM-size source feeds and weakens the bounded-recall contract. | Source admission now obtains winner, assembly, and routing key from one bounded offline competition and records `assembly_policy=bounded_offline_competition_winner_assembly` plus `dense_source_admission_assembly_retired=true`. `reports/bounded_replay_window_20260618/source-episode-admission-bounded-v2.json` kept grounding at `0.25 -> 1.0`, admitted `5/5` selected episodes, reported `2725.253 ms` admission latency, kept archival tensors on CPU, and used `cuda:0` only for active assembly computation. The paired 524288-token protection run stayed in band at `6412.209 tokens/sec` with bounded `12/65536` route rows, `65526` cached transition rows, and zero runtime failures. | Revisit only with a lower-level source encoder or assembly path that reports candidate budget, device placement, quality, and long-run hot-path protection without restoring dense all-column source-admission work. |
| Service-owned direct archive lookup for runtime concept observation | retired implementation shape | `OperatorInteractionRuntime._observe_runtime_concept_batch_locked(...)` directly read `slow_routing_keys`, `slow_texts`, `slow_raw_windows`, and STC metadata to build concept matches from train-step `memory_index` evidence. It did not scan the archive, but service-owned archive reads left the memory budget, duplicate payload cost, device placement, and cadence boundary unreported. | Runtime concept observation now calls `DualMemoryStore.resolve_runtime_concept_memory_matches(...)`, which accepts only explicit train-step memory indices, caps the observation batch, caches duplicate payloads, emits `bounded_runtime_concept_memory_lookup.v1`, and persists the report through summaries, device reports, and checkpoints. `reports/bounded_replay_window_20260618/runtime-concept-memory-lookup-bounded.json` preserved selected-index parity, reduced raw payload reads from `512` to `64`, recorded `448` cache hits, and cut mean lookup latency from `47.156 ms` to `6.380 ms`; the 524288-token protection run stayed in band at `6237.075 tokens/sec`, with `concept_observation=0.000474 ms/token`, bounded `12/65536` route rows, GPU memory `1809->1861 MiB`, no observed contention, and zero graph/native/sequence failures. | Revisit service-side archive access only if a new memory-store API cannot express the evidence window and a clean long-run benchmark proves the replacement keeps `runs_every_token=false`, no global scans, explicit device placement, quality parity, and protected throughput. |
| ConceptStore archive-materializing signature lookup | retired implementation shape | `ConceptStore._memory_signature(...)` accepted already-selected memory indices but then materialized `slow_routing_keys`, `slow_input_patterns`, or `slow_buffer` with `list(...)` before direct indexing. That preserved an archive-shaped access pattern inside cadenced semantic observation and scaled poorly for future LLM-size memory. | ConceptStore observation now emits `bounded_concept_memory_signature_lookup.v1`: each evidence source is capped at `8` unique indices with a `32`-reference scan budget, archival arrays are direct-indexed on CPU, `archive_list_materialization_count=0`, and no global candidate/score scan or archival mutation is reported. `reports/bounded_replay_window_20260617/concept-signature-lookup-bounded.json` preserved diagnostic legacy signature quality (`min cosine=0.9999998212`) while reducing mean lookup latency from `12.490 ms` to `1.454 ms` over `65536` entries; the clean hot-path gate stayed in band at `6143.768 tokens/sec`, flat `1746 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | Revisit only if a new semantic lookup API keeps evidence-window selection explicit, reports memory budget/device placement/quality, and proves with clean long runs that no archive materialization or global recall enters the live tick. |
| Semantic frontier-gap full archive raw-window scan | retired implementation shape | `frontier_gap_plan(...)` reached through a helper that materialized `slow_raw_windows` and rebuilt side lists while ranking terms. That was slow-path planning, not live tick, but it preserved an archive-wide text-planning shape that could look like hidden memory reasoning and would scale poorly for future LLM-size memory. | `frontier_gap_plan(...)` now calls `DualMemoryStore.collect_frontier_gap_indices(...)`, scores only a capped CPU recency or bucket candidate window, and emits `bounded_frontier_gap_selection.v1` with no global candidate/score scan, raw text loaded only for selected candidates, CPU archival placement, `runs_live_tick=false`, and `language_reasoning=false`. `reports/bounded_replay_window_20260617/frontier-gap-bounded.json` preserved expected and diagnostic legacy terms with `quality.min=1.0`, scored `192/65536` entries, and reduced mean latency from `217.530 ms` to `9.073 ms`; the current 524288-token active-pressure run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-frontier-gap-collector-required.json` stayed in band at `6233.085 tokens/sec`, GPU memory `1844->1840 MiB`, no observed contention, and zero graph/native/sequence failures. | Revisit only if a stronger indexed semantic planner reports an explicit candidate budget, quality target, device placement, and repeated long-run proof that no archive-wide text scan or language replay enters the live tick. |
| Frontier-gap report-dropping terms helper and prefix fallback | retired and removed | `frontier_gap_terms(...)` called the bounded scorer but discarded `bounded_frontier_gap_selection.v1`, and `_frontier_candidate_report(...)` still had a compatibility path that read a bounded prefix from `slow_raw_windows` when the memory store lacked `collect_frontier_gap_indices(...)`. Both kept old implementation surfaces around the otherwise bounded planner. | `frontier_gap_terms(...)` is deleted. Callers use `frontier_gap_plan(...)` and receive terms plus the selection report. Missing bounded collectors now return an empty `bounded_frontier_gap_selection.v1` fallback with `fallback_reason=memory_store_missing_bounded_frontier_collector`, zero candidates, zero text payloads, no global scans, CPU archival placement, and no language reasoning. The refreshed `reports/bounded_replay_window_20260617/frontier-gap-bounded.json` includes this missing-collector gate and keeps the bounded quality/latency gate passing. | Revisit only if a public API requires a terms-only adapter; it must expose or carry the bounded report and must not read archive text without a collector-owned candidate window. |
| Full slow-memory timestamp/bucket scan for recent replay setup | retired implementation shape | Recent tagging walked archival timestamps and anchor capture walked recent memory/bucket state to prepare replay tags and column anchors. That was slow-window work rather than live tick, but it preserved an archive-linear setup shape beside the bounded replay selector and would scale poorly for future LLM-size memory. | `DualMemoryStore` now keeps a CPU recency index and emits `bounded_recent_memory_window.v1`. `tag_recent_entries(...)` emits `bounded_recent_memory_tag.v1` from a capped recent-index window, and `capture_recent_memory_anchors(...)` emits `bounded_recent_anchor_capture.v1` with `require_bucket=true`. The focused tests cap `10` entries to indices `[9, 8, 7]`; the synthetic report `reports/bounded_replay_window_20260617/synthetic-recent-anchor-window.json` kept recall/prototype gates passing with `candidate_window_limit=256`, `14` indexed entries, no global scans, CPU archival storage, and `runs_live_tick=false`; the 262144-token active-pressure run stayed in band at `6228.243 tokens/sec`, flat `1846 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | Revisit only if a stronger indexed setup policy improves prediction/grounding/reconstruction, reports an explicit memory budget and device placement, keeps archival metadata CPU-resident unless active replay computation benefits from CUDA, and repeated long-run evidence proves the live tick remains protected. |
| Coupling candidate homeostasis wake to dead-column threshold | retired | `dead_column_steps` mixed two different decisions: when structural maintenance may consider dead columns, and when threshold/win-rate homeostasis can stop updating every column. A same-code fused RTX 3060 A/B measured `64.8535` versus `47.4682 ticks/sec` after splitting the homeostasis gate. | `candidate_homeostasis_start_tokens` defaults to `512`; candidate-scoped threshold/win-rate updates can start earlier while stale counters, spike windows, and explicit dead-column maintenance keep their prior boundaries. | Revisit only if grounding/diversity evidence shows earlier candidate homeostasis harms cognition more than its measured metabolism gain. |
| Coupling candidate predictive-update wake to dead-column threshold | retired | `dead_column_steps` kept predictive error/confidence/failure-streak updates all-column until structural dead-column retirement. The 2026-06-15 CPU complete-step A/B preserved exact winners while scoped predictive update plus cached vote updated `10/2048`, cached `2038`, reported `runs_all_columns=false`, and improved mean `train_step` latency by `6.26%`. The promoted fused CUDA transition later kept predictive update/location bounded inside the route transition and reached `6141.078 tokens/sec` on the 131072-token gate. | `candidate_predictive_update_start_tokens` defaults to `512`; retained CPU and promoted fused CUDA predictive state wake on the routed candidate set while non-candidate prediction state remains cached. Dense CUDA predictive update remains only as a fallback when the fused route cannot truthfully run. | Revisit only if long-run diversity/grounding evidence shows earlier predictive-update scoping harms learning more than its measured metabolism gain. |
| Eager candidate-indexed CUDA predictive writeback | rejected experiment | The 2026-06-15 isolated 8192-column CUDA writeback benchmark updated only `10/8192` predictive rows and matched dense state on those candidate rows, but mean latency regressed from `3.0670796875 ms` dense writeback to `6.7971921875 ms`; a later three-arm rerun still measured eager indexing at `7.0080195312499995 ms` versus `3.080762890625 ms` dense. | Use the promoted fused in-place/graph transition so candidate predictive state updates inside the existing transition launch. If candidate homeostasis starts before predictive wake, the CUDA graph path uses `candidate_subset_dense_predictive` until `candidate_predictive_update_start_tokens`, then switches to the candidate predictive graph. | Do not restore eager PyTorch candidate indexing as a runtime path; future sparse CUDA work must use fused/custom CUDA/persistent-kernel ownership or a broader graph executor with parity and complete `train_step` evidence. |
| Disabling fused CUDA predictive transition when predictive gate is later than homeostasis gate | retired implementation shape | The old guard reported `fused_inplace_requires_predictive_gate_no_later_than_graph_candidate_gate`, which forced dense predictive fallback whenever candidate homeostasis could start earlier than predictive wake. That preserved truth but unnecessarily coupled two scheduler gates the domain vocabulary had already separated. | Persistent CUDA graph capture now has separate `candidate_subset_dense_predictive` and `candidate_subset` graph variants. Focused CUDA parity proves dense predictive updates stay all-column before the predictive gate and candidate predictive updates match retained state after the gate. | Revisit only if the split graph increases capture/startup cost enough to fail repeated complete-runtime evidence or if a lower-level executor replaces both graph variants with one device-owned conditional path. |
| Standalone candidate predictive writeback side path | rejected and removed | The fused Triton writeback was fast in isolation, but standalone wiring before `inplace_column_transition_cuda` added predictive materialization and runtime orchestration cost. A valid 1024-column direct A/B preserved winners and reported `runs_all_columns=false`, but complete step mean/median/p95 regressed from `8.607221875/7.88135/14.0249 ms` to `27.7788925/14.7495/118.536 ms`; the attempted 8192-column direct run hit Triton's tensor-size warmup limit and is not promotion evidence. The leftover standalone `candidate_predictive_writeback_cuda` helper and `predictive_transition_benchmark` writeback arm were removed from active code after the fused transition became the maintained boundary. | The replacement is the promoted fused in-place/graph transition, where candidate predictive state is updated inside the existing transition launch and Runtime Truth reports `candidate_predictive_transition_mode=fused_inplace`. Dense eager predictive transition remains only the explicit fallback/oracle inside active runtime code. | Revisit only if a future side-launch design can beat the fused in-transition path without host materialization tax, preserve state parity, and win repeated complete-runtime benchmarks. |
| Configurable `fused_eager`, `compiled`, or `legacy` predictive dense transition | rejected and removed | The isolated predictive-state benchmark showed torch-compiled dense transition speedups, but the maintained runtime moved predictive state into `ColumnTransitionRuntime` and fused in-place/graph transition ownership. Leaving `compiled`, `legacy`, or selectable `fused_eager` configurable meant new runs could bypass the promoted scheduler boundary, carry hidden compile/warmup/fallback behavior, or force dense CUDA predictive mutation after the in-place transition won the real-path evidence. The remaining internal `apply_dense_transition(..., transition_mode=...)` selector was later removed so `fused_eager` cannot survive as a hidden core mode. | `predictive_dense_transition_mode` is fixed to `inplace_triton`; checkpoints missing the field or carrying `fused_eager`, `compiled`, or `legacy` migrate to `inplace_triton`, even when revision-stamped. If in-place cannot start, trainer still falls back internally to the dense eager tensor fallback/oracle and reports `last_dense_transition_mode=dense_eager_fallback` with the concrete in-place fallback reason. | Revisit only if a new executor owns the same Runtime Truth, candidate gating, graph/cache lifecycle, parity tests, and 131072-token complete-runtime evidence as the promoted in-place path. |
| Selectable `tensor` or `fused_triton_text` route-vote modes after graph promotion | retired implementation shape | The graph route/vote/text-tick island had already won promotion evidence, but the config surface still let new runs or local checkpoints select tensor or fused-only route/vote paths. That kept a second production route boundary alive after `cuda_graph_text` became the promoted path. | `predictive_route_vote_mode` is fixed to `cuda_graph_text`; checkpoints missing the field or carrying retired `tensor`/`fused_triton_text` values migrate to the promoted mode even when revision-stamped. Tensor and fused-only route/vote remain only internal fallback/evaluation controls, surfaced with `route_vote_requested_mode_source`, not selectable production config. The 2026-06-16 131072-token default gate stayed in the 6k-ish band at `6014.550 tokens/sec` with `train_compute=0.136192 ms/token`, `route_vote_resolved_mode=cuda_graph_text`, `131072` route-vote executions, and zero graph/sequence/native failures, with GPU contention observed at the threshold. | Revisit only if repeated long CUDA runs show the promoted graph path harms throughput/quality and a new ADR restores a selector with Runtime Truth, fallback, parity, and 131072-token evidence; otherwise old modes are controls, not runtime choices. |
| Pairwise all-columns candidate membership inside `inplace_triton` transition warmup | retired implementation shape | The 8192-column real-path scheduler probe failed before mutation because warmup compiled `column_offsets[:, None] == candidate_ids[None, :]` for an all-columns candidate set, producing `67,108,864` elements and tripping Triton's `1,048,576` tensor limit. | All-columns candidate sets now compile as a direct column mask; candidate subsets keep bounded membership checks. Focused CUDA warmup tests cover the 8192-column shape. | Reintroduce a pairwise membership matrix only if Triton/kernel limits change and repeated real-path benchmarks prove it is faster than the direct mask, which is unlikely. |
| Dense startup graph/precompile for already candidate-gated scheduler checkpoints | retired implementation shape | A promoted checkpoint whose candidate gate is already due never needs the dense all-columns startup graph or dense transition warmup, but the runtime still paid capture/compile cost and kept old graph names alive. | `ColumnTransitionRuntime` precompiles only the routed candidate shape after the gate, and `CudaGraphRouteTransition` captures only `candidate_subset`; Runtime Truth reports `capture_graph_policy=candidate_subset_only_after_homeostasis_gate`. | Revisit only for checkpoints that start before the candidate gate; those must report the dense startup phase truthfully rather than pretending every tick is candidate-bounded. |
| Treating bounded awake count as bounded route cost | rejected evidence claim | The real 8192-column promoted run woke only `10` columns and stayed on CUDA, but throughput fell to `3564.222 tokens/sec` and `0.251487 ms/token` versus the 1024 control at `6108.728` and `0.133438`. Runtime Truth showed route-vote input rows still scaled from `1024` to `8192`, and the follow-up truth-surface run showed dense state transition still scaling from `1024` to `8192` columns. | Keep the bounded awake-mask claim, but record total route-score rows and state-transition column count separately. The next scheduler boundaries are sparse/GPU-owned route-candidate retrieval before vote/sleep filtering and lazy/sparse column-state transition, not another status projection. | Revisit only with long real-path evidence where total route input rows and state-transition cost stay bounded or neutral-or-better as columns scale. |
| Per-tick exact-cache route-vote as the promoted scaling boundary | superseded implementation shape | Exact-cache fused route-vote was graph eligible and correct, but at 8192 columns it scored `8192` rows every tick and dropped the 131072-token real path to `3564.222 tokens/sec` with `0.251487 ms/token` train compute. Keeping it as the scaling story would preserve a proportional total-column tax after the scheduler already chose a small awake set. | The promoted route-candidate-bank path uses the exact cache only for the explicit seed/oracle, then steady fused/graph ticks score the bounded bank plus fixed probe lane. The 8192 probe-lane gate at `reports/column_scheduler_20260616/route-bank-probe-lane-8192-131072-i32.json` reached `6141.234 tokens/sec`, `0.130664 ms/token` train compute, `route_input_rows_scored=12/8192`, `route_output_candidate_count=10`, `state_transition_cached_count=8182`, and zero graph/native/sequence failures. | Revisit per-tick exact-cache scoring only as a fallback/oracle with explicit `route_rows_run_all_columns=true`, or if repeated long evidence proves it is neutral-or-better than the bounded bank without hiding route rows. |
| Exact `torch_topk` pre-narrowing before fused route/vote | rejected evidence claim | Calling `routing_index.search_tensors()` before `core.fused_route_vote_cuda` looks like a route-candidate subset, but the current `torch_topk` implementation computes exact scores over the whole routing tensor before returning the top-k ids. Feeding that result into fused route/vote would either duplicate all-row scoring or hide it outside the route/vote truth surface, so it cannot satisfy the Thousand-Brains-style small-awake scheduler contract. | Runtime Truth now exposes `route_vote_scoring` from `ColumnTransitionRuntime`, including `route_input_rows_scored`, `route_rows_run_all_columns`, and `bounded_route_scoring`. The exact torch cache remains the seed/oracle; steady fused/graph route-vote uses the training-owned route candidate bank plus fixed probe lane. | Revisit only with a candidate router that owns capacity/fallback truth, recall or winner-parity gates, no hot-path all-column prepass, and long complete-runtime evidence against the 131072-token baseline. |
| Unwarmed route-candidate-bank exact seed | rejected implementation shape | The first route-bank 131072-token CUDA run proved `route_input_rows_scored=10` after seeding, but throughput fell to `5323.085 tokens/sec` and `train_compute=0.158101 ms/token` because the first exact seed triggered a measured full-cache Triton JIT (`train_compute.max=3859.036 ms`). This is a scheduler warmup bug, not the promoted steady path. | Warm both the exact full-cache seed route and the indexed route-bank kernel before measurement. The promoted warm-seed gate at `reports/column_scheduler_20260616/route-candidate-bank-warmseed-131072-i32.json` reached `6109.301 tokens/sec`, `train_compute=0.130536 ms/token`, `route_input_rows_scored=10`, `bounded_route_scoring=true`, and zero graph/native/sequence failures. | Revisit only if a future compiler/runtime makes seed JIT impossible in measured windows or a different seed path has complete-runtime evidence that is faster without hiding the exact seed. |
| Wider retained route bank as immediate live promotion | rejected and removed | The corrected 2026-06-17 quality gate separates retained bank capacity from awake candidates. On the restored 8192-column checkpoint, bank256/probe64 scored `320/8192` rows and reached top-1 `0.92578125` but winner `0.630859375`; bank512/probe128 scored `640/8192`, reached top-1 `1.0`, but winner only `0.826171875`; bank1024/probe256 scored `1280/8192` and passed offline quality with top-1 `1.0`, winner `0.97265625`, and worst miss `0`. A live CUDA attempt then added GPU-owned `1024+256` retained-bank refresh while waking only `10`, but the 65536-column 131072-token gate reached only `4656.790 tokens/sec` and `train_compute=0.186216 ms/token` against the retained k+2 path at `6156.500` and `0.130623`. A `sorted=False` top-k short check still measured only `4596.633` and `0.176464`. | Do not keep the failed branch as an opt-in or second path. `route_candidate_bank_size` remains absent from live config; `route_candidate_bank_quality_gate` may model wider retained banks as offline evidence, but the promoted runtime stays at `k_routing + 2` route rows with `indexed_route_bank_vote_device_refresh`. | Revisit only with a different fused/GPU-owned discovery router that keeps explicit route rows/cached-vote truth, avoids service decision logic and hot-path all-column prepasses, passes winner quality, and preserves or beats the 6k-ish long-run `train_compute`/throughput baseline. |
| Sleep-enabled synthetic checkpoints as route-scheduler scale evidence | rejected evidence shape | A 16384-column synthetic checkpoint initially left default micro/deep sleep intervals enabled. The 131072-token run still reported bounded route/state truth (`route_input_rows_scored=12/16384`, `state_transition_cached_count=16374`), but it reached only `2499` tokens before timeout, repeatedly hit `sleep_boundary` burst fallbacks, and measured `6.935 tokens/sec`. That is sleep-maintenance interruption evidence, not promoted scheduler scaling evidence. | `marulho.evaluation.promoted_scheduler_checkpoint` now builds restored route-bank checkpoints for scaling gates and sets `micro_sleep_interval_tokens=1e9` and `deep_sleep_interval_tokens=1e9`, matching the 8192 promoted checkpoint shape. The corrected 16384 run at `reports/column_scheduler_20260616/promoted-scheduler-16384-131072-i32.json` reached `6154.503 tokens/sec`, `train_compute=0.130874 ms/token`, `route_input_rows_scored=12/16384`, and `state_transition_cached_count=16374` with no graph/native/sequence failures. | Revisit sleep-enabled checkpoints only as explicit sleep/replay benchmarks, never as route-scheduler scale gates. |
| Exact route-bank seed after checkpoint restore | retired implementation shape | The promoted route bank bounded steady scoring, but restored checkpoints still started with no ready bank, so the first live route tick ran `exact_full_cache_score_seed_route_bank` and bypassed CUDA graph replay before returning to bounded rows. That was truthful but left a proportional restore/startup tax when the previous runtime already had a valid training-owned bank. | Checkpoints save ready route-bank IDs, probe cursor, and scored-since-refresh state. `ColumnTransitionRuntime` restores those IDs against the current routing tensor cache before graph capture, reports `checkpoint_restore_count` and `restore_reason`, and the first post-restore tick can stay on `training_owned_route_candidate_bank_plus_probe_lane` with `seed_count=0`. The 8192-column 131072-token restore run stayed in band at `6129.693 tokens/sec`, `train_compute=0.130218 ms/token`, and `route_input_rows_scored=12/8192`. | Revisit exact restore seeding only when the checkpoint lacks a valid bank snapshot or the routing cache no longer contains the saved IDs; such fallback must keep `route_rows_run_all_columns=true` and a concrete unbounded reason. |
| Host-side route-bank refresh after graph replay | retired implementation shape | The fixed probe lane originally refreshed the next route-bank positions through Python-side candidate-to-route-position indexing and probe-position tensor copies at graph/burst quantum boundaries. That preserved bounded route rows, but it left a host-owned scheduler maintenance step after a graph-owned route/vote decision. | The fused route/vote select kernel now writes selected route-cache positions back into the route bank and advances the two-row probe lane on device during steady graph/burst replay. Runtime Truth reports `route_vote_kernel_variant=indexed_route_bank_vote_device_refresh`, `refresh_owner=fused_route_vote_device`, `device_refresh_count`, `host_refresh_count`, and bounded `route_input_rows_scored`. The 32768-column 131072-token gate reached `6008.953 tokens/sec`, `train_compute=0.133379 ms/token`, `route_input_rows_scored=12/32768`, `state_transition_cached_count=32758`, and zero graph/native/sequence failures. | Revisit host refresh only as explicit seed/restore fallback, or if device refresh fails parity and the fallback truthfully reports host ownership without pretending the graph owns refresh. |
| Every-host-truth CUDA structural-review capture | rejected implementation shape | Capturing structural-review candidate evidence on every CUDA host-truth boundary made growth/prune continuation more immediate but added a small CPU materialization/sync to the graph hot path. The 8192-column 131072-token real-path run fell to `4788.708 tokens/sec`, `train_compute=0.180271 ms/token`, and `tick_duration_ms.p95=30.495` despite bounded route/state truth and no observed contention. | `ColumnStructuralReviewQueue` remains training-owned and checkpoint-backed, but CUDA graph bursts capture review evidence only on a slow `4096`-token cadence and mark intervening bursts as deferred. Retained CPU ticks can still update from the awake mask immediately. The cadenced 8192-column rerun reached `6312.338 tokens/sec` with `train_compute=0.129495 ms/token`, `route_input_rows_scored=12/8192`, and `state_transition_cached_count=8182`. | Revisit only with a GPU-owned structural-review reducer or event queue that preserves checkpoint/operator review semantics and beats the cadenced long real-path gate without hidden host syncs or all-column scans. |
| K-only self-refreshing route bank as quality-complete relevance scheduler | superseded execution shape, still rejected quality claim | The promoted k-only route bank was a real speed boundary, but the offline 8192-column quality gate showed default text quickly leaves the current bank. `reports/column_scheduler_20260616/route-candidate-bank-quality-8192-default-text-s512.json` matched exact top-1 only `0.009765625`, matched the simulated exact winner only `0.001953125`, had mean top-k overlap `0.02578125`, and missed exact top-1 for `134` consecutive ticks while steady bank scoring remained bounded at `10` rows. A 32-tick evaluation-only exact reseed improved top-1-in-bank to `0.486328125` but still failed winner parity, so hidden exact reseed is not a promotion. | The live scheduler now keeps the warm-seed route bank but scores `k_routing + 2` route rows through a fixed quantum-cadenced probe lane. The 8192-column longer run `reports/column_scheduler_20260616/route-bank-probe-lane-8192-131072-i32.json` stayed in band at `6141.234 tokens/sec`, `0.130664 ms/token`, `route_input_rows_scored=12/8192`, and `state_transition_runs_all_columns=false`. `route_candidate_bank_quality_gate` remains offline evidence; Runtime Truth must expose any exact full-cache fallback/reseed as unbounded work. | Revisit quality-complete route-bank claims only with quality/winner gates on real text plus 131072-token complete-runtime evidence showing bounded route rows, no hidden all-column scans, and neutral-or-better ms/token against the 6k-ish baseline. |
| Widening blind sequential probe rows as relevance-quality scheduler | rejected evidence claim | The promoted two-row probe lane preserved the 6k-ish path, but the updated quality gates showed blind sequential probes do not discover the moving exact route fast enough. On 8192 columns, probe2 q16 scored `12/8192` rows and matched exact top-1 only `0.017578125` with winner match `0.001953125`; probe64 q16 scored `74/8192` rows but reached only `0.140625` top-1 and `0.0078125` winner match; probe256 q16 scored `266/8192` rows but reached only `0.34765625` top-1 and `0.044921875` winner match. On the 32768 same-checkpoint diagnostic, per-token probe16 scored `26/32768` rows and barely moved top-1 (`0.279296875`) while winner stayed `0.046875`; probe64 scored `74/32768` rows and improved top-1 to `0.591796875`, but winner match still stayed `0.046875` and even the offline oracle-previous diagnostic reached only `0.455078125`. | Keep the fixed two-row probe lane as bounded route-cost truth only while the next router is designed. Discovery quality must come from a fused/GPU-owned candidate router, graph-ordered device refresh, or another bounded policy that uses relevance structure and preserves previous-winner/reference-frame continuity rather than a blind sequential sweep. | Revisit only with real-text quality gates and a 131072-token complete-runtime run showing bounded route rows, winner-quality pass, no hot-path all-column scan, no service ownership, and neutral-or-better ms/token against the 6k-ish baseline. |
| Column-ID hypercube neighbor lane as promoted route scheduler | rejected evidence claim | A bounded bit-flip neighbor probe was added to the offline quality gate to test whether column IDs already encode useful local routing topology. A toy local-bitflip fixture passed, proving the evaluator can recover locality when it exists, but the real 32768-column default-text checkpoint did not improve: probe2 plus hypercube16 scored only `25.215/32768` rows on average and stayed at exact top-1 `0.2734375`, exact-winner-in-bank `0.09375`, winner match `0.046875`, and oracle-previous diagnostic `0.08984375`. | Keep this as evaluation-only rejection evidence inside `route_candidate_bank_quality_gate`; the live scheduler remains the route bank plus fixed two-row probe lane with device refresh. Column ID adjacency is not routing locality evidence for the current checkpoint. | Revisit only if training creates explicit topographic column IDs or a learned topology, and the candidate router passes real-text winner-quality gates plus a 131072-token complete-runtime run without hidden all-column scans or service ownership. |
| Same-row graph-neighbor replacement for the two-row probe lane | rejected evidence claim | A graph-neighbor probe at the exact promoted row budget looked attractive because it could keep `12/8192` route rows: `neighbor_count=2`, `capacity_rows=12`, no sequential probe. The 8192 default-text diagnostic improved exact top-1 from the same-session probe2 rerun's `0.013671875` to `0.078125`, but exact winner match stayed `0.001953125`, exact-winner-in-bank stayed `0.001953125`, mean top-k overlap was only `0.0796875`, and worst top-1 miss streak was `63`. | Do not replace the live fixed probe lane with same-row graph neighbors. Keep graph-neighbor work in the offline quality gate until it improves winner/reference-frame quality, not only top-1 recall. | Revisit only with a bounded graph/ANN router that passes winner-quality gates on real text and then preserves or beats the 131072-token 6k-ish complete-runtime baseline without service ownership or hidden all-column scans. |
| Runtime route-neighbor graph expansion as promoted scheduler | rejected and removed | The corrected offline graph-neighbor gate was quality-promising: `neighbor_count=208`, `capacity_rows=1536`, mean scored rows `1481.068`, exact top-1 `0.994140625`, winner match `0.98828125`, mean overlap `0.9748046875`, and worst miss streak `1`. But the attempted live 8192-column runtime path scored about `1509/8192` route rows and the 131072-token run reached only `4682.167 tokens/sec` with `train_compute=0.183927 ms/token`, below the then-promoted k-only bank at `6110.715 tokens/sec` and `0.135007 ms/token`. Keeping an opt-in runtime graph-neighbor path would create a second non-promoted scheduler. | Keep graph-neighbor expansion only in `route_candidate_bank_quality_gate` as offline evidence. The live scheduler is the promoted warm-seed route bank plus fixed two-row quantum-cadenced probe lane until a fused/GPU-owned discovery router wins both quality and long-run throughput. | Revisit only with fixed-shape dedupe/mask or ANN discovery inside a fused GPU-owned route boundary, no hot-path all-column scan, service projection only, quality/winner gates on real text, and 131072-token evidence that preserves or beats the 6k-ish ms/token baseline. |
| Bounded graph-walk route discovery as promoted scheduler | rejected evidence claim | A CAGRA-style graph-walk probe was added to the offline quality gate and tested against the 8192-column default-text checkpoint. It improved exact top-1, but winner parity failed before the row budget became runtime-plausible: neighbor64/beam4/round4/cap512 scored `512/8192` rows with top-1 `0.896484375` and winner `0.263671875`; neighbor128/beam2/round6/cap1024 scored `1024/8192` rows with top-1 `1.0` and winner `0.73046875`; neighbor256/beam2/round6/cap1536 scored `1536/8192` rows with top-1 `1.0` and winner `0.767578125`. | Keep graph walk inside `route_candidate_bank_quality_gate` as evaluation-only evidence. The live scheduler remains the promoted route bank plus fixed two-row probe lane; top-1 recovery alone is not enough because the winner path depends on the supporting candidate set and previous-winner/location effects. | Revisit only as a fused/GPU-owned candidate router that passes winner-quality gates before runtime promotion and then preserves or beats the 131072-token 6k-ish baseline without service ownership or hidden all-column scans. |
| Landmark/random-projection bucket discovery as promoted route scheduler | rejected and removed | Cheap bounded-discovery indexes looked like plausible sparse-MoE or ANN gates, but the 8192-column default-text quality probe did not pass winner-quality gates. Landmark256/top8/bucket128 q16 scored `926.544/8192` route rows on average but matched exact top-1 only `0.77734375` and winner only `0.4296875`; random-projection512/top32/bucket64 q16 scored `1790.795/8192` rows and matched top-1 only `0.255859375`; per-token landmark512/top16/bucket128 reached top-1 `1.0` but still only `0.91015625` winner match while scoring `1642.932/8192` route rows plus `512` selector rows per tick. Keeping `route_candidate_discovery_probe` after rejection would leave a second non-promoted scheduler research path beside the live route bank. | Delete the landmark/projection probe module and tests; keep the measured rejection in this ledger and the benchmark note. The live scheduler remains the warm-seed route bank plus fixed two-row probe lane, while `route_candidate_bank_quality_gate` remains the only offline oracle for future bounded graph/ANN discovery work. The 32768-column cleanup validation `reports/column_scheduler_20260616/promoted-scheduler-32768-131072-i32-after-discovery-cleanup.json` stayed in-band at `6128.457 tokens/sec`, `train_compute=0.132636 ms/token`, `route_input_rows_scored=12/32768`, and `state_transition_cached_count=32758`. | Revisit only with fixed-shape fused GPU discovery, no hot-path all-column scan, service projection only, winner/top-1 pass, and 131072-token evidence that preserves or beats the 6k-ish ms/token baseline. |
| Graph-internal route-bank/probe refresh kernel | rejected and removed | A fused device-side bank/probe refresh was tested during the probe-lane promotion, but graph-vs-fused parity exposed that the captured graph refresh read the captured seed candidate rows instead of the route-vote result, resetting score positions and later diverging winners. Keeping that helper would leave a second, unproven scheduler refresh path. | Refresh bank/probe positions in `ColumnTransitionRuntime` at the fixed graph quantum boundary. This preserves fused sequential, graph sequential, and burst parity while keeping route scoring bounded at `k_routing + 2`. | Revisit only as a single fused route/vote/refresh kernel or proven graph-ordered device update with focused parity tests, burst parity, no service ownership, and a 131072-token run that preserves the 6k-ish baseline. |
| Legacy `turboquant_plus` as the scheduler route boundary | rejected and removed | Google Research TurboQuant and community implementations support compressed vector storage or approximate inner-product scoring, not a wake/sleep capacity scheduler by themselves. MARULHO's legacy backend was also not paper-faithful: it used per-prototype min/max codes, kept FP32 prototypes, applied a QJL-style correction, lacked `routing_tensor_cache()`, and scanned every route row. The 2026-06-15 CUDA audit measured `turboquant_plus` slower than `torch_topk` at 1024 (`3.280950` vs `1.034438 ms`) and 8192 (`5.502870` vs `0.822393 ms`), with exact top-1 recall `0.650` and `0.750` and `route_input_rows_scored` equal to total columns. | Delete the backend, config option, and tests; keep exact torch-cache routing as seed/oracle while the promoted steady CUDA route/vote path uses the bounded route candidate bank plus fixed probe lane. Use TurboQuant only as research/audit input unless a paper-faithful compressed scorer is embedded below a bounded GPU-owned candidate router. | Revisit only with exact or explicitly quality-gated winner parity, graph-cache or fused-kernel eligibility, no all-row scheduler tax, and long complete-runtime evidence against the 131072-token baseline. |
| Standalone pure-PyTorch `IVFRouter` as scheduler boundary | rejected and removed | The module was exported and tested but had no live trainer/config/Runtime Truth caller. It built per-query candidate lists with Python control, exposed only a benchmark helper, and did not own a wake/sleep capacity contract, tensor cache, fallback truth, or graph/fused-route eligibility. | Keep exact torch-cache routing as the maintained retrieval surface. Future IVF/RaBitQ work must be a new bounded GPU-owned candidate router, not a restored standalone prototype. | Revisit only with tensor/cache eligibility, explicit capacity and fallback fields, recall/winner-quality gates, no all-row wake tax, and long complete-runtime evidence against the 131072-token baseline. |
| Selectable CPU FAISS/HNSW and numpy exact-cosine routing backends | rejected and removed | After the CUDA route/vote graph became the promoted path, `auto`, `faiss_hnsw`, and `exact_cosine` preserved alternate routing modes that could not provide `routing_tensor_cache()`. Selecting them either bypassed the graph-cache scheduler path or forced fallback before the real route/vote boundary, while the default `auto` choice could silently differ by device and local FAISS installation. | `routing_index_mode` is no longer a live config field. `HierarchicalAssemblyIndex` is a torch-backed exact top-k index with tensor search over the same device cache, index constructors no longer accept backend selector arguments, and checkpoint load drops old `routing_index_mode` keys with `retired_routing_backend_config_surface` migration evidence. | Revisit only as a new bounded GPU-owned candidate router with capacity, fallback, recall/winner-quality gates, Runtime Truth, no all-row scheduler tax, and long complete-runtime evidence. |
| Disabling merged torch cache for sharded routing | retired and removed | The `merge_torch_routing_shards=False` config and benchmark switches kept a second exact per-shard tensor-search path after the promoted CUDA route/vote graph required one merged `routing_tensor_cache()`. The branch could benchmark historical per-shard merging, but it could not feed stable graph cache pointers or winner prototype updates in the true path. | Sharded routing still exists, but tensor search and fused/graph route-vote always use the merged torch cache. Checkpoint load drops old `merge_torch_routing_shards` keys and records `retired_non_promoted_sharded_route_cache_switch`. The 131072-token gate at `reports/column_scheduler_20260616/sharded-merge-cache-cleanup-131072-i32.json` stayed in band at `6169.616 tokens/sec`, `train_compute=0.131525 ms/token`, with zero graph/sequence/native failures and `state_transition_runs_all_columns=false`. | Revisit only if a new sharded GPU-owned candidate router can feed graph-compatible candidates/cache coherence without an all-row prepass and beats the 131072-token complete-runtime gate. |
| List-returning routing search and benchmark switches | retired and removed | The legacy `routing_index.search()` API materialized candidate ids through Python lists and CPU/numpy distance arrays. It survived only in query-detail projection and evaluation switches such as `routing_candidate_mode=list` or the old list-backed `candidate_source=routing_index` while the promoted trainer path already used tensor-native search. Keeping both surfaces made benchmarks compare a path that could not feed the promoted CUDA graph route/vote boundary. | Retrieval now exposes `search_tensors()` as the only live candidate search API. Query details convert the tensor result at the display edge, hot-window benchmarks no longer select list routing, and compiled hot-path benchmarks use `candidate_source=routing_index` for tensor-native candidates. The 131072-token gate at `reports/column_scheduler_20260616/routing-list-surface-cleanup-131072-i32.json` stayed in band at `6142.710 tokens/sec`, `train_compute=0.132356 ms/token`, with zero graph/sequence/native failures and `state_transition_runs_all_columns=false`. | Revisit only as an explicit offline export helper outside retrieval, never as a trainer/benchmark route source or scheduler boundary. |
| Treating candidate scoring as complete scheduler ownership | rejected evidence claim | Candidate-scoped scoring, cached vote, and fused route-vote are real execution effects, but the 2026-06-15 truth-surface reports still exposed `state_transition_runs_all_columns=true` with `state_transition_column_count=1024` and `8192`. A scheduler could not claim many-total/few-awake scaling while stale counters, spike windows, dense assembly, homeostasis buffers, or predictive transition state still mutated over every column. | Runtime Truth projects state-transition mode/count/cached/fallback beside awake counts. Retained CPU and candidate-subset CUDA graph/burst transition now keep state mutation bounded; remaining same-throughput scaling work is the route-score input-row boundary. | Revisit only if a future path hides dense state mutation behind awake-count evidence again; otherwise use the active state-transition truth fields and route-row scoring evidence. |
| Retained CPU dense stale-counter and spike-window transition under candidate scheduling | retired implementation shape | Candidate-scoped scoring/homeostasis still paid a retained CPU all-column `steps_since_win += 1` and dense recent-spike row write, making sleep/cached-vote truth incomplete whenever `state_transition_runs_all_columns=true`. The 2026-06-16 cleanup proved exact logical stale-age snapshots, checkpoint roundtrip, sparse spike-window overwrite correctness, and an 8192-column CPU A/B where scoped state transition stayed bounded at `10/8192` with exact winners and faster median/mean complete step cost. | Retained CPU `CompetitiveColumnLayer.process()` uses `candidate_subset_lazy_state_transition`, materializes only routed candidates plus winners, keeps non-awake stale-age cached with checkpointed stamps, and exposes cached/materialized counts through Runtime Truth. Candidate-subset CUDA graph/burst transition now follows the same cached-stamp contract inside the promoted transition launch. | Revisit dense retained CPU transition only if lazy state transition breaks correctness and the replacement truthfully marks `runs_all_columns=true` with complete-step evidence. |
| Dense CUDA stale-counter, spike-window, assembly, and candidate homeostasis bookkeeping under candidate scheduling | retired implementation shape | After route-vote and candidate-predictive updates became bounded, the CUDA graph/burst transition still physically advanced dense stale counters, spike rows, assembly clearing, and fallback/homeostasis-related state for every column, so cached sleep could still be fake if Runtime Truth cleared `runs_all_columns`. The 2026-06-16 sparse CUDA state-transition run updated only `10/1024` rows, cached `1014`, reported `state_transition_runs_all_columns=false`, and improved the 131072-token comparison from `5947.863` to `6007.582 tokens/sec` with train compute moving from `0.136041` to `0.132374 ms/token`. | Candidate-subset CUDA transition now uses logical stale-age stamps, a scalar all-materialized step, recent-spike active IDs, assembly active-winner state, and candidate-scoped threshold/win-rate writes inside the existing transition launch. Dense all-column startup/fallback remains available only when it truly runs and must report dense truth. | Revisit dense candidate-scheduled CUDA bookkeeping only if sparse state transition fails parity or repeated long CUDA runs show dense is faster; the fallback must keep `state_transition_runs_all_columns=true` and an explicit reason. |
| Per-burst CUDA metadata tensor fill for dense state-transition truth | retired implementation shape | The CUDA graph transition already mutates dense physical `steps_since_win` when it truly runs all columns; filling a per-column `steps_since_win_last_update_step` tensor every burst only stamped report metadata and added an avoidable CUDA launch. | Dense CUDA transition records scalar all-materialized-step metadata and materializes the per-column stamp tensor only on checkpoint/control paths that need it. Candidate-subset CUDA transition uses per-row stamps only for the bounded awake rows that physically update. | Reintroduce per-burst fills only if scalar metadata cannot preserve checkpoint/snapshot correctness and a long 131072-token run proves the fill is neutral. |
| Deep-sleep backfill before any column can be deep-sleep eligible | retired implementation shape | The retained route requested a `k * candidate_deep_sleep_backfill_factor` candidate pool as soon as the sleep filter start gate opened, even when `token_count < dead_column_steps` meant no column could truthfully be deep-sleep filtered. That added bounded routing work without changing the awake mask. | The trainer now gates backfill on `token_count >= dead_column_steps`; before that age gate it requests only `k`, records `candidate_deep_sleep_filter_no_column_can_be_deep_sleep_yet`, and keeps the scheduler-owned awake mask bounded without an all-column sleep scan. | Revisit only if a new sleep state can become eligible before `dead_column_steps` and focused tests plus complete-step benchmarks prove the earlier backfill changes execution truth and cost. |
| Report-local top-k awake projection as live Runtime Truth execution mask | retired implementation shape | The core report could compute a second all-column top-k awake set from prediction error, confidence, usefulness, and recent winners, causing top-level Runtime Truth awake counts, wake reasons, and vote samples to drift from the training-owned `ColumnWakePlan`. | Live model reports pass `ColumnWakePlan` candidates, reasons, fallback, and consumers into `core.column_runtime`; the report-local top-k path remains only for standalone/offline helper calls when no execution mask is supplied. | Revisit only for offline/status sample evidence, never as live execution truth unless training/core owns it and focused tests plus complete-runtime benchmarks prove no hot-path tax. |
| Hot-path hypercube hub topology refresh | retired | Every `HypercubeBindingLayer.bind()` updated hub evidence and immediately refreshed structural outreach, allowing adjacency growth/pruning and mutation-ledger writes during ordinary ticks without checkpoint/operator authorization. | Bind updates activation EMA only. The explicit core refresh is reachable solely through the checkpoint-backed, revision-bound, operator-confirmed Structural Mutation Application, with target/reason hash binding, edge-budget enforcement, exact delta evidence, and rollback. | Never restore per-bind refresh. Further automation requires a separate evidence-backed promotion decision while preserving the same transaction guarantees. |
| Always-running inactive Hypercube Binding | retired | A representative 1024-column CUDA checkpoint had zero binding usage and output, but the binding specialist still launched its full local computation every token. This violated the column-society rule that dormant specialists should not consume routine metabolism. | Conditional Binding Wake probes inactive binding at a bounded cadence, preserves cached state between probes, exposes execution evidence, and immediately resumes every-tick execution after learned or restored usage becomes active. | Revisit the cadence only with prediction/learning-quality evidence and an end-to-end benchmark; do not restore unconditional inactive execution as compatibility behavior. |
| Always-running text-only Cross-Modal Grounding | retired | Live profiling showed `on_text_spike()` cost about `3.9 ms` median every token even when no visual/audio evidence was present, so text-only background ticks spent grounding metabolism without a sensory co-occurrence event. | Conditional Cross-Modal Text Wake runs text-only grounding at a bounded cadence, records cached-idle skips, preserves sensory-backed every-tick updates, and exposes Runtime Truth counters. | Revisit only with grounding-quality evidence plus endpoint and p95 benchmarks; do not restore unconditional text-only grounding in the hot path. |
| Zero-blend lightweight input-synapse rewrites | retired | With `input_weight_blend=0.0`, lightweight input weights cannot affect competition or assembly output, but the hot path still rewrote and renormalized the winner row every tick. Controlled RTX 3060 hot-window probes showed lower median and p95 latency when this zero-contribution work was skipped. | Keep prototype learning, homeostasis, stale counters, and spike evidence active; update lightweight input synapses only when their blend is nonzero. Local STDP remains active because it owns additional projection, inhibition, scaling, and spike-state mechanisms. | Revisit only by enabling a nonzero blend or through a separately benchmarked plasticity mechanism whose state contributes to live cognition. |
| ConceptStore refresh from binding-topology structural evidence | retired | The structural design and isolated evaluation measured binding edge deltas, but the executor called `ConceptStore.refresh_structural_capacity`, so review evidence did not identify the state being mutated. | Structural Mutation Application now targets only `HypercubeBindingLayer.refresh_hub_topology`; concept-capacity mutation needs its own evidence and transaction if promoted later. | Reintroduce only as a separately named target with target-specific evaluation, checkpoint verification, bounded delta, and rollback tests. |
| Row-wise CUDA scalar scan for binding-growth planning | rejected experiment | Reading sparse degree/neighbor rows one scalar at a time synchronized the host repeatedly and measured about `303.2645 ms` median at 1024 columns. | Snapshot adjacency tensors twice, then perform vectorized candidate-membership planning on the CPU control plane; measured CUDA-backed median fell to `5.1334 ms`. | Revisit only as a fused device-side sparse planner that beats the bounded snapshot path and preserves exact plan hashes. |
| External source `close()` under the manager lock | retired | A blocked Hugging Face iterator shutdown could hold the shared `RLock`, freezing stop, status, and checkpoint operations together. | Stop sets the cooperative event and records lifecycle state without invoking external stream shutdown under the manager lock; checkpoint save fails fast while runtime mutation is active. | Revisit only with a proven bounded, thread-safe cancellation primitive that cannot execute provider I/O while holding runtime state locks. |
| SNN language thought-era public HTTP route vocabulary | retired | Public readout-ledger routes now use canonical `snn-language-*` route names and `snn_language_*` request fields. Keeping the previous route families documented individually made old vocabulary look more important than the current API. | Canonical SNN language surface, memory, consolidation, structural plasticity, capacity mutation, newborn-neuron integration, critical-period learning, maturation review, and newborn-synapse pruning routes over the same reviewed ledger/executor gates. | Only if a real external integration appears; this local system does not keep compatibility routes by default. |
| Legacy multi-tab dashboard and decorative neural-space UI | retired | Thirteen overlapping monitor/model/control tabs, duplicated API state, and Three.js/R3F neural animation preserved the old system vocabulary while increasing bundle weight and implying runtime activity without Runtime Truth evidence. | Six maintained operator workspaces using shadcn controls, plus a read-only 2D Runtime Machinery Plane driven by current source, device, sparse-column, vote, memory, binding, and execution telemetry. | Reintroduce a visualization only when it maps a maintained Runtime Truth field or operator workflow and its bundle/runtime cost is measured. |
| Functional full-state compiled steady-state column transition | rejected and removed | Isolated `torch.compile(mode="reduce-overhead")` execution improved substantially, but the functional transition returned eleven full state tensors and copied them into stable runtime buffers. Two controlled full hot-window A/B pairs averaged about `34.75` versus `36.65 tokens/sec` for compiled versus the retained live path, with mixed median/p95 latency and about `14 s` first-use warmup. After the in-place/graph transition became the maintained path, keeping the full-state function and benchmark in active `core`/`evaluation` preserved a rejected architecture as a test oracle. | Delete `core.column_transition`, the standalone steady-state benchmark, and the obsolete in-place isolated comparison runner. CUDA parity tests compare the promoted kernel against retained module semantics without importing a rejected production-shaped implementation. | Revisit only as a new evaluator outside `core` if a future executor needs an explicit oracle and the oracle avoids full-state output/writeback as the promoted shape. |
| Competition-only compiled column-kernel benchmark runner | retired and removed | The runner measured only candidate-scoped competition without projection, routing, predictive state, runtime mutation, trainer orchestration, memory, binding, grounding, or service cost. Wider `compiled_hot_path_kernel_benchmark` and production `ColumnTransitionRuntime`/route-vote evidence superseded it, so keeping a separate competition-only runner preserved an old partial promotion path. | Keep historical benchmark numbers in hot-path notes. Use the wider compiled hot-path benchmark for exploratory fusion evidence and complete runtime stress gates for promotion. | Revisit only if a future isolated competition kernel is needed for a specific regression and cannot be covered by the wider benchmark. |
| Evaluation monkeypatch for the in-place column transition | retired | Benchmark-local replacement of the trainer transition boundary duplicated lifecycle logic, could not prove startup behavior, and did not expose the executing mode or failure policy through Runtime Truth. | `training.column_transition_runtime.ColumnTransitionRuntime`, selected by checkpoint configuration and reused by evaluation runners. | Do not restore. New executors need a production-owned lifecycle, pre-mutation eligibility checks, Runtime Truth, and checkpoint-backed rollback. |
| Every-token or unbounded background ConceptStore observation | retired | Live stage profiling attributed `5490.65 ms` of a `7292.26 ms` 12-token tick to concept observation. Direct profiling found repeated centroid normalization and structural scans; neither needs to run after every token in a source window. A later 24-token CUDA service run still spent `2391.1165 ms` in concept observation before batching, and a 128-token uncapped CUDA source tick spent `66.8507 ms` in concept observation. | Sample source windows, cap the ConceptStore batch at four observations per service tick by default, run ConceptStore structural maintenance once at the source-window boundary, cache normalized centroids with mutation/restore invalidation, and expose interval, max-per-tick, attempts, skipped attempts, observations, batch count, and structural-maintenance passes in tick evidence. | Revisit only if a tighter cadence improves measured grounding or prediction quality enough to justify its live latency and CPU cost. |
| Every-source-tick ConceptStore observation | retired | After token-level ConceptStore observation was bounded, every source tick still woke the knowledge layer. Isolated 1024-token continuous CUDA stress measured interval-1 at `401.973 tokens/sec` with 8 sampled ticks and 32 ConceptStore attempts, while interval-4 measured `517.818 tokens/sec` with 3 sampled ticks, 5 cadenced skips, and 12 attempts. Both runs used the same full-warm 1024-token shape, active `inplace_triton`, 1024 graph replays, and zero CUDA graph failures. | Source ConceptStore observation wakes on the first source tick and then every configured `source_concept_observation_tick_interval` ticks by default; neural `train_step` remains per-token, and tick evidence reports `sampled_batched` versus `cadenced_tick_skip`, due state, interval, attempts, skips, and observations. | Revisit only with grounding or prediction-quality evidence showing a tighter source-tick cadence pays for itself in complete-runtime throughput and cognition quality. |
| 512-token service source tick default | retired | After source ConceptStore observation was capped, the same CUDA checkpoint measured `128` source tokens at `714.957 tokens/sec`, while `256` regressed to `376.380 tokens/sec` and `512` to `460.848 tokens/sec`. The larger default increased trainer/control cost without improving sustained service throughput. | Default Terminus source ticks use the measured `128`-token window, while benchmarks and operator config can still set larger values explicitly for remeasurement. | Revisit only with uncontended complete service evidence showing a larger window improves throughput and acceptable tick latency without hiding skipped sequential training. |
| Per-token cross-modal trace decay after sensory trace expiry | retired | Text-only ticks with no live sensory trace already skipped cross-modal learning, but `record_text_idle_skip()` still decayed text, visual, and audio traces every token. A same-checkpoint 128-token CUDA service profile reduced `cross_modal` from `0.2153` to `0.0127 ms/token` after clearing expired traces once and then sleeping the specialist. | On trace expiry, the trainer resets cross-modal traces once, records `cross_modal_idle_trace_reset_count`, and then calls cached-idle skip without trace decay until visual/audio evidence wakes the specialist again. | Revisit only if sensory-grounding quality evidence proves post-expiry trace tails are useful enough to justify the per-token tensor decay cost. |
| Candidate-homeostasis start at 128 tokens | rejected experiment | Lowering `candidate_homeostasis_start_tokens` from `512` to `128` forced the first 128-token source tick onto the candidate-subset graph, but complete CUDA service throughput regressed from `830.548` to `406.525 tokens/sec`; trainer-stage throughput fell from `900.9459` to `621.0137 tokens/sec`. | Keep `candidate_homeostasis_start_tokens=512` until a broader scheduler or graph executor proves earlier candidate-scoped homeostasis improves complete ticks and quality. | Revisit only with repeated same-checkpoint complete service wins and diversity/grounding evidence showing earlier homeostasis does not over-specialize columns. |
| CUDA graph host-truth sync interval 16 as default, before a wider event queue | superseded experiment | Raising `cuda_graph_host_truth_sync_interval_tokens` from `8` to `16` reduced host sync count from `17` to `9` and the host-sync substage from about `0.0875` to `0.0456 ms/token`, but complete 128-token CUDA service throughput regressed to `702.015 tokens/sec` versus the retained `830.548`, with trainer-stage throughput `778.0866` versus `900.9459`. | This result is superseded by the 32-token Deferred Device Event Queue and 32-token host-truth cadence. Exact per-token mirrors remain available with interval `1` for parity runs. | Revisit only if the result packet or Runtime Truth mirror changes enough that a lower interval wins repeated complete service benchmarks against the 32-token path. |
| Predictive `hypothesis` vote tensor | retired | The full-column tensor was zeroed and one winner slot rewritten every tick, serialized in checkpoints, and reported as device evidence, but no voting, prediction, routing, readout, or Runtime Truth algorithm consumed it. | Compute consensus directly from winner confidence/location evidence; old checkpoint fields are ignored and new checkpoints omit the decorative tensor. | Reintroduce only when a concrete consumer needs persistent hypothesis amplitudes and a full hot-window benchmark proves the storage/write cost is justified. |
| Retained all-column predictive vote scan after candidate routing | retired | Once routing has already produced a bounded candidate set, recomputing reference-frame agreement for every predictive column is a hot-path all-column tax. The 2026-06-15 CPU A/B preserved exact winners while scoped cached voting updated `10/2048` votes, cached `2038`, kept `runs_all_columns=false`, and improved complete `train_step` mean latency by `7.14%`. | `PredictiveColumnState.vote()` accepts trainer-owned candidate indices, updates only the awake mask, preserves cached consensus gain for non-awake columns, and exposes updated/cached counts plus fallback reason through Runtime Truth. | Revisit only if a future routing mode consumes non-candidate predictive gains before competition or repeated complete-runtime/quality evidence shows all-column retained voting is worth the cost. |
| All-column predictive vote inside retained CUDA selection fallback | retired | The fused CUDA vote/competition fallback path already has a selected candidate set. Calling retained consensus vote without those candidates would make a context-gain fallback wake every predictive column while the scheduler still reported a bounded candidate selection. Focused CUDA tests now force that fallback and require `updated_column_count=4`, cached votes for `12` non-awake columns, and `runs_all_columns=false`. | `_retained_consensus_gain()` receives the selected candidates, calls `PredictiveColumnState.vote(..., candidate_indices=...)`, returns candidate-local consensus/context gain, and `CompetitiveColumnLayer.compete()` accepts candidate-local context gain tensors. The longer 8192-column check stayed in the 6k-ish band at `6016.247 tokens/sec` with `route_input_rows_scored=12/8192` and `state_transition_runs_all_columns=false`. | Revisit only if a future fallback truthfully reports all-column vote execution and repeated complete-runtime evidence proves the all-column tax is necessary. |
| Vectorized predictive lazy-wake catch-up | rejected experiment | The first bounded vectorized replacement for per-step predictive materialization made the 8192-column 400-sample CPU A/B cost-neutral, but it changed the long winner sequence (`winner_sequence_equal=false`) in `reports/column_scheduler_20260615/cpu-8192-lazy-fast-materialization-long.json`. A later closed-form recurrence passed focused synthetic parity and one long seed, but repeated evidence still failed: `reports/column_scheduler_20260615/cpu-scaling-large-lazy-exact-predictive-fast-long-sweep.json` reported `all_winner_sequences_equal=false`, and seed-20260616 reruns such as `reports/column_scheduler_20260615/cpu-8192-lazy-exact-predictive-fast-seed20260616-long-rerun2.json` showed one-tick winner shifts near fallback-threshold boundaries. | Keep ordered candidate-bounded predictive wake replay for cached predictive state. The retained homeostasis optimization remains limited to the exact no-relaxation/no-clamp zero-activity catch-up and falls back to ordered replay when event order matters. | Revisit only with repeated long winner-parity evidence across seeds, focused predictive-state parity tests beyond short synthetic cases, and complete-tick cost evidence that is neutral or better without adding an all-column sleep scan. |
| Eager per-tick wake-plan report materialization | rejected implementation path | The first `ColumnWakePlan` implementation stored the plan and also converted it to the legacy candidate sleep-filter report dict every tick. It preserved scheduler truth but added avoidable Python control-plane work. The long CUDA stress run measured `5808.990 tokens/sec` with `train_compute=0.142889 ms/token`; after deferring legacy dict/JSON projection to report time and using a slotted wake-plan object, the same 131072-token shape reached `5822.624 tokens/sec` with `train_compute=0.142080 ms/token`. | Keep the live wake plan as a small training-owned object; materialize `column_wake_plan` and legacy `candidate_sleep_filter_execution` only when Runtime Truth or benchmarks request a report. | Revisit only if a lower-level GPU/native scheduler owns the wake mask and can publish bounded truth without Python report work on the measured hot path. |
| One-token background execution with a forced 5 ms yield | retired | Runtime Control released/reacquired mutation ownership and slept after every sequential token. Reversed RTX 3060 production-loop A/B measured only `135.240 tokens/sec` on average and attributed about `720 ms` per 128-token tick to artificial yield time. | Bounded Continuous Execution Quantum: default `8` sequential tokens per lock acquisition, zero forced yield, stop checks between quanta, and Runtime Truth schedule evidence. The reversed A/B averaged `662.800 tokens/sec` (`4.901x`) with quantum-arm stop latency below `14 ms`. | Revisit only if measured operator responsiveness fails at the bounded quantum and an alternative preserves comparable complete-loop throughput without skipping sequential training. |
| Candidate-scoped or separately compiled predictive vote | rejected experiment | Candidate-local voting reduced arithmetic but added gathers and small CUDA launches, measuring `47.29` versus `71.74 ticks/sec`. Separately compiling the dense vote measured `75.82` versus `109.85 ticks/sec` and added about `9.8 s` warmup despite a faster isolated microbenchmark. | The proven learned-chunk/no-extra-gain path now fuses candidate-local vote, prototype score, inhibition, fallback, and winner selection inside `ColumnTransitionRuntime`; unsupported shapes retain the eager dense vote. | Do not restore a standalone vote launch. Extend only through a broader retrieval/transition fusion with complete `train_step` and trajectory-quality evidence. |
| Skipping repeated routing-key normalization | rejected experiment | Removing apparently redundant normalization reduced profiler launches, but sequential feedback matched only `30/64` winners against the retained path and reached `0.5493` maximum predictive-location divergence without a repeatable full-hot-window gain. | Preserve the current projection, retrieval, and competition normalization order until a fused implementation reproduces it or passes a separate bounded cognitive-quality gate. | Revisit inside a fused routing kernel with exact candidate/winner parity or quality evidence strong enough to justify a changed trajectory. |
| Batched projection+routing fusion as a promotion proxy | rejected experiment | A 256-token CUDA batch of learned-chunk projection, tensor routing, and competition looked fast but did not preserve production trajectory: projection differed by `8.66e-6`, candidate sets diverged on `13/256` rows, and winners diverged on `5/256` rows. The batched comparison also hides sequential threshold decay inside `compete()`. | Use single-tick parity first. The safe next executor boundary starts after production `routing_key_from_pattern` and fuses tensor routing plus competition for one live tick before any broader batch/graph capture. | Revisit only with exact sequential parity or a declared quality gate that proves the altered trajectory is better enough to justify the change. |
| Incremental per-update torch routing-cache maintenance | rejected experiment | Reversed complete hot-window pairs were mixed and averaged about `91.17` ticks/sec for incremental maintenance versus `95.33` for retained rebuild-on-demand. More mutation bookkeeping did not justify itself. | Keep invalidation plus bounded exact cache rebuild, and let the fused text route/vote lifecycle refresh cache pointers after retrieval rebuilds. | Revisit only with stable complete-tick wins under the real update cadence and exact routing parity. |
| Existing-id torch routing-cache in-place update | rejected experiment | Updating existing `torch_topk` vectors in the ready cache preserved pointer stability and reduced `cuda_graph_prepare_eligible` from the retained `0.1122` to about `0.0317-0.0330 ms/token`, but complete CUDA service profiles regressed. The first implementation raised `routing_index_buffer` to `0.4161 ms/token`; the position-map version raised it to `0.6745 ms/token` and total profiled cost to `3.1060 ms/token` versus the retained `1.6251`. | Keep simple invalidation/rebuild-on-demand and avoid per-update cache mutation in the live routing index. | Revisit only as part of a broader device-owned route/index executor that wins complete ticks and preserves routing quality. |
| Global fused route/vote on sensory ticks | rejected experiment | The two-launch Triton route/vote path preserved every winner and cross-modal tensor, but a reversed sensory-heavy grounded pair measured only `0.919x` before modality gating. | The internal fused route/vote family is text/idle only inside the promoted graph path or explicit evaluation overrides; visual/audio ticks retain tensor routing and increment sensory-fallback evidence. | Revisit only if a sensory-stage fusion removes enough surrounding work to beat retained routing in reversed grounded runs. |
| Standalone compiled route/competition runtime | rejected experiment | A trainer-owned prototype proved warmup, execution, and exact single-tick kernel parity, but complete encoded hot-window A/B did not beat the retained route/competition path. Pair A measured retained/compiled `82.00/71.10 ticks/sec`; reversed Pair B measured compiled/retained `88.15/90.32 ticks/sec`. Compiled also reserved more VRAM (`50 MB` versus `28 MB`) and added warmup cost. | Remove the runtime/config/status path and retain only the evaluation benchmark. The default route/competition path stays retained. | Revisit only as a larger fused cluster that includes projection/vote/route/competition or removes host orchestration, with exact sequential parity and repeated complete `train_step` wins. |
| Wider routing-index hot-path flush interval as throughput fix | rejected experiment | A same-checkpoint 2026-06-12 scan of `_routing_index_flush_interval` values `16`, `64`, and `256` over the fused CUDA hot-window measured `50.16`, `48.73`, and `49.91 ticks/sec`; interval 16 remained fastest on throughput. | Keep the existing bounded routing-index update buffer cadence and target broader trainer orchestration, routing/projection fusion, and cross-modal/text-only wake policy. | Revisit only with complete hot-window and routing-quality evidence showing a repeated win, not a single p95 improvement. |
| Lazy per-entry STC tag/PRP decay on the live memory update | rejected experiment | Deferring full stored-entry decay preserved materialized values to floating-point tolerance, but paired complete CUDA trainer runs were mixed: one seed improved throughput by `0.8%`, while another regressed by `3.1%`. The extra decay timestamps and checkpoint state were not justified. | Keep exact every-tick STC decay for now; avoid only optional archival payload copies after reservoir rejection, which has a direct steady-state cost and no cognitive-state change. | Revisit only with a simpler representation or replay-window design that wins repeated full-trainer A/B and preserves STC/replay quality. |
| Hardcoded ten-token trainer telemetry refresh | retired | Scalar metric extraction is Runtime Truth display work, not cognition. On the 1024-column CUDA checkpoint, refreshing every 64 tokens beat the legacy ten-token cadence in two reversed complete hot-window pairs: `108.64` versus `95.12 ticks/sec` and `98.12` versus `90.24 ticks/sec`, with lower median and p95 latency in both pairs. | `trainer_telemetry_interval_tokens` defaults to `64`, and train-step metrics expose both interval and refresh status while cached values fill intervening ticks. | Revisit only if a live operator workflow needs fresher scalar telemetry and benchmark evidence shows the lower interval does not hurt throughput or p95. |
| Persistent previous-routing-key buffer reuse | rejected experiment | Replacing the previous routing-key clone with an in-place copied buffer was exact but not consistently faster. One pair improved slightly (`61.03` versus `59.40 ticks/sec`), while the reversed pair regressed (`71.77` versus `76.47`) and p95 worsened. | Keep the simple per-tick clone until previous-key ownership is folded into a broader persistent CUDA execution cluster. | Revisit only as part of a fused/persistent host-orchestration reduction with repeated complete-tick wins. |
| Python RNG for archival reservoir sampling | rejected experiment | Replacing the one-element `torch.randint` reservoir draw with `random.randrange` was not a stable complete-tick improvement. One pair improved (`98.35` versus `89.14 ticks/sec`), while the reversed pair regressed badly (`48.23` versus `64.12`) with worse p95. | Keep the existing torch reservoir draw and focus on larger host-orchestration or memory-context costs. | Revisit only with repeated full-trainer wins and explicit reproducibility/checkpoint implications. |
| Regex precompile-only stream text optimization | rejected experiment | Moving stream-text regex patterns to module-level compiled objects looked harmless but did not win the direct text-path benchmark: median was `117.245 ms` versus `110.9716 ms` for the legacy path over 2000 updates. | Cache the immutable episode-term set instead, which won both text-path and complete hot-window evidence. | Revisit only with a measured source-text workload that proves a regex-level change helps after term caching. |
| Bulk/asynchronous archival CUDA-to-CPU staging | rejected experiment | Paired complete trainer runs were neutral (`69.14/72.76` and `83.91/83.79 ticks/sec` for retained/staged observations), while safe host consumption would require pinned destination ownership and explicit synchronization barriers. That complexity was not justified by end-to-end evidence. | Decide reservoir admission before optional copies and keep admitted archival transfer explicit. | Revisit only with a bounded pinned-buffer lifecycle, checkpoint-safe ownership, and repeated complete-tick wins. |
| CUDA-resident per-bucket consolidation observation state | rejected experiment | Moving small EMA/drift/STC observation work to CUDA added launch and transfer pressure. The tested path reached `75.69 ticks/sec` versus `97.18 ticks/sec` for retained CPU observation. | Keep archival observation and STC scalar decay on CPU; use contiguous zero-copy numeric buffers and move only sampled replay tensors to the model device. | Revisit only as part of a larger fused device-resident replay/consolidation window with complete-tick and replay-quality evidence. |
| Coalesced pinned host readback for in-place transition scalars | rejected experiment | Grouping winner/fallback scalar readback and sparse CPU assembly looked cleaner, but controlled hot-window evidence was negative on average: one retained/coalesced pair measured `112.30/94.26 ticks/sec`, and the reversed pair measured `83.36/91.56`. Safe pinned host ownership did not justify the added path. | Keep the promoted fused in-place transition's existing scalar evidence path until a broader persistent host-orchestration design wins complete ticks. | Revisit only with repeated complete hot-window wins, no extra Triton specializations, and Runtime Truth evidence for synchronization boundaries. |
| Lazy cross-modal text normalization | rejected experiment | Deferring text-normalization work reduced `F.normalize` profiler calls, but full CUDA trainer A/B did not hold up: one pair regressed from `12.19` to `14.45 ms` mean latency, while the reversed pair improved only from `14.26` to `14.07 ms`. | Keep eager normalization in the cross-modal trainer path and target larger fused/stage-level bottlenecks. | Revisit only inside a complete cross-modal wake/fusion benchmark with grounding-quality parity and repeated hot-window wins. |
| Lazy cross-modal trace decay | rejected experiment | Accumulating idle decay age and materializing three traces only at the next real event preserved all winners and kept cross-modal state within `1.2e-7`, but reversed complete-tick evidence regressed to `84.38` versus `96.25 ticks/sec` (`0.877x`). | Keep the three direct in-place trace decays on cached-idle ticks; reduce larger synchronized stages instead. | Revisit only inside a broader captured cross-modal event executor with repeated complete-tick gains and grounding parity. |
| Universal vectorized awake-ripple scan | rejected experiment | Zero-copy NumPy scanning is clearly faster for large ledgers, but setup cost does not justify forcing it on small memory stores. Capacity-64 and capacity-256 crossover probes retained scalar mode, while capacity-1000 improved from `1.247` to `0.15545 ms` median with vector mode. | Use the adaptive scalar-small/vector-large policy and expose `memory_hot_path` scan counters in Runtime Truth. | Revisit the threshold only with a new crossover benchmark on the target hardware and no replay-quality regression. |
| Global recent-memory awake-ripple scan after scheduler wake plan | retired implementation shape | Once training owns a `ColumnWakePlan`, scanning all recent slow-memory entries for awake-ripple tagging touches sleeping/non-awake buckets after the scheduler already has a bounded mask. The first 8192-entry scoped benchmark measured `0.989598 ms` mean for 10 awake buckets versus `1.183196 ms` for the global path. The 2026-06-17 follow-up removed the production unscoped default entirely and capped scoped tagging before mutation: `reports/bounded_replay_window_20260617/awake-ripple-bounded-scope-8192-i256.json` measured the diagnostic global vector path at `1.433332 ms` mean and the wake-bucket candidate window at `1.091997 ms` mean (`1.312579x`), with scoped `ripple_scalar_scan_count=0`, `ripple_vector_scan_count=0`, and `ripple_awake_bucket_scan_count=256`. The 524288-token 65536-column check stayed in band at `6152.328 tokens/sec`, `train_compute=0.131727 ms/token`, bounded `12/65536` route rows, flat `2013 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. | `DualMemoryStore.ripple_tag_awake(...)` now requires awake bucket scope for production tagging, caps candidates through the recent bucket index, and reports `bounded_awake_ripple_tag.v1` with candidate budget, device placement, scan flags, `runs_every_token=false`, and CPU archival storage. Omitting scheduler context returns an empty retired report with `fallback_reason=awake_bucket_scope_required_for_ripple_tagging`; the old scalar/vector global scan can run only with `allow_global_diagnostic=true` and reports `diagnostic_awake_ripple_global_tag.v1`. | Revisit only if replay-quality evidence requires cross-bucket tagging and a bounded scheduler-owned policy preserves or beats the long 6k-ish ms/token baseline without hiding an all-memory scan. |
| Uncached deterministic file-source ingestion on every tick | retired | Static local/file sources were encoded synchronously during the tick even though their contents are deterministic. A same-source cold/warm benchmark reduced `collect_source_queue` from `1385.2147 ms` to `0.0293 ms` after cache restore and cut the 24-token tick from `2459.6596 ms` to `784.4856 ms`. | Cache bounded encoded windows for local/file sources under a file-fingerprint key, restoring ready queues on configure when the source path, size, and modification timestamp match. | Revisit only if source encoding becomes learned/adaptive or the cache harms grounding freshness; stale file contents must miss the cache. |
| Synchronous unchanged runtime-cache rewrite during tick preparation | retired | Warm deterministic sources no longer needed to rewrite identical restored raw-window material, but `prepare_training` still called `torch.save` inside the tick. A CUDA service run before the skip spent `388.0982 ms` in preparation; the unchanged-material skip reduced final warm preparation to `4.0725 ms` with `source_cache.cache_skip_count=1`. | Runtime Sources records a material hash for brain-source cache payloads, restores it with the ready queue, skips identical rewrites, and exposes cache write/skip counters in source progress and benchmark configured-source summaries. | Revisit only if cache material includes adaptive learned state that must be persisted every tick; otherwise unchanged deterministic source cache writes stay out of the hot path. |
| Every-token slow-memory archival admission | retired | A clean configured-source tick profile on the 1024-column CUDA checkpoint measured `memory_update` as the largest trainer substage: `10.3515 ms/token` before cadencing. Most source tokens do not need full replay-admission payload copies and stream-text episode reconstruction during the hot tick. | `slow_memory_archive_interval_tokens` defaults to `8`; the trainer archives the first token, cadence tokens, or high-surprise override tokens, skips non-archive stream-text context work, and exposes archive/skip counters plus last reason in Runtime Truth `memory_hot_path`. | Revisit only with complete-tick throughput and replay/grounding quality evidence showing every-token archival admission is worth the latency. |
| Narrow route/vote-plus-transition CUDA Graph | rejected experiment | Capturing only the already-fused route/vote and transition replayed quickly in isolation, but the complete text tick still paid separate input projection and reconstruction launches; same-process complete-tick evidence was only about `1.035x`. | The promoted `cuda_graph_text` island also captures production input normalization/projection and exact fresh reconstruction distance while preserving the same text-only and fail-closed boundaries. | Revisit a smaller graph only if fixed-address or VRAM constraints make the wider island unavailable and complete-tick evidence beats the retained fused path. |
| Split pre-route and transition CUDA Graph replays | retired | The first promoted `cuda_graph_text` island synchronized reconstruction to Python, updated neuromodulators on the host, then launched a second graph. Profiling showed this CPU/GPU boundary remained a material fixed-shape tick cost. | The Persistent Text Tick Executor captures projection, reconstruction, device neuromodulation, route/vote, and transition in one replay, then performs one bounded host truth synchronization. | Reintroduce only if a future device or graph backend cannot support persistent neuromodulator state and a measured fallback beats the one-replay executor without weakening quality or Runtime Truth. |
| Every-token CUDA graph host truth mirror | retired | The persistent CUDA graph already updates neuromodulator and surprise state on device every replay, but mirroring the scalar truth packet to Python every token forced host synchronization. A 24-token CUDA service profile reduced graph host truth syncs from `24` to `7`, lowering `cuda_graph_prepare_host_truth_sync` from about `1.0241` to `0.3349 ms/token` while keeping 24 graph-owned surprise updates. | `cuda_graph_host_truth_sync_interval_tokens` defaults to `32`; Runtime Truth reports sync/skip counts and exact per-token mirrors remain available for parity runs with interval `1`. | Revisit only if a measured operator workflow requires fresher scalar mirrors and complete-tick benchmarks show the lower interval does not harm throughput or p95. |
| Immediate transition-stage winner host readback | retired | The CUDA graph transition wrote the winner into a persistent device tensor, but `ColumnTransitionRuntime.apply()` immediately called `.tolist()` to mirror the id for Python. Deferring materialization moved the sync to the first real host consumer and reduced `column_transition_winner_readback` from `0.8222` to `0.0014 ms/token` in the 24-token CUDA service profile. | Keep winner ids device-native through graph transition and routing-index enqueue; materialize host ids lazily for memory metadata, `last_winner`, checkpoint/status mirrors, or batched routing-index flush. | Revisit only if a future CPU-owned algorithm genuinely needs the winner before subsequent tensor work and complete-tick benchmarks prove the earlier sync is cheaper. |
| CUDA winner-consolidation metric readback | retired | `winner_consolidation_level` is Runtime Truth telemetry, but graph-backed ticks still used `consolidation.index_select(...).mean().item()` on telemetry refreshes, synchronizing CUDA for a scalar report. Same-checkpoint 128-token profiles reduced `column_transition_consolidation_readback` from `0.7027` to `0.0026-0.0032 ms/token` by reading CPU memory-store consolidation only when the cadenced host truth packet already had a winner id, otherwise reusing the cached metric. | Device consolidation still drives transition math and is protected by the consolidation generation guard. Runtime Truth reports `winner_consolidation_cpu_metric_count` and `winner_consolidation_cached_metric_count` so operators can see metric freshness without forcing a device sync. | Revisit only if a CPU-owned mutation requires exact per-token winner consolidation before it can run, and complete-tick benchmarks prove the synchronization cost is justified. |
| Hot-path stream episode reconstruction | retired | Live slow-memory archive ticks still rebuilt expanded stream episode text even when the bounded raw window was already the durable source record. The deferred-winner CUDA service profile measured `stream_text_context=2.9539 ms/token`; storing raw-window archive text directly reduced that stage to `0.0009 ms/token` in the next 24-token profile. | Live `train_step` stores bounded `raw_window` text as replay text when an archive record is due. `_update_stream_text()` remains available for slow/evaluation/display helpers that explicitly need expanded episode text. | Revisit only if replay-quality evidence proves expanded episode text is required and a prewarmed or slow-path builder removes the hot-path cost. |
| Every-token winner host mirror | retired | After deferred transition readback, live CUDA graph ticks still called `winners.tolist()` on graph-skip ticks only to keep Python `winner`/`last_winner` mirrors fresh. A 24-token configured-source profile with cadenced winner mirroring removed `winner_host_materialize` from the per-tick profile and improved profiled trainer cost from `6.2952` to `4.0150 ms/token` (`158.85` to `249.07 tokens/sec`). | The device winner stays authoritative. Host winner mirrors refresh from the graph truth packet or when a CPU-owned consumer needs exact ids, such as slow-memory archive; metrics expose `winner_host_mirror_fresh`, sync count, and skip count. | Revisit only if an operator or CPU-owned algorithm needs exact per-token host winner ids and complete-tick evidence proves the sync cost is acceptable. |
| Four-token CUDA graph host truth mirror default | retired | Interval-4 scalar mirroring kept Runtime Truth fresher than the hot path needed after device-owned surprise and cadenced winner mirroring were in place. On the same 24-token configured-source CUDA checkpoint, raising the default interval to `8` reduced host truth syncs from `7` to `4`, lowered `cuda_graph_prepare_host_truth_sync` from `0.4596` to `0.1055 ms/token`, and improved profiled trainer throughput from `249.07` to `258.48 tokens/sec`. | Default `cuda_graph_host_truth_sync_interval_tokens` is now `32`; exact per-token parity remains available with interval `1`, and interval `4` remains an explicit test/evaluation choice rather than production default. | Revisit only if operator workflows require fresher scalar mirrors and repeated complete-tick evidence shows the lower interval is worth the synchronization cost. |
| Per-tick consolidation tensor cache lookup in graph eligibility | retired | The persistent text graph captured the bucket-consolidation tensor pointer, but eligibility still called `bucket_consolidation_tensor()` on every warm-memory tick only to re-confirm the pointer. The profiled RTX 3060 hot-window A/B after replacing that lookup with a memory-store generation guard raised persistent throughput from `360.0649` to `408.8154 ticks/sec` and reduced `cuda_graph_prepare_eligible` from `0.2482` to `0.2041 ms/tick`, with active graph replay, zero failures, and zero generation mismatches. | `DualMemoryStore.bucket_consolidation_cache_generation` changes on cache invalidation, ordinary in-place cache adjustments preserve it, and the graph fails closed before replay if generation or memory warm-state changes. Runtime Truth reports fast-path and mismatch counters. | Revisit only if consolidation cache ownership changes and a complete hot-window/profiled service run proves a different pointer-validation mechanism is faster and equally fail-closed. |
| Same-token duplicate graph candidate eligibility check | retired | After `prepare_routing()` replayed the persistent text graph and produced the candidate buffer for a token, `route_candidates()` still rechecked routing-cache and graph eligibility before returning that same buffer. The new token-scoped reuse path reports `route_vote_prepared_graph_reuse_count=80` per persistent benchmark arm and keeps CUDA graph replay active with zero failures. Throughput evidence was noisy (`371.0043` then `437.2580 ticks/sec` against the prior `408.8154`), so the retirement is justified as duplicate-check cleanup rather than a large speed claim. | Reuse the graph-prepared candidate buffer only for the same trainer token after successful persistent replay; sensory, bootstrap, inactive, or fallback states use the retained path. Runtime Truth exposes the reuse count. | Revisit only if routing ownership changes so candidates are no longer prepared by graph replay, or if quality/parity tests show same-token reuse can return stale candidates. |
| Per-token host staging of previous-routing flag | retired | The persistent text graph already owns the previous-routing-key buffer and the Triton transition kernel persists `has_previous_routing_key` after replay. Copying that boolean from pinned host memory every tick kept a stale CPU control bit in the graph-prep path. Focused CUDA graph parity/cadence tests passed after narrowing host parameter staging to the still-host-owned modulator; profiled evidence reduced `cuda_graph_prepare_parameter_stage` from `0.3219` to `0.2462 ms/tick`, but unprofiled complete-tick evidence was noisy, so this is cleanup rather than a throughput promotion. | Keep the previous flag graph/device-owned and expose `previous_flag_device_owned_count`; continue staging only the host-owned competitive modulator until modulator/control state moves into a broader persistent executor. | Revisit only if parity proves a graph-ineligible path needs host ownership of this flag, or if the transition kernel stops persisting previous-routing state. |
| Per-token host staging of graph text-tick learning rate | retired | For graph-backed text ticks, `comp.get_lr()` is a deterministic function of `update_count`, and the graph replay advances exactly one winner. Copying the learning-rate scalar from host every graph tick kept one more CPU control value in graph prep. Focused CUDA tests passed after moving the update-count scalar into graph-owned state, and a mixed sensory-fallback test proved the next graph tick resynchronizes after Python `update_count` changes outside the graph. Profiled evidence was not promotional (`297.9833 ticks/sec`, `cuda_graph_prepare_parameter_stage=0.2913 ms/tick`), while unprofiled repeats measured `336.8304` and `361.1783 ticks/sec`; this is exact control-state cleanup, not a broad speed win. | Compute the graph learning rate from device-owned update-count state on graph ticks; resync from host after fallback/sensory ticks; expose `learning_rate_device_owned_count` and `learning_rate_host_resync_count`. | Revisit only if multi-winner graph replay changes the per-tick update increment, or if a future executor owns all plasticity-control state and no host resync is needed. |
| Unconditional per-token graph modulator copy | retired | After host-truth cadencing, the CPU-visible competitive modulator only changes when surprise/error state is mirrored or recorded, but graph prep still recomputed and copied the scalar every graph tick. Revision-cached staging reduced `cuda_graph_prepare_parameter_stage` to `0.0317 ms/tick` from the prior learning-rate-control profile at `0.2913 ms/tick`, with focused tests proving interval-1 parity and interval-4 skip behavior. Unprofiled complete-tick evidence remained noisy (`296.7573` then `354.1062 ticks/sec`), so this is a stage-level graph-prep win rather than endpoint proof. | `SurpriseMonitor.modulator_revision` invalidates the staged scalar after CPU-visible surprise changes; graph prep copies only on revision changes and exposes `modulator_stage_copy_count` and `modulator_stage_skip_count`. | Revisit only if surprise history becomes fully device-owned, or if a future modulator consumer requires every-tick host-visible recomputation. |
| Per-token routing-cache dirty-bit/pointer validation on unchanged graph cache | retired | The persistent text graph captured routing-cache tensor pointers, but eligibility still consulted cache freshness and pointer state on ordinary ticks whose retrieval-owned cache generation had not changed. The profiled routing-cache generation fast path measured persistent `482.2661` versus fused `165.9433 ticks/sec`, reduced `cuda_graph_prepare_eligible` to `0.2017 ms/tick`, and reported `76` generation fast-path hits plus zero failures per persistent arm. Unprofiled repeats remained noisy (`360.1320` then `282.9019 ticks/sec` persistent mean), so this is a graph-eligibility cleanup rather than endpoint proof. | Retrieval owns `routing_tensor_cache_generation()` and advances it on cache-invalidating add/remove/rebuild events. The graph treats unchanged generation as pointer-current; generation mismatch goes through `routing_tensor_cache()` and fixed-pointer validation before replay, with Runtime Truth counters for fast-path, mismatch, rebuild-check, and clean-cache cases. | Revisit only if routing-cache ownership changes, if generation equality can no longer imply unchanged cache identity, or if a broader persistent route/index executor removes this eligibility boundary entirely with parity and complete-tick evidence. |
| Every-token awake ripple tagging attempt | retired | Awake ripple tagging is replay-priority metadata, but the live loop attempted it on every high-dopamine warm-memory tick. On the 24-token CUDA configured-source profile this cost `post_surprise_replay_tag=0.4866 ms/token`. Cadencing it with slow-memory archive events reduced that stage to `0.0685 ms/token` and improved profiled trainer throughput from `258.48` to `319.85 tokens/sec`; later fixed-cadence archive admission itself was retired from the hot path. | Attempt awake ripple tagging only on retained/fallback archive or strong-capture archive ticks when dopamine is high; high-dopamine non-archive and deferred-cadence ticks increment skip counters. Runtime Truth exposes tag attempts, skips, last reason, and tagged count. | Revisit only with replay-quality evidence proving every-token or fixed-cadence awake ripple tagging improves consolidation enough to justify the hot-path scan. |
| Full cross-modal idle bookkeeping on text-only ticks | retired | Text-only ticks with no sensory evidence and no residual trace already skipped cross-modal learning, but still walked the full cross-modal bookkeeping block. A CUDA configured-source profile reduced `cross_modal` from `0.6770` to `0.4933 ms/token` after adding a fast-idle branch with Runtime Truth counters. | Record cached idle state directly on pure text-idle ticks; keep the full path for visual/audio ticks, residual sensory traces, and due self-criticism windows. Runtime Truth exposes `cross_modal_hot_path.fast_idle_skip_count`. | Revisit only if grounding-quality evidence shows the idle bookkeeping has cognitive value or if a future sensory scheduler moves this work to an explicit slow path. |
| Per-entry CUDA winner-id scalar materialization during routing-index flush | retired | routing-index flush deduplicates pending winner ids on CPU, but materializing each pending CUDA id one scalar at a time creates repeated host synchronization opportunities. Batched materialization during flush reduced `routing_index_buffer` from `1.2420` to `0.7771 ms/token` in the next 24-token CUDA configured-source profile. | Keep the existing routing-index flush cadence, but materialize pending CUDA ids as one tensor batch at the explicit flush boundary. | Revisit only if a future routing-index owner keeps ids device-native through the index update or proves a different flush mechanism improves routing quality and complete-tick speed. |
| Per-token CUDA winner-id snapshot for routing-index buffering | rejected experiment | Cloning CUDA winner ids when enqueueing routing-index updates would make pending ids stable, but it added per-token allocation pressure. The 24-token CUDA configured-source profile regressed `routing_index_buffer` to `3.6198 ms/token` and profiled trainer throughput to `160.94 tokens/sec`. | Do not snapshot ids per tick. Materialize ids in batch at flush, and revisit stable device-id staging only with preallocated buffers and complete-tick wins. | Revisit only with a preallocated, pointer-stable id staging design that avoids per-token allocation and preserves routing-index correctness. |
| Blocking persistent-graph input staging copy | retired | The persistent CUDA graph input buffer was copied with default blocking semantics even though graph parameters already used non-blocking staging. Switching input staging to non-blocking copy preserved graph parity and reduced `cuda_graph_prepare_input_copy` from `0.4154` to `0.3203 ms/token` in the next CUDA configured-source profile. | Stage source tick tensors into the fixed graph input buffer with `non_blocking=True` while preserving the existing graph replay and fixed-address ownership. | Revisit only if a future input pipeline requires an explicit synchronization barrier before graph replay and complete-tick evidence shows the blocking copy is cheaper or safer. |
| Post-replay previous-route host flag assignment | retired | After each persistent text graph replay, Python rewrote `host_parameters[4] = 1.0`, but the next tick already recomputed that flag from `trainer._prev_routing_key` before staging parameters to the device. Focused CUDA graph parity and cadence tests passed after deleting the write. A configured-source profile lowered only `cuda_graph_prepare_bookkeeping` (`0.0645` to `0.0139 ms/token`) while total trainer cost regressed from noisy neighboring stages, so this is cleanup, not a throughput promotion. | Keep the device graph's persisted previous-routing-key buffer authoritative; compute the host flag once at the start of the next preparation step. | Revisit only if graph replay starts consuming a host-side post-replay flag before the next preparation copy, with parity tests proving the need. |
| Conditional CUDA graph parameter staging by host signature | rejected experiment | Skipping the pinned host-parameter copy seemed plausible, but the competitive modulator signature changed every token. The test profile showed `parameter_copy_count=25`, `parameter_skip_count=0`, added a `cuda_graph_prepare_parameter_stage=0.2998 ms/token` bucket, and reduced profiled throughput to `415.10 tokens/sec` versus the prior `615.34`. | Keep the unconditional tiny parameter staging copy until the modulator path is moved fully device-side or fused into a larger persistent executor. | Revisit only when a device-owned modulator/control state removes the changing host signature and complete-tick evidence proves a repeated win. |
| Device-owned recent-spike-row graph cursor without pre-replay host fill | rejected experiment | Letting the persistent graph own the spike-row cursor and synchronizing only at capture looked like a clean CPU-to-GPU write deletion. Focused CUDA graph parity still passed, but same-current configured-source profiles rejected it: the retained host-fill path measured `487.93 tokens/sec` and `2.0495 ms/token`, while the device-owned attempt measured `366.21` and `342.98 tokens/sec` across two runs with `2.7307` and `2.9156 ms/token`. | Keep the small pre-replay recent-spike-row fill until a larger persistent executor removes the surrounding control overhead and wins complete ticks. | Revisit only with repeated same-current complete-tick wins and exact spike-window parity. |
| Deferred routing-index winner-vector gather at flush | rejected experiment | Deferring prototype-vector materialization until routing-index flush reduced the local `routing_index_buffer` bucket from the prior warm `0.5031 ms/token` to `0.2040`, `0.1748`, and `0.1808 ms/token`, but complete configured-source profiles did not produce a stable full-tick win: the attempted path measured `435.55`, `327.18`, and `424.24 tokens/sec` versus the existing warm routing-cache baseline at `626.22 tokens/sec`. Neighboring stages such as routing prepare, graph input copy, cross-modal bookkeeping, and metrics build dominated the full tick. | Non-graph paths retain bounded winner/vector buffering. Eligible persistent graph ticks use Device-Owned Routing Cache Coherence and do not move this gather to flush. | Revisit only inside a same-process A/B or persistent route/index executor that improves complete sequential ticks while preserving routing-index correctness. |
| Per-token routing-index snapshot on persistent graph ticks | retired | The persistent transition already computes the normalized next winner prototype, but the trainer separately indexed that prototype and queued winner/vector objects for a later routing-index flush. Fresh profiling measured `routing_index_buffer=0.3263 ms/tick` before the broader ownership change. | Eligible graph ticks update the exact device routing cache inside the in-place Triton transition and skip the duplicate enqueue. The CPU index store is an explicit stale-capable slow mirror synchronized before retained index mutation. | Revisit only if a non-graph retrieval backend becomes the production hot route or quality evidence proves the device cache cannot remain authoritative between slow-path mirror synchronizations. |
| Mutation-enabled scalar CUDA chunk encoding during live source collection | retired | The empty-codebook 1024-column checkpoint still ran per-character learned-chunk inference and mutation on CUDA before training. Direct 24-character measurements were about `708 ms` with learning and `381 ms` inference-only. A fresh configured-source tick spent `505.6024 ms` collecting 24 tokens and reached `43.8417 tokens/sec`. | Live Runtime Sources uses inference-only bounded batches when the chunk codebook is empty; explicit training/remote bootstrap retains mutation-enabled scalar learning. Scalar/batch representation parity is within `1.19e-7`. Fresh cold evidence reduced collection to `37.0373 ms` and raised complete-tick throughput to `203.0565 tokens/sec`; cache-restored steady evidence reached `566.3356 tokens/sec`. | Revisit live chunk plasticity only through a separately scheduled or fused learning window with representation/learning-quality evidence and a complete-tick win. |
| Synchronous source-cache `torch.save` during tick preparation | retired | A changed live-source queue wrote its cache while holding the tick preparation path, adding roughly `45-63 ms` of host filesystem/checkpoint work to cold ticks. The same cache helper could also be called twice for one ordinary tick. | Runtime Sources hashes bounded raw windows, coalesces unchanged material, and schedules one worker-owned atomic write. Shutdown flushes pending writes; failures clear the material hash for retry and remain visible in Runtime Truth. A five-tick CUDA run ended at `585.6701 tokens/sec` with `prepare_training=3.1371 ms`. | Revisit synchronous persistence only if a durability requirement proves the tick cannot acknowledge before disk commit and complete-tick evidence justifies the latency. |
| Cold configured-source benchmark as the default service speed path | retired | The service benchmark configured a local source with `prewarm_on_startup=false`, so the first measured tick paid source-window construction and cache bookkeeping before neural training. Same-checkpoint CUDA evidence measured only `257.306 tokens/sec`, with `collect_source_queue=147.5918 ms` and `prepare_training=28.5999 ms`. | Configured-source benchmarks now prewarm by default, wait for Runtime Truth `ingestion.full_warm_ready`, and record warmup under `configured_source_summary.warmup.not_hot_path=true` before measuring `/terminus/tick`. The warm path measured `586.775 tokens/sec`, `collect_source_queue=0.0559 ms`, and `prepare_training=1.2179 ms`. `--disable-local-source-prewarm` remains for explicit cold-regression runs. | Revisit only when testing cold-start behavior, remote-source bootstrap, or regression gates that need to prove startup cost separately from warmed cognitive tick throughput. |
| Every-eight-token slow-memory archival default | retired | Slow-memory archival is replay/consolidation support, but the previous default attempted it every 8 tokens. Same-checkpoint CUDA profiling after moving the cadence to 64 reduced `memory_archive` from `0.188347` to `0.018416 ms/token` and improved trainer-stage throughput from `638.6378` to `663.6743 tokens/sec`; later 4096-token full-warm stress proved a wider 256-token cadence improved complete runtime further. | Default `slow_memory_archive_interval_tokens` is `256`; first-token and strong-capture archival remain active, and Runtime Truth reports interval, archive count, skip count, and last reason. Legacy unstamped checkpoints carrying retired values `8` or `64` migrate on load. | Revisit only with replay-quality evidence and repeated complete service benchmarks proving a tighter archive cadence pays for itself. |
| Sixty-four-token slow-memory archival default | retired | The interval-64 default was a useful intermediate metabolism cut, but the active 1024-column checkpoint still paid regular CPU replay-memory admission during long continuous ticks. In 4096-token full-warm CUDA stress, the stale interval-8 checkpoint measured `668.976 tokens/sec`, interval-64 measured `742.767`, and a config-only interval-256 clone measured `832.915`; shorter migrated samples were noisy, while the clean 32768-token migrated run reached `1779.859` with active CUDA graph replay, no measurement polling, all 256 tick events captured, and zero failures. | Promote interval 256 as the current production/migrated default while keeping first-token and strong-capture archival. Keep interval changes in trainer/checkpoint config, not service runtime scheduling. | Revisit only with replay/consolidation-quality evidence plus complete-runtime benchmarks showing interval 64 improves cognition enough to justify its hot-path cost. |
| Per-token persistent-graph input copy and recent-row host fill | retired | Testing the recent-spike-row cursor alone previously regressed complete service ticks, so it remained host-filled. The broader Persistent Quantum Input Ring moved both changing inputs and cursor control into persistent CUDA ownership. Two reversed 256-tick continuous A/B runs improved mean complete encoded throughput from `758.57` to `1026.38 ticks/sec` (`1.353x`) and from `746.19` to `877.53` (`1.176x`), with exact sequential trajectory tests, every staged token reused, and zero mismatch/fallback/failure counts. | Brain Runtime offers bounded sequential quanta; training stages one or two contiguous ring segments, verifies pointer order, and falls back before mutation on mismatch or sensory boundaries. The graph advances input and spike-row slots. | Revisit per-token host copies only if a future input producer cannot provide stable tensor order or if complete-tick parity demonstrates the ring boundary is unsafe. |
| Separate dense reconstruction scan inside persistent text tick | retired | Eligible graph ticks already compute exact route scores over the same 1024-column device cache. Keeping an additional dense prototype scan duplicated fixed-shape GPU work before neuromodulation. A pinned-clean-HEAD 1024-sample comparison measured `796.219` versus `630.168 tokens/sec` on quantum-staged complete encoded ticks, with `1088` fused reconstruction updates, zero graph failures, and zero staged-input mismatches. | Derive graph-owned reconstruction error from `fused_route_score_max`, update neuromodulators from that device scalar, and expose reconstruction source/update counts in Runtime Truth. Retained, sensory, and graph-ineligible paths keep their existing fallback behavior. | Revisit only if route-cache semantics diverge from competitive prototypes or bounded reconstruction/cognitive-quality gates show route-score reconstruction is unsafe. |
| Sixteen-token continuous execution quantum as per-token fallback | retired | The earlier q16 experiment was correctly rejected because Brain Runtime delegated only eight-token quanta to training-owned sequence execution. A configured q16 quantum bypassed the burst path and fell through to retained per-token `train_step`, measuring `602.249 tokens/sec` versus q8 at `638.394`. | Brain Runtime delegates wider no-yield quanta to `MarulhoTrainer.train_text_sequence`, and training internally consumes them as exact ordered eight-token persistent CUDA bursts. The maintained default is now `execution_quantum_tokens=16`; after the 32-token truth/event cadence promotion, the 131072-token CUDA stress reached `4577.595 tokens/sec`, `train_compute=0.181855 ms/token`, `8192` training-owned quanta, and zero graph/burst failures. | Reintroduce a q16 rejection only if the wide quantum again loses the burst path, weakens stop responsiveness, or repeated long-run complete-runtime evidence falls below the q8 path. |
| Full-warm source-cache tail rewrites | retired | A 1024-token full-warm continuous-runtime stress run showed the source-cache worker scheduled a new cache write after every 128-token tick as the already cached source queue drained, leaving `8` writes for one measured run. The writes were asynchronous, but the hot path still rebuilt/hashed shrinking raw-window material during `prepare_training` and the worker consumed disk I/O for snapshots that were strictly worse than the full cache. | Keep the full cached material once `cache_material_token_count` reaches the configured queue target. Subsequent partial-tail material is skipped and exposed through `cache_partial_skip_count`; recovery still persists partial material when no cache exists. The same stress surface rose from `686.196` to `711.113 tokens/sec`, `prepare_training` fell from `5.6861` to `1.8056 ms/tick`, and cache writes fell from `8` to `1` with zero failures. | Revisit only for an explicit resumable-stream checkpoint policy that proves partial-tail cache snapshots improve recovery enough to justify complete-runtime cost and disk traffic. |
| Unconditional post-transition assembly clone | retired | `train_step` cloned the assembly after every column transition before checking whether the optional abstraction layer existed. The current 1024-column speed checkpoint has `enable_abstraction_layer=false`, so the allocation and device copy had no consumer on the promoted path. | Clone the assembly only inside the enabled abstraction-layer branch; abstraction-enabled semantics and tests remain unchanged. A profiled 8192-token CUDA stress run completed 8192 graph replays with zero failures and measured `column_transition=0.093578 ms/tick`, but clean 32768-token endpoint repeats remained below the prior best, so this is dead-work cleanup rather than a broad throughput claim. | Reintroduce an unconditional snapshot only if another live consumer proves it requires pre-abstraction transition state and complete-runtime evidence justifies the copy. |
| Full-quantum pre-staging across known burst fallback boundaries | retired | After q16 quantum staging, training could copy a whole metric-free quantum before the second eight-token burst discovered a sleep or other fallback boundary, producing more staged than consumed tokens (`32784` staged for `32768` consumed) and wasting CUDA input-copy work. | Training now classifies each burst-sized slice with the same boundary logic before full-quantum staging, but without mutating Runtime Truth counters. Only fully device-continuous q16 windows are pre-staged; other windows fall back to per-burst or per-token staging before mutation. The follow-up 32768-token CUDA run reported exact `32768` staged and reused tokens with zero discards. | Revisit only if the preflight overhead regresses an uncontended long run and a replacement proves exact staged/consumed accounting, fail-closed boundary behavior, and complete-runtime speed. |
| Host-truth synchronization intervals above 16, before event-capacity widening | superseded experiment | Shorter evidence made interval 32 look promising, but wider intervals did not hold up as a sustained default when the device event queue still forced earlier drains. Config-only 8192-token runs measured interval 32/64/128 at `1530.180`/`1267.956`/`1098.737 tokens/sec`; the clean 32768-token interval-32 run reached `1560.459`, below the retained interval-16 clean reference at `1779.859`. | The superseding path widens the Deferred Device Event Queue to 32 tokens, skips empty host event-apply calls, and promotes `cuda_graph_host_truth_sync_interval_tokens=32`; the 131072-token run reached `4577.595 tokens/sec` with all transitions on CUDA and zero graph/burst failures. | Revisit only through repeated long-run same-checkpoint A/B evidence that improves complete runtime while preserving bounded Runtime Truth freshness and exact device-owned surprise updates. |
| Duplicate graph-path consolidation lookup and empty revival allocation | retired | Graph eligibility already validated the captured consolidation tensor by generation, but downstream transition bookkeeping reacquired it from `DualMemoryStore` and allocated a fresh empty CUDA revival-id tensor on every token. These operations did not change cognition or Runtime Truth content. | Graph ticks skip the duplicate lookup and reuse a persistent empty tensor; retained/sensory and explicit structural-maintenance paths keep normal ownership. Profiled bookkeeping fell from `0.054251` to `0.014375 ms/tick`; repeated 32768-token CUDA runs reached `2169.815` and `2191.057 tokens/sec` with zero failures and 32768 skip/reuse events each. | Reintroduce only if graph eligibility no longer protects consolidation identity or a real per-token revival payload exists; require parity and repeated complete-runtime evidence. |
| Eight-tick monolithic CUDA graph | rejected experiment | Capturing eight complete tick bodies into one larger graph looked like a direct launch-reduction path, but it duplicated graph scheduling and required per-tick evidence copies. A prior 32768-tick hot-window run regressed to `0.951x` (`6748.34` versus `7097.98 ticks/sec`). A later same-checkpoint 32768-token complete CUDA service run also rejected the idea: focused parity passed, but throughput fell to `2249.691 tokens/sec` with `train_compute=0.372436 ms/token`; reverting to the retained burst executor restored `3171.826 tokens/sec` and `train_compute=0.273302 ms/token`. | Keep the proven one-tick graph plus Boundary-Aware Text Burst. The next speed target is a real fused/persistent kernel or lower-cost CPU evidence drains, not nesting ordinary graph replays into a larger graph. | Revisit only through a lower-level device-resident loop or conditional graph implementation that beats sustained one-tick replay without adding evidence-copy traffic or weakening rollback/truth boundaries. |
| Native host loop over `cudaGraphExec_t` | rejected experiment | Moving the per-token graph replay loop from Python into C++ still called `cudaGraphLaunch` once per token. The long recorded comparison lost: `reports/native_graph_replay_20260614/stress-131072-native.json` reached `4159.316 tokens/sec`, while the disabled Python replay comparison reached `4347.554 tokens/sec`. | Use the promoted repeated-child parent CUDA graph, which launches one parent graph per eight-token burst and measured `4671.202 tokens/sec` over the 131072-token long gate with zero native fallbacks/failures. | Revisit only if a native loop becomes a true device-resident sequence executor or if repeated uncontended evidence beats the parent-graph path while preserving exact sequential state and fail-closed Runtime Truth. |
| Direct mixed `.cpp` + `.cu` PyTorch JIT for the conditional graph executor on Windows | rejected implementation path | The first native conditional-WHILE sequence prototype could not safely rely on PyTorch's normal mixed-source extension build on this machine. Direct `load()` over `.cpp` plus `.cu` failed in the Windows CUDA toolchain while emitting the CUDA object, and a manual object-link probe then failed until the CUDA object was compiled with the same dynamic CRT runtime as PyTorch. Treating that fragile build path as production would put the promoted hot path at startup risk. | The conditional sequence executor keeps its own lazy Windows loader: it precompiles the `.cu` helper to a stamped `.obj` with `/MD`, links that object through the PyTorch C++ extension build, and leaves the retained native repeated-child replay as the default fallback. | Revisit only if the Windows CUDA/PyTorch extension path reliably builds mixed `.cu` sources without object or CRT mismatch on clean machines, and only with startup/capture cost still reported outside warm throughput. |
| Full train-step metrics for prepared source concept samples | retired | Source concept sampling requested full metrics at sampled positions and the final token, which forced otherwise ordinary eight-token burst quanta through individual `train_step` calls. A 131072-token complete CUDA run before the change executed `116680` burst-owned tokens with `train_compute=0.259876 ms/token`; after using device-burst lightweight metrics, burst-owned tokens rose to `126952`, `train_compute` fell to `0.241568 ms/token`, and throughput rose from `3359.378` to `3565.968 tokens/sec`. | Ordinary prepared source ticks use final Device-Burst Lightweight Metrics from the CUDA result packet. Full metrics remain available only for explicit evaluator evidence positions, retained/fallback paths, or future consumers that prove cognitive value and cost. | Revisit only if source concept observation gains a real slow-memory-backed consumer for those exact sampled tokens and long complete-service evidence shows the richer metrics are worth the burst loss. |
| Fixed-cadence slow-memory admission as a burst fallback | retired | The `slow_memory_boundary` fallback forced routine 256-token cadence samples through retained per-token `train_step`, even when no strong/surprising replay event existed. The prior 131072-token run still had `512` slow-memory fallbacks and reached `3565.968 tokens/sec`. | Fixed cadence is deferred maintenance, not hot-path replay admission. First-token retained/fallback records and strong-capture Device Strong-Event Ring admissions remain exact. The promoted 131072-token run reached `3901.906 tokens/sec`, `train_compute=0.219858 ms/token`, `131056` burst-owned tokens, `512` deferred cadence events, and zero graph/burst failures. | Reintroduce cadence admission only through a slow replay/consolidation window or with replay-quality evidence proving fixed cadence beats surprise/coverage-based selection enough to justify complete-runtime cost. |
| Exact fixed-cadence archive payloads in the burst event ring | rejected experiment | Extending the burst event ring to store routing/assembly payloads for cadence tokens removed slow-memory fallbacks, but the added payload stores/staging slowed the complete service path. Two 32768-token variants measured `3052.470` and `3145.964 tokens/sec`, below the retained lightweight-metrics run at `3203.188` and the promoted cadence-deferred run at `3341.821`. | Keep the result-only/strong-capture event ring on the hot path. Routine fixed cadence is deferred; richer archive payload capture belongs in an explicit slow replay/consolidation path or a future device-resident design that wins sustained service evidence. | Revisit only if a sparse device event queue can capture selected cadence/coverage samples without adding launch/copy/store overhead and beats the retained path on long complete-service benchmarks. |
| Previous-result strong-capture burst gate | retired | Burst eligibility originally inspected only the last host-mirrored reconstruction error. A later token inside the eight-tick burst could cross the strong-capture threshold, so this gate could silently skip a real replay-memory event. | The Device Strong-Event Ring snapshots every burst result on CUDA, records only threshold-crossing assembly/routing payloads, and drains them once at the host-truth boundary into CPU archival memory. | Revisit only if strong-memory admission is removed entirely or a broader device event queue proves exact event preservation with lower sustained complete-runtime cost. |
| Immediate device-event drain after every successful text burst | retired | The original eight-row Device Strong-Event Ring called `.tolist()` after every successful burst, so the first half of a configured sixteen-token host-truth interval still synchronized after eight tokens. The prior 32768-token Runtime Truth sample executed only `9760` burst-owned tokens and recorded `1033` `host_truth_boundary` fallbacks. | A fixed thirty-two-token Deferred Device Event Queue retains up to four eight-token bursts and drains at the promoted truth boundary. CPU fallback and service tick completion force a bounded trainer-owned drain. Exact CUDA trajectory/strong-event tests pass; the long 131072-token run reached `4577.595 tokens/sec`, all transitions executed on CUDA, and graph/burst failures remained zero. | Revisit an immediate eight-token drain only if bounded Runtime Truth freshness must be below thirty-two tokens or if repeated complete-runtime evidence shows the smaller queue wins without losing event correctness. |
| Trainer-stage profiling as a burst fallback | retired | `--profile-trainer-stages` used to set `trainer_profile_enabled`, and `train_text_burst()` treated that flag as a burst-fallback reason. The profiler therefore measured per-token `train_step` fallback costs instead of the promoted CUDA burst path, making the next bottleneck easy to misread. | Trainer profiling now keeps successful text bursts active and records burst-level buckets for boundary planning, host-truth gating, graph replay, pending metadata, event/idle handling, and CPU maintenance. Focused CUDA tests require burst execution under profiling, and the 8192-token promoted-path profile recorded `8184` burst-owned tokens with zero graph/burst failures. | Reintroduce profiling fallback only for an explicit retained-path diagnostic flag; default speed profiling must observe the promoted executor. |
| Coarse-only burst graph replay profiling | superseded diagnostic | The first promoted-path profile correctly proved that burst execution stayed active under profiling, but its `text_burst_graph_replay` bucket still wrapped eligibility checks, input staging, control-state staging, repeated graph replays, Python mirrors, and event drains. That made the next architecture target too broad. | Runtime sub-buckets now split burst eligibility, quantum input staging, control-state staging, replay loop, Python mirrors, event drain, and event defer under the same opt-in trainer profiler. The 8192-token subprofile showed event drain at `0.153854 ms/token`, replay loop at `0.115524`, and input staging at `0.037902`, all on the promoted CUDA burst path with zero graph/burst failures. | Revisit a coarse-only profile only for a top-level sanity check; architecture decisions should use the runtime sub-buckets plus clean unprofiled promotion runs. |
| Full result-row materialization on every burst event drain | retired | The deferred event ring copied every pending result row into Python on each cadence drain even when no strong replay event existed. Ordinary no-strong drains only need the final Runtime Truth row and bounded strong flags; exact per-token row evidence is only needed for actual strong captures. | Use the Slim Burst Event Packet: one final result row plus strong-result rows for strong indices only. Focused CUDA tests preserve forced strong captures, and the 8192-token profile reduced `text_burst_runtime_event_drain` from `0.153854` to `0.056869 ms/token` with `256` slim packets and zero strong rows. | Reintroduce full result-row lists only if a CPU-owned consumer proves it needs every per-token row at cadence drains and long complete-runtime evidence pays for the extra host materialization. |
| CPU strong-flag scan on every no-strong burst drain | retired | After the slim packet change, no-strong drains still copied the bounded strong-flag vector to Python even when no strong replay-memory event existed. This kept a host materialization step in the common truth-drain path. | The in-place transition kernel maintains a device-owned cumulative strong-event count. Host drains read the count, skip CPU flag scans for no-strong windows, and still materialize flags/payload rows for actual strong captures with count/flag agreement checks. The 131072-token validation skipped `4096` no-strong flag scans with zero graph/burst failures. | Reintroduce unconditional flag scans only if the device count regresses sustained complete runtime or a strong-capture correctness test proves the count cannot protect archival payload materialization. |
| Unconditional no-strong burst payload loads | retired | The Device Strong-Event Ring masked routing/assembly payload stores for no-strong ticks, but the Triton packet publisher still loaded the full routing key and assembly before discarding them. That preserved correctness but kept archival-payload memory traffic on every ordinary burst tick even when Runtime Truth reported zero strong rows. | Predicate routing/assembly payload loads and stores on the strong-event condition. The result row and strong flag still update every packet; strong-event captures still preserve exact routing/assembly payloads. Focused CUDA tests prove no-strong sentinel rows stay untouched and strong rows copy exact payloads. A 131072-token clean stress run preserved the high-throughput path at `4438.669 tokens/sec` with zero graph/burst failures, below but close to the retained `4577.595` top. | Reintroduce unconditional payload loads only if a future device event format proves predicated loads regress sustained complete runtime or a real hot-path consumer needs no-strong payload rows. |
| Standalone burst-event snapshot Triton kernel | retired | The persistent burst graph launched the in-place transition and then launched `snapshot_burst_event_cuda` to publish the result row, strong flag, and optional archival payload. That kept an extra device launch in every ordinary burst tick even though the transition kernel already had the winner, assembly, routing key, neuromodulator state, effective modulator, and competitive-surprise values. | The in-place transition kernel now publishes the Slim Burst Event Packet and optional strong payload directly, advances the device event slot, and exposes full-capacity slot reset skips through Runtime Truth. Focused CUDA tests cover no-strong sentinel preservation, forced strong payload capture, partial forced drains, and natural full-capacity slot wrap. The 131072-token stress run stayed on CUDA with zero graph/burst failures and `4094` reset skips, but reached `3923.410 tokens/sec` under recorded CPU contention, below the retained `4577.595` top. | Reintroduce a separate snapshot kernel only if a future packet format cannot safely be written by the transition kernel and long uncontended complete-runtime evidence beats the fused path. |
| Sixty-four/one-hundred-twenty-eight-token host-truth/event cadence default | rejected experiment | Widening the device event queue and host-truth interval reduced drain count but not complete runtime. The fresh 64-token long run cut drains from `4096` to `2049` and had no observed contention, yet reached only `4402.958 tokens/sec`; the retained 32-token repeat reached `4771.221`, and the refreshed native base comparison reached `4992.049`. The 128-token path also lost earlier long evidence (`3973.773`) and the fresh 32768-token recheck (`3832.220` versus `4177.463` for 32). | Keep `cuda_graph_host_truth_sync_interval_tokens=32` and the fixed 32-token burst event capacity. Exact graph-owned CUDA state still updates every replay; host truth remains sampled. Do not keep a configurable hot-path event-capacity knob without a promotion win. | Revisit only with a device-resident or asynchronous truth publisher that beats long complete-runtime evidence while preserving bounded Runtime Truth freshness, strong-event correctness, stop/flush behavior, and fail-closed fallback. |
| Compact/shared Python burst-event metadata as a speed fix | rejected experiment | Replacing per-token pending burst-event tuples with a compact pending window preserved focused strong-event tests but slowed two clean 32768-token CUDA service runs to `3551.811` and `3334.081 tokens/sec`. Narrowing the change to share one metadata dict while keeping tuple shape still measured only `3543.149 tokens/sec`, below the retained interval-32 evidence (`4237.534` on 32768 tokens and `4577.595` on 131072 tokens). The Python allocation cleanup was not the dominant host cost. | Keep the proven pending event tuple path and focus on broader persistent executor work: graph replay cost, routing preparation, boundary ownership, and source/finalize stages. | Revisit burst metadata only with paired same-process or long complete-runtime evidence that beats the retained path while preserving exact strong-event archival payloads. |
| Bounded CUDA column Runtime Truth export as current default | rejected experiment | A device-side scheduler export reduced a 1024-column five-tensor status materialization from `20480` bytes to `400`, but warmed CUDA latency was worse (`15-20 ms`) than the retained latency-first full report (`6.3695/8.6799 ms` median/p95) at current sizes. Smaller host materialization did not beat fewer synchronization boundaries. | Keep CUDA column Runtime Truth latency-first with one full snapshot for current status reads. CPU reports may still materialize bounded samples, and bounded CUDA export remains a future large-column experiment. | Revisit only with larger-column benchmarks showing bounded export wins latency, not just bytes, and without weakening Runtime Truth evidence. |
| Exploration noise/reset scalar and burst gate | retired | The trainer multiplied and decayed `_exploration_noise_scale`, checkpointed `last_network_reset_token`, emitted exploration metrics, and rejected CUDA bursts when the CPU norepinephrine mirror crossed a threshold. Repository-wide inspection found no consumer of the scale in routing, plasticity, curiosity, action, or any other cognitive update, so the path imposed fallback cost while only reporting decorative state. | Delete the unused scale, reset token, checkpoint field, metrics, `should_boost_exploration()` helper, and `exploration_boundary`. Keep real graph-owned norepinephrine/surprise dynamics. Future exploration must enter through a measured consumer with explicit cognitive effect and Runtime Truth. | Reintroduce only as a concrete local mechanism that changes a named routing/plasticity/action signal, has bounded device/CPU ownership, and wins cognitive-quality plus sustained-throughput gates. |
| Telemetry, drift refresh, and drift-floor closure as per-token burst fallbacks | retired | Observation cadence and CPU archival-memory maintenance forced an entire eight-token quantum through individual `train_step` calls. In the prior 32768-token run, telemetry and drift refresh caused `373` and `447` fallbacks, while drift-floor closure added another control boundary. | The Device-Owned Cognitive Boundary Controller defers telemetry and separates drift maintenance from execution gating. Drift refresh is sync-free on burst ticks, using global drift when the winner mirror is stale; drift-floor closure no longer forces a CUDA event drain. A 131072-token run reached `4045.419 tokens/sec`, executed `131056` burst-owned tokens, performed `2620` sync-free drift refreshes, reduced forced burst-event drains to `0`, and had zero graph/burst failures. | Revisit a per-token fallback only if a maintenance operation is proven to alter the current token's neural transition and cannot be expressed before mutation or as sync-free CPU maintenance. |
| Per-token Python loop for cognitive boundary classification | retired | The Device-Owned Cognitive Boundary Controller still scanned every token in a proposed burst to discover telemetry, drift, slow-memory cadence, sleep, and routing-index boundaries. This was host planning work, not neural computation. | Classify burst boundaries with range arithmetic over the proposed token interval. Focused tests compare the range classifier against the previous loop semantics, Runtime Truth exposes `classification_mode=range_arithmetic`, and the 8192-token profile kept the promoted CUDA burst path active with zero graph/burst failures. | Reintroduce a per-token planning loop only if a new boundary cannot be represented as arithmetic over the interval and complete-runtime evidence proves the loop cost is acceptable. |
| Drift refresh event drain for fresh winner-local drift | retired | Burst drift refresh flushed pending CUDA event evidence so CPU drift computation could use the latest winner id. This made a maintenance scalar force host truth. The previous 131072-token run still had `1310` forced burst-event drains and `9503` host-truth syncs. | Use winner-local drift only when the host winner mirror is already fresh; otherwise compute global drift and expose `drift_refresh_global_count`. The promoted run reduced host-truth syncs to `8193`, forced drains to `0`, and improved sustained throughput from `3901.906` to `4045.419 tokens/sec`. | Reintroduce only if winner-local drift is proven to drive an immediate mutation that global drift cannot support, and long complete-runtime evidence shows the sync cost is worth it. |
| Always-on context/binding quick-start preset | retired | The control-room quick-start preset enabled context and binding layers on startup. `train_text_burst` rejects those always-on higher layers and falls back to retained per-token execution, so the UI run measured a slower system than the promoted CUDA burst path and could make stop/status behavior look stuck under live source work. | The curriculum preset now keeps context and binding off the hot path, configures ingestion prewarm, and uses the promoted 128-token tick with 16-token execution quantum. Higher layers return only through a conditional measured scheduler that proves wake cost, cached state, and Runtime Truth evidence. | Revisit only with a column/specialist scheduler that wakes context or binding conditionally, preserves burst execution for ordinary ticks, and wins sustained complete-runtime benchmarks plus grounding-quality checks. |
| Sixteen-token Python burst group as launch reduction | rejected experiment | Raising the preferred persistent burst group from 8 to 16 reduced host burst groups (`1023` to `511` in an 8192-token profile) but did not reduce the underlying one CUDA Graph replay per token. The clean 32768-token complete-runtime gate reached only `2283.710 tokens/sec`, far below retained interval-32 evidence, with `train_compute=0.379761 ms/token`. | Keep the maintained eight-token device burst boundary while the trainer queries runtime capacity instead of hard-coding its own chunk size. The next launch-reduction path must be a lower-level fused or persistent device-owned multi-tick executor, not a wider Python replay loop. | Revisit only with sustained complete-runtime evidence showing fewer actual graph/kernel launches, exact sequential trajectory, bounded Runtime Truth freshness, stop responsiveness, and zero graph/burst failures. |
| Per-burst/per-quantum input staging as the default source-sequence policy | retired | Once service delegated q16 text sequences to training, repeated eight-token or sixteen-token input staging still fragmented an already ordered source tick and kept avoidable CPU staging work in `train_compute`. | Warm metric-free text sequences pre-stage the longest safe boundary-clean segment up to the 128-token CUDA input ring; each eight-token burst consumes pointer-checked staged slices, and host-truth/sleep/metrics boundaries stop the segment before mutation. The same-process 16384-token A/B measured `1.119x` over per-quantum staging, and the 65536-token service-style run proved active sequence staging with zero graph/burst failures. | Revisit narrower staging only if boundary-aware sequence staging weakens stop responsiveness, mismatches input pointer order, or uncontended long-run evidence shows the narrower stage wins. |
| Execution quantum above 16 as the default | rejected experiment | Clean short runs made q32 look competitive, but the long 131072-token q32 run reached `3735.329 tokens/sec`, below the retained q16 long evidence at `4247.306 tokens/sec`. Clean 32768-token q64 also measured `3178.163`, below q32 short and not a promotion. | Keep `execution_quantum_tokens=16` as the maintained default while future wider/device-owned schedulers remain benchmark-only. | Revisit only with uncontended long complete-runtime evidence beating q16 while preserving stop latency, exact sequential ordering, CUDA burst success, and Runtime Truth freshness. |
| Per-tick full prepared-source reconstruction and hash | retired | Even after asynchronous persistence and partial-tail skipping, every 128-token warm tick copied up to the entire 131072-window queue into a Python list and JSON-hashed it before proving the cache was unchanged. The prior long run spent `0.156315 ms/token` in preparation. | Runtime Sources treats consumption-only prepared material as one immutable generation and skips before queue traversal. Refill and stream replacement explicitly mark material changed. The 131072-token run recorded `1024` stable-generation skips and reduced preparation to `0.008415 ms/token`. | Revisit full scanning only for a real material mutation or integrity audit on an explicit slow path; ordinary consumption must remain O(1). |
| Per-quantum service dispatch of neural text execution | retired | Brain Runtime reacquired orchestration control for every eight-token quantum, selected burst versus retained execution, staged fallbacks, flushed events, and marked mutation repeatedly. This duplicated training policy in service and fragmented the enclosing workflow. | Brain Runtime submits one complete tick to `MarulhoTrainer.train_text_sequence`; training owns ordered quanta, metric positions, burst/fallback selection, event drains, and between-quantum stop checks. Service keeps source, lock, semantic observation, and Runtime Truth responsibilities. The long run completed `1024` sequence calls and improved sustained throughput to `3359.378 tokens/sec`. | Reintroduce per-quantum service dispatch only if a service-owned external event must intervene within a quantum and cannot be represented as a bounded training input or cancellation boundary. |
| Direct one-block route/vote Triton fusion for the live 1024x64 cache | rejected experiment | The candidate removed one visible route-score launch by folding score computation, top-k selection, and vote into one Triton program for the live exact-cache shape. CUDA parity passed, but complete-runtime evidence regressed: the 8192-token profiled direct run measured `2381.587 tokens/sec` versus `2408.630` before direct selection, and the 32768-token direct clean run measured `2266.882` versus `2359.929` after reverting to the retained two-stage route/vote path. | Keep the retained `two_stage_route_vote` family and expose `route_vote_kernel_variant` in Runtime Truth. The direct kernel body was deleted so the hot path has no dormant rejected code. | Revisit route/vote fusion only as part of a lower-level device-owned multi-tick executor or persistent sequence kernel that lowers the real per-token graph/kernel launch boundary and wins long complete-runtime evidence. |
| Partial native parent-graph replay as the default | rejected and removed | Lazily constructing native repeated-child parent graphs for non-eight-token burst tails removed the partial Python replay fallback locally, but a complete 16640-token `tick_tokens=130` run regressed to `2786.829 tokens/sec` versus `2907.600` with partial native replay disabled. It also compiled new parent graph counts inside the measured tick and did not address the real slowdown: `384` host-truth burst fallbacks from an unaligned tick width. | Keep exact eight-token native parent replay as the only maintained repeated-child native capacity. The live runtime no longer reads `MARULHO_CUDA_GRAPH_NATIVE_PARTIAL_BURST_REPLAY`; non-eight-token tails use the explicit Python-loop fallback and report `python_loop_partial_disabled`. | Revisit only as part of a startup-warmed or lower-level device-owned multi-tick executor that avoids hot-path compile, preserves aligned truth/event boundaries, and wins long complete-runtime evidence. |
| Startup-warmed sixteen-token native parent graph as the default | rejected and removed | The exact 16-token parent graph was warmed at startup, preserved sequential state, exposed Runtime Truth capacity and `[16]` parent coverage, halved parent launches in the contended 8192-token profile pair, and covered `131040` tokens with zero native/graph/burst failures in the clean 131072-token run. It still reached only `4887.767 tokens/sec` with `train_compute=0.168278 ms/token`, below the retained exact eight-token native base at `4992.049` and `0.166575`. | `cuda_graph_native_burst_tokens` is fixed at `8`; checkpoint load migrates old `16` or `32` values back to `8` with `retired_native_burst_capacity_prototype`; the env and stress-benchmark capacity override surfaces were removed. | Revisit only through a lower-level device-owned sequence executor or persistent C++/CUDA/Triton/hybrid kernel that reduces the real Python/CUDA Graph replay boundary, preserves exact sequential state and fail-closed fallback, and wins a clean long sustained run. |
| Promoting conditional-WHILE by changing native burst capacity | rejected implementation path | The winning executor is conditional-WHILE q16, not sixteen-token repeated-child native replay. Using `cuda_graph_native_burst_tokens=16` as the default would silently promote the rejected native16 fallback path and weaken the evidence boundary between sequence-loop ownership and repeated-child graph capacity. | Split capacity ownership remains explicit: `cuda_graph_sequence_loop_tokens=16` owns the promoted conditional-WHILE sequence loop, while `cuda_graph_native_burst_tokens=8` is the fixed repeated-child internal fallback capacity. Runtime Truth reports both. | Revisit only if repeated-child native16 itself beats the promoted conditional default in clean long evidence and passes the same fallback/parity gates, and only by introducing a new reviewed executor path rather than restoring the old config/env knobs. |
| Conditional-WHILE q8/q32 sequence-loop capacity knobs | rejected and removed | Conditional-WHILE q8 was useful promotion evidence and q16 became the winner. Wider sequence-loop capacity is a new execution-boundary decision, not a runtime tuning knob; leaving `cuda_graph_sequence_loop_tokens`, `MARULHO_CUDA_GRAPH_SEQUENCE_LOOP_TOKENS`, or `--sequence-loop-tokens` live would preserve a second path around the promoted q16 scheduler evidence. | `cuda_graph_sequence_loop_tokens` is fixed at `16`; checkpoint load migrates old non-16 values back to `16` with `retired_sequence_loop_capacity_prototype`; the env and stress-benchmark capacity override surfaces were removed. | Revisit only as a new lower-level executor or execution-quantum decision with parity gates and clean long complete-runtime evidence beating the current q16 path. |
| CUDA sequence executor selector | retired and removed | After conditional-WHILE q16 became the promoted path, keeping `cuda_graph_sequence_executor`, `MARULHO_CUDA_GRAPH_SEQUENCE_EXECUTOR`, or stress-benchmark `--sequence-executor` as live selectors preserved a second path around the evidence gate. That made native8 look like an operator choice rather than a fail-closed fallback. | `cuda_graph_sequence_executor` is fixed at `conditional_while`, old checkpoint selector values migrate with `retired_sequence_executor_selector`, the env/CLI override surfaces are removed, and repeated-child native8 remains available only as internal pre-mutation fallback when conditional construction is unavailable. | Revisit only by introducing a new reviewed executor path with parity gates and clean long complete-runtime evidence, not by restoring the old selector. |
| Native32 parent graph under q16 execution quantum | rejected invalid benchmark and removed | The exact fast shape keeps `execution_quantum_tokens=16`, so the former `--native-burst-tokens 32` probe warmed `[32]` parent graphs but never launched them for token coverage. Runtime Truth in `reports/native_burst_sequence_20260615/native32-131072-i32.json` reported `native_burst_replay_success_count=0`, `native_burst_replay_fallback_count=8190`, `native_burst_replay_python_loop_token_count=131040`, and backend `python_loop_partial_disabled`; the run was also GPU-contended, so its `4454.287 tokens/sec` throughput is not promotion evidence. | The stress benchmark no longer accepts repeated-child native burst capacity overrides; exact eight-token native replay is the only maintained fallback capacity. | Revisit native32 only with an explicit execution-quantum boundary decision and long-run evidence, or as part of a lower-level sequence executor that no longer depends on Python q16 chunking. |
| Filtering deep-sleep candidates after CUDA graph/fused route-vote selection | retired implementation shape | CUDA graph/fused route-vote had already selected the candidate set and winner inside its own route boundary. Filtering that tensor afterward would make Runtime Truth claim a sleep-aware wake mask while the selected winner came from the unfiltered set. | The CUDA route-vote owner now masks deep-sleep route rows inside `core.fused_route_vote_cuda` before route top-k vote and winner selection, writes `route_vote_deep_sleep_filter.v1`, and lets training build the `ColumnWakePlan`. | Reintroduce post-selection sleep filtering only for offline diagnostics; live scheduler truth must filter before selection or report fallback. |
| Filtering memory-pressure candidates after CUDA graph/fused route-vote selection | retired implementation shape | The CUDA graph/fused route-vote owner has already selected the candidate set and winner. Filtering high-pressure candidates afterward would make Runtime Truth claim a pressure-aware wake mask while winner selection and route vote still came from the unfiltered set. | The CUDA route-vote owner now masks high-pressure route rows inside `core.fused_route_vote_cuda` before top-k candidate and winner selection, but only when `ColumnMetabolismState` has real cached pressure evidence. The retained CPU route still filters only bounded retrieved candidates. | Reintroduce post-selection pressure filtering only for offline diagnostics; live scheduler truth must filter before selection or report fallback. |
| All-column memory-pressure census to decide candidate sleep | rejected implementation shape | Scanning `ColumnMetabolismState.memory_pressure` across every column just to decide whether a bounded candidate filter should run would add the all-column hot-path tax the scheduler goal forbids. It also revives the same class of status-driven sleep decision the wake-plan boundary removed. | The trainer applies the retained pressure filter only to retrieved candidate IDs, and the CUDA route owner applies pressure only from cached evidence already maintained by `ColumnMetabolismState`. Missing memory-store bucket evidence is reported as `no_memory_store_bucket_evidence`; the pressure gate is disabled instead of fabricating pressure or scanning for it. | Revisit only with a device-owned aggregate pressure signal maintained outside the tick, or a fused candidate router that proves no all-column wake tax with long complete-runtime evidence. |
| CUDA route-vote memory-pressure filtering without cached pressure evidence | rejected implementation path | Reading `ColumnMetabolismState.memory_pressure` across every route row when the source is `not_run` or `no_memory_store_bucket_evidence` would add route-owner memory traffic while filtering nothing. It would also make Runtime Truth look pressure-aware without pressure evidence. | `ColumnTransitionRuntime` enables the CUDA pressure gate only when the pressure source proves cached evidence exists; otherwise the scheduler-filter packet reports memory-pressure disabled. | Revisit only if a device-owned pressure source can be maintained outside the measured tick and long complete-runtime evidence proves the extra route-row read is neutral or better. |
| Route-vote sleep filter with per-host-truth state sync | rejected implementation path | The first fused route-vote sleep-filter long run proved the execution effect but synced the small filter state on every host-truth mirror, recording `4097` state syncs and only `5930.322 tokens/sec` with `train_compute=0.139577 ms/token`. The count is Runtime Truth evidence, not transition input. | Mirror `route_vote_deep_sleep_filter.v1` on a coarser graph cadence; the follow-up 131072-token run synced `129` times and reached `6135.026 tokens/sec` with no observed contention. | Lower the cadence only if a live scheduler decision consumes the mirrored count and long complete-runtime evidence stays neutral or better. |
| Partial awake backfill lane inside fused route-vote sleep filtering | rejected implementation path | Backfilling a filtered route result inside the fused route-vote path would add more selection/control work for the rare case where fewer than `k` awake route rows remain, while the retained fixed-k unfiltered fallback is truthful and bounded. | If eligible route rows are fewer than the requested candidate count, keep the unfiltered fixed-k route result and report `insufficient_awake_route_scores_after_deep_sleep_filter`; do not pretend sleep was applied. | Revisit only with a fused device-side backfill implementation that beats the fallback path in complete-runtime evidence and preserves winner/state parity. |
| Forced CPU structural-review ticket capture as a speed promotion | rejected evidence claim | The current forced 8192-column `column_scheduler_benchmark` report proves bounded growth plus prune/sleep ticket capture and operator-reviewed continuation fields, but the forced audit runs after measured timing and the scoped forced arm regressed mean/median/p95 complete-step latency from `7.11954625/6.6511/12.3495 ms` to `12.20728125/9.28945/32.3889 ms`. Calling that a speed win would confuse a Runtime Truth/coverage promotion with throughput evidence. | Keep `ColumnStructuralReviewQueue` in the scheduler truth contract, use forced evidence only to prove bounded growth/prune tickets, checkpoint/operator-review flags, and `runs_all_columns=false`; rely on cadenced CUDA capture plus longer real-path gates for throughput claims. | Revisit only if a retained CPU or GPU-owned structural-review capture path preserves winner/state correctness, stays bounded without all-column scans, and wins repeated complete-runtime benchmarks. |
| Unbound isolated structural deltas as transaction readiness | retired evidence shape | A pre/post topology delta plus rollback policy could make isolated structural evaluation look ready without proving which repeated-failure or prune/sleep candidate caused it, what exact baseline was reviewed, what usefulness/cost/latency/RAM/VRAM impact was observed, or whether evaluation itself avoided mutation. | Isolated structural-plasticity evaluation now includes a checkpointed-candidate gate that binds training-owned ticket evidence, baseline hash, candidate reason, cost/usefulness and latency/RAM/VRAM impact, Runtime Truth summary, rollback artifact, and no-mutation proof before reviewed transaction design can be considered fully evidenced. | Revisit only if a future evaluation artifact binds equivalent candidate provenance and no-mutation proof under a stronger transaction model. |
| Candidate provenance outside structural application preflight hash | retired evidence shape | A structural preflight could carry candidate evidence fields in its design binding while the executor recomputed the application preflight hash from only target/reason/budget/revision/checkpoint fields. That left candidate provenance indirectly trusted through earlier gates instead of bound at the final executor boundary. | Structural Mutation Preflight and Structural Mutation Application now both include checkpointed candidate gate status, candidate evidence hash, baseline hash, reason, and cost-impact hash in the preflight hash material. The executor returns candidate provenance plus a tombstone/rollback artifact for blocked, rejected, over-budget, no-delta, tampered, or failed-commit outcomes. | Revisit only if a stronger transaction envelope binds equivalent candidate provenance, rollback, and tombstone evidence and focused tests prove tampered candidate fields fail closed before mutation. |
| Input-unbounded service replay-plan construction | retired | `build_replay_plan(...)` returned a capped candidate plan, but it still materialized every runtime episode, action, prediction, uncertain domain, and recent-feedback item before ranking. On a synthetic `20000`-row-per-stream payload this measured `6860.919 ms` mean latency, and the shape would not scale to LLM-size runtime histories even though it did not mutate neural state. | Replay planning now reports `bounded_replay_plan_source_window.v1`: `64` timestamp-oriented rows per source stream, `128` indexed recent feedback rows, and up to `32` high-signal feedback-target stubs ranked by contradiction/correction before recency. Runtime summaries preserve source limits/counts/truncation, CPU-only placement, `runs_live_tick=false`, and `gpu_used=false`. The bounded benchmark preserved the old contradicted target `ep-42` and reduced mean latency to `14.684 ms` with no CUDA/VRAM use; the paired 65536-column `524288`-token protection run stayed in band at `6344.404 tokens/sec` with bounded `12/65536` route rows and flat `1799 MiB` GPU memory. | Reintroduce input-unbounded replay planning only as an explicit diagnostic report with a source-size budget and quality/latency evidence that beats the bounded planner without entering live tick, replay execution, or hidden language reasoning. |
| Stale local checkpoint compatibility branches | retired project policy | MARULHO currently has only development users, so old local checkpoints are not a public compatibility contract. Keeping import shims or dormant runtime branches solely so stale checkpoint folders still load preserves dead code and bloats the workspace. | Keep current published-checkpoint transaction semantics, exact checkpoint inputs referenced by benchmark reports, and operator-reviewed rollback artifacts. Delete generated checkpoint/cache folders and old compatibility wrappers when a path is promoted. | Revisit only after a real external release or shared checkpoint format requires a documented migration window. |
| Hidden all-column predictive-location decay under a candidate-scoped update claim | retired | `PredictiveColumnState.update_location()` decayed velocity and normalized location for every column even when prediction error and vote updates were candidate-scoped. That made cached non-awake predictive state only partially true and could hide all-column work behind `predictive_update_execution.runs_all_columns=false`. | Candidate-scoped retained predictive updates now pass the awake mask into `update_location()`, update location/velocity only for candidates plus winners, and expose `location_update_count`, `location_cached_count`, and `location_update_runs_all_columns` in Runtime Truth. | Reintroduce all-column location decay only if a benchmark proves it is required for correctness and Runtime Truth marks predictive update as all-column. |
| Cached predictive/homeostasis state without lazy wake materialization | rejected scheduler variant | The retained CPU scheduler could skip non-awake predictive/homeostasis work, but a 400-sample 8192-column check diverged once a cached column woke with stale predictive decay or homeostasis threshold state. That made cached votes and sleep evidence status-like rather than a proven execution boundary. | Cached columns now carry checkpointed predictive and homeostasis step stamps. When routing wakes a cached candidate, core/training materializes only that bounded candidate set through missed non-winner predictive updates and missed idle homeostasis before refreshing vote/scoring/update state. | Revisit only with a different bounded materialization proof that preserves long-run winner parity and complete-runtime cost without scanning all columns. |
| Split retained candidate predictive transition as the CPU scheduler path | retired implementation shape | The wake-plan route still called prediction error, location, and prediction-weight update as three separate candidate-scoped methods. That preserved correctness but re-canonicalized the same awake set, repeated materialization checks, and could overwrite useful cached-materialization evidence with later no-op calls in the same tick. | `PredictiveColumnState.update_candidate_prediction_transition()` applies the retained CPU candidate transition through one core-owned awake mask and falls back to the old split sequence only when the requested winner scope is not represented by that mask. Focused tests prove state parity with the split path and preserved materialization evidence. | Reintroduce split retained updates only for debugging or if a benchmark shows the fused CPU path regresses correctness or complete-step cost. CUDA remains on the dense predictive transition until a scoped CUDA implementation wins complete-runtime evidence. |
| Global fallback threshold relaxation under candidate-scoped scheduling | retired | `CompetitiveColumnLayer.compete()` relaxed every column threshold when a candidate set had no positive activation. Under candidate-scoped homeostasis this mutated sleeping columns globally and changed the order of fallback relaxation versus later lazy homeostasis, causing long-run threshold drift. | Fallback threshold relaxation is recorded as a per-tick event and applied only to the awake/homeostasis scope, with missed relaxation events replayed for a cached candidate before its materialized idle homeostasis. | Reintroduce global relaxation only if the scheduler truthfully reports all-column execution and long-run benchmarks prove the all-column tax is worth it. |
| Report-derived usefulness as sleep authority | rejected scheduler variant | `column_runtime` could compute usefulness from confidence and win-rate for a compact report, but using that report-only value to claim sleeping/awake decisions would put scheduler ownership in Runtime Truth projection and could still run all route/vote work. | `ColumnMetabolismState.usefulness` is now checkpointed training/core state updated only for awake candidates from predictive confidence/error, win-rate, and cost. Report-derived usefulness remains a fallback for visibility only; scheduler masks consume the cached metabolism tensor. | Revisit only if a new training-owned utility estimator is updated without all-column hot-path work and proves wake-mask effects with tests and long complete-runtime evidence. |
| Filtering low-usefulness candidates after CUDA route-vote selection | retired implementation shape | CUDA graph/fused route-vote had already selected candidates and winner inside its own route boundary. Dropping low-usefulness IDs after that point would make Runtime Truth claim a usefulness-aware scheduler while the selected winner still came from the unfiltered set. | The CUDA route-vote owner now masks low-usefulness route rows inside `core.fused_route_vote_cuda` before top-k candidate and winner selection, using cached `ColumnMetabolismState.usefulness` only when `last_usefulness_source` proves evidence exists. | Reintroduce post-selection usefulness filtering only for offline diagnostics; live scheduler truth must filter before selection or report fallback. |
| All-column usefulness scan to decide sleep | rejected implementation shape | Scanning usefulness across every column to decide whether a bounded route filter should run would recreate the all-column hot-path tax the scheduler goal forbids and could make usefulness another fake status field. | Trainer/core enable the usefulness gate from cached source state and apply it only to retrieved candidates or route rows already owned by the fused route-vote path. Runtime Truth reports enable/applied/fallback fields instead of hiding missing evidence. | Revisit only with a device-owned aggregate utility signal maintained outside the tick, or a bounded GPU candidate router that proves no all-column wake tax and passes long complete-runtime evidence. |
