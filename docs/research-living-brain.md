# Living Brain Research Notes

This file records research anchors for current architecture work. It is not a proof that MARULHO is a biological brain; it is the paper trail for the engineering direction.

## Current Anchors

- Service advisory replay retirement note, June 2026: complementary learning
  systems, continual-learning replay, synaptic tagging/capture, sparse replay,
  and modern Hopfield-style associative recall justify selected local replay
  windows, not a second service-level text replay lane. MARULHO therefore
  deletes the service replay-plan/sample/dataset route family and keeps bounded
  replay review in trainer/SNN slow paths: regeneration permits, replay
  artifacts, sleep-plasticity review tickets, scheduler tickets, transition
  memory replay artifacts, and runtime trace export as trace-only evidence.
  The retirement removes `living_loop_replay.py`, replay dataset packagers and
  runners, replay source-window benchmark modules, schema/facade/API/status
  fields, and persisted replay-sample history. The current invariant is no
  full-memory scan in the live tick, no every-token slow-memory admission, CPU
  archival metadata by default, CUDA only for active tensor computation, and no
  hidden language reasoning through replay text. Initial local hot-path reports
  for this retirement slice are retained as rejected/noisy evidence: they
  completed `524288` tokens with bounded `12/65536` route rows and no
  measurement-window status polling, but recorded contention and only
  `3982.916` then `4559.668 tokens/sec`; an unpinned same-session A/B measured
  `5067.347` versus `4859.400 tokens/sec`. The diagnosis was import
  contamination from the editable main checkout: worktree comparisons must pin
  `PYTHONPATH` and verify `marulho.__file__`. Corrected pinned-import evidence
  returned to the maintained band: full `524288`-token current HEAD reached
  `5865.705 tokens/sec` versus pinned old known-good `fbb788de` at
  `5880.863 tokens/sec` (`-0.26%`), both with no observed contention, bounded
  `12/65536` route rows, no all-column transition, no measurement-window status
  polling, RTX 3060 CUDA placement, and zero graph/native sequence failures.
  This makes the replay-lane retirement an accepted absence/protection result,
  not a new recall/replay promotion.
- Recent anchor-capture row note, June 2026: selected replay anchors may be
  chosen from a bounded recent window, but sparse replay and synaptic
  tagging/capture do not justify trainer code reading `slow_bucket_ids`
  directly after selection. `capture_recent_memory_anchors(...)` now reads
  anchor buckets through `DualMemoryStore.recent_anchor_capture_row(...)` under
  `bounded_recent_anchor_capture_row.v1`; the production trainer no longer
  indexes `slow_bucket_ids` for anchor capture. The external report
  `..\..\MARULHO_reports\bounded_replay_window_20260622\recent-anchor-capture-store-owned-row.json`
  passed with `64` captured rows, `anchor_row_read_count=64`, zero invalid
  anchor rows, CPU archival placement, no raw replay text, no hidden language
  reasoning, no live tick, no every-token work, no global scan, `0.0 MiB` CUDA
  allocation delta, and mean capture latency `1.743 ms`. The paired hot-path
  report
  `..\..\MARULHO_reports\bounded_replay_window_20260622\hotpath-active-pressure-65536-524288-i32-recent-anchor-capture-row.json`
  processed `524288` tokens at `5916.223 tokens/sec` with p95 tick
  `21.992 ms`, `train_compute=0.135626 ms/token`, bounded `12/65536` route
  rows, zero graph/native/sequence failures, no observed contention, CUDA on
  the RTX 3060, and RTX memory `1997->1998 MiB`.
- Inherited replay-query cap note, June 2026: selected replay remains bounded
  even when a caller or restored checkpoint supplies an old query-collection
  report. `_bounded_replay_recall_evaluation(...)` now accepts inherited
  `candidate_bucket_ids` only from the canonical
  `bounded_replay_query_anchor_bucket_source_window.v1` report and re-caps them
  to the `16`-bucket anchor-window budget before local associative recall. The
  external benchmark
  `..\..\MARULHO_reports\bounded_replay_window_20260623\replay-query-inherited-bucket-cap.json`
  cut an oversized `4096`-bucket report to `16`, truncated `4080` inherited
  buckets, kept exact input recall (`mean_input_pattern_distance=0.0`), used
  CPU archival/active recall placement, and allocated `0.0 MiB` CUDA. The
  same-checkpoint `524288`-token run
  `..\..\MARULHO_reports\bounded_replay_window_20260623\hotpath-active-pressure-65536-524288-i32-inherited-query-cap-pinned-main-rerun.json`
  reached `6162.974 tokens/sec`, `train_compute=0.130390 ms/token`, p95
  `20.644 ms`, and zero graph/native failures.
- All-anchor benchmark-code cleanup note, June 2026: after the historical
  all-anchor comparison reports had already proven the bounded source windows,
  MARULHO removed the benchmark-local all-anchor replay-query and sleep-replay
  implementations. Current maintained-only reports in
  `..\..\MARULHO_reports\bounded_replay_window_20260623\replay-query-anchor-maintained-only.json`
  and
  `..\..\MARULHO_reports\bounded_replay_window_20260623\sleep-replay-anchor-maintained-only.json`
  pass with `retired_all_anchor_source_absence.implementation_present=false`,
  newest-anchor hit rates `1.0`, CPU archival/source placement, `0.0 MiB` CUDA
  allocation delta, no live tick, no every-token work, and no hidden language
  reasoning.
  `20.644 ms`, bounded `12/65536` route rows, no all-column transition, no
  measurement polling, and zero graph/native sequence failures. This keeps
  modern-Hopfield-style recall local to the selected anchor window and avoids
  treating inherited report metadata as trusted global memory scope.
- Semantic frontier store-owned row note, June 2026: modern Hopfield-style
  recall may score a local window, but complementary learning systems,
  continual replay, synaptic tagging/capture, and sparse replay do not justify
  semantic planners reading archive arrays directly or using
  `replay_entry(...)` just to fetch returned text. `bank_memory_matches_with_report(...)`,
  `frontier_gap_plan(...)`, and `concept_frontier_metrics_with_report(...)`
  now read bounded rows through `DualMemoryStore.query_match_row(...)`; the
  production `_effective_capture_strength(...)` helper is removed, and
  semantic frontier code has no `replay_entry(...)` or direct `slow_*` archive
  row reads. The external source-bank report
  `..\..\MARULHO_reports\bounded_replay_window_20260622\source-bank-store-owned-row-reader.json`
  passed on a `65536`-entry store with selected-index parity `1.0`, `196`
  store-owned row reads (`192` scoring rows plus `4` text rows), raw text
  loaded only for returned rows through `query_match_row(...)`,
  `stc_state_advance=false`, CPU archival/score placement, `0.0 MiB` CUDA
  allocation/reservation, and mean latency `160.781 ms` versus `958.681 ms`
  for the diagnostic path. The frontier-gap report
  `..\..\MARULHO_reports\bounded_replay_window_20260622\frontier-gap-store-owned-row-reader.json`
  kept term recall `1.0`, read `192/65536` rows, and reduced mean latency from
  `229.118 ms` to `8.897 ms` (`25.752x`). The concept-frontier report
  `..\..\MARULHO_reports\bounded_replay_window_20260622\concept-frontier-store-owned-row-reader.json`
  passed quality, bounded-scan, latency, and live-tick gates with `64` row
  reads at `8192` capacity, `top1_match=true`, no direct slow-memory row reads,
  no capture helper, no live tick, and CPU archival placement. The replay
  quality check
  `..\..\MARULHO_reports\bounded_replay_window_20260622\semantic-row-reader-replay-quality.json`
  kept sleep recall passing with `1` query and best input-pattern distance
  `5.96046447753906e-08`. The hot-path protection report
  `..\..\MARULHO_reports\bounded_replay_window_20260622\hotpath-active-pressure-65536-524288-i32-semantic-row-reader.json`
  processed `524288` tokens at `5926.502 tokens/sec` with p95 tick
  `21.856 ms`, `train_compute=0.135858 ms/token`, bounded `12/65536` route
  rows, zero graph/native/sequence failures, no observed contention, CUDA on
  the RTX 3060, and flat `1993 MiB` GPU memory.
- Replay restore source-window note, June 2026: modern-Hopfield-style recall,
  CLS, continual replay, synaptic tagging/capture, and sparse replay all keep
  restore-time replay state as selected evidence, not as permission to
  normalize every retained checkpoint record before applying the controller
  retention budget. MARULHO therefore retires the full-materialized
  replay-controller restore shape: `ReplayController` now emits
  `bounded_replay_restore_source_window.v1` and slices replay sample history,
  regeneration permits, replay-evaluation contexts, review tickets, scheduler
  installations, and transition-memory replay artifacts to their retention
  limits before normalization, indexing, or evaluated-artifact validation.
  `MarulhoServiceManager` and `RuntimePersistence` no longer add extra full
  `list(...)` copies around those replay fields on restore. The benchmark
  `reports/bounded_replay_window_20260620/replay-restore-source-window.json`
  used `65536` records in each of eight replay restore fields and passed with
  latest-window parity against a benchmark-local retired full-materialized
  diagnostic: bounded restore inspected `656` records instead of `524288`
  (`799.219512x` less source work), averaged `15.600729 ms` versus
  `6605.339529 ms` (`423.399426x`), restored `64` valid evaluated artifacts,
  used `0.581783 MiB` traced Python peak, kept CUDA allocation/reservation at
  `0.0 MiB`, and reported no live tick, no every-token work, no raw replay
  text, no hidden language reasoning, no mutation/plasticity, and no
  GPU-resident archival metadata. The accepted `524288`-token protection rerun
  `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-replay-restore-source-window-rerun.json`
  stayed in band at `5945.577 tokens/sec`, p95 `22.062 ms`,
  `train_compute=0.136201 ms/token`, `prepare_training=0.007152 ms/token`,
  `finalize_total=0.006825 ms/token`, bounded `12/65536` route rows,
  `65526` cached transition rows, no observed contention, RTX 3060 memory
  `2061->2062 MiB`, and zero graph/native sequence failures. A first same-shape
  run succeeded at `5904.020 tokens/sec` but is secondary because velocity
  observed GPU contention at `22%`.
- Generic replay-entry API retirement note, June 2026: modern-Hopfield-style recall can rank bounded tensor traces, but CLS/replay/tagging evidence does not justify keeping a generic mutating replay row API beside explicit local operators. `DualMemoryStore.replay_entry(...)` is removed. MARULHO now routes mutating sleep repair through `sleep_repair_replay_row(...)`, read-only sleep recall through `replay_recall_row(...)`, and query/source-bank/context recall through `query_match_row(...)`. Fresh external reports in `..\..\MARULHO_reports\bounded_replay_window_20260622\` passed: query payload parity loaded `5` bounded text payloads instead of `192`; context comparison kept selected-index parity while loading `8` payloads instead of `16`; sleep repair improved mean anchor distance by `0.076463`, reached `1.954494x` input-prepare speedup, deferred `8` missing-key rows, made `0` dense assembly calls, and kept global scan, live tick, every-token work, raw replay text, and language reasoning false.
- Query fallback retirement and replay-bundle source-window note, June 2026:
  modern-Hopfield-style recall remains valid only after a local memory set is
  selected; CLS, continual replay, synaptic tagging/capture, and sparse replay
  do not justify widening a routed query readout with a recent-entry text
  sweep. MARULHO therefore retires the
  `query_runner_memory_match_recent_text_support_fallback` path from
  production query matching. `query_runner.memory_matches_with_report(...)`
  now keeps the routing-owned bucket-indexed candidate window as the only
  source for explicit query readout, no longer emits `recent_fallback_*`
  fields, and does not call `collect_recent_entry_indices(...)` to find
  semantic text support outside the candidate buckets. The benchmark
  `reports/bounded_replay_window_20260622/query-recent-fallback-retired-bucket-only.json`
  passed on a `65536`-capacity store: the recent collector was not called,
  returned indices stayed inside the routed bucket window (`[0]`), raw text was
  loaded only for the selected candidate, no global candidate/score scan ran,
  archival metadata stayed CPU-resident, CUDA was available but unused, and
  mean bounded latency was `62.085745 ms`. The same slice makes replay-dataset
  packaging carry `bounded_replay_dataset_bundle_source_window.v1`, so preview
  selection criteria, source counts, CPU placement, no live/every-token work,
  no training/plasticity authority, and no hidden replay-text reasoning survive
  the package gate instead of being dropped at bundle time. The paired
  `524288`-token protection run
  `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-query-fallback-retired-bundle-source-window.json`
  stayed in the maintained band at `6117.434 tokens/sec`, p95 `20.621 ms`,
  `train_compute=0.132414 ms/token`, bounded `12/65536` route rows, no
  observed contention, CPU max `38%`, GPU max `13%`, RTX memory
  `1775->1775 MiB`, and zero graph/native sequence failures.
- SNN readout-surface production naming retirement note, June 2026:
  internal-representation and concept-formation research can inspire bounded
  readout surfaces, but MARULHO must not expose them as hidden thought streams.
  The production SNN language surface chain now uses only
  `snn_language_readout_surface_design.v1`,
  `snn_language_readout_surface_preflight.v1`,
  `snn_language_readout_surface_executor.v1`, and
  `snn_language_readout_surface_event_review.v1`; checkpoint load/save keeps
  canonical readout-ledger fields and drops noncanonical readout-ledger state.
  Focused route and checkpoint tests prove canonical JSON and saved metadata, while
  `reports/bounded_replay_window_20260622/snn-readout-ledger-normalization-readout-surface-canonical.json`
  passed with bounded mean `408.799567 ms` versus legacy diagnostic
  `6288.893500 ms` (`16x` source-work reduction) and autonomous-chain bounded
  mean `906.320467 ms` versus `19796.135733 ms`. The paired `524288`-token
  protection run
  `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-readout-surface-canonical.json`
  stayed in band at `6012.300 tokens/sec`, p95 `21.322 ms`,
  `train_compute=0.134205 ms/token`, bounded `12/65536` route rows, no
  observed contention, CPU max `59%`, GPU max `13%`, RTX memory
  `1771->1772 MiB`, and zero graph/native sequence failures.
- Replay-sample single-path note, June 2026: modern-Hopfield-style recall, CLS, continual replay, synaptic tagging/capture, and sparse replay all argue for named slow-path windows with visible authority boundaries. MARULHO therefore retires the audit-only `/terminus/replay-execute` alias, `mode="execute"`, `execution_id`, and `replay_executor_summary` projection instead of preserving a second execution-shaped surface beside `POST /terminus/replay-sample`. The maintained sampler exposes only `dry_run` and `sample`, normalizes old execute-shaped records to `sample`, returns 404 for the retired alias, and carries one bounded `replay_sample_summary` with CPU archival placement, no raw replay text, no hidden language reasoning, no live-tick/every-token work, and no training/plasticity/action side effects. `reports/bounded_replay_window_20260620/replay-sample-single-path-service-benchmark.json` passed with no `replay_executor_summary` and replay-sample history latency `4.798 ms`; `reports/bounded_replay_window_20260620/replay-dataset-source-window-replay-sample-single-path.json` preserved `50/50` dataset target/link parity with canonical `sample` records; and `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-replay-sample-single-path.json` stayed in band at `5951.781 tokens/sec`, p95 `21.962 ms`, `train_compute=0.136320 ms/token`, bounded `12/65536` route rows, zero graph/native sequence failures, no observed contention, CPU max `38%`, GPU max `14%`, and RTX 3060 memory `1894->1881 MiB`.
- Sleep-plasticity ticket queue source-window note, June 2026: modern-Hopfield-style recall, CLS, continual replay, synaptic tagging/capture, and sparse replay support replay and consolidation as selected slow windows, not retained-history scans before every scheduler/autonomy proposal. MARULHO therefore bounds `snn_sleep_plasticity_review_ticket_queue(...)` and `snn_sleep_plasticity_scheduler_design_review_ticket_queue(...)` to the newest `16` retained records while reporting `retained_count` separately. The focused benchmark `reports/bounded_replay_window_20260620/sleep-plasticity-ticket-queue-source-window.json` passed with `16/64` inspected records on both queues, latest verified tickets matching diagnostic full-retained scans, `4x` less source work, mean bounded latency `3.326372 ms` for sleep tickets and `201.999864 ms` for scheduler-design tickets, CPU archival/score placement, `0.072 MiB` traced Python peak, CUDA available but unused (`0.0 MiB` allocated/reserved), no global candidate/score scan, no live tick, no every-token cadence, and no hidden language reasoning. The maintained-only cleanup report `..\..\MARULHO_reports\bounded_replay_window_20260623\sleep-plasticity-ticket-queue-legacy-baseline-removed.json` now removes the executable full-retained benchmark verifier too: deterministic seeded quality still passes on `16/64` source windows, sleep queue latency averages `2.836412 ms`, scheduler-design queue latency averages `273.395856 ms`, the report carries `retired_full_retained_ticket_queue_absence.implementation_present=false`, Python peak is `0.067 MiB`, and CUDA archival allocation remains `0.0 MiB`. The refreshed paired `524288`-token protection run `..\..\MARULHO_reports\bounded_replay_window_20260623\hotpath-active-pressure-65536-524288-i32-sleep-ticket-legacy-baseline-removed-default-nosample.json` stayed in the 6k-ish band at `6040.427 tokens/sec`, p95 `21.752 ms`, `train_compute=0.133547 ms/token`, `prepare_training=0.006586 ms/token`, `finalize_total=0.006690 ms/token`, bounded `12/65536` route rows, zero graph/native sequence failures, no observed contention, CPU max `39%`, GPU max `14%`, RTX 3060 memory `1791->1795 MiB`, and a `240302.172 ms` prewarm outside the measured token window.

### Replay provenance source-window binding

- Known readout-evidence source-window note, June 2026: modern Hopfield
  networks support attention-like associative retrieval only after a local
  memory set is selected; complementary learning systems, continual replay,
  synaptic tagging/capture, latent replay, and sparse replay all reinforce
  carrying selection evidence into consolidation rather than passing bare
  identity sets. MARULHO therefore removes the private
  `_known_readout_evidence_hashes()` helper and public
  `known_readout_evidence_hashes()` helper. The only production path is
  `known_readout_evidence_hashes_with_report()`, which returns hashes plus
  `bounded_snn_readout_known_evidence_hash_source_window.v1`. Replay design,
  dry-run, plasticity preflight, bridge, and evaluated replay-artifact
  recording now include that report in required evidence and artifact hashes.
  The replay controller requires the report before recording evaluated replay
  artifacts and persists `readout_evidence_source_window_hash` for later
  permit/checkpoint verification. The benchmark
  `reports/bounded_replay_window_20260620/snn-replay-artifact-known-readout-source-window.json`
  passed with known-readout source window `1/8`, CPU archival placement, no
  global scans, no raw text, no language reasoning, `0.0 MiB` CUDA allocation,
  and `0.014095 MiB` traced Python peak. The accepted `524288`-token rerun
  stayed in band at `6007.228 tokens/sec`, with bounded `12/65536` route rows,
  `65526` cached transition rows, and zero graph/native sequence failures,
  though GPU contention was observed.
- Replay-priority source-window binding note, June 2026: the same research
  boundary applies to the priority selector itself. Modern Hopfield-style
  recall remains a local associative operator after a source window is chosen;
  CLS, continual replay, synaptic tagging/capture, latent replay, and sparse
  replay all argue that selection evidence must travel with replay artifacts
  rather than being trusted as a caller-side report. MARULHO therefore retires
  the readout replay-priority report-dropping artifact shape:
  `transition_memory_replay_artifact_proposal(...)` now carries
  `bounded_snn_readout_replay_priority_source_window.v1` and a matching hash,
  `ReplayController` requires and persists that source window for evaluated
  transition-memory replay artifacts, and rollout replay-artifact review
  recomputes artifact hashes with the same optional source-window material.
  The focused benchmark
  `reports/bounded_replay_window_20260620/snn-replay-artifact-readout-priority-source-window.json`
  passed with replay-priority source window `1/32`, CPU archival and scoring
  placement, no global scans, no raw text, no language reasoning, no live-tick
  or every-token work, CUDA available but unused, `0.0 MiB` CUDA allocation,
  `0.014385 MiB` traced Python peak, and `0.421992 ms` mean permit-verification
  latency. The first `524288`-token protection run was rejected as primary
  evidence at `4662.031 tokens/sec` because GPU contention was observed. The
  no-contention rerun stayed in the maintained noisy band at
  `5937.908 tokens/sec`, with `train_compute=0.136165 ms/token`, bounded
  `12/65536` route rows, `65526` cached transition rows, flat RTX 3060 memory,
  and zero graph/native sequence failures.

### Predictive spiking substrate

- Persistent text-tick executor note, June 2026: profiling the promoted graph path showed that reconstruction scalar readback and Python neuromodulation still split each eligible text tick into two graph replays. NVIDIA's CUDA Graph programming guide states that whole-workflow submission reduces CPU launch cost and enables optimizations unavailable to piecewise stream submission; NVIDIA Holoscan's GPU-resident execution guidance similarly targets CPU/GPU coordination boundaries. MARULHO therefore moved reconstruction-driven dopamine, serotonin, acetylcholine, norepinephrine, and predicted-error updates onto persistent CUDA state and captured reconstruction, neuromodulation, fused route/vote, and transition as one replay. Three-factor SNN learning research supports neuromodulators as global factors over local plasticity, but does not justify graphing archival memory or replacing local spike state. Sixteen sequential ticks preserved every winner and reconstruction; all model tensors were exact except three velocity values with maximum `5.82e-11` drift. A fresh-process 512-tick comparison reached `95.58` versus `63.49 ticks/sec` (`1.506x`), while the text quality gate reached `57.14` versus `47.52 ticks/sec` (`1.202x`) with exact winners and preserved declared quality. One real 24-token source tick reported 24 persistent replays, 24 host truth synchronizations, and zero failures, but only `8.79 tokens/sec`; memory, cross-modal/text, source, and other Python stages remain the next target. Sources: [CUDA Graphs](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html), [GPU Resident Execution](https://docs.nvidia.com/holoscan/sdk-user-guide/using-the-sdk/gpu-resident-execution), and [Three-Factor Learning in Spiking Neural Networks](https://arxiv.org/abs/2504.05341).
- Fused reconstruction note, June 2026: CUDA Graph and Triton guidance still points toward removing duplicated fixed-shape GPU work rather than adding host-visible reports. MARULHO found that the persistent text tick computed reconstruction with a separate dense prototype scan even though fused route/vote had already scored the same exact routing cache. The promoted path now derives reconstruction error from the fused route-score maximum before device neuromodulation, preserving exact winners and bounded `1e-6` reconstruction tolerance. A pinned-clean-HEAD 1024-sample comparison measured `796.22` versus `630.17 tokens/sec` on the quantum-staged complete encoded tick (`1.264x`), with `1088` graph replays, `1088` fused reconstruction updates, zero graph failures, and zero staged-input mismatches. This changes implementation direction only inside the graph-owned text-tick boundary; sensory ticks, source handling, archival memory, replay, and service orchestration remain outside the claim.
- Competitive-surprise readback note, June 2026: the next profile found that the one-replay executor still launched `torch.norm` and synchronized a second scalar after every transition solely to append competitive surprise history. The exact post-transition norm now runs inside the existing captured workflow and occupies the ninth slot of the already-required host truth packet. A reversed 256-tick A/B measured `86.11` versus `70.21 ticks/sec` (`1.226x`); a 16-tick sequential gate preserved all winners and matched surprise history within `1e-7`, and Runtime Truth counted 320 graph-owned competitive-surprise updates with one host packet sync per tick. This follows the same CUDA Graph direction already documented above; no new research changed the architecture.
- Thousand Brains Project / Monty note, June 2026: Monty's learning-module direction treats cortical-column-like modules as repeatable sensorimotor modelers that communicate through a common messaging protocol and reference-frame evidence. MARULHO borrows the bounded module/vote/metabolism shape without importing Monty as a runtime dependency. `core.column_runtime` keeps awake budgets, cached votes, disagreement, growth, and pruning evidence report-only. The first promoted execution slice is narrower: existing retrieval candidates bound competitive scoring when the learned-chunk routing key does not depend on dense assembly. The report scheduler still does not control execution or topology.
- Column-runtime implementation note, June 2026: Thousand Brains / Monty sources reinforced the repeated learning-module and voting contract, while Switch Transformer-style sparse routing reinforced keeping active modules few and capacity-bound. MARULHO therefore adds sampled Column Registry evidence and scheduler/vote metadata without promoting scheduler execution. Modern Hopfield work maps associative recall to attention-like top-k retrieval, so MARULHO adds `bounded_column_associative_recall` only as a capped, single-column helper; it is not a whole-mind memory or language-generation path. Growth now fails closed unless repeated prediction-failure streak evidence is present, so one-shot surprise cannot create a candidate column. The repeated-failure signal is owned and checkpointed by `PredictiveColumnState`, not by `service`, preserving the module/vote evidence boundary and rollback-aware growth path.
- Column-society export note, June 2026: the same Thousand Brains/MoE direction also changes Runtime Truth metabolism, but smaller exports are not automatically faster on CUDA. A bounded device-side scheduler export reduced a 1024-column five-tensor status materialization from `20480` bytes to `400`, but measured warmed CUDA latency was worse (`15-20 ms`) than the retained latency-first full report (`6.3695/8.6799 ms` median/p95) at current sizes. MARULHO therefore keeps CUDA column Runtime Truth latency-first with one full snapshot and records bounded device export as a future large-column experiment. This keeps the scheduler gate execution-relevant only for already-promoted candidate scoring/homeostasis; cached votes, sleep/deep-sleep state, growth, pruning, and local Hopfield recall remain non-mutating Runtime Truth evidence.
- Predictive-vote metabolism note, June 2026: Monty's voting contract and sparse-MoE literature support limiting active specialists, but they do not imply that every mathematically sparse operation is faster on a GPU. MARULHO's complete hot-window A/B rejected candidate-local voting and a separately compiled dense vote because gathers, launches, and warmup outweighed reduced arithmetic. The implementation direction is therefore broader routing/competition fusion while preserving dense vote semantics; an unused persistent `hypothesis` tensor was removed because it was not part of the actual vote message or any consumer contract.
- Predictive-vote cache wake note, June 2026: the scheduler contract now separates retained CPU/tensor voting from the rejected standalone CUDA vote experiments. Thousand Brains/Monty/CMP supports repeated learning modules exchanging bounded vote/reference-frame evidence; Expert Choice and sparse-MoE routing support explicit capacity-bound specialist activation; spiking predictive-coding work keeps prediction error local to columns; and SparseProp/neuromorphic event-driven work reinforces skipping irrelevant units instead of scanning all neurons after an event route is known. MARULHO therefore moved retained predictive voting behind routed candidates: `PredictiveColumnState.vote()` recomputes agreement only for the awake mask and keeps cached consensus gain for non-awake columns. The CPU complete-step A/B at `2048` columns preserved exact winners, updated `10` votes, cached `2038`, and improved mean `train_step` latency `7.14%`. This does not overturn earlier CUDA vote-fusion rejections; it is a training-owned cached-vote scheduler effect for the retained path. Sources: [Thousand Brains Project](https://arxiv.org/html/2412.18354v1), [Cortical Messaging Protocol](https://thousandbrainsproject.readme.io/docs/cortical-messaging-protocol), [Expert Choice Routing](https://papers.nips.cc/paper_files/paper/2022/hash/2f00ecd787b432c1d36f3de9800728eb-Abstract-Conference.html), [Predictive Coding with Spiking Neural Networks](https://arxiv.org/html/2409.05386v1), and [SparseProp](https://arxiv.org/abs/2312.17216).
- Predictive-update wake note, June 2026: the same research contract applies to local predictive-state mutation, not only consensus voting. Thousand Brains/Monty/CMP treats learning modules as local modelers that exchange bounded messages; Expert Choice and sparse MoE make fixed capacity an execution rule; predictive-coding SNN work keeps prediction error local to the affected population; and SparseProp-style neuromorphic metabolism supports event-local updates over whole-population scans. MARULHO therefore split `candidate_predictive_update_start_tokens` from structural `dead_column_steps`: CPU retained predictive updates now mutate only routed awake columns while non-candidate prediction error, confidence, failure streak, and prediction weights stay cached. Runtime Truth now exposes `predictive_update_execution` and keeps aggregate `runs_all_columns=true` if any live specialist still scans every column. The CPU complete-step A/B at `2048` columns preserved exact winners, changed predictive update and vote from `2048/2048` to `10/2048`, cached `2038`, and improved mean `train_step` latency `6.26%`. CUDA remains dense with explicit fallback because prior scoped-CUDA update evidence was launch-bound.
- Routing-normalization note, June 2026: profiling can identify redundant-looking tensor operations, but iterative predictive systems amplify small floating-point changes. Skipping repeated routing normalization reduced launches yet changed sequential winners and predictive location materially. MARULHO therefore treats trajectory parity or bounded cognitive quality as part of the optimization contract, not just single-step algebraic similarity.
- Route/competition fusion note, June 2026: PyTorch `torch.compile(mode="reduce-overhead")`, CUDA Graph guidance, and Triton's fusion model all support reducing launch overhead for small fixed-shape GPU work, but MARULHO's trajectory is sensitive to projection precision, top-k tie/order, and sequential threshold decay. A single-tick compiled route/competition prototype preserved parity yet lost complete `train_step` A/B and increased warmup/VRAM cost, so it was removed from runtime code. The next implementation direction must fuse a broader projection/vote/route/competition cluster or remove host orchestration; another standalone compiled launch is not enough.
- Device-resident winner-selection note, June 2026: NVIDIA's CUDA Graph guidance identifies repeated host launch/setup as material overhead for short kernels, and PyTorch documents `reduce-overhead` as useful for small GPU workloads. MARULHO therefore tested the narrower remaining synchronization boundary inside the already-promoted in-place transition rather than adding an unrelated compiled graph. A one-block Triton selector now writes the winner and fallback evidence into persistent CUDA buffers; the existing transition consumes the fallback boolean for exact threshold-decay semantics. Two reversed 256-tick hot-window pairs were noisy but averaged `62.32` versus `59.38 ticks/sec` for device versus retained selection, with effectively neutral average p95. A stricter grounded 128-tick gate passed with exact winners, exact cross-modal tensors, finite state, zero measured compilation events, and `35.96` versus `25.44 ticks/sec` against the retained transition baseline. The first empty-process selector compilation took about `111 s`, so the direction remains persistent-cache deployment plus broader fusion, not proliferating standalone Triton kernels. Sources: [CUDA Graphs](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html), [PyTorch Performance Tuning Guide](https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html), and [torch.compile](https://docs.pytorch.org/docs/stable/generated/torch.compile.html).
- Fused vote/competition note, June 2026: persistent-kernel work such as PERKS keeps iterative state and time-loop control closer to the GPU, SparseProp shows that SNN acceleration depends on genuinely event-local work, and fused sparse-expert systems such as FlashDMoE remove the cost of separately dispatching routing and specialist computation. MARULHO applied that direction to the proven active shape by fusing candidate-local predictive agreement, prototype score, inhibition, fallback selection, and previous-winner writeback. A 96-tick recurrent clone comparison was bit-exact across winners, prototypes, thresholds, locations, prediction error, and confidence. Reversed complete hot-window pairs measured fused/unfused `84.11/66.43` and `141.30/65.77 ticks/sec`, with median/p95 improvements in both orders and unchanged `26 MB` reserved VRAM. A 12-tick profiler reduced launches `902→670`, async copies `155→149`, stream synchronizations `117→109`, CPU self-time `236.2→154.0 ms`, and CUDA self-time `49.9→7.8 ms`. The grounded gate passed at `35.84` versus `31.61 ticks/sec` with exact winners and cross-modal tensors. This changes the next implementation direction: fuse tensor retrieval and the promoted vote/competition/transition cluster, or move toward a bounded persistent tick executor; do not return to standalone vote or competition kernels. Sources: [PERKS](https://arxiv.org/abs/2204.02064), [SparseProp](https://proceedings.neurips.cc/paper_files/paper/2023/file/0b443d358a391166d1fbf551fb53de02-Paper-Conference.pdf), [FlashDMoE](https://arxiv.org/html/2506.04667v1), and [Kernel Batching with CUDA Graphs](https://arxiv.org/abs/2501.09398).
- Device route-bank refresh note, June 2026: CUDA Graph guidance favors keeping repeated fixed-shape workflow state inside captured device execution, and Expert Choice / sparse-MoE routing reinforces explicit capacity over all-expert routing. MARULHO therefore moved steady route-bank/probe refresh into the fused route/vote select kernel: the same bounded `k+2` score rows now write the next bank positions and advance the probe cursor on device, while exact seed/checkpoint restore remain truthfully host-owned. The 32768-column 131072-token gate kept `route_input_rows_scored=12/32768`, `state_transition_cached_count=32758`, and zero graph/native/sequence failures while improving the immediate usefulness-filter comparison from `5886.235` to `6008.953 tokens/sec`. Same-checkpoint quality gates did not improve winner match (`0.046875` before and after), so this is a scheduler-ownership/speed cleanup, not a quality-complete ANN discovery router. Future CAGRA/cuVS-style or other GPU ANN work must still pass winner quality and long-run cost gates before entering runtime. Sources: [CUDA Graphs](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html), [Expert Choice Routing](https://papers.nips.cc/paper_files/paper/2022/hash/2f00ecd787b432c1d36f3de9800728eb-Abstract-Conference.html), and [NVIDIA cuVS CAGRA](https://docs.rapids.ai/api/cuvs/stable/cpp_api/neighbors_cagra/).
- Nested sequence-graph rejection note, June 2026: CUDA Graph research supports submitting stable workflows with low CPU launch overhead, but MARULHO's already-promoted burst path keeps sequential state alive across ordinary graph replays. Capturing eight sequential text ticks inside one larger CUDA Graph increased capture count from `4` to `6` and regressed the same 32768-token complete CUDA service run to `2249.691 tokens/sec`, with `train_compute=0.372436 ms/token`, despite exact burst parity tests and `3645` sequence-graph replays. Reverting to the retained burst executor restored the same checkpoint to `3171.826 tokens/sec` and `train_compute=0.273302 ms/token`. The implementation direction is therefore a real fused/persistent kernel or fewer CPU evidence drains, not a nested graph that replays the same per-token kernel cluster inside capture.
- Device-burst lightweight metrics note, June 2026: PyTorch performance guidance and NVIDIA CUDA Graph best-practice material both point at avoiding CPU/GPU synchronization and host-visible tensor materialization on the hot path. MARULHO's concrete version was subtler: service asked training for full metrics at sampled source-concept positions, so otherwise ordinary eight-token burst quanta fell back to individual `train_step` calls just to build evidence dictionaries. Training now returns final lightweight metrics from the device burst result packet while full metrics remain an explicit evaluator request. The 131072-token complete CUDA service run improved from `3359.378` to `3565.968 tokens/sec`, reduced `train_compute` from `0.259876` to `0.241568 ms/token`, and increased burst-owned tokens from `116680` to `126952`, with zero graph/burst failures. Sources: [PyTorch Performance Tuning Guide](https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html) and [NVIDIA Writing Sync-Free Code](https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/sync-free-code.html).
- Deferred slow-memory cadence note, June 2026: replay remains important for continual learning, but selective replay work supports choosing experiences by surprise, reward, distribution, or coverage instead of blindly admitting every fixed-interval sample into long-term memory. PyTorch/NVIDIA performance guidance also argues against host/device synchronization and tensor materialization on the hot path. MARULHO therefore retired fixed-cadence slow-memory admission as a burst execution gate: first-token retained/fallback admission and strong-capture device-ring events remain, while routine cadence is reported as deferred maintenance. A richer exact-cadence event-ring prototype was rejected because it removed fallbacks but slowed the 32768-token service path (`3052.470` and `3145.964 tokens/sec` variants versus the retained lightweight path). The promoted hot-path deletion reached `3901.906 tokens/sec` on the 131072-token CUDA service run, reduced `train_compute` from `0.241568` to `0.219858 ms/token`, deferred `512` cadence events, and retained zero graph/burst failures. Sources: [Selective Experience Replay for Lifelong Learning](https://ojs.aaai.org/index.php/AAAI/article/view/11595), [Investigating the Interplay of Prioritized Replay and Generalization](https://arxiv.org/html/2407.09702v2), [PyTorch Performance Tuning Guide](https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html), and [NVIDIA Writing Sync-Free Code](https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/sync-free-code.html).
- Bounded replay-window repair note, June 2026: modern Hopfield networks support attention-like associative retrieval, but MARULHO keeps that operator local to a selected replay window rather than treating it as a transformer mind. CLS, continual-learning replay, synaptic tagging/capture, latent replay, and sparse replay all point toward the same boundary: select salient tagged evidence, rehearse it in slow windows, and commit only useful local change. MARULHO therefore retired the stored-bucket modulated deep-replay mutation and replaced it with `bounded_reconstruction_gated_candidate_repair`: selected replay traces are de-duplicated, candidate columns come from bounded route candidates plus explicit stored-bucket fallback candidates, and a prototype repair commits only if it improves a local selected-window reconstruction metric. The synthetic benchmark at `reports/bounded_replay_window_20260617/synthetic-selection-candidate-repair.json` passed both stored-input recall (`5.960464477539063e-08` mean input distance) and prototype reconstruction (`0.0467838377` relative degradation under `0.05`) with `6` commits and `14` rejected non-improving attempts. The longer 65536-column hot-path check processed `262144` tokens at `6306.507 tokens/sec`, `train_compute=0.129511 ms/token`, `route_input_rows_scored=12/65536`, and zero graph/native/sequence failures, so replay remains selected and cadenced rather than every-token background work. Follow-up bounded slices applied the same rule to drift repair and micro maintenance: anchored repair may reanchor from a bucket-indexed window, anchored micro refresh updates CPU metadata without competitive replay, and no-anchor repair/micro paths record `no_anchor_bucket_scope_for_repair_replay` or `no_anchor_bucket_scope_for_micro_replay` with no mutation. The HF-backed runner now shows bounded stored-experience recall can pass on real source patterns (`mean_input_pattern_distance=0.0`) even while reconstruction recovery remains blocked. Sources: [Modern Hopfield Networks](https://arxiv.org/abs/2008.02217), [Complementary Learning Systems](https://pubmed.ncbi.nlm.nih.gov/7624455/), [Experience Replay for Continual Learning](https://papers.neurips.cc/paper/8327-experience-replay-for-continual-learning), [Synaptic Tagging and Capture](https://pubmed.ncbi.nlm.nih.gov/9020359/), [Latent Replay](https://arxiv.org/abs/1912.01100), and [Sparse Experience Replay](https://arxiv.org/abs/2009.04891).
- Capped replay-candidate note, June 2026: the same literature does not justify scoring every entry inside a hot local bucket. Modern-Hopfield-style recall remains an attention-like local operator only after selection, while CLS/replay/tagging evidence argues for bounded candidate admission before scoring. MARULHO therefore keeps per-bucket replay indices in recency order, collects a recent round-robin candidate pool across anchor buckets up to `candidate_window_limit=max(requested_count,candidate_pool)`, and records `candidate_index_available_count` separately from the actually scored `candidate_index_count`. A focused hot-bucket test proves `10` available entries can be capped to `4` scored entries before ranking; unscoped random replay is retired unless explicitly marked as a diagnostic global candidate scan. The fresh synthetic replay report kept positive-pressure recall and prototype gates passing while reporting CPU archival/scoring and no global scan, and the 65536-column 262144-token hot-path check stayed in band at `6148.125 tokens/sec` with `12/65536` route rows, `65526` cached transition rows, flat `1848 MiB` GPU memory, and zero graph/native/sequence failures. For future LLM-size scale, selected replay must become more indexed and sparse as memory grows; it cannot become an always-on full-memory attention pass.
- Selected replay routing-index refresh note, June 2026: the same local-window rule applies after replay commits. Deep/repair replay may update a few selected prototypes, but that does not justify rebuilding the whole routing index before the next search. MARULHO now refreshes only existing updated routing rows through `routing_index_existing_row_refresh.v1`, backed by a CPU ID-to-cache-row map. Missing IDs, missing row-update APIs, or dirty tensor caches now report deferred recovery and do not call `add()+rebuild()` inside selected replay. The maintained-only `65536`-entry report `..\..\MARULHO_reports\bounded_replay_window_20260623\sleep-replay-routing-index-legacy-baseline-removed-rerun.json` records the old full-rebuild diagnostic as absent, updates `16` selected rows, defers `1` missing row without inserting it, preserves exact top-1 recall for updated rows, and measures `9.892320 ms` mean bounded latency across `25` runs (`median=5.693200 ms`). The sharded maintained-only report passes at `19.582580 ms` mean latency with `16` direct shard and merged updates. Archival storage and row metadata remain CPU-resident while active routing tensors stay on CUDA; Python trace-memory peak stays below `0.085 MiB`. The current `524288`-token protection run stays in band at `6097.811 tokens/sec`, bounded `12/65536` route rows, CPU max `32%`, GPU max `15%`, RTX 3060 memory `1825->1824 MiB`, and zero graph/native failures. This is replay-window maintenance, not a live-tick recall operator or GPU-resident archival store.
- Bucket consolidation cache lookup note, June 2026: CLS/replay/tagging evidence supports bucket-level consolidation pressure, but it does not justify recomputing that pressure by scanning every slow-memory entry when a winner metric is needed. MARULHO now treats bucket consolidation as maintained CPU metadata: live scalar reads use `bucket_consolidation_level_cache_lookup.v1`, report `full_memory_scan=false`, and return a no-scan miss if the cache is absent. Explicit tensor rebuilds remain load/capture/offline, explicit tensor request, checkpoint load, graph capture/prewarm, or offline work. The maintained-only `65536`-entry report `..\..\MARULHO_reports\bounded_replay_window_20260623\bucket-consolidation-cache-legacy-baseline-removed.json` removes the executable full-bucket scan comparator, matches a seeded bucket expectation exactly, reports scan count `0`, averages `0.017516 ms`, uses `0.002090 MiB` Python trace-memory peak, and allocates `0.0 MiB` CUDA memory. The paired `524288`-token run stays same-band at `6461.135 tokens/sec`, bounded `12/65536` route rows, RTX 3060 memory `1929->1928 MiB`, no observed contention, and zero graph/native sequence failures.
- Selected replay cache-recovery retirement note, June 2026: the same CLS, synaptic tagging/capture, continual replay, and sparse replay boundary applies after replay selection. A selected replay window should consolidate selected traces; it should not repair missing global bucket-cache metadata by scanning the whole archive first. `DualMemoryStore.consolidate_replay(...)` now reports `bounded_selected_replay_consolidation.v1`: selected entries still update replay counts, capture tags, consolidation levels, consolidation events, and EMAs; selected-bucket cache deltas run only when cache metadata is already present; and missing cache metadata records `cache_missing_deferred_no_full_rebuild`. The `65536`-entry benchmark `reports/bounded_replay_window_20260620/selected-replay-consolidation-cache.json` matched selected-entry consolidation and EMA state against a benchmark-local retired full-cache rebuild diagnostic, scanned `0` cache-rebuild entries, averaged `2.291943 ms` versus `2979.156029 ms`, reduced source work by `4096x`, used CPU archival/cache metadata, and used no CUDA allocation. The clean `524288`-token rerun stayed in the 6k-ish band at `5973.047 tokens/sec`, `train_compute=0.135713 ms/token`, bounded `12/65536` route rows, zero graph/native sequence failures, no observed contention, CPU max `27%`, GPU max `13%`, and RTX 3060 memory `2039->2041 MiB`.
- Sync-free drift maintenance note, June 2026: PyTorch and NVIDIA guidance both warn that hidden CPU/GPU synchronizations block the host and weaken CUDA Graph benefits. MARULHO's drift refresh was still forcing a bounded event drain so CPU memory drift could use a fresh winner id. That made drift maintenance a synchronization dependency even though it does not alter the current neural transition. Burst drift refresh now uses winner-local drift only when the host mirror is already fresh; otherwise it uses global drift and reports the global refresh count. The 131072-token CUDA service run improved from `3901.906` to `4045.419 tokens/sec`, reduced `train_compute` from `0.219858` to `0.211096 ms/token`, reduced host-truth syncs from `9503` to `8193`, and reduced forced burst-event drains from `1310` to `0`, with zero graph/burst failures. Sources: [PyTorch Performance Tuning Guide](https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html) and [NVIDIA Writing Sync-Free Code](https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/sync-free-code.html).
- Wider sequence-quantum note, June 2026: PyTorch and NVIDIA CUDA Graph guidance support reducing repeated CPU launch/orchestration overhead, but NVIDIA's PyTorch CUDA Graph performance guidance also warns that graph changes can show no benefit or regress when they do not cover the real bottleneck. MARULHO's earlier q16 service quantum was therefore rightly rejected because it bypassed the eight-token burst executor and fell through to retained per-token `train_step`. The promoted implementation changes the ownership boundary instead of the neural transition: Brain Runtime delegates wider quanta to training, and training consumes each q16 quantum as two exact ordered eight-token persistent CUDA bursts. The 131072-token CUDA service run improved from `4045.419` to `4247.306 tokens/sec`, reduced `train_compute` from `0.211096` to `0.200979 ms/token`, retained `131056` burst-owned tokens, and reported zero graph/burst failures. Sources: [Accelerating PyTorch with CUDA Graphs](https://pytorch.org/blog/accelerating-pytorch-with-cuda-graphs/), [NVIDIA Constant Time Launch for Straight-Line CUDA Graphs](https://developer.nvidia.com/blog/constant-time-launch-for-straight-line-cuda-graphs-and-other-performance-enhancements/), and [NVIDIA CUDA Graph Performance Issues](https://docs.nvidia.com/dl-cuda-graph/troubleshooting/performance-issues.html).
- Thirty-two-token truth/event cadence note, June 2026: earlier interval-32 host-truth runs were rejected because the event queue still forced earlier host drains. After widening the Deferred Device Event Queue to 32 tokens and skipping empty host event-apply calls when no truth packet drained, the same complete CUDA service path promoted `cuda_graph_host_truth_sync_interval_tokens=32`. Sequential 32768-token q16/q32 evidence improved from `2768.913` to `4237.534 tokens/sec`, and the 131072-token confirmation reached `4577.595 tokens/sec` with `train_compute=0.181855 ms/token`, all transitions on the RTX 3060 through `inplace_triton`, `4097` host-truth syncs, `126975` skips, zero forced event drains, and zero graph/burst failures. This supports the implementation rule that Runtime Truth scalar freshness is sampled hot-path evidence while graph-owned cognition updates every replay. Sources remain the CUDA Graph and synchronization guidance above; no new model architecture claim is implied.
- Boundary-aware source-sequence staging note, June 2026: CUDA Graph guidance, NVIDIA constant-time launch work, and Triton persistent-kernel examples all reinforce the same engineering direction: avoid repeatedly re-submitting tiny host-controlled pieces when the memory addresses and control boundaries are known. NeuronSpark's fused Triton PLIF kernels and PyTorch's persistent grouped-GEMM MoE work reinforce that high-throughput spiking/sparse systems should batch stable local work without hiding routing or specialist boundaries. MARULHO therefore promotes source-sequence input staging inside training: warm metric-free sequences pre-stage the longest boundary-clean segment into the fixed CUDA input ring, then eight-token bursts consume exact pointer-checked slices while host-truth, sleep, metrics, and fallback boundaries remain visible. The 16384-token same-process A/B measured `1.119x` over per-quantum staging; the 65536-token service-style run proved active staging for `65408` tokens with zero graph/burst failures but did not beat the retained top because the host was GPU-contended. Sources: [CUDA Graphs](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html), [NVIDIA constant-time CUDA Graph launch](https://developer.nvidia.com/blog/constant-time-launch-for-straight-line-cuda-graphs-and-other-performance-enhancements/), [Triton Persistent Matmul](https://triton-lang.org/main/getting-started/tutorials/09-persistent-matmul.html), [NeuronSpark](https://arxiv.org/abs/2603.16148), and [PyTorch persistent cache-aware grouped GEMM](https://pytorch.org/blog/accelerating-moes-with-a-triton-persistent-cache-aware-grouped-gemm-kernel/).
- Native repeated-child graph note, June 2026: CUDA Graph documentation explicitly supports child graph nodes and repeated graph execution after instantiation, while PyTorch exposes `raw_cuda_graph()` only when `keep_graph=True` and `raw_cuda_graph_exec()` after instantiation. Triton persistent kernels remain the right direction for a future rewritten tick body, but Triton cannot launch an existing PyTorch CUDA Graph body from inside a kernel. MARULHO therefore promoted a narrower bridge: keep the proven one-tick graph body, compose it as an eight-child parent CUDA graph in a native extension, and launch one parent graph per eligible burst. This is different from the rejected eight-tick PyTorch graph body because it does not recapture or duplicate the transition cluster. The 131072-token long gate reached `4671.202 tokens/sec` with `train_compute=0.177193 ms/token`, `16382` native parent-graph launches for `131056` burst-owned tokens, zero native fallbacks/failures, zero graph/burst failures, and no observed contention. The same long command with native replay disabled reached `4340.160 tokens/sec`, and the older C++ loop over `cudaGraphLaunch(graph_exec)` was rejected because it still launched once per token and lost its long comparison. Sources: [CUDA Graphs](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html), [PyTorch CUDAGraph](https://docs.pytorch.org/docs/2.12/generated/torch.cuda.CUDAGraph.html), [NVIDIA constant-time CUDA Graph launch](https://developer.nvidia.com/blog/constant-time-launch-for-straight-line-cuda-graphs-and-other-performance-enhancements/), and [Triton Persistent Matmul](https://triton-lang.org/main/getting-started/tutorials/09-persistent-matmul.html).
- CUDA conditional-WHILE sequence promotion note, June 2026: NVIDIA documents conditional CUDA Graph nodes as a way to execute conditional or repeated graph portions without returning control to the CPU, and its programming guide documents device graph launch and conditional body requirements. PyTorch's Python CUDA Graph API still does not expose this control surface, so MARULHO added an isolated native CUDA extension instead of another Python wrapper. The extension wraps the retained one-tick child graph in a conditional `WHILE` parent graph and uses a tiny device counter kernel to preserve exact ordered per-token replay. A focused CUDA parity test matched sequential state across prototypes, velocity, thresholds, spike windows, predictive location/velocity/weights, and prediction error. The first clean 131072-token evidence changed implementation direction: retained native8 rerun reached `5035.537 tokens/sec`; conditional-WHILE q8 reached `5277.975`; and conditional-WHILE q16 reached `5559.473` with `train_compute=0.146978 ms/token`, `8190` conditional parent launches, `131040` conditional-owned tokens, zero sequence/native fallbacks, zero sequence/native failures, host-truth cadence `4097/126975`, startup compile/capture reported outside measured warm throughput, and `velocity_environment.v1` contention `not_observed`. Repeated promotion pairs then kept the q16 win in both orders (`5883.805` versus `5485.105`, then `6027.856` versus `5816.477`), and the post-promotion default run reached `6116.646 tokens/sec` with zero sequence/native failures or fallbacks. MARULHO therefore fixes conditional-WHILE q16 for eligible CUDA text sequences while leaving repeated-child native replay at eight tokens only as an internal pre-mutation fallback; the next lower-level direction is C++/CUDA, Triton persistent-kernel, or hybrid sequence ownership only if it beats this promoted boundary. Sources: [CUDA Graphs](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html), [NVIDIA conditional CUDA Graph nodes](https://developer.nvidia.com/blog/dynamic-control-flow-in-cuda-graphs-with-conditional-nodes/), [PyTorch CUDAGraph](https://docs.pytorch.org/docs/2.12/generated/torch.cuda.CUDAGraph.html), [Triton Persistent Matmul](https://triton-lang.org/main/getting-started/tutorials/09-persistent-matmul.html), [NeuronSpark](https://arxiv.org/abs/2603.16148), and [FlashRNN](https://arxiv.org/abs/2412.07752).
- Partial native burst replay rejection, June 2026: CUDA Graph/PyGraph research reinforces that graph benefits depend on stable addresses, stable shapes, and avoiding host-side deployment work inside the hot loop. MARULHO tested lazy native parent-graph construction for non-eight-token burst tails as a possible cleanup before a true multi-tick executor. Focused CUDA parity passed and Runtime Truth exposed `parent_graph_token_counts=[2, 8]`, but complete runtime on a 16640-token `tick_tokens=130` stress shape regressed to `2786.829 tokens/sec` versus `2907.600` with partial native replay disabled. The same run showed `384` host-truth burst fallbacks from tick/truth misalignment, making the larger architecture lesson clear: keep aligned 128-token source ticks and exact eight-token native bursts, and move future launch reduction into a startup-warmed or lower-level device-owned multi-tick executor rather than lazily compiling extra parent graphs during measured cognition. Sources: [CUDA Graphs](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html), [PyGraph](https://arxiv.org/html/2503.19779v2), and [NVIDIA CUDA Graph Performance Issues](https://docs.nvidia.com/dl-cuda-graph/troubleshooting/performance-issues.html).
- Live fused-service note, June 2026: an opt-in 1024-column checkpoint processed a real 24-token local-source tick with 24 fused executions, zero failures or fallbacks, observed CUDA execution, and a trace-derived `556.27 tokens/sec`. This confirms the fused research direction survives service integration. It also narrows the next problem: the same tick retained all-column homeostasis and broader per-token orchestration, so the next research-backed slice should combine exact tensor routing with candidate-scoped maintenance or a bounded persistent tick executor rather than adding another isolated kernel.
- CUDA Graph text-tick island note, June 2026: NVIDIA and PyTorch document CUDA Graphs as a way to replay fixed-address GPU work with lower CPU launch overhead, but require stable memory addresses and graph-safe control flow. MARULHO therefore captured a bounded production island rather than the whole trainer: input normalization/projection, exact fresh reconstruction distance, fused route/vote, and the in-place transition. Checkpoint capture was moved after state restoration, same-shape retrieval rebuilds preserve cache addresses, pointer changes fail before mutation, and sensory ticks bypass graph pre-routing. Controlled 128-tick parity was bit-exact across winners, reconstruction, competitive state, predictive state, spike windows, and prepared input/projected tensors. Three fresh-process 512-tick arms improved mean throughput from `176.24` to `264.46 ticks/sec` (`1.501x`), median latency reached `2.806-3.105 ms`, and measured VRAM rose from `10.76/26 MB` allocated/reserved to `18.88/50 MB`. A configured 24-token source tick proved 24 graph replays and zero failures through Runtime Truth but took `1240.473 ms`, so the next direction is removing remaining host orchestration and per-token stages, not adding another narrow graph. Sources: [NVIDIA CUDA Graphs](https://docs.nvidia.com/cuda/cuda-c-programming-guide/index.html#cuda-graphs), [PyTorch CUDAGraph](https://docs.pytorch.org/docs/stable/generated/torch.cuda.CUDAGraph.html), and [Accelerating PyTorch with CUDA Graphs](https://pytorch.org/blog/accelerating-pytorch-with-cuda-graphs/).
- Archival reservoir note, June 2026: bounded replay research supports maintaining representative experience buffers rather than synchronously preserving every complete payload. MARULHO keeps exact reservoir admission and every-tick assembly EMA/drift observation, but now decides admission before copying optional input/routing tensors from CUDA to the CPU archival ledger. This follows Rolnick et al.'s constrained-buffer result and latent/compressed replay work without changing replay selection or moving archival storage into VRAM. A broader lazy STC-decay prototype was rejected because complete-trainer A/B was mixed. Sources: [Experience Replay for Continual Learning](https://arxiv.org/abs/1811.11682), [Latent Replay for Real-Time Continual Learning](https://arxiv.org/abs/1912.01100), and [Compressed Latent Replays for Lightweight Continual Learning on Spiking Neural Networks](https://arxiv.org/abs/2407.03111).
- Bucket-source replay note, June 2026: complementary learning systems, synaptic tagging/capture, sparse replay, and modern-Hopfield-style associative recall all assume a selected local evidence window before recall or consolidation. MARULHO therefore treats hot-bucket source construction itself as part of the memory budget: `_candidate_indices_for_bucket_ids(...)` now reads bucket tails by cursor, requires an explicit source budget, reports `candidate_source_window_policy=tail_indexed_bucket_round_robin_no_full_bucket_materialization`, and forbids full-bucket source materialization or scan in replay, query readout, frontier planning, and awake ripple tagging. The refreshed external benchmark `..\..\MARULHO_reports\bounded_replay_window_20260623\bucket-candidate-source-window-explicit-budget.json` kept newest-candidate parity on a `65536`-entry hot bucket while reading exactly `32` source indices, scoring `32`, materializing `0`, keeping `candidate_source_full_bucket_scan=false`, keeping archival state CPU-resident, allocating `0.0 MiB` CUDA, and reducing mean source latency from `0.518753 ms` to `0.035120 ms` (`14.770743x`). The paired `524288`-token gate `..\..\MARULHO_reports\bounded_replay_window_20260623\hotpath-active-pressure-65536-524288-i32-bucket-candidate-explicit-budget-default-nosample.json` stayed in band at `6047.414 tokens/sec`, p95 `21.647 ms`, `train_compute=0.133284 ms/token`, bounded `12/65536` route rows, no observed contention, RTX 3060 memory `1787->1788 MiB`, and zero graph/native sequence failures.
- Lee, Dora, and Mejias, "Predictive coding with spiking neurons and feedforward gist signaling", 2024: supports the project posture that prediction error can be represented with spiking mechanisms and local learning, rather than treating the subcortex as a dense language model.
- "Predictive Coding with Spiking Neural Networks: a Survey", 2024: useful taxonomy for future code reviews because it separates explicit error-neuron approaches, membrane-potential error encodings, and implicit prediction-error encodings.
- "Confidence and second-order errors in cortical circuits", 2024: supports keeping predictive confidence in the Cognitive Signal alongside prediction error, because confidence-weighted errors are a plausible control variable rather than a display-only score.
- "Dynamic predictive coding: A model of hierarchical sequence learning and prediction in the neocortex", 2024: supports the existing emphasis on hierarchical temporal prediction, sequence learning, and context-sensitive dynamics.

### Sparse/GPU execution

- PyTorch's performance guidance recommends avoiding unnecessary accelerator work and synchronization. MARULHO found a stronger exact case: learned-chunk routing computed a dense column assembly and discarded it before candidate retrieval. Candidate-first preparation now scores only the retrieved competitive subset, while representations whose routing key depends on full assembly retain the dense path: https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html
- Thousand Brains Project architecture and Cortical Messaging Protocol documentation support independently operating modules with shared evidence contracts and voting. MARULHO uses that as a modularity constraint, not as proof of biological equivalence or a runtime dependency: https://thousandbrainsproject.readme.io/docs/architecture-overview and https://thousandbrainsproject.readme.io/docs/cortical-messaging-protocol
- "A flexible framework for structural plasticity in GPU-accelerated sparse spiking neural networks", 2025: supports the idea that structural plasticity and sparse neural communication should remain first-class when optimizing for GPU.
- "Sparse Spiking Neural-like Membrane Systems on Graphics Processing Units", 2024: reinforces that sparse spike-style workloads need GPU-aware representation choices, not only generic dense matrix acceleration.
- "Prosperity: Accelerating Spiking Neural Networks via Product Sparsity", 2025: useful for future acceleration design because it frames SNN speedups around activation sparsity and product sparsity.
- "Event- and Time-Driven Techniques Using Parallel CPU-GPU Co-processing for Spiking Neural Networks", 2017: supports treating AdEx-style neuron dynamics as a real GPU simulation surface, with accuracy/performance trade-offs that depend on integration method.
- Knight et al., "Fast simulations of highly-connected spiking cortical models using GPUs", 2021: supports reporting neuron and spike-delivery placement separately; large AdEx networks can benefit from GPU execution, but performance claims require observed runtime evidence.

### Neuron dynamics

- "Dynamics and Bifurcation Structure of a Mean-Field Model of Adaptive Exponential Integrate-and-Fire Networks", 2025: reinforces that AdEx voltage/adaptation state is not just an implementation detail; it is the dynamical substrate being claimed.
- "Reproduction of AdEx dynamics on neuromorphic hardware through data embedding and simulation-based inference", 2024: supports keeping AdEx state inspectable and serializable so hardware/runtime differences can be compared against observed dynamics.

### GPU routing and vector search

- Johnson, Douze, and Jegou, "Billion-scale similarity search with GPUs", 2017: establishes GPU k-selection, brute-force search, approximate search, and compressed-domain product-quantization search as first-class designs for high-dimensional retrieval.
- Ootomo et al., "CAGRA: Highly Parallel Graph Construction and Approximate Nearest Neighbor Search for GPUs", 2023/2024: supports treating CPU HNSW as only one point in the design space; graph ANN construction and search can be redesigned around GPU parallelism.
- Shi et al., "GPU-Native Approximate Nearest Neighbor Search with IVF-RaBitQ: Fast Index Build and Search", 2026: supports the direction of fusing IVF-style cluster routing with low-bit quantization and GPU-native distance kernels for high-throughput routing. MARULHO's historical standalone `ivf_router.py` was removed because it had no live trainer/config/Runtime Truth caller and used Python list/gather control rather than a bounded graph/cache-eligible GPU router.
- Zandieh et al., "TurboQuant: Online Vector Quantization with Near-optimal Distortion Rate", 2025: supports considering compressed inner-product scoring under a future bounded GPU router, but the legacy MARULHO `turboquant_plus` backend was removed after audit because it scanned all route rows, lacked graph-cache eligibility, and was slower than exact torch routing on the measured CUDA path.
- Sparse MoE and Expert-Choice capacity-routing work supports the route-candidate-bank contract now promoted in MARULHO: many columns may exist, but a training-owned bounded bank supplies capacity-limited route rows per tick. Hopfield-style recall remains valid only as bounded within-column recall, not as a global scheduler or memory substrate.

### Multimodal sensory grounding

- "Crossmodal hierarchical predictive coding for audiovisual sequences in the human brain", 2024: supports treating audio/visual sensory transitions as predictive evidence rather than detached media annotations.
- "Enhancing Audio-Visual Spiking Neural Networks through Semantic-Alignment and Cross-Modal Residual Learning", 2025: supports explicit semantic alignment between audio and visual SNN features.
- "SpikeMLLM: Spike-based Multimodal Large Language Models via Modality-Specific Temporal Scales and Temporal Compression", 2026: reinforces that multimodal spike-language work depends on modality-specific temporal scales; MARULHO sensory loaders should preserve device-resolved visual/audio tensors before encoding instead of flattening modalities through CPU staging.
- "Spike-HAR++: an energy-efficient and lightweight parallel spiking transformer for event-based human action recognition", 2024: reinforces event-based vision as a natural match for sparse spiking computation.

### Topographic and high-dimensional binding

- "TDSNNs: Competitive Topographic Deep Spiking Neural Networks for Visual Cortex Modeling", 2026: supports maintaining topographic binding as a first-class subcortical structure rather than treating all column interactions as dense all-to-all associations.
- Reimann et al., "Cliques of Neurons Bound into Cavities Provide a Missing Link between Structure and Function", 2017: supports keeping high-dimensional sparse binding structures observable, because the architecture's hypercube path is a topology claim as much as a performance claim.

### Context and abstraction dynamics

- Jiang and Rao, "Dynamic predictive coding: A model of hierarchical sequence learning and prediction in the neocortex", 2024: supports treating context as a dynamic sequence-prediction state that modulates lower-level gain rather than as passive metadata.
- "HetSyn: Versatile Timescale Integration in Spiking Neural Networks via Heterogeneous Synapses", 2025: supports the adaptive-context direction where heterogeneous temporal integration is a runtime mechanism, not merely a scalar hyperparameter.
- "Spiking Neural Networks with Adaptive Membrane Time Constant for Event-Based Tracking", 2025: supports keeping learned/adaptive timescale state observable when claiming temporal CUDA acceleration.

### Runtime self-model and observability

- "Active Inference for Learning and Development in Embodied Neuromorphic Agents", 2024: supports treating runtime status as part of the agent's developmental loop, not just external logging.
- "Active Inference as a Model of Agency", 2024: supports making differences in world model, uncertainty, and exploration/exploitation posture explicit in reports.
- "Self-evolving Embodied AI", 2026: supports requiring memory, task, environment, embodiment, and model adaptation evidence before claiming self-evolving or living-brain behavior.
- "Operational manifolds in spiking neural networks", 2026: supports adding lightweight health evidence for Subcortex stability, especially spike-train correlation statistics, drift indicators, reset/carry state policy, and energy/accuracy trade-off proxies.
- Grimaud, Longin, and Herzig, "SNN-Based Online Learning of Concepts and Action Laws in an Open World", 2026: supports keeping concepts, action laws, and runtime signals in the Subcortex control plane instead of routing active cognition through a static language backend.
- "Active inference and cognitive control: Balancing deliberation and habits through precision optimization", 2025: supports deriving deliberation pressure from surprise, uncertainty, control cost, and expected information gain rather than from free-form text generation.
- "Intermittent Active Inference", 2026: supports exposing control candidates as advisory replanning triggers, because replanning should fire when prediction error or expected free energy crosses a threshold rather than on every tick.

### Backend-neutral deliberation

- Putra, Marchisio, and Shafique, "SNN4Agents: a framework for developing energy-efficient embodied spiking neural networks for autonomous agents", 2024: supports treating embodied SNN control as the primary agent substrate rather than assuming language-model inference is the runtime core.
- "Towards biologically plausible model-based reinforcement learning in recurrent spiking networks by dreaming new experiences", 2024: supports moving dream/replay work toward spiking world-model mechanisms that can improve policy learning without requiring an LLM loop.
- "World Models for Cognitive Agents: Transforming Edge Intelligence in Future Networks", 2025: supports considering world models as embedded cognitive engines for prediction, planning, and causal reasoning, with language backends treated as optional interaction adapters.
- "SPikE-SSM: A Sparse, Precise, and Efficient Spiking State Space Model for Long Sequences Learning", 2024: supports exploring spiking/state-space sequence backends for long-context deliberation surfaces where an LLM would be too slow or opaque.
- "SNN-BERT: Training-efficient Spiking Neural Networks for energy-efficient BERT", 2024: supports preserving language-facing capability as a possible SNN-backed path rather than equating language with external LLM services.

### Text encoding and consolidation

- Zhu et al., "SpikeGPT: Generative Pre-trained Language Model with Spiking Neural Networks", 2023: supports treating language input as a sparse/event-coded sequence problem rather than as a detached dense text embedding concern.
- Public API boundary note, May 2026: SpikeGPT, NeuronSpark, Nord-AI, and SymbolicLight-style evidence supports moving language toward MARULHO-owned spiking sequence/readout machinery. It does not support preserving a top-level `marulho.cortex` mock/LLM-style package API, so the package is deleted after useful language packet/result contracts move into Subcortex/semantics ownership.
- Mock deletion note, May 2026: SNN-native language references make a deterministic mock-LM path actively misleading. MARULHO now removes MockCortex from production source; language/readout contracts should be tested with semantics-owned packets and grounded evidence fixtures, not mock Cortex generation.
- External adapter deletion note, May 2026: pure-SNN language reports such as NeuronSpark and SpikeGPT make the remaining NIM/MultiCortex module an architectural liability. Removing the importable adapter path keeps language work pointed at MARULHO-owned spiking sequence machinery instead of a static external chat backend.
- External embedding deletion note, May 2026: SNN-native language and associative-memory work supports keeping memory vectors under local sparse/SNN control. Removing NIMEmbedder prevents Episodic Memory from depending on remote API keys or external embedding telemetry while MARULHO develops device-owned encoders.
- Remote limiter deletion note, May 2026: event-driven SNN and local-plasticity research points throttling/evidence toward device-local spike and source workloads. Keeping a Cortex/NIM rate limiter after deleting the adapters preserves the wrong operational center, so the limiter is deleted with the external path.
- "NeuronSpark: A Spiking Neural Network Language Model with Selective State Space Dynamics", 2026: supports tracking SNN-native language generation as an emerging path, but its early loss/dialogue evidence means MARULHO should first expose language as a grounded Subcortex state decoder rather than reintroducing an LLM-style cognition core.
- "SymbolicLight V1: Spike-Gated Dual-Path Language Modeling with High Activation Sparsity and Sub-Billion-Scale Pre-Training Evidence", 2026: supports requiring explicit sparsity reports for SNN-language work. Its reported activation-sparsity focus strengthens MARULHO's decision to add a local decoder probe that reports sparse readout occupancy without claiming generation.
- "SpikeMLLM: Spike-based Multimodal Large Language Models via Modality-Specific Temporal Scales and Temporal Compression", 2026: reinforces that multimodal spike-language work depends on modality-specific temporal encoding and compression; MARULHO's language surface should preserve sensory timing evidence instead of flattening everything into text-first prompts.
- Liu, Liu, and Chen, "Scaling Natively-Trained Spiking Language Models to Multi-Domain Pre-training with 85% Global Activation Sparsity", 2026: supports the architectural bet that language-capable SNNs should preserve spike-gated associative lookup and sparsity, not collapse into dense transformer-style runtime claims.
- Stoeckl et al., "Learning Long Sequences in Spiking Neural Networks", 2024: supports preserving temporal sequence structure when moving text-derived traces onto accelerated tensor devices.
- Lv, Xu, and Zheng, "Spiking Convolutional Neural Networks for Text Classification", 2024: supports keeping text encoders as first-class SNN-facing modules with explicit feature/spike tensors.
- "Online Continual Learning via Spiking Neural Networks with Sleep Enhanced Latent Replay", 2025: supports the existing distinction between CPU archival replay storage and device-local replay computation.
- "Compressed Latent Replays for Lightweight Continual Learning on Spiking Neural Networks", 2024: supports keeping replay representations compact and explicit rather than treating memory consolidation as hidden model state.

### Developmental growth and pruning

- "Adaptive sparse structure development with pruning and regeneration for spiking neural networks", 2025: supports treating self-growth and pruning as a bounded structural-plasticity mechanism with explicit synaptic constraints, neuronal pruning, and regeneration rather than as open-ended architecture mutation.
- "A flexible framework for structural plasticity in GPU-accelerated sparse spiking neural networks", 2025: connects developmental plasticity to CUDA-first execution; growth/pruning evidence should include both sparse structural changes and observed device placement.
- "Self-Motivated Growing Neural Network for Adaptive Architecture via Local Structural Plasticity", 2025/2026: supports using local activation and weight-update statistics as growth/prune signals instead of central planner decisions, with reward stability as a promotion criterion.
- "Adaptively Pruned Spiking Neural Networks for Energy-Efficient Intracortical Neural Decoding", 2025: supports measuring pruning by retained task performance and efficiency gain, not just by lower parameter count.

### Novel correlations

- Spiking language models plus the retired Cortex decision imply a two-stage language roadmap: first, a grounded Subcortex Language Surface that decodes assemblies/replay/world-model state into auditable text; later, an SNN-native generator only after it can report sparsity, device placement, and support evidence. The responder's native-decode surface and the Cognitive Signal status surface are the first bridges because they attach confidence, memory support, evidence coverage, prediction error, neuromodulator pressure, and concept focus to language claims.
- NeuronSpark/Nord-style pure-SNN language projects imply language generation can plausibly move into spiking machinery, but MARULHO's target is not external model loading. Their code and checkpoints are references for a repo-owned language-neuron module, decoder, training loop, sparsity telemetry, and grounding gates that remain under MARULHO operator control.
- SymbolicLight-style high-sparsity language reports plus MARULHO Runtime Truth imply SNN language readiness should be summarized in liveness evidence, but only as a compact gate. Full research references, checks, and generator requirements remain separate so a status read cannot become text generation.
- Spike-based multimodal language work plus the Sensory Encoder direction imply language generation should be a decoder over time-preserving multimodal evidence. Operator UI should route "Subcortex" to spike dynamics and grounding evidence, not to a retired Cortex/Mind page.
- Text SNN work plus the CUDA-first evidence rule implies encoder reports must capture the observed placement of emitted feature vectors and spike traces, not just configured modules. This keeps language-facing telemetry grounded in runtime tensors instead of static declarations.
- SNN language readiness plus CUDA-first telemetry implies readiness gates should fail closed on observed CPU tensors even if configuration requested CUDA. This prevents language/readout promotion from masking CPU fallback behind static runtime intent.
- Spike-language decoder probes should report sparse code occupancy, leaky recurrent temporal state, and grounded slot support before any generator exists. A probe is useful only if it makes missing training/evaluation evidence explicit rather than acting as a deterministic text substitute.
- A local language-neuron adapter can be useful before a generator only if it reports bounded membrane/spike dynamics, activation sparsity, and device placement without decoding text. This gives MARULHO a repo-owned neuron boundary for future training while preserving the fail-closed language gate.
- SNN language adapter evaluation should be isolated before training: heldout grounded readout slots, grounding delta, activation-sparsity delta, Runtime Truth delta, and rollback evidence are needed before a local adapter can justify a controlled training loop. This follows continual-learning/replay evaluation patterns without granting generation capability.
- Heldout adapter evaluation should reuse the same local decoder and neuron adapter that readiness reports, because separate evaluator machinery would risk measuring a different substrate than the one being promoted. MARULHO therefore evaluates heldout readout-slot batches through the local spike decoder and PLIF-style adapter while keeping weights frozen.
- Structural plasticity plus replay promotion gates imply a clean self-growth loop: propose local growth/prune candidates from surprise and replay failures, evaluate them in isolated replay experiments, then promote only if Runtime Truth, grounding support, and device telemetry improve.
- Self-motivated growth controllers plus adaptive pruning evidence imply MARULHO's autonomy loop should grow topology from local stress signals and prune from sustained inactivity/redundancy, with replay-gated rollback. This is a cleaner path than a language planner that rewrites architecture.
- Structural-plasticity work plus MARULHO's hypercube binding layer imply that sparse topology mutation must be ledgered at the edge level: added/removed hub outreach edges are the first auditable self-growth/pruning evidence before broader model self-modification is allowed.
- GPU sparse structural-plasticity work plus ConceptStore growth pressure implies structural promotion readiness should fuse two evidence streams: semantic pressure to split/grow/prune concepts and binding/topology device evidence proving sparse edge state is observable on the runtime device.
- Adaptive pruning/regeneration work plus self-motivated local structural plasticity imply a third evidence stream for MARULHO: local STDP eligibility traces, synaptic scaling, inhibitory balance, spike-health risk, synaptic validation, and device placement should be summarized beside concept growth and binding mutation ledgers before any growth/prune evaluation is promoted.
- Structural-plasticity and adaptive-pruning work imply growth pressure is not itself promotion evidence. MARULHO structural readiness now requires observed binding or local-plasticity tensor placement, homeostatic local-plasticity state, and non-failed synaptic validation before an isolated growth/prune evaluation can be marked ready.
- Structural-plasticity, adaptive pruning/regeneration, and sparse CUDA SNN work imply isolated growth/prune trials should be judged by deltas, not intent. MARULHO therefore exposes `/terminus/subcortical-structural-plasticity/evaluate`, an isolated structural-plasticity evaluator that compares pre/post edge counts, spike-health risk, Runtime Truth verdicts, CUDA/device consistency, and rollback evidence without mutating the live runtime.
- Reward-modulated STDP, homeostatic plasticity, sparse GPU structural plasticity, event-based delay learning, and self-growing network work all point to local evidence windows rather than one-shot surprise. Recent growth-stability work also warns that newly inserted structures can be active but poorly integrated, so MARULHO now adds a checkpointed-candidate gate inside isolated structural evaluation: the candidate ticket must bind repeated failure or prune/sleep pressure to an exact baseline hash, candidate reason, cost/usefulness metrics, latency/RAM/VRAM impact, Runtime Truth summary, rollback artifact, and no-mutation proof before the reviewed transaction path can treat it as ready.
- Runtime Truth plus sparse structural-plasticity evidence implies a compact status summary is useful, but the full Structural Plasticity Gate Artifact must remain separate so topology cases, device payloads, and mutation ledgers do not become implicit execution commands.
- Sparse structural-plasticity and dynamic SNN pruning/regrowth work imply Runtime Truth should show whether growth/prune review has local plasticity, validation, rollback, and device prerequisites before an operator opens the full artifact. MARULHO therefore enriches `structural_plasticity_gate` with compact rule/backend, synaptic-validation, spike-health-risk, replay-review, isolated-evaluation, rollback-ledger, Runtime Truth improvement, and device-evidence breadcrumbs while still omitting structural cases and topology payloads.
- Dynamic sparse SNN pruning/regrowth and homeostatic rewiring work imply structural-evaluation readiness should bind the exact pre/post states that produced the topology delta. MARULHO therefore adds Structural Evaluation Snapshot Binding: canonical pre/post snapshot hashes, optional state revisions, revision-order validity, nonzero structural-delta evidence, and rollback-to-pre-snapshot hash matching are required before `/terminus/subcortical-structural-plasticity/evaluate` can report ready-for-operator-review.
- Dynamic sparse SNN work such as CH-SNN, C-DPSS, and GPU sparse structural-plasticity frameworks implies growth/prune should move through an explicit design boundary before any checkpoint-backed write path. MARULHO therefore adds `/terminus/subcortical-structural-plasticity/mutation-design`, a read-only Structural Mutation Design artifact that binds the isolated evaluation, snapshot hashes, rollback pre-snapshot hash, bounded edge delta, and operator confirmation while keeping checkpoint writes, growth/prune calls, and topology mutation disabled.
- Runtime-assurance and rollback-safe learning work imply a mutation design still needs a separate checkpoint preflight before any structural executor can exist. MARULHO therefore adds `/terminus/subcortical-structural-plasticity/mutation-preflight`, which recomputes the design hash, checks expected/current runtime revision, requires a rollback checkpoint path and future restore verification, and still blocks checkpoint writes, growth/prune calls, and topology mutation.
- NeuronSpark/Nord-style pure-SNN language work plus dynamic sparse structural-plasticity research supports MARULHO's SNN-only direction, but reference implementations should remain research inputs rather than runtime dependencies. MARULHO therefore adds `/terminus/subcortical-structural-plasticity/mutation-application`, a checkpoint-backed Structural Mutation Application executor that accepts only bound preflight evidence, current revision, operator confirmation, real checkpoint/restore verification, and an observed binding-hub edge delta before publishing a committed checkpoint.
- Candidate binding-growth trial note, June 2026: structural-plasticity work separates frequent neural dynamics from less frequent rewiring periods, while adaptive sparse-network work bounds structural attempts and grows from local activity evidence. MARULHO therefore converts predictive-owned repeated-failure candidates into a deterministic, edge-budgeted `binding_growth_trial_design.v1` artifact. The core planner snapshots sparse adjacency once, proposes only existing-column hypercube outreach, hashes the baseline topology and exact edges, and leaves checkpoint cloning, isolated cognitive evaluation, and mutation to later gates. This follows the slow structural epoch direction in [An Optimized Structural Plasticity Rule for Spiking Neural Networks on Multiprocessor Systems](https://arxiv.org/abs/2510.19764) and the fixed-attempt local rewiring direction in [Learning Efficient Deep Spiking Neural Networks with Structural Plasticity](https://arxiv.org/abs/2307.11735); neither paper is used as a runtime dependency.
- Operational-manifold and open-world SNN agent work plus Cortex retirement imply status vocabulary should identify only active control evidence. `retired_runtime_path` is deleted from active status, runtime, replay, and report contracts; former LLM paths are tested by absence, not represented as compatibility channels.
- Closed-loop neuromorphic and SNN homeostasis work imply runtime evidence should name the active control substrate, not the retired language loop. Replay/evidence code should read Subcortex Runtime Truth, spike health, memory pressure, and Subcortex Sleep Pressure directly.
- Runtime-assurance and operational-manifold SNN work imply status snapshots should minimize ambiguous inactive channels. Removing both the brain-runtime `cortex` sibling payload and the `retired_runtime_path` source keeps active liveness centered on Subcortex CUDA and spike-health surfaces.
- Active-inference policy selection and SNN sleep/homeostasis work imply fatigue/sleep pressure belongs to the control-state payload, not to a Cortex API argument or retired compatibility snapshot.
- Sleep-homeostasis and operational-manifold SNN work imply consolidation pressure should come from active Subcortex memory and sleep counters, not a retired compatibility snapshot. Subcortex Sleep Pressure is now the source policy/replay should read.
- Active-inference self-model work plus retired-path cleanup implies Operational Self-Model payloads should separate active Subcortex status from deleted runtime paths. Policy and replay should read Subcortex Sleep Pressure, not old runtime-path snapshots.
- Runtime-monitoring work for autonomous agents plus operational-manifold SNN evidence implies long-run reports should not foreground retired LLM paths. Operator-facing reports should emphasize Subcortex progress, Runtime Truth, memory pressure, spike health, and CUDA/device evidence.
- Runtime-assurance work plus Runtime Truth implies reports should expose active evidence, not a retired-path source of record. Keeping `retired_runtime_path` or `cortex_*` booleans creates an ambiguous operational channel.
- Closed-loop SNN control and runtime-monitoring work imply replay pressure should be derived from the same operational state channel operators inspect. Reading fatigue and sleep pressure from Subcortex Sleep Pressure keeps consolidation pressure auditable without preserving a retired-loop surface.
- Modular SNN and SNN-native language work imply MARULHO should remove lazy LLM/ThoughtLoop initialization paths rather than keep them dormant. A compatibility controller that can still build a ThoughtLoop keeps architectural gravity pointed at the wrong substrate.
- Runtime-assurance work plus Cortex retirement implies the retired path should be absent from active state, not represented as a small inert state channel. Deleting the retired-runtime holder removes ambiguous control paths instead of preserving audit evidence for old snapshots.
- Runtime safety-monitoring surveys plus SNN operational-manifold work imply dormant control channels are worse than absent channels: they invite false assurance. Removing the ThoughtLoop slot and action-mirroring branch keeps assurance evidence centered on Subcortex ledger, replay, and spike-health surfaces.
- SNN language and online-learning work imply language capability should be rebuilt as MARULHO-owned spiking sequence machinery. Keeping top-level Cortex exports for ThoughtLoop or NIM factories makes the deprecated LLM path look like the public cognition API, so the package boundary should retire those exports first.
- SNN-native language work such as NeuronSpark, SpikeGPT, and high-sparsity pure-SNN language reports imply text generation should become MARULHO-owned sequence/world-model machinery, not a dormant `ThoughtLoop` class. Blocking `ThoughtLoop()` construction keeps future language/thought work routed toward Subcortex modules with sparsity, grounding, and device evidence.
- ThoughtLoop body deletion note, May 2026: SpikeGPT, NeuronSpark, and SpikeMLLM make hidden text-loop code a liability even when construction raises. MARULHO therefore removes the runnable generation/sleep/background body and deletes the import boundary while language moves to Subcortex-owned spiking readouts.
- Long-sequence SNN and spiking state-space work imply MARULHO should separate sequence readout utilities from the retired cognition class. A deterministic deliberation merge helper is useful as a language-surface readout, but it should be callable without importing `ThoughtLoop` so future spiking sequence generators can own the actual dynamics.
- Language packet ownership note, May 2026: NeuronSpark and SpikingSSM evidence points language state toward sequence/readout packets owned by the active semantics layer. MARULHO therefore moves mode, depth, memory slots, and context packets into `marulho.semantics.language_packet` and deletes `marulho.cortex.core`.
- Prompt deletion note, May 2026: SNN-native language work points toward learned/readout dynamics with explicit support evidence, not a static bank of Cortex prompts. MARULHO deletes `marulho.cortex.prompts`; future text policy belongs beside Subcortex language surfaces.
- Spike readout evidence note, May 2026: NeuronSpark's state-space spiking dynamics and SpikeGPT's event-coded sequence framing support an owned intermediate layer before generation. MARULHO now exposes Subcortex Spike Readout Evidence: deterministic population-code bands, readout slots, and CUDA/device evidence that can guide a future in-repo SNN decoder without loading external checkpoints or producing text.
- Device evidence hardening note, May 2026: SNN-native language readiness must treat configured CUDA intent as weaker than observed Subcortex tensor placement. Spike readout evidence now reads actual device report keys such as competitive prototypes, predictive locations, binding state, and nested local plasticity before falling back to `tensor_device`.
- Decoder probe note, May 2026: SNN language reports such as NeuronSpark, SpikingSSMs, and SymbolicLight make a local sparse decoder path plausible, but not automatically safe. MARULHO therefore adds a Spike Language Decoder Probe that consumes grounded spike readout slots and reports sparse tensor occupancy, leaky recurrent temporal state, support, and device evidence while keeping text generation false.
- Language-neuron adapter note, May 2026: SNN-native language and adaptive-timestep work supports creating a MARULHO-owned PLIF-style adapter over decoder-probe sparse indices. The adapter reports membrane/spike dynamics, sparse activation, adaptive timesteps, and device evidence, but remains non-generative until a controlled training/evaluation loop exists.
- Language-adapter evaluation note, May 2026: continual-learning SNN work such as PS-SNN and HLML-SNN reinforces that expansion/training should be judged through explicit replay/evaluation evidence. MARULHO therefore exposes `/terminus/snn-language-evaluation` as a read-only gate for adapter grounding, sparsity, Runtime Truth delta, and rollback evidence before any training or generation path can be considered.
- Heldout evaluator note, May 2026: sparse SNN language and continual-learning work imply that adapter promotion needs concrete heldout support, not only a readiness checklist. MARULHO therefore adds `/terminus/snn-language-evaluation/heldout`, a deterministic heldout adapter evaluator that reports grounded support, active spike counts, activation sparsity, and device evidence while keeping generation and training disabled.
- Training-readiness note, May 2026: NeuronSpark-style next-token SNN training, local Hebbian/predictive-coding SNN learning, and continual-learning SNN evidence imply MARULHO needs an explicit trainer-design gate after heldout adapter support. MARULHO therefore exposes `/terminus/snn-language-training/readiness`, which can approve only local trainer design review while keeping weight updates, text generation, and cognition-substrate promotion disabled.
- Trainer dry-run note, May 2026: local Hebbian, predictive-coding, and delay-learning SNN work implies the first MARULHO-owned language trainer should be evidence-only before it becomes a runtime learner. MARULHO therefore exposes `/terminus/snn-language-training/dry-run`, an isolated local update over spike readout transitions that reports transition support, sparsity, and device evidence while discarding weights and keeping generation disabled.
- Trainer evaluation note, May 2026: sparse language-model evidence and recurrent sequence-learning SNN work imply dry-run support must pass a separate promotion gate before any live trainer exists. MARULHO therefore exposes `/terminus/snn-language-training/evaluate`, a read-only evaluator for dry-run support, sparse weight evidence, device placement, Runtime Truth delta, and rollback evidence while keeping runtime trainer promotion disabled.
- Sequence prediction note, May 2026: spiking temporal-memory, predictive-coding, and sparse SNN language work imply that language should first become next-code prediction over grounded spike populations, not text generation. MARULHO therefore exposes `/terminus/snn-language-sequence/predict`, which predicts sparse population-code indices from ephemeral local transition weights while keeping text decoding, weight persistence, and cognition-substrate promotion disabled.
- Persistent transition memory note, May 2026: recurrent SNN sequence-memory, Nord-style persistent memory routing, and online local-learning work imply live plasticity must feed back into future sparse prediction. MARULHO therefore lets checkpointed sparse transition weights from `/terminus/snn-language-sequence/plasticity-live-application` bias `/terminus/snn-language-sequence/predict`, with explicit influence evidence and no text decoding.
- Readout-draft note, June 2026: NeuronSpark-style selective state dynamics, Nord-style persistent memory routing, and pure-SNN language work imply MARULHO should cross from sparse next-code prediction into text only through grounded local labels first. MARULHO therefore adds `/terminus/snn-language-sequence/readout-draft`, a read-only bounded draft generator that maps sparse prediction and persistent transition influence to grounded readout-slot labels while blocking free-form language, fact promotion, action authority, and cognition-substrate claims.
- Readout-rollout note, June 2026: NeuronSpark, SpikingSSMs, SPikE-SSM, and compute-in-memory SSM/SNN sequence work all point to temporal sparse state rollout as the next language step after one-code prediction. MARULHO therefore adds `/terminus/snn-language-sequence/readout-rollout-candidate`, a read-only multi-step sparse transition rollout over MARULHO-owned transition memory that emits only grounded readout-vocabulary labels and keeps free-form generation, fact/action promotion, training, plasticity, and runtime mutation disabled.
- Readout-rollout replay-evaluation note, June 2026: NeuronSpark and Nord-AI are useful open-source references for pure-SNN language pressure, but MARULHO should not import their runtime code or checkpoints as dependencies. MARULHO therefore adds `/terminus/snn-language-sequence/readout-rollout-candidate/replay-evaluation`, a read-only gate that converts MARULHO-owned rollout traces into hash-addressed, grounded replay-review targets while leaving ledger recording, replay priority, training, plasticity, free-form language, and runtime mutation disabled.
- Readout-rollout ledger-record note, June 2026: hippocampal replay and SNN continual-learning work argue that sequence replay should be recorded as reviewed evidence before any consolidation or plasticity. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/record-rollout-replay-evaluation`, which durably records operator-approved rollout replay evaluations into a separate rollout-event channel while keeping draft replay priority, live replay, training, plasticity, mutation, facts, actions, and cognition-substrate claims disabled.
- Readout-rollout rehearsal-promotion note, June 2026: prioritized hippocampal replay, temporal-aligned SNN rehearsal, sleep-enhanced latent replay, and compressed latent replay all imply that sequence selection should be auditable before consolidation. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-rehearsal-promotion-policy`, a read-only deterministic policy over recorded rollout evidence that reports grounded trace integrity, sparse occupancy, and CUDA/device evidence while keeping rehearsal execution, draft replay priority, training, plasticity, facts, actions, and cognition-substrate claims disabled.
- Bounded readout-rollout rehearsal source note, June 2026: temporal replay and compressed latent replay support selecting compact grounded rollout traces, not scoring every retained rollout record before a rehearsal policy applies its output limit. MARULHO therefore bounds `snn_language_readout_rollout_rehearsal_promotion_policy.v1` to a CPU-resident `16`-event recent rollout source window with `32` targets per event, and reports `bounded_snn_readout_rollout_rehearsal_source_window.v1` with no global candidate/score scan, no raw text payload, no language reasoning, `runs_live_tick=false`, `runs_every_token=false`, and `gpu_used=false`. The benchmark `reports/bounded_replay_window_20260618/snn-rollout-rehearsal-source-window.json` matched the diagnostic full-retained top rollout, scored `16` of `2048` retained rollout events (`128x` less scoring work), averaged `2.090592 ms` versus `309.922768 ms` (`148.246414x`), used `0.066692 MiB` traced peak Python allocation, and allocated `0.0 MiB` CUDA memory. The paired `524288`-token hot-path run stayed in band at `6339.682 tokens/sec`, `train_compute=0.129022 ms/token`, `prepare_training=0.006321 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, GPU memory `1867->1865 MiB`, and zero graph/native/sequence failures; the velocity sampler observed brief GPU-side contention (`22%` max utilization), so the claim is throughput protection rather than contention-free hardware.
- SNN rollout rehearsal baseline-removal note, June 2026: the retained rollout scorer is no longer kept as benchmark-local executable code. The external maintained-only report `..\..\MARULHO_reports\bounded_replay_window_20260623\snn-rollout-rehearsal-legacy-baseline-removed.json` passed by selecting the seeded recent high-signal rollout from a `16/2048` CPU source window, returning `8` candidates, reporting `retired_full_retained_rollout_policy_absence.implementation_present=false` and `diagnostic_callable=false`, averaging `3.282180 ms`, and using no CUDA archival allocation.
- Readout-rollout rehearsal-evaluation note, June 2026: spiking temporal-memory replay, STAER-style temporal alignment, and cortical spontaneous replay evidence imply rehearsal quality should be measured on sparse step trajectories before any consolidation. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-rehearsal-evaluation`, an isolated ephemeral tensor replay over policy-approved rollout evidence that measures sparsity and adjacent-state temporal continuity on the recorded observed device while discarding weights, checkpoints, and runtime updates.
- Readout-rollout rehearsal-experiment note, June 2026: stable sequence propagation, repeated presentation in spiking temporal-memory systems, and offline replay consolidation research imply a trajectory should survive bounded repeated cycles before any later write path exists. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-rehearsal-experiment`, a deterministic ephemeral repeated-cycle experiment that reports stability, drift, continuity retention, and a provenance hash while keeping persistence, checkpoint writes, live replay, and plasticity disabled.
- Readout-rollout consolidation-design note, June 2026: local STDP, homeostatic components in temporal-memory SNNs, and sleep-based synaptic regularization imply consolidation should first be an operator-reviewable bounded local design. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-consolidation-design`, which proposes capped local temporal-synapse deltas, homeostatic decay, normalization, and rollback evidence while keeping structural writes, persisted weights, live replay, and plasticity disabled.
- Readout-rollout consolidation-shadow note, June 2026: event-based local plasticity, memory-efficient STDP connectivity, and homeostatic recurrent-SNN work imply proposed updates should be materialized as sparse device-visible shadow tensors before any write path exists. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-consolidation-shadow-delta`, which computes bounded local deltas on CUDA/device, reports fallback honestly, and discards tensors without writing synapses, weights, checkpoints, or runtime state.
- Readout-rollout consolidation-shadow-application-preflight note, June 2026: local online-plasticity and homeostatic structural-plasticity work imply functional synapse deltas must be integrity-checked separately from growth and pruning before an application path exists. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-consolidation-shadow-application-preflight`, which verifies exact design/shadow hashes, bounded local coordinates, normalization, homeostatic decay, CUDA/device truth, rollback-snapshot binding, and sparse topology budgets against server-held transition memory. Missing edges are routed to separate structural-growth review while the preflight applies no synapse write or runtime mutation.
- Readout-rollout developmental-plasticity note, June 2026: GPU structural-plasticity, adaptive sparse SNN development, and developmental pruning/regeneration research all separate topology growth from ordinary weight updates. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-developmental-plasticity-review`, which turns missing rollout transition edges into read-only growth-candidate evidence against server-held sparse memory while enforcing locality, fanout, row-mass, and global edge budgets without issuing permits or mutating synapses.
- Readout-rollout regeneration-adapter note, June 2026: replay-backed structural regeneration work implies rollout growth evidence should be translated into the existing regeneration-design shape without becoming a permit or live write. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-regeneration-proposal-adapter`, which emits bounded candidate synapses, locality radius, explicit rollout-growth evidence, and blocked replay/permit requirements while leaving replay evidence, permit issuance, checkpointing, and mutation to the established regeneration flow.
- Readout-rollout regeneration-replay-artifact-review note, June 2026: replay-consolidation and circuit-reactivation work imply a structural growth design should be paired with replay provenance before any permit request. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-regeneration-replay-artifact-review`, which recomputes adapter and server-owned replay-artifact hashes, binds internal-ledger replay evidence to the regeneration design preview, and prepares a permit-request preview without issuing permits, checkpointing, or mutating synapses.
- Replay-bound regeneration-design note, June 2026: prediction-error driven SNN plasticity and replay consolidation work imply structural growth should use replay-proven mismatch pressure, not a free-floating design default. MARULHO therefore records compact mismatch/pressure summaries in server-owned SNN transition-memory replay artifacts and lets rollout regeneration review replace adapter `mismatch_score=0.0` with hash-protected replay mismatch before permit hashing.
- Readout-rollout regeneration-permit-request note, June 2026: synaptic consolidation and replay literature imply permit issuance should be an explicit operator-confirmed boundary between replay evidence and structural mutation. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-regeneration-permit-request`, which delegates permit creation to the existing replay controller while leaving checkpointing, synapse growth, pruning, and runtime mutation of transition weights to the established regeneration executor.
- Readout-rollout regeneration-application-preflight note, June 2026: homeostatic replay-consolidation work implies a final non-mutating check should bind permit evidence to current runtime revision and checkpoint intent before structural mutation. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-regeneration-application-preflight`, which emits an executor-shaped regeneration proposal only when permit, revision, and checkpoint evidence are present while leaving checkpoint writes and synapse growth to the existing executor.
- Readout-rollout regeneration-application note, June 2026: structural-plasticity and sequence-replay SNN work supports local synapse growth when bounded by homeostatic row mass, sparse topology, replay provenance, and rollback. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rollout-regeneration-application`, an operator-confirmed wrapper that delegates preflight-approved, replay-bound rollout growth to the existing checkpoint-backed regeneration executor instead of adding a second mutation path. The service API now proves an accepted path from server-owned replay mismatch evidence through permit issuance to checkpoint-backed sparse synapse growth.
- Bounded readout emission note, June 2026: SpikeGPT, SpikingSSM/NeuronSpark-style language work, and recurrent multi-timescale SNN memory evidence all support sparse sequence readout as the first honest language surface before open-ended generation. MARULHO therefore adds `/terminus/snn-language-sequence/readout-emission`, which exposes operator-visible language only from a ready SNN readout draft whose labels are bound to sparse trajectory, prediction, transition-memory evaluation, and persistent-weight hashes while keeping facts, actions, cognition-substrate promotion, checkpoints, and freeform generation disabled.
- Emission-review assurance note, June 2026: human-in-the-loop neuromorphic assurance and runtime-assurance work imply that output display should be auditable without becoming learning authority. MARULHO therefore adds `/terminus/snn-language-sequence/readout-emission/operator-review`, an operator-confirmed display-history record for ready bounded SNN emissions that stores emission/provenance hashes while staying separate from replay memory, plasticity consent, fact promotion, action authority, and cognition-substrate evidence.
- Bounded emission-review replay-policy source note, June 2026: modern Hopfield-style association remains useful only after a selected local source window exists, while CLS, replay, sparse replay, and synaptic-tagging evidence argue against matching every retained reviewed emission against every retained readout before a replay context can even be proposed. MARULHO therefore retires the retained-review/readout match scan in `snn_language_readout_emission_replay_evaluation_policy.v1` and the follow-on design verifier: both now use `bounded_snn_emission_review_replay_policy_source_window.v1`, capped to `16` recent reviewed emissions and `16` recent internal readout events, CPU archival/score placement, no global candidate/score scan, no raw reviewed-text payload, no language reasoning, `runs_live_tick=false`, `runs_every_token=false`, and `gpu_used=false`. The focused benchmark `reports/bounded_replay_window_20260618/snn-emission-review-replay-policy-source-window.json` matched the diagnostic full-retained policy/design top candidate, selected `8` hash-only seeds, checked `32` source events instead of `4096` retained review/readout records (`128x` less match work), averaged `2.476164 ms` versus `166.924984 ms` (`67.412734x`), used `0.046277 MiB` traced peak Python allocation, and allocated `0.0 MiB` CUDA memory. After an earlier profiled hot-path run was rejected because external GPU contention was present, the clean profiled rerun processed `524288` tokens at `6376.714 tokens/sec`, `train_compute=0.128297 ms/token`, `prepare_training=0.006487 ms/token`, `finalize_total=0.005965 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, GPU memory `2122->2123 MiB`, no observed contention, and zero graph/native/sequence failures; a no-profile rerun reached `6392.672 tokens/sec`.
- Maintained-only replay/status benchmark cleanup note, June 2026: the same research boundary now applies to repo-local evidence harnesses. The executable full-retained emission-review policy/design comparator and the full-retained Runtime Truth replay-path status projection comparators were deleted rather than kept as side baselines. The maintained reports under `..\..\MARULHO_reports\bounded_replay_window_20260623\` pass with deterministic seeded quality checks, explicit absence sentinels, CPU archival/score placement, no global scans, no raw replay text, no hidden language reasoning, no live tick, no every-token cadence, and `0.0 MiB` CUDA archive allocation. The paired `524288`-token hot-path gate reached `6518.530 tokens/sec`, `train_compute=0.124903 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, flat `1747 MiB` RTX 3060 memory, no observed contention, no in-window environment samples, and zero graph/native/sequence failures.
- Transition-memory prediction-evaluation note, June 2026: temporal-memory SNN work and replay-based sequence prediction imply persistent memory must earn promotion through repeated prediction utility, not through existence or a single influenced next-code. MARULHO therefore adds `/terminus/snn-language-sequence/transition-memory-prediction-evaluation`, comparing baseline and memory-assisted sparse predictions across grounded windows while keeping generation, training, plasticity, fact promotion, and runtime mutation disabled.
- Readout-readiness coupling note, June 2026: NeuronSpark-style long-sequence SNN language work, SpikingSSM sequence evidence, and temporal-memory SNN replay all point to the same guardrail: sparse memory influence should be useful across windows before it is allowed to justify text readout. MARULHO therefore separates draft emission from draft readiness; `/terminus/snn-language-sequence/readout-draft` may emit bounded grounded labels, but promotion now requires a non-worsening `/terminus/snn-language-sequence/transition-memory-prediction-evaluation` artifact with influenced predictions and MARULHO-owned, non-mutating provenance.
- Evaluated-prediction provenance note, June 2026: replay and sequence-evaluation systems only become useful control evidence when the evaluated trajectory is the same trajectory later promoted. MARULHO therefore hashes SNN language prediction inputs, current readout slots, persistent transition-memory weights, evaluation windows, and evaluated memory-backed predictions; readout-draft readiness now requires exact provenance matching instead of trusting a passing evaluation summary from another context.
- Readout-evidence ledger note, June 2026: continual-learning replay and memory-consolidation work imply a living system needs bounded, replayable evidence traces, not only transient endpoint responses. MARULHO therefore adds a command-shaped SNN Language Readout Evidence Ledger that records only operator-confirmed, provenance-matched bounded readout drafts and persists them in service state for later replay/evaluation while keeping free-form generation, fact promotion, action authority, and cognition-substrate claims disabled.
- Readout replay-priority note, June 2026: prioritized replay, sleep-mediated SNN consolidation, and spiking sequence-replay work imply the next autonomy step is ranking bounded evidence for isolated rehearsal, not executing learning immediately. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/replay-priority`, a read-only advisory surface over the readout ledger that ranks provenance-bound evidence by deterministic recency, repetition, and transition-memory reuse while keeping generation, training, plasticity, mutation, facts, and actions disabled.
- Readout rehearsal-evaluation note, June 2026: latent replay and spiking sequence-timing work imply a rehearsal candidate should first be measured as sparse activity before any live replay or plasticity is considered. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rehearsal-evaluation`, which converts prioritized readout-ledger candidates into isolated sparse rehearsal vectors, reports activation sparsity, similarity, priority support, and tensor device evidence, and keeps runtime mutation, training, plasticity, generation, facts, and actions disabled.
- Readout rehearsal-experiment note, June 2026: sleep-enhanced and compressed latent replay work in SNNs suggests replay should pass a bounded pressure/stability simulation before it can become a live consolidation path. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/rehearsal-experiment`, an isolated read-only experiment that estimates pressure non-worsening from sparse readout rehearsal cycles while persisting no weights and keeping live replay, plasticity, generation, facts, actions, and cognition-substrate promotion disabled.
- Readout replay-design note, June 2026: stability-plasticity work and replay-buffer cost studies imply replay must be constrained before execution, especially in a sparse living-brain path. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/replay-design`, a read-only operator-review gate that selects hash-addressed readout evidence targets and bounds future isolated replay by candidate count, replay cycles, pressure-gain threshold, stability floor, and rollback evidence while keeping execution, mutation, plasticity, generation, facts, and actions disabled.
- Readout replay dry-run note, June 2026: controlled spiking sequence replay and SNN consolidation work imply replay speed/cycles should be measured as isolated sparse dynamics before any weight promotion. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/replay-dry-run`, an operator-approved dry-run that accepts only design-ready, internal-ledger-backed, hash-addressed targets, reports explicit CUDA/device placement, blocks silent CUDA fallback, and keeps checkpoints, runtime mutation, plasticity, decoding/generation, facts, and actions disabled.
- Readout plasticity-preflight note, June 2026: local-plasticity and replay-consolidation work imply sparse replay should become plasticity intent only after a second read-only review of pressure stability, provenance, Runtime Truth, and rollback evidence. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/plasticity-preflight`, a bridge gate that emits bounded candidate replay sequences from dry-run sparse indices for the existing application-design path while keeping live replay, mutation, plasticity, checkpoints, generation, facts, and actions disabled.
- Readout replay-bridge note, June 2026: predictive-coding SNNs, self-normalizing STDP, and replay-based continual learning all point toward reusing one audited local-plasticity path rather than creating separate update logic for each evidence source. MARULHO therefore adds `/terminus/snn-language-sequence/readout-ledger/plasticity-replay-bridge`, a read-only adapter that converts readout-preflight evidence into the existing `snn_language_plasticity_replay_experiment.v1` contract and carries canonical replay sequences, copied application-design bounds, a sparse-transition target hint, and checkpoint-transaction requirements for the established shadow/readiness/preflight path while preserving readout provenance and keeping live replay, mutation, plasticity, checkpoints, generation, facts, and actions disabled.
- Readout-derived live-application note, June 2026: recent online/local SNN plasticity work emphasizes stabilization, normalization, rollback, and explicit safety boundaries before live weight changes. MARULHO therefore allows readout-derived evidence to reach live transition-memory mutation only through the existing `/terminus/snn-language-sequence/plasticity-live-application` executor after shadow delta, live readiness, live preflight, current revision, operator confirmation, and checkpoint evidence; no readout-specific executor is introduced.
- Readout-derived synapse-provenance note, June 2026: replay/plasticity work on continual SNNs warns that weight changes without source evidence are hard to stabilize or audit after replay, while sparse/online SNN work points to bounded weights and explicit topology constraints as stability requirements. MARULHO therefore preserves readout evidence, prediction, transition-memory evaluation, persistent-weight, sequence, and sparse-index provenance through shadow deltas, applied synapses, recent live-application events, and checkpoint-restored `synapse_provenance_by_key` state, then audits exact ledger-field matches, recomputed ledger material hashes, canonical 64-neuron sparse keys, finite bounded weights, and source-index consistency rather than only checking that a readout hash exists.
- Transition-memory homeostasis note, May 2026: sleep-based recurrent-SNN regularization, self-normalizing STDP, astrocyte-gated stability, and adaptive sparse pruning/regeneration all imply persistent transition memory must stabilize growth explicitly. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-homeostatic-maintenance`, a checkpoint-backed mutation command that decays weights, bounds outgoing row mass, prunes weak edges, and records maintenance evidence.
- Transition-memory sleep-policy note, May 2026 / updated June 2026: sleep-enhanced latent replay, active Subcortex Sleep Pressure, and replay-bound structural plasticity imply maintenance and growth should be sequenced by an advisory scheduler without becoming automatic hidden mutation. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-sleep-policy`, a read-only bridge from active sleep/replay/readout-ledger/rollout-regeneration evidence to the next operator-confirmed gate: replay artifact review, regeneration permit request, regeneration application, or homeostatic maintenance after growth.
- Sleep-plasticity review-ticket note, June 2026: sleep/replay consolidation and synaptic homeostasis work couple replay, memory stabilization, and downscaling, but they do not collapse those operations into one automatic write. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-ticket`, a Replay Controller-owned hash ticket that records the reviewed Sleep Plasticity Policy, operator identity, current Runtime State revision, recommended action, suggested endpoint, and evidence hashes while keeping replay recording, permit issuance, checkpoint writes, growth/pruning, plasticity, and runtime mutation disabled.
- Sleep-plasticity ticket-queue note, June 2026: autonomous consolidation needs visibility into pending reviewed gates, but sleep/replay/homeostatic-plasticity research still argues against merging review, replay, and weight updates into one hidden action. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-tickets`, a read-only queue over verified Sleep Plasticity Review Tickets that reports stale/tamper counts, pending action counts, and the latest reviewed next gate without scheduling, replaying, issuing permits, writing checkpoints, or mutating transition memory.
- Sleep-plasticity autonomy-proposal note, June 2026: online SNN continual-learning work increasingly uses gates, metaplasticity, homeostasis, and replay regulation to decide when plasticity should be allowed. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/autonomy-proposal`, a non-executing candidate derived only from verified Sleep Plasticity Review Tickets so autonomy can inspect the next reviewed gate while action execution, replay, permits, checkpoints, growth/pruning, plasticity, and structural writes remain disabled.
- Sleep-plasticity autonomy-status note, June 2026: gate evidence must be operationally visible before any future scheduler can be evaluated. MARULHO therefore attaches the verified non-executing Sleep Plasticity Autonomy Proposal to `/terminus/living-loop`, `/terminus/policy-actuator`, `/terminus`, and `/status`, with a condensed gate inside Runtime Truth evidence, while preserving the same blocked action, replay, checkpoint, plasticity, and structural-write boundaries.
- Sleep-plasticity scheduler-experiment note, June 2026: sleep-based recurrent-SNN regularization, sleep-enhanced replay, and gated multi-timescale plasticity all suggest that consolidation timing must be measured before it is automated. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/scheduler-experiment`, an isolated control-plane experiment that repeatedly verifies stable ticket/revision/action/next-gate identity across bounded cycles, emits deterministic hash provenance, installs no scheduler, calls no suggested endpoint, and leaves replay, permits, checkpoints, growth/pruning, plasticity, and runtime mutation disabled.
- Sleep-plasticity scheduler-design note, June 2026: multi-timescale plasticity work indicates that fast eligibility and slow homeostatic gates require explicit scheduling boundaries rather than an unreviewed timer. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/scheduler-design`, a deterministic read-only design proposal that binds stable experiment evidence to bounded review cadence, minimum stable-cycle evidence, current-revision checks, and operator confirmation while scheduler installation, endpoint execution, replay, permits, checkpoints, growth/pruning, plasticity, and runtime mutation remain disabled.
- Sleep-plasticity scheduler-design review-ticket note, June 2026: sleep replay and homeostatic down-selection are coupled but distinct operations, and recent SNN sleep regularization work uses bounded periodic offline phases rather than unconstrained mutation. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/scheduler-design/review-ticket`, a durable Replay Controller-owned operator review record that recomputes current internal design evidence before recording acceptance while scheduler installation, endpoint execution, replay, permits, checkpoints, growth/pruning, plasticity, and transition-memory mutation remain disabled.
- Sleep-plasticity scheduler-design review-ticket queue note, June 2026: replay-prioritization work distinguishes memory selection from consolidation and renormalization. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/scheduler-design/review-tickets`, a read-only queue that verifies accepted scheduler-design evidence, reports stale/tampered records, and exposes the latest current design without installing a scheduler, calling endpoints, or mutating runtime state.
- Sleep-plasticity scheduler-installation autonomy note, June 2026: recent work on sleep consolidation emphasizes temporal structure, neuromodulatory prioritization, and a balance between strengthening and weakening rather than one undifferentiated sleep mutation. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/scheduler-installation-autonomy-proposal`, a read-only autonomy candidate over the latest verified accepted design while scheduler creation, timer registration, workers, endpoint calls, replay, plasticity, and runtime mutation remain disabled.
- Sleep-plasticity scheduler-installation preflight note, June 2026: periodic offline phases can stabilize recurrent STDP-SNN weight dynamics, but a configured offline phase is distinct from executing consolidation. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/scheduler-installation-preflight`, a deterministic integrity review of accepted cadence and revision-bound design evidence that creates no scheduler, timer, worker, endpoint call, replay run, or plasticity update.
- Sleep-plasticity scheduler-installation visibility note, June 2026: neuromodulatory and oscillatory sleep research suggests that timing and prioritization are system-level control evidence, while synaptic remodeling remains a separate local operation. MARULHO therefore exposes a compact scheduler-installation autonomy gate in Living Loop, Policy Actuator, Terminus, Status, and Runtime Truth while keeping the full accepted-design queue and installation-review preflight behind dedicated read-only endpoints.
- Sleep-plasticity passive review-scheduler installation note, June 2026: periodic offline phases in recurrent SNN regularization motivate durable cadence state, but timing evidence does not itself justify replay or synaptic updates. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/install` and `/review-scheduler`: an operator-confirmed persistent MARULHO configuration and a read-only due-state surface that register no OS timer, start no worker, call no endpoint, record no replay, and apply no plasticity.
- Sleep-plasticity review-scheduler cycle-inspection note, June 2026: biological replay is temporally structured and neuromodulated; cadence can open an inspection window without deciding which sequence to replay or which synapse to change. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/cycle-inspection`, a deterministic read-only due-cycle artifact that preserves the reviewed endpoint for operator inspection while endpoint execution, replay, checkpoints, and plasticity remain disabled.
- Sleep-plasticity review-scheduler cycle-acknowledgment note, June 2026: biological sleep is cyclic, and periodic offline phases stabilize recurrent STDP-SNN dynamics without implying one unbounded consolidation run. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/cycle-acknowledgment`, an operator-confirmed cadence-only transition that requires a verified due-cycle replay review ticket, binds the active scheduler configuration hash, appends an immutable successor configuration, and advances the passive review deadline by exactly one accepted interval. It registers no timer, starts no worker, calls no endpoint, records no replay artifact, writes no checkpoint, and applies no plasticity.
- Sleep-plasticity review-scheduler cycle-acknowledgment preflight note, June 2026: phase-specific sleep processing and periodic SNN offline regularization support keeping cadence bookkeeping separate from replay and plasticity execution. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/cycle-acknowledgment-preflight`, a deterministic read-only integrity review over active scheduler hash, due state, due-cycle replay review ticket, deadline binding, and one-time ticket consumption before cadence metadata may advance.
- Sleep phase-separation note, June 2026: sleep-memory research distinguishes NREM sharp-wave/spindle replay-like consolidation mechanisms from REM/theta-associated integration, excitability reset, and stabilization hypotheses, while recent SNN sleep work also separates replay sampling from homeostatic regularization. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/sleep-phase-separation-proposal`, a read-only proposal with an NREM-like replay-nomination gate over bounded due-cycle evidence and a REM-like stabilization-review gate over cycle-acknowledgment preflight. Both phases remain non-executing and blocked from replay, artifact recording, checkpoints, and plasticity.
- REM-like homeostatic stabilization preflight note, June 2026: sleep homeostasis and REM/theta stabilization work suggest that integration/down-selection pressure should be inspected before any synaptic maintenance write. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/rem-like-homeostatic-stabilization-preflight`, a read-only bridge from REM-like stabilization review to the existing checkpoint-backed Transition Memory Homeostasis command. It verifies phase readiness, transition-memory pressure, and already-bounded maintenance parameters without silently normalizing unsafe controller input, then stops before endpoint execution, replay, artifact recording, checkpoints, pruning, decay, or plasticity.
- SNN readout rollout server-state binding note, June 2026: NeuronSpark, SpikeGPT, Sorbet, Nord-AI, SpikingSSMs, and sparse neuromorphic language work all point toward sparse/event-driven language as a real direction, but MARULHO's strength is owning the memory and rollout path internally rather than importing an outside generator. MARULHO therefore hardens `/terminus/snn-language-sequence/readout-rollout-candidate` so public rollout text is derived from current server-held sparse transition memory, records server/rollout memory hashes, binds prediction and transition-memory evaluation provenance, exposes bounded sparse trajectory evidence, ignores caller-carried transition weights, and fails closed when direct semantic callers provide out-of-bound rollout parameters.
- SNN readout rollout Runtime Truth binding note, June 2026: NeuronSpark, SpikingSSMs, and SpikeGPT support the feasibility of sparse/event-driven sequence language, but they also reinforce that MARULHO needs internal provenance before generation pressure can be trusted. MARULHO therefore exposes `snn_readout_rollout_server_state_binding` as compact Runtime Truth evidence: server-held transition-memory availability, hash/count, current revision, and negative execution flags are visible through `/status` and `/terminus` without executing rollout, replay, ledger recording, checkpoint writes, plasticity, or text generation.
- SNN readout rollout consolidation-path visibility note, June 2026: replay-consolidation and sleep/rehearsal work imply operators need to know whether reviewed sparse sequence evidence exists before any rehearsal policy is inspected, but a status read must not run tensor rehearsal or create consolidation intent. MARULHO therefore exposes `snn_readout_rollout_consolidation_path` in Runtime Truth as a compact count/hash/next-gate summary over the readout ledger while keeping rehearsal execution, ledger recording, checkpoint writes, plasticity, live replay, and language generation disabled.
- SNN readout emission-review history visibility note, June 2026: NeuronSpark/Nord-style pure-SNN language outputs make bounded spike-derived text a plausible target, while neuromorphic runtime-assurance work argues that output history should be auditable before it influences closed-loop adaptation. MARULHO therefore exposes `snn_readout_emission_review_history` in Runtime Truth as compact count/hash/next-gate evidence over reviewed emissions while hiding raw text/labels and keeping endpoint calls, replay, ledger recording, checkpoint writes, plasticity, fact/action promotion, cognition-substrate promotion, and freeform generation disabled.
- SNN readout emission-review operator-history note, June 2026: human-in-the-loop assurance still needs a deliberate place where operators can inspect the bounded text they reviewed. MARULHO therefore adds `GET /terminus/snn-language-sequence/readout-emission/operator-review/history`, a narrow read-only surface over emission-review events only; it may show reviewed bounded text/labels, but excludes draft ledger events, rollout events, replay targets, prediction reports, transition-memory evaluations, endpoint execution, replay, checkpoints, plasticity, fact/action promotion, and cognition-substrate promotion.
- SNN emission replay-policy note, June 2026: replay-consolidation research and sparse sequence SNN work imply text history is not enough to drive replay; replay should remain tied to spike-sequence evidence and grounding. MARULHO therefore adds `GET /terminus/snn-language-sequence/readout-emission/operator-review/replay-evaluation-policy`, which selects reviewed emissions only when their hashes, labels, and grounding match an internal SNN Language Readout Evidence Ledger row, while keeping display text out of policy candidates and leaving replay memory, live replay, plasticity, checkpoints, facts/actions, and cognition-substrate promotion disabled.
- SNN emission replay-design note, June 2026: sleep-enhanced latent replay, homeostatic replay, and GPU sparse structural-plasticity work all support replay as a controlled internal consolidation loop, not a text-history training loop. MARULHO therefore adds `POST /terminus/snn-language-sequence/readout-emission/operator-review/replay-evaluation-design`, which converts policy-ready reviewed emissions into hash-only replay-context seeds after internal-ledger and device-review checks, while still requiring the existing server-computed replay-evaluation-context gate for mismatch/pressure evidence before any artifact recording, replay memory, plasticity, checkpoint write, fact/action, or cognition-substrate path.
- SNN emission replay-design visibility note, June 2026: neuromorphic runtime-assurance and continual-learning replay work imply operators need compact visibility into whether reviewed SNN output is eligible for replay-design inspection, but status must not become the replay design or replay context. MARULHO therefore adds `snn_readout_emission_replay_design_path` to Runtime Truth, exposing only counts and provenance hashes for matched reviewed emissions while leaving raw text/labels, candidate arrays, endpoint calls, replay-context recording, replay execution, plasticity, checkpoints, facts/actions, and cognition-substrate promotion disabled.
- SNN emission replay-context review note, June 2026: prediction-error and replay literature supports recording consolidation contexts only from recomputed mismatch and pressure evidence, not from language display history. MARULHO therefore adds `POST /terminus/snn-language-sequence/readout-emission/operator-review/replay-context-review`, which verifies the reviewed-emission replay design, matching prediction hash, observed sparse readout slots, device evidence, runtime-truth/rollback pressure evidence, and operator confirmation before delegating to the existing Replay Controller context recorder; the bridge records no replay memory, runs no replay, applies no plasticity, writes no checkpoints, and exposes no reviewed text.
- Bounded emission replay-context bridge note, June 2026: the same modern-Hopfield/CLS/continual-replay/STC boundary applies after emission replay-design: hash-only replay-context seeds and observed sparse slots are local evidence for one Replay Controller context, not a caller-sized replay payload. MARULHO therefore routes `snn_language_readout_emission_replay_context_review(...)` through the shared `SNN_READOUT_REPLAY_TARGET_WINDOW_LIMIT=32` readout replay source-window operator for both `selected_replay_context_seeds` and `observed_readout_slots`, and requires bounded, untruncated, well-formed payloads before mismatch, pressure, or replay-context recording. The benchmark `reports/bounded_replay_window_20260619/emission-replay-context-review-window.json` passed with exact `32/32` seeds and slots recording one context while oversized seeds and observed slots blocked at `32/2048`; blocked payloads made no mismatch, pressure, or Replay Controller calls, projected source work fell `64x`, traced Python peak was `1.832774 MiB`, and CUDA allocation/reservation stayed `0.0 MiB`. The first clean `524288`-token hot-path run was below band at `5877.891 tokens/sec` and is retained as variance evidence; the rerun `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-emission-replay-context-review-window-rerun.json` stayed in the maintained band at `5990.908 tokens/sec`, `train_compute=0.135901 ms/token`, bounded `12/65536` route rows, `65526` cached rows, no observed contention, GPU memory `2032->2031 MiB`, and zero graph/native sequence failures. The former full-payload facade bridge is retired rather than kept as a side path.
- SNN emission replay-context lineage note, June 2026: replay-based continual learning and neuromorphic assurance both depend on auditable provenance, otherwise later consolidation queues cannot distinguish internally grounded spike evidence from text-history pressure. MARULHO therefore stores hash-verified `source_metadata` on replay evaluation contexts created from reviewed emissions, binding emission/review/design/readout/prediction/operator hashes into the context evidence while keeping raw display text and labels out of durable replay context state.
- SNN replay-context lineage-propagation note, June 2026: Replay4NCL-style replay selection and neuromorphic runtime-assurance monitoring both imply that downstream review surfaces must preserve why a replay context was nominated, not just its mismatch score. MARULHO therefore carries only the verified source metadata hash plus compact emission/readout/prediction/design hashes into the consolidation priority queue, artifact-recording policy proposal, and review ticket; raw source metadata, operator identity, display text, labels, replay execution, artifact recording, checkpoints, plasticity, facts/actions, and structural writes remain blocked.
- SNN replay-artifact lineage note, June 2026: online/local SNN continual-learning work with replay buffers, eligibility traces, and memory budgets suggests durable replay records must identify the selected trace source before any later structural-plasticity permit. MARULHO therefore extends the same hash-only emission lineage into evaluated replay artifacts and replay-backed regeneration permits, while still requiring server-held context, review ticket, internal readout evidence, current revision, and bounded regeneration design verification before any mutation path.
- SNN applied replay-lineage provenance note, June 2026: synapse-local eligibility traces, STDP-style local plasticity, and neuromorphic continual-learning surveys imply that once structural growth is applied, the new synapse still needs a compact trace of the replay artifact that justified it. MARULHO therefore copies only source metadata hash and compact emission/readout/prediction lineage into applied replay-regenerated synapse provenance and reports completeness through Runtime Truth and Synapse Provenance Audit, while leaving raw source metadata, operator identity, text, labels, permit issuance, checkpoint writes, and plasticity execution behind their separate gates.
- SNN durable applied replay-lineage checkpoint note, June 2026: long-term memory consolidation requires the applied synapse trace to survive persistence, not only the mutation event, but checkpoint save must not become another archive-wide replay scan. MARULHO therefore maintains `snn_applied_replay_lineage_incremental_summary.v1` as CPU mutation-time evidence: replay regeneration adds one row hash per replay-regenerated synapse, non-replay overwrites/pruning remove that row, and `snn_applied_replay_lineage_checkpoint_summary` reads counts/digests with `source_record_scan_count=0` and `full_provenance_scan=false`. The maintained-only refresh `..\..\MARULHO_reports\bounded_replay_window_20260623\applied-replay-lineage-checkpoint-legacy-baseline-removed.json` removes the executable benchmark-local full-provenance scan too: `65536` seeded replay-lineage rows match the mutation-maintained incremental summary with `0` provenance source reads, `0.082714 ms` active mean latency, `0.001343 MiB` Python peak, CPU archival placement, `0.0 MiB` CUDA allocation, and `retired_full_scan_absence.implementation_present=false`.
- SNN applied replay-lineage restore-validation note, June 2026: durable synaptic consolidation should be rechecked when a checkpoint becomes active again, but missing checkpoint lineage must block exact validation rather than trigger a provenance rebuild or legacy-source compatibility label. MARULHO therefore compares saved checkpoint summary fields against the restored mutation-maintained summary and reports `snn_applied_replay_lineage_restore_validation`, exposing mismatches or missing incremental state without executing replay, applying plasticity, scanning `synapse_provenance_by_key`, issuing permits, or exposing raw source metadata.
- SNN applied replay-lineage restore-visibility note, June 2026: neuromorphic runtime assurance requires restore integrity signals to be visible before later audits, not buried in checkpoint metadata. MARULHO therefore exposes `snn_applied_replay_lineage_restore_validation` through Runtime Truth as read-only advisory evidence over saved/restored lineage count and material-hash agreement, while blocking checkpoint loading, replay, plasticity, permit issuance, checkpoint writes, facts/actions, and raw source metadata from status.
- SNN restore-gated synapse audit readiness note, June 2026: runtime-assurance work for neuromorphic safety monitors suggests mismatch signals should constrain readiness gates, while continual SNN replay/plasticity work still needs live, non-restored learning paths. MARULHO therefore lets complete live applied synapse provenance reach audit review without restore metadata, but when restore validation is available, any restored lineage count or material-hash mismatch blocks `snn_readout_applied_synapse_provenance` from claiming readout synapse provenance audit readiness.
- SNN restore-gated synapse audit execution note, June 2026: safety-monitor evidence should constrain both dashboards and the concrete operator audit surface. MARULHO therefore forwards restored applied replay-lineage validation from the runtime facade into `snn_language_readout_synapse_provenance_audit.v1`; the endpoint still returns inspectable non-mutating audit rows, but any restored lineage mismatch becomes required-evidence failure and prevents operator review readiness.
- SNN restore-gated regeneration permit boundary note, June 2026: online STDP and structural-plasticity work support local synapse growth, but runtime-assurance monitors imply uncertain restored lineage must not be allowed to authorize new mutations. MARULHO therefore blocks rollout regeneration permit issuance and checkpoint-backed regeneration preflight when restored applied replay-lineage validation is available and mismatched, while preserving read-only audit inspection and fresh never-restored runtime paths.
- SNN language capacity-pressure note, June 2026: expandable SNN and structural-evolution work suggests a living language substrate eventually needs neuron/structure expansion, while homeostatic structural plasticity and runtime assurance require evidence before topology changes. MARULHO therefore exposes `snn_language_capacity_pressure` in Runtime Truth as non-mutating evidence over fixed 64-neuron sparse edge occupancy, active-neuron coverage, fanout saturation, key validity, and provenance cleanliness before any future capacity-expansion design can be reviewed.
- SNN language capacity-expansion design note, June 2026: neuron-expandable SNN work such as NG-SNN/CogniSNN and sparse GPU structural-plasticity frameworks suggest expansion should be bounded, provenance-preserving, and hardware-aware. MARULHO therefore adds `snn_language_neuron_capacity_expansion_design.v1` as a read-only operator-review artifact that proposes neuron and sparse-edge budget targets only when capacity pressure is clean, device evidence is present, CUDA relayout is preferred, and rollback/checkpoint policy exists; it performs no resize, training, replay, or checkpoint write.
- SNN language capacity-expansion preflight note, June 2026: dynamic topology and sparse GPU SNN work imply resize should be validated as a separate preflight before any executor, with design-hash and checkpoint evidence protecting provenance. MARULHO therefore adds `snn_language_neuron_capacity_expansion_preflight.v1`, which checks design hash, current revision, checkpoint snapshot/restore evidence, and CUDA relayout evidence while still blocking tensor resize, neuron/layer growth, training, replay, plasticity, and checkpoint writes.
- SNN language capacity-state note, June 2026: dynamic SNN topology work makes capacity a first-class runtime property, while sparse GPU simulation and checkpoint recovery require layout metadata to survive save/restore before a safe executor exists. MARULHO therefore persists `snn_language_capacity_state.v1` inside SNN language plasticity checkpoints and exposes it through plasticity snapshots and capacity-pressure evidence, but keeps dynamic capacity disabled and forbids resize, neuron/layer growth, training, replay, plasticity, and checkpoint writes from the metadata alone.
- SNN capacity-resize compatibility-audit note, June 2026: MorphSNN-style dynamic topology and GPU structural-plasticity systems support changing sparse connectivity, but fixed index validators, CUDA tensor shapes, sparse budgets, and checkpoint provenance can make a proposed resize unsafe even after preflight. MARULHO therefore adds `snn_language_capacity_resize_compatibility_audit.v1`, a read-only audit that inventories capacity runtime boundaries and blocks any resize executor while readout-ledger dense tensor and fixed-index assumptions remain unresolved.
- SNN language fixed-capacity boundary visibility note, June 2026: hardware-aware sparse SNN work shows dynamic sparse growth must account for index storage, irregular memory access, and workload imbalance before deployment. MARULHO therefore exposes `snn_language_capacity_fixed_boundary_evidence.v1` in Runtime Truth so operators can see fixed neuron-index validators, dense CUDA tensor shapes, and sparse-edge budgets as current blockers before requesting a target-specific resize compatibility audit.
- SNN capacity-boundary inventory note, June 2026: sparse hardware/software co-design work treats sparsity layout and index storage as shared model/runtime constraints, so blocker lists must not diverge across status and executor-review surfaces. MARULHO therefore uses one SNN Capacity Boundary Inventory for both Runtime Truth fixed-boundary evidence and target-specific capacity-resize compatibility audits; future dynamic-capacity work should migrate individual boundaries in that inventory instead of creating new local allowlists.
- SNN capacity-aware sparse-plasticity boundary note, June 2026: adaptive sparse-structure development and GPU sparse structural-plasticity work support local synapse regeneration inside a bounded sparse representation before dense readout tensors are resizable. MARULHO therefore migrates the SNN language plasticity executor's sparse candidate index, fanout, and global sparse-edge checks to durable `snn_language_capacity_state.v1`; this permits checkpointed sparse transition-memory growth above the original 64-neuron/256-edge defaults while keeping readout-ledger dense tensor and generation paths blocked from claiming full resize.
- SNN capacity-aware readout-regeneration adapter note, June 2026: sparse structural-plasticity pipelines can separate candidate review from mutation, which lets readout-side sparse candidate routing become capacity-aware before dense readout tensor materialization is dynamic. MARULHO therefore migrates the rollout regeneration proposal adapter to validate reviewed sparse candidate indices against `snn_language_capacity_state.v1`, allowing above-default candidates to reach permit review while preserving the replay permit, checkpoint executor, dense tensor, mutation, and generation blocks.
- SNN capacity-aware replay-artifact review note, June 2026: hippocampal replay and sparse synaptic development research support binding growth candidates to replay evidence before consolidation, but replay binding should not erase capacity or provenance constraints. MARULHO therefore carries adapter `snn_language_capacity_state.v1` into rollout regeneration replay-artifact review so above-default sparse candidates can remain valid through replay binding while permit issuance, checkpoint-backed application, dense tensor resize, and language generation stay blocked.
- SNN capacity-aware regeneration-permit request note, June 2026: replay-to-plasticity gating should preserve the same sparse capacity envelope until a server-issued permit exists, because permits authorize later checkpoint-backed mutation. MARULHO therefore validates replay-bound regeneration-design indices against carried `snn_language_capacity_state.v1` in the runtime-facade permit request before calling the replay controller, while keeping plasticity application, checkpoint writes, dense readout resize, and language generation blocked.
- SNN capacity-aware regeneration-application preflight note, June 2026: GPU sparse structural-plasticity work and dynamic sparse SNN training both separate topology proposals from the expensive mutation step, so MARULHO must preserve the same capacity envelope after a permit is issued and before checkpoint-backed regeneration. MARULHO therefore revalidates permitted regeneration-design indices against carried `snn_language_capacity_state.v1` in application preflight, allowing above-default sparse candidates to reach executor review while plasticity application, checkpoint writes, dense readout resize, and language generation remain blocked.
- SNN capacity-aware regeneration-application note, June 2026: dynamic sparse training and GPU structural-plasticity systems make growth a staged mutation with hardware and checkpoint constraints, not a single unchecked write. MARULHO therefore requires matching preflight/proposal `snn_language_capacity_state.v1` in the runtime-facade application boundary before delegating to the checkpoint-backed regeneration executor, allowing above-default sparse transition-memory mutation only when capacity, checkpoint, revision, permit, and operator evidence remain aligned.
- SNN capacity-aware readout sparse-budget note, June 2026: sparse SNN evolution and synaptic-crowding work treat edge budgets as explicit topology constraints rather than fixed constants. MARULHO therefore migrates rollout shadow preflight and developmental plasticity review to compare simulated sparse transition-memory edges against carried `snn_language_capacity_state.v1`, so expanded sparse budgets can be reviewed before checkpoint-backed regeneration while dense readout resize and language generation remain blocked.
- SNN dense readout resize-plan note, June 2026: recent SNN language and structured recurrent SNN work uses sparse readout populations and output-layer learning, but hardware-aware execution still requires explicit tensor/layout transitions when capacity grows. MARULHO therefore exposes `snn_language_dense_readout_resize_plan.v1` as read-only Runtime Truth evidence for the remaining dense readout blockers, describing target shape, preserved copy window, zero-fill region, CUDA relayout, and checkpoint/restore requirements before any dense resize executor exists.
- SNN dense readout resize-preflight note, June 2026: SNN accelerator and deployment work shows sparse event algorithms can still become dense-kernel or launch-bound if layout changes are not checked against the target device. MARULHO therefore adds `snn_language_dense_readout_resize_preflight.v1`, which validates plan hash, runtime revision, checkpoint save/restore, CUDA relayout evidence, preserved dense window, and zero-fill region while still blocking tensor mutation until a checkpoint-backed CUDA resize executor exists.
- SNN dense readout resize-transaction note, June 2026: GPU structural-plasticity and tensor-checkpoint work imply topology changes need an explicit transaction recipe before runtime mutation, especially when dense layout can dominate cost. MARULHO therefore adds `snn_language_dense_readout_resize_transaction_proposal.v1`, a non-executing recipe for checkpoint load, CUDA target allocation, preserved-window copy, zero-fill, invariant verification, and migration evidence before any dense tensor executor is implemented.
- SNN dense readout resize-executor readiness note, June 2026: dynamic SNN topology work highlights that connectivity updates and device-specific allocation can dominate runtime unless executor responsibilities are explicit. MARULHO therefore adds `snn_language_dense_readout_resize_executor_readiness_audit.v1`, a non-executing audit for dense tensor ownership, CUDA allocation, checkpoint writing, migration-ledger writing, post-resize boundary marking, and migration tests before any dense resize executor can be considered ready.
- SNN dense readout layout-state note, June 2026: SNN training and checkpointing work emphasizes that layout and state handling affect stability, memory cost, and deployment efficiency. MARULHO therefore persists `snn_language_dense_readout_layout_state.v1` beside SNN language plasticity state and exposes it in Runtime Truth, recording fixed/current shape, capacity-derived target shape, preserved copy window, zero-fill region, CUDA relayout requirement, and migration status before dense tensor ownership exists.
- SNN dense readout layout-ownership evidence note, June 2026: structural-plasticity and GPU-accelerated SNN work continues to separate topology/layout evolution from concrete device-memory mutation. MARULHO therefore lets the resize-executor readiness audit consume owned Runtime Truth layout metadata as proof of the shape contract, while keeping live dense tensor weight ownership as a separate missing executor capability.
- SNN dense readout layout-migration executor note, June 2026: sparse SNN structural-plasticity and pruning work warns that dense representations can erase sparse/event efficiency unless layout transitions are explicit and checkpointed. MARULHO therefore adds a checkpoint-backed layout migration executor that persists the target dense readout layout and migration ledger without claiming CUDA tensor weight materialization; the later tensor executor must still own allocation, copy, zero-fill, and migration tests before dense resize is complete.
- SNN dense tensor materialization-readiness note, June 2026: GPU SNN deployment work treats memory allocation, preserved-state copy, and zero initialization as distinct execution risks from topology planning. MARULHO therefore adds `snn_language_dense_readout_tensor_materialization_readiness.v1`, a read-only gate after layout migration that names the remaining CUDA allocator, preserved-window copy, zero-fill, checkpoint writer, migration-ledger writer, boundary marker, and migration-test capabilities before dense readout tensor materialization can become an executor.
- SNN dense tensor materialization executor note, June 2026: sparse SNN deployment and NeuronSpark-style language work imply that expanded language capacity needs a concrete readout tensor before generation training can be evaluated. MARULHO therefore adds a checkpoint-backed dense tensor materialization executor that projects owned sparse transition weights into a target dense readout tensor, records actual device placement, preserves/zero-fills the resize window, and commits through Runtime Persistence while keeping text decoding and generation disabled.
- SNN dense tensor integrity note, June 2026: dynamic SNN and sparse GPU work makes tensor materialization risky unless the runtime can verify the materialized layout still matches the sparse source state. MARULHO therefore adds Runtime Truth `snn_language_dense_readout_tensor_integrity.v1`, reporting tensor shape/device/dtype/nonzero counts and sampled sparse-weight cell matches without exposing raw matrix weights or claiming language generation.
- SNN dense readout training-readiness note, June 2026: NeuronSpark, SymbolicLight, SpikeGPT, and recent online/STDP SNN learning work all imply that generation needs a separate training/evaluation loop after readout tensors exist. MARULHO therefore adds `snn_language_dense_readout_training_readiness.v1`, requiring tensor integrity, heldout adapter evidence, device evidence, and rollback evidence before a dense readout training-loop design can be reviewed while keeping runtime training and generation disabled.
- SNN dense readout training-loop design note, June 2026: NeuronSpark's SNN/SSM training-stability direction, Adaptive Spiking Neurons for language modeling, and sparse CUDA SNN training work all argue that the first post-tensor step should be a bounded local-learning design with device and rollback constraints, not immediate runtime training. MARULHO therefore adds `snn_language_dense_readout_training_loop_design.v1`, a read-only design gate that requires bounded training/validation transition counts, a supported local spike-learning rule, learning-rate/epoch/delta caps, device evidence, and rollback evidence while keeping trained weights, checkpoint writes, runtime mutation, and language generation disabled.
- SNN dense readout training-loop preflight note, June 2026: SNN training-stability papers emphasize surrogate/local update stability, homeostatic normalization, sparse updates, and device-specific kernels; NeuronSpark-scale language work also shows that spiking language training is a heavy GPU/checkpointed operation rather than a status-side decision. MARULHO therefore adds `snn_language_dense_readout_training_loop_preflight.v1`, a read-only gate requiring current revision, checkpoint path, ready design evidence, bounded local-learning constraints, CUDA/device compatibility, checkpoint-writer capability, and bounded-delta capability while still refusing training execution, checkpoint writes, trained weights, runtime mutation, and language generation.
- SNN dense readout training executor note, June 2026: local/STDP-style learning and sparse/surrogate SNN training work both require explicit bounded weight updates, normalization discipline, device-aware execution, and rollback because small unstable updates can corrupt temporal spike dynamics. MARULHO therefore adds `snn_language_dense_readout_training.v1`, a checkpoint-backed executor that applies bounded local spike-transition deltas to the materialized dense readout tensor and mirrors updated cells into sparse transition memory/provenance, preserving dense/sparse Runtime Truth auditability while still refusing raw weight return, text decoding, free-form language generation, and action execution.
- SNN dense readout post-training evaluation note, June 2026: continual-learning SNN work highlights catastrophic forgetting, stability-plasticity balance, and the need to evaluate post-update behavior against prior support rather than assuming any successful update is useful. MARULHO therefore adds `snn_language_dense_readout_post_training_evaluation.v1`, a read-only gate requiring checkpointed training evidence, post-training dense/sparse tensor integrity, heldout adapter support, runtime-truth stability evidence, and rollback evidence before decoder-probe design can be reviewed while keeping runtime mutation, further training, raw weights, text decoding, and language generation disabled.
- SNN dense readout decoder-probe design note, June 2026: NeuronSpark, SpikeGPT, WTA Spikingformer, and sparse SNN language scaling work all suggest language-facing behavior should remain sparse, event-driven, and explicitly evaluated before any text-generation claim. MARULHO therefore adds `snn_language_dense_readout_decoder_probe_design.v1`, a read-only bridge from post-training dense readout evidence to the existing Spike Language Decoder Probe, requiring grounded readout slots, sparse population-code evidence, temporal-state evidence, device evidence, and free-form generation lockout while keeping sampling, decoding, runtime mutation, checkpoint writes, and language generation disabled.
- SNN dense readout decoder-probe preflight note, June 2026: sparse SNN language and decoder work emphasizes bounded activation sparsity, temporal state, and device placement before a decoder can be trusted as an evidence source. MARULHO therefore adds `snn_language_dense_readout_decoder_probe_preflight.v1`, a read-only gate requiring ready decoder design evidence, current revision, bounded sparse probe evidence, device/CUDA compatibility, and language-generation lockout before any later decoder-probe execution can be reviewed.
- SNN dense readout decoder-probe execution note, June 2026: sparse/event-driven SNN decoder work supports exposing low-dimensional readout evidence before higher-level language surfaces, but such evidence should remain bounded and reviewable. MARULHO therefore adds `snn_language_dense_readout_decoder_probe_execution.v1`, a read-only execution surface that emits grounded label candidates and sparse/device evidence from a preflighted dense readout decoder probe while keeping free-form generation, text sampling, runtime mutation, checkpoints, actions, and plasticity disabled.
- SNN dense readout label-candidate review note, June 2026: neural decoder calibration and human-in-the-loop assurance work both argue that decoder outputs should carry uncertainty/evidence review before downstream use, while SNN language papers still separate sparse readout behavior from full free-form generation. MARULHO therefore adds `snn_language_dense_readout_label_candidate_review.v1`, a read-only operator-confirmed gate that hashes bounded grounded labels with decoder execution, sparse activity, tensor-device evidence, and review context while keeping fact promotion, actions, replay artifacts, checkpoints, plasticity, text decoding, and language generation disabled.
- SNN dense label-candidate evidence ledger note, June 2026: replay and continual-learning literature treats durable memory as a consolidation substrate that should be gated separately from raw decoder output, and decoder-calibration work warns that candidate outputs need evidence trails before downstream use. MARULHO therefore adds `snn_language_dense_readout_label_candidate_evidence_record.v1`, an append-only audit record for reviewed dense decoder labels that persists hashes, labels, device/activity evidence, revision, and operator identity while explicitly blocking replay memory, plasticity, facts, actions, checkpoints, and language generation.
- SNN dense label-candidate history note, June 2026: neural decoder and BCI review literature separates decoder output inspection from downstream commands or adaptation, while SNN language work still requires explicit evaluation before any generation claim. MARULHO therefore adds `snn_language_dense_label_candidate_history.v1`, a read-only inspection surface over reviewed dense decoder labels that exposes bounded label/hash/device/activity metadata for audit while refusing ledger writes, replay, checkpoints, plasticity, fact/action promotion, and language generation.
- SNN dense label-candidate calibration policy note, June 2026: decoder calibration research shows that neural-spike decoders can be miscalibrated, so reviewed outputs need a separate calibration-review policy before any downstream adaptation. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_policy.v1`, a read-only ranking over dense label-candidate history that uses bounded labels, hashes, device/activity evidence, recency, and repetition to nominate operator calibration review while refusing training, replay, plasticity, checkpoints, fact/action promotion, and language generation.
- SNN dense label-candidate calibration evaluation design note, June 2026: neural decoder calibration work emphasizes heldout validation, calibration error, and coverage before trusting decoder outputs, and SNN language-generation work still requires separate evaluated training loops. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_evaluation_design.v1`, a read-only design that binds selected dense-label candidates to heldout labels, supported metrics, cross-validation, coverage checks, and device evidence while refusing evaluation execution, training, plasticity, checkpoints, facts/actions, and language generation.
- SNN dense label-candidate calibration evaluation preflight note, June 2026: BCI decoder calibration and SNN uncertainty-calibration work both treat recalibration as device-sensitive, validation-backed, and vulnerable to nonstationary neural signals. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_evaluation_preflight.v1`, a read-only preflight that verifies current revision, design hash, selected candidate hashes, heldout target hash, metrics, cross-validation/coverage requirements, CUDA/device evidence, and executor capability while refusing evaluation execution, training, plasticity, checkpoints, facts/actions, and language generation.
- SNN dense label-candidate calibration evaluation note, June 2026: expected calibration error and reliability-bin methods compare confidence with observed accuracy, while BCI decoder literature recommends validation metrics and careful calibration timing before adaptation. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_evaluation.v1`, a non-mutating executor that computes bounded ECE-style bins, coverage gap, label-set stability, and sample evidence from preflight-selected dense decoder-label candidates while refusing training, replay, plasticity, checkpoints, fact/action promotion, and language generation.
- SNN dense label-candidate calibration evaluation review note, June 2026: calibration literature warns that global ECE can miss local/subgroup failures and BCI decoder updates should be reviewed against validation evidence before adaptation. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_evaluation_review.v1`, a read-only operator review gate that binds calibration metrics, thresholds, evaluation hash, preflight hash, revision, and confirmation while refusing training, weight updates, replay, plasticity, checkpoints, facts/actions, and language generation.
- SNN dense label-candidate calibration update design note, June 2026: post-hoc calibration work commonly uses bounded confidence/temperature scaling, while BCI decoder-adaptation research emphasizes explicit update-rule design, rollback, and validation before changing decoder parameters. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_update_design.v1`, a read-only design that proposes bounded post-hoc calibration parameters with rollback and device evidence while refusing runtime updates, persistence, training, replay, plasticity, checkpoints, facts/actions, and language generation.
- SNN dense label-candidate calibration update preflight note, June 2026: temperature-scaling calibration should preserve underlying weights and be validated on held-out evidence, while closed-loop BCI decoder adaptation can change user/system dynamics and therefore needs explicit update readiness checks before deployment. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_update_preflight.v1`, a read-only checkpoint-backed gate that verifies design/review/evaluation hashes, current revision, bounded update parameters, rollback policy, checkpoint path, CUDA/device evidence, and executor capability while refusing calibration updates, checkpoint writes, persistence, training, replay, plasticity, facts/actions, and language generation.
- SNN dense label-candidate calibration update application note, June 2026: post-hoc temperature scaling preserves label/order semantics by adjusting confidence rather than retraining weights, but BCI decoder literature treats recalibration as a closed-loop mutation that must be revision-bound and reversible. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_update_application.v1`, a checkpoint-backed operator-confirmed mutation that records bounded dense label calibration parameters and lineage into MARULHO-owned readout state while refusing weight persistence, training, replay, plasticity, facts/actions, and language generation.
- SNN dense label-candidate calibration update application review note, June 2026: deployment calibration monitoring uses reliability/ECE-style evidence after calibration changes, and BCI recalibration research emphasizes post-update validation because neural signals drift and closed-loop behavior can change. MARULHO therefore adds `snn_language_dense_label_candidate_calibration_update_application_review.v1`, a read-only lineage audit that verifies applied calibration state, current revision, rollback/checkpoint/device evidence, and bounded deltas before any observation window while refusing training, replay, plasticity, facts/actions, and language generation.
- SNN dense label calibration update source-window note, June 2026: continual-replay, complementary-learning-systems, and BCI recalibration work all favor selected post-update evidence over archive-wide background passes. MARULHO therefore bounds operator and autonomous calibration update application/review to `bounded_snn_dense_label_calibration_update_source_window.v1`, reading only `dense_label_calibration_update_events`, keeping archival metadata and lookup/write work on CPU, avoiding raw text/language reasoning/live-tick/every-token work, and retiring the broad all-family ledger normalization/write path from production.
- SNN autonomous confidence-use source-window note, June 2026: selective prediction and confidence-aware neural decoding can use calibrated confidence as a bounded abstention/ranking signal, but modern-Hopfield-style recall, CLS, continual replay, STC, and sparse replay still reject archive-wide ledger passes. MARULHO therefore bounds autonomous calibrated confidence-use execution and event review to `bounded_snn_autonomous_confidence_use_source_window.v1`, reading only `autonomous_confidence_use_events`, storing only that event family/count/timestamp, keeping archival metadata and lookup/write work on CPU, avoiding raw text/language reasoning/live-tick/every-token work, and retiring the broad all-family normalization duplicate/review lookup from production.
- SNN dense label-candidate post-calibration observation window note, June 2026: calibration reliability diagrams/ECE require post-change samples, and BCI recalibration papers warn that apparent offline decoder improvements can fail under drift or closed-loop use. MARULHO therefore adds `snn_language_dense_label_candidate_post_calibration_observation_window.v1`, a read-only observation surface that measures bounded confidence/correctness gaps and confidence drift after applied calibration while refusing additional calibration updates, training, replay, plasticity, facts/actions, and language generation.
- SNN dense label-candidate post-calibration operator review note, June 2026: calibration drift monitoring work emphasizes explicit thresholds and review before model updates or deployment decisions, while BCI recalibration literature shows neural decoder stability must be validated after updates. MARULHO therefore adds `snn_language_dense_label_candidate_post_calibration_operator_review.v1`, a read-only operator-confirmed gate that binds observation metrics, thresholds, revision, and applied-calibration lineage before any later calibrated-confidence-use design while refusing training, replay, plasticity, facts/actions, and language generation.
- SNN calibrated dense label confidence use design note, June 2026: calibrated probabilities are useful for ranking, thresholds, abstention, and average reliability, but they do not prove that an individual candidate is correct or language-capable; confidence-aware BCI decoding similarly treats confidence as a robustness signal rather than a generation source. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_use_design.v1`, a read-only design for using calibrated dense-label confidence as bounded metadata while refusing text decoding, generation, training, replay, plasticity, facts/actions, and checkpoints.
- SNN calibrated dense label confidence use preflight note, June 2026: selective prediction and confidence-aware neural decoding both require a validation step between calibrated scores and deployment-time use. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_use_preflight.v1` to require current revision evidence, bounded candidate hashes/confidences, threshold/pass evidence, device/CUDA evidence, and executor capability before any future rank/abstain executor is eligible, while still refusing language generation or runtime mutation.
- SNN calibrated dense label confidence use executor note, June 2026: selective classification research frames confidence use as a risk/coverage operation with abstention, and BCI uncertainty work treats low-confidence neural decoding as a reason to withhold unsafe output. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_use_executor.v1`, a non-mutating rank/abstain executor over preflight-matched dense label-candidate hashes that keeps outputs hash-level and refuses text decoding, generation, training, replay, plasticity, checkpoints, facts/actions, and cognition claims.
- SNN calibrated dense label confidence operator display review note, June 2026: human-in-the-loop AI work emphasizes calibrated uncertainty, contextualized confidence display, and meaningful oversight rather than blind reliance on scores; BCI decoder work similarly treats uncertainty as a signal for user-facing deferral. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_operator_display_review.v1`, an operator-confirmed display-only review of hash-level rank/abstain confidence output that refuses new text, language generation, training, replay, plasticity, checkpoints, facts/actions, and cognition claims.
- SNN calibrated dense label confidence internal stability review note, June 2026: self-recalibrating BCI and plug-and-play stability work reduce repeated supervised calibration by using internal drift/stability evidence, while continual-calibration research shows coverage and confidence stability can fail even when accuracy looks acceptable. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_internal_stability_review.v1`, a no-operator self-review gate over repeated hash-only confidence-use cycles that checks selected-hash consistency and bounded confidence drift before any future autonomous replay/recalibration path.
- SNN calibrated dense label confidence autonomous replay review design note, June 2026: long-term iBCI stabilization, self-recalibrating classifiers, and continual-calibration replay all point toward internal replay/drift evidence replacing repeated supervised interruption. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_replay_review_design.v1`, a no-operator design surface for future bounded confidence replay review that carries selected hashes, replay-consistency thresholds, confidence-drift limits, and device/CUDA evidence while refusing replay execution, calibration updates, plasticity, checkpoints, facts/actions, and language generation.
- SNN calibrated dense label confidence autonomous replay review preflight note, June 2026: self-recalibrating BCI and continual-calibration replay work both depend on current-state stability and executor readiness before online adaptation. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_replay_review_preflight.v1`, a no-operator preflight that verifies design/stability hashes, current Runtime State revision, selected-hash stability, replay/drift bounds, device/CUDA evidence, and executor capability while still refusing replay execution, recalibration, plasticity, checkpoints, facts/actions, and language generation.
- SNN calibrated dense label confidence autonomous replay review executor note, June 2026: plug-and-play iBCI and self-recalibration work use replay buffers, pseudo-label consistency, and repeated stability checks before online updates. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_replay_review_executor.v1`, a no-operator bounded executor that computes confidence replay-review cycles and drift/consistency metrics from selected hashes while refusing live replay memory, recalibration, training, plasticity, checkpoints, facts/actions, and language generation.
- SNN calibrated dense label confidence autonomous recalibration design note, June 2026: self-recalibrating iBCI classifiers show autonomous adaptation can reduce repeated supervised retraining, while plug-and-play brain-to-text and continual calibration replay show that update parameters must remain bounded by pseudo-label/replay stability, drift, and rollback evidence. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_recalibration_design.v1`, a no-operator design surface that proposes bounded temperature/confidence-rescale deltas from passing replay-review metrics while refusing actual calibration updates, training, replay execution, plasticity, checkpoints, facts/actions, and language generation.
- SNN calibrated dense label confidence autonomous recalibration preflight note, June 2026: online BCI adaptation and calibration-replay work make pre-update checks explicit: current decoder state, replay/pseudo-label stability, bounded update magnitude, rollback capability, and device capability must be available before autonomous recalibration. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_recalibration_preflight.v1`, a no-operator preflight that verifies current revision, recalibration design lineage, replay/drift thresholds, bounded temperature/confidence deltas, rollback hash evidence, device/CUDA compatibility, and executor capability while still refusing actual calibration updates, training, replay execution, plasticity, checkpoints, facts/actions, and language generation.
- SNN calibrated dense label confidence autonomous recalibration executor note, June 2026: recent unsupervised and continual BCI recalibration work shows online decoder updates can use pseudo-labels, confidence weighting, and recurrent recalibration to handle neural drift, but these updates should remain bounded and distinguish calibration from full model training. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_recalibration_executor.v1`, a no-operator executor that applies a bounded runtime confidence-calibration ledger update from a ready autonomous preflight while refusing dense-weight training, replay-memory execution, plasticity, checkpoint writes, facts/actions, and language generation.
- SNN calibrated dense label confidence autonomous recalibration application review note, June 2026: BCI drift and adaptation work evaluates recalibration against post-update stability rather than assuming an update remains valid forever. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_recalibration_application_review.v1`, a no-operator read-only audit that verifies the autonomous calibration update is the current runtime ledger update, bounded, rollback-hashed, device-compatible, and non-training/non-generative before any autonomous post-calibration observation path.
- SNN calibrated dense label confidence autonomous post-calibration observation note, June 2026: calibration drift detectors and neural decoder calibration work both rely on post-update observation windows, confidence/coverage error, and drift thresholds before trusting recalibrated outputs. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_post_calibration_observation_window.v1`, a no-operator observation surface that measures expected calibration error, confidence drift, sample hashes, and bounded confidence values after autonomous recalibration while refusing additional updates, training, replay execution, plasticity, checkpoints, facts/actions, and language generation.
- SNN calibrated dense label confidence autonomous post-calibration stability review note, June 2026: online calibration monitoring research distinguishes a single update from ongoing stability, and BCI decoder stability work shows recalibration only matters if post-update behavior remains bounded across observation windows. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_post_calibration_stability_review.v1`, a no-operator review that checks sample sufficiency, hash-backed observations, expected calibration error, confidence drift, and absence of hidden mutation before a future autonomous calibrated-confidence use design.
- SNN calibrated dense label confidence autonomous use design note, June 2026: selective prediction and confidence-aware neural decoding use calibrated confidence for ranking, thresholding, and abstention rather than as proof of semantics. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_use_design.v1`, a no-operator design surface that consumes stable autonomous post-calibration evidence and plans bounded hash-level rank/threshold/abstain use while refusing text decoding, language generation, training, replay execution, plasticity, checkpoints, facts/actions, and runtime mutation.
- SNN calibrated dense label confidence autonomous use preflight note, June 2026: confidence-aware BCI decoding and selective prediction both require candidate confidence, calibration, threshold, and abstention checks before acting on a prediction. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_use_preflight.v1`, a no-operator preflight that verifies current revision, design lineage, bounded candidate hashes/confidences, threshold pass/fail behavior, device/CUDA evidence, and executor capability while refusing execution, runtime mutation, text decoding, language generation, training, replay execution, plasticity, checkpoints, and fact/action promotion.
- SNN calibrated dense label confidence autonomous use executor note, June 2026: calibrated neural decoders and selective prediction research support acting on confidence only as bounded selection/abstention evidence, not as semantic language. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_use_executor.v1`, a no-operator ledger writer that records current-revision candidate-hash ranking/selection events while rejecting text payloads and refusing decoding, language generation, training, replay execution, plasticity, checkpoints, and fact/action promotion.
- SNN calibrated dense label confidence autonomous use event review note, June 2026: uncertainty-aware BCI work treats decoder confidence as a control signal that needs reliability and audit checks before downstream communication decisions. MARULHO therefore adds `snn_language_calibrated_dense_label_confidence_autonomous_use_event_review.v1`, a no-operator read-only audit that verifies recorded event lineage, current Runtime State, selected/ranked hash bounds, abstention consistency, and hash-only output while still refusing decoding, language generation, training, replay execution, plasticity, checkpoints, and fact/action promotion.
- SNN autonomous hash readout binding design note, June 2026: spike-language systems such as NeuronSpark, SpikeGPT, and SymbolicLight preserve sparse temporal state and event-driven dynamics rather than collapsing immediately into ordinary text tokens. MARULHO therefore adds `snn_language_autonomous_hash_readout_binding_design.v1`, a no-operator design surface that binds reviewed autonomous candidate hashes to bounded readout slot hashes for later SNN readout compatibility while refusing decoding, language generation, training, replay execution, plasticity, checkpoints, fact/action promotion, and runtime mutation.
- SNN autonomous hash readout binding preflight note, June 2026: SNN language-model work emphasizes sparse temporal state, activation sparsity, and hardware-aware execution, so a hash-to-readout bridge should verify slot/hash integrity, current runtime revision, device/CUDA fit, and executor capability before any binding executor. MARULHO therefore adds `snn_language_autonomous_hash_readout_binding_preflight.v1`, a no-operator preflight that validates bounded reviewed bindings while refusing decoding, language generation, training, replay execution, plasticity, checkpoints, fact/action promotion, and runtime mutation.
- SNN autonomous hash readout binding executor note, June 2026: sparse SNN language models preserve state through event-driven activations and explicit readout pathways, so binding selected hashes to readout slots should become durable evidence before any later training or generation path. MARULHO therefore adds `snn_language_autonomous_hash_readout_binding_executor.v1`, a no-operator ledger writer that records current hash-to-slot binding events while refusing decoding, language generation, training, replay execution, plasticity, checkpoints, fact/action promotion, and synaptic mutation.
- SNN autonomous hash readout binding event review note, June 2026: event-driven SNN language and speech models rely on sparse state traces that must remain auditable before downstream readout use. MARULHO therefore adds `snn_language_autonomous_hash_readout_binding_event_review.v1`, a no-operator read-only audit that verifies recorded binding-event lineage, current Runtime State, hash-only candidate/slot bindings, and bounded event counts while refusing decoding, language generation, training, replay execution, plasticity, checkpoints, fact/action promotion, and runtime mutation.
- SNN autonomous bound readout observation design note, June 2026: activation-sparse SNN language work such as NeuronSpark, SpikeGPT, SymbolicLight, and SpikingSSMs suggests readout readiness should observe sparse temporal slot behavior before emitting language or training. MARULHO therefore adds `snn_language_autonomous_bound_readout_observation_design.v1`, a no-operator design surface that plans bounded observation cycles, activation-sparsity thresholds, slot-drift limits, and binding-reactivation checks while refusing decoding, language generation, training, replay execution, plasticity, checkpoints, fact/action promotion, and runtime mutation.
- SNN autonomous bound readout observation preflight note, June 2026: sparse SNN language and SNN-SSM work emphasizes that sparse temporal activations must be preserved and verified before inference-like execution. MARULHO therefore adds `snn_language_autonomous_bound_readout_observation_preflight.v1`, a no-operator preflight that checks current revision, target/candidate/slot hashes, activation-sparsity and drift thresholds, device/CUDA fit, and executor capability while refusing decoding, language generation, training, replay execution, plasticity, checkpoints, fact/action promotion, and runtime mutation.
- SNN autonomous bound readout observation executor note, June 2026: current SNN language research points toward sparse event observations as the bridge between hash-bound readout slots and later training/generation gates, not as language by itself. MARULHO therefore adds `snn_language_autonomous_bound_readout_observation_executor.v1`, a no-operator ledger writer that records bounded activation-sparsity, slot-drift, and binding-reactivation metrics for current preflight targets while refusing decoding, language generation, training, replay execution, plasticity, checkpoints, fact/action promotion, and unreviewed cognition claims.
- SNN autonomous bound readout observation event review note, June 2026: language-capable SNN work such as NeuronSpark, SymbolicLight, SpikeGPT, and SpikingSSMs keeps sparse activation behavior central to scalable generation, so MARULHO treats recorded readout observations as evidence that still needs hash, revision, and metric review before training-window design. MARULHO therefore adds `snn_language_autonomous_bound_readout_observation_event_review.v1`, a no-operator read-only review that verifies ledger presence, sample count, activation sparsity, slot drift, and binding reactivation while refusing decoding, language generation, training, replay execution, plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous readout training-window design note, June 2026: SNN language training papers increasingly combine sparse event representations with memory-aware training windows, surrogate/truncated temporal credit assignment, and hardware-conscious execution. MARULHO therefore adds `snn_language_autonomous_readout_training_window_design.v1`, a no-operator design surface that converts reviewed bound-readout observation evidence into a CUDA-aware, spike-compressed, gradient-checkpointed training-window plan while still refusing training execution, decoding, language generation, replay execution, plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous readout training-window preflight note, June 2026: recent SNN training work emphasizes that surrogate or online credit assignment must be paired with bounded memory usage, especially gradient checkpointing and spike compression, before long temporal windows can be trained safely on accelerators. MARULHO therefore adds `snn_language_autonomous_readout_training_window_preflight.v1`, a no-operator current-revision, CUDA/device, memory-plan, executor-capability, and hash-integrity audit before any autonomous readout training executor while still refusing training execution, decoding, language generation, replay execution, plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous readout training-window executor note, June 2026: language-capable SNN systems such as SpikeGPT and NeuronSpark show that sparse readout training can support generation, while 2026 online and surrogate-gradient work stresses bounded weight updates, memory efficiency, and stability. MARULHO therefore adds `snn_language_autonomous_readout_training_window_executor.v1`, a no-operator ledger writer that records bounded runtime weight-update evidence, loss movement, gradient norm, weight delta, and spike sparsity for a current preflight while still refusing decoding, language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous readout training-window event review note, June 2026: stability methods in SNN language and online-learning work make post-update validation as important as the update itself; loss, gradient norm, weight delta, spike sparsity, and update hashes must remain bounded before decoder probing. MARULHO therefore adds `snn_language_autonomous_readout_training_window_event_review.v1`, a no-operator read-only review of recorded training-window events that opens only an autonomous decoder-probe design gate while still refusing decoding, language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous decoder probe design note, June 2026: SNN language systems such as SpikeGPT, SymbolicLight, and NeuronSpark demonstrate that trained sparse readouts can support language generation, but the first post-training step should remain a bounded probe over sparse activation/readout hashes rather than free-form text. MARULHO therefore adds `snn_language_autonomous_decoder_probe_design.v1`, a no-operator design surface that binds reviewed training-event, update, target, and sample hashes into hash-only decoder probe targets while still refusing decoding, language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous decoder probe preflight note, June 2026: recent SNN language and sparse-probing work indicates that post-training decoder access should validate sparse readout targets and device execution separately before any output interpretation. MARULHO therefore adds `snn_language_autonomous_decoder_probe_preflight.v1`, a no-operator current-revision, CUDA/device, executor-capability, probe-target, and hash-integrity audit before autonomous decoder probing while still refusing decoding, language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous decoder probe executor note, June 2026: SNN language models such as SpikeGPT, SymbolicLight, and NeuronSpark show that sparse readouts can support language behavior, while sparse probing work argues for bounded top-k/readout inspection before interpreting activations. MARULHO therefore adds `snn_language_autonomous_decoder_probe_executor.v1`, a no-operator ledger writer that records hash-only probe targets, output hashes, rank hashes, score, spike sparsity, and slot drift while still refusing decoded text, language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous decoder probe event review note, June 2026: recent SNN language and online-learning work supports sparse, event-driven readouts, but also shows that stability, sparsity, and bounded post-update evidence matter before treating outputs as language-facing. MARULHO therefore adds `snn_language_autonomous_decoder_probe_event_review.v1`, a no-operator read-only review that confirms the recorded hash-only probe event is ledger-present, current, sparse, bounded, and side-effect-free before opening only an autonomous language-output design gate while still refusing decoded text, language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous language output design note, June 2026: SpikeGPT, NeuronSpark, Nord-AI, and sparse spike-gated language work suggest language-facing SNNs need an explicit output path from sparse activity to token-like candidates, but current MARULHO evidence is still hash-level rather than decoded semantics. MARULHO therefore adds `snn_language_autonomous_language_output_design.v1`, a no-operator read-only design that binds reviewed probe hashes, candidate output slots, output length, score/sparsity/drift thresholds, and device evidence before any output preflight while still refusing decoded text, language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous language output preflight note, June 2026: sparse SNN language generation work, including NeuronSpark, SpikeGPT, Nord-AI, and WTA spiking transformers, points toward constrained candidate-output paths rather than unconstrained dense decoding. MARULHO therefore adds `snn_language_autonomous_language_output_preflight.v1`, a no-operator current-revision, device/CUDA, slot-hash, and executor-capability audit before bounded hash-only language-output execution while still refusing decoded text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous language output executor note, June 2026: NeuronSpark and SpikeGPT show SNNs can be trained for next-token language behavior, while sparse scaling and WTA spiking-transformer work point toward bounded sparse candidate execution before broader decoding. MARULHO therefore adds `snn_language_autonomous_language_output_executor.v1`, a no-operator ledger writer for hash-only output slots that records candidate hashes, emitted hashes, rank hashes, confidence, spike sparsity, and slot drift while still refusing decoded text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous language output event review note, June 2026: SNN language models and constrained-decoding research both argue for separating bounded candidate execution from later interpretation, because sparse output evidence can be structurally valid without proving semantic correctness. MARULHO therefore adds `snn_language_autonomous_language_output_event_review.v1`, a no-operator read-only audit that confirms ledger presence, revision adjacency, hash-only output slots, confidence, sparsity, and drift bounds before opening only an autonomous decoded-output design gate while still refusing decoded text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous decoded output design note, June 2026: SNN next-token models and constrained-decoding systems both depend on an explicit vocabulary/token constraint surface before candidate evidence becomes interpretable text. MARULHO therefore adds `snn_language_autonomous_decoded_output_design.v1`, a no-operator read-only design that binds reviewed emitted hashes to token-candidate hashes, tokenizer hash, vocabulary hash, decode-constraint hash, score/sparsity/drift thresholds, and device evidence before any decoded-output preflight while still refusing token strings, decoded text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous decoded output preflight note, June 2026: constrained-decoding work shows that token masks, vocabulary transitions, and schema/trie constraints should be validated before execution, while SNN language work keeps the candidate stream sparse and device-sensitive. MARULHO therefore adds `snn_language_autonomous_decoded_output_preflight.v1`, a no-operator current-revision, device/CUDA, tokenizer/vocabulary/constraint-hash, decode-slot, and executor-capability audit before constrained decoded-output execution while still refusing token strings, decoded text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous decoded output executor note, June 2026: constrained decoding executes by masking or validating token transitions at each step, while SNN language systems such as NeuronSpark keep the candidate stream sparse and spike-derived. MARULHO therefore adds `snn_language_autonomous_decoded_output_executor.v1`, a no-operator ledger writer that records constrained decoded-token hashes, token-id hashes, constraint-state hashes, confidence, spike sparsity, and drift while still refusing token strings, decoded text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous decoded output event review note, June 2026: constrained decoding provides structural/token-transition validity, but recent structured-output work cautions that syntactic validity and semantic correctness are separate, while SNN language work still requires sparse confidence and stability checks. MARULHO therefore adds `snn_language_autonomous_decoded_output_event_review.v1`, a no-operator read-only audit that confirms ledger presence, revision adjacency, decoded-token hash integrity, constraint-state hashes, confidence, sparsity, and drift bounds before opening only bounded text-emission design while still refusing token strings, decoded text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous output-chain source-window note, June 2026: modern Hopfield recall is useful here only as a bounded associative lookup after a local source family has been selected, and CLS/continual-replay/synaptic-tagging work argues against making output review an archive-wide background pass. MARULHO therefore routes autonomous language-output and decoded-output execution/review through `bounded_snn_readout_ledger_record_family_source_window.v1`, reading only `autonomous_language_output_events` or `autonomous_decoded_output_events` before duplicate or ledger-presence checks. The old broad `_normalized_state()` production path is retired to benchmark-only evidence; archival ledger metadata stays CPU-resident, CUDA is unused for this control-plane work, no raw text payload is loaded, and the surfaces still refuse hidden language reasoning, live-tick execution, every-token cadence, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text-surface source-window note, June 2026: structured-output and constrained-decoding work both separate token/fragment validity from durable surface state, while CLS/continual replay still require selected evidence before consolidation. MARULHO therefore routes bounded text-emission execution/review and text-surface commit execution/review through `bounded_snn_readout_ledger_record_family_source_window.v1`, reading only `autonomous_bounded_text_emission_events` or `autonomous_text_surface_commit_events` and preserving the single current-commit pointer without normalizing unrelated ledger families. The old broad `_normalized_state()` append/review path is retired to benchmark-only evidence; archival ledger metadata remains CPU-resident, CUDA is unused for this control-plane work, literal text is not loaded for hidden reasoning, and the surfaces still refuse live-tick execution, every-token cadence, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous bounded text emission design note, June 2026: structured-output and constrained-decoding systems show that rendering text should be treated as a separate surface from token selection, with schema, normalization, and semantic constraints still validated independently. MARULHO therefore adds `snn_language_autonomous_bounded_text_emission_design.v1`, a no-operator read-only design that binds reviewed decoded-token hashes to text-fragment hashes, text-surface schema hash, text normalizer hash, semantic constraint hash, and bounded emission slots while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous bounded text emission preflight note, June 2026: constrained decoding and structured-output gateways need schema versioning, token/slot constraints, semantic validation, repair or rejection paths, and tracing before output leaves the model boundary; sparse SNN language evidence adds confidence, spike-sparsity, drift, and device-readiness checks. MARULHO therefore adds `snn_language_autonomous_bounded_text_emission_preflight.v1`, a no-operator read-only audit that verifies current revision, device/CUDA evidence, hash-only text fragments, text-surface schema, normalizer, semantic constraint, slot bounds, and executor capability before opening only bounded text-emission execution while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous bounded text emission executor note, June 2026: recent structured-output work emphasizes that execution needs traceable schema/semantic witnesses, not just syntactic conformance, while SNN language evidence such as NeuronSpark and sparse spiking language-model reports keep confidence, activation sparsity, and drift central to bounded generation. MARULHO therefore adds `snn_language_autonomous_bounded_text_emission_executor.v1`, a no-operator ledger writer that records hash-only text-fragment, slot, schema, normalizer, semantic-constraint, confidence, sparsity, and drift evidence as an event while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous bounded text emission event review note, June 2026: structured-output literature separates schema validity from semantic correctness, and constrained-decoding papers now emphasize semantic constraints and traceable review events; SNN language work keeps sparse confidence and stability as core evidence. MARULHO therefore adds `snn_language_autonomous_bounded_text_emission_event_review.v1`, a no-operator read-only audit that confirms ledger presence, revision adjacency, hash-only text fragments, schema/normalizer/semantic-constraint hashes, confidence, sparsity, and drift before opening only later hash-surface sequence review while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface sequence review note, June 2026: structured-output and constrained-decoding systems need sequence-level validation in addition to slot-level validation, because schema-conformant fragments can still be semantically invalid or out of order. MARULHO therefore adds `snn_language_autonomous_text_surface_sequence_review.v1`, a no-operator read-only audit that binds reviewed text-fragment hashes into a fragment-sequence hash under the same schema, normalizer, semantic constraint, confidence, sparsity, and drift evidence while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface commit design note, June 2026: structured-output production guidance emphasizes traceable schema versions, commit metadata, and reconstructable runtime evidence before validated outputs become durable state; sparse SNN language work adds confidence, sparsity, and drift evidence to that commit boundary. MARULHO therefore adds `snn_language_autonomous_text_surface_commit_design.v1`, a no-operator read-only design that turns a reviewed fragment-sequence hash into a commit plan hash with commit scope and retention class while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface commit preflight note, June 2026: runtime-evidence and structured-output guidance emphasize canonical schema hashes, commit-plan hashes, chain integrity, and executor admission checks before validated output becomes state. MARULHO therefore adds `snn_language_autonomous_text_surface_commit_preflight.v1`, a no-operator current-revision, device/CUDA, commit-plan, schema, normalizer, semantic-constraint, and executor-capability audit before hash-surface commit execution while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface commit executor note, June 2026: structured-output runtime guidance emphasizes traceable commit events, schema-bound state, chain evidence, and observability before a validated output becomes system state; sparse SNN language work keeps confidence, activation sparsity, and drift as required output-stability evidence. MARULHO therefore adds `snn_language_autonomous_text_surface_commit_executor.v1`, a no-operator ledger writer that records a hash-only committed text surface, state-chain hash, commit-plan hash, sequence hash, event hash, schema hash, normalizer hash, semantic-constraint hash, and fragment hashes while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface commit event-review note, June 2026: runtime-assurance work for neuromorphic and autonomous systems supports checking committed events against current state and bounded stability evidence before promotion, while structured-output systems separate structural conformance from semantic correctness and trace every schema-bound state transition. MARULHO therefore adds `snn_language_autonomous_text_surface_commit_event_review.v1`, a no-operator read-only audit that verifies ledger presence, current committed-surface match, revision adjacency, state-chain hash, commit-plan hash, fragment-sequence hash, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, and drift while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface materialization-design note, June 2026: constrained-decoding and structured-output work shows that output materialization needs an explicit schema/contract boundary, while NeuronSpark/Nord-style SNN language work suggests text generation should remain tied to sparse confidence, drift, and activation evidence rather than a detached language backend. MARULHO therefore adds `snn_language_autonomous_text_surface_materialization_design.v1`, a no-operator read-only design that binds a reviewed committed hash surface to a bounded materialization mode, output contract, maximum surface size, state-chain hash, commit-plan hash, fragment-sequence hash, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, and drift while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface materialization-preflight note, June 2026: pre-generation admission gates and constrained-output systems emphasize that executor eligibility, device evidence, schema contracts, and semantic constraints should be checked before generation contaminates runtime state; SNN language work such as NeuronSpark also makes CUDA/Triton-style execution evidence relevant for spike-derived language paths. MARULHO therefore adds `snn_language_autonomous_text_surface_materialization_preflight.v1`, a no-operator current-revision, device/CUDA, materialization-plan, schema, normalizer, semantic-constraint, and executor-capability audit before bounded text-surface materialization while still refusing literal text, free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface materialization-executor note, June 2026: semantic constrained-output work such as ChopChop and projectional decoding emphasizes that executable output should satisfy structural and semantic witnesses, while SNN language reports such as NeuronSpark/Nord keep sparse stability and quality limits visible. MARULHO therefore adds `snn_language_autonomous_text_surface_materialization_executor.v1`, a no-operator ledger writer that returns bounded literal text only when supplied fragments hash back to committed text-fragment hashes under the same schema, normalizer, and semantic-constraint evidence, while recording rendered-text hash, materialization-plan hash, state-chain hash, confidence, sparsity, and drift and still refusing free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous text surface materialization event-review note, June 2026: structured-output production guidance separates parse/schema validity from semantic correctness and requires traceable review/evaluation, while SNN language outputs need confidence, sparsity, and drift evidence before they influence later behavior. MARULHO therefore adds `snn_language_autonomous_text_surface_materialization_event_review.v1`, a no-operator read-only audit that verifies ledger presence, current materialized-state match, revision adjacency, rendered-text hash, fragment-hash recomputation, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, and drift while still refusing free-form language generation, replay execution, structural plasticity, checkpoints, fact/action promotion, and cognition-substrate claims.
- SNN autonomous bounded language-surface review note, June 2026: data-to-text evaluation and structured-output research both distinguish surface validity from semantic accuracy, and SNN language work requires spike confidence/sparsity evidence before generated text can influence later state. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_review.v1`, a no-operator read-only audit that classifies a verified bounded text surface as language-facing evidence while carrying rendered text, rendered-text hash, fragment hashes, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, and drift and still refusing replay execution, structural plasticity, checkpoints, fact/action promotion, free-form language generation, and cognition-substrate claims.
- SNN autonomous bounded language-surface commit-design note, June 2026: structured runtime-evidence and constrained/semantic output work both point to traceable state commitment as separate from text production, while current SNN language work still needs confidence, sparsity, and drift evidence before language surfaces can influence later memory or action paths. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_commit_design.v1`, a no-operator read-only commit plan that binds reviewed bounded text, rendered-text hash, fragment hashes, materialization hashes, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, drift, commit scope, and retention class while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous bounded language-surface commit-preflight note, June 2026: neuromorphic runtime-assurance work and constrained-output production guidance both favor explicit admission checks before validated outputs become state; SNN language evidence further requires confidence, sparsity, drift, and device readiness rather than schema validity alone. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_commit_preflight.v1`, a no-operator read-only audit that verifies current revision, device/CUDA evidence, executor capability, rendered-text bounds, fragment hashes, materialization hashes, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, and drift before a later bounded language-surface commit executor can be considered, while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous bounded language-surface commit-executor note, June 2026: SNN language work such as NeuronSpark, SpikeGPT, and natively trained sparse SNN language models makes bounded language state plausible only when sparse activation and stability evidence stay attached to output surfaces, while runtime-evidence standards emphasize verifiable state-transition receipts. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_commit_executor.v1`, a no-operator mutation boundary that records a bounded literal language-surface commit event, current bounded language-surface commit state, state-chain hash, rendered-text hash, fragment hashes, materialization hashes, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, and drift while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous bounded language-surface commit-event-review note, June 2026: runtime-assurance work for neuromorphic systems emphasizes checking internal state after transitions, and structured-output guidance warns that valid structure still needs semantic and trace validation. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_commit_event_review.v1`, a no-operator read-only audit that verifies a bounded language-surface commit event is recorded, current, revision-adjacent, rendered-text bounded, hash-lineage complete, schema-bound, normalizer-bound, semantic-constraint-bound, confidence-bounded, sparse, and drift-bounded while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous bounded language-surface use-review note, June 2026: semantic calibration and structured-output research both warn that a valid text surface should not be trusted merely because it parses or has confidence metadata; traceability, bounded semantics, and downstream use scope need their own evidence. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_use_review.v1`, a no-operator read-only review that classifies a reviewed committed bounded language surface as usable bounded language evidence only under an explicit use scope, while preserving rendered-text, fragment, state-chain, schema, normalizer, semantic-constraint, confidence, sparsity, and drift evidence and still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous bounded language-surface use-preflight note, June 2026: confidence-based abstention and semantic calibration work both support a deployment-time admission gate between valid language evidence and downstream use, while SNN language reports keep sparse activation and stability evidence central. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_use_preflight.v1`, a no-operator current-revision/device/executor audit that verifies use-review lineage, use scope, bounded rendered text, fragment hashes, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, drift, CUDA compatibility, and executor capability before any bounded use event can be recorded, while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous bounded language-surface use-executor note, June 2026: current SNN language-model reports such as NeuronSpark, Winner-Take-All Spiking Transformer, and natively trained sparse language models show that sparse language surfaces can become generative substrates, while runtime-attestation/provenance work emphasizes receipts for every state transition before downstream autonomy trusts an output. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_use_executor.v1`, a no-operator ledger mutation that records reviewed bounded text, use scope, state-chain, schema, normalizer, semantic-constraint, confidence, sparsity, drift, and use-chain hashes as traceable bounded-use evidence while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, text decoding, free-form generation, and cognition-substrate claims.
- SNN autonomous bounded language-surface use-event-review note, June 2026: provenance-bearing memory and agent trace research argue that autonomous downstream behavior needs verifiable state-transition evidence, while SNN language work such as NeuronSpark and Nord-style sparse text generation suggests bounded spike-derived language evidence can become useful only after stability and sparsity checks survive recording. MARULHO therefore adds `snn_language_autonomous_bounded_language_surface_use_event_review.v1`, a no-operator read-only audit that verifies the recorded use event, revision adjacency, use-chain hash, text bounds, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, and drift before naming a future autonomous SNN language-generation design gate, while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, text decoding, free-form generation, and cognition-substrate claims.
- SNN autonomous SNN language-generation design note, June 2026: NeuronSpark, Winner-Take-All Spiking Transformer, Adaptive Spiking Neurons, Nord-AI, and sparse natively trained SNN language-model reports all point toward bounded spike-native next-token projection as a plausible language path when sparsity, stability, and device evidence are explicit. MARULHO therefore adds `snn_language_autonomous_snn_language_generation_design.v1`, a no-operator read-only design that binds verified bounded language-use evidence to a sparse SNN generation mode, spike projection strategy, bounded token/fragment budgets, target device/CUDA intent, schema hash, normalizer hash, semantic-constraint hash, confidence, sparsity, and drift while still refusing token decoding, generated text output, replay, plasticity, checkpoint persistence, fact/action promotion, LLM/Cortex/external-checkpoint paths, free-form generation, and cognition-substrate claims.
- SNN autonomous SNN language-generation preflight note, June 2026: SNN language generation work increasingly depends on sparse activation, bounded token projection, and hardware-aware execution, while GPU-oriented SNN frameworks show that local spike updates can be expressed as parallel tensor work. MARULHO therefore adds `snn_language_autonomous_snn_language_generation_preflight.v1`, a no-operator current-revision/device/CUDA/executor audit that verifies the SNN generation design, use-event lineage, sparse context hashes, schema hash, normalizer hash, semantic-constraint hash, generation budgets, confidence, sparsity, and drift before execution can be considered, while still refusing token decoding, generated text output, replay, plasticity, checkpoint persistence, external checkpoints, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous SNN language-generation executor note, June 2026: sparse SNN language-model inference reports emphasize that generated next-token evidence is tied to spike activity, activation sparsity, and hardware-aware execution rather than dense LLM-style hidden-state decoding. MARULHO therefore adds `snn_language_autonomous_snn_language_generation_executor.v1`, a no-operator ledger mutation that records generated token hashes, spike-projection hashes, active-neuron hashes, membrane-state hashes, output-fragment hashes, generation-plan lineage, use-event lineage, and device evidence as bounded autonomous SNN generation evidence while still refusing token decoding, literal generated text output, replay, plasticity, checkpoint persistence, external checkpoints, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous SNN language-generation event-review note, June 2026: computational-provenance work argues that downstream autonomy should verify how an inference output was produced, while sparse SNN language reports emphasize activation sparsity and spike traces as part of the generation evidence. MARULHO therefore adds `snn_language_autonomous_snn_language_generation_event_review.v1`, a no-operator read-only audit that verifies the recorded generation event, revision adjacency, projection hash, generated-token hashes, spike-projection hashes, active-neuron hashes, membrane-state hashes, output-fragment hashes, use lineage, generation-plan lineage, and device evidence before bounded decoding can be designed, while still refusing token decoding, literal generated text output, replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous SNN language-decoding design note, June 2026: constrained/projectional decoding work shows that generated token materialization should be bounded by schema and semantic state, while sparse SNN language reports keep spike provenance and activation sparsity central to language output evidence. MARULHO therefore adds `snn_language_autonomous_snn_language_decoding_design.v1`, a no-operator read-only design that binds reviewed SNN generation hashes, spike-projection hashes, active-neuron hashes, membrane-state hashes, output-fragment hashes, schema/normalizer/semantic constraints, target surface, and decoding budgets before any decoding preflight may run, while still refusing token decoding, literal text return, replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous SNN language-decoding preflight note, June 2026: constrained-decoding systems separate semantic/schema readiness from the actual decode, and sparse SNN language reports imply any text materialization should preserve spike, activation, membrane, and output-fragment provenance. MARULHO therefore adds `snn_language_autonomous_snn_language_decoding_preflight.v1`, a no-operator current-revision/device/CUDA/decoder-capability audit that proves a decoding design is bounded and machine-ready for a later executor while still refusing token decoding, literal text return, replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN autonomous SNN language-decoding executor note, June 2026: NeuronSpark and Nord-style SNN language reports make bounded text generation plausible from sparse spike-native state, while constrained-decoding work stresses schema validity, semantic checks, and tracing at execution time. MARULHO therefore adds `snn_language_autonomous_snn_language_decoding_executor.v1`, a no-operator ledger mutation that records bounded decoded literal fragments plus fragment/render hashes, generated-token lineage, spike-projection hashes, active-neuron hashes, membrane-state hashes, output-fragment hashes, schema/normalizer/semantic validity, and device lineage while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, external checkpoints, and cognition-substrate claims.
- SNN autonomous SNN language-decoding event-review note, June 2026: execution-provenance research argues that autonomous systems should verify produced outputs against their trace and evidence units before downstream use, while constrained decoding work separates structural conformance from semantic correctness. MARULHO therefore adds `snn_language_autonomous_snn_language_decoding_event_review.v1`, a no-operator read-only audit that verifies the recorded decoded-language event, revision adjacency, rendered-text hash, decoded-fragment hashes, generated-token lineage, spike/membrane/output provenance, schema validity, normalization, semantic validity, and device lineage before any readout-surface design can consume it, while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, external checkpoints, and cognition-substrate claims.
- SNN language readout-surface design note, June 2026: concept formation and symbolic-control work can inspire bounded internal representational surfaces, but MARULHO must keep them operator-facing evidence rather than a hidden thought stream. The maintained production shape is now `snn_language_readout_surface_design.v1`, a no-operator read-only plan that binds reviewed decoded SNN language to a bounded readout role, rendered-text hash, decoded-fragment hashes, generated-token lineage, spike/membrane/output provenance, schema/normalizer/semantic constraints, and bounded association budgets while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-surface preflight note, June 2026: internal-representation work points toward organized latent/control surfaces, but MARULHO still needs a machine-checkable boundary before recording bounded readout evidence. The maintained production shape is now `snn_language_readout_surface_preflight.v1`, a no-operator current-revision/device/CUDA/executor-capability audit that verifies readout-surface design, rendered text bounds, decoded-fragment hashes, generated-token lineage, spike/membrane/output provenance, schema/normalizer/semantic hashes, and bounded association budgets while still refusing event recording, replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-surface executor note, June 2026: concept-formation and working-memory work support separating internal representational evidence from direct action, while sparse SNN language work keeps spike provenance central. The maintained production shape is now `snn_language_readout_surface_executor.v1`, a no-operator ledger mutation that records a bounded readout-surface event with rendered text, fragment/render hashes, decoded/generated-token lineage, spike/membrane/output provenance, device lineage, readout role, binding mode, and association budgets while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, hidden language reasoning, and cognition-substrate claims.
- SNN language readout-surface event-review note, June 2026: current SNN working-memory work emphasizes sequential spike motifs and recurrent temporal state, while concept-formation results distinguish internal representational units from direct motor action. The maintained production shape is now `snn_language_readout_surface_event_review.v1`, a no-operator read-only audit that verifies a recorded readout-surface event is ledger-present, revision-adjacent, hash-linked to preflight/design/decoded-language evidence, bounded, and spike/membrane/output-provenanced before any readout-memory design may consume it, while still refusing replay, plasticity, checkpoint persistence, fact/action promotion, free-form generation, hidden language reasoning, and cognition-substrate claims.
- SNN language readout-memory design note, June 2026: current work on expandable SNNs, recurrent spike-motif working memory, local Hebbian/predictive-coding learning, and oscillation-timed consolidation suggests that memory candidates should preserve local spike-time evidence before any consolidation or plasticity step. MARULHO therefore uses `snn_language_readout_memory_design.v1`, a no-operator read-only plan that binds reviewed readout-surface events to bounded memory-trace hashes and local-learning target hashes while still refusing memory recording, replay, plasticity, checkpoint persistence, structural growth/pruning, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-memory preflight note, June 2026: hardware-efficient SNN training and local-learning papers emphasize sparse device-aware execution, memory-access bounds, and stable local plasticity before any weight update. MARULHO therefore uses `snn_language_readout_memory_preflight.v1`, a no-operator current-revision/device/CUDA/executor-capability audit that verifies readout-memory trace hashes, local-learning target hashes, spike/membrane/output provenance, and bounded trace budgets before any memory executor may run, while still refusing memory recording, replay, plasticity, checkpoint persistence, structural growth/pruning, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-memory executor note, June 2026: BrainTrace-style linear-memory online learning and self-normalizing STDP both point to the need for stable, bounded trace evidence before any local weight update. MARULHO therefore uses `snn_language_readout_memory_executor.v1`, a no-operator ledger mutation that records a bounded readout-memory trace and local-learning targets while still refusing replay, plasticity, checkpoint persistence, structural growth/pruning, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-memory event-review note, June 2026: current trace-propagation, local-learning, and neuromorphic-architecture reviews all separate trace evidence from the later weight-update mechanism. MARULHO therefore uses `snn_language_readout_memory_event_review.v1`, a no-operator read-only audit that verifies recorded readout-memory traces, local-learning targets, sparse spike/membrane/output provenance, and ledger presence before any consolidation design may consume them, while still refusing replay, plasticity, checkpoint persistence, structural growth/pruning, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-consolidation design note, June 2026: recent local-learning, delay-learning, and neuromorphic-architecture work treats trace evidence, candidate local updates, and the later plasticity application as separate stages. MARULHO therefore uses `snn_language_readout_consolidation_design.v1`, a no-operator read-only plan that derives bounded candidate local updates from reviewed readout-memory traces while still refusing replay, plasticity application, checkpoint persistence, structural growth/pruning, fact/action promotion, free-form generation, hidden thought claims, and cognition-substrate claims.
- SNN language readout-consolidation preflight note, June 2026: device-calibrated local-learning and neuromorphic training work emphasize bounded sparse updates, memory-access limits, and device readiness before plasticity is applied. MARULHO therefore uses `snn_language_readout_consolidation_preflight.v1`, a no-operator current-revision/device/CUDA/executor-capability audit over candidate local updates and spike/membrane/local-target provenance while still refusing replay, plasticity application, checkpoint persistence, structural growth/pruning, fact/action promotion, free-form generation, hidden thought claims, and cognition-substrate claims.
- SNN language readout-consolidation executor note, June 2026: local Hebbian, device-calibrated, and self-normalizing plasticity results make bounded local updates plausible only when provenance and stability terms are preserved. MARULHO therefore uses `snn_language_readout_consolidation_executor.v1`, a no-operator ledger mutation that applies bounded local readout-consolidation updates from ready preflight evidence while still refusing replay loops, checkpoint persistence, structural growth/pruning, fact/action promotion, free-form generation, hidden thought claims, and cognition-substrate claims.
- SNN language readout-consolidation event-review note, June 2026: recent structural-evolution, expandable-SNN, and homeostatic STDP work all separate plasticity application from later structural growth and stability-preserving consolidation review. MARULHO therefore uses `snn_language_readout_consolidation_event_review.v1`, a no-operator read-only audit over an applied local-plasticity ledger event, verifying bounded updates, learning-rate/weight-delta/homeostatic-decay limits, ledger presence, and spike/membrane/output provenance while still refusing replay, checkpoint persistence, structural growth/pruning execution, fact/action promotion, free-form generation, hidden thought claims, and cognition-substrate claims.
- SNN language readout-structural-plasticity design note, June 2026: PS-SNN, NG-SNN, CogniSNN, and GPU sparse-structural-plasticity work support dynamic growth/pruning as a separate structural stage after stable local learning evidence. MARULHO therefore uses `snn_language_readout_structural_plasticity_design.v1`, a no-operator read-only plan that derives bounded sparse growth and homeostatic-prune candidates from reviewed readout-consolidation plasticity while still refusing replay, checkpoint persistence, structural mutation execution, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-structural-plasticity preflight note, June 2026: GPU sparse structural-plasticity work emphasizes ragged sparse layouts, device-side update costs, and memory remapping bounds before growth/prune mutation. MARULHO therefore uses `snn_language_readout_structural_plasticity_preflight.v1`, a no-operator current-revision/CUDA/executor-capability audit over bounded readout-driven growth and prune candidates while still refusing replay, checkpoint persistence, structural mutation execution, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-structural-plasticity executor note, June 2026: GPU sparse structural-plasticity frameworks, neurogenesis-inspired SNN growth, MorphSNN-style dynamic topology, and operational-manifold stability work support bounded event-driven topology mutation only when sparse provenance and device evidence are preserved. MARULHO therefore uses `snn_language_readout_structural_plasticity_executor.v1`, a no-operator ledger mutation that applies bounded readout-driven growth/prune candidates as structural-plasticity evidence while still refusing dense tensor resize, checkpoint persistence, replay, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-structural-plasticity event-review note, June 2026: structural-evolution reviews, dynamic sparse SNN training, MorphSNN, and operational-manifold work all imply topology mutation needs a post-application audit before it can feed larger capacity changes. MARULHO therefore uses `snn_language_readout_structural_plasticity_event_review.v1`, a no-operator read-only audit over ledgered growth/prune events, verifying sparse budgets, provenance links, and no dense resize/checkpoint/replay escalation while still refusing fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-capacity-mutation design note, June 2026: expandable SNN, dynamic-structure-development, sparse neuromorphic training, and spiking-language scaling work all separate sparse topology evidence from tensor-capacity relayout. MARULHO therefore uses `snn_language_readout_capacity_mutation_design.v1`, a no-operator read-only plan that derives target neuron capacity, sparse synapse budget, dense relayout shape, CUDA relayout requirement, checkpoint requirement, and restore-validation requirement from reviewed readout structural-plasticity evidence while still refusing tensor resize execution, replay, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-capacity-mutation preflight note, June 2026: expandable-SNN and sparse neuromorphic-training work imply tensor-capacity mutation needs current-revision, CUDA relayout, checkpoint, restore, and shape-preservation evidence before execution. MARULHO therefore uses `snn_language_readout_capacity_mutation_preflight.v1`, a no-operator audit over target neuron capacity, sparse synapse budget, dense relayout shape, checkpoint/restore evidence, CUDA relayout evidence, and executor capability while still refusing tensor resize execution, replay, fact/action promotion, free-form generation, and cognition-substrate claims.
- SNN language readout-capacity-mutation executor note, June 2026: PS-SNN-style neurogenesis preserves existing representations while expanding capacity, GPU sparse structural-plasticity work separates topology mutation from storage relayout, and PyTorch documents that low-level in-place resize leaves new storage uninitialized. MARULHO therefore uses `snn_language_readout_capacity_mutation_executor.v1`, which allocates a fresh zero-initialized target tensor on the requested device, copies the preserved window, expands the sparse-edge budget, records dynamic-capacity provenance, and commits through pre/post checkpoint verification. New capacity remains untrained and inactive until later local learning or replay supplies bounded synaptic evidence.
- SNN language readout-capacity-mutation event-review note, June 2026: NG-SNN integrates new neurons selectively by functional contribution, biological adult neurogenesis places newborn neurons through an activity-dependent integration period, and operational-manifold work requires stable activity rather than storage alone. MARULHO therefore uses `snn_language_readout_capacity_mutation_event_review.v1`, which recomputes mutation provenance and matches the executor event to the live dynamic-capacity state, tensor shape/device, zero-filled newborn region, and committed checkpoints before any newborn-neuron integration design may begin.
- SNN language readout newborn-neuron-integration design note, June 2026: adult-born neurons exhibit a temporary period of enhanced plasticity rather than immediate mature connectivity ([Ge et al., 2007](https://pubmed.ncbi.nlm.nih.gov/17521569/)); experience remodels their inputs during a bounded developmental window ([Bergami et al., 2015](https://pubmed.ncbi.nlm.nih.gov/25661179/)); recent activity-dependent integration review emphasizes coincident local activity as a survival/integration signal ([Bao et al., 2024](https://pubmed.ncbi.nlm.nih.gov/38553878/)); and a biologically grounded SNN model links critical-period responsiveness to maturation of inhibitory control ([Neural activity responsiveness, 2024](https://www.frontiersin.org/journals/neural-circuits/articles/10.3389/fncir.2024.1519704/full)). MARULHO therefore adds `snn_language_readout_newborn_neuron_integration_design.v1`: each zero-filled newborn slot is bound to reviewed spike/membrane/local-learning provenance with zero applied weight, unresolved live source index, bounded co-activity evidence, a finite critical period, homeostatic firing limits, and later inactivity-pruning eligibility. The design is read-only and cannot connect, activate, train, or otherwise claim that the newborn neuron has matured.
- SNN language readout newborn-neuron-integration preflight note, June 2026: biological integration is competitive and activity-dependent, with early low-spiking newborn cells receiving staged inputs before stable excitatory incorporation ([Functions of adult-born neurons](https://pmc.ncbi.nlm.nih.gov/articles/PMC7397477/)); synaptic development is most flexible during an early bounded window ([Kelsch et al., 2009](https://pmc.ncbi.nlm.nih.gov/articles/PMC2773669/)); and structural SNN learning benefits from local activity correlation rather than global architecture updates ([Blessing of dimensionality, 2025](https://www.frontiersin.org/journals/applied-mathematics-and-statistics/articles/10.3389/fams.2025.1553779/full)). MARULHO therefore adds `snn_language_readout_newborn_neuron_integration_preflight.v1`: it resolves mature source neurons only from sorted, unique live active-index populations whose canonical hashes match the stored readout lineage, requires repeated co-activity and homeostatic firing bounds, rejects newborn-to-newborn seeding and existing edges, verifies the expanded tensor/device and rollback checkpoint, and still performs no mutation.
- SNN language readout newborn-neuron-integration executor note, June 2026: newborn-neuron survival and integration depend on competitive activity while early connectivity remains sparse and staged ([Experience matters](https://pmc.ncbi.nlm.nih.gov/articles/PMC5476834/)); functional incorporation requires both synaptic inputs and outputs to mature over time rather than appearing at birth ([Synaptic integration and plasticity](https://pmc.ncbi.nlm.nih.gov/articles/PMC2538931/)); and GPU sparse structural-plasticity work supports bounded topology mutation without densifying the active graph ([GPU sparse structural plasticity](https://arxiv.org/abs/2510.19764)). MARULHO therefore adds `snn_language_readout_newborn_neuron_integration_executor.v1`: it creates only the preflight-resolved mature-to-newborn seed edges, uses one quarter of the bounded maximum initial weight, mirrors each edge into sparse and dense owned state, records critical-period provenance, and commits or rolls back through checkpoint verification.
- SNN language readout newborn-neuron-integration event-review note, June 2026: input-dependent survival is cell-specific during a short post-birth window ([Tashiro et al., 2006](https://www.nature.com/articles/nature05028)), while adult-born neurons form real output synapses rather than merely occupying capacity ([Toni et al., 2008](https://www.nature.com/articles/nn.2156)). MARULHO therefore adds `snn_language_readout_newborn_neuron_integration_event_review.v1`: it accepts integration only when each edge agrees across its content hash, sparse state, sampled dense tensor cell, exact provenance, critical-period record, current revision, and committed checkpoint. The review is read-only and promotes only to developmental learning design.
- SNN language readout newborn-neuron critical-period-learning design note, June 2026: enhanced plasticity is temporary and has a lower induction threshold during a defined developmental period ([Ge et al., 2007](https://pubmed.ncbi.nlm.nih.gov/17521569/)); young adult-born neurons combine enhanced excitability with already functional presynaptic signaling ([Vyleta and Snyder, 2023](https://www.nature.com/articles/s42003-023-04678-5)); and recurrent sequence learning can combine STDP, structural plasticity, and homeostatic plasticity without a global static trainer ([Wang et al., 2023](https://www.frontiersin.org/journals/neuroscience/articles/10.3389/fnins.2023.1224752/full)). MARULHO therefore adds `snn_language_readout_newborn_neuron_critical_period_learning_design.v1`: finite age, bounded local timing updates, firing-rate homeostasis, activity-dependent survival evidence, and explicit mature/prune-eligible outcomes replace an indefinite high-plasticity state.
- SNN language readout newborn-neuron critical-period-learning preflight note, June 2026: selective survival depends on a newborn cell's own input activity during the critical period ([Tashiro et al., 2006](https://www.nature.com/articles/nature05028)), and correlation-sensitive STDP pruning retains supported connections while removing weakly correlated ones ([Rathi et al., 2017](https://arxiv.org/abs/1710.04734)). MARULHO therefore adds `snn_language_readout_newborn_neuron_critical_period_learning_preflight.v1`: it canonicalizes hash-bound spike times, computes causal and anti-causal pairs itself, derives a bounded homeostatic proposal from current live weight state, and requires matching sparse, dense, provenance, revision, device/CUDA, checkpoint, and executor evidence. Caller-authored weight deltas are not an authority.
- SNN language readout newborn-neuron critical-period-learning executor note, June 2026: survival and maturation are cumulative developmental outcomes rather than a property assigned when a first synapse appears ([Tashiro et al., 2006](https://www.nature.com/articles/nature05028)); homeostatic plasticity stabilizes recurrent STDP learning instead of replacing local timing evidence ([Wang et al., 2023](https://www.frontiersin.org/journals/neuroscience/articles/10.3389/fnins.2023.1224752/full)). MARULHO therefore adds `snn_language_readout_newborn_neuron_critical_period_learning_executor.v1`: one verified cycle updates sparse and dense weight owners together, persists age and activity counters, extends provenance, commits through checkpoint verification, and records `critical_period`, `mature`, or `prune_eligible` state without conflating prune eligibility with deletion.
- SNN language readout newborn-neuron critical-period-learning event-review note, June 2026: longitudinal imaging shows newborn-neuron structural plasticity is local, cumulative, and sustained rather than a sequence of independent resets ([Mizrahi, 2007](https://www.nature.com/articles/nn1875)); repeated learning can recruit distinct survival waves during a bounded availability period ([Forest et al., 2019](https://www.nature.com/articles/s41467-019-13521-7)). MARULHO therefore adds `snn_language_readout_newborn_neuron_critical_period_learning_event_review.v1`: each committed cycle must match live sparse/dense state, persistent developmental counters, provenance history, revision, and checkpoint before another cycle can be reconstructed.
- SNN language readout newborn-neuron critical-period-learning continuation note, June 2026: STDP requires slower or coordinated homeostatic mechanisms to remain stable across repeated updates ([Watt and Desai, 2010](https://www.frontiersin.org/articles/10.3389/fnsyn.2010.00005/full)), while mature adult-born neurons can retain experience-dependent plasticity after integration without remaining permanently newborn ([Livneh and Mizrahi, 2012](https://www.nature.com/articles/nn.2980)). MARULHO therefore adds a reviewed continuation design that preserves current weight, age, remaining critical-period cycles, accumulated active/inactive evidence, and policy lineage. It feeds the same preflight/executor contract for cycle two and later, and terminal `mature` or `prune_eligible` states cannot re-enter the continuation path.
- SNN language readout newborn developmental naming retirement note, June 2026: adult neurogenesis, critical-period plasticity, STDP/homeostasis, and sparse replay literature support an explicit developmental plasticity chain, but they do not justify preserving `thought_newborn` as a second production vocabulary. MARULHO therefore retired `autonomous_snn_language_thought_newborn_*` and `thought_newborn_*` production names across service schema, facade, ledger, executor, developmental autonomy, runtime snapshots, and API mapping. The maintained path is `snn_language_readout_newborn_*`; the quality report `reports/bounded_replay_window_20260622/synthetic-readout-newborn-canonical.json` stayed on `bucket_indexed_candidate_window` with zero global fallback cycles, and the paired `524288`-token report `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-readout-newborn-canonical.json` stayed same-band at `5783.832 tokens/sec` with bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, and RTX memory `1915->1913 MiB`.
- Server-bound rollout ledger note, June 2026: sparse sequence rehearsal is only useful if it rehearses the same MARULHO-owned memory path that produced the rollout. MARULHO therefore carries `server_transition_memory_hash`, `server_transition_memory_hash_match`, and the runtime-facade state source from rollout candidate into replay evaluation, durable rollout ledger material, and rehearsal-policy eligibility; missing or tampered server binding blocks recording or removes the event from the rehearsal path.
- Sparse rollout candidate-edge note, June 2026: NeuronSpark, SpikingSSMs, and SpikeGPT make sparse/event language trajectories plausible, while STDP/replay-consolidation work implies later synaptic proposals must be local edges derived from observed adjacent spike states. MARULHO therefore derives rollout consolidation candidates from rehearsed sparse active-index transitions, preserving rollout step indices only as provenance and using source/target neuron coordinates for shadow deltas, developmental growth review, and regeneration evidence.
- Replay-bound sparse-edge provenance note, June 2026: structural plasticity and STDP-driven rewiring evidence treat growth as the consolidation of local activity, not a generic topology edit. MARULHO therefore requires rollout developmental growth candidates and rollout regeneration designs to carry source/target rollout step indices plus source/target active-index hashes; missing trajectory provenance now blocks operator review before permit or checkpoint-backed regeneration can occur.
- Applied regeneration provenance note, June 2026: circuit reactivation, replay consolidation, and STDP-driven rewiring imply the final structural mutation ledger must explain which replay-bound local edge was consolidated. MARULHO therefore records `local_edge_provenance` on applied regenerated synapses and in `synapse_provenance_by_key`, preserving source synapse identity, rollout step indices, and active-index hashes beside replay permit/artifact provenance.
- Synapse provenance audit local-edge note, June 2026: replay and structural-plasticity visibility must survive checkpoint restore, not only the moment of mutation. MARULHO therefore extends `snn_language_readout_synapse_provenance_audit.v1` so replay-regenerated synapses are audit-ready only when their stored local-edge provenance is complete and source/target rollout step order remains valid.
- Runtime Truth applied-provenance note, June 2026: runtime monitoring for structural plasticity should reveal whether applied sparse growth is explainable without turning status into an audit or execution command. MARULHO therefore adds `snn_readout_applied_synapse_provenance` to Runtime Truth as a compact non-executing summary of sparse weights, provenance rows, replay-regenerated rows, complete/missing local-edge provenance, invalid rollout order, orphan weights, and dangling provenance.
- Self-repair evaluation Runtime Truth note, June 2026: adaptive pruning/regeneration, homeostatic structural plasticity, and sparse GPU SNN work imply repair pressure must be visible before autonomous growth/pruning can be trusted, but status must not become the repair evaluator. MARULHO therefore adds `self_repair_evaluation_gate` to Runtime Truth as a compact non-executing summary of isolated replay/deep-sleep readiness, success-evidence terms, Runtime Truth delta, rollback/operator requirements, and device-evidence requirements while keeping repair cases and execution payloads behind the dedicated artifact.
- Sleep-plasticity review-scheduler cycle-autonomy note, June 2026: sleep timing and replay prioritization are related but separate control problems; a due cadence may nominate a reviewed gate for inspection without selecting memories or authorizing synaptic change. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/cycle-autonomy-proposal`, a deterministic non-executing candidate that remains blocked from timer registration, workers, endpoint calls, replay, checkpoints, plasticity, and runtime mutation.
- Due-cycle bounded replay-selection note, June 2026: hippocampal replay research indicates that consolidation is capacity-limited and biased toward salient, novel, repeated, weakly learned, or prediction-error-rich experiences, while the sleep window and the selected memory remain distinct control decisions. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/due-cycle-bounded-replay-selection-proposal`, which combines due passive cadence evidence with the internal current-revision consolidation-priority queue to nominate bounded replay-evaluation contexts for operator inspection only. It does not select replay-window content, record artifacts, run replay, write checkpoints, call endpoints, or apply plasticity.
- Due-cycle replay-selection visibility note, June 2026: cadence and salience evidence must be visible before a future autonomous consolidation policy can be evaluated. MARULHO therefore attaches the non-executing Due-Cycle Bounded Replay-Selection Proposal to Living Loop, Policy Actuator, Terminus, Status, and Runtime Truth while preserving the same blocked endpoint, replay, artifact-recording, checkpoint, plasticity, and structural-write boundaries.
- Due-cycle replay artifact-recording review-proposal note, June 2026: replay research and synaptic-tagging work suggest that consolidation timing, memory nomination, local replay-window construction, and durable recording consent should remain separate gates. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/due-cycle-replay-artifact-recording-review-proposal`, a deterministic read-only adapter that binds one due-cycle nomination to the existing artifact-recording policy recommendation only when both select the same current verified context. It creates no ticket, replay window, artifact, replay execution, checkpoint, or plasticity update.
- Due-cycle replay artifact-recording review-ticket note, June 2026: durable review consent should preserve the evidence chain that caused a replay context to be nominated without creating a second artifact recorder. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-sleep-policy/review-scheduler/due-cycle-replay-artifact-recording-review-ticket`, an operator-confirmed route that delegates to the existing Replay Artifact Recording Review Ticket recorder and binds the due-cycle adapter hash plus source selection hash. The existing internal-ledger replay-window proposal and evaluated artifact recorder remain the only artifact-recording path.
- Transition-memory regeneration note, May 2026: adaptive sparse SNN development with pruning/regeneration and GPU sparse structural-plasticity work imply pruning is incomplete without bounded repair of over-pruned topology. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-regeneration-proposal` and `/terminus/snn-language-sequence/plasticity-regeneration`: a read-only local-regrowth proposal followed by a checkpoint-backed operator-confirmed command with canonical 64-neuron indices, fixed sparse budgets, row-mass and fan-out ceilings, and a checkpoint-persistable runtime ledger.
- SNN transition-memory replay-context note, June 2026: replay prioritization by prediction error, expected utility, and vulnerable memory implies the evidence selecting replay must be owned before it can guide structural change. MARULHO therefore records a Replay Controller-owned `snn_replay_evaluation_context.v1` from source prediction/observation inputs: the service recomputes sequence mismatch, derives plasticity pressure, binds both hashes to the current Runtime State revision, and rejects stale or tampered contexts. Read-only `snn_transition_memory_replay_artifact_proposal.v1` windows are then derived from ranked internal Readout Evidence Ledger entries plus that verified context, preserving per-label grounding and rejecting partially grounded candidates. Operator-confirmed evaluated proposals become checkpoint-persisted `snn_transition_memory_replay_artifact.v1` records tied to the context, operator identity, and Runtime State revision. Regeneration permits reject fabricated IDs, raw caller-window artifacts, and caller-carried mismatch or pressure reports. Automatic artifact recording remains blocked until an explicit autonomy policy is defined.
- Raw replay-artifact recorder retirement note, June 2026: complementary-learning and replay-consolidation evidence require durable structural-write consent to pass through selected, server-held replay windows rather than caller-supplied replay payloads. MARULHO therefore removes the public raw `record_snn_transition_memory_replay_artifact(...)` path: evaluated artifacts are recorded only through `record_evaluated_snn_transition_memory_replay_artifact(...)`, raw caller-window artifacts are dropped during Replay Controller load, and artifact verification recomputes the known-readout, replay-priority, and provenance source-window hashes before a permit can be issued. The updated benchmark `reports/bounded_replay_window_20260620/snn-replay-artifact-raw-recorder-retired.json` passed with `public_raw_recorder_callable=false`, `raw_loaded_artifact_count=0`, `raw_artifact_index_hit=false`, `4` bounded provenance index lookups instead of `256` retained-record checks (`64x`), mean verification latency `0.538460 ms`, traced Python peak `0.017773 MiB`, and `0.0 MiB` CUDA allocation/reservation. The `524288`-token protection run `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-raw-replay-artifact-recorder-retired.json` stayed in the maintained band at `6004.719 tokens/sec`, `tick_duration_ms.p95=21.825`, `train_compute=0.135314 ms/token`, `prepare_training=0.007010 ms/token`, `finalize_total=0.006575 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, and zero graph/native sequence failures; velocity observed GPU contention, so this is throughput protection rather than a clean speed ceiling.
- Bounded generic replay-context observed-slot note, June 2026: modern-Hopfield/CLS/continual-replay/STC evidence supports local recall only after a selected evidence window exists, so the generic `snn_replay_evaluation_context(...)` facade must not be a second caller-sized path into Replay Controller context recording. MARULHO now routes `observed_readout_slots` through the shared `SNN_READOUT_REPLAY_TARGET_WINDOW_LIMIT=32` readout replay source-window operator, requires a bounded, untruncated, well-formed observed-slot window before mismatch, pressure, or context recording, and stores the source-window report in the recorded context metadata. The benchmark `reports/bounded_replay_window_20260619/snn-replay-evaluation-context-window.json` passed with an exact `32/32` context, oversized observed slots blocked at `32/2048`, blocked payloads making no mismatch/pressure/Replay Controller calls, `64x` projected source-work reduction, `0.656714 MiB` traced Python peak, and `0.0 MiB` CUDA allocation/reservation. The `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-snn-replay-evaluation-context-window.json` stayed in the maintained band at `6009.932 tokens/sec`, `train_compute=0.135671 ms/token`, bounded `12/65536` route rows, `65526` cached rows, GPU memory `2031->2045 MiB`, and zero graph/native sequence failures, with sampled GPU contention at `22%`. The former generic full-payload observed-slot bridge is retired as an implementation shape rather than preserved as a side path.
- SNN replay-consolidation priority note, June 2026: hippocampal replay work points to prioritization by expected utility, prediction error, recent relevance, and weak/grounded memory support, while SNN sleep/homeostasis work warns against hidden plasticity without a separate safety gate. MARULHO therefore adds read-only `snn_replay_consolidation_priority_queue.v1`: it ranks only current, verified Replay Controller contexts using bounded mismatch surprise, plasticity pressure, recency, and internal readout-priority support. The queue is advisory, deterministic, non-mutating, non-generative, and ineligible for artifact recording or structural writes by itself; it only chooses which context should be reviewed next.
- Bounded SNN replay-priority source note, June 2026: prioritized replay and synaptic tagging support ranking salient error/pressure traces, but not verifying every retained replay context each time a due-cycle review asks for a queue. MARULHO therefore retires the retained-queue scan shape inside `snn_replay_consolidation_priority_queue.v1`: the controller keeps a replay-evaluation-context ID index, verifies a `16`-context recent source window plus up to `16` explicit readout-target context IDs, and reports `bounded_snn_replay_priority_source_window.v1` with CPU placement, no global scan, no raw replay text, no language reasoning, `runs_live_tick=false`, and `gpu_used=false`. Runtime Truth now carries the due-cycle queue source-window policy/counts instead of only the final nominated candidate count. The benchmark `reports/bounded_replay_window_20260618/snn-replay-priority-source-window.json` selected an old high-signal readout-targeted context outside the recent window, verified `17` contexts instead of all `64` retained contexts (`3.764706x` less verification work), averaged `1.825268 ms`, used `0.050346 MiB` traced peak Python allocation, and allocated `0.0 MiB` CUDA memory. The paired 65536-column `524288`-token hot-path run stayed in band at `6298.310 tokens/sec`, `train_compute=0.129349 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, GPU memory `1799->1800 MiB`, no observed contention, and zero graph/native/sequence failures.
- Bounded SNN readout replay-priority source note, June 2026: modern Hopfield-style association and CLS/replay evidence support local retrieval over a selected trace set, not scoring every retained language-readout event before a downstream replay queue sees it. MARULHO therefore retires the retained-readout scorer inside `snn_language_readout_replay_priority.v1`: it now reads a CPU-resident `32`-event recent source window, scores only provenance, label repetition, transition reuse, and recency inside that source, and reports `bounded_snn_readout_replay_priority_source_window.v1` with no global candidate/score scan, no raw text payload, no language reasoning, `runs_live_tick=false`, `runs_every_token=false`, and `gpu_used=false`. The benchmark `reports/bounded_replay_window_20260618/snn-readout-replay-priority-source-window.json` matched the diagnostic full-retained scorer's top high-signal readout, scored `32` of `2048` retained events (`64x` less scoring work), averaged `1.424948 ms` versus `51.002932 ms` (`35.792837x`), used `0.065639 MiB` traced peak Python allocation, and allocated `0.0 MiB` CUDA memory on RTX 3060. The paired `524288`-token hot-path run stayed in the maintained band at `6284.379 tokens/sec`, `train_compute=0.129905 ms/token`, `prepare_training=0.006326 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, GPU memory `1852->1858 MiB`, no observed contention, and zero graph/native/sequence failures.
- SNN readout replay-priority baseline-removal note, June 2026: the retained-readout scorer is no longer kept as benchmark-local executable code. The external maintained-only report `..\..\MARULHO_reports\bounded_replay_window_20260623\snn-readout-replay-priority-legacy-baseline-removed.json` passed by selecting the seeded recent high-signal readout from a `32/2048` CPU source window, returning `8` candidates, reporting `retired_full_retained_priority_absence.implementation_present=false` and `diagnostic_callable=false`, averaging `0.911452 ms`, and using no CUDA archival allocation.
- Indexed replay-artifact provenance note, June 2026: replay artifacts and regeneration permits are slow control-plane records, but future LLM-size memory still rejects retained-history scans as the proof mechanism. MARULHO therefore replaces linear ID lookups across replay contexts, artifact-review tickets, transition-memory replay artifacts, scheduler review tickets, sleep tickets, and regeneration permits with controller-owned indexes, and binds new evaluated artifacts/permits to `bounded_snn_replay_artifact_provenance_source_window.v1`. The window caps provenance to context, ticket, artifact, and permit IDs, reports CPU archival placement, no global candidate/score scan, no raw replay text, no hidden language reasoning, `runs_live_tick=false`, and `gpu_used=false`. The benchmark `reports/bounded_replay_window_20260618/snn-replay-artifact-provenance-source-window.json` kept the oldest retained context/ticket/artifact/permit chain verifiable at the retention tail, used `4` indexed lookups instead of the old worst-case `256` retained-record checks (`64x` less lookup work), averaged `0.348376 ms`, used `0.012636 MiB` traced peak Python allocation, and allocated `0.0 MiB` CUDA memory. The accepted 65536-column `524288`-token hot-path rerun stayed in band at `6286.248 tokens/sec`, `train_compute=0.129585 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, GPU memory `1817->1799 MiB`, no observed contention, and zero graph/native/sequence failures after one same-code run at `5849.047 tokens/sec` was rejected as below the maintained band. This keeps replay-backed structural-write consent selected and auditable without making the artifact path a memory scan or live replay loop.
- Runtime global-scan hook retirement note, June 2026: Hopfield-style associative recall is useful only as a local operator over an already selected column or replay window, and CLS/continual-replay evidence does not justify runtime escape hatches that can rescan the whole archive. MARULHO therefore removes the remaining `allow_global_diagnostic` and `allow_global_score_scan` runtime hooks from awake-ripple tagging, replay-window selection, and SFA sampling. Missing scope now reports `awake_bucket_scope_required`, `bucket_index_scope_required`, or `selected_replay_window_required` with no mutation, no global scan, no language reasoning, and CPU archival placement. Retired full-scan comparisons live only inside benchmark-local baseline code. The direct reports `reports/bounded_replay_window_20260618/awake-ripple-runtime-global-hooks-retired.json` and `reports/bounded_replay_window_20260618/sfa-runtime-global-hooks-retired.json` measured scoped awake-ripple tagging at `1.082768 ms` versus a benchmark-local retired full scan at `1.285064 ms` (`1.186832x`) and selected-window SFA purity at `1.0` versus retired full-buffer purity `0.00439453125`, with SFA latency improving from `1.740475 ms` to `0.622956 ms` (`2.793896x`). The paired `524288`-token hot-path run stayed above the maintained band at `6342.218 tokens/sec`, `train_compute=0.128534 ms/token`, `prepare_training=0.006349 ms/token`, `finalize_total=0.006160 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, GPU memory `1801->1802 MiB`, and zero graph/native/sequence failures; the velocity surface observed brief GPU-side contention (`23%` max utilization), so the claim is sustained-throughput protection rather than contention-free hardware.
- SNN replay-artifact recording policy note, June 2026: active-inference control suggests a policy should convert prediction-error/expected-utility evidence into a next review action, while replay and sleep-plasticity work still require a separate safety gate before consolidation changes state. MARULHO therefore adds read-only `snn_replay_artifact_recording_policy_proposal.v1`: it consumes the internal consolidation-priority queue, applies bounded policy thresholds, verifies the recommended context is still current, and recommends an operator artifact-recording review target. It does not record the artifact, replay live activity, mutate transition memory, generate language, promote facts/actions, or issue permits.
- Replay-backed regeneration permit note, June 2026: hippocampal replay prioritization and replay-mediated consolidation work imply that a replay payload is not itself structural-write authority. MARULHO now requires a Replay Controller-issued permit that binds high mismatch, plasticity pressure, a server-held SNN transition-memory replay artifact, normalized operator identity, operator confirmation, the current Runtime State revision, and the canonical bounded regeneration design into a content hash. The permit is checkpoint-persistable, verified again at mutation time, rejects fabricated replay artifact IDs and post-permit candidate edits, and is stale after a successful revision advance.
- Structural-write checkpoint note, May 2026: reversible developmental plasticity requires stronger evidence than a deserializable file. MARULHO now compares the serialized pre-mutation SNN language transition state and Runtime State revision against the live snapshot before applying live deltas, homeostatic maintenance, or regeneration.
- Durable structural-plasticity transaction note, June 2026: structural-plasticity turnover and reconsolidation work imply that accepted rewiring must become durable without destroying the rollback image. MARULHO now writes a distinct pre-mutation rollback checkpoint and an atomically replaced post-mutation committed checkpoint, verifies exact state/revision round trips, restores in-memory state if commit verification fails, and hydrates saved Runtime State revisions exactly on service startup.
- Published-current-checkpoint note, June 2026: crash-consistency work implies that durable images need an atomic publication point and immutable referenced objects. MARULHO Runtime Persistence now copies verified images into UUID-named managed objects and publishes a small atomically replaced current-checkpoint manifest carrying relative path, size, SHA-256, revision, and previous descriptor. Startup validates current then previous before falling back to the bootstrap checkpoint; interrupted manifest replacement leaves the prior committed pointer intact.
- Root-capture refresh note, June 2026: checkpoint/restart work treats recovery as restoration of one consistent application state, not replacement of a single root object. MARULHO now rebinds concrete trainer, encoder, metadata, checkpoint-path, and action-root captures across Brain Runtime, Interaction Pipeline, Status Read Model, and Action Executor after publication, explicit restore, and preset-driven trainer rebuilds before runtime work resumes.
- Operator-restore commit note, June 2026: rollback-recovery work and atomic-commit practice imply explicit restore must not remain a transient in-memory state. MARULHO now treats operator restore as an import-and-publish transaction: it saves an unpublished recovery image, hydrates the selected image through owner APIs, advances revision beyond live/imported histories, stages a fresh checkpoint, refreshes captures before the commit point, then atomically replaces the manifest as the final action. Publication failure recovers the previous live image and leaves the prior manifest untouched.
- Directory-durability note, June 2026: crash-consistency studies distinguish file fsync, atomic rename, and parent-directory persistence. MARULHO now best-effort fsyncs parent directories after atomic object, manifest, and trainer-checkpoint replacement when the platform exposes syncable directory handles; Windows may still rely on filesystem behavior when Python cannot open the directory for fsync.
- Sequence mismatch note, May 2026: predictive-coding SNN and temporal-memory work imply prediction error should be measured explicitly before it becomes plasticity. MARULHO therefore exposes `/terminus/snn-language-sequence/mismatch`, which compares predicted sparse indices with observed next sparse code and reports mismatch evidence without applying learning, promoting facts, or generating text.
- Plasticity-pressure note, May 2026: local Hebbian predictive-coding SNN work implies mismatch should first become an auditable pressure signal before it becomes a weight update. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-pressure`, which turns prediction mismatch into local update focus and sparse-code targets while keeping plasticity application disabled.
- Plasticity-trial note, May 2026: error-modulated and agreement-driven SNN plasticity work implies a candidate local update should be tested in isolation before any live weights change. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-trial`, which estimates pressure reduction from an ephemeral local sequence update while discarding weights and keeping runtime training disabled.
- Plasticity-replay evaluation note, May 2026: replay and continual-learning SNN work imply simulated plasticity must pass an isolated replay gate before any live adaptation. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-replay-evaluation`, which checks replay-window evidence, expected pressure non-worsening, Runtime Truth delta, and rollback policy while keeping plasticity application disabled.
- Plasticity-replay experiment note, May 2026: sequence-replay and continual-learning SNN work imply replay should be measured as sparse-code rehearsal before it becomes live adaptation. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-replay-experiment`, which estimates grounded replay coverage and pressure stability while discarding traces and keeping runtime weight updates disabled.
- Plasticity-application design note, May 2026: self-normalizing STDP, gated multi-timescale plasticity, GPU-accelerated sparse SNN work, and continual SNN stability work imply live plasticity needs explicit local bounds plus device evidence before any update is allowed. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-application-design`, which specifies learning-rate, weight-delta, locality, normalization, device placement, Runtime Truth, and rollback constraints while keeping live application disabled.
- Plasticity-shadow delta note, May 2026: local Hebbian, STDP, and agreement-driven SNN learning rules imply shadow verification should use measured pre/post sparse-index support rather than operator-authored delta numbers. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-shadow-delta`, a read-only shadow-delta builder that reports affected synapses, max bounded delta, locality, pressure change, and tensor device without applying weights.
- Plasticity-shadow application note, May 2026: online SNN plasticity work shows that stability depends on bounded local deltas, normalization, device-aware verification, and rollback-safe checks before live adaptation. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-shadow-application`, which checks a proposed shadow delta against design bounds, pressure non-worsening, device evidence, Runtime Truth, and rollback evidence while keeping runtime weights unchanged.
- Plasticity-live readiness note, May 2026: online SNN plasticity and runtime-assurance work imply a live update must remain blocked until measured shadow evidence is paired with checkpoint/restore rollback readiness and explicit operator approval. MARULHO therefore exposes `/terminus/snn-language-sequence/plasticity-live-application-readiness`, a read-only gate that keeps live application disabled.
- Plasticity-live executor note, May 2026: self-normalizing STDP and astrocyte-gated continual-learning work reinforce that live SNN updates need a command boundary, not a status read side effect. MARULHO therefore adds `/terminus/snn-language-sequence/plasticity-live-application`, which revalidates readiness, saves a real checkpoint, and applies only bounded sparse local transition-weight deltas to MARULHO-owned state.
- Plasticity-path evidence note, May 2026: operational-manifold and runtime-monitoring SNN work imply operators need a compact status summary of the live-learning path without turning the summary into an execution command. MARULHO therefore includes `snn_language_plasticity_path` in Runtime Truth evidence, naming the current gates, rollback readiness, and fail-closed invariants while detailed artifacts remain the review surfaces.
- Long-test readout vocabulary note, May 2026: SNN language and runtime-assurance work imply long-run reports should measure active Subcortex readouts and control evidence, not ThoughtLoop-era thought lifecycle, narrative-self, global-workspace, or dream-verification fields. MARULHO long-test reports therefore use readout lifecycle, Subcortex workspace, and Language Surface sections.
- Sensory spike device note, May 2026: GPU SNN simulation and event-based sensory work imply sensory CUDA evidence must include emitted spike tensor placement, not only encoder configuration. MARULHO sensory encoders therefore report the last visual/audio spike device and shape beside persistent trace/filterbank/reference tensor devices.
- Multimodal loader device note, May 2026: multimodal SNN and spike-language work makes CPU-only sensory staging a weak substrate for language/readout growth. MARULHO now resolves standalone sensory streams and file-backed multimodal tensors to `MARULHO_DEVICE` or CUDA-when-available before encoding, while CPU remains the deterministic test/runtime fallback when explicitly selected.
- Cortex package deletion note, May 2026: the remaining Cortex modules had no production consumers after language packets moved to semantics. Deleting the whole package prevents old drive, memory, prompt, narrative, and ThoughtLoop abstractions from re-forming around a static LLM-style center.
- Homeostatic plasticity and adaptive pruning/regeneration work plus Subcortex Spike Health imply the first self-repair surface should be advisory: convert silence, saturation, stale routing, and over-correlation into review candidates, then require a Self-Repair Promotion Gate before any deep-sleep repair, replay repair, revive, prune, or structural mutation occurs.
- Sleep/replay consolidation work plus the current Replay Plan contract implies self-repair pressure should have its own gate artifact. Runtime Truth can summarize the gate status, but the artifact can point to deep-sleep/replay review without reusing replay execution candidate fields until an experiment proves the repair is safe to promote.
- Adaptive repair, homeostatic plasticity, and sparse structural-plasticity work imply a second non-executing gate: a Self-Repair Evaluation Artifact should name baseline spike-health metrics, isolated replay/deep-sleep evidence, Runtime Truth delta, rollback policy, and device evidence before revive/prune/grow promotion is allowed.
- Self-repair hardening note, May 2026: spike-health risk without a sufficient correlation window is repair pressure, not replay-review readiness. Self-repair candidates now require observed correlation evidence before the replay/deep-sleep repair gate can become ready.
- Spark modular SNN work implies module boundaries should be real ownership boundaries, not import shims, manager compatibility wrappers, or mixin-shaped active modules. MARULHO therefore deletes the `living_loop.py` aggregator, the manager's interaction-mixin delegate wrappers, and the `InteractionRuntimeMixin` module once direct owning modules can be wired.
- Open-world SNN action-law learning implies action evidence should be an owned ledger/control surface, not a compatibility mixin. MARULHO therefore keeps digital action execution, feedback, and audited action reuse in ActionExecutor and deletes the duplicate `ActionRuntimeMixin` path plus the stale action-assist mixin module.
- Modular SNN autonomy work implies action modules should be explicit graph nodes, not behavior leaked through a composition root. MARULHO therefore wires delayed-consequence and interaction callbacks directly to ActionExecutor and deletes manager-private action delegate wrappers.
- Event-driven modular SNN systems such as Spark and HiAER-Spike favor explicit graph/module routing over hidden dispatcher layers. MARULHO therefore deletes the manager's generic mixin delegate trampoline and keeps remaining compatibility callbacks explicit until their owning modules are renamed or extracted.
- Multimodal SNN grounding and spike-based language evidence imply grounding probes are observability tools for active Subcortex state, not mixin behavior on the composition root. MARULHO therefore renames the reporting helper to `ServiceReporter` without keeping a `ServiceReportingMixin` alias.
- Runtime-assurance, operational-manifold, and spiking replay work imply runtime traces and replay previews are evidence/readout artifacts for the active Subcortex path, not inherited manager behavior. MARULHO therefore renames `RuntimeEvidenceMixin` to `RuntimeEvidenceReporter` without preserving a mixin alias.
- Spiking replay, hippocampal replay timing, and continual-learning replay evidence imply replay dataset packaging is an operator-approved evidence export gate, not a manager mixin. MARULHO therefore renames `ReplayDatasetBundleMixin` to `ReplayDatasetPackager` without preserving a mixin alias.
- Local feedback/plasticity work in SNNs implies operator correction should be a single explicit plasticity boundary, not a compatibility mixin. MARULHO therefore keeps runtime/action correction in `FeedbackApplier` and deletes the stale `RuntimeFeedbackMixin` module after moving its remaining behavior assertions to the owner tests.
- Backpropagation-free/local-learning SNN work implies feedback code should stay close to the module that applies the local update. MARULHO therefore removes the Service Manager's duplicate runtime-feedback normalization/application body and leaves it as a temporary delegation point into `FeedbackApplier` while evidence/reporting callbacks are unwound.
- Modular/local-learning SNN evidence plus the Path Retirement Gate implies compatibility code should not be treated as safety. In MARULHO, a migration path earns deletion as soon as the active owner is testable; absence guards are the record of retirement, not dormant imports.
- Pure/modular SNN language work such as NeuronSpark, SymbolicLight, and Spark strengthens the same rule at the composition boundary: manager-level constant re-exports and "compatibility facade" wording keep static old paths attractive. MARULHO therefore removes stale manager constants and makes runners import limits from their owning runtime-evidence/packaging modules.
- Modular SNN routing work also argues against snapshot/restore aliases on a central manager. MARULHO therefore deletes no-caller manager wrappers for interaction snapshots, runtime restore, delayed-consequence restore, and runtime-episode trace mutation; callers should use the owner module or RuntimeFacade boundary that actually owns the state.
- HiAER-Spike, UniSpike, and stream-dataflow SNN accelerator work all treat routing and stream construction as first-class runtime-system concerns. MARULHO therefore moves text/sensory stream builders and remote tuning patches to `RuntimeSources`, `BrainRuntime`, `RuntimePrewarmer`, and `SensoryRuntimeCore` instead of preserving Service Manager stream-builder compatibility hooks.
- Event-driven autonomous SNN systems imply episode traces and query-gap state are part of the active interaction substrate, not manager-owned convenience records. MARULHO therefore removes Service Manager store wrappers for Interaction Pipeline and Runtime Persistence; evidence readers and feedback appliers use the owner stores directly, while operators use RuntimeFacade.
- Modular SNN autonomy and continual-learning work imply feedback/routing callbacks should be explicit edges in the living graph. MARULHO therefore wires delayed-consequence, source-focus, runtime-evidence, and autonomy-calibration callbacks directly to their owner modules instead of preserving Service Manager owner-callback wrappers.
- Runtime-assurance vocabulary must name the active control surface. A normal Terminus stop-timeout warning is not retired-runtime evidence, so the logger namespace now uses active Terminus runtime naming.
- Event-driven neuromorphic platforms such as HiAER-Spike and multi-core neuromorphic training systems emphasize runtime routing and buffer movement as active control-plane behavior. MARULHO therefore renames `RuntimePrewarmMixin` to `RuntimePrewarmer`: queue warming remains active Terminus runtime work, but no longer carries a mixin identity.
- Open-world SNN action-law learning and modular SNN autonomy imply source selection, focus planning, and provider curriculum are active control-plane functions. MARULHO therefore renames `TerminusAutonomyMixin` to `TerminusAutonomyCore` while `AutonomyPlanner` remains the explicit dependency seam.
- Event-based multimodal SNN and neuromorphic sensing work imply sensory previews are read-only evidence artifacts over active spike/sensory state. MARULHO therefore deletes the stale `SensoryPreviewMixin` module and keeps preview projection under `StatusReadModel`.
- Multisensory neuromorphic systems emphasize event-driven fusion, modality-specific routing, and direct sensor-to-spike processing. MARULHO therefore renames `SensoryRuntimeMixin` to `SensoryRuntimeCore` so multimodal selection, prefetch, and injection read as active Subcortex runtime behavior rather than legacy composition machinery.
- Operational-manifold SNN work treats internal spike/correlation state as deployable health evidence. MARULHO therefore renames `StatusRuntimeMixin` to `RuntimeStatusCore` so runtime status reads as an observability core for active Subcortex health, not a manager mixin.
- Runtime self-model evidence is an active liveness surface, not a manager mixin. MARULHO therefore renames `LivingStatusMixin` to `LivingStatusCore` while keeping StatusReadModel as the public read boundary for living-loop, policy, and Cognitive Signal snapshots.
- GPU sparse-SNN work plus Terminus Runtime Truth implies CUDA claims should favor sparse state transitions and topology updates over dense "everything on GPU" rewrites; efficient living-brain behavior needs event sparsity preserved in the evidence report.
- Operational-manifold work plus Runtime Truth implies liveness should include internal stability evidence, not only endpoint availability: a running Subcortex can still be unhealthy if spike activity is silent, saturated, or over-correlated.
- Operational-manifold work plus MARULHO's current competitive-column state implies a staged stability metric: expose silent/saturated/stale routing evidence from live EMAs and spike fractions, then add bounded windowed correlation evidence from recent winner vectors. This still avoids over-claiming full manifold health from scalar endpoint/status signals.
- Confidence-weighted predictive-coding work plus the retired Cortex decision implies the **Cognitive Signal** should be the active control packet for future deliberation: prediction error, confidence, neuromodulator mirrors, source, and concept candidates should be observable without requiring a ThoughtLoop consumer.
- Open-world SNN concept/action-law learning plus MARULHO's retired Cortex decision implies naming matters in code: `cognitive_signal` is the runtime contract, while `cortex_signal` aliases are deleted so retired internals cannot leak back into active service code.
- Confidence/second-order-error circuit work plus active-inference precision control imply Cognitive Signal deserves its own Subcortex-owned state primitive. Keeping the state class inside `ThoughtLoop` misplaces the control packet under the retired LLM path, while a semantics-owned primitive can feed policy, language surfaces, and future SNN deliberation without reviving Cortex.
- Operational-manifold SNN work plus self-evolving-agent memory work imply runtime metrics should measure grounded output quality, latency, topic diversity, and SNN alignment as observability evidence, but not as liveness proof. Moving Brain Runtime Metrics out of `ThoughtLoop` lets those counters support future Subcortex language/readout modules without preserving the retired LLM loop as their owner.
- Active-inference cognitive-control work plus Cognitive Signal implies a clean first Subcortex Deliberation surface: convert prediction error, confidence, neuromodulator pressure, and concept focus into ranked control hypotheses, then require replay/policy/operator evidence before promotion.
- Intermittent-planning active inference plus MARULHO Policy Actuator implies control candidates should be attached to advisory policy status as non-executable context. That lets operators and replay gates see why the Subcortex wants to replan without allowing a control candidate to become an action.
- Active-inference exploration work plus open-world SNN concept/action-law learning implies exploration targets should be first-class control evidence: target, reason, source, score, and timestamp. Moving Active Exploration State out of `ThoughtLoop` and into ThalamicGate's active state lets curiosity and source-focus modules share uncertainty-reduction targets without making a retired text loop the owner of exploration.
- Closed-loop SNN control work plus active-inference feedback framing implies language-facing results should be treated as controller observations, not as Cortex ownership. `emit_deliberation_feedback` keeps topic boosts, grounding candidates, confidence, and valence on the Subcortex control side, while the old Cortex feedback path has been deleted.
- SNN autonomous-agent and open-world concept/action-law work imply action evidence should stay in Subcortex-owned ledgers. Verified digital actions can train calibration and replay/evaluation surfaces, but they should not boot a retired ThoughtLoop merely to mirror action memory.
- Multimodal/event-based SNN grounding work plus open-world SNN agents imply source and sensory observations should be runtime-owned evidence packets. MARULHO should derive focus from query gaps, autonomy, curiosity, concept state, and source metadata instead of a retired ThoughtLoop exploration target.
- Spike-based multimodal language and temporal-grounding work imply language readouts need explicit support diagnostics: target terms, evidence coverage, alignment, and recovery state. Moving Grounding Diagnostics out of `ThoughtLoop` makes those checks reusable by future SNN-native language/readout modules while keeping fluent text subordinate to observed evidence.
- Replay-gated adaptation work plus advisory control candidates implies every candidate needs an explicit promotion gate. Readiness for replay review is not readiness for action or fact promotion; those remain separate gates with operator/evidence requirements.
- Synaptic tagging, neuromodulatory replay shaping, and pure-SNN language work such as NeuronSpark imply consolidation should carry a local tag before durable replay evidence is recorded. MARULHO therefore records a server-held SNN Replay Artifact Recording Review Ticket from the current replay policy proposal and requires that ticket for evaluated replay-artifact recording, instead of trusting caller-carried policy payloads or external reference implementations.
- Synaptic tagging-and-capture work, neuromodulated memory-trace replay, and SNN continual-learning replay all point to consolidation as a selected, traceable synapse event rather than a free replay blob. MARULHO now carries internal readout evidence hashes from evaluated replay artifacts into regeneration permits, regeneration proposals, regenerated synapse provenance, and audit rows so later sparse growth can be explained as local SNN memory consolidation.

### Developmental pruning and autonomous maintenance, June 2026

- Ge et al. found a finite period of enhanced plasticity in adult-born neurons, supporting explicit developmental age and terminal maturation rather than indefinite newborn learning: https://pubmed.ncbi.nlm.nih.gov/17521569/
- Kelsch et al. found activity-dependent synaptic development during adult neurogenesis, supporting survival decisions from observed activity rather than elapsed runtime alone: https://authors.library.caltech.edu/90088
- Deger, Seeholzer, and Gerstner combined spike-timing and structural plasticity and showed that cooperative active contacts stabilize while structural turnover remains explicit: https://academic.oup.com/cercor/article/28/4/1396/4780821
- Fauth et al. modeled spike-timing-dependent structural plasticity in which maturation and pruning rates depend on active synaptic contacts, supporting a separate structural-removal transaction after local learning: https://pmc.ncbi.nlm.nih.gov/articles/PMC3447982/
- MARULHO therefore treats `prune_eligible` as evidence, not deletion. A separate checkpointed Newborn Synapse Pruning transaction removes sparse and dense state together and retains a provenance tombstone. Open critical periods do not advance from aggregate column counters; they wait for exact pre/post spike timings bound to the live synapse.

### Dynamic SNN population capacity, June 2026

- Pan et al. evolve recurrent SNN structure together with biologically plausible plasticity, supporting the requirement that changed topology must participate in the network dynamics rather than exist only as metadata: https://www.nature.com/articles/s41598-023-43488-x
- Wang et al. combine recurrent sequence learning with explicit structural growth and pruning, supporting one shared topology contract across learning and inference: https://www.frontiersin.org/journals/neuroscience/articles/10.3389/fnins.2023.1224752/full
- NG-SNN uses contribution-guided selective integration to grow a compact spiking classifier on demand. MARULHO uses the same general constraint that new units must enter functional computation, while retaining local critical-period learning instead of adopting its classifier-specific analytical training: https://doi.org/10.1016/j.neunet.2026.108656
- MARULHO found that persistent language capacity could grow beyond 64 while sequence and rollout functions still folded indices into fixed 64-neuron tensors. The active language prediction, mismatch, isolated trainer, rollout, replay, regeneration, and neuron-adapter paths now carry the runtime language-neuron count and preserve new indices without modulo aliasing.

### CUDA scalar synchronization and skipped work, June 2026

- PyTorch's performance guidance identifies `cuda_tensor.item()` and Python control flow that depends on CUDA results as host-device synchronization points: https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html
- MARULHO tested replacing zero-state scalar branches in column fallback, context prediction, and binding modulation with unconditional tensor computation. Although this removed synchronization points, two uncontended service runs made hot-path p95 and total latency worse.
- MARULHO also tested sharing exact routing-index distances with reconstruction error. An exact pending-update overlay preserved numerical parity but made the complete 1024-column CUDA routing pipeline slower, so index reuse is not promoted merely because it removes a dense-looking operation.
- MARULHO also found that routine competitive-column processing scanned `steps_since_win` every tick and could revive dead columns directly from the hot path. Homeostatic plasticity and structural-pruning evidence support treating that as maintenance, not routing. The hot path now leaves stale columns as spike-health evidence; explicit deep-sleep or maintenance paths own revival.
- The implementation direction is therefore narrower: retain cheap zero-state short-circuits, eliminate mathematically dead branches such as zero-weight input drive, move structural mutation out of routine ticks, and require endpoint-level evidence before promoting fusion, cadence, or event-driven alternatives.
- Cadenced CUDA graph host truth mirrors extend that boundary: the persistent graph owns neuromodulator and competitive-surprise updates on device every replay, while Python mirrors scalar packets only on a configured cadence. This preserves exact per-token mirror mode for parity/evaluation (`interval=1`) but makes the production path treat Runtime Truth scalars as sampled evidence, not a reason to synchronize CUDA every token.

### Event-driven binding wake, June 2026

- PyTorch profiler guidance supports measuring CPU launch and synchronization cost before assuming a CUDA kernel is the bottleneck: https://docs.pytorch.org/docs/stable/profiler.html
- Thousand Brains Project learning modules remain independent local models that communicate through bounded messages and voting rather than requiring every module to update continuously: https://docs.thousandbrains.org/docs/learning-modules
- Sparse Mixture-of-Experts routing provides an engineering analogue for bounded specialist activation, while Expert Choice routing emphasizes explicit capacity constraints rather than unconstrained expert fan-out: https://arxiv.org/abs/2202.09368
- A representative 1024-column checkpoint showed inactive Hypercube Binding with zero usage and zero output while still paying routine launch cost. An isolated profiler trace measured 70 CUDA kernels and 136 ATen operations for one idle probe, versus zero CUDA kernels and zero ATen operations at the binding boundary for one cached idle skip.
- MARULHO therefore promotes Conditional Binding Wake: inactive binding probes every four tokens and keeps cached neural state between probes; learned or checkpoint-restored binding wakes immediately and runs every tick. The policy is reversible through configuration and does not move topology mutation, replay, or checkpoint work into the hot path.
- A synchronized RTX 3060 checkpoint A/B over 120 samples per arm measured median train-step latency from `32.2069` to `29.5535 ms`, p95 from `46.3573` to `42.7967 ms`, and mean from `32.6817` to `31.0250 ms`. Both arms retained zero binding usage/state and identical allocated/reserved VRAM, so this is evidence for skipped inactive work, not improved binding cognition.
- The same conditional-specialist rule now applies to text-only Cross-Modal Grounding. Text without accepted visual/audio evidence is a dormant grounding specialist, so MARULHO runs periodic text probes and otherwise records cached-idle state while still decaying traces. Accepted sensory evidence wakes text updates immediately. On the live 1024-column RTX 3060 checkpoint, interval-4 text-only wake improved median train-step latency from `57.59725` to `51.5753 ms` and mean from `59.4546` to `54.8716 ms`; p95 worsened from `82.0887` to `83.8702 ms`, so this is a median/mean metabolism improvement, not tail-latency proof. A later fused-runtime RTX 3060 scan promoted interval 16 as the default text-only cadence: interval 4 measured conditional median/mean/p95 `21.48485/21.542455/37.3566 ms`, while interval 16 measured `16.91405/18.49991/32.0321 ms`. This changes wake cadence only; it is not grounding-quality proof.
- Homeostatic plasticity literature treats firing-rate stabilization as a distinct compensatory mechanism from structural growth or pruning, and continual-learning work emphasizes multi-timescale plasticity rather than one always-on update for every unit. MARULHO applies that distinction by splitting competitive-column threshold/win-rate homeostasis from the dead-column structural threshold. `candidate_homeostasis_start_tokens` lets threshold and win-rate maintenance wake only for routed candidates after a measured warmup, while stale counters, spike-health evidence, and explicit maintenance revival stay intact. On RTX 3060, forced all-column homeostasis measured `47.4682 ticks/sec` versus `64.8535 ticks/sec` for the default candidate gate with `10/1024` columns updated. Sources: [Turrigiano 2012](https://pmc.ncbi.nlm.nih.gov/articles/PMC3249629/) and [Zenke et al. 2017](https://proceedings.mlr.press/v70/zenke17a.html).

### Hot-window throughput baseline, June 2026

- PyTorch documents `torch.compile(mode="reduce-overhead")` as a CUDA-graph-backed mode for reducing Python overhead, with possible tradeoffs: https://docs.pytorch.org/docs/stable/generated/torch.compile.html
- NVIDIA's CUDA Graph guide frames CUDA Graphs as a launch-overhead reduction tool for repeated GPU work and points PyTorch users to `torch.compile`, `torch.cuda.graph`, and `make_graphed_callables`: https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/torch-integration.html
- MARULHO's fixed-shape hot-window benchmark on the revision-960 1024-column RTX 3060 checkpoint measured `24.6404 tokens/sec`, median `35.18625 ms`, p95 `76.0758 ms`, and mean `40.5594 ms` over 256 already-encoded CUDA tensors, with service/source/UI/sleep excluded. The 1000 tokens/sec target is therefore about `40.58x` above the current eager Python core loop.
- A bounded pure-tensor candidate-competition kernel over the same 1024-column checkpoint reached `212698.98 isolated tokens/sec` with `torch.compile(mode="reduce-overhead")` after installing `triton-windows` and using its bundled TinyCC compiler. The same canonical run measured eager batched execution at `79651.68 isolated tokens/sec`. This supports the implementation direction: isolate fixed-shape tensor kernels, batch awake-column work, and compile/fuse them before changing live service runtime.
- The next wider fixed-shape benchmark added input projection and candidate-local predictive-state math while still refusing runtime mutation. Its canonical `matmul_precision=high` run reached `242784.01 isolated tokens/sec` with `torch.compile` default mode versus `45118.41 isolated tokens/sec` for eager batched execution. A default-precision comparison reached `203483.21 isolated tokens/sec`, so the runner keeps precision policy explicit and measured rather than static.
- When the wider benchmark used real routing-index candidates through the old list-returning API, candidate preparation measured only `721.32 tokens/sec` because candidate ids and distances crossed through Python lists and CPU numpy. The retrieval layer then exposed `search_tensors()` for torch-backed indexes. On the same RTX 3060 checkpoint, the tensor route preparation measured `1203.25 tokens/sec` cold after checkpoint/cache work and `53931.07 tokens/sec` warm with CUDA caches ready, while the compiled projection/competition/prediction kernel reached `234286.58 isolated tokens/sec`. The list-returning routing API is now removed from active code, so routing candidates remain tensor/device-native, but full production velocity still requires fusing or graphing routing prep with the compiled observe-route-predict block.
- The live trainer now consumes tensor routing candidates for wake, replay, and offline competition paths. Two controlled 256-sample hot-window A/B pairs on the same checkpoint/seed measured tensor versus list throughput of `24.7167` versus `23.9371 tokens/sec` and `32.6721` versus `32.3280 tokens/sec`; median latency improved in both pairs, while p95 was mixed in the first pair and slightly better in the second. This is a modest roughly `2%` aggregate throughput promotion with unchanged `20.6758/50.0 MB` allocated/reserved VRAM, not the compiled-kernel speedup or a service-throughput claim.
- Sharded `torch_topk` routing still launched one search per shard plus a merge top-k. MARULHO now keeps shard ownership for updates but optionally builds one invalidation-aware merged CUDA cache for exact tensor search. On the revision-960 checkpoint, warm 256-token routing preparation improved from `37863.10` to `127066.06 tokens/sec` (`3.36x`). Two reversed-order hot-window pairs measured merged/per-shard throughput of `38.5582/37.0260` and `37.3134/36.8980 tokens/sec`; median latency improved in both pairs, while p95 remained noisy. The merged cache added `532480` bytes of device storage for 1024 vectors and ids.
- PyTorch documents `torch.compiler.cudagraph_mark_step_begin()` for iteration boundaries and warns that CUDA Graph output tensors may be overwritten on subsequent replay: https://docs.pytorch.org/docs/stable/generated/torch.compiler.cudagraph_mark_step_begin.html. MARULHO's first live promotion attempt reproduced that failure because compiled predictive outputs became next-step runtime state. That branch is now historical: the configurable `compiled` predictive dense transition and isolated predictive-transition benchmark were removed after `ColumnTransitionRuntime` and the fused in-place/graph transition became the maintained CUDA scheduler boundary.
- The fixed-shape predictive transition benchmark measured `204.35 transitions/sec` eager, `497.90/sec` with compile default, and `1121.66/sec` with `torch.compile(mode="reduce-overhead")`, with median/p95 `0.72745/1.8407 ms` for the best arm. Two full hot-window A/B pairs measured compiled/legacy throughput of `47.8081/34.5995` and `42.0725/30.1816 tokens/sec`; compiled median and p95 were better in both pairs. A later one-graph confirmation measured `38.7050 tokens/sec`, showing run-to-run variance. The evidence remains historical only; new configs and migrated checkpoints use `inplace_triton`, and failed in-place eligibility falls back to dense eager tensor semantics with Runtime Truth fallback reason.
- `1000 tokens/sec` is only a low reference floor for evaluating whether a path is worth further work, not MARULHO's goal. The goal is maximum sustainable local throughput with correctness, Runtime Truth, device evidence, and hot-path metabolism protected. Per-token eager Python cannot be the final production-velocity substrate.
- PyTorch and NVIDIA frame CUDA Graphs as repeated-launch tools over stable workloads; they do not remove the cost of allocating and copying a model's complete mutable state after every replay. MARULHO tested a functional steady-state transition spanning competition, prediction, prototype plasticity, and homeostasis. The isolated compiled arm was `4.18x` faster than eager, but two controlled full hot-window pairs averaged about `34.75` versus `36.65 tokens/sec` for compiled versus the retained runtime. The implementation is therefore rejected from the always-on path.
- This changes the implementation direction: the next steady-state substrate must mutate runtime-owned device buffers in place through a bounded Triton/custom CUDA kernel or equivalent persistent graph. Promotion requires repeated complete-`train_step` wins, state parity, finite-state checks, stable checkpoint ownership, startup cost, and VRAM evidence. Returning eleven replacement state tensors and copying them after every tick is now a documented rejected path.
- NVIDIA's CUDA Graph guidance states that repeated workflows benefit from paying launch setup once, while its PyTorch guidance requires static persistent buffers with fixed addresses and in-place updates. MARULHO therefore implemented an evaluation-only single-launch Triton transition over the persistent column and predictive buffers rather than another functional graph: https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html and https://docs.nvidia.com/dl-cuda-graph/examples/gpt-3-175b.html
- PyTorch and NVIDIA CUDA Graph guidance also supports widening graph ownership across repeated short-kernel workflows instead of returning to CPU between dependent state updates. MARULHO applies that direction narrowly: the in-place transition now writes the next normalized winner prototype into the captured exact routing cache, while the CPU index store becomes an explicit slow mirror. This removes duplicate per-token routing-index maintenance without moving retrieval algorithms into `service` or claiming that all routing backends should be graph-owned.
- On the 1024-column RTX 3060 checkpoint, eager competition plus the in-place Triton transition reached `314.5419 transitions/sec`, median/p95 `2.52885/6.8216 ms`, versus `59.8240/sec` and `16.3240/23.0268 ms` for the functional eager transition with stable writeback. State addresses remained stable, state stayed finite, reserved VRAM was `28 MB`, and first-process warmup/compilation cost `10.38 s`. This is a `5.26x` transition-cluster result, not full cognitive-tick evidence; the trainer remains on the previously promoted path pending repeated full-`train_step` A/B.
- After moving the changing spike-ring cursor into a persistent CUDA scalar, two reversed-order complete hot-window pairs also favored the in-place executor: `55.28` versus `38.02 ticks/sec`, then `105.40` versus `37.95`, with lower median and p95 latency in both pairs. However, an identical 32-input trajectory comparison retained only `46.875%` winner agreement and ended with maximum location/threshold differences of `0.5904/0.2607`. Small floating-order differences can become discrete routing differences through predictive voting. MARULHO therefore does not equate numerical kernel parity or throughput with cognitive-trajectory equivalence; promotion remains blocked on prediction, grounding, spike-health, and trajectory-quality evidence or a tighter numerical design.
- A same-checkpoint 128-tick quality A/B then measured the retained runtime at `46.5678 ticks/sec` and the evaluation-only in-place executor at `68.0983`, a `1.4623x` gain. Median/p95 latency improved from `20.8646/27.6272 ms` to `14.4042/18.3551 ms`. Despite only `31.25%` winner agreement, the faster trajectory reduced mean reconstruction error by `0.001027`, slightly reduced mean prediction error, preserved confidence, increased normalized winner entropy from `0.4345` to `0.4816`, remained finite and sparse-responsive, introduced no silence/saturation/stale risk, and did not regress bounded memory capture or fragility. This changes the blocker: exact specialist identity is not required when aggregate cognitive quality holds, but promotion still requires visual/audio grounding evidence and a production solution for cold Triton compilation/cache lifecycle. The synthetic checkpoint had cross-modal grounding disabled, so this run cannot support a grounded cognition claim.
- Triton's JIT API exposes both `do_not_specialize` and `do_not_specialize_on_alignment`, allowing changing scalar values and transient pointer alignment to share a compiled kernel when those properties do not change semantics: https://triton-lang.org/main/python-api/generated/triton.jit.html
- MARULHO's grounded runner observed two cold specializations totaling `223.93 s`. Instrumentation isolated the second event to the transient one-element `winners` tensor changing from 16-byte-divisible to unaligned at measured tick 93. Disabling alignment specialization only for `winners` reduced the workload to one warmup event and zero measured compile events.
- The stabilized 1024-column synthetic multimodal A/B reached `27.4966` in-place versus `22.9012` retained ticks/sec (`1.2007x`). Median/p95 fell from `38.3108/49.6470 ms` to `30.8081/38.4805 ms`; winners matched exactly, visual/audio acceptance and prediction metrics matched, and all cross-modal weights and confidence tensors were bit-exact. This closes the synthetic sensory-preservation and mid-run specialization blockers, but not real camera/microphone grounding or production cold-start lifecycle.
- MARULHO now promotes that transition through a trainer-owned, checkpoint-opt-in production lifecycle. A cold empty-cache compile-only warmup for the all-column and `k=10` shapes took `80.746 s`; the same startup against the populated disk cache took `0.348 s`. Compile-only warmup preserves every model tensor, unsupported configurations fall back before mutation, and a launch-time failure is fail-closed because partial in-place mutation cannot safely retry.
- Production-backed complete hot-window runs reached `80.591` and `110.360 ticks/sec` for the in-place executor versus `70.773` and `51.556 ticks/sec` for retained compiled runs in reversed/order observations. The strongest measured complete encoded hot window is therefore `110.36 ticks/sec`, about `9.06x` below the low `1000 ticks/sec` reference floor. A production-backed synthetic grounded gate also passed at `42.808` versus `38.775 ticks/sec` (`1.104x`) with exact winners, bit-exact cross-modal tensors, and zero measured compile events.
- One live source tick processed 12 tokens in `3481.45 ms`, or `3.447 tokens/sec`, while Runtime Truth recorded 12 in-place executions, zero failures, CUDA observation, and revision progress. A clean post-restart repetition processed the same 12-token delta in `8646.97 ms`, or `1.39 tokens/sec`, again with 12 in-place executions and zero failures. The production kernel is executing, but source/tick orchestration and the remaining per-token cognitive stages dominate full-runtime latency and variance. The next implementation direction is stage-level live-tick profiling followed by persistent scheduling, batching, fusion, or event-driven skips outside the already-promoted transition.

### Cadenced adaptive-context plasticity, June 2026

- Multiple-timescale plasticity work separates fast activity-dependent dynamics from slower homeostatic and consolidation processes, while local eligibility-trace approaches likewise preserve continuously evolving neural state without requiring every synaptic consequence to be materialized at every simulation step: https://www.nature.com/articles/ncomms7922 and https://proceedings.neurips.cc/paper_files/paper/2022/file/6fca3ed3c54ffeae947ae668a0841ab2-Paper-Conference.pdf
- MARULHO's adaptive context state already evolves independently from its dense Hebbian `w_in` update. A 1024-column CPU/CUDA benchmark showed that retaining continuous state updates while applying weight plasticity every four tokens preserved synthetic context-state and prediction cosine agreement at `0.999999` over 256 sparse observations, while reducing measured sequence time by about `1.71x` on CPU and `1.48x` on CUDA in the interval-4 run.
- The retained update is not multiplied by the interval: a `4x` learning-rate compensation increased weight divergence in the same check. The promoted policy therefore changes plasticity cadence, exposes state/plasticity counters, and leaves long-run learning-quality equivalence explicitly unproven.

### Hugging Face source-bank grounding, June 2026

- Hugging Face Dataset Viewer validation showed `izumi-lab/open-text-books` exposes a direct `text` field under `default/train`, so it fits the existing `StreamingCorpusLoader` contract without a parser or hot-path adapter. Offset-20 samples contained worked instructional prose about measurement precision, unit conversion, ions, and nomenclature.
- `wikimedia/structured-wikipedia` is a better future Wikimedia path than raw full-text Wikipedia because it exposes abstracts, sections, references, tables, and stable Parquet structure, but MARULHO should not promote it as the default until Runtime Sources has a section/table-aware adapter and bounded sampling policy.
- `HuggingFaceFW/fineweb-edu` remains useful as a broad educational web stream, but its curation is classifier-filtered web data and therefore complements rather than replaces textbook-grounded instructional examples.
- MARULHO therefore replaces raw `wikimedia/wikipedia` as the default Terminus source with `izumi-lab/open-text-books`, while keeping S2ORC ArXiv abstracts and FineWeb-Edu in the same explicit Source Bank. This is a source-bank decision only; it does not create a fact-promotion claim or change Subcortex cognition.

### Persistent quantum input and control staging, June 2026

- PyTorch CUDA Graph guidance requires long-lived input tensors with stable
  addresses and updates those buffers before replay:
  https://pytorch.org/blog/accelerating-pytorch-with-cuda-graphs/
- PyTorch CUDA Graph Trees and NVIDIA dynamic-pattern guidance both reinforce
  the same boundary: dynamic values may change inside persistent storage, while
  pointer identity and memory ownership must remain stable across replays:
  https://docs.pytorch.org/docs/stable/user_guide/torch_compiler/torch.compiler_cudagraph_trees.html
  and
  https://docs.nvidia.com/dl-cuda-graph/latest/torch-cuda-graph/handling-dynamic-patterns.html
- NVIDIA's CUDA Graph programming guide supports updating repeated workflows
  without returning launch control to the host between every dependent
  operation:
  https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html
- MARULHO applies that direction as a bounded 128-row input ring owned by
  training. Brain Runtime offers sequential sub-batches, training stages them
  once per quantum, and the captured graph advances its input slot and
  recent-spike-row cursor. This does not batch neural time or skip intermediate
  SNN state.
- Two reversed 256-tick continuous CUDA A/B runs improved complete encoded
  sequential throughput by `1.353x` and `1.176x`. A synchronized-per-token
  diagnostic regressed because each forced barrier removes the intended
  cross-token overlap; it is retained as latency/profile evidence, not the
  continuous-throughput promotion gate.
- The 2026-06-14 follow-up keeps the same stable-address rule but moves staging
  to the wider training-owned quantum when the graph is warm and no per-token
  metric evidence is requested. The burst graph still replays exactly eight
  sequential ticks at a time, but each burst now consumes pointer-checked slices
  from a pre-staged q16 window instead of staging its own smaller copy. The
  diagnostic profile reduced measured input staging from
  `0.053073 ms/token` to a combined `0.026171-0.031875 ms/token`; clean runs
  proved CUDA execution and exact staged-token reuse but were not velocity
  promotions because benchmark environment snapshots showed CPU contention.
- NVIDIA CUDA Graph performance guidance warns that capture boundaries can
  fail to improve or can regress when the graph misses the real bottleneck,
  input-copy overhead dominates, or dynamic control flow forces fallbacks:
  https://docs.nvidia.com/dl-cuda-graph/troubleshooting/performance-issues.html
  and
  https://docs.nvidia.com/dl-cuda-graph/latest/torch-cuda-graph/handling-dynamic-patterns.html
  MARULHO therefore does not pre-stage a whole q16 input window merely because
  the ring can hold it. Training first runs a non-mutating boundary preflight
  over every burst-sized slice; only fully device-continuous windows are copied
  into the ring. The 32768-token follow-up kept `staged == reused == 32768`
  with zero graph/burst failures under CUDA, while refusing to promote the
  lower `2624.774 tokens/sec` result because run-condition evidence showed CPU
  and GPU contention.
- The result changes the next implementation direction. More isolated Triton
  arithmetic is unlikely to remove the dominant host tax by itself; the next
  slice should widen persistent ownership across routing preparation,
  post-transition bookkeeping, metrics packets, and memory admission while
  retaining event-driven specialist wake and slow-path archival boundaries.

### Archival memory transfer and decay boundaries, June 2026

- PyTorch documents that `non_blocking=True` does not make a CUDA-to-CPU result immediately safe for host consumption; the host must synchronize before reading it. Its pinned-memory guidance also makes the destination-buffer ownership and synchronization contract explicit: https://docs.pytorch.org/tutorials/intermediate/pinmem_nonblock.html and https://docs.pytorch.org/docs/stable/notes/cuda.html
- MARULHO tested bulk/asynchronous staging of archival payloads, but complete trainer runs were neutral and the required pinned-buffer lifecycle would add synchronization and checkpoint complexity. The staging path is not promoted.
- MARULHO also tested moving the short-term consolidation observation/decay state to CUDA. A complete hot-window comparison reached `75.69 ticks/sec` for the CUDA observation path versus `97.18 ticks/sec` for the retained CPU path. The small launches and host-visible consumers cost more than the arithmetic saved.
- Continual-learning replay work supports bounded replay stores and latent/compressed replay rather than making all archival metadata device-resident: https://arxiv.org/abs/1811.11682, https://arxiv.org/abs/1912.01100, and https://arxiv.org/abs/2407.03111
- Complementary-learning and replay research supports replay as a consolidation/planning mechanism, but does not require synchronous archival admission on every live token. MARULHO first widened stale fixed cadence from every 8/64 tokens to the 256-token checkpoint default, then retired fixed-cadence admission itself from the per-token fallback path. Slow-memory writes now come from first-token retained/fallback records or selected strong-capture events; fixed cadence records deferred maintenance evidence only. In the 4096-token full-warm CUDA stress surface, interval-8 reached `668.976 tokens/sec`, interval-64 reached `742.767`, and a config-only interval-256 clone reached `832.915`, all with active CUDA graph replay and zero failures. The focused fixed-cadence retirement report `reports/bounded_replay_window_20260620/slow-memory-fixed-cadence-admission-retired.json` kept the first-token archive, removed ordinary cadence writes (`1` bounded archive versus `17` retired writes over 256 tokens), preserved non-strong strong-capture counters, kept archival storage on CPU, and closed the slow-memory fixed-cadence execution gate. The original unstamped checkpoint still migrates to interval-256 to avoid stale tight configurations, but the interval is now a deferred maintenance/reporting boundary rather than an admission trigger. Sources: https://pmc.ncbi.nlm.nih.gov/articles/PMC5847173/, https://papers.neurips.cc/paper/8327-experience-replay-for-continual-learning.pdf, and https://www.nature.com/articles/s41467-022-34938-7.
- The retained direction keeps archival records and STC scalar state on CPU, decides reservoir admission before optional CUDA-to-CPU payload copies, and stores exact capture-tag, strong-tag, and local-PRP vectors in contiguous numeric buffers. NumPy decays those buffers in place through zero-copy views; checkpoint snapshots remain list-based for compatibility and portability.
- MARULHO also tested wider host-side and normalization shortcuts around this boundary. Coalesced pinned host readback for step scalars and sparse CPU assembly regressed controlled hot-window pairs on average, and lazy cross-modal text normalization reduced profiler calls but failed repeated full-trainer A/B. Both were removed. The retained memory direction is narrower: vectorize only the large-ledger awake-ripple scan where the measured crossover is clear, keep small ledgers scalar, and surface scan counts through Runtime Truth without moving archival memory onto CUDA.

### Bounded batched live text ingestion, June 2026

- NVIDIA documents batching as a way to reduce dispatch overhead and recommends minimizing host/device transfers; PyTorch likewise treats fusion and reduced launch overhead as primary GPU tuning tools. Sources: https://docs.nvidia.com/cuda/cuda-programming-guide/03-advanced/advanced-host-programming.html, https://docs.nvidia.com/cuda/cuda-c-best-practices-guide/index.html, and https://docs.pytorch.org/tutorials/recipes/recipes/tuning_guide.html
- MARULHO's live RTF source path contradicted that guidance: an empty learned-chunk codebook still triggered scalar CUDA operations and host-visible decisions per character. Direct 24-character measurements were about `708 ms` with mutation enabled, `381 ms` for inference only, `131 ms` for CPU mutation, and `69 ms` for CUDA base encoding.
- The implementation direction changes at the ingestion boundary, not the cognitive representation: live source collection is inference-only, empty-codebook order-weighted windows and deterministic chunk signatures are assembled in bounded batches, and emitted tensors remain on the selected device. Mutation-enabled chunk learning remains available in explicit training and remote-bootstrap paths.
- Scalar/batch parity measured a maximum absolute representation difference of `1.19e-7`. On the same 1024-column checkpoint, fresh cold configured-source collection fell from `505.6024` to `37.0373 ms`; the complete 24-token tick improved from `43.8417` to `203.0565 tokens/sec`. A cache-restored repeat reached `566.3356 tokens/sec`. This is ingestion/orchestration evidence, not proof that learned chunk quality is equivalent under a different plasticity schedule.

### Static routing fusion boundary, June 2026

- Triton's fused-softmax tutorial emphasizes that fusion wins by avoiding intermediate global-memory traffic and framework launch overhead, while its `tl.topk` API restricts `k` to powers of two. MARULHO's routed set is `k=10`, so the implementation uses a bounded static repeated-argmax selector rather than padding the cognitive candidate set or changing routing semantics. Sources: https://triton-lang.org/main/getting-started/tutorials/02-fused-softmax.html and https://triton-lang.org/main/python-api/generated/triton.language.topk.html
- PyTorch CUDA Graph guidance requires static addresses and warns that CPU-side scalar work and dynamic behavior break capture. MARULHO therefore keeps persistent score/candidate/winner buffers under `ColumnTransitionRuntime`, refreshes only retrieval-cache pointers after invalidation, and does not claim the whole tick is graph-safe yet. Sources: https://docs.pytorch.org/docs/stable/user_guide/torch_compiler/torch.compiler_cudagraph_trees.html and https://pytorch.org/blog/accelerating-pytorch-with-cuda-graphs/
- Exact recurrent evaluation on the 1024-column RTX 3060 checkpoint produced zero candidate, winner, or positive-branch mismatches across 128 keys. The isolated cluster improved from `415.16` to `1716.56 ticks/sec`; production-owned complete text/idle A/B improved from `66.80` to `92.00 ticks/sec`. A global sensory variant was rejected, so the promoted checkpoint mode falls back on every visual/audio tick.

### Continuous execution quantum, June 2026

- NVIDIA's CUDA Graph guidance identifies repeated host setup and launch work as significant overhead for short GPU kernels, while its GPU-resident execution guidance explicitly targets CPU scheduling, synchronization, and orchestration costs. Sources: https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html and https://docs.nvidia.com/holoscan/sdk-user-guide/using-the-sdk/gpu-resident-execution
- Event-driven SNN work supports computation tied to sparse events rather than mandatory clock-like host work around every update: https://arxiv.org/abs/2403.00270
- MARULHO does not yet have a fully GPU-resident cognitive loop, and this change does not batch or reorder neural learning. It removes a host-only `5 ms` sleep and per-token execution-lock/mutation cycle, then groups up to eight still-sequential token updates under one bounded Runtime Control quantum. Stop checks remain between quanta, with a hard 128-token configuration ceiling.
- Reversed production-loop A/B on the 1024-column RTX 3060 checkpoint measured `662.800` versus `135.240 tokens/sec` (`4.901x`). Both quantum arms retained full warm source queues, observed CUDA on the RTX 3060, executed the in-place Triton transition `256` times with zero failures, and stopped in `13.896` and `11.883 ms`. The next direction is broader persistent device ownership of route/index/graph-preparation state, not increasing the host quantum without bound.

### Persistent burst versus larger graph, June 2026

- NVIDIA CUDA Graphs reduce repeated CPU setup and launch overhead, but graph
  nodes still execute scheduled device work; a larger graph is not free.
  Conditional graph nodes can move loops and decisions device-side, while
  PyTorch does not yet expose the full CUDA conditional-node surface used by
  the native API. Sources:
  https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html,
  https://docs.nvidia.com/dl-cuda-graph/latest/torch-cuda-graph/handling-dynamic-patterns.html,
  and https://docs.pytorch.org/docs/stable/user_guide/torch_compiler/torch.compiler_cudagraph_trees.html
- MARULHO tested an eight-tick monolithic graph. Exact trajectory passed, but
  sustained device execution regressed to `0.951x`, so it was deleted.
- The implementation direction changed to a boundary-aware host burst over the
  already efficient one-tick graph. This preserves causal SNN updates and
  keeps slow/event-driven work awake only at explicit boundaries. Repeated
  complete 32768-token runs reached `2387.898` and `2607.316 tokens/sec`, with
  all transitions on CUDA and zero graph/burst failures.
- Data-dependent strong-memory admission cannot be inferred from the previous
  host mirror. MARULHO therefore adds one bounded device event snapshot to the
  burst graph, keeps archival assembly/input/routing tensors CPU-resident, and
  performs host work only when the burst reaches its existing truth boundary.
  This follows CUDA Graph fixed-address ownership and PyTorch's requirement
  that CUDA-to-CPU data must be synchronized before host consumption; it does
  not move the replay ledger onto CUDA.
- Repeated final complete-runtime runs reached `2648.747`, `2533.719`, and
  `2599.013 tokens/sec` with all 32768 transitions on the RTX 3060 and zero
  graph failures. Runtime Truth now shows that host-truth, exploration, drift,
  and telemetry boundaries dominate failed burst attempts. The next research
  direction is a truth-interval device event queue or native conditional/device
  loop, not more isolated arithmetic fusion.
- The first promoted follow-up kept the checkpoint's measured
  `cuda_graph_host_truth_sync_interval_tokens=16` unchanged and replaces the
  immediate eight-row drain with a fixed sixteen-row device event queue. This
  is intentionally different from the rejected interval-32/64/128 experiments:
  truth freshness remains bounded at sixteen tokens, while two eight-token
  service quanta can share one exact drain. CPU fallback and tick completion
  force a drain before control leaves training. Exact 16-tick CUDA trajectory
  parity and forced strong-event archival tests pass. Repeated full-warm
  32768-token runs reached `2631.493` and `2656.216 tokens/sec`; burst-owned
  tokens increased from the prior Runtime Truth sample's `9760` to `18248` and
  `18328`, `host_truth_boundary` fallbacks fell from `1033` to zero, all
  `32768` transitions executed on the RTX 3060, and graph/burst failure counts
  remained zero. Remaining fallback quanta are now dominated by exploration,
  drift refresh, and telemetry. CUDA Graph fixed-address and synchronization
  semantics support bounded device retention, while native conditional/device
  graph control remains a later lower-level option rather than a PyTorch
  assumption. Sources:
  https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html,
  https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__GRAPH.html,
  https://docs.pytorch.org/docs/stable/user_guide/torch_compiler/torch.compiler_cudagraph_trees.html,
  and https://docs.pytorch.org/docs/stable/notes/cuda.html.
- A later same-day follow-up superseded the sixteen-row queue after the event
  queue, host-truth cadence, and empty host event-apply path moved together.
  The current production default is a thirty-two-token queue and
  `cuda_graph_host_truth_sync_interval_tokens=32`; the 131072-token complete
  CUDA service confirmation reached `4577.595 tokens/sec` with
  `train_compute=0.181855 ms/token`, all transitions on the RTX 3060 through
  `inplace_triton`, `4097` host-truth syncs, `126975` skips, zero forced event
  drains, and zero graph/burst failures. The implementation implication is
  narrow: lower Runtime Truth scalar freshness is acceptable on the hot path
  only because graph-owned cognition still updates every replay and fallback or
  tick completion drains exact bounded events before control leaves training.
- Boundary-controller follow-up, June 2026: CUDA Graph guidance favors defining
  repeated device work once and avoiding piecewise host submission for short
  kernels, but does not justify moving archival memory or arbitrary Python
  policy into a graph. MARULHO therefore classified boundaries by ownership:
  telemetry became deferred observation; drift refresh and drift-floor closure
  remain exact CPU memory maintenance after a bounded device-event drain; and
  sleep/replay, sensory wake, slow-memory admission, and index mutation remain
  real fail-closed boundaries. Repository inspection also found that the
  claimed exploration noise scale had no consumer anywhere in cognition, so
  its update, checkpoint state, metrics, and burst gate were deleted instead of
  being ported decoratively to CUDA. A clean 32768-token run reached
  `2745.790 tokens/sec`. The authoritative longer 131072-token run reached
  `2126.013 tokens/sec`, with all transitions on the RTX 3060, `116696`
  burst-owned tokens, zero graph/burst failures, `2333` drift refreshes, `12`
  drift-floor closures, `1790` deferred telemetry observations, and only two
  sleep/replay fallbacks. The longer run also identified preparation as the
  next broad bottleneck at `0.156315 ms/token`, behind train compute at
  `0.281598 ms/token`. Source:
  https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html.
- Prepared-source/tick-executor follow-up, June 2026: the same CUDA Graph
  guidance implies that repeated short device work should be submitted through
  a stable enclosing workflow, but it does not justify moving source
  persistence or semantic observation onto CUDA. MARULHO therefore made
  prepared source queues generation-aware and moved complete text-tick neural
  sequencing into training while retaining eight-token causal quanta and
  between-quantum cancellation. A 131072-token complete-runtime run improved
  sustained throughput from `2126.013` to `3359.378 tokens/sec`; preparation
  fell `94.6%`, mean tick latency fell `38.2%`, and p95 fell `44.5%`, with
  exact focused CUDA trajectory parity and zero graph/burst failures. The next
  bottleneck is device transition work at `0.259876 ms/token`; source prewarm
  remains a separate `84.725 s` startup cost. Source:
  https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html.
- Nemotron 3 Ultra source-bank note, June 2026: NVIDIA's Nemotron 3 Ultra
  release is useful to MARULHO as a candidate source/replay data direction, not
  as a runtime cognition dependency. NVIDIA reports released checkpoints and
  datasets, including 173B tokens of fresh GitHub code through September 30,
  2025, synthetic legal/specialized datasets, and posttraining data for
  agentic/reasoning/general capabilities. The implementation direction is to
  review those Hugging Face datasets as explicit source-bank candidates with
  license, field, sampling, and memory-pressure checks before any promotion.
  They must not become hidden LLM cognition, a default hot-path dependency, or
  a replacement for Runtime Truth evidence. Sources:
  https://research.nvidia.com/labs/nemotron/Nemotron-3-Ultra/ and
  https://huggingface.co/datasets/nvidia/Nemotron-Pretraining-Code-v3.
- Direct route/vote fusion rejection, June 2026: CUDA Graph and persistent-kernel
  literature supports reducing repeated host launch/orchestration only when the
  fused boundary actually reduces the dominant live work. MARULHO tested a
  one-block Triton direct route/vote kernel for the live `1024 x 64` exact-cache
  shape after reviewing CUDA Graph launch-overhead guidance, PyTorch CUDA Graph
  fixed-address constraints, persistent-kernel/RNN execution, NeuronSpark-style
  fused Triton spiking kernels, and GPU-native ANN routing directions. The
  direct route/vote candidate passed CUDA parity but failed complete-runtime
  evidence: the profiled 8192-token run measured `2381.587 tokens/sec` versus
  `2408.630` before direct selection, and the 32768-token clean direct run
  measured `2266.882` versus `2359.929` after reverting to the retained
  two-stage route/vote path. Runtime Truth now reports
  `route_vote_kernel_variant`, but the direct kernel body was deleted. The
  implementation direction remains a lower-level device-owned multi-tick
  executor or persistent sequence kernel, not a local one-block top-k fusion
  that leaves one CUDA Graph replay per token. Sources:
  https://docs.nvidia.com/dl-cuda-graph/troubleshooting/performance-issues.html,
  https://pytorch.org/blog/accelerating-pytorch-with-cuda-graphs/,
  https://proceedings.mlr.press/v48/diamos16.html,
  https://arxiv.org/html/2603.16148v1, and
  https://arxiv.org/abs/2602.23999.
- Sustained velocity environment note, June 2026: PyTorch CUDA Graph guidance
  explicitly calls out CPU load and operating-system launch timing variance for
  small GPU workloads, while the retained MARULHO long-run evidence showed the
  same logical CUDA path ranging from `4577.595` to `3804.642 tokens/sec` under
  different host conditions. MARULHO therefore adds `velocity_environment.v1` to
  the continuous stress benchmark, collected outside the measured tick window.
  It records CPU/GPU contention and probe availability so long-run throughput
  can be compared honestly before changing architecture. This does not move any
  probe into the hot path and does not weaken the next implementation direction:
  a lower-level device-owned multi-tick executor or persistent sequence kernel
  is still the credible route to higher sustained cognitive velocity. Sources:
  https://pytorch.org/blog/accelerating-pytorch-with-cuda-graphs/ and
  https://docs.nvidia.com/dl-cuda-graph/troubleshooting/performance-issues.html.
- Sparse burst-payload note, June 2026: NVIDIA CUDA Graph guidance warns that
  CPU-bound launches and synchronization can erase small-kernel gains, while
  sync-free graph code examples keep device work asynchronous unless host
  consumption is required. NVIDIA's CUDA Graph quick checklist and PyTorch's
  pinned/non-blocking transfer tutorial also reinforce that GPU-to-CPU
  consumption has synchronization semantics and should not be hidden in ordinary
  ticks. MARULHO therefore deletes no-strong routing/assembly payload loads from
  the Device Strong-Event Ring instead of creating a decorative async host
  mirror: only the result row and strong flag are ordinary truth evidence, and
  archival payloads are loaded only for real strong captures. The measured
  implication is narrow but important: ordinary no-strong ticks no longer pay
  replay-payload memory traffic. A 131072-token clean stress run preserved the
  maintained high-throughput path at `4438.669 tokens/sec` with zero graph/burst
  failures, but did not beat the retained `4577.595 tokens/sec` top; the next
  major speed work stays focused on a lower-level device-owned multi-tick
  executor or persistent sequence kernel that reduces actual graph/kernel
  launches. Sources:
  https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/sync-free-code.html,
  https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/quick-checklist.html,
  and https://docs.pytorch.org/tutorials/intermediate/pinmem_nonblock.html.
- Fused burst-packet publication note, June 2026: NVIDIA and PyTorch CUDA Graph
  guidance both frame graph replay as a way to reduce repeated CPU launch/setup
  overhead, while NVIDIA's performance guidance warns that graphing or replaying
  the wrong range can give little or no speedup. MARULHO therefore removed the
  separate post-transition burst snapshot kernel and folded packet publication
  into the already-required in-place transition kernel, where winner, assembly,
  routing key, neuromodulator state, effective modulator, and competitive
  surprise are live. The implementation also skips redundant event-slot resets
  when a normal full-capacity drain observes a slot that already wrapped on
  device. A 131072-token clean run proved CUDA correctness with zero graph/burst
  failures and `4094` reset skips, but measured only `3923.410 tokens/sec`
  under recorded CPU contention, below the retained `4577.595` top. The research
  implication is unchanged: packet fusion removes real dead work, but maximum
  sustained velocity requires reducing the one CUDA Graph replay per token with
  a lower-level device-owned multi-tick or persistent sequence executor. Sources:
  https://docs.nvidia.com/dl-cuda-graph/troubleshooting/performance-issues.html
  and https://pytorch.org/blog/accelerating-pytorch-with-cuda-graphs/.
- Device strong-count drain note, June 2026: the same sync-free guidance also
  applies to small host materialization inside an already bounded truth drain.
  MARULHO kept exact strong-capture payloads, but moved ordinary no-strong
  detection to a device-owned cumulative count written by the in-place
  transition kernel. Host drains now skip CPU strong-flag scans when the device
  count proves the pending window has no strong events, while count/flag
  mismatch fails closed for actual strong captures. A profiled 8192-token run
  recorded `256` no-strong scan skips, `0` strong scans, zero graph/burst
  failures, and `text_burst_runtime_event_drain=0.057988 ms/token`; the
  131072-token validation stayed on RTX 3060 CUDA at `4122.568 tokens/sec` with
  `4096` no-strong scan skips, but CPU/GPU contention kept the retained
  `4577.595 tokens/sec` best as the velocity ceiling. This is a host-evidence
  cleanup, not the persistent multi-tick executor needed for the next large
  speed jump. Sources remain:
  https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/sync-free-code.html
  and https://docs.pytorch.org/tutorials/intermediate/pinmem_nonblock.html.
- Native parent-graph capacity note, June 2026: current CUDA Graph guidance
  still favors static work, fixed virtual addresses, and synchronization-free
  replay, while NVIDIA's dynamic-pattern guidance and CUDA device-graph launch
  support do not give PyTorch Python a general device-owned conditional loop
  for this stateful sequence. MARULHO therefore tested a startup-warmed exact
  sixteen-token repeated-child parent graph before considering a deeper
  executor. The prototype preserved sequential SNN state and fail-closed
  Runtime Truth, halved parent launches on an 8192-token profile, and exposed
  active/default/allowed burst capacity in Runtime Truth, but the clean
  131072-token run reached only `4887.767 tokens/sec` versus the retained
  eight-token native base at `4992.049`. A follow-up native32 attempt under
  the exact q16 shape warmed `[32]` parent graphs but delivered zero native
  successes because every sixteen-token quantum was partial relative to the
  parent graph; benchmark validation now rejects that misaligned probe before
  startup. FlashRNN and persistent-RNN work, Triton persistent-kernel examples,
  PyTorch persistent grouped-GEMM work, and NeuronSpark fused PLIF kernels all
  point the same way: a real next executor must own more of the time loop/state
  update below Python/CUDA Graph replay, likely as C++/CUDA, Triton persistent
  kernels, or a hybrid. ADR 0007 records that lower-level executor boundary as
  the next promotable direction. Sources:
  https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/handling-dynamic-patterns.html,
  https://docs.nvidia.com/dl-cuda-graph/torch-cuda-graph/sync-free-code.html,
  https://docs.nvidia.com/cuda/cuda-programming-guide/index.html#cuda-graphs,
  https://github.com/NX-AI/flashrnn,
  https://triton-lang.org/main/getting-started/tutorials/09-persistent-matmul.html,
  https://pytorch.org/blog/accelerating-moes-with-a-triton-persistent-cache-aware-grouped-gemm-kernel/,
  and https://arxiv.org/abs/2603.16148.
- Next-throughput goal synthesis, June 2026: recent research and current
  MARULHO evidence point to seven large work tracks rather than more local
  wrappers. CUDA Graph guidance says graphs help only when launch/setup overhead
  is the actual bottleneck, and MARULHO has already rejected direct one-block
  route/vote fusion and partial-tail parent graphs as defaults. FlashRNN and
  persistent-RNN work therefore strengthen the next speed direction: a true
  device-owned multi-tick or persistent sequence executor that reduces the real
  per-token graph/kernel boundary while preserving sequential state and Runtime
  Truth cadence. Thousand Brains/Monty and Expert Choice MoE support the second
  track: a column-society scheduler where total specialists can grow, but awake
  columns per tick remain bounded by relevance, surprise, cost, and cached
  votes. NeuronSpark, SpikeGPT, multi-token prediction, and Nemotron data support
  a third track: MARULHO-owned spike-native language/readout with verified
  bounded drafts, not external checkpoints or hidden LLM cognition. Predictive
  Coding Light, Difference Predictive Coding, structural plasticity, modern
  Hopfield recall, and self-evolving-agent surveys support the remaining tracks:
  compressed surprise traffic, local learning, reversible growth/pruning,
  bounded column-local recall/replay, explicit slow-path data acquisition, and
  control-room Runtime Truth parity. The copy-ready goal queue is recorded in
  `docs/goals/marulho-next-throughput-goals.md`. Sources:
  https://arxiv.org/html/2412.07752v2,
  https://proceedings.mlr.press/v48/diamos16.html,
  https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html,
  https://docs.nvidia.com/dl-cuda-graph/troubleshooting/performance-issues.html,
  https://arxiv.org/html/2412.18354v1,
  https://papers.nips.cc/paper_files/paper/2022/hash/2f00ecd787b432c1d36f3de9800728eb-Abstract-Conference.html,
  https://arxiv.org/html/2603.16148v1,
  https://openreview.net/forum?id=tL89RnzIiCd,
  https://www.nature.com/articles/s41467-025-64234-z,
  https://openreview.net/forum?id=iu9dbz2lB9,
  https://research.nvidia.com/labs/nemotron/Nemotron-3-Ultra/,
  and https://huggingface.co/datasets/nvidia/Nemotron-Pretraining-Code-v3.
- SNN language readout corpus evaluation note, June 2026: NeuronSpark,
  SpikeGPT, SpikingSSMs, Nord-AI, multi-token prediction, and speculative
  decoding all support evaluating bounded sparse readout trajectories before a
  draft is trusted, but none justify importing a checkpoint as MARULHO
  cognition. MARULHO therefore adds a slow-path readout-corpus evaluator around
  the existing transition-memory prediction gate. It records corpus provenance
  including license/terms/cache path, grounding coverage, device placement,
  latency, memory/VRAM cost, mutation absence, and a promote/reject decision
  for bounded operator review. A paired checkpoint-review artifact writes an
  isolated sparse transition checkpoint, verifies restore, and records a
  rollback manifest while keeping production runtime unchanged. Runtime Truth
  only projects the latest saved reports; it does not run the evaluator or
  weaken the throughput contract for long hot-path runs. The paired long CUDA
  gate used the same active-pressure
  `65536`-column checkpoint for `262144` tokens and reached `6307.305
  tokens/sec` with no observed contention, `train_compute=0.129545 ms/token`,
  `route_input_rows_scored=12/65536`, and zero native sequence failures or
  fallbacks, keeping the same-day throughput level while the readout evaluator
  stays slow-path.

## Engineering Implications

- Bounded query row-access note, June 2026: modern Hopfield-style recall is useful in MARULHO only as a local associative operator after routing or replay has selected a bounded candidate window. CLS, continual-learning replay, synaptic tagging/capture, latent replay, and sparse replay still argue against letting a query readout inspect archive rows directly as a second memory path. MARULHO therefore moves query-match scoring/payload reads behind `DualMemoryStore.query_match_row(...)` and query-episode neighbor stitching behind `DualMemoryStore.query_neighbor_source_row(...)`. `query_runner.memory_matches_with_report(...)` now emits `bounded_query_memory_match_row.v1` evidence, reports the row-read budget, and has no production references to slow-memory arrays. The fresh report `reports/bounded_replay_window_20260622/query-memory-store-owned-row-access.json` preserved diagnostic eager selected indices `[0, 16, 32, 48, 64]`, loaded raw text for `5` returned matches instead of all `192` candidates, used `197` bounded row reads, reduced mean query latency from `42.525 ms` to `33.718 ms` (`1.261x`), kept archival/score placement on CPU, and reported no global scan, live tick, every-token work, mutation, plasticity, or language reasoning. The same-slice replay quality report kept sleep associative recall bounded to `4` queries with mean best input-pattern distance `5.960464477539063e-08`. The `524288`-token hot-path rerun processed `5935.802 tokens/sec` with bounded `12/65536` route rows, `65526` cached rows, zero graph/native/sequence failures, CUDA active on the RTX 3060, and RTX memory `1952->1954 MiB`; run-condition sampling observed GPU contention, so this is same-band live-tick protection and old-path retirement evidence, not a speed ceiling or durable goal completion.
- Keep **Subcortex** tensor state on the configured `torch.device`.
- Preserve sparse/event structure where possible before reaching for dense CUDA kernels.
- Record actual device/backend evidence in reports before claiming CUDA acceleration.
- For routing, separate configured intent (`search_device`) from observed cache placement (`torch_vector_cache_device`) and benchmarked execution. Removed experimental backends should stay out of the live config surface until fresh evidence justifies a new implementation.
- For predictive/plasticity/binding work, local traces and eligibility state are the auditable CUDA surface. Device reports should prove where those live tensors reside before performance claims.
- For neuron dynamics, report membrane voltage, adaptation, and spike-time tensors directly, and preserve them across checkpoints without silently restoring to CPU.
- For cross-modal grounding, report the live devices of text/visual/audio traces, cross-modal weights, and confidence state.
- For spatial or hypercube binding, report topology tensor devices as well as learned binding-state devices; sparse topology claims need observable placement.
- For context and abstraction, report recurrent/context tensors, adaptive timescales, slow abstraction state, and top-down feedback tensors because they directly gate routing and curiosity.
- For text encoders, report learned chunking codebooks, semantic bucket embeddings, adapters, feature vectors, and spike traces on the selected device while keeping string parsing and segmentation as control-plane work.
- Keep memory consolidation storage CPU-resident unless the operation is active replay computation. Archival buffers, tags, timestamps, and raw windows are evidence records; replay tensors should move to the model device when consumed.
- Carry sensory encoder device metadata and last emitted spike tensor device/shape into grounded observations and previews so CUDA sensory claims are auditable.
- Move generated and directory-loaded visual/audio tensors to the resolved runtime device before sensory encoding; `map_location="cpu"` is archival behavior, not a CUDA-first sensory path.
- Active checkpoint restore should resolve `torch.load` to the runtime device. CPU archival saves remain acceptable for portability, but a live restored trainer must re-enter the Subcortex path through the selected device, not through a compatibility CPU default.
- Surface trainer-owned encoder device reports in the operator-facing runtime evidence, because model-only CUDA scope is incomplete when the active input encoder is owned by the trainer/service layer.
- Treat **Living Brain** as an evidence-gated runtime target: tick, train, think, replay, act, sleep, and runtime truth must be observable.
- Treat the former **Cortex Backend** as deleted from active runtime claims. LLM/NIM adapters must not remain as compatibility or experiment code while Subcortex/world-model paths become the real cognition surface.
- Make weak paths retireable: latency, external dependency, grounding quality, and liveness contribution should be visible before a path earns permanence.
- Live tick profiling note, June 2026: no new paper changed the implementation direction in this slice. Measured service evidence showed concept observation dominating the tick, while direct profiling isolated repeated centroid normalization and structural scans. MARULHO therefore caches normalized ConceptStore centroids, samples background concept observation at bounded source-window intervals, overlaps existing remote-source refill with CUDA training, and keeps the stage profiler synchronization-free. This is an implementation correction from runtime evidence, not a new cognitive mechanism or CUDA claim.
- Batched semantic-maintenance note, June 2026: Thousand Brains/Monty work treats intelligence as repeated communicating column-like modules rather than a single global loop, predictive-coding work separates fast prediction/error exchange from slower hierarchy updates, and complementary-learning systems work warns that immediate unregulated transfer into cortical memory can harm generalization. MARULHO therefore keeps mutable ConceptStore learning, but stops running structural growth/pruning maintenance inside every sampled token observation. Background-source concept evidence is still sampled at the same first/eighth/final cadence, while structural maintenance runs once at the source-window boundary and reports attempts, observations, batches, structural-maintenance passes, and latency. The implementation direction is grounded by service evidence: the previous 24-token CUDA source tick spent `2391.1165 ms` in concept observation, while the batched run with valid concept evidence spent `27.3519 ms`, and the final warm-source run reported `0.1805 ms` concept observation with one structural-maintenance pass. Sources: [The Thousand Brains Project](https://arxiv.org/html/2412.18354v1), [Dynamic Predictive Coding](https://journals.plos.org/ploscompbiol/article?id=10.1371%2Fjournal.pcbi.1011801), and [Organizing memories for generalization in complementary learning systems](https://www.nature.com/articles/s41593-023-01382-9).
- Column-society deep-sleep filter note, June 2026: Thousand Brains/Monty and CMP support repeated column-like modules communicating through bounded messages, while sparse MoE/Expert Choice routing and event-driven SNN work support capacity-limited wake rather than proportional all-column computation. MARULHO therefore promotes a retained CPU candidate deep-sleep filter that acts only on the retrieved candidate pool, sorts by retrieval distance, filters `dead_column_steps` candidates without scanning all columns, and reports route-owner fallback for CUDA graph/fused route-vote paths. Predictive coding with SNNs also implies cached non-awake predictive state must include location/velocity, not only prediction-error vectors, so Runtime Truth now reports predictive location update/cached counts. This is scheduler metabolism, not automatic pruning or growth. Sources: [The Thousand Brains Project](https://arxiv.org/html/2412.18354v1), [Cortical Messaging Protocol](https://thousandbrainsproject.readme.io/docs/cortical-messaging-protocol), [Expert Choice Routing](https://arxiv.org/abs/2202.09368), [Predictive Coding with Spiking Neural Networks](https://arxiv.org/html/2409.05386v1), and [SparseProp](https://arxiv.org/abs/2312.17216).
- Column-society pressure-gate note, June 2026: the same research contract applies to metabolism. Thousand Brains/Monty and CMP imply that columns communicate bounded evidence rather than waking as one dense block; Expert Choice routing argues for capacity-bound specialist work; predictive-coding SNNs keep error/state local; and event-driven neuromorphic work rewards skipping work unless an event or cached evidence makes it useful. MARULHO therefore moved memory-pressure filtering into the CUDA route-vote owner only when `ColumnMetabolismState` has cached pressure evidence. High-pressure rows are masked before top-k/winner selection; if pressure evidence is absent, the pressure gate is disabled instead of adding a no-op all-route pressure read. The active 65536-column pressure fixture then proved the execution effect on the complete CUDA path: restored route-vote masked two pressured route-bank rows while still scoring `12/65536`, waking `10`, caching `65526`, and the 131072-token run stayed in band at `6297.455 tokens/sec` with `train_compute=0.130524 ms/token`, `observed_filtered_memory_pressure_total=2`, `observed_filtered_deep_sleep_total=254`, and zero filter fallback. Sources: [The Thousand Brains Project](https://arxiv.org/html/2412.18354v1), [Cortical Messaging Protocol](https://thousandbrainsproject.readme.io/docs/cortical-messaging-protocol), [Expert Choice Routing](https://arxiv.org/abs/2202.09368), [Predictive Coding with Spiking Neural Networks](https://arxiv.org/html/2409.05386v1), and [SparseProp](https://arxiv.org/abs/2312.17216).
- Bounded replay-window note, June 2026: modern Hopfield networks show attention-like associative retrieval can be a powerful local memory operator, but MARULHO treats that as bounded column/replay-window recall rather than a transformer-style global mind. Complementary learning systems and continual-learning replay support separating fast online learning from slower replay/consolidation; synaptic tagging/capture supports replay only when tag/PRP evidence marks a trace as worth stabilizing; latent and sparse replay work supports compact, selected rehearsal rather than GPU-resident archival metadata. MARULHO therefore adds `bounded_replay_window_selection.v1`, `bounded_replay_window_recall.v1`, and `bounded_reconstruction_gated_candidate_repair` to the sleep/replay slow path: deep sleep scores only memory entries attached to checkpointed column anchors through the bucket index, unscoped selection now reports `bucket_index_scope_required` with no runtime diagnostic full scan, recall is CPU slow-path and non-mutating, and repair commits only if selected-window reconstruction improves over bounded candidate columns. Zero-pressure, no-anchor deep replay, stored-bucket modulated deep replay, no-anchor emergency repair, unanchored micro-sleep maintenance refresh, unscoped replay sampling, and unscoped SFA sampling are retired as production mutation or helper defaults. The synthetic report `reports/bounded_replay_window_20260617/synthetic-selection-candidate-repair-bounded-micro.json` now passes stored input-pattern recall with mean distance `5.960464477539063e-08`, passes prototype reconstruction with relative degradation `0.0467838377` under the `0.05` gate, applies `6` bounded repair commits, rejects `14` non-improving commits, and keeps zero-pressure/no-anchor arms at `0` updates. Anchored micro maintenance reports `bounded_micro_maintenance_refresh`, bypasses `competitive.process(...)`, and updates CPU metadata only; no-anchor micro maintenance reports `no_anchor_bucket_scope_for_micro_replay` and changes nothing. The HF-backed runner now records `bounded_replay_window_hf_recall_summary.v1` plus `reconstruction_guarded_replay_consolidation.v1`: it passed stored-experience recall after consolidation over `3` Task-A anchor-window queries with `mean_input_pattern_distance=0.0`, attempted `9` bounded candidate repair updates across `3` replay cycles, rejected all `9` because Task-A reconstruction would regress, rolled the model/memory snapshot back, and kept the memory-consolidation gate passing with effective replay updates at `0`. The cadenced follow-up records `skip_repeated_rejected_selection`, reducing that same post-Task-B guard shape to `3` attempted repairs plus `2` skipped repeated rejected cycles while keeping recall and the consolidation gate passing. The longer 65536-column 262144-token hot-path run stayed in band at `6306.507 tokens/sec`, `train_compute=0.129511 ms/token`, `route_input_rows_scored=12/65536`, and zero graph/native/sequence failures with no observed contention; the current-tree repair-scope rerun stayed in band at `6252.073 tokens/sec`, the micro-scope rerun stayed in band at `6332.439 tokens/sec`, the guarded-consolidation current-tree rerun reached `6606.251 tokens/sec` with `train_compute=0.123393 ms/token`, and the accepted cadenced rerun stayed in band at `6199.988 tokens/sec` with `train_compute=0.130574 ms/token`, `route_input_rows_scored=12/65536`, `state_transition_cached_count=65526`, zero graph/native/sequence failures, and no observed contention after one low-throughput same-code run was rejected. For future LLM-size scale, recall/replay must become more indexed, sparse, selected, and slow-windowed as memory grows; it must not become an every-token full-memory scan, GPU-resident archive, or hidden language reasoning loop. Sources: [Hopfield Networks is All You Need](https://arxiv.org/abs/2008.02217), [Complementary Learning Systems](https://pubmed.ncbi.nlm.nih.gov/7624455/), [Experience Replay for Continual Learning](https://papers.neurips.cc/paper/8327-experience-replay-for-continual-learning), [Synaptic tagging and LTP](https://pubmed.ncbi.nlm.nih.gov/9020359/), [Latent Replay for Real-Time Continual Learning](https://arxiv.org/abs/1912.01100), and [Meta-Learning with Sparse Experience Replay](https://arxiv.org/abs/2009.04891).
- Bounded replay-query anchor-source note, June 2026: the same research boundary now applies before HF replay recall asks the store for Task-A anchor queries. The old runner capped returned queries but still passed every retained `column_anchors` bucket into `collect_replay_query_indices(...)`, leaving an all-anchor source pass ahead of the bounded memory window. MARULHO now records durable anchor capture recency, checkpoints it, refreshes recency when anchors are recaptured, and emits `bounded_replay_query_anchor_bucket_source_window.v1` before query collection or recall evaluation. The source window is `16` anchor buckets in reverse capture recency, reports source total/window counts, CPU archival placement, no live tick, no every-token work, no raw text payload, no language reasoning, and `anchor_source_full_scan=false`. The historical benchmark `reports/bounded_replay_window_20260618/replay-query-anchor-source-window-bounded.json` used `8192` retained anchors and `64` iterations: the retired all-anchor source pass averaged `16.414 ms`, the bounded path averaged `0.346 ms` (`47.373x`), selected the newest anchor queries with hit rate `1.0` versus `0.0`, kept exact input recall (`mean_input_pattern_distance=0.0`), and used CPU archival/compute with `0.0 MiB` CUDA allocation. The current maintained-only report `..\..\MARULHO_reports\bounded_replay_window_20260623\replay-query-anchor-maintained-only.json` passes with the all-anchor implementation absent, exact recall, inherited cap `4096->16`, and `0.0 MiB` CUDA allocation. The `524288`-token protection run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-replay-query-anchor-source-window.json` stayed above the maintained band at `6376.873 tokens/sec`, `train_compute=0.128288 ms/token`, `prepare_training=0.006247 ms/token`, `finalize_total=0.005964 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, zero graph/native/sequence failures, and flat `1787 MiB` GPU memory; the velocity sampler marked borderline GPU contention at `20%`, so this is hot-path protection evidence rather than a clean hardware-speed ceiling.
- Bounded sleep-replay anchor-source note, June 2026: sleep replay now uses the same selected-source rule before `DualMemoryStore.select_replay_window(...)`. The retired trainer path built a sorted list of every checkpointed `column_anchors` bucket, so the store selector was bounded internally but its source bucket pass still scaled with retained anchor count. MARULHO now extracts the shared anchor-window operator into `replay_anchor_window.py`: HF replay-query and trainer sleep replay both read at most `16` reverse-recency anchor buckets, report exact source/window counts, CPU archival/source placement, no live tick, no every-token work, no raw replay text, no hidden language reasoning, and `anchor_source_full_scan=false`. The sleep surface is `bounded_sleep_replay_anchor_bucket_source_window.v1` and remains a slow-path replay selection step, not a transformer-style memory scan. The historical benchmark `reports/bounded_replay_window_20260622/sleep-replay-anchor-source-window-bounded.json` used `8192` anchors and `64` iterations: source selection improved from `0.892263 ms` to `0.037825 ms` (`23.589x`), full sleep-window selection improved from `7.797869 ms` to `0.104864 ms`, selected the newest anchor window and selected replay buckets with hit rate `1.0`, read `16/8192` anchors, and used CPU archival/source/compute placement with `0.0 MiB` CUDA allocation. The current maintained-only report `..\..\MARULHO_reports\bounded_replay_window_20260623\sleep-replay-anchor-maintained-only.json` passes with the all-anchor implementation absent, bounded source and selected-hit rates `1.0`, `16` selected replay rows, source mean `0.039967 ms`, selection mean `0.101736 ms`, and `0.0 MiB` CUDA allocation. The same-shape `524288`-token protection run `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-sleep-replay-anchor-source-window.json` stayed in the maintained band at `6135.629 tokens/sec`, `train_compute=0.132272 ms/token`, `prepare_training=0.006595 ms/token`, `finalize_total=0.006411 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, zero graph/native sequence failures, and RTX 3060 memory `1615->1614 MiB`; velocity observed borderline GPU contention at the `20%` threshold, so the claim is live-tick protection and retirement of the all-anchor source pass, not a new speed ceiling.
- Replay adapter stack retirement note, June 2026: modern-Hopfield/CLS/continual-replay framing supports selected slow-window replay, not an active second adapter-training track that survives beside the maintained replay path. MARULHO therefore deletes the isolated replay-adapter experiment stack: dry-run approval, dry-run plan, metadata-only adapter artifact, experimental promotion gate, and replay-to-adaptation experiment evidence. Neutral JSON/hash helpers moved to `marulho.evaluation.artifact_io`, service validation summaries no longer expose replay-adapter report kinds, and `tests/test_replay_adapter_stack_retired.py` asserts the old modules remain absent. The same-shape protection run `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-replay-adapter-stack-retired.json` stayed in band at `6167.298 tokens/sec`, with `train_compute=0.131572 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, zero graph/native sequence failures, and RTX memory `1728->1729 MiB`; GPU utilization touched the `20%` contention threshold, so this is protection evidence, not a speed ceiling. Future adapter experiments must start as bounded offline proposals with quality evidence, no service-visible report kind, no hidden replay-text reasoning, no live/every-token work, no runtime mutation, and hot-path protection evidence before promotion.
- Target-aware replay-strength budget note, June 2026: the literature supports replay as selected, feedback-gated consolidation, not as unbounded rehearsal. MARULHO therefore keeps target-aware strength search inside the explicit reconstruction guard: each bounded replay window can test a small repair-strength schedule from the same snapshot, score the external target reconstruction, record the trial budget/policy, and commit only the best non-regressing trial. The patched HF text runner now uses `[0.1]` with `repair_strength_trial_budget=1`: `reports/bounded_replay_window_20260617/hf-recall-target-strength-budget-single-010-promoted/summary.json` accepted `6` post-Task-B repairs, rejected `0` trial attempts, improved Task-A reconstruction from `0.0170305534` to `0.0149637708`, kept stored-experience recall exact (`mean_input_pattern_distance=0.0`), and kept the memory-consolidation gate passing while cutting post-B guard latency from `3477.025 ms` to `1040.506 ms`. The medium HF qualification `reports/bounded_replay_window_20260617/hf-recall-target-strength-budget-single-010-medium-2048/summary.json` kept the same budget on `2048/2048` train tokens, `512` eval tokens, `128` columns, and `2048` memory capacity: it accepted `28` post-Task-B repairs, rejected `0`, improved Task-A reconstruction from `0.0103354922` to `0.0101451825`, and passed recall plus the consolidation gate; checkpoint reload preserved bounded recall/selection reports with `runs_live_tick=false`. The synthetic prototype stress default now uses compact escalation `[0.1, 0.5, 1.0]`: `reports/bounded_replay_window_20260617/synthetic-target-strength-budget-compact-default.json` passes recall and prototype gates with budget `3`, while the single-strength synthetic control is rejected because it failed the prototype gate. The clean 65536-column 262144-token hot-path check stayed in band at `6232.282 tokens/sec`, scored only `12/65536` route rows, cached `65526` state-transition rows, had zero graph/native/sequence failures, and reported no observed contention with flat `1715 MiB` GPU memory. This moves replay from "safe but often rejected" toward target-specific useful consolidation while preserving the live tick.
- Replay text/SFA boundary note, June 2026: modern Hopfield-style recall is useful here only as a bounded associative tensor operator, while CLS, continual replay, synaptic tagging/capture, latent replay, and sparse replay all argue against letting archival text become hidden reasoning or unselected rehearsal. MARULHO therefore makes sleep replay tensor-only through the explicit `DualMemoryStore.sleep_repair_replay_row(...)` repair reader and `DualMemoryStore.replay_recall_row(...)` read-only recall reader; neither path loads `raw_window`, expanded text, or metadata. Selection/recall reports state `raw_text_payload_loaded=false` and `language_reasoning=false`. Sleep replay adds `sleep_replay_text_payload_loaded=false`, `sleep_replay_language_reasoning=false`, and `sleep_replay_text_payload_policy=sleep_replay_uses_tensor_payloads_only`. The old raw-window eligibility-trace branch and the generic `replay_entry(...)` API are retired for sleep replay. Deep replay with abstraction also retires full-buffer SFA sampling; SFA correction now samples from selected `processed_indices` and reports `sleep_replay_sfa_correction_scope=selected_replay_window` plus `sleep_replay_sfa_full_memory_sample_retired=true`; the remaining unscoped helper defaults now refuse archival scans unless a caller explicitly marks a diagnostic global replay/SFA operation. The synthetic boundary report `reports/bounded_replay_window_20260617/synthetic-replay-tensor-payload-boundary.json` kept bounded recall/prototype gates passing with `2` accepted post-B repairs, `0` zero-pressure/no-anchor updates, `score_device=cuda`, `archival_storage_device=cpu`, and `runs_live_tick=false`. The current repair-row cleanup report `..\..\MARULHO_reports\bounded_replay_window_20260622\sleep-repair-replay-row-api-retired.json` passed with quality delta `0.076463`, `1.954494x` input-prepare speedup, `8` missing-key deferrals, `0` dense input-assembly calls, CPU archival metadata, CUDA active compute, and no global scan/live/every-token/raw-text/language-reasoning work. For future LLM-size scale, this is the contract: replay may use selected tensor traces inside explicit slow windows, but it must not reason through replay text, sample archival memory globally, or tax the live tick.
- Source tick sleep-deferral note, June 2026: sleep replay and offline consolidation remain useful in CLS, continual-replay, STC, and SNN sleep regularization work, but none of those require incidental replay execution inside a live source tick that only fell back to `train_step` for metrics. MARULHO therefore passes `allow_sleep_maintenance=False` from BrainRuntime source-tick per-token fallback, so due deep/micro/repair sleep records deferred maintenance while explicit trainer sleep windows still run. The CPU benchmark `reports/bounded_replay_window_20260620/source-tick-sleep-replay-deferred.json` passed with service fallback sleep calls `0`, explicit slow-path projection sleep calls `1`, visible `sleep_maintenance_deferred=1`, CPU archival placement, no global scans, and no hidden language reasoning. The paired `524288`-token hot-path run `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-source-tick-sleep-replay-deferred.json` stayed in the maintained band at `5993.959 tokens/sec`, `train_compute=0.135624 ms/token`, bounded `12/65536` route rows, `65526` cached rows, no observed contention, flat RTX 3060 memory at `1959 MiB`, and zero graph/native sequence failures. This keeps sleep replay as a selected slow-window operation rather than service-owned background replay.
- Bounded live memory-summary note, June 2026: CLS, STC, and sparse-replay framing also rejects treating display/status memory summaries as a live consolidation operator. `DualMemoryStore.summary_stats()` advances memory-state time and builds tensors over all retained slow entries, so trainer telemetry and service/status projections use `DualMemoryStore.live_summary_stats()` instead; full summaries remain available only for explicit offline consolidation or quality windows. The repo-local benchmark no longer keeps an executable full-summary comparator. The current maintained-only report `..\..\MARULHO_reports\bounded_replay_window_20260623\live-memory-summary-legacy-baseline-removed.json` passed with scalar fill/report parity, zero live scan entries, read-only STC tags/state, `retired_live_full_summary_scan_absence.implementation_present=false`, `65536` retired scan rows removed, `0.237312 ms` mean bounded projection latency, `0.259369 MiB` Python peak, and `0.0 MiB` CUDA allocation. The clean paired `524288`-token hot-path run `..\..\MARULHO_reports\bounded_replay_window_20260623\hotpath-active-pressure-65536-524288-i32-live-summary-legacy-baseline-removed-default-nosample-rerun.json` stayed in band at `6530.655 tokens/sec`, `tick_duration_ms.p95=18.804`, `train_compute=0.123872 ms/token`, `prepare_training=0.006295 ms/token`, `finalize_total=0.005965 ms/token`, bounded `12/65536` route rows, `65526` cached rows, no observed contention, CPU max `6%`, GPU max `10%`, flat RTX 3060 memory `1929->1929 MiB`, and zero graph/native sequence failures. This keeps status as read-only Runtime Truth projection, not replay selection or hidden consolidation.
- Bounded repair-replay input preparation note, June 2026: selected repair replay still had a dense `assembly_from_input(...)` preparation call after the replay window had already chosen anchored entries. That call did not load text or scan memory, but it scored all columns and weakened the selected-window contract for future LLM-size memory. MARULHO now uses `prepare_input_for_candidate_routing(...)` when a stored routing key is present, clears stale dense caches when no input trace exists, and reports `sleep_replay_unconditional_dense_input_assembly_retired=true` with a measured dense fallback count for legacy entries without routing keys. The 65536-column benchmark `reports/bounded_replay_window_20260618/sleep-repair-replay-bounded-input-prepare.json` selected and repaired `32/32` anchored replay entries, improved mean anchor distance from `0.508855` to `0.360171`, reduced selected input-prep latency from `61.351 ms` dense to `32.613 ms` bounded (`1.881x`), made `0` dense assembly calls during repair, kept archival tensors on CPU, and ran active repair computation on CUDA. The paired `524288`-token hot-path run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-sleep-repair-bounded-input-prepare.json` stayed in band at `6302.207 tokens/sec`, `train_compute=0.129260 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `1805->1806 MiB`, and zero runtime failures.
- Repair replay missing-key deferral note, June 2026: the remaining legacy branch for selected repair traces without stored routing keys first rebuilt keys from input patterns, then used stored-assembly projection as a replacement fallback. Both shapes preserved repair mutation from entries missing the promoted local recall key. MARULHO now removes that side path: repair replay and deep candidate repair require stored routing keys, defer missing-key entries with `sleep_replay_missing_routing_key_deferred_count`, and no longer emit `sleep_replay_stored_assembly_projection_fallback_count`. The mixed-key benchmark `reports/bounded_replay_window_20260620/sleep-repair-replay-missing-routing-key-deferred.json` selected `32` anchored repair entries, updated the `16` entries with stored routing keys, deferred `16` missing-key entries, made `0` dense input-assembly calls, improved stored-key repair quality by `0.149600`, and kept selected input-prep speedup at `1.869720x` with CPU archival storage and CUDA active repair computation. The 65536-column `524288`-token hot-path run `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-repair-missing-routing-key-deferred.json` stayed in band at `5988.223 tokens/sec`, `train_compute=0.135762 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, GPU memory `1877->1878 MiB`, and zero graph/native/sequence failures; velocity observed a GPU-utilization sample at `31%`, so this is protection evidence, not a speed ceiling.
- Reported SFA sample note, June 2026: the same literature boundary now applies to the abstraction correction sampler itself. MARULHO deletes the list-only `sample_replay_indices(...)`, deletes the unreported `sample_for_sfa(...)`, and replaces SFA sampling with `sample_for_sfa_with_report(...)`, which emits `bounded_sfa_sample.v1`, persists it through memory-store summaries/checkpoints, and reports selected replay indices, sample count, CPU archival/sample placement, no global candidate scan, `runs_live_tick=false`, `runs_every_token=false`, and `language_reasoning=false`. The source-bank `bank_memory_matches(...)` list wrapper is also deleted so bank recall callers use the reported path. The benchmark `reports/bounded_replay_window_20260618/sfa-sample-bounded-window.json` used a `65536`-entry archive, `192` selected replay-window candidates, and `64` SFA samples; selected-window sample purity improved from `0.00439453125` under the retired full-buffer sampler to `1.0`, while mean latency improved from `1.451 ms` to `0.656 ms` (`2.210x`). The accepted 65536-column `524288`-token protection run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-reported-sfa-sampler-noprofile-rerun2.json` stayed in the sustained band at `6127.490 tokens/sec`, with last-tick `train_compute=17.738 ms` (`0.138579 ms/token` over the 128-token tick), bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `1840->1861 MiB`, and zero graph/native/sequence failures. This is grounding/selection quality for slow-window abstraction correction, not a transformer-style global memory read.
- Capped replay-candidate window note, June 2026: bucket scoping alone was not enough for future memory scale because a single hot anchor bucket could still feed every stored entry into the replay scorer. MARULHO therefore bounds the selection before score computation: the bucket index preserves recency, selected anchors are traversed in recent round-robin order, and scoring stops at `candidate_window_limit` while reporting both available and scored entry counts. The focused cap test rejects the old shape by making an old high-importance trace invisible when it falls outside the capped window, and unscoped random replay now returns the retired unscoped report unless diagnostic global candidate scanning is explicit. The fresh synthetic report `reports/bounded_replay_window_20260617/synthetic-selection-candidate-repair-capped-window.json` kept positive-pressure recall/prototype gates passing, and the hot-path report `reports/bounded_replay_window_20260617/hotpath-active-pressure-65536-262144-i32-capped-replay-window.json` processed `262144` tokens at `6148.125 tokens/sec`, with `route_input_rows_scored=12/65536`, `state_transition_cached_count=65526`, no observed contention, flat `1848 MiB` GPU memory, and zero graph/native/sequence failures. This keeps replay useful without letting local attention-like recall become a full-memory scan.
- Bounded replay-query collection note, June 2026: CLS, continual replay, STC, latent replay, sparse replay, and modern Hopfield-style associative recall all point to selected local evidence rather than archive-wide rehearsal. MARULHO therefore retires the HF replay runner's linear `slow_bucket_ids` query walk and records `bounded_replay_query_collection.v1`: Task-A anchor queries are collected through the bucket-indexed recent round-robin candidate window, capped by `max_queries`, and reported with available/collected counts, `score_count=0`, no global score/candidate scan, CPU archival placement, and `runs_live_tick=false`. The HF report `reports/bounded_replay_window_20260617/hf-recall-capped-query-collection/summary.json` collected `3` bounded Task-A anchor queries under `candidate_window_limit=16`, kept after-consolidation recall exact (`mean_input_pattern_distance=0.0`), accepted `6` guarded repairs, improved target quality from `0.0234637554` to `0.0213608844`, and kept the memory-consolidation gate passing. The matching 65536-column hot-path run processed `262144` tokens at `6221.949 tokens/sec`, with `route_input_rows_scored=12/65536`, `state_transition_cached_count=65526`, no observed contention, flat `1848 MiB` GPU memory, and zero graph/native/sequence failures. This removes another old scan shape while keeping replay selected, cadenced, and off the live tick.
- Bounded query-memory readout note, June 2026: the same research boundary applies to explicit query recall. Modern Hopfield-style matching can retrieve local associative evidence, but it must not become an archive-wide text reasoning pass. MARULHO therefore retires `query_runner.memory_matches(...)` as a full slow-buffer scan and adds `bounded_query_memory_match.v1`: routing supplies candidate bucket ids, the memory store returns a capped bucket-indexed candidate window, and query readout computes similarity plus replay-priority scores only for those entries while reporting no global score/candidate scan, CPU archival placement, and `runs_live_tick=false`. The query report `reports/bounded_replay_window_20260617/query-memory-match-bounded-window.json` used `candidate_window_limit=192`, scored `1` candidate, returned `1` memory match with similarity `0.9932903051`, and did not mutate runtime state. The matching 65536-column hot-path run processed `262144` tokens at `6137.185 tokens/sec`, with `route_input_rows_scored=12/65536`, `state_transition_cached_count=65526`, no observed contention, flat `1848 MiB` GPU memory, and zero graph/native/sequence failures. This keeps query readout useful but bounded for future LLM-size memory.
- Bounded context-comparison memory recall note, June 2026: context A/B readout is another explicit query surface, so it must carry the same bounded evidence instead of hiding behind a report-dropping compatibility helper. MARULHO deletes `query_runner.memory_matches(...)`; context comparison now calls `memory_matches_with_report(...)` for each context, shares one returned replay-entry payload cache across contexts, returns per-context reports, and emits `bounded_context_comparison_memory_match.v1` with context count, candidate totals, raw payload loads/cache hits, CPU archival/score placement, no global scans, `runs_live_tick=false`, `runs_every_token=false`, and `language_reasoning=false`. The benchmark `reports/bounded_replay_window_20260618/context-memory-match-bounded.json` preserved selected indices for both contexts (`quality.min=1.0`), reduced raw payload reads from `16` to `8` with `8` cache hits, and improved mean latency from `71.927 ms` to `70.550 ms` (`1.020x`) over a `65536`-entry synthetic store. The current-tree 65536-column 524288-token hot-path run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-context-memory-match.json` stayed in the maintained band at `6065.987 tokens/sec`, with `train_compute=0.135179 ms/token`, `concept_observation=0.000474 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `1839->1845 MiB`, and zero graph/native/sequence failures. This keeps context recall inspectable without reviving a report-dropping readout path.
- Returned-only query text payload note, June 2026: explicit readout may display selected text evidence, but modern Hopfield-style recall should still rank locally before text payload expansion. MARULHO therefore keeps similarity-only query readout tensor-first: `bounded_query_memory_match.v1` now reports `raw_text_payload_policy=returned_similarity_matches_only`, `raw_text_payload_count`, and `language_reasoning=false`, while term/focus ranking must explicitly report candidate-window text ranking. The benchmark `reports/bounded_replay_window_20260617/query-memory-payload-returned-only.json` used `65536` entries and a `192`-entry candidate window, preserved selected indices against the retired eager candidate-payload shape, reduced raw text payload loads from `192` to `5`, and reduced mean latency from `33.612 ms` to `25.881 ms` (`1.299x`). The longer 65536-column hot-path gate processed `524288` tokens at `6152.079 tokens/sec`, with `train_compute=0.132199 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, flat GPU memory (`1874->1878 MiB`), and zero graph/native/sequence failures. This keeps future LLM-size query recall inspectable without turning every selected candidate into text reasoning work.
- Bounded query episode readout note, June 2026: selected query memories can now be stitched into operator-facing episodes only through `bounded_query_memory_episode_readout.v1`. The old list-only `build_memory_episodes(...)` helper is removed; callers use `build_memory_episodes_with_report(...)`, which records selected match count, bounded neighbor radius, direct-indexed neighbor windows, CPU archival placement, no global score/candidate scan, `runs_live_tick=false`, `runs_every_token=false`, and `language_reasoning=false`. The benchmark `reports/bounded_replay_window_20260618/query-episode-readout-bounded.json` used a `65536`-entry synthetic archive and four returned fragment matches; fragment-only readout missed the target top episode (`"els safe."`), while selected-neighbor readout recovered `"a cat purrs when it feels safe."` with `10` direct neighbor-window payloads under a `28`-entry budget. Mean readout latency rose from `0.490 ms` to `0.936 ms`, a visible slow-query cost rather than a live-tick cost. The 65536-column `524288`-token protection run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-query-episode-readout.json` stayed in band at `6219.926 tokens/sec`, `train_compute=0.130647 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `1810->1811 MiB`, and zero graph/native/sequence failures.
- Bounded source-episode admission note, June 2026: complementary learning systems and synaptic tagging/capture support preserving selected source episodes for later replay, but not every-token archival dumping. MARULHO therefore adds `bounded_feed_source_episode_admission.v1` after explicit query-runner feed only: it deduplicates sentence-like source units, caps the admission budget at `32` episodes and `240` chars each, stores archival payloads on CPU, and reports no live tick, no every-token work, no global memory scan, and no language reasoning. Query readout now treats complete admitted source episodes as authoritative bounded episodes, filters zero-support episodes when any query-supported evidence exists, and retires mixed-provenance neighbor stitching by requiring character overlap and refusing to stitch raw cadence fragments across source-admission boundaries. The benchmark `reports/bounded_replay_window_20260618/source-episode-admission-bounded.json` improved the simple-animals grounding gate from `0.25` to `1.0` pass rate, admitted `5` source episodes, kept all archival tensors on CPU, and measured a `17293.226 ms` explicit-feed latency increase with query mean latency improving by `44.827 ms`. The `524288`-token hot-path protection run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-source-episode-admission.json` stayed above the maintained band at `6702.362 tokens/sec`, `train_compute=0.121727 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `1808->1808 MiB`, and zero graph/native/sequence failures.
- Dense source-admission assembly retirement note, June 2026: the same future LLM-size boundary rejects source-admission work that silently builds a dense all-column assembly after source candidates have already been selected. MARULHO now derives the source episode winner, assembly, and routing key from one bounded offline competition (`assembly_policy=bounded_offline_competition_winner_assembly`) instead of calling the dense `assembly_for_pattern(...)` helper from the source-admission path. The refreshed benchmark `reports/bounded_replay_window_20260618/source-episode-admission-bounded-v2.json` kept simple-animals grounding at `0.25 -> 1.0`, admitted `5/5` selected episodes, reported `latency_ms=2725.253`, kept archival storage on CPU while active assembly computation ran on `cuda:0`, and reduced explicit-feed overhead to `46.234 ms` with query mean latency improving by `16.968 ms`. The paired `524288`-token protection run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-source-episode-admission-v2.json` stayed in band at `6412.209 tokens/sec`, `train_compute=0.126270 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, zero runtime failures, and GPU memory `1812->1866 MiB` despite observed GPU-side contention during sampling.
- Bounded service replay-plan source window note, June 2026: modern Hopfield-style recall, CLS, continual replay, synaptic tagging/capture, prioritized replay, and sparse replay all support selecting useful evidence before rehearsal; they do not justify a service planner that sorts all accumulated runtime history just because the returned plan is capped. MARULHO therefore retires the input-unbounded `build_replay_plan(...)` construction shape. The active planner records `bounded_replay_plan_source_window.v1`, caps source windows to `64` timestamp-oriented rows per stream, indexes `128` recent feedback rows, adds at most `32` feedback-target stubs ranked by contradiction/correction signal before recency, and keeps advisory replay planning CPU-only with `runs_live_tick=false`, `gpu_used=false`, and no hidden language reasoning. The benchmark `reports/bounded_replay_window_20260618/replay-plan-source-window-bounded.json` used `20000` episodes, `20000` actions, `20000` predictions, `2000` uncertain domains, and `128` feedback rows; it preserved the old high-signal contradicted target as top candidate (`ep-42`), reduced plan latency from a measured pre-change unbounded mean of `6860.919 ms` to `14.684 ms` (`467.225x`), used `0.519 MiB` traced peak Python allocation, and used no CUDA/VRAM. The paired hot-path run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-replay-plan-source-window.json` processed `524288` tokens at `6344.404 tokens/sec`, with `train_compute=0.128679 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, flat `1799 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. This keeps service replay selection inspectable and scalable without moving archival metadata to the GPU or taxing the neural live tick.
- Bounded status replay-path projection note, June 2026: the same modern-Hopfield/CLS/continual-replay/STC boundary applies to observability. Status reports must summarize selected replay evidence, not recreate a full-memory replay scan in the service layer. MARULHO therefore bounds `StatusReadModel` replay-path projection before exporting emission review-history, emission replay-design, and rollout consolidation readiness: `bounded_snn_status_emission_review_history_source_window.v1` reads `16` recent reviewed emissions, `bounded_snn_status_emission_replay_design_path_source_window.v1` reads `16` recent reviewed emissions plus `16` recent internal readout events, and `bounded_snn_status_rollout_consolidation_path_source_window.v1` reads `16` recent rollout events plus `16` recent internal readout events. All reports keep retained/truncated counts visible and state CPU archival/score placement, no live tick, no every-token cadence, no raw text payload, no hidden language reasoning, no global candidate/score scan, and no CUDA archival metadata. The benchmark `reports/bounded_replay_window_20260618/status-replay-path-source-window.json` matched diagnostic full-retained latest history, emission, and rollout evidence while checking `80` source rows instead of `10240` retained rows, reducing combined projection latency from `102.831789 ms` to `1.309999 ms` (`78.497629x`) with `0.0 MiB` CUDA allocation/reservation. The profiled `524288`-token protection run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-status-replay-path-source-window-profile.json` stayed bounded and contention-free at `6081.034 tokens/sec`; the no-profile rerun `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-status-replay-path-source-window-noprofile-rerun.json` reached `6408.252 tokens/sec` with bounded `12/65536` route rows, zero graph/native/sequence failures, and observed GPU-side contention. This closes a reporting-layer scan shape without turning Runtime Truth into replay execution.
- Bounded readout-ledger normalization/store/known-hash note, June 2026: for future LLM-size readout history, even control-plane replay/status normalization, checkpoint-style ledger persistence, and replay-provenance hash checks must not build every retained ledger family before capping it. MARULHO therefore retires the SNN Readout Evidence Ledger's full-materialize-then-cap normalizer, the remaining full-materialize store-state copy, and the broad-normalized known-readout-evidence hash lookup: retained event-family copies use the same bounded newest-first helper, while known-hash provenance checks now read only the bounded `events` source window. The report surfaces `bounded_snn_readout_ledger_normalization_source_window.v1`, benchmarked `bounded_snn_readout_ledger_store_state_source_window.v1`, and `bounded_snn_readout_known_evidence_hash_source_window.v1`, exposes CPU archival/normalization/store/lookup placement, no global candidate/score scan, no live tick, no every-token cadence, no hidden language reasoning, no mutation/plasticity in the hash lookup, and no CUDA archive. The refreshed benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-store-state-source-window.json` used `23` event families with `2048` rows each, kept normalization at `2944` rows instead of `47104` (`16x`), preserved newest-first retention (`1.0` versus `0.0` for the retired normalizer), and cut mean normalization latency from `2415.385992 ms` to `159.388156 ms`. The store-state boundary also read `2944` rows instead of `47104`, preserved newest-first store-window parity with the retired list-slice shape, and measured `159.156636 ms` versus `169.042904 ms` (`1.062117x`) with `0.0 MiB` CUDA allocation and `6.514462 MiB` traced Python peak. The known-hash follow-up `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-store-state-known-hash-source-window.json` preserved hash-set parity while checking `128` `events` rows instead of `2944` normalized rows (`23x`), reducing mean lookup latency from `156.192384 ms` to `6.792628 ms` (`22.994397x`) with `6.538644 MiB` traced Python peak and `0.0 MiB` CUDA allocation/reservation. The no-profile `524288`-token ledger-store protection rerun `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-ledger-store-state-window-noprofile-rerun.json` stayed in band at `6044.412 tokens/sec`, `train_compute=0.134651 ms/token`, bounded `12/65536` route rows, no observed contention, GPU memory `2029->2032 MiB`, and zero graph/native sequence failures. The known-hash protection rerun `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-known-readout-hash-window-rerun.json` stayed same-band at `5938.461 tokens/sec`, `train_compute=0.136714 ms/token`, bounded `12/65536` route rows, no observed contention, GPU memory `2032->2031 MiB`, and zero graph/native sequence failures; this is protection evidence, not a speed promotion. This keeps replay/readout ledgers bounded and auditable without turning normalization, restore/store persistence, or provenance checks into live replay operators.
- Bounded dense-label calibration source-window note, June 2026: dense readout calibration is useful only as selected operator-reviewed evidence, not as another broad readout-history scan. MARULHO therefore routes `dense_label_candidate_history(...)` and `dense_label_candidate_calibration_policy(...)` through `bounded_snn_dense_label_candidate_calibration_source_window.v1`, reading only `dense_label_candidate_events` with CPU archival/lookup placement, no raw text payload, no hidden language reasoning, no live tick, no every-token cadence, no mutation/plasticity, and no CUDA archive. The benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-store-state-known-hash-dense-label-source-window.json` preserved history-hash, policy-hash, and ready-candidate parity while checking `128` dense-label rows instead of `2944` normalized ledger rows (`23x`) and reducing mean history+policy latency from `222.453668 ms` to `49.093244 ms` (`4.531248x`) with `9.320584 MiB` traced Python peak and `0.0 MiB` CUDA allocation/reservation. The `524288`-token protection run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-dense-label-calibration-source-window.json` stayed in band at `6018.915 tokens/sec`, `train_compute=0.135317 ms/token`, bounded `12/65536` route rows, no observed contention, GPU memory `2030->2029 MiB`, and zero graph/native sequence failures. This keeps calibration evidence local and measured for future LLM-size readout without leaving a broad-normalized production side path.
- Bounded dense-label calibration evaluation source-window note, June 2026: expected-calibration-error style evaluation is useful only after preflight selects finite dense-label evidence; it does not justify reopening every readout-ledger family to find selected hashes. MARULHO therefore routes `dense_label_candidate_calibration_evaluation(...)` through `bounded_snn_dense_label_candidate_calibration_evaluation_source_window.v1`, reading only `dense_label_candidate_events` with CPU archival/lookup/evaluation placement, no raw text payload, no hidden language reasoning, no live tick, no every-token cadence, no mutation/plasticity, and no CUDA archive. The benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-store-state-known-hash-dense-label-evaluation-source-window.json` preserved sample-hash and metric parity for `8` selected samples while checking `128` dense-label rows instead of `2944` normalized ledger rows (`23x`) and reducing mean evaluation latency from `225.545020 ms` to `12.673884 ms` (`17.796046x`) with `9.320584 MiB` traced Python peak and `0.0 MiB` CUDA allocation/reservation. The first `524288`-token protection run at `5906.886 tokens/sec` is retained as below-band variance evidence; the rerun `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-dense-label-evaluation-source-window-rerun.json` stayed in band at `6116.710 tokens/sec`, `train_compute=0.133135 ms/token`, bounded `12/65536` route rows, no observed contention, GPU memory `2030->2030 MiB`, and zero graph/native sequence failures. This keeps the calibration chain single-path from history/policy through evaluation without reviving a broad-normalized production lookup.
- Readout-ledger record-family append note, June 2026: the same bounded local-memory rule now covers the ledger write side. Draft, rollout-replay, emission-review, and dense-label candidate recorders append through `bounded_snn_readout_ledger_record_family_source_window.v1`, reading only the target event family for duplicate checks and persisting only that event family plus its count and timestamp. The benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-record-family-append.json` preserved latest-hash and total-count parity while checking `128` rows instead of `2944` normalized ledger rows (`23x`) and reducing mean append latency from `883.251340 ms` to `57.255420 ms` (`15.426511x`) with CPU archival/lookup/write placement, no CUDA archive, no raw text, no hidden language reasoning, no live tick, and no every-token cadence. The `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-record-family-append.json` stayed same-band at `5966.765 tokens/sec`, `train_compute=0.136141 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `2046->2043 MiB`, and zero graph/native sequence failures. The old broad-normalized single-family append shape is retired as benchmark-only evidence, not kept as production code.
- Autonomous hash-readout chain source-window note, June 2026: sparse SNN readout evidence is useful only when selected, hash-bound, and locally audited; it does not justify reopening every ledger family for each binding or observation event. MARULHO therefore routes autonomous hash-readout binding execution/review through `autonomous_hash_readout_binding_events` only, and bound-observation execution/review through `autonomous_bound_readout_observation_events` only. The benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-autonomous-chain.json` preserved binding/observation hash parity, review-match parity, and total-count parity while checking `512` target-family rows instead of `11776` normalized rows (`23x`) and reducing mean chain latency from `2371.472400 ms` to `110.685950 ms` (`21.425234x`) with CPU archival/lookup/write placement, no CUDA archive, no raw text, no hidden language reasoning, no live tick, and no every-token cadence. The `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-autonomous-chain.json` stayed same-band at `6272.156 tokens/sec`, `train_compute=0.130202 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `2044->2045 MiB`, and zero graph/native sequence failures. The old broad-normalized autonomous binding/observation append and review shape is retired as benchmark-only evidence, not kept as production code.
- Autonomous training/probe chain source-window note, June 2026: the same bounded local-memory rule now covers the downstream autonomous readout training-window and decoder-probe event families. `execute_autonomous_readout_training_window(...)`, `autonomous_readout_training_window_event_review(...)`, `execute_autonomous_decoder_probe(...)`, and `autonomous_decoder_probe_event_review(...)` read only `autonomous_readout_training_window_events` or `autonomous_decoder_probe_events` through `bounded_snn_readout_ledger_record_family_source_window.v1`; broad ledger normalization and `snapshot(limit=0)` summaries are no longer production write/review paths for those families. The expanded benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-training-probe-chain.json` preserved binding, observation, training, and decoder hash/review/count parity while checking `1024` target-family rows instead of `23552` normalized rows (`23x`) and reducing mean chain latency from `4927.213200 ms` to `197.573467 ms` (`24.938638x`) with CPU archival/lookup/write placement, no CUDA archive, no raw text, no hidden language reasoning, no live tick, and no every-token cadence. CUDA was available on the RTX 3060 but unused for archival ledger work (`0 MiB` allocated/reserved). The `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-training-probe-chain.json` stayed same-band at `6057.953 tokens/sec`, `train_compute=0.134322 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, GPU memory `2046->2064 MiB`, and zero graph/native sequence failures; GPU-side contention was observed at `24%`, so this is protection evidence under contention rather than a clean speed ceiling. The old broad-normalized autonomous training/probe append and review shape is retired as benchmark-only evidence.
- Bounded readout replay payload note, June 2026: modern Hopfield-style recall remains useful only as a selected local associative operator, and CLS, continual replay, synaptic tagging/capture, latent replay, and sparse replay all reject turning replay/readout payloads into unbounded background work. MARULHO therefore now bounds caller-supplied SNN readout replay payloads after design selection and before tensor or bridge work: dry-run targets, dry-run traces, and bridge candidate sequences are capped at `32` records and report `bounded_snn_readout_replay_dry_run_target_window.v1`, `bounded_snn_readout_plasticity_preflight_trace_window.v1`, and `bounded_snn_readout_plasticity_bridge_sequence_window.v1`. The benchmark `reports/bounded_replay_window_20260619/readout-replay-target-window.json` passed with dry-run `32/2048`, bridge `32/2048`, `64x` source-work reduction, CPU archival/source/replay placement, no CUDA allocation, no global candidate/score scan, no live tick, no every-token cadence, no raw replay text, and no hidden language reasoning. The `524288`-token hot-path check `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-readout-replay-target-window.json` stayed in the sustained band at `6109.000 tokens/sec`, `train_compute=0.133186 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `2020->2018 MiB`, and zero graph/native sequence failures. The old full-payload caller shape is retired as a report projection, not kept as executable code.
- Bounded language-plasticity replay semantics note, June 2026: the same local-replay rule now applies below the readout-ledger bridge. Modern Hopfield-style association is only a bounded operator over already-selected replay evidence, while CLS, continual replay, synaptic tagging/capture, latent replay, and sparse replay argue against caller-sized replay records or sparse-index lists inside a plasticity review surface. MARULHO therefore bounds `evaluate_spike_language_plasticity_replay(...)`, `run_spike_language_plasticity_replay_experiment(...)`, and `build_spike_language_plasticity_shadow_delta(...)` with `SNN_LANGUAGE_PLASTICITY_REPLAY_WINDOW_LIMIT=32`; shadow delta also caps each sparse side at `16` indices before pair scoring. The benchmark `reports/bounded_replay_window_20260619/language-plasticity-replay-window.json` passed with replay evaluation `32/2048`, replay experiment `32/2048`, shadow pair checks `8192/134217728`, `64x` record-work reduction, `16384x` pair-work reduction, CPU archival/source/replay placement, `14.474813 MiB` traced Python peak allocation, and `0.0 MiB` CUDA allocation/reservation. Two `524288`-token protection runs succeeded in the 6k-ish band (`6025.620` and `5999.398 tokens/sec`) with bounded `12/65536` route rows and zero graph/native sequence failures, but both were marked GPU-contended at `22%`; treat them as live-tick protection evidence under contention, not clean top-speed evidence. The old full-payload semantics path is retired rather than kept as a side implementation.
- Bounded checkpointed application synapse-window note, June 2026: after replay semantics select evidence, the checkpointed mutation boundary must not reopen a caller-sized side path. Structural-plasticity, STC, CLS, continual replay, and sparse replay literature support local growth only when it is selected, audited, checkpointed, and homeostatically bounded. MARULHO therefore routes `SNNLanguagePlasticityApplicationExecutor.apply_live_application(...)` and `regenerate_transition_memory(...)` through `SNN_LANGUAGE_APPLICATION_SYNAPSE_WINDOW_LIMIT=32` before topology validation or checkpoint writes, emits `bounded_snn_language_plasticity_live_application_synapse_window.v1` and `bounded_snn_transition_memory_regeneration_candidate_synapse_window.v1`, and requires the source payload to be untruncated before mutation. The benchmark `reports/bounded_replay_window_20260619/language-application-synapse-window.json` passed with oversized live and regeneration payloads blocked at `32/2048`, no checkpoint calls, no runtime mutation, exact-window live/regeneration payloads still applying `32` synapses through checkpoints, `64x` projected source-work reduction, CPU archival/source/application placement, `1.982166 MiB` traced Python peak allocation, and `0.0 MiB` CUDA allocation/reservation. The clean `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-language-application-synapse-window.json` stayed in band at `6039.734 tokens/sec`, `train_compute=0.134728 ms/token`, bounded `12/65536` route rows, `65526` cached rows, no observed contention, GPU memory `2020->2034 MiB`, and zero graph/native sequence failures. The retired downstream full-payload application behavior is now a projection only, not executable production code.
- Bounded rollout-regeneration facade candidate-window note, June 2026: replay-to-structural-write literature supports explicit permits, but the permit/preflight facade must not materialize a caller-sized candidate list before the bounded executor sees it. MARULHO therefore routes rollout-regeneration permit request, application preflight, and application through the same `SNN_LANGUAGE_APPLICATION_SYNAPSE_WINDOW_LIMIT=32` source-window operator used by the executor. The facade emits `bounded_snn_rollout_regeneration_permit_candidate_synapse_window.v1`, `bounded_snn_rollout_regeneration_application_preflight_candidate_synapse_window.v1`, and `bounded_snn_rollout_regeneration_application_candidate_synapse_window.v1`, requires untruncated candidate payloads before replay-controller or executor calls, and stores these source-window boundaries in Runtime Truth capacity inventory. The benchmark `reports/bounded_replay_window_20260619/rollout-regeneration-facade-candidate-window.json` passed with oversized permit, preflight, and application payloads blocked at `32/2048`, zero replay-controller calls for oversized permits, zero executor/checkpoint calls for oversized applications, exact-window flow still reaching the single executor path, `64x` projected source-work reduction, CPU archival/source/facade/application placement, `1.852119 MiB` traced Python peak, and `0.0 MiB` CUDA allocation/reservation. The first clean `524288`-token run was below band at `5938.820 tokens/sec` and is retained as variance evidence; the rerun `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-rollout-regeneration-facade-candidate-window-rerun.json` stayed in band at `6121.143 tokens/sec`, `train_compute=0.133293 ms/token`, bounded `12/65536` route rows, `65526` cached rows, GPU memory `2031->2031 MiB`, and zero graph/native sequence failures, with sampled GPU contention at `23%`.
- Bounded readout-ledger rollout candidate-window note, June 2026: modern Hopfield-style recall remains an attention-like local memory operator only after a bounded candidate set exists, while CLS, continual-learning replay, STC, and sparse replay reject caller-sized structural-candidate materialization inside replay review. MARULHO therefore routes rollout consolidation design, shadow delta, developmental plasticity review, regeneration proposal adapter, regeneration replay-artifact review, and Replay Controller regeneration-design normalization through the same `SNN_LANGUAGE_APPLICATION_SYNAPSE_WINDOW_LIMIT=32` source-window operator. Each stage requires an untruncated source payload before permit-preview or permit hashing, keeps archival/source-window/gate placement on CPU, reports no global candidate/score scan, no hidden language reasoning, no live tick, and no every-token cadence. The benchmark `reports/bounded_replay_window_20260619/readout-ledger-rollout-candidate-window.json` passed with exact-window rollout evidence reaching permit preview at `32/32`, oversized design/shadow/developmental/adapter/replay-review/controller payloads blocked at `32/2048`, `64x` projected source-work reduction, `9.073439 MiB` traced Python peak, and `0.0 MiB` CUDA allocation/reservation. The clean `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-readout-ledger-rollout-candidate-window.json` stayed in band at `6075.293 tokens/sec`, `train_compute=0.134312 ms/token`, bounded `12/65536` route rows, `65526` cached rows, no observed contention, GPU memory `2031->2043 MiB`, and zero graph/native sequence failures. The former full-list readout-ledger/controller candidate materialization is retired rather than kept as a side path.
- Bounded dense-readout training transition-window note, June 2026: local/STDP-style SNN learning can be useful only when training evidence is selected, finite, checkpointed, and reversible. MARULHO therefore retires caller-sized dense-readout training payload materialization and gives `apply_dense_readout_training_loop(...)` one shared budget with the schema and read model: `SNN_LANGUAGE_DENSE_READOUT_TRAINING_TRANSITION_WINDOW_LIMIT=32` and `SNN_LANGUAGE_DENSE_READOUT_TRAINING_INDEX_WINDOW_LIMIT=32`. The executor emits `bounded_snn_dense_readout_training_transition_source_window.v1` and `bounded_snn_dense_readout_training_transition_index_window.v1`, blocks truncated transition or sparse-index payloads before checkpoint writes, and keeps raw text, hidden language reasoning, live-tick replay, and every-token admission absent. The benchmark `reports/bounded_replay_window_20260619/dense-readout-training-transition-window.json` passed with oversized transition and index payloads blocked at `32/2048`, zero checkpoint calls, zero state mutation, an exact `32`-transition window still producing `32` dense/sparse updates through checkpointed training, `64x` projected transition-record reduction, CPU archival/source/training placement, `5.696876 MiB` traced Python peak, and `0.0 MiB` CUDA allocation/reservation. The clean `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-dense-readout-training-transition-window.json` stayed in band at `6028.820 tokens/sec`, `train_compute=0.135088 ms/token`, bounded `12/65536` route rows, `65526` cached rows, no observed contention, GPU memory `2029->2028 MiB`, and zero graph/native sequence failures.
- Strong-capture admission cadence note, June 2026: synaptic tagging/capture supports marking unusually strong traces for later stabilization, but CLS and sparse replay argue against treating every tag as an immediate archival write. MARULHO therefore keeps device strong-event detection as bounded evidence while cadencing slow-memory admission through `slow_memory_archive_strong_capture_min_interval_tokens` (`16` by default, invalid at `<=1`). `bounded_strong_capture_admission_cadence.v1` archives at most one strong capture per interval, exposes archive/refractory counters and the last archived token in Runtime Truth, keeps archival storage on CPU, and does not run hidden language reasoning. The focused report `reports/bounded_replay_window_20260618/strong-capture-admission-cadence.json` forced all `256` tokens to qualify as strong candidates and archived `17` records instead of the retired every-strong projection of `256`, a `15.058824x` write reduction, while preserving selected strong coverage with max gap `16` and final gap `14`. Bounded mean latency was `1172.027720 ms` over `10` CPU runs with `0.0 MiB` CUDA allocation/reservation. The `524288`-token hot-path check `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-strong-capture-admission-cadence.json` stayed in the sustained band at `6100.415 tokens/sec`, `train_compute=0.133405 ms/token`, bounded `12/65536` route rows, zero graph/native/sequence failures, and flat `2390 MiB` GPU memory; because GPU contention was observed, this is hot-path protection evidence rather than a clean speed ceiling. The slower same-code rerun remains variance evidence, not promotion. This keeps STC-like strong events useful for selected replay windows without reviving every-token slow-memory admission.
- Fixed-cadence slow-memory admission retirement note, June 2026: the same CLS/STC/sparse-replay boundary also rejects preserving fixed interval writes as a second per-token fallback path after burst cadence had already been deferred. `train_step` now archives the first token and selected strong captures only; ordinary `slow_memory_archive_interval_tokens` hits record `cadence_deferred` through the cognitive boundary controller and do not call `DualMemoryStore.update(...)`. The focused report `reports/bounded_replay_window_20260620/slow-memory-fixed-cadence-admission-retired.json` passed with `1` bounded archive versus `17` retired fixed-cadence writes over `256` tokens (`17x` write reduction), deferred count `16`, first-token retention intact, CPU archival placement, no global scans, no active replay computation, and no hidden language reasoning. The refreshed strong-capture report `reports/bounded_replay_window_20260620/strong-capture-admission-cadence-after-fixed-cadence-retirement.json` still archived `17` selected strong captures versus `256` retired every-strong writes (`15.058824x`), showing the useful selected path remains. The first `524288`-token protection run after retirement is rejected as primary evidence at `5758.051 tokens/sec` because the same-code rerun reached the maintained band at `6043.321 tokens/sec`, `train_compute=0.134537 ms/token`, bounded `12/65536` route rows, `65526` cached rows, fixed-cadence deferred count `2048`, zero graph/native sequence failures, and flat RTX 3060 memory at `1958 MiB`; observed borderline GPU contention means this is protection evidence, not a new speed ceiling.
- Bounded concept-frontier metric note, June 2026: source acquisition can use modern Hopfield-style local matching as a planning signal, but the same CLS/continual-replay/STC boundary rejects an archive-wide memory sweep. MARULHO therefore replaces `concept_frontier_metrics(...)` direct iteration over every `slow_routing_keys` entry with `bounded_concept_frontier_memory_metrics.v1`: the probe-bank signature asks the routing index for candidate buckets, the memory store returns a capped bucket-indexed window, and autonomy planning scores novelty, uncertainty, and support only for those selected entries. The benchmark `reports/bounded_replay_window_20260617/concept-frontier-bounded-scope.json` kept the diagnostic full-scan top-1, scored `64/8192` entries, reduced mean metric latency from `658.116 ms` to `5.040 ms`, and kept deltas within gate (`novelty_delta=0.0`, `uncertainty_delta=0.0`, `support_delta=0.015893`). The matching 65536-column hot-path run processed `262144` tokens at `6148.846 tokens/sec`, with `train_compute=0.131437 ms/token`, `route_input_rows_scored=12/65536`, `state_transition_cached_count=65526`, no observed contention, flat `1805 MiB` GPU memory, and zero graph/native/sequence failures. This keeps acquisition recall useful for future LLM-size memory without hidden full-archive reasoning or live-tick work.
- Bounded source-bank concept-frontier probe signature note, June 2026: the same research boundary now applies before source acquisition asks the routing index for frontier candidates. Modern Hopfield-style recall can average a local probe signature, but CLS, continual replay, STC, and sparse replay reject letting a large source bank turn that signature into an unbounded pre-scan. `concept_frontier_metrics_with_report(...)` and `candidate_semantic_signature(...)` therefore sample an evenly spaced `16`-probe source-bank signature window, report `source_probe_count`, `source_probe_window_limit`, selected probe indices, and budget fields, then score only the capped bucket-indexed candidate memory window. The direct report `reports/bounded_replay_window_20260618/concept-frontier-source-probe-window-bounded.json` used `64` source probes over a `16384`-entry archive, sampled `16` probes, scored `64` memory entries instead of `16384`, preserved the diagnostic full-scan top-1, kept `novelty_delta=0.0000706911`, `uncertainty_delta=0.0`, and `support_delta=0.0219844`, and reduced mean latency from `1556.602 ms` to `7.637 ms` (`203.829x`). The paired hot-path check compared the last committed runtime-global-hook slice at `6307.437 tokens/sec` with the source-probe current tree at `6303.548 tokens/sec` on the same `524288`-token shape; both stayed bounded at `12/65536` route rows, `65526` cached transition rows, zero graph/native/sequence failures, no observed contention, and flat `1789 MiB` current-tree GPU memory. Earlier current-tree hot-path runs at `5935.082`, `5753.405`, and `5798.030 tokens/sec` are rejected as primary evidence because the paired baseline/current pass showed the same-code throughput protection only after the hardware reached the sustained power state.
- Merged source-bank memory recall note, June 2026: source-bank semantic gap planning now treats modern-Hopfield-style recall as one bounded local associative operator over a selected bank window, not as per-probe query replay or a hidden archive read. `bank_memory_matches_with_report(...)` samples capped source-bank probes, unions their routing-index bucket ids, collects one CPU bucket-indexed candidate window capped at `192`, vector-scores probes against that local window, and records `bounded_source_bank_memory_match.v1` with `merged_probe_candidate_window=true`, `per_probe_query_match_call_count=0`, candidate/window budgets, raw text loaded only for returned matches, CPU archival/score placement, no global scans, `runs_live_tick=false`, `runs_every_token=false`, and `language_reasoning=false`. The benchmark `reports/bounded_replay_window_20260622/source-bank-merged-probe-window.json` preserved selected indices against the retired per-probe diagnostic path (`quality.min=1.0`), scored `192` candidates and `1536` local similarities, reduced raw text payload loads from `32` to `4`, and improved mean latency from `560.177 ms` to `106.543 ms` (`5.258x`) over a `65536`-entry store. CUDA was available but archival recall allocated/reserved `0.0 MiB`, with `3.059 MiB` traced Python peak. The 65536-column 524288-token hot-path run at `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-source-bank-merged-probe-window.json` reached `6129.933 tokens/sec`, `train_compute=0.132126 ms/token`, `prepare_training=0.006695 ms/token`, `finalize_total=0.006421 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, mild GPU contention (`21%` against a `20%` threshold), flat `1763 MiB` GPU memory, and zero graph/native/sequence failures. This keeps source selection memory useful without adding every-token replay work or GPU-resident archival metadata.
- Source-bank per-probe baseline-removal note, June 2026: the per-probe query-memory diagnostic aggregation is no longer kept as benchmark-local executable code, and `bounded_source_bank_memory_match.v1` no longer projects the retired per-probe call-count field. The external maintained-only report `..\..\MARULHO_reports\bounded_replay_window_20260623\source-bank-legacy-baseline-removed.json` passed with expected selected indices, `192` candidates, `1536` local similarities, `4` returned text payloads, `134.160628 ms` bounded mean latency, CPU archival/score placement, no CUDA archival allocation, and `retired_per_probe_query_match_absence.active_report_field_present=false`. The paired clean long-run gate `..\..\MARULHO_reports\bounded_replay_window_20260623\hotpath-active-pressure-65536-524288-i32-legacy-benchmark-baselines-removed-default-nosample.json` processed `524288` tokens at `6098.818 tokens/sec`, with `train_compute=0.132657 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, CUDA active on RTX 3060, GPU memory `2069->2068 MiB`, no observed contention, and zero graph/native/sequence failures.
- Bounded runtime concept memory lookup note, June 2026: cadenced source/feed concept observation also follows the same local-recall boundary. MARULHO retires service-owned direct reads of `slow_routing_keys`, `slow_texts`, `slow_raw_windows`, and STC metadata for runtime concept observation; `OperatorInteractionRuntime` now calls `DualMemoryStore.resolve_runtime_concept_memory_matches(...)`, which accepts only trainer-provided `memory_index` evidence, caps the observation batch, caches duplicate payload lookups, and records `bounded_runtime_concept_memory_lookup.v1`. The report is explicit that this can run in the live runtime observation cadence (`runs_live_tick=true`) but not every token (`runs_every_token=false`), uses CPU archival/score placement, has no global candidate or score scan, and does no hidden language reasoning. The benchmark `reports/bounded_replay_window_20260618/runtime-concept-memory-lookup-bounded.json` used a `65536`-entry synthetic archive with `512` observed indices and `64` unique payloads, preserved selected-index parity (`quality.min=1.0`), reduced payload reads from `512` to `64` with `448` cache hits, and improved mean lookup latency from `47.156 ms` to `6.380 ms` (`7.391x`). The 65536-column 524288-token protection run `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-runtime-concept-memory-lookup.json` stayed in the sustained band at `6237.075 tokens/sec`, with `train_compute=0.131104 ms/token`, `concept_observation=0.000474 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `1809->1861 MiB`, and zero graph/native/sequence failures. This makes existing cadenced concept recall reportable without turning it into an archive scan or every-token slow-memory admission path.
- Bounded ConceptStore signature lookup note, June 2026: semantic observation must also keep recall local to selected evidence. Modern Hopfield-style associative recall is acceptable here only after query/source/concept evidence has already selected memory indices; complementary learning systems and continual replay still reject unregulated archive traversal inside the live source loop. MARULHO therefore replaces the old `ConceptStore._memory_signature(...)` list-materializing archive access with `bounded_concept_memory_signature_lookup.v1`: each evidence source is capped at `8` unique indices with a `32`-reference scan budget, archival arrays are direct-indexed on CPU, and the concept summary reports no archive list materialization, no global candidate scan, no global score scan, no archival mutation, and no hidden language reasoning. The benchmark `reports/bounded_replay_window_20260617/concept-signature-lookup-bounded.json` used `65536` entries and `512` eight-index evidence groups, preserved diagnostic legacy signature quality (`min cosine=0.9999998212`), removed `4096` archive list materializations, and reduced mean lookup latency from `12.490 ms` to `1.454 ms` (`8.591x`). The clean 65536-column hot-path gate processed `262144` tokens at `6143.768 tokens/sec`, with `train_compute=0.131822 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, flat `1746 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures; two longer `524288`-token same-code runs reached `6183.670` and `6196.447 tokens/sec` but are secondary because pre-measurement GPU contention was observed after prewarm.
- Bounded frontier-gap planner note, June 2026: semantic gap planning may use modern Hopfield-style local association only after a bounded candidate window exists; CLS, continual-learning replay, synaptic tagging/capture, and sparse replay reject the old shape where a planner materializes the whole text archive as a background reasoning surface. MARULHO therefore replaces the archive-wide `slow_raw_windows` planning scan with `bounded_frontier_gap_selection.v1`: `DualMemoryStore.collect_frontier_gap_indices(...)` returns a capped CPU recency or bucket candidate window, raw text payloads are loaded only for those selected candidates, archival metadata remains CPU-resident, and reports state `runs_live_tick=false`, `global_candidate_scan=false`, `global_score_scan=false`, and `language_reasoning=false`. The report-dropping `frontier_gap_terms(...)` helper is deleted, and a memory store without the bounded collector now returns an empty reported fallback instead of reading prefix archive text. The benchmark `reports/bounded_replay_window_20260617/frontier-gap-bounded.json` used `65536` entries, scored `192` candidates, preserved expected and diagnostic legacy terms with `quality.min=1.0`, reduced mean latency from `217.530 ms` to `9.073 ms` (`23.975x`), and passed a missing-collector gate with zero candidates, zero text payloads, and no global scans. The fresh 65536-column hot-path gate `reports/bounded_replay_window_20260618/hotpath-active-pressure-65536-524288-i32-frontier-gap-collector-required.json` processed `524288` tokens at `6233.085 tokens/sec`, with `train_compute=0.131067 ms/token`, `concept_observation=0.000470 ms/token`, bounded `12/65536` route rows, `65526` cached transition rows, no observed contention, GPU memory `1844->1840 MiB`, and zero graph/native/sequence failures. This keeps future LLM-size recall as selected slow-window evidence, not hidden language replay.
- Bounded recent tag/anchor setup note, June 2026: synaptic tagging/capture supports marking traces for later stabilization, but MARULHO should not implement that as an archive-wide recent-memory walk. The memory store now maintains a CPU recency index and records `bounded_recent_memory_window.v1`; recent tagging records `bounded_recent_memory_tag.v1`, and anchor capture records `bounded_recent_anchor_capture.v1` while requiring bucketed entries before a column anchor is created. The synthetic report `reports/bounded_replay_window_20260617/synthetic-recent-anchor-window.json` kept replay recall and prototype gates passing, used `candidate_window_limit=256` for tag and anchor setup, touched `14` indexed entries, captured `4` anchors, reported no global score/candidate scan, kept `runs_live_tick=false`, and kept archival storage on CPU. The matching 65536-column hot-path run processed `262144` tokens at `6228.243 tokens/sec`, with `route_input_rows_scored=12/65536`, `state_transition_cached_count=65526`, no observed contention, flat `1846 MiB` GPU memory, and zero graph/native/sequence failures. This removes the old timestamp/bucket scan shape while keeping STC-like setup selected, measured, and off the live tick for future LLM-size memory.
- Replay-score helper retirement note, June 2026: sparse replay and modern Hopfield-style local recall both require selected candidate sets before scoring. MARULHO therefore deletes the leftover public `DualMemoryStore.replay_scores(...)` full-buffer helper and keeps replay priority available only through `replay_scores_for_indices(...)` after a bounded window has selected candidates. The synthetic report `reports/bounded_replay_window_20260617/synthetic-replay-score-helper-retired.json` kept replay recall and prototype gates passing with `2` bounded updates and `0` global fallback cycles. The matching 65536-column hot-path run processed `262144` tokens at `6211.859 tokens/sec`, with `route_input_rows_scored=12/65536`, `state_transition_cached_count=65526`, no observed contention, flat `1852 MiB` GPU memory, and zero graph/native/sequence failures. This removes old implementation code rather than preserving a tempting archive-wide scorer.
- Score tensor helper retirement note, June 2026: after deleting the public replay-priority helper, MARULHO still had `maintenance_scores(...)`, `consolidation_scores(...)`, `repair_scores(...)`, `fragility_scores(...)`, and unused capture/tag/PRP tensor builders that produced archive-wide score tensors. That shape conflicts with the bounded replay contract from modern Hopfield-style local recall, complementary learning systems, continual replay, and synaptic tagging/capture: select a local replay window first, then score inside it. The helpers are now removed. Production selection scores only candidate indices through `_score_replay_index(...)`, and the later runtime hook cleanup also removes the private `allow_global_score_scan` escape hatch; retired full-scan comparisons are benchmark-local only. The synthetic report `reports/bounded_replay_window_20260617/synthetic-score-tensor-helpers-retired.json` kept recall/prototype gates passing with `2` bounded updates, `4` bounded cycles, `16` scored/selected entries, and `0` global fallback cycles. The accepted 65536-column hot-path rerun processed `262144` tokens at `6151.952 tokens/sec`, with `train_compute=0.132119 ms/token`, `route_input_rows_scored=12/65536`, `state_transition_cached_count=65526`, no observed contention, flat `1805 MiB` GPU memory, and zero graph/native/sequence failures. This retires old implementation code instead of leaving a production-looking archive-wide tensor API for future LLM-size memory.
- Bounded awake-ripple tagging note, June 2026: synaptic tagging/capture and hippocampal ripple literature support tagging traces for later consolidation, but in MARULHO that mechanism must be selected and scheduler-owned. `ripple_tag_awake(...)` now requires awake bucket scope for production tagging, caps candidates through the CPU bucket/recency index, reports `bounded_awake_ripple_tag.v1`, and returns an empty report if a caller omits scheduler context. The old scalar/vector recent-memory scan was removed from `DualMemoryStore`; benchmark-local retired baselines carry any full-scan comparison. The direct report `reports/bounded_replay_window_20260617/awake-ripple-bounded-scope-8192-i256.json` measured the diagnostic global vector path at `1.433332 ms` mean and the wake-bucket scoped path at `1.091997 ms` mean over `256` iterations (`1.312579x`), with scoped `ripple_scalar_scan_count=0`, `ripple_vector_scan_count=0`, `ripple_awake_bucket_scan_count=256`, and `last_ripple_awake_candidate_count=10`. The synthetic report `reports/bounded_replay_window_20260617/synthetic-awake-ripple-bounded-scope.json` kept positive replay recall/prototype gates passing, and the 65536-column `524288`-token hot-path run processed `6152.328 tokens/sec` with `train_compute=0.131727 ms/token`, bounded `12/65536` route rows, flat `2013 MiB` GPU memory, no observed contention, and zero graph/native/sequence failures. This keeps ripple tagging a local replay-priority operator, not an archive-wide memory scan or hidden language reasoning path.
- Bounded autonomous language-surface ledger note, June 2026: the same modern-Hopfield/CLS/continual-replay/STC/sparse-replay boundary now covers the last operator-facing language-surface ledger stages. `execute_autonomous_text_surface_materialization(...)` and `autonomous_text_surface_materialization_event_review(...)` read only `autonomous_text_surface_materialization_events`; `execute_autonomous_bounded_language_surface_commit(...)` and its review read only `autonomous_bounded_language_surface_commit_events`. Both executor/review pairs return `bounded_snn_readout_ledger_record_family_source_window.v1`, keep archival/source/review metadata CPU-resident, state `runs_live_tick=false`, `runs_every_token=false`, and `language_reasoning=false`, and update only the target family plus the single current pointer. The benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-language-surface-chain.json` preserved hash, review, total-count, and current-pointer parity across the full ten-family autonomous language-surface chain while checking `2560` target-family rows instead of `58880` broad-normalized rows (`23x`) and reducing mean chain latency from `11175.229267 ms` to `525.534133 ms` (`21.264517x`). The `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-language-surface-chain.json` stayed in the maintained band at `5994.060 tokens/sec`, `tick_duration_ms.p95=21.991`, `train_compute=0.135570 ms/token`, `prepare_training=0.007057 ms/token`, `finalize_total=0.006414 ms/token`, bounded `12/65536` route rows, `10` output candidates, `65526` cached rows, CUDA runtime on RTX 3060, GPU memory `2044->2059 MiB`, and zero graph/native sequence failures. This retires the broad-normalized production path instead of leaving old side code around; benchmark-local broad comparison remains only retired-path evidence.
- Bounded autonomous language-generation ledger note, June 2026: bounded language-surface use and SNN language-generation now follow the same one-path record-family window. `execute_autonomous_bounded_language_surface_use(...)` and its review read only `autonomous_bounded_language_surface_use_events`; `execute_autonomous_snn_language_generation(...)` and its review read only `autonomous_snn_language_generation_events`. Both pairs return `bounded_snn_readout_ledger_record_family_source_window.v1`, keep archival/source/review metadata CPU-resident, avoid live-tick and every-token work, and do not perform hidden language reasoning or text replay. The benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-language-generation-chain.json` preserved hash, review-match, total-count, and current-pointer parity across the expanded autonomous language-generation chain while checking `3072` target-family rows instead of `70656` broad-normalized rows (`23x`) and reducing mean chain latency from `13505.919533 ms` to `631.221 ms` (`21.396499x`). The clean `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-language-generation-chain.json` stayed in band at `6074.417 tokens/sec`, `tick_duration_ms.p95=21.376`, `train_compute=0.133727 ms/token`, `prepare_training=0.007038 ms/token`, `finalize_total=0.006252 ms/token`, bounded `12/65536` route rows, `10` output candidates, `65526` cached rows, no observed contention, CUDA runtime on RTX 3060, GPU memory `2044->2047 MiB`, and zero graph/native sequence failures. The old broad-normalized language-use/generation production shape is retired; only benchmark-local broad comparison remains as evidence.
- Bounded autonomous language-readout ledger note, June 2026: SNN language decoding, readout-surface, readout-memory, readout-consolidation, and readout-structural-plasticity executor/review use the same single-family source-window path instead of calling `_normalized_state()` to append or review one downstream language/readout event. Each pair returns `bounded_snn_readout_ledger_record_family_source_window.v1`, keeps archival/source/review metadata CPU-resident, avoids live-tick and every-token work, and does not replay or reason through text. Checkpoint load/save keeps canonical readout-ledger fields and drops noncanonical readout-ledger state. The readout-structural canonicalization report `reports/bounded_replay_window_20260622/snn-readout-ledger-normalization-readout-structural-canonical.json` passed with bounded mean `568.337767 ms` versus the benchmark-local legacy diagnostic `7518.428000 ms` (`16x` source-work reduction), while the downstream autonomous-chain bounded mean stayed `967.423500 ms` versus `21131.022800 ms`. The `524288`-token hot-path protection run `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-readout-structural-canonical.json` stayed same-band at `5885.572 tokens/sec`, p95 `22.879800 ms`, `train_compute=0.136747 ms/token`, `prepare_training=0.007215 ms/token`, `finalize_total=0.006836 ms/token`, bounded `12/65536` route rows, no observed contention, GPU memory `1741->1970 MiB`, and zero graph/native/sequence failures. Prewarm was explicit slow-path setup at `418.781 s` and completed before measured throughput. The older broad-normalization benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-thought-structural-chain.json` remains retired-path evidence for the full downstream chain. The `autonomous_snn_language_thought_memory_*`, `autonomous_snn_language_thought_consolidation_*`, and `autonomous_snn_language_thought_structural_plasticity_*` production naming paths are retired; only benchmark-local broad/legacy comparisons remain.
- Bounded synapse-provenance event-map note, June 2026: readout-derived synapse audits now use requested-hash local recall rather than normalizing every retained readout-ledger family before provenance validation. `synapse_provenance_audit(...)` collects only hashes referenced by `synapse_provenance_by_key`, reads `events` through `bounded_snn_readout_evidence_event_map_source_window.v1`, and records the source-window report in the promotion gate. The benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-synapse-provenance-map.json` preserved requested event-map hash parity while checking `128` `events` rows instead of `2944` broad-normalized rows (`23x`) and reducing event-map mean latency from `319.823233 ms` to `13.972533 ms` (`22.889424x`), with CPU archival/lookup placement, no CUDA archive, no raw text, no hidden language reasoning, no live tick, and no every-token cadence. The clean `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-synapse-provenance-map.json` stayed in band at `5994.111 tokens/sec`, `tick_duration_ms.p95=21.885`, `train_compute=0.135406 ms/token`, `prepare_training=0.007135 ms/token`, `finalize_total=0.006412 ms/token`, bounded `12/65536` route rows, `10` output candidates, `65526` cached rows, no observed contention, CUDA runtime on RTX 3060, GPU memory `1980->1976 MiB`, and zero graph/native sequence failures. The old broad-normalized synapse-provenance event-map production shape is retired; broad comparison remains benchmark-only evidence.
- Bounded emission-review history note, June 2026: operator display history may expose reviewed bounded text, but it must not become broad ledger recall or hidden replay-text reasoning. `emission_review_history(...)` now reads only `emission_review_events` through `bounded_snn_emission_review_history_source_window.v1`, reports source/window/total counts, keeps archival/lookup metadata CPU-resident, and states `runs_live_tick=false`, `runs_every_token=false`, and `language_reasoning=false`. The benchmark `reports/bounded_replay_window_20260619/snn-readout-ledger-normalization-emission-history.json` preserved review-hash and text-hash parity while checking `128` rows instead of `2944` broad-normalized rows (`23x`) and reducing mean display-history latency from `345.815600 ms` to `25.503433 ms` (`13.559570x`). The clean `524288`-token hot-path run `reports/bounded_replay_window_20260619/hotpath-active-pressure-65536-524288-i32-emission-history.json` stayed in band at `6051.817 tokens/sec`, `tick_duration_ms.p95=21.323`, `train_compute=0.134300 ms/token`, `prepare_training=0.006972 ms/token`, `finalize_total=0.006364 ms/token`, bounded `12/65536` route rows, `10` output candidates, `65526` cached rows, no observed contention, CUDA runtime on RTX 3060, flat GPU memory at `1972 MiB`, and zero graph/native sequence failures. The old broad-normalized emission-history production shape is retired; broad comparison remains benchmark-only evidence.
- Structural candidate transaction note, June 2026: structural plasticity, reward-modulated STDP, homeostatic plasticity, sparse GPU structural plasticity, event-based delay learning, and self-growing-network work all point toward local repeated evidence and budgeted growth rather than one-shot surprise. MARULHO implements that as a checkpointed candidate gate: training-owned tickets bind repeated failure or prune/sleep pressure to cost/usefulness and Runtime Truth evidence; the final executor recomputes candidate-bound preflight hashes and tombstones rejected or retired candidates with rollback artifacts. This keeps research inspiration below MARULHO's operator-reviewed rollback gate rather than making literature claims automatic runtime authority.
- Bounded replay-dataset preview note, June 2026: modern Hopfield recall supports associative lookup inside an already-selected local memory, not a transformer-style global memory scan. Complementary learning systems and continual-learning replay support separating fast online experience from slower integration, while synaptic tagging/capture supports stabilizing selected evidence rather than admitting or replaying everything. MARULHO applies those boundaries to the checkpoint replay-dataset preview: it reads at most `50/64` retained runtime episode traces, indexes at most `64/256` retained replay samples, and keeps at most `16` already-sanitized candidate links per sample. `bounded_replay_dataset_preview_source_window.v1` and `bounded_replay_dataset_sample_link_source_window.v1` report CPU archival/source placement, no global candidate or score scan, no raw replay text, no hidden language reasoning, no mutation/plasticity/training, `runs_live_tick=false`, and `runs_every_token=false`. The old export also had a silent count/payload mismatch: it reported up to `50` items while the generic sanitizer returned only `16`; the maintained export sanitizer now returns the declared bounded item window without widening stored replay-sample candidate payloads beyond `16`. The 15-run benchmark `reports/bounded_replay_window_20260620/replay-dataset-source-window.json` preserved all `50` selected targets and replay-link coverage against a diagnostic full-retained walk, reduced replay-sample and selected-candidate source work from `256` to `64` and `4096` to `1024` (`4x`), and recorded `2006.587280 ms` mean slow-path cost, `2.842 MiB` traced Python peak, CUDA available but unused, and no GPU-resident archival metadata. The exact-tree clean `524288`-token protection run stayed in the noisy 6k-ish band at `5923.269 tokens/sec`, `tick_duration_ms.p95=22.446`, `train_compute=0.136941 ms/token`, bounded `12/65536` route rows, `65526` cached rows, no observed contention, GPU memory `3554->3541 MiB`, and zero graph/native sequence failures. This is a measured export/review window, not a replay promotion or live cognition path. Research sources: [modern Hopfield networks](https://arxiv.org/abs/2008.02217), [complementary learning systems](https://pubmed.ncbi.nlm.nih.gov/7624455/), [brain-inspired continual replay](https://www.nature.com/articles/s41467-020-17866-2), and [synaptic tagging and capture](https://www.nature.com/articles/385533a0).
- Replay-dataset candidate endpoint retirement note, June 2026: the same literature argues against preserving a second candidate-preview export beside replay-plan and bounded dataset preview. MARULHO therefore removes `GET /terminus/replay-dataset/candidates`, its response schema, RuntimeFacade method, service benchmark summary, and runtime helper. Candidate evidence now has one public source of truth: `/terminus/replay-plan` for replay-plan candidates, while `/terminus/replay-dataset/preview` and `/terminus/replay-dataset/bundle` carry dataset/export source windows. The preview source-window report now records `candidate_context_source=replay_plan_summary_inside_dataset_preview`, `retired_public_candidate_preview_endpoint=/terminus/replay-dataset/candidates`, and `replacement_candidate_endpoint=/terminus/replay-plan`. Focused API coverage verifies the retired route returns `404`; the refreshed report `reports/bounded_replay_window_20260622/replay-dataset-candidates-retired-source-window.json` passed with `50/50` selected target and link parity, `4x` replay-sample and selected-candidate source-work reduction, CPU archival/source/link placement, no live-tick/every-token work, no replay text or hidden language reasoning, CUDA available but unused, `3.53 MiB` Python traced peak, and mean bounded preview cost `1851.868400 ms`. The paired `524288`-token run `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-replay-dataset-candidates-retired.json` stayed in band at `6131.415 tokens/sec`, p95 `20.720 ms`, `train_compute=0.132230 ms/token`, bounded `12/65536` route rows, `65526` cached rows, no observed contention, RTX 3060 memory `1758->1760 MiB`, and zero graph/native/sequence failures.
- Replay-dataset history and anchor-source fallback retirement note, June 2026: modern Hopfield-style recall, CLS, continual-learning replay, synaptic tagging/capture, and sparse replay all favor selected local replay windows over duplicate export surfaces or archive-shaped fallbacks. MARULHO therefore removes `GET /terminus/replay-dataset/history` because `/terminus/replay-sample/history` is the one replay-history path, keeps dataset-specific work on preview/bundle, and exposes computed `source_window` evidence on replay-plan, runtime-trace export, and replay-dataset preview API responses instead of dropping it in response models. The service benchmark report `reports/bounded_replay_window_20260622/service-benchmark-replay-dataset-history-retired.json` passed with the dataset-history endpoint and `replay_dataset_history_summary` absent, slow-path endpoints reduced to `5`, hot-path budget passing (`p95=340.668 ms`, total `664.890 ms`), and preview/bundle summaries intact. The same slice retires the non-reversible anchor-source materialization fallback: `replay_anchor_window.py` now blocks non-reversible sources with `fallback_reason=non_reversible_anchor_bucket_source` and `anchor_bucket_source_materialized_count=0` instead of building `list(anchors)`. The anchor report `reports/bounded_replay_window_20260622/sleep-replay-anchor-nonreversible-fallback-retired.json` passed over `8192` anchors with `16` bounded reads, `0` materialized entries, `1.0` newest-anchor and selected-bucket hit rates, CPU archival/source placement, `0.0 MiB` CUDA delta, and no live/every-token work. The long protection run `reports/bounded_replay_window_20260622/hotpath-active-pressure-65536-524288-i32-replay-dataset-history-anchor-retired.json` processed `524288` tokens at `6151.826 tokens/sec`, p95 `20.696 ms`, `train_compute=0.132211 ms/token`, bounded `12/65536` route rows, `65526` cached rows, zero graph/native/sequence failures, RTX 3060 memory `1766->1769 MiB`, and sampled borderline GPU contention at `20%`, so the claim is throughput protection rather than a new speed ceiling.
- Bounded runtime trace export and replay-sample summary note, June 2026: the same boundary now covers the remaining operator trace/export status path. `export_runtime_trace_examples(...)` no longer walks all retained runtime episode traces before applying an endpoint filter, `LivingStatusCore` builds self-model traces from a bounded status window, `_runtime_feedback_summary_locked()` reads a bounded recent trace/action window, and `_replay_sample_summary_locked()` reports `bounded_replay_sample_summary_source_window.v1` over at most `64/256` retained replay sample records. The generic runtime-trace export sanitizer now uses bounded `islice` for mappings and sequences instead of materializing then trimming. The benchmark `reports/bounded_replay_window_20260620/replay-dataset-runtime-trace-export-summary-source-window.json` passed with `bounded_runtime_trace_export_source_window.v1` over `50/64` traces, replay sample summary `64/256`, replay sample links `64/256`, selected-candidate links `1024/4096`, exact `50/50` selected target and trace-export ID parity against the diagnostic window, CPU archival/source placement, no raw replay text, no hidden language reasoning, no live-tick or every-token work, no mutation/plasticity/training, and no GPU-resident archival metadata. Direct bounded summary latency was `7.440207 ms` on the small retained fixture versus `0.794520 ms` for the simplified diagnostic full summary, so this is a source-budget and scale-safety retirement, not a small-fixture speed claim. The accepted long hot-path rerun `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-runtime-trace-export-summary-source-window-rerun.json` processed `524288` tokens at `6047.311 tokens/sec`, `tick_duration_ms.p95=21.357`, `train_compute=0.134598 ms/token`, bounded `12/65536` route rows, `65526` cached rows, `state_transition_runs_all_columns=false`, no observed contention, flat RTX 3060 memory at `1911 MiB`, and zero graph/native sequence failures.
- Bounded readout-ledger snapshot note, June 2026: the same slow-window rule now applies to service snapshots of SNN readout-ledger history. `SNNLanguageReadoutEvidenceLedger.snapshot(...)` no longer calls `_normalized_state()` and then trims output; it reads only the event families returned by the snapshot through `bounded_snn_readout_ledger_snapshot_source_window.v1`, capped by the requested display limit and the ledger retention limit. The retained count summary remains visible, but returned rows, unique hash counts, and source-window evidence are scoped to the snapshot window. The 25-run benchmark `reports/bounded_replay_window_20260620/snn-readout-ledger-snapshot-source-window.json` read `260` rows instead of `2944` in the diagnostic retired normalization model (`11.323077x` less source work), reduced mean snapshot latency from `393.040600 ms` to `67.334088 ms` (`5.837171x`), preserved newest-first quality and retained-count parity, used `0.575356 MiB` traced Python peak, and used `0.0 MiB` CUDA allocation/reservation. The `524288`-token protection run `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-ledger-snapshot-source-window.json` processed `524288` tokens at `6443.960 tokens/sec`, `train_compute=0.127084 ms/token`, `prepare_training=0.006251 ms/token`, `finalize_total=0.005922 ms/token`, prewarmed in `245.711 s`, kept route scoring at `12/65536`, cached `65526` transition rows, reported no observed contention, and kept RTX 3060 memory flat at `1899 MiB`. This retires another broad control-plane read path without making ledger snapshots a replay operator, language reasoning path, or live-tick workload.
- Bounded applied-synapse provenance status note, June 2026: modern Hopfield-style recall, CLS, continual replay, synaptic tagging/capture, and sparse replay all support selecting a bounded memory/replay window before asking associative or integrity questions. MARULHO applies that rule to applied-synapse status: `StatusReadModel._snn_readout_applied_synapse_provenance()` no longer scans every retained sparse weight and provenance row before reporting audit readiness. It emits `bounded_snn_status_applied_synapse_provenance_source_window.v1`, reads at most `32` sparse-weight keys and `32` provenance rows, keeps archival/lookup metadata CPU-resident, reports no live tick, no every-token cadence, no replay, no raw text, and no hidden language reasoning, and blocks exact audit readiness when the source window is truncated. The benchmark `reports/bounded_replay_window_20260620/status-applied-synapse-provenance-source-window.json` read `64` rows instead of `4096` in the diagnostic retired broad scan (`64x` less source work), reduced mean latency from `66.313336 ms` to `3.242332 ms` (`20.452358x`), preserved bounded-window replay-regeneration provenance health, and used `0.0 MiB` CUDA allocation/reservation. The first long protection run succeeded at `5875.245 tokens/sec` but is retained as secondary variance; the accepted rerun `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-status-applied-synapse-provenance-source-window-rerun.json` processed `524288` tokens at `6350.288 tokens/sec`, `train_compute=0.128968 ms/token`, `prepare_training=0.006470 ms/token`, `finalize_total=0.005945 ms/token`, prewarmed in `241.741 s`, kept route scoring at `12/65536`, cached `65526` transition rows, reported no observed contention, and kept RTX 3060 memory flat at `1936 MiB`. This retires the broad status readiness scan while keeping exact provenance integrity in an explicit slow audit window, not in the live tick or a hidden replay-text loop.
- Bounded applied-synapse provenance audit note, June 2026: exact `synapse_provenance_audit(...)` now applies the same selected-source rule before asking the readout ledger for provenance hashes. The audit emits `bounded_snn_readout_synapse_provenance_audit_source_window.v1`, reads at most `64` applied sparse-weight/provenance rows from CPU archival state, requests ledger rows only for those selected hashes, reports no global candidate or score scan, no raw replay text, no hidden language reasoning, no live tick, no every-token work, no mutation/plasticity, and no GPU-resident archival metadata, and blocks exact review when the applied-synapse source window is truncated. The benchmark `reports/bounded_replay_window_20260620/synapse-provenance-audit-source-window.json` matched the diagnostic first source window while reading `64` bounded source rows instead of `4096` diagnostic records and `2048` materialized rows (`32x` less source work), reducing mean audit latency from `259.221928 ms` to `75.262088 ms`, using `1.909667 MiB` traced Python peak allocation, and using no GPU allocation. The accepted protection rerun `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-synapse-provenance-audit-source-window-rerun.json` processed `524288` tokens at `6441.166 tokens/sec`, `tick_duration_ms.p95=19.527`, `train_compute=0.127184 ms/token`, `prepare_training=0.006068 ms/token`, `finalize_total=0.005747 ms/token`, route scoring `12/65536`, `65526` cached transition rows, no observed contention, flat RTX 3060 memory at `1866 MiB`, and zero graph/native sequence failures. The full applied-synapse audit scan is retired from production and kept only as benchmark-local diagnostic evidence.
- Bounded transition-memory status projection note, June 2026: the same modern Hopfield/CLS/continual-replay/STC/sparse-replay boundary now covers all transition-memory status projections. Capacity pressure, dense readout tensor integrity, applied-synapse provenance, and rollout/server binding read through one bounded newest-first CPU source-window helper instead of materializing all retained `sparse_transition_weights` and `synapse_provenance_by_key` rows per status question. The status-local insertion-order helper is removed, so status and plasticity runtime-state views share one source-window rule. Each projection reads at most `32 + 32` source rows, reports retained/source/truncated counts, blocks exact review readiness when truncated, and records no global scan, no replay, no mutation/plasticity, no raw text, no hidden language reasoning, no live tick, and no every-token cadence. The benchmark `reports/bounded_replay_window_20260620/status-transition-memory-source-window.json` read `256` bounded rows across four projections instead of `10240` rows in the benchmark-local retired repeated broad projection (`40x` less source work), reduced mean status latency from `89.558896 ms` to `11.162376 ms` (`8.023282x`), kept Python peak allocation at `0.065983 MiB` versus `1.372842 MiB`, and used `0.0 MiB` CUDA allocation/reservation. The refresh `..\..\MARULHO_reports\bounded_replay_window_20260623\status-transition-memory-source-window-recent-helper.json` proves recency quality by inserting `32` stale invalid rows first and `32` valid recent rows last; the selected source window keeps `invalid_synapse_key_count=0`, reads `32 + 32` current rows, still reads `256` total rows versus `10240` retired broad rows, and reduces mean latency from `87.635592 ms` to `12.310596 ms` (`7.118712x`) with CPU archival placement and `0.0 MiB` CUDA allocation. This retires the broad transition-memory status projection family and the status-local insertion-order helper; exact integrity moves to explicit slow audit/replay windows with separate quality and long-run throughput evidence.
- Transition-memory status hot-path protection note, June 2026: the first `524288`-token protection run for the consolidated status source window succeeded but is rejected as primary evidence at `5278.529 tokens/sec`, `train_compute=0.153055 ms/token`, and GPU contention at the `20%` threshold. The accepted rerun `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-status-transition-memory-source-window-rerun.json` processed `524288` tokens at `6371.238 tokens/sec`, `train_compute=0.128035 ms/token`, `prepare_training=0.006444 ms/token`, `finalize_total=0.006113 ms/token`, prewarmed in `268.296 s`, kept route scoring at `12/65536`, cached `65526` transition rows, kept `state_transition_runs_all_columns=false`, and recorded zero graph/native sequence failures. Velocity still reported borderline GPU contention (`max_gpu=23%`, memory utilization max `19%`), so this is same-band throughput protection, not a clean speed ceiling. The refreshed current-tree run `..\..\MARULHO_reports\bounded_replay_window_20260623\hotpath-active-pressure-65536-524288-i32-status-transition-recent-helper-default-nosample.json` processed `524288` tokens at `6054.480 tokens/sec`, p95 `21.719 ms`, `train_compute=0.133249 ms/token`, `prepare_training=0.006702 ms/token`, `finalize_total=0.006722 ms/token`, prewarmed in `244.570 s`, kept route scoring at `12/65536`, cached `65526` transition rows, kept `state_transition_runs_all_columns=false`, recorded no observed contention (`cpu max=45%`, `gpu max=10%`), kept RTX 3060 memory `1817->1816 MiB`, and recorded zero graph/native sequence failures.
- Readout-ledger production normalizer retirement note, June 2026: modern Hopfield-style recall stays useful only as a bounded associative operator after a local source family or replay window has been selected; CLS, continual replay, synaptic tagging/capture, and sparse replay all argue against retaining archive-wide helper APIs just because no current live tick calls them. MARULHO therefore removes the production `SNNLanguageReadoutEvidenceLedger._normalized_state()` all-family normalizer and its production policy export. All-family normalization is now benchmark-local retired evidence with `production_callable=false` and `benchmark_local_only=true`; production paths use snapshot, record-family, known-hash, dense-label, emission-history, and transition-memory source windows instead. The focused guard test proves `_normalized_state()` is absent, and the service checkpoint/reload test still restores the readout ledger and replay-priority views. The benchmark `reports/bounded_replay_window_20260620/snn-readout-ledger-normalization-production-normalizer-retired.json` passed all checks, kept bounded all-family comparison at `2944` rows instead of `47104` full-materialized rows (`16x`), preserved newest-first retention (`1.0` versus `0.0`), reduced mean normalization latency from `5807.281164 ms` to `379.736352 ms`, kept CUDA allocation/reservation at `0.0 MiB`, and marked archival/normalization placement as CPU. The snapshot smoke `reports/bounded_replay_window_20260620/snn-readout-ledger-snapshot-source-window-production-normalizer-retired-smoke.json` preserved quality while reading `260` rows instead of `2944` (`11.323077x`) and reduced mean snapshot latency from `409.182080 ms` to `71.702280 ms`. The `524288`-token protection run `reports/bounded_replay_window_20260620/hotpath-active-pressure-65536-524288-i32-readout-ledger-production-normalizer-retired.json` stayed in band at `6224.717 tokens/sec`, `train_compute=0.130755 ms/token`, `prepare_training=0.006849 ms/token`, `finalize_total=0.006249 ms/token`, route scoring `12/65536`, `65526` cached rows, GPU memory `1988->1987 MiB`, and zero graph/native sequence failures; borderline `21%` GPU contention makes it throughput protection, not a clean ceiling claim.
- Bounded plasticity runtime-state transition-memory note, June 2026: modern Hopfield-style recall is only a bounded local associative operator after the relevant memory has been selected; complementary learning systems, continual replay, synaptic tagging/capture, and sparse replay support selected slow consolidation, not status endpoints exporting every retained synapse. MARULHO applies that rule to `/terminus/snn-language-sequence/plasticity-runtime-state`: `SNNLanguagePlasticityApplicationExecutor.snapshot()` no longer deep-copies the complete retained `sparse_transition_weights` and `synapse_provenance_by_key` maps. It emits `bounded_snn_language_plasticity_runtime_transition_memory_source_window.v1`, returns at most `64 + 64` newest CPU archival rows, preserves retained counts, reports truncation, and forces status/readout-ledger consumers to treat bounded maps as partial unless `source_window_complete=true`. The focused benchmark `reports/bounded_replay_window_20260621/plasticity-runtime-state-source-window.json` read `256` active rows instead of `262144` in the benchmark-local retired full snapshot (`1024x` less source work), reduced mean latency from `752.314014 ms` to `7.770271 ms` (`96.819528x`), used `0.110454 MiB` traced Python peak versus `12.287186 MiB`, and used `0` CUDA allocation/reservation deltas on the RTX 3060. The old full runtime-state snapshot is retired from production and remains benchmark-local only; exact transition-memory integrity moves to explicit slow audit/replay windows, not the live tick, every-token cadence, raw replay text, hidden language reasoning, or GPU-resident archival metadata.
- Plasticity runtime-state hot-path audit note, June 2026: the first paired `524288`-token protection runs for the bounded runtime-state source window succeeded with no observed contention, bounded `12/65536` route scoring, `65526` cached transition rows, `state_transition_runs_all_columns=false`, and zero graph/native sequence failures, but measured `5642.888` and `5736.332 tokens/sec`. A same-shape audit then ran `fbb788de` at `6120.090 tokens/sec` and current HEAD at `6112.120 tokens/sec`, both with no observed contention, no snapshot polling, bounded route scoring, flat RTX 3060 memory, and zero graph/native sequence failures. The low pair is retained as variance evidence rather than a confirmed regression in the bounded runtime-state slice. Current-tree evidence is back in the maintained 6k-ish band, but this is still live-tick protection evidence, not durable goal completion or a new speed ceiling.
- Sleep replay associative-recall note, June 2026: modern Hopfield networks support associative recall as an attention-like local memory operator, while complementary learning systems, continual-learning replay, synaptic tagging/capture, and sparse replay require that recall happen inside selected slow windows instead of as every-token memory scanning. MARULHO therefore wires `bounded_sleep_replay_associative_recall.v1` into trainer-owned deep sleep after the bounded anchor/replay-window selection step. It uses at most `4` selected replay entries as queries, reads selected query tensors through `DualMemoryStore.replay_recall_row(...)`, recalls only inside the selected bucket-indexed candidate window, reports CPU archival/source/score placement, and declares no live tick, no every-token cadence, no raw replay text, no hidden language reasoning, no mutation authority, and no plasticity authority. The benchmark `reports/bounded_replay_window_20260622/sleep-replay-associative-recall-window.json` passed the new recall gate on the positive-pressure arm with `4` queries and mean best input-pattern distance `5.96046447753906e-08`; zero-pressure and no-anchor controls ran `0` queries and made no quality claim. Prototype repair still does not claim improvement in that report. The same slice closed the reopened source-tick sequence fallback: `BrainRuntime` now passes `allow_sleep_maintenance=false` to `train_text_sequence(...)`, and the per-token sequence fallback forwards that gate into `train_step(...)`. `reports/bounded_replay_window_20260622/source-tick-sequence-sleep-deferral.json` passed with service fallback sleep calls `0`, sequence fallback sleep calls `0`, explicit slow-path sleep calls `1`, and visible sequence deferred counts. The 65536-column `524288`-token protection run stayed in band at `6487.329 tokens/sec`, `train_compute=0.125633 ms/token`, `prepare_training=0.005922 ms/token`, `finalize_total=0.005852 ms/token`, zero graph/native sequence failures, no observed contention, CPU max `12%`, GPU max `19%`, and RTX 3060 memory `1709->1707 MiB`; prewarm took `304.955 s`, so this is live-tick protection plus bounded recall quality, not a startup-speed claim. Sources: [Hopfield Networks is All You Need](https://arxiv.org/abs/2008.02217), [Complementary Learning Systems](https://pubmed.ncbi.nlm.nih.gov/7624455/), [Experience Replay for Continual Learning](https://papers.neurips.cc/paper/8327-experience-replay-for-continual-learning), [Synaptic tagging and LTP](https://pubmed.ncbi.nlm.nih.gov/9020359/), [Latent Replay for Real-Time Continual Learning](https://arxiv.org/abs/1912.01100), and [Meta-Learning with Sparse Experience Replay](https://arxiv.org/abs/2009.04891).
- Sleep replay read-only recall-row note, June 2026: the same research boundary now applies inside the recall query and candidate-selection readers. The former associative recall query path used `DualMemoryStore.replay_entry(...)`, and `recall_replay_window(...)` called the normal replay selector; both could advance synaptic-tag/capture state while the aggregate report claimed read-only recall. MARULHO now uses `bounded_replay_recall_row.v1` for selected replay query tensors and calls `select_replay_window(..., advance_stc_state=false)` inside `bounded_replay_window_recall.v1`. The focused tests prove associative recall does not use a mutating row reader and that `_state_token`, capture tags, local PRP, global PRP, and bucket PRP do not change. The generic `replay_entry(...)` API is now removed; actual repair replay uses `sleep_repair_replay_row(...)`, and active reports no longer keep compatibility telemetry for the deleted generic row. The local external benchmark `..\..\MARULHO_reports\bounded_replay_window_20260622\read-only-recall-row-telemetry-retired.json` passed positive-pressure sleep recall with `1` query, mean best input-pattern distance `5.96046447753906e-08`, `query_row_reader=DualMemoryStore.replay_recall_row`, `query_row_read_count=1`, `query_row_state_advance_count=0`, `recall_selection_state_advance_count=0`, `read_only_replay_row=true`, `recall_selection_read_only=true`, `mutates_runtime_state=false`, and CPU archival tensors. HF replay-query collection now uses the same read-only row reader after bounded anchor-bucket selection and reports `direct_slow_memory_input_pattern_reads_retired=true`; `..\..\MARULHO_reports\bounded_replay_window_20260622\hf-query-row-reader-retired\summary.json` kept the memory-consolidation gate passing but did not promote bounded recall quality in that smoke. Two external no-profile `524288`-token protection runs stayed in the same noisy band at `5872.559` and `5943.110 tokens/sec`; the follow-up telemetry-retirement run stayed same-band at `5819.770 tokens/sec`, kept route scoring at `12/65536`, cached `65526` rows, skipped `524288` graph consolidation lookups, selected CUDA on the RTX 3060, kept RTX memory flat at `1934 MiB`, and recorded zero native sequence-loop fallback. This retires mutating replay rows, compatibility telemetry, and direct runner archive reads from read-only recall; repair replay uses the explicit mutating repair row only.
- Semantic/frontier benchmark baseline-removal note, June 2026: the maintained bounded semantic-frontier operators now stand on their own instead of keeping benchmark-local executable comparators. `concept_signature_lookup_benchmark.py`, `concept_frontier_scope_benchmark.py`, and `frontier_gap_bounded_benchmark.py` no longer carry archive-list materializing signature lookup, full slow-memory concept-frontier scan, or full raw-window frontier-gap scan helpers. The external maintained-only reports `..\..\MARULHO_reports\bounded_replay_window_20260623\concept-signature-legacy-baseline-removed.json`, `concept-frontier-legacy-baseline-removed.json`, and `frontier-gap-legacy-baseline-removed.json` passed with seeded signature cosine min `0.9999998212`, concept-frontier target hit rate `1.0`, frontier-gap expected term recall `1.0`, bounded CPU candidate windows, Python trace peaks below `0.09 MiB`, and `0.0 MiB` CUDA allocation. The paired `524288`-token hot-path run `..\..\MARULHO_reports\bounded_replay_window_20260623\hotpath-active-pressure-65536-524288-i32-semantic-frontier-legacy-baselines-removed-default-nosample.json` stayed in band at `6496.154 tokens/sec`, p95 `19.229 ms`, `train_compute=0.125202 ms/token`, bounded `12/65536` route rows, cached `65526` transition rows, no observed contention, flat RTX memory `1866->1866 MiB`, and zero graph/native sequence failures. This keeps modern-Hopfield-style recall as a bounded local operator inside selected semantic/frontier windows, not an archive-wide hidden reasoning path.
- Source-window benchmark baseline-removal note, June 2026: the same local-window rule now applies to benchmark code for hot-bucket source construction, SFA correction sampling, and awake-ripple tagging. Modern-Hopfield-style association can operate only after a bounded candidate/sample/window exists, while CLS, continual replay, synaptic tagging/capture, latent replay, and sparse replay reject preserving executable full-source comparators as side implementations. `bucket_candidate_source_window_benchmark.py`, `sfa_sample_scope_benchmark.py`, and `awake_ripple_scope_benchmark.py` no longer carry full hot-bucket materialization, full-buffer SFA sampling, or scalar/vector full-memory awake-ripple scan helpers. The external maintained-only reports `..\..\MARULHO_reports\bounded_replay_window_20260624\bucket-candidate-source-window-comparator-removed.json`, `sfa-sample-comparator-removed.json`, and `awake-ripple-comparator-removed.json` passed with newest-candidate hit rate `1.0`, selected-window SFA purity `1.0`, awake-ripple `0` scalar/vector scans over `256` wake-bucket scans, CPU archival placement, and `0.0 MiB` CUDA allocation where the reports touch CUDA accounting. The paired `524288`-token hot-path run `..\..\MARULHO_reports\bounded_replay_window_20260624\hotpath-active-pressure-65536-524288-i32-source-window-comparators-removed-default-nosample.json` stayed in band at `6580.539 tokens/sec`, p95 `19.094 ms`, `train_compute=0.123491 ms/token`, bounded `12/65536` route rows, cached `65526` transition rows, no observed contention, flat RTX memory `1875 MiB`, and zero graph/native sequence failures.
- Query/readout benchmark baseline-removal note, June 2026: bounded query recall must expose its candidate, payload, and report budgets directly instead of preserving old readout comparators beside the maintained path. `context_memory_match_benchmark.py`, `query_memory_payload_benchmark.py`, `runtime_concept_memory_lookup_benchmark.py`, and `query_episode_readout_benchmark.py` no longer carry report-dropping context readout, eager candidate text payload loading, direct runtime concept archive lookup, or fragment-only episode readout comparators. The external maintained-only reports `..\..\MARULHO_reports\bounded_replay_window_20260624\context-memory-match-comparator-removed.json`, `query-memory-payload-comparator-removed.json`, `runtime-concept-memory-lookup-comparator-removed.json`, and `query-episode-readout-comparator-removed.json` passed with context selection consistency `1.0`, returned-only query payload policy, explicit memory-index evidence recall `1.0`, target phrase recovery for `a cat purrs when it feels safe.`, CPU archival placement, no global scan, no live tick, and no hidden language reasoning. The paired `524288`-token hot-path run `..\..\MARULHO_reports\bounded_replay_window_20260624\hotpath-active-pressure-65536-524288-i32-query-readout-comparators-removed-default-nosample.json` stayed in band at `6586.097 tokens/sec`, p95 `18.732 ms`, `train_compute=0.123762 ms/token`, bounded `12/65536` route rows, cached `65526` transition rows, no observed contention, flat RTX memory `1886 MiB`, and zero graph/native sequence failures.
- Readout-ledger benchmark comparator removal note, June 2026: the same modern-Hopfield/CLS/continual-replay/STC/sparse-replay boundary now applies to the benchmark code itself. `snn_readout_ledger_normalization_source_window_benchmark.py` no longer carries full-materialized normalization/store comparators or broad-normalized per-boundary comparators. The maintained benchmark calls only the bounded ledger source-window helpers, checks seeded newest-first reconstruction, records explicit absence fields for the deleted comparator families, and keeps archival/normalization/store/lookup/evaluation placement on CPU with no live tick, no every-token cadence, no global candidate/score scan, no hidden language reasoning, and no CUDA archive. The external report `..\..\MARULHO_reports\bounded_replay_window_20260624\snn-readout-ledger-normalization-comparators-removed.json` passed with `2944` bounded rows out of `47104` known source rows, `44160` full-materialized rows removed, mean bounded normalization `370.034532 ms`, store `363.536084 ms`, known-hash lookup `15.750868 ms`, event-map lookup `14.613768 ms`, dense-label evaluation `17.247176 ms`, record append `36.442308 ms`, Python trace peak `226.780760 MiB`, and `0.0 MiB` CUDA allocation/reservation on the RTX 3060. The focused tests also prove `_normalized_state()` stays absent, `_store_state()` and known/event-map source windows stay bounded, and checkpoint save keeps only canonical readout-ledger state. The paired `524288`-token hot-path run `..\..\MARULHO_reports\bounded_replay_window_20260624\hotpath-active-pressure-65536-524288-i32-readout-ledger-normalization-comparators-removed-default-nosample.json` stayed in band at `6507.349 tokens/sec`, p95 `19.722 ms`, `train_compute=0.124647 ms/token`, `prepare_training=0.006070 ms/token`, `finalize_total=0.005990 ms/token`, route scoring `12/65536`, `65526` cached transition rows, `state_transition_runs_all_columns=false`, no observed contention (`CPU max 41%`, `GPU max 10%`), flat RTX memory `1968->1968 MiB`, and zero graph/native sequence failures.
