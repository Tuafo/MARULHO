# NVIDIA NIM API key (required for pi agent with nvidia-nim provider)
NVIDIA_API_KEY=

# Optional: override the shell used by the host-direct sandbox provider
# On Windows, defaults to C:\Program Files\Git\bin\sh.exe
# On Linux/macOS, defaults to sh
# SANDCASTLE_SHELL=/bin/bash
