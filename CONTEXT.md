# MARULHO Domain Language

MARULHO is a grounded Subcortex runtime for auditable autonomous cognition. "Living brain" is an aspiration only when backed by runtime evidence; the maintained claim is behavioral liveness under explicit validation conditions.

## Documentation Layout

`CONTEXT.md` is the domain vocabulary source of truth. The root `README.md`
explains the project orientation and current runtime direction. Package-local
`README.md` files under `src/marulho/*` describe machinery ownership, hot-path
rules, and evidence boundaries. Keep the documentation surface small and close
to the code that owns the machinery.

## Core Concepts

**MarulhoBrain** — the main runtime owner. It owns load/restore, feed/source buffering, tick/learn orchestration, local generation/readout, replay, growth/prune hooks, compact `BrainTrace` telemetry, and save/checkpoint metadata continuity. It delegates CUDA/Triton/native graph execution to `MarulhoTrainer`; service and UI code are adapters over this runtime, not owners of neural algorithms.
_Avoid_: rebuilding the brain as service/status/schema ceremony, hiding an external LLM/ThoughtLoop/Cortex behind the readout, or moving CUDA trainer decisions into FastAPI routes.

**Brain-Owned Lifecycle Loop** — the `/brain/start` and `/brain/stop` contract is a `MarulhoBrain` loop over queued source ticks. It reports `marulho_brain_loop_state.v1`, owner `MarulhoBrain`, configured tick width, interval, tick count, and uptime.
_Avoid_: hiding service-owned schedulers behind `/brain/start`, moving trainer algorithms into lifecycle handlers, or using lifecycle status as evidence of cognition by itself.

**Brain Service Adapter** — the active FastAPI composition root is `MarulhoBrainServiceManager`, a small adapter that loads one checkpoint into `MarulhoBrain`, exposes `/brain/*`, lists/saves/restores checkpoints, and stops the brain loop on shutdown. The HTTP/UI path should stay thin and should not become a second runtime owner.
_Avoid_: keeping a hidden service owner behind a slim route list, importing giant schema/status stacks on normal API startup, or treating compatibility aliases as the maintained runtime owner.

**BrainTrace** — compact telemetry emitted by `MarulhoBrain` for status, generation before/after evidence, tick throughput, checkpoint path, replay/growth events, device/executor names, and negative external-cognition flags. It replaces broad Runtime Truth as the default runtime status surface, while deeper gates remain explicit validation tools.
_Avoid_: re-expanding status into a giant schema court system, treating trace text as proof of cognition, or using status reads as replay/consolidation work.

**Subcortex** — the grounded predictive spiking substrate. Owns sparse routing, multimodal grounding, predictive error, neuromodulation, replay, and curiosity pressure. Does not reason in language.
_Avoid_: SSN, raw SNN side

**Autonomous Continual Language Runtime** — the target MARULHO-owned next-token language model architecture described in `docs/autonomous-continual-language-runtime.md`. It keeps tokenizer/vocabulary state, selective spiking recurrence, routed columns/experts, replay, structural plasticity, LM head checkpoints, Triton kernels, and evaluation gates inside MARULHO ownership. The Iteration 2 foundation now includes a MARULHO-owned byte tokenizer, selective spiking next-token LM, heldout loss/perplexity report, checkpoint round trip, and a brain-selectable `active_language_path=marulho_lm_head` generation path. Current local transition readout remains the fallback evidence path until the LM path has online learning, rollback, throughput, and long-run Runtime Evidence.
_Avoid_: treating a design lock as implemented language-model capability, hiding an external LLM/Cortex/ThoughtLoop behind generation, moving cognition into `service`, or promoting short/toy runs as scale-ready evidence.

**MARULHO Selective Spiking State Block** — the training-owned recurrent language block behind the Iteration 2 LM head. Its PyTorch foundation carries membrane/spike/selective-state/eligibility-trace cache, input-dependent leak and threshold, trainable current terms, RMSNorm residual output, adaptive timestep telemetry, and spike-health telemetry. It is implementation evidence for the state-space/SNN direction, not a promoted CUDA/Triton kernel or long-run capability claim.
_Avoid_: calling the PyTorch fallback a promoted hot path, dropping the recurrent cache during streaming generation, or treating spike telemetry alone as language quality.

**LM Vectorized State-Block Training Path** — the training-owned batched forward path for the MARULHO selective spiking LM block. It precomputes token-independent RMSNorm, select, leak-input, threshold-input, current, input-drive, and residual projections across `[batch,time]` on the runtime device, runs only the causal membrane/spike/selective-state recurrence in the loop, then applies `state_output_proj` once across the stacked mixed-state sequence. The 2026-07-03 CUDA profile for `batch=16`, `seq=64`, `state_dim=128`, `16` experts, and `4` active experts moved state-block forward from `266.571 ms` to `149.390 ms`, full forward loss from `293.187 ms` to `170.986 ms`, and full train step from `823.405 ms` to `443.763 ms`. The local 2026-07-04 artifact `reports/language_training_experiments/cuda-sampled-padded-horizon8-tf32-clip8-batched-state-output-524288-63744.json` trained the `524288` model-vocab, `1024` sampled-row, horizon-8, TF32, clip-interval-8 shape for `63744` tokens at `2954.763 train tokens/sec`, improved heldout loss from `7.1370` to `0.2009`, and sustained `524288/524288` generation at `7217.290 tokens/sec`; versus the prior clip-8 baseline this is `+16.386%` training throughput, with forward/loss down `9.988%`, backward down `17.251%`, and batch total down `14.094%`. This is batched training speed evidence, not PLIF/selective-scan Triton promotion.
_Avoid_: moving the recurrent update out of causal order, treating vectorized projection as a fused PLIF kernel, or using the batch forward path to bypass streaming-state parity.

**LM Continual Learning Window** — the training-owned bounded update surface for the MARULHO LM head. It snapshots language-model weights, applies gradient updates on new batches with replay protection, measures old-domain forgetting, new-domain improvement, replay retention, spike-rate delta, update throughput, and rollback hashes, and records the report through `MarulhoBrain` when installed. It now supports sampled/padded vocab models with sparse token-embedding and LM-head row gradients, `AdamW_dense_core_plus_SparseAdam_vocab_rows`, sparse-aware clip cadence, telemetry-light update steps, sampled-vocab batch precompute for online new/replay/eval batches, deferred GPU-scalar metric aggregation so update/replay loss and max-gradient-norm readbacks do not synchronize each optimizer step, deferred heldout-eval loss aggregation so eval batches do not synchronize for each loss readback, and phase timings for snapshot, precompute, pre/post evaluation, optimizer setup, update, rollback, and total window. The local 2026-07-04 baseline artifact `reports/language_continual_learning/cuda-sampled-padded-horizon8-tf32-clip8-524288.json` ran a `524288` model-vocab, `1024` sampled-row, horizon-8, TF32, clip-interval-8 online window for `65536` new+replay tokens at `2619.310 train tokens/sec`; new-domain heldout loss improved from `7.1125` to `0.5576`, old-domain loss improved from `7.0595` to `1.7194`, replay loss improved from `7.0595` to `1.7182`, and the checkpoint rollback gate accepted the update without materializing full vocab logits. The follow-up `reports/language_continual_learning/cuda-sampled-padded-horizon8-tf32-clip8-precomputed-sampled-vocab-524288.json` kept the same vocab/sample/horizon/TF32/clip and token-count shape, recorded precomputed sampled vocab for both new and replay batches, reached `3023.964 train tokens/sec`, and is `+15.449%` throughput versus the retained baseline. The deferred-metric report `reports/language_continual_learning/cuda-sampled-padded-horizon8-tf32-clip8-deferred-metrics-precomputed-sampled-vocab-524288.json` keeps the same current corpus and shape, records `metric_readback_mode=deferred_gpu_scalar_aggregation`, `per_step_metric_cpu_sync=false`, explicit CUDA timing-window syncs, and reaches `3089.664 train tokens/sec`: `+2.173%` over precompute-only and `+17.957%` over the retained baseline. The eval-precompute/phase-timing report `reports/language_continual_learning/cuda-sampled-padded-horizon8-tf32-clip8-eval-precompute-deferred-metrics-524288.json` records old/new eval sampled-vocab precompute, precomputed sampled IDs inside heldout evaluation loss evidence, `3057.041 train tokens/sec`, `1690.405 total-window tokens/sec`, and phase costs of `0.355s` precompute, `5.829s` pre-update eval, `2.283s` optimizer setup, `21.438s` update, and `6.245s` post-update eval. This is `+16.712%` versus the retained baseline and `+1.094%` versus precompute-only, but `-1.056%` versus the immediately prior deferred-metric run, so treat it as full-window visibility plus eval precompute evidence rather than a new update-loop speed promotion. The deferred-eval-metric report `reports/language_continual_learning/cuda-sampled-padded-horizon8-tf32-clip8-deferred-eval-metrics-524288.json` uses the repeatable `language_continual_learning_experiment.py` runner, keeps the same `22` old and `27` new heldout eval batch counts as the eval-precompute report, records heldout `metric_readback_mode=deferred_gpu_scalar_aggregation` and `per_batch_metric_cpu_sync=false`, and reaches `2990.395 train tokens/sec` plus `1712.349 total-window tokens/sec`; with matched eval counts, pre-update eval is `0.147s` faster, post-update eval is `0.934s` faster, and total-window throughput is `+1.298%`, while update-loop throughput is `-2.180%`, so this is heldout-eval sync and repeatable evidence plumbing, not a new update-loop promotion. The older retained baseline corpus differs from the current reports, so treat the original-baseline comparison as same-shape throughput evidence, not a quality comparison. It is online-learning evidence, not a service-owned cognition loop or long-run promotion by itself.
_Avoid_: triggering language learning from status reads, accepting improvement without old-domain/replay measurements, or hiding rollback/forgetting evidence.

**LM Routed Expert Layer** — the training-owned sparse expert foundation behind the MARULHO LM head. It uses a bounded candidate plan to score a subset of expert columns, wakes top-k experts, reports active columns, candidate rows, output candidates, active parameters per token, route device, latency, candidate-ID source, all-awake fastpath use, and fallback truth when all experts are scored. Gradient training now dispatches selected expert MLPs with batched matmul instead of per-token einsum, while the all-awake candidate path avoids materializing an `arange` awake-index tensor and maps token-hash candidate positions directly by modulo. The local 2026-07-04 historical fast artifact `reports/language_training_experiments/cuda-sampled-padded-horizon8-tf32-clip8-expert-matmul-524288-63744.json` trained the `524288` model-vocab, `1024` sampled-row, horizon-8, TF32, clip-interval-8 shape for `63744` tokens at `3531.685 train tokens/sec`, improved heldout loss from `7.1551` to `0.2409`, and sustained `524288/524288` generation at `7166.620 tokens/sec`; versus the previous batched-state-output baseline this was `+19.525%` training throughput, with forward/loss down `17.864%`, backward down `15.522%`, and batch total down `16.342%`. Same-session paired evidence keeps the direct all-awake candidate fastpath because `reports/language_training_experiments/cuda-sampled-padded-horizon8-tf32-clip8-all-awake-route-fastpath-524288-63744.json` reached `2994.386 train tokens/sec` and `7257.759 sustained tokens/sec`, versus `2880.361` and `7050.359` for `reports/language_training_experiments/cuda-sampled-padded-horizon8-tf32-clip8-current-control-rerun-524288-63744.json`; the win is `+3.959%` training and `+2.942%` sustained generation for that paired run, with forward/loss down from `0.129139` to `0.125210 ms/token`, backward down from `0.204089` to `0.195349 ms/token`, and batch total down from `0.346854` to `0.333647 ms/token`. It is PyTorch sparse-expert training impact evidence, not a promoted Triton block-sparse training dispatcher.
_Avoid_: counting configured experts as active work, hiding all-column route scoring, or claiming sparse execution without active-parameter and fallback evidence.

**LM Structural Plasticity Transaction** — the training-owned checkpoint-backed mutation boundary for MARULHO LM expert growth, explicit expert prune, explicit expert merge, and explicit expert deep sleep. It creates non-mutating expert-spawn proposals from routing/learning pressure, expert-prune proposals from explicit inactive or low-utility expert evidence, expert-merge proposals from duplicate/high-similarity expert-pair evidence, and expert-deep-sleep proposals from stale, low-activation, low-utility, high-cost, or dead-spike expert evidence. Application requires operator approval, writes a baseline checkpoint, applies the candidate topology change or checkpointed sleep mask in isolation, checks heldout non-regression, and records rollback hashes before `MarulhoBrain` accepts the candidate. It is expert-column growth/prune/merge/deep-sleep evidence, not a general self-modification permit.
_Avoid_: applying growth/prune/merge/deep-sleep from status, skipping proposal/approval, hiding checkpoint restore evidence, or treating expert spawn/prune/merge/deep-sleep as proof that split, synapse-bundle growth, memory expansion, route-bank expansion, or retire are implemented.

**LM Checkpoint Evolution Window** — the training-owned controlled evaluation boundary for MARULHO LM checkpoint evolution. It writes a parent checkpoint, forks an isolated child checkpoint, runs child-only learning/replay/optional growth, compares parent and child heldout evidence, verifies the parent hash and checkpoint restore path, and records lineage through `MarulhoBrain` without replacing the installed parent model. It is promotion-review evidence, not self-copying, deployment, or automatic runtime mutation.
_Avoid_: mutating the parent during child evaluation, promoting a child without operator review, hiding lineage hashes, or treating a checkpoint fork as uncontrolled replication.

**LM Sustained Runtime Evidence** — the evaluation-owned report boundary for checkpointed MARULHO LM-head streaming runs. `language_sustained_runtime_evidence.py` loads or receives a MARULHO language checkpoint, streams recurrent-cache next-token generation to a target token count, and writes JSON plus README evidence for final, timeout, manual-stop partial, interrupt, and exception outcomes. CUDA runs attempt a fixed-shape `torch_cuda_graph_burst` stream that replays ordered `quantum_tokens` token steps on device and records graph replay count, graph token count, setup time, eager tail tokens, and graph failure reason before falling back to `torch_eager_cuda`. Reports include active language path, checkpoint metadata, token delta, throughput, device/backend, routing columns, spike health, fallback counts, environment contention, promotion gates, and model/tokenizer/generation vocab policy. Current 2026-07-04 padded-vocab evidence from `reports/language_training_experiments/padded-vocab-generation-policy-524288-checkpoint.pt` reached `524288/524288` at `7248.118 tokens/sec` on `cuda:0` with `torch_cuda_graph_burst`, `16`-token bursts, zero graph failures, `524288` model vocab rows, `262` tokenizer/generation rows, and `524026` padded rows masked from generation. It remains component evidence and still keeps `promotes_runtime_claim=false` and `promotes_hot_path=false` until generation quality and Triton/kernel parity evidence exist.
_Avoid_: using a short LM smoke run as promotion evidence, treating component generation throughput as the full brain loop, hiding graph fallback behind a speed claim, letting padded vocab rows decode as byte tokens, or using CUDA graph burst speed to erase missing generation-quality or Triton/parity evidence.

**LM Training Experiment Runner** — the evaluation-owned fast experiment path for training and inspecting the MARULHO LM head. `language_training_experiment.py` builds a configurable routed selective-spiking LM, packs source windows into device-resident batches, trains with telemetry-light GPU optimizer steps, records train tokens/sec, heldout loss/perplexity before and after training, owned generation samples, source-continuation quality probes, a checkpoint, and paired sustained-inference reports. It now supports padded model vocabularies, sampled/adaptive vocab loss, sparse token-embedding and LM-head row gradients, tokenizer-row generation decode limits, and sampled-vocab batch precompute outside the measured update window. Current 2026-07-04 CUDA sampled/padded evidence in `reports/language_training_experiments/cuda-sampled-padded-default-policy-524288-63744.json` trained `63744` tokens with `524288` model vocab rows, `1024` sampled rows, `batch_size=16`, `AdamW_dense_core_plus_SparseAdam_vocab_rows`, selected-row PyTorch autograd loss, and no full vocab logits at `2361.928 train tokens/sec`; heldout loss moved from `7.1612` to `0.1997`, and the paired `524288` token sustained report reached `7273.947 tokens/sec` on `torch_cuda_graph_burst` with `generation_vocab_size=262` and `524026` padded rows masked. The precomputed sampled-vocab CUDA report `reports/language_training_experiments/cuda-sampled-padded-horizon8-tf32-clip8-precomputed-sampled-vocab-524288-63744.json` trained the current all-awake shape at `3041.246 train tokens/sec`, with forward/loss down to `0.121173 ms/token` and batch total down to `0.328424 ms/token`; versus the retained all-awake fastpath report this is `+1.565%` training throughput and no inference-code change. It is designed for quick mutable science loops, not as a promotion gate or proof of general language quality.
_Avoid_: stopping at category bookkeeping, treating one corpus run as language competence, hiding bad generated text instead of recording it for the next mutation, confusing source-continuation memorization with broad generation coherence, or treating padded-vocab sampled training as dense full-vocab training.

**LM Training Stage Profile** — the evaluation-owned opt-in profile for the `language_training_experiment.py` update window. `--profile-training-stages` records CUDA-event timings for `zero_grad`, `forward_loss`, `backward`, sparse-aware `gradient_clip`, optimizer step, and batch total without per-stage synchronizing inside the hot loop. The local 2026-07-04 artifact `reports/language_training_experiments/cuda-sampled-padded-stage-profile-524288-63744.json` trained the same `524288` model-vocab, `1024` sampled-row, batch-16 shape for `63744` tokens at `2342.586 train tokens/sec`, improved heldout loss from `7.0637` to `0.1827`, and sustained `524288/524288` generation at `7244.434 tokens/sec`. The profile shows backward as the dominant cost (`0.245897 ms/token`), then forward/loss (`0.150728 ms/token`), then sparse-aware gradient clipping (`0.020030 ms/token`), with optimizer step much smaller (`0.007629 ms/token`). This points the next speed slice at state-block/PLIF backward and forward/loss work, not at the sparse optimizer step.
_Avoid_: treating profiler overhead as ordinary runtime work, using stage timings without complete training/sustained evidence, or optimizing optimizer step before the measured larger backward/forward costs.

**LM Recurrent Gradient Horizon** — the training-owned bounded BPTT policy for fast mutable LM experiments. `LanguageModelConfig.recurrent_gradient_horizon` keeps the forward recurrent state causal on device, but detaches membrane/spike/selective-state/eligibility tensors every configured number of tokens during gradient-enabled multi-token state-block training. The local 2026-07-04 artifact `reports/language_training_experiments/cuda-sampled-padded-horizon8-profile-524288-63744.json` used horizon `8` on the same `524288` model-vocab, `1024` sampled-row, batch-16 shape, recorded `7` detach boundaries per 64-token batch, trained `63744` tokens at `2435.816 train tokens/sec`, improved heldout loss from `7.1213` to `0.2303`, and sustained `524288/524288` generation at `7225.847 tokens/sec`. Versus the previous profiled full-sequence BPTT run, training throughput improved `3.980%`, batch-total cost fell by `0.016323 ms/token`, forward/loss fell by `0.009386 ms/token`, and backward fell by `0.006034 ms/token`; backward remains the top cost.
_Avoid_: treating truncated BPTT as full long-context credit assignment, using it as generation-quality evidence, detaching state in one-token streaming generation, or hiding the boundary count when reporting training speed.

**LM CUDA Math Policy** — the evaluation-owned CUDA precision policy for fast mutable LM experiments. `language_training_experiment.py` now applies an explicit experiment-scoped TF32 policy on CUDA (`cuda_allow_tf32=true`, `cuda_float32_matmul_precision=high` by default), records the before/active policy in the report, and restores the prior process policy afterward. The local 2026-07-04 artifact `reports/language_training_experiments/cuda-sampled-padded-horizon8-tf32-profile-524288-63744.json` started from `matmul_allow_tf32=false`, applied TF32, trained the horizon-8 `524288` model-vocab, `1024` sampled-row, batch-16 shape for `63744` tokens at `2487.980 train tokens/sec`, improved heldout loss from `7.0918` to `0.2066`, and sustained `524288/524288` generation at `7239.247 tokens/sec`. This is `+2.142%` training throughput over the horizon-8 non-TF32 report and `+6.207%` over the full-sequence profiled baseline; backward remains largest at `0.231877 ms/token`.
_Avoid_: treating TF32 as bitwise-identical float32, enabling it silently without report fields, leaking process-wide math settings after an experiment, or using this precision speedup as a generation-quality claim.

**LM Gradient Clip Cadence** — the evaluation-owned optimizer-safety cadence for fast mutable LM experiments. `language_training_experiment.py` keeps every-step sparse-aware gradient clipping as the default, but `--gradient-clip-interval N` can run the full GPU norm/clip pass every N optimizer updates while still reporting applied and skipped counts. The local 2026-07-04 artifact `reports/language_training_experiments/cuda-sampled-padded-horizon8-tf32-clip8-profile-524288-63744.json` used horizon `8`, TF32, and clip interval `8`; it clipped `8/64` updates, skipped `56`, trained the `524288` model-vocab, `1024` sampled-row, batch-16 shape for `63744` tokens at `2538.756 train tokens/sec`, improved heldout loss from `7.0764` to `0.2083`, and sustained `524288/524288` generation at `7223.490 tokens/sec`. This is `+2.041%` training throughput over the horizon-8 TF32 every-step clip run and `+8.374%` over the full-sequence profiled baseline; gradient-clip stage cost fell to `0.002918 ms/token`.
_Avoid_: hiding skipped gradient-norm passes, treating clip cadence as a universal stability guarantee, or promoting a speed run without heldout-loss and sustained-generation evidence.

**LM Deferred Training Metric Readback** — the evaluation/training-experiment host-boundary policy for CUDA LM training reports. The optimizer loop keeps per-batch loss and gradient-norm metrics as detached device scalars, performs CUDA synchronization before starting and stopping the training timer, and reads aggregate scalar metrics back after the measured update window. The local 2026-07-04 artifact `reports/language_training_experiments/cuda-deferred-metrics-8192.json` trained the same `63744` token PLIF-surrogate shape at `2720.929 train tokens/sec`, versus the earlier `2596.380 train tokens/sec` per-batch-readback report, with `3840` Triton PLIF backward calls, `cuda_synchronized_before_timing_start=true`, `cuda_synchronized_before_timing_stop=true`, and `per_batch_metric_cpu_sync=false`; its paired house-scale sustained report `reports/language_training_experiments/cuda-deferred-metrics-524288-sustained.json` reached `524288/524288` at `7502.156 tokens/sec` on `cuda:0` with `torch_cuda_graph_burst`, no external LLM, and zero graph failures. This is training-loop host-sync reduction evidence, not a new generation-quality or runtime-promotion claim.
_Avoid_: hiding CUDA completion time by stopping timers before synchronization, restoring per-batch `.cpu().item()` readbacks in the hot update loop, or treating a neutral sustained inference rerun as an inference-speed promotion.

**LM Generation Coherence Report** — the evaluation-owned grounded prompt-suite artifact for MARULHO-owned LM generation. `language_generation_coherence.py` loads or receives a MARULHO language checkpoint, runs source-anchored prompts through `marulho_lm_head`, records raw generated continuations, source-prefix match, printability, token-run and bigram-diversity checks, and blocks on external LLM use or missing source anchors. The local 2026-07-04 artifact `reports/language_generation_coherence/plif-surrogate-grounded-prompt-suite-20260704.json` passed `4/4` anchored prompts from the PLIF-surrogate checkpoint with mean prefix match `46` characters, mean prefix fraction `0.71875`, printable fraction `1.0`, and next-character match rate `1.0`; it explicitly records `human_review_available=false`, `promotes_generation_quality_claim=false`, and `promotes_runtime_claim=false`.
_Avoid_: treating anchored source-continuation coherence as broad language competence, hiding fractured generated text, or replacing long-run/kernel gates with a prompt-suite report.

**LM RMSNorm Triton Kernel Evidence** — the core/evaluation-owned first LM-head Triton slice. `language_rmsnorm_triton.py` adds a Triton forward kernel, PyTorch fallback, autograd-compatible backward, and runtime-use counters for the RMSNorm primitive inside the selective spiking LM block. `reports/language_kernel_evidence/rmsnorm-triton-20260703.json` passed `float32` and `float16` shape parity with geometric microbenchmark speedup `1.440x`; forced Triton on one-token streaming was measured and rejected for sustained throughput, so the maintained model policy uses Triton for batched rows and reports PyTorch/CUDA fallback for streaming rows. The accepted `524288`-token policy report reached `7003.004 tokens/sec` with CUDA graph burst, zero graph failures, and no observed contention. This is partial kernel evidence, not full hot-path promotion.
_Avoid_: forcing a slower Triton kernel into one-token streaming for optics, treating RMSNorm parity as PLIF/selective-scan/expert/vocab parity, or hiding PyTorch/CUDA fallback counts in sustained reports.

**LM PLIF Triton Forward Evidence** — the core/evaluation-owned PLIF/adaptive-LIF forward slice for the selective spiking LM block. `language_plif_triton.py` adds a Triton forward kernel, PyTorch fallback, no-grad runtime-use counters, and a hard-spike forward reference for membrane, spike, selective-state, eligibility-trace, and mixed-state updates. The local 2026-07-03 artifact `reports/language_kernel_evidence/plif-forward-triton-20260703.json` passed six CUDA shape/dtype cases for `language_plif_forward` (`float32` and `float16`) with geometric microbenchmark speedup `3.145x`. `reports/language_benchmark_suite/language-suite-plif-forward-kernel.json` records `rmsnorm_triton_parity=true`, `plif_triton_forward_parity=true`, long-run throughput available through the current `524288` LM sustained report, and kept promotion blocked on generation coherence plus then-missing PLIF backward surrogate, selective-scan, expert-dispatch, and vocab-cross-entropy kernel evidence.
_Avoid_: claiming PLIF backward, selective scan, or full hot-path promotion from forward parity alone, running the no-grad kernel in gradient training, or hiding one-token streaming fallback when batched Triton rows are not used.

**LM PLIF Triton Surrogate Backward Evidence** — the core/training/evaluation-owned PLIF backward slice for the selective spiking LM block. `language_plif_triton.py` now adds a custom autograd path whose Triton forward/backward implements the same hard-spike forward and sigmoid surrogate derivative used by the PyTorch straight-through update. The Triton surrogate path is intentionally `float32` only; `float16` backward remains unsupported until gradient parity is proven. The local 2026-07-03 artifact `reports/language_kernel_evidence/plif-surrogate-triton-20260703.json` passed three CUDA `float32` shape sweeps for `language_plif_surrogate_backward` with geometric forward+backward microbenchmark speedup `1.662x`, while marking `float16` as unsupported rather than failed. The complete training-impact artifact `reports/language_training_experiments/cuda-plif-surrogate-8192.json` trained `63744` tokens at `2596.380 train tokens/sec` versus the prior vectorized-state `2293.991`, used `3840` Triton PLIF forward and `3840` backward calls with zero failures, and improved heldout loss to `0.1907`. The paired `reports/language_training_experiments/cuda-plif-surrogate-524288-sustained.json` reached `524288/524288` at `7578.052 tokens/sec` with CUDA graph burst and zero graph failures. `reports/language_benchmark_suite/language-suite-plif-surrogate-impact.json` records RMSNorm, PLIF forward, and PLIF surrogate backward parity while keeping promotion blocked on generation coherence plus selective-scan, block-sparse expert dispatch, and sampled-vocab cross-entropy evidence.
_Avoid_: treating float32 PLIF backward parity as half-precision coverage, claiming selective-scan or expert-dispatch fusion from PLIF evidence, or promoting generation quality from source-continuation probes.

**LM Selective Scan Triton Evidence** — the core/evaluation-owned selective recurrent state scan slice for the MARULHO LM state block. `language_selective_scan_triton.py` implements the standalone recurrence `state[t] = decay[t] * state[t-1] + input[t] * spike[t]` over `[batch,time,state_dim]` tensors, with PyTorch fallback, CUDA/Triton forward counters, and forced parity/benchmark execution. The local 2026-07-04 artifact `reports/language_kernel_evidence/selective-scan-triton-20260704.json` passed six CUDA shape/dtype cases for `language_selective_state_scan` (`float32` and `float16`, 64 recurrent steps) with geometric microbenchmark speedup `114.077x` over the PyTorch recurrent-loop reference. `reports/language_benchmark_suite/language-suite-selective-scan-kernel.json` records RMSNorm, PLIF forward, PLIF surrogate backward, and selective-scan parity, keeps long-run throughput available through the current PLIF-surrogate house-scale sustained report, and keeps promotion blocked on generation coherence plus block-sparse expert dispatch and sampled-vocab cross-entropy evidence.
_Avoid_: treating standalone scan parity as full state-block fusion, claiming training-loop runtime impact before the scan is integrated into the causal block, or erasing the remaining expert/vocab/generation blockers.

**LM Expert Dispatch Triton Evidence** — the core/training/evaluation-owned block-sparse expert dispatch/combine slice for the MARULHO LM routed expert layer. `language_expert_dispatch_triton.py` implements selected two-layer expert MLP dispatch for `[token,state_dim]` hidden rows, `[token,active_expert]` selected IDs, softmax combine weights, and stacked expert weights, with PyTorch fallback and no-grad CUDA use when the token-count policy allows it. The local 2026-07-04 artifact `reports/language_kernel_evidence/expert-dispatch-triton-20260704.json` passed three CUDA `float32` shape sweeps for `language_block_sparse_expert_dispatch` with geometric microbenchmark speedup `4.389x`; `float16` dispatch is explicitly unsupported until parity is proven. `reports/language_benchmark_suite/language-suite-expert-dispatch-kernel.json` records RMSNorm, PLIF forward, PLIF surrogate backward, selective-scan, and expert-dispatch parity, keeps long-run throughput available through the current PLIF-surrogate house-scale sustained report, and is an older suite snapshot that remained blocked on generation coherence plus then-missing sampled-vocab cross-entropy evidence.
_Avoid_: treating float32 expert-dispatch parity as half-precision coverage, claiming dense/all-column routing quality from selected-expert dispatch, or promoting the LM hot path before vocab-loss, generation-review, and complete-runtime impact evidence are closed.

**LM Sampled-Vocab CE Triton Evidence** — the core/evaluation-owned sampled/adaptive vocabulary loss slice for the MARULHO LM head. `language_sampled_vocab_ce_triton.py` implements forward sampled-vocabulary cross entropy for `[token,state_dim]` hidden rows, selected vocabulary IDs, LM-head weight/bias rows, and target IDs that must all be present in the sample, with PyTorch fallback and runtime-use counters. The local 2026-07-04 artifact `reports/language_kernel_evidence/sampled-vocab-ce-triton-20260704.json` passed three CUDA `float32` shape sweeps for `language_sampled_vocab_cross_entropy` at `8192` total vocab rows and `1024` sampled rows with geometric microbenchmark speedup `1.047x`; `float16` sampled-vocab CE is explicitly unsupported until numerical parity is proven. The module now also has a forceable Triton-forward/custom-autograd training path with sparse LM-head row gradients, but `MARULHO_LANGUAGE_SAMPLED_VOCAB_CE_TRITON_TRAINING` stays off by default because complete b16/r8 sampled-only evidence was slower than the maintained selected-row autograd path (`2622.292` versus `2675.442 train tokens/sec`). `reports/language_benchmark_suite/language-suite-sampled-vocab-kernel.json` records RMSNorm, PLIF forward, PLIF surrogate backward, selective-scan, expert-dispatch, and sampled-vocab CE parity, moves GPU kernel correctness to `pass`, keeps long-run throughput available through the current PLIF-surrogate house-scale sustained report, and keeps promotion blocked on generation coherence review.
_Avoid_: treating sampled-vocab forward CE parity as proof of faster gradient training, enabling the forced Triton-autograd path without complete-runtime evidence, claiming half-precision vocab loss coverage, or promoting the LM hot path from kernel parity alone.

**LM Sampled-Vocab Training Impact** — the training/evaluation-owned sampled/adaptive vocabulary gradient path for the MARULHO LM head. `MarulhoLanguageModel.next_token_loss` can avoid materializing full `[token,vocab]` logits when `sampled_vocab_size` is configured, builds selected vocabulary IDs on the runtime device by default, accepts precomputed sampled row IDs plus target positions for fixed batches, and uses the measured-faster PyTorch selected-row autograd path by default. Large padded-vocab training can opt into sparse token-embedding and LM-head weight gradients, then step those rows with a sparse vocab optimizer while dense core parameters stay on AdamW. The local 2026-07-04 artifact `reports/language_training_experiments/sampled-vocab-training-impact-default-policy-524288-b16-r8-sampled-only.json` measured `524288` model vocab rows, `1024` sampled rows, `batch=16`, `seq=64`, warmup `2`, repeats `8`, backward, sparse-aware gradient clipping, and optimizer steps on CUDA: sampled training reached `2675.442 train tokens/sec`, peak CUDA allocation was `2368.205 MiB`, full vocab logits were not materialized, and the report recorded `8` selected-row fallback calls with zero Triton CE training calls. The integrated precompute report `reports/language_training_experiments/cuda-sampled-padded-horizon8-tf32-clip8-precomputed-sampled-vocab-524288-63744.json` records `sampled_vocab_precompute.enabled=true`, `sampled_vocab_id_source=precomputed_batch_sampled_vocab_ids`, and `sampled_target_position_source=precomputed_batch_target_positions`, moving sampled-row/target-position construction out of the measured update window. This is large-vocab training impact evidence, not generation-quality or runtime-promotion evidence.
_Avoid_: using sampled training with a dense full-vocab optimizer, hiding sparse optimizer policy differences in speed claims, treating padded vocab rows as decodable byte tokens, or promoting generation/runtime quality before checkpoint, long-run, and review gates exist.

**LM Padded-Vocab Generation Policy** — the training/evaluation-owned decode boundary for LM configs whose model vocab is larger than the tokenizer vocab. `generation_vocab_size` must be explicit before a padded-vocab checkpoint can be saved or loaded; generation, sustained runs, and CUDA graph burst capture request decode-limited LM-head logits, so padded rows are masked instead of being emitted as unknown byte tokens. The local 2026-07-04 artifact `reports/language_training_experiments/padded-vocab-generation-policy-524288-sustained.json` loaded a `524288`-row checkpoint, limited generation to the `262` tokenizer rows, masked `524026` padded rows, kept generated tail IDs inside tokenizer range (`max=261`), and reached the `524288` house-scale target at `7248.118 tokens/sec` on `torch_cuda_graph_burst`. This proves checkpoint restore plus long-run decode masking for padded vocabularies; it does not prove language quality or broad runtime promotion.
_Avoid_: saving padded-vocab checkpoints without `generation_vocab_size=tokenizer.vocab_size`, computing full padded logits for generation, decoding padded rows as `<unk>`, or treating padded-vocab throughput as quality evidence.

**LM Scale Ladder** — the evaluation-owned scale-readiness report for MARULHO LM-head configurations. `language_scale_ladder.py` defines the small fixture, 140M-class, 500M-class, 0.9B-class, and 2B+ research targets, estimates total parameters, active parameters per token, routed-column budgets, dense vocab-head cost, and memory footprint without allocating large models, and can run a CI-sized smoke fixture for heldout loss and owned generation evidence. It is scale planning and evidence inventory, not proof that the large ladders were trained, restored, benchmarked, or competitive.
_Avoid_: claiming frontier competitiveness from parameter count, instantiating large ladders in status/service code, or treating an analytic estimate as train-token, throughput, forgetting, or generation-quality evidence.

**LM Runtime Benchmark Suite** — the evaluation-owned aggregator for MARULHO LM-head benchmark evidence. `language_runtime_benchmark_suite.py` writes JSON plus README reports covering next-token loss, heldout perplexity, generation smoke/coherence evidence, grounding support, continual learning, forgetting, replay recovery, growth/prune safety, long-run throughput, active compute, GPU kernel correctness, checkpoint restore, rollback, service contract, and scale-ladder evidence. It may run CI-sized fixtures and subreports, but it must keep Triton/CUDA parity, real long-run, and generation-review gates explicit and must not promote a runtime claim from smoke evidence. Existing `marulho_language_sustained_runtime_evidence.v1` reports can satisfy the long-run throughput category only when final MARULHO-owned LM reports reach the 8192 diagnostic and 131072 long-run token gates; existing `marulho_language_generation_coherence_report.v1` reports can satisfy generation coherence only when a grounded prompt suite passes and still leaves broad quality/runtime promotion false. The local `reports/language_benchmark_suite/language-suite-generation-coherence.json` suite is `ready_for_review` with zero missing categories while keeping `promotes_runtime_claim=false`.
_Avoid_: treating a suite smoke report as promotion, hiding missing required categories, or running the benchmark suite from status/service code.

**LM Grounding Support Report** — the evaluation-owned source-term support artifact for the MARULHO LM head. `language_grounding_support.py` writes JSON plus README evidence proving that a prompt's required terms are supported by source text, that the generation artifact comes from `marulho_lm_head` with `external_llm_used=false`, and that unsupported generated terms remain visible for later review. It can satisfy the benchmark suite's grounding-support category, but it is not a human or grounded generation-quality review.
_Avoid_: treating source-term coverage as factual correctness, hiding unsupported continuation terms, or using the support report to promote language quality.

**External LLM Boundary** — external LLM, NIM, Cortex, or ThoughtLoop components are not the MARULHO brain. They may be external tools or research references only when a caller explicitly wires them outside the MARULHO runtime.
_Avoid_: treating LLM/NIM as the mind, mandatory reasoning core, or active production path

**Subcortex Action Ledger** — the runtime-owned record of digital action execution, verification, contradiction, feedback, and consequence evidence. Action recording must not initialize or mirror into the external-LLM boundary.
_Avoid_: action history as ThoughtLoop memory, booting Cortex to mirror an action

**Subcortex Grounded Observation** — the runtime-owned source or sensory evidence packet created from real text, visual, audio, or multimodal input. It carries grounded metadata, focus terms, salience, and device/encoder evidence without requiring ThoughtLoop observation or surprise injection.
_Avoid_: treating source/sensory observations as Cortex memory writes

**Grounding Diagnostics** — term-level evidence that a language-facing result is supported by observed source or sensory evidence. It records target terms, matched terms, evidence support, coverage, alignment, and recovery state; it is Subcortex language evidence, not a Cortex-only type.
_Avoid_: fluent answers without support terms, hidden recovery paths

**Active Exploration State** — the current bounded focus target selected to reduce uncertainty or resolve a control gap. It records normalized target text, reason, source, score, and sample time; it is Subcortex control evidence, not a ThoughtLoop-private field.
_Avoid_: free-form curiosity text without source/reason/score

**Subcortex Deliberation** — the active replacement direction for cognition that needs planning, replay, or hypothesis formation. It should be implemented through SNN-compatible prediction, world-model, memory, and policy mechanisms rather than an external LLM loop. Early deliberation candidates are bounded control candidates derived from Cognitive Signal pressure and concept focus.

**Deliberation Feedback** — the topic, grounding, valence, and confidence payload routed from a language-facing result back into Subcortex curiosity, drives, and context control.
_Avoid_: naming closed-loop control feedback as Cortex-owned

**Deliberation Text Merge** — deterministic language-surface helper that compresses multi-step deliberation outputs into one auditable result. It is a formatting/readout primitive, not a cognition substrate, and must be usable without instantiating ThoughtLoop.
_Avoid_: treating merge formatting as thought generation

**Brain Runtime Metrics** — observable counters and quality scores for language-facing runtime output, grounding, topic diversity, SNN alignment, and inference latency. They are telemetry, not proof of liveness, and must be usable without instantiating ThoughtLoop.
_Avoid_: treating generated text counts as Living Brain evidence by themselves

**Benchmark Evidence Currency** — read-only Runtime Truth evidence about whether saved benchmark reports are present, fresh, stale, failed, or missing. It may summarize accepted baselines, fresh benchmark bundles, and regression gates from report files, but it must not run benchmarks, mutate runtime state, change Runtime Truth verdicts, or claim CUDA/speed capability by itself.
_Avoid_: treating a fresh report as liveness proof, running benchmark work from status, or hiding stale hot-path evidence

**Background Tick Execution Evidence** — retired Terminus-era wording for the old service-owned loop. The active lifecycle surface is `Brain-Owned Lifecycle Loop`; `/brain/start` and `/brain/stop` report compact `marulho_brain_loop_state.v1` from `MarulhoBrain`, not `RuntimeControl`.

**File Source Ingestion Cache** — the runtime-source cache for deterministic local/file text sources. It stores bounded encoded source windows under the checkpoint runtime cache using the source path, size, and modification timestamp in the cache key, so a later configure can restore ready source tokens before the cognitive tick. Restored material carries a hash so an unchanged source window can skip a synchronous cache rewrite during tick preparation. It is ingestion metabolism, not memory, replay, or cognition.
_Avoid_: reusing stale file contents, counting cache restore as learned memory, rewriting unchanged cache material inside tick preparation, or hiding source encoding inside tick latency when a reviewed cache/prewarm path can own it.

**Prepared Source Generation** — one immutable, cache-backed generation of encoded source windows owned by Runtime Sources. Ordinary queue consumption does not change the generation, so a cognitive tick can prove cache stability in constant time without rebuilding or hashing the remaining queue. Only refill, stream replacement, source change, failed persistence retry, or another real material mutation may construct a new cache payload. Runtime Truth reports stable-generation skips separately from partial-tail and hash-equality skips.
_Avoid_: treating deque consumption as new persistence material, scanning a full warm queue on every tick, hiding refill behind a stable-generation claim, or interpreting ingestion-cache reuse as neural cognition.

**Bounded Batched Text Ingestion** — the live Runtime Sources rule that inference-only character windows are assembled in a bounded CPU control batch and emitted as one device tensor batch when the learned-chunk codebook is empty. The output preserves the scalar RTF representation within declared floating tolerance, while chunk-codebook mutation stays in explicit training or remote-bootstrap work. Runtime Truth reports the encoder mode and that hot-path chunk plasticity is disabled.
_Avoid_: launching scalar CUDA work per character, consuming an unbounded source before yielding, learning chunk codebooks inside source collection, or calling cache/prewarm speed a cognitive-kernel speedup.

**Bounded Source Concept Observation** — the Brain Runtime rule that source-window ConceptStore observation is sampled, capped, and cadenced across service ticks. Neural `train_step` still runs for every emitted token, but knowledge-layer concept observation and structural maintenance wake on the first source tick and then every configured `source_concept_observation_tick_interval` ticks by default. Due ticks run one batched ConceptStore observation over at most four sampled source windows; skipped ticks retain neural learning and report `cadenced_tick_skip`, `tick_interval`, `tick_due`, attempts, skipped attempts, observed updates, and structural-maintenance pass counts in tick evidence.
_Avoid_: scaling ConceptStore observation linearly with a larger source tick, hiding skipped telemetry samples, treating cadenced ConceptStore observation as skipped neural learning, or moving concept-maintenance algorithms into `service`.

**Measured Source Tick Window** — the maintained Terminus source tick default is the currently measured fastest sustained service window, not a biological claim or compatibility promise. The 2026-06-13 CUDA evidence promotes `128` source tokens per tick because larger `256` and `512` windows regressed complete service throughput despite the same persistent graph executor. Runtime Truth and benchmarks must report the configured tick width so future runners can remeasure and promote a different window.
_Avoid_: treating bigger tick windows as automatically faster, hiding tick width in benchmarks, keeping stale defaults after contrary evidence, or using source-window batching to skip sequential token training.

**Source-Window Prewarm Gate** — the service benchmark and production source path treat source ingestion/encoding as a slow-path readiness boundary before a measured cognitive tick. A configured local source can enable ingestion prewarm, wait for Runtime Truth `full_warm_ready`, and then measure the tick over an already buffered 128-token source window. The warmup evidence records wait budget, ready/full source counts, buffered tokens, startup latency, and `not_hot_path=true`; the tick evidence still reports collection and training costs. This is source metabolism, not a neural-kernel speed claim.
_Avoid_: measuring cold source encoding as if it were unavoidable cognition, claiming prewarm wait time as free, hiding whether the source queue was full before tick measurement, or deleting the cold-source benchmark escape hatch needed for regressions.

**Hot-Path-Safe Quick Start** — the control-room quick-start preset must configure the same promoted source-tick runtime used by sustained speed evidence: bounded source prewarm, 128-token source ticks, 16-token execution quanta, and no always-on context or binding layer that would force the burst executor to fall back before a conditional scheduler owns those specialists. Context, binding, and future column specialists can wake through explicit measured schedulers; they are not startup defaults simply because the UI wants a richer brain view.
_Avoid_: enabling all higher layers in quick-start, treating the control-room run as comparable speed evidence when it bypasses the burst path, or hiding startup source prewarm behind the first measured cognitive tick.

**Deferred Slow Memory Cadence** — the trainer policy that keeps routine fixed-interval replay admission out of the live burst transition path. The hot path still records first-token retained admission on warmup/fallback and exact strong-capture events from the Device Strong-Event Ring, but fixed-cadence hits are now deferred maintenance evidence instead of an archival write path. They must not force per-token `train_step` execution or CPU `DualMemoryStore.update()`. Runtime Truth reports deferred cadence count and last cadence token through the cognitive boundary controller, while `memory_hot_path` exposes skipped archival pressure. Retired checkpoint defaults at `8` and `64` still migrate to `256` to avoid stale tight-cadence configurations.
_Avoid_: treating fixed-interval slow-memory writes as required spike cognition, hiding cadence deferrals, moving replay/consolidation work into `service`, or using a richer event ring that slows the complete runtime without replay-quality evidence.

**Continuous Execution Quantum** — the Runtime Control scheduling budget for sequential background and manual source ticks. The production default trains up to `16` tokens under one execution-lock acquisition, performs no artificial inter-token sleep, and checks stop responsiveness between quanta. Training queries the transition runtime for its current burst capacity and consumes each quantum as exact ordered persistent device bursts. The promoted CUDA sequence boundary is the conditional-WHILE q16 executor; the repeated-child native parent graph remains an exact eight-token internal fallback when conditional construction cannot run before mutation. A measured q16 Python burst-group candidate halved host burst groups but did not reduce per-token CUDA graph replay launches and regressed complete runtime, so a real speed win requires device-owned sequence control rather than merely widening the Python group. The configured quantum is bounded by both the source tick width and a `128`-token ceiling. Runtime Truth exposes the quantum, yield, stop boundary, inner burst evidence, and the fact that token training remains sequential. This is host orchestration policy, not neural batching or a change to SNN learning order.
_Avoid_: restoring one-token lock/yield cycles as the default, claiming the quantum executes tokens in parallel, widening Python burst groups as a launch-reduction claim, allowing unbounded quanta to freeze stop/status responsiveness, or moving trainer algorithms into Runtime Control.

**Source Tick Sleep Deferral** — the `MarulhoBrain.tick` rule that a live/background source tick must not run deep, micro, or repair sleep replay from the per-token `train_step` fallback. When a brain tick needs per-token fallback for metrics or unsupported burst execution, it delegates to training with `allow_sleep_maintenance=False` so due sleep is counted as deferred maintenance and replay remains available only through explicit slow-path sleep/replay windows. This protects source-tick latency while preserving trainer-owned sleep replay quality gates.
_Avoid_: letting service or a recreated service BrainRuntime become a sleep scheduler, hiding due sleep replay, running sleep replay because a source tick needed full metrics, or treating deferred sleep as completed consolidation.

**Bounded Live Memory Summary Projection** — the read-only memory-store status surface for trainer telemetry, service status, living-loop snapshots, and brain-runtime summaries. It reports scalar fill/counter/report fields through `DualMemoryStore.live_summary_stats()` without advancing STC decay, constructing tensors over retained slow entries, or scanning archival metadata. Full `summary_stats()` remains available only for explicit offline consolidation and quality windows that can pay for a complete memory scan.
_Avoid_: calling full memory summary from a live tick or status endpoint, letting display telemetry decay STC tags, using status as replay selection, or hiding a full archive scan behind a compact Runtime Truth memory field.

**Bounded Plasticity Runtime Transition Memory Source Window** — the read-only SNN language plasticity runtime-state projection for retained transition memory. `/terminus/snn-language-sequence/plasticity-runtime-state` may expose recent sparse weights and synapse provenance for operator/status consumers, but it must not deep-copy the full retained transition memory. It reports `bounded_snn_language_plasticity_runtime_transition_memory_source_window.v1`, keeps retained counts separate from the returned source rows, limits the returned source window to newest CPU archival rows, and marks exact readiness as partial when truncated. Exact audits or consolidation still move to explicit slow windows; the runtime-state endpoint is not replay execution, hidden language reasoning, or live-tick memory recall.
_Avoid_: treating returned runtime-state maps as complete when `source_window_complete=false`, restoring full snapshot deep copies, putting archival provenance on CUDA, or letting status/runtime-state reads become every-token replay or consolidation work.

**Route/Vote Kernel Variant Evidence** — the Runtime Truth field that names the active Triton route/vote kernel family for promoted CUDA text routing. The maintained live path is `two_stage_route_vote`: one Triton route-score kernel followed by the retained select/vote kernel. That select/vote owner may mask deep-sleep rows and, when cached column-pressure evidence exists, high memory-pressure rows before top-k candidate and winner selection; the expanded scheduler-filter packet reports both counts and fallback reasons. A direct one-block route/vote fusion for the live `1024 x 64` checkpoint was measured and rejected because it slowed complete runtime; it is not retained as hot-path code. This evidence is a kernel-boundary truth field, not a capability claim or a promise that the next executor has fewer launches.
_Avoid_: hiding route/vote fallback behind generic `fused_triton_text`, reintroducing direct route/vote fusion without sustained complete-runtime evidence, or claiming launch reduction from a kernel variant that leaves one CUDA Graph replay per token intact.

**Native Burst Replay Coverage** — the Runtime Truth fields that show which token counts are covered by native CUDA Graph parent launches. The maintained repeated-child fallback now covers exact eight-token bursts only; partial parent-graph replay for non-eight-token tails is retired after the complete-runtime 130-token tick shape regressed despite removing the partial Python replay fallback. Non-eight-token tails fall back explicitly to the Python replay loop and report `python_loop_partial_disabled`. Native16 and native32 repeated-child capacity probes are historical evidence only: the config is fixed at `cuda_graph_native_burst_tokens=8`, old checkpoint values are migrated back to `8`, and the stress benchmark no longer accepts native burst capacity overrides. The 2026-06-16 8192-column cleanup gate at `reports/column_scheduler_20260616/native-burst-capacity-cleanup-8192-131072-i32.json` reached `6276.616 tokens/sec`, `train_compute=0.129187 ms/token`, `tick_p95=21.513 ms`, `route_input_rows_scored=10/8192`, `state_transition_cached_count=8182`, `native_partial_burst_replay_enabled=false`, zero graph/native/sequence failures, and no observed contention. Conditional-WHILE parent graphs expose separate `native_sequence_loop_*` Runtime Truth fields because they are a different lower-level executor, not merely another repeated-child capacity. Parent token counts, success/token coverage, fallback counters, and Python-loop token counts must be read before claiming a launch-boundary improvement.
_Avoid_: lazily compiling new parent graph token counts inside an ordinary measured cognitive tick, restoring native16/native32 or partial-tail replay as dormant knobs, hiding unaligned tick-token choices that force host-truth burst fallbacks, or treating a warmed parent graph as native token coverage when the replay counters show Python-loop fallback.

**Conditional-WHILE Text Sequence Executor** — the promoted CUDA Graph conditional-node executor fixed as the eligible CUDA text sequence path. It wraps the retained one-tick child graph in a CUDA conditional `WHILE` parent graph and uses a tiny device kernel to decrement the bounded loop counter after each ordered child replay. It is a true device-owned sequence boundary because the loop control is in CUDA Graph/device code, while pre-mutation fallback returns to the retained repeated-child executor if construction fails and launch failures still fail closed. The old config/env/CLI selector is retired: `cuda_graph_sequence_executor` is fixed at `conditional_while`, the stress benchmark no longer accepts a sequence-executor override, and checkpoint load migrates old selector values with `retired_sequence_executor_selector`. The first clean 131072-token evidence beat both the historical `4992.049 tokens/sec` ceiling and a same-session native8 rerun; repeated paired gates then kept q16 ahead in both orders, the post-promotion default run reached `6116.646 tokens/sec`, and the current completion-audit rerun reached `5955.123 tokens/sec` with zero sequence/native failures or fallbacks and no observed velocity contention. Runtime Truth reports the requested executor, token coverage, failures/fallbacks, startup compile/capture cost, fixed q16 sequence-loop capacity, and explicit sequential-state parity plus bounded-quality gate statuses. The promoted loop capacity is fixed at `cuda_graph_sequence_loop_tokens=16`; `cuda_graph_native_burst_tokens=8` is fixed as the repeated-child internal fallback capacity. The 2026-06-16 fixed-q16 cleanup rerun at `reports/column_scheduler_20260616/sequence-loop-capacity-cleanup-8192-131072-i32-rerun.json` reached `6145.401 tokens/sec`, `train_compute=0.129706 ms/token`, `tick_p95=21.270 ms`, `route_input_rows_scored=10/8192`, `state_transition_cached_count=8182`, `persistent_executor_sequence_loop_capacity_fixed=true`, zero graph/native/sequence failures, and no observed contention. The 2026-06-16 selector-cleanup run at `reports/column_scheduler_20260616/sequence-executor-selector-cleanup-8192-131072-i32.json` reached `6133.925 tokens/sec`, `train_compute=0.129945 ms/token`, `tick_p95=21.499 ms`, `route_input_rows_scored=10/8192`, `state_transition_cached_count=8182`, `state_transition_runs_all_columns=false`, `native_sequence_executor_requested=cuda_graph_conditional_while`, zero graph/native/sequence failures, and no observed contention.
_Avoid_: hiding CUDA extension build compatibility, restoring rejected native16/native32 repeated-child capacity knobs, restoring q8/q32 conditional-WHILE capacity knobs, or calling a requested conditional run successful without `native_sequence_loop_success_count`, token coverage, fallback/failure counters, host-truth cadence, and startup compile/capture cost.

**Lower-Level Text Sequence Executor Requirement** — the active CUDA boundary that says the next promotable text executor must move below the current Python/CUDA Graph replay wrapper. Conditional-WHILE q16 is now the promoted default for eligible text sequences; native8 repeated-child replay remains an internal fallback, not a selectable parallel path. Native16 and native32 repeated-child capacity probes, nested graph bodies, native host loops, wider truth cadences, and route/vote wrappers are rejection evidence rather than the next default. A candidate successor must own a bounded multi-tick sequence loop in C++/CUDA, Triton persistent kernels, CUDA device/conditional graph code, or a hybrid while preserving sequential state parity, fail-closed fallback, host-truth cadence, startup-cost reporting, and Runtime Truth coverage.
_Avoid_: claiming another parent-capacity knob, local launcher, scalar report, or route/vote wrapper is the requested device-owned sequence executor without long clean evidence and parity gates.

**Sustained Runtime Evidence** — the rule that production speed/runtime claims use long, full-warm continuous runtime runs, not short hot-window or 4096-token samples. The promotion ladder is `8192` tokens as the first diagnostic boundary, `131072` tokens as the normal long-run gate, and `524288` tokens as the house-scale target when hardware/runtime budget allows. Benchmark reports must state target tokens, token delta, tick width, quantum, polling behavior, event coverage, final/last `BrainTrace`, CUDA graph replay/failure counts, native/burst/sequence fallback counters, dominant stage costs, and slow-path run-condition evidence for CPU/GPU contention. Short `256`, `1024`, and `4096` token runs remain useful only for smoke/debug checks and A/B rejection; they are not promotion evidence. The same-day 2026-07-03 unqualified reports `reports/runtime_evidence_20260703/diagnostic-8192.json`, `reports/runtime_evidence_20260703/long-gate-131072.json`, and `reports/runtime_evidence_20260703/house-scale-524288.json` are rejected regression evidence, not current speed evidence: `MarulhoBrain.feed(..., learn=False)` still forced learned-chunk mutation/projection, and tick readout keys recomputed offline winners per token after the trainer already had winner evidence. The fixed long-run artifact `reports/runtime_evidence_20260703/long-gate-131072-after-feed-readout-fix.json` reached `131072/131072` tokens at `5608.147 tokens/sec`, mean tick `17.800 ms`, p95 `20.073 ms`, CUDA RTX 3060, `conditional_while`, zero CUDA graph/native/sequence failures or fallbacks, bounded `12/65536` route rows, no all-column state transition, bounded streaming source refill with `16` feeds, zero source drops, and no observed contention. The fixed house-scale artifact `reports/runtime_evidence_20260703/house-scale-524288-after-feed-readout-fix.json` reached `524288/524288` tokens at `5877.601 tokens/sec`, mean tick `17.445 ms`, p95 `19.358 ms`, with the same CUDA backend, bounded-route/state-transition evidence, `64` bounded source feeds, zero source drops, and no observed contention. These fixed reports close the normal long-run and house-scale artifact boundaries while preserving the historical 6k-ish hot-path band. The preserved pre-fix failure `reports/runtime_evidence_20260703/long-gate-131072-source-exhausted-before-refill.json` exhausted the bounded `8192`-token source buffer after `8192` tokens, so long targets must use bounded streaming refill.
_Avoid_: quoting the fastest short sample as system speed, allowing measurement polling to contend for runtime locks, hiding prewarm/source-refill cost, ignoring host/GPU contention when comparing long runs, treating graph/kernel throughput as complete cognitive runtime throughput, or feeding a long source into the bounded brain source buffer in one shot.

**Velocity Environment Evidence** — the non-hot-path CPU/GPU condition snapshot collected around a sustained stress benchmark. It records whether benchmark comparability was likely affected by host CPU saturation, graphics/GPU contention, unavailable probes, or other run-condition signals. It is evidence for interpreting throughput variance, not a cognitive signal, scheduler input, runtime capability claim, or reason to synchronize inside a live tick.
_Avoid_: moving machine probes into the cognitive tick, treating contention as a code regression by itself, or discarding a correct CUDA run because the environment was busy.

**Device-Burst Lightweight Metrics** — the bounded Runtime Truth metrics derived from the persistent CUDA burst result packet at a device-event drain boundary. They expose final token, winner, reconstruction/prediction error, neuromodulator scalars, and execution mode without forcing sampled source-concept slots through full per-token `train_step` metrics. They are operator/runtime evidence, not slow-memory admission evidence; absent `memory_index` means ConceptStore observation must skip rather than synthesizing memory support.
_Avoid_: draining CUDA tensors just to build rich display metrics, treating lightweight metrics as replay-memory records, or making source concept sampling break burst execution when no slow-memory record exists.

**Quiescent Checkpoint Save** — the Runtime Persistence rule that manual checkpoint save is accepted only while Terminus is stopped and no active mutation owns the runtime lock. Busy or running saves fail fast without creating an artifact; operators stop the runtime first, then save the coherent state. Stream interruption must not call external `close()` implementations while holding the manager lock.
_Avoid_: waiting indefinitely for a training tick, serializing a live-mutating trainer, closing remote iterators under the global lock, or treating an HTTP timeout as proof that no checkpoint file was written
_Avoid_: treating an in-progress phase as completed learning, increasing `/status` blocking time to fake freshness, or moving neural algorithms into service lifecycle code

**Subcortex Language Surface** — an evidence-facing translation layer that can express runtime state in language without becoming the cognition substrate. It has two grounded slices: the interaction responder's native-decode surface, and the Cognitive Signal surface that turns prediction error, confidence, neuromodulator pressure, and concept focus into auditable operator text.
_Avoid_: treating text fluency as liveness, hidden LLM mind, or ungrounded thought generation

**Subcortex Spike Readout Evidence** — a MARULHO-owned, read-only bridge from Cognitive Signal and CUDA/runtime placement evidence into bounded spike-language readout slots. It is deterministic population-code evidence for future SNN decoders; it must not generate text, mutate runtime state, load external checkpoints, or become the cognition substrate.
_Avoid_: treating readout slots as generated thoughts, importing reference checkpoints, or bypassing sparsity/device/grounding gates

**Subcortex Readout** — operator-facing language or status output decoded from MARULHO-owned Subcortex state, grounded evidence, and readout metrics. It is reportable evidence, not a ThoughtLoop thought stream, narrative self, or proof of liveness by fluency.
_Avoid_: `thoughts` counters, narrative-self report sections, dormant LLM generation aliases

**SNN-Native Language Readiness Gate** — the operator-facing read-only artifact for future MARULHO-owned language generation. External pure-SNN language projects may inform design, but MARULHO must own the language neurons, decoder, training loop, grounding, telemetry, and promotion gates before language generation can move beyond a surface.
_Avoid_: loading external checkpoints as the brain, outsourcing language cognition, treating reference implementations as runtime dependencies

**Spike Language Decoder Probe** — a MARULHO-owned, read-only probe that turns Subcortex Spike Readout Evidence into sparse population-code, leaky recurrent state, temporal transition, device, and support evidence. It is evidence for future SNN language machinery; it must not generate text, decode free-form language, train, mutate runtime state, or satisfy the language-generation gate by itself.
_Avoid_: treating sparse labels as generated thoughts, using the probe as a mock generator, or calling temporal-state evidence a trained language model

**Spike Language Neuron Adapter** — a MARULHO-owned, read-only PLIF-style adapter that consumes Spike Language Decoder Probe sparse indices and reports bounded membrane/spike dynamics, activation sparsity, adaptive timestep use, and device placement. It is the first local language-neuron module boundary, but it is still evidence only until training, grounding evaluation, and operator gates promote a generator.
_Avoid_: treating adapter spikes as generated text, loading external language checkpoints, or using the adapter as a cognition substrate

**SNN Language Adapter Evaluation Gate** — the operator-facing read-only plan for testing the Spike Language Neuron Adapter in isolation. It names heldout grounded readout slots, grounding delta, activation-sparsity delta, Runtime Truth delta, and rollback evidence required before any later training loop can be approved; it cannot train, decode text, generate language, or promote the adapter as a cognition substrate.
_Avoid_: treating evaluation readiness as generation approval, hiding rollback requirements, or promoting adapter spikes directly into facts/actions

**Heldout Language Adapter Evaluation** — a deterministic, read-only evaluator that runs the local Spike Language Decoder Probe and Spike Language Neuron Adapter over heldout readout-slot batches, available to operators through `/terminus/snn-language-evaluation/heldout`. It reports grounded support, adapter spike counts, activation sparsity, and device evidence without training or decoding text.
_Avoid_: evaluating on generated prompts, mutating adapter weights during evaluation, or using heldout support as language-generation proof

**SNN Language Training Readiness Gate** — the read-only operator gate after Heldout Language Adapter Evaluation, available through `/terminus/snn-language-training/readiness`. It checks heldout support, activation sparsity, Runtime Truth delta, rollback evidence, and absence of external checkpoints before a MARULHO-owned local SNN language trainer can be designed; it cannot train, generate text, or promote a cognition substrate.
_Avoid_: treating training-design readiness as trainer execution, loading external SNN language checkpoints, or skipping grounded heldout evidence

**SNN Language Trainer Dry Run** — an isolated MARULHO-owned local-learning experiment over grounded spike readout slot sequences, available through `/terminus/snn-language-training/dry-run`. It may update ephemeral in-memory weights to measure transition support and sparsity, but it must discard weights, avoid text generation, and never mutate the live runtime model.
_Avoid_: treating dry-run weights as a checkpoint, decoding text from dry-run support, or calling dry-run success production training

**SNN Language Trainer Isolated Evaluation** — the read-only gate after SNN Language Trainer Dry Run, available through `/terminus/snn-language-training/evaluate`. It checks validation transition support, sparse weight evidence, device placement, Runtime Truth delta, and rollback evidence before a local trainer design can be reviewed; it still cannot promote runtime training, return weights, decode text, or generate language.
_Avoid_: treating trainer evaluation as a live learner, storing dry-run weights, or skipping operator review

**SNN Language Sequence Prediction Probe** — an isolated MARULHO-owned probe that trains ephemeral sparse transition weights from grounded spike readout slot sequences and predicts the next sparse population-code indices, available through `/terminus/snn-language-sequence/predict`. It may report predicted spike indices and strengths, but it cannot decode text, generate language, store weights, or mutate runtime state.
_Avoid_: calling predicted sparse indices a sentence, fact, or thought; persisting probe weights; using the probe as a cognition substrate

**Persistent SNN Language Transition Memory** — checkpointed MARULHO-owned sparse transition weights created by the SNN Language Plasticity Live Application Executor and read by the Sequence Prediction Probe. It can bias future sparse-code prediction and report influence evidence, but it is still not text generation, fact promotion, or an external model checkpoint.
_Avoid_: treating transition-memory influence as decoded language, accepting non-MARULHO weights, or hiding whether persistent state changed prediction

**SNN Language Readout Draft** — the first MARULHO-owned bounded text-producing surface over SNN language evidence. It maps sparse next-code prediction and persistent transition-memory influence onto grounded readout-slot labels to produce an operator-reviewable draft. A draft can be emitted before promotion, but bounded-readout generation readiness requires a non-worsening SNN Language Transition Memory Prediction Evaluation over grounded windows. It is not free-form language generation, fact promotion, action authority, or a cognition substrate.
_Avoid_: calling readout labels thoughts, bypassing grounding vocabulary, treating one influenced prediction as draft readiness, or treating a draft as autonomous truth

**SNN Language Readout Rollout Candidate** — a MARULHO-owned, read-only multi-step sparse transition rollout over Persistent SNN Language Transition Memory. It starts from current sparse readout evidence, advances bounded sparse states, and maps each step only to grounded readout-vocabulary labels so operators can review short SNN-native language candidates without free-form decoding, external checkpoints, runtime mutation, fact promotion, or action authority.
_Avoid_: naming rollout candidates thoughts, using ungrounded vocabulary labels, treating rollout text as autonomous truth, or allowing rollout evidence to apply plasticity

**SNN Language Readout Rollout Replay Evaluation** — a MARULHO-owned, read-only review gate that turns a bounded Readout Rollout Candidate into hash-addressed replay-review targets. It verifies candidate provenance, grounded labels, transition-memory review evidence, and trace hashes before any ledger recording or replay-priority step. It does not generate text, decode free-form language, train, apply plasticity, mutate runtime state, or load external NeuronSpark/Nord-AI artifacts.
_Avoid_: recording rollout replay targets directly, treating review targets as replay priority, importing reference-project code as runtime dependency, or using this gate as cognition-substrate approval

**SNN Language Readout Rollout Evidence Ledger Record** — an operator-approved durable record of a review-ready SNN Language Readout Rollout Replay Evaluation. It stores grounded rollout replay targets and provenance in the readout ledger's separate rollout-event channel so they can be audited later without entering draft replay priority, live replay, plasticity, fact promotion, action authority, or cognition-substrate status.
_Avoid_: mixing rollout evidence into draft replay priority, recording without operator confirmation, recording ungrounded or hash-invalid targets, or treating durable rollout evidence as learned language

**SNN Language Readout Rollout Rehearsal Promotion Policy** — a MARULHO-owned, read-only deterministic ranking over recorded rollout evidence. It measures provenance, grounded replay targets, trace integrity, recency, transition-memory reuse, repetition, sparse occupancy, and CUDA/device placement to identify candidates for operator review of isolated rollout rehearsal. It does not execute rehearsal, enter draft replay priority, train, apply plasticity, promote facts/actions, or become the cognition substrate.
_Avoid_: ranking unrecorded rollouts, treating policy rank as replay execution, hiding CPU fallback when CUDA is requested, or bypassing operator review

**SNN Language Readout Rollout Rehearsal Evaluation** — an isolated MARULHO-owned ephemeral SNN replay measurement over policy-approved rollout evidence. It reconstructs sparse step vectors on the recorded observed device, measures activation sparsity and adjacent-state temporal continuity, verifies device placement and target hashes, and discards every tensor after review. It does not persist weights, write checkpoints, mutate runtime state, execute live replay, or apply plasticity.
_Avoid_: accepting caller-selected devices, rehearsing unranked rollout records, persisting ephemeral tensors, or treating rehearsal stability as learned language

**SNN Language Readout Rollout Rehearsal Experiment** — a MARULHO-owned repeated-cycle stability experiment over an approved Rollout Rehearsal Evaluation. It replays the isolated sparse trajectory for bounded cycles, measures cycle-to-cycle hash stability, drift, and continuity retention, and emits an operator-reviewable experiment hash. It does not persist weights, write checkpoints, mutate runtime state, execute live replay, or apply plasticity.
_Avoid_: calling repeated ephemeral cycles consolidation, accepting unstable cycles for promotion, persisting experiment tensors, or bypassing operator review

**SNN Language Readout Rollout Consolidation Design** — a MARULHO-owned read-only proposal for bounded local synaptic adjustments after stable rollout rehearsal cycles. It derives local temporal synapse candidates, capped deltas, homeostatic decay, normalization, and rollback evidence for operator review. It does not write synapses, persist weights, mutate runtime state, execute replay, or apply plasticity.
_Avoid_: treating a proposal as consolidation, omitting rollback evidence, allowing non-local updates, or crossing directly into structural writes

**Sparse Rollout Consolidation Candidate Edge** — a local source-neuron to target-neuron proposal derived from adjacent sparse rollout trajectory states. Rollout step indices are provenance only; the candidate edge identity must come from canonical sparse neuron indices observed in the rehearsed transition, so later shadow deltas, growth review, and regeneration evidence stay tied to actual spike activity rather than placeholder sequence positions.
Its promotion evidence must preserve source/target rollout step indices and source/target active-index hashes through developmental growth review and regeneration design, so replay-bound growth remains auditable after the design is adapted for the checkpoint-backed executor.
_Avoid_: using rollout step numbers as synapse coordinates, fabricating edges without adjacent sparse states, dropping active-index hashes before regeneration review, importing reference-project topology, or treating one candidate edge as applied structural growth

**SNN Language Readout Rollout Consolidation Shadow Delta** — an isolated MARULHO-owned sparse tensor materialization of a reviewed Rollout Consolidation Design. It computes bounded local synapse deltas on the requested CUDA/device surface, reports honest fallback evidence, and discards the tensor after review. It does not write synapses, persist weights, mutate runtime state, execute live replay, or apply plasticity.
_Avoid_: hiding CUDA fallback, applying shadow tensors, accepting empty designs, or treating shadow deltas as consolidated memory

**SNN Language Readout Rollout Consolidation Shadow Application Preflight** — a MARULHO-owned, read-only integrity review of a Rollout Consolidation Shadow Delta before any application path exists. It verifies hash identity, bounded local coordinates, normalization, homeostatic decay, CUDA/device truth, rollback-snapshot binding, and topology invariants against server-held transition memory. Missing runtime edges are classified as growth candidates for separate structural review. It does not apply shadow tensors, grow or prune synapses, mutate runtime state, persist weights, execute live replay, or authorize plasticity.
_Avoid_: bypassing shadow hashes, accepting a rollback snapshot unrelated to the design, mixing functional consolidation with structural growth/pruning, or treating preflight review as application authority

**Column Runtime Metabolism** — the control-plane evidence for a Thousand-Brains-inspired MARULHO runtime where many predictive columns may exist, but only a bounded subset should be awake on a tick. It summarizes total columns, awake budget, active/idle/candidate/sleeping/deep-sleeping/retired counts, cached votes, sampled registry state, vote confidence/evidence/usefulness/cost/memory-pressure/disagreement, wake reasons, fail-closed repeated-surprise growth gates, pruning/homeostasis gates, bounded local associative-recall availability, and the last observed execution scopes. CUDA-resident status uses one latency-first column-state snapshot for scheduler evidence because a bounded device-export attempt reduced bytes but increased current 1024/8192-column latency. Candidate-scoped competitive scoring, candidate homeostasis with lazy idle materialization, lazy candidate threshold-relaxation replay, retained CPU and promoted CUDA sparse state transition for stale counters/recent-spike rows/assembly active winner, candidate-scoped CPU and fused CUDA predictive updates with ordered candidate-bounded cached-state wake, retained predictive-vote cache wake, retained CPU candidate deep-sleep/memory-pressure/usefulness filters, CUDA route-owned deep-sleep plus evidence-backed memory-pressure and usefulness masking, wake-plan-scoped structural-review ticket capture, and wake-plan-scoped awake-ripple replay tagging may be live when retrieval or route selection has already narrowed a bounded awake mask. `ColumnMetabolismState.usefulness` is checkpointed cached scheduler state, updated only for awake candidates from predictive confidence/error, win-rate, and cost; report-derived usefulness is only Runtime Truth fallback, not sleep authority. The live retained route now produces a training-owned **Column Wake Plan** instead of treating a raw candidate tensor as the scheduler contract; the plan carries awake IDs, wake/sleep reason, fallback reason, bounded truth, `runs_all_columns=false`, filter counts, and the specialist consumers that must share the same mask. Runtime Truth must keep `runs_all_columns` true whenever any live specialist still touches every column, including predictive location, prediction update, vote, homeostasis, state transition, fallback-relaxation paths, structural-review capture, or unscoped memory/replay helper work. Growth/pruning mutation and associative recall remain explicit reviewed helper surfaces until separately promoted; `ColumnStructuralReviewQueue` may persist checkpoint-backed operator-review tickets from bounded candidates, but it still reports `mutates_runtime_state=false`.
_Avoid_: treating bounded awake-column evidence as execution authority, growing columns from status, pruning from a report, building wake plans in `service`, or claiming Thousand-Brains completeness from a compact Runtime Truth summary

**Route-Vote Scheduler Filter** — the training/core-owned CUDA scheduler effect that masks deep-sleep, high-memory-pressure, and low-usefulness route rows inside the existing fused route-score/top-k vote boundary before candidate voting and winner selection. It uses the existing route-score rows, `steps_since_win`, cached `memory_pressure`, and cached `usefulness` device state; it does not run a separate all-column sleep, pressure, or utility scan. Runtime Truth reports input/output route counts, filtered deep-sleep count, filtered pressure count, filtered low-usefulness count, eligible counts, cached state-sync cadence, fallback reason, tensor device, and `runs_all_columns=false` only when the selected specialist work shares that bounded route-vote mask. If too few eligible route rows remain, the route-vote owner keeps the unfiltered fixed-k route result and reports an explicit fallback instead of pretending sleep or usefulness filtering was applied.
_Avoid_: filtering a fused route-vote candidate tensor after winner selection, deriving usefulness only in service/report code and calling it sleep, synchronizing every token just to mirror filter counts, or keeping old route-vote compatibility wrappers for stale development checkpoints

**Development Checkpoint Compatibility** — during current foundation work, local development checkpoints are evidence inputs or rollback artifacts, not public compatibility promises. Keep the current published checkpoint transaction and the exact checkpoint files referenced by active benchmark reports; otherwise stale checkpoint folders, generated runtime caches, and import shims must not force old code paths to remain alive.
_Avoid_: preserving dead branches solely for old local checkpoints, deleting benchmark JSON evidence, or weakening checkpoint-backed growth/pruning transactions

**Runtime Machinery Plane** — the read-only operator visualization that spatially projects current Runtime Truth into grounded input, spike encoding, sparse predictive columns, context/binding, memory/replay, and consensus/readout regions. Its highlighted paths and awake-column points must come from observed runtime fields. Dormant column density, replay links, and topology feedback are architectural context unless Runtime Truth explicitly reports them as active.
_Avoid_: decorative neural animation, implying every drawn subsystem executes on every tick, treating the plane as a scheduler, or using UI motion as liveness/CUDA evidence

**Prediction Failure Streak** — predictive-column-owned consecutive raw prediction failure evidence used by Column Runtime growth gates. It is stored on the same device as predictive state, checkpointed with the model, reset by successful prediction, and exposed through Runtime Truth only as bounded growth evidence. It is not structural mutation authority by itself.
_Avoid_: deriving growth candidates from one-shot surprise, keeping streaks only in service/status projections, or treating a ready growth gate as permission to mutate without operator review and checkpoint evidence

**Binding Growth Trial Design** — an explicit read-only slow-path artifact that converts current Prediction Failure Streak candidates into a deterministic, candidate-scoped, edge-budgeted hypercube outreach proposal. Core owns sparse edge planning and a hash of the exact baseline topology; semantics binds that plan to the current Runtime State revision and growth evidence. It does not clone a checkpoint, run cognition, refresh topology, or authorize mutation.
_Avoid_: planning edges during the tick, accepting caller-authored candidate IDs as live evidence, treating proposed edges as improved cognition, or skipping isolated Runtime Truth/spike-health evaluation before the operator transaction

**Column Execution Scope** — the observed number and fraction of competitive columns whose prototype and input-drive scores were evaluated for the latest routing step. With learned chunking, projected input is already the routing key, so MARULHO may retrieve candidates before scoring columns and evaluate only that exact candidate set. Dense assembly remains required for representations whose routing key is derived from the full assembly. This does not prove that sleeping columns avoid every state update or that the report scheduler controls execution.
_Avoid_: counting configured `k_routing` as observed sparsity, calling an all-column candidate set sparse, or skipping dense assembly where it contributes to the routing key

**Candidate-Scoped Homeostasis** — the first promoted sleep-aware competitive-column execution effect. Once stale/deep-sleep counters can exist, the trainer passes the retrieved competitive candidate set into `CompetitiveColumnLayer.process()`, and competitive win-rate/threshold homeostasis updates only those active candidates. Idle columns keep checkpointed step stamps; when a cached column wakes as a routed candidate, core materializes only that bounded set through missed zero-activity homeostasis and missed fallback threshold-relaxation events before scoring. The common no-relaxation/no-clamp wake window may use an exact closed-form zero-activity homeostasis catch-up; relaxation or clamp-sensitive windows retain ordered replay. Early learning remains all-column to preserve diversity; stale counters still advance as evidence; topology does not mutate.
_Avoid_: candidate-scoping homeostasis before stale evidence can exist, freezing early learning, scanning all stale counters just to avoid updates, applying fallback threshold relaxation to sleeping columns globally, using closed-form wake catch-up when relaxation/clamp order matters, or claiming that this makes predictive/context/binding state sparse

**Candidate-Scoped State Transition** — the retained CPU and promoted CUDA competitive-column path that advances logical `steps_since_win`, recent-spike-window evidence, assembly active-winner state, and candidate threshold/win-rate homeostasis without mutating every column on each candidate-scoped tick. Awake candidates and winners are materialized exactly before update; non-awake columns keep cached stale-age stamps, and `state_transition_steps_snapshot()` reconstructs exact logical counters for Runtime Truth, checkpoint save, spike-health checks, route-vote deep-sleep masking, and explicit maintenance. CUDA graph/text ticks pass the same step-counter, all-materialized-step, per-row stamp, recent-spike active-id, and assembly-active-winner tensors into the fused transition so candidate-subset runs report `candidate_subset_sparse_*`, cached count, and `state_transition_runs_all_columns=false`. Dense all-column startup/fallback remains truthful and still records scalar all-materialized-step metadata instead of filling report tensors every burst.
_Avoid_: treating cached stale counters as fake sleep, scanning all columns before deciding sleep, filling CUDA metadata tensors every burst just for report truth, or clearing `runs_all_columns` when a dense startup/fallback transition actually touched every column

**Candidate-Scoped Predictive Update** — the trainer-owned execution path where `PredictiveColumnState` updates location/velocity decay, prediction error, confidence, failure streaks, and high-prediction non-winner decay only for the retrieved candidate set after `candidate_predictive_update_start_tokens` (default `512`) when the retained CPU route or promoted fused CUDA route owns the tick. Idle predictive columns keep checkpointed step stamps; when a cached column wakes for vote or update, predictive state materializes only that bounded candidate set through ordered missed non-winner prediction/error/confidence/velocity/weight-decay steps before its cached vote is refreshed. Vote and transition may share that one materialization inside the tick, and the same completed candidate set reports `candidate_subset_completed_noop` instead of repeating a no-op replay. Checkpoint restore recomputes whether cached predictive columns exist from the step stamps, so rollback cannot turn cached predictive wake into a fake status. It is separate from the structural `dead_column_steps` threshold, preserves full-vector state shape and cached non-candidate values, and reports updated/cached counts plus separate predictive-location/materialization scope and `runs_all_columns` truth through Runtime Truth. A retained CUDA selection fallback that already has routed candidates must also keep its predictive consensus vote and context gain candidate-local; it may not recompute all predictive votes merely because the fused vote/competition kernel cannot consume a context gain. On CUDA, eager sparse indexing remains rejected because the 2026-06-15 isolated 8192-column experiment measured `7.0080195312499995 ms` mean versus `3.080762890625 ms` dense writeback. The promoted fused in-place/graph route instead updates only the wake-plan candidates inside the existing transition launch, stamps those rows on device, and reports `candidate_predictive_transition_mode=fused_inplace`, active/fallback truth, execution count, last-transition cached-row count, and cumulative cached row-skips. If candidate homeostasis starts before predictive wake, CUDA captures a `candidate_subset_dense_predictive` graph for the interim dense predictive update and switches to the candidate predictive graph at `candidate_predictive_update_start_tokens`; other unsupported prerequisites fall back before claiming sparse predictive updates.

**Candidate Deep-Sleep Filter** — the trainer-owned retained CPU route boundary where the routing-index/tensor retrieval path may request a bounded backfill pool, sort candidates by returned distance, then remove only candidates whose `steps_since_win` has reached `dead_column_steps`. Before `token_count >= dead_column_steps`, no column can be deep-sleep eligible, so the scheduler requests only the target awake budget and reports `candidate_deep_sleep_filter_no_column_can_be_deep_sleep_yet` rather than paying a backfill pool that cannot filter anything. It does not scan all columns to decide sleep, and it does not mutate pruning or retirement state. If CUDA graph/fused route-vote has already selected candidates, Runtime Truth reports a route-owner fallback instead of filtering after winner selection. The filter reports input/output candidate counts, deep-sleep filtered count, backfill count, fallback reason, tensor device, and `runs_all_columns=false`.
_Avoid_: tying predictive-update wake to structural dead-column retirement, hiding CUDA fallback, claiming sparse CUDA acceleration without benchmark evidence, treating cached non-candidate prediction state as fresh evidence, or replacing exact predictive wake replay with an approximate closed-form/vectorized shortcut that lacks long winner-parity evidence

**Candidate Memory-Pressure Filter** — the trainer-owned retained route boundary where the already retrieved candidate set may be filtered against cached per-column `ColumnMetabolismState.memory_pressure`. The trainer may request a bounded backfill pool, but the retained filter indexes only candidate IDs and never runs an all-column pressure census to decide sleep. `ColumnMetabolismState` updates estimated cost and memory-pressure evidence only for the awake mask, reuses an already cached memory-store bucket-consolidation tensor when one exists, and otherwise reports `no_memory_store_bucket_evidence` instead of fabricating usage. CUDA route-vote now owns the same pressure gate when real cached pressure evidence exists: `core.fused_route_vote_cuda` masks high-pressure route rows before top-k/winner selection and reports pressure counts in the scheduler-filter packet. When pressure evidence is absent, the CUDA gate stays disabled rather than reading pressure rows decoratively. Post-selection CUDA pressure filtering remains rejected.
_Avoid_: scanning every column to decide whether memory pressure is active, calling memory-store consolidation builders every token, filtering CUDA route-vote candidates after winner selection, or treating missing memory-store evidence as high/low pressure.

**Wake-Plan-Scoped Awake Ripple Tagging** — the trainer-owned memory/replay consumer of the Column Wake Plan. `DualMemoryStore` maintains a CPU archival bucket-to-entry index while the live CUDA route/vote/transition work stays device-owned. When a retained tick or CUDA text burst already has awake route candidates, `ripple_tag_awake(..., awake_bucket_ids=...)` tags only recent memory entries whose bucket IDs are awake; explicit empty awake buckets return zero instead of falling back to a global scan. The old scalar/vector recent-memory scan remains only for callers without scheduler context and reports its mode honestly. Runtime Truth projects `ripple_awake_bucket_scan_count`, candidate counts, scan mode, archive cadence, tag attempts/skips, and last reason; `service` must not choose bucket IDs or infer scheduler ownership.
_Avoid_: running an all-memory recent-entry scan after the scheduler already has a wake mask, moving archival ledger bookkeeping to GPU as a fake speed claim, hiding unscoped fallback behind `awake_bucket_index`, or letting status reads decide replay tagging scope.

**Column Wake Plan** — the trainer-owned scheduler object that turns the retrieved candidate set into the one awake mask for the retained tick. It replaces scattered raw candidate-tensor ownership with one bounded plan consumed by predictive vote, competitive scoring, predictive update/location update, and candidate homeostasis. It records total columns, awake budget/count, input/backfill/deep-sleep-filter/memory-pressure-filter counts, wake reason, sleep reason, fallback reason, tensor device, bounded truth, and `runs_all_columns` truth; `service` may project it but must not build or reinterpret it. Live Runtime Truth projections must use this plan's awake IDs and reasons when it exists; the older report-local top-k scheduler score is only standalone/offline evidence when no execution plan is available. Per-column estimated cost and memory pressure now come from checkpointed `ColumnMetabolismState`, not report-local placeholders. The plan is live scheduler state, not checkpoint-backed growth/pruning authority.
_Avoid_: materializing large JSON wake-plan reports in the hot path, letting status reads infer an awake mask, filtering after CUDA graph/fused route-vote has already selected a winner, or treating a bounded wake plan as proof that dense CUDA predictive updates have become sparse

The 2026-06-15 wake-plan slice proved bounded ownership, not a speed win. The
8192-column CPU A/B preserved exact winners and kept reported specialist work
at `10/8192`, but scoped mean latency was `12.24272625 ms` versus
`7.4809125 ms`. The long CUDA gate after deferring legacy report materialization
and using a slotted plan reached `5822.624 tokens/sec` with
`train_compute=0.142080 ms/token`, `prepare_training=0.006576 ms/token`, and
`finalize_total=0.005888 ms/token`, preserving the conditional16 executor
counters and the broad 6k-ish sustained band without matching the historical
top run.

The follow-up Runtime Truth projection audit made the top-level awake mask,
vote samples, wake reasons, and service projection come from the training-owned
`ColumnWakePlan` instead of recomputing a second report-local top-k mask. It is
not a speed promotion: the valid 131072-token conditional-WHILE CUDA rerun
reached `4975.507 tokens/sec` with `train_compute=0.159667 ms/token`,
`prepare_training=0.009256 ms/token`, `finalize_total=0.008210 ms/token`, zero
sequence/native failures or fallbacks, and no observed contention, which is
below the recent `5.8k-6.1k` sustained band. Treat this slice as a truth-boundary
correction until complete-runtime benchmarks prove neutral-or-better cost.

The fused CUDA candidate-predictive path is now the promoted CUDA route when
the graph/candidate gates are compatible. It keeps the existing in-place/graph
transition launch as the owner, updates predictive state only for the wake-plan
candidates, and writes `predictive_last_update_step` for those candidates on
device so non-awake columns remain truthful cached state. The old
`cuda_candidate_predictive_transition_mode` config surface was removed rather
than preserved as a dense-retained compatibility switch. Current evidence: an
isolated RTX 3060 writeback rerun matched dense candidate rows and measured
`0.300185 ms` mean for fused Triton candidate writeback versus `5.378011 ms`
dense, but the standalone side-launch/helper path is removed because the
maintained CUDA owner is the fused transition launch itself; the promoted
131072-token conditional-WHILE stress gate at
`reports/column_scheduler_20260615/promoted-fused-candidate-predictive-131072-i32.json`
reached `6141.078 tokens/sec` with `0.126682 ms/token` train compute,
`0.006629 ms/token` prepare, `0.004710 ms/token` finalize, zero sequence/native
failures or fallbacks, `contention.verdict=not_observed`, `130816` fused
candidate predictive updates, and `132647424` cumulative cached predictive
row-skips. This is a
real scheduler execution effect, not growth/pruning authority.

**Predictive Vote Cache Wake** — the retained training-owned consensus-vote path where routing first selects a bounded awake candidate set, `PredictiveColumnState.vote()` recomputes reference-frame agreement only for those candidates, and non-awake columns keep cached consensus-gain state. Runtime Truth reports updated columns, cached-vote use, fallback reason, tensor device, and whether the vote scanned all columns. This is a real scheduler execution effect, but it does not authorize sleep/deep-sleep mutation, growth, pruning, or service-owned scheduling.
_Avoid_: recomputing all predictive votes after candidates are known, calling cached votes real sleep if other execution still touches the column, or moving vote selection into `service`

**Cadenced Context Plasticity** — the always-on trainer policy that updates adaptive context neural state on every token while applying its dense Hebbian input-weight update only on the first token and then every configured `context_plasticity_interval_tokens` (default `4`). Context-owned Runtime Truth counters distinguish state updates from weight-plasticity updates. Replay and explicit offline callers retain their own `update_weights` choice.
_Avoid_: skipping context state dynamics between plasticity ticks, claiming the context projection is sparse, multiplying the retained learning rate without parity evidence, or treating short synthetic prediction agreement as proof of long-run learning quality

**Hot-Path Structural Mutation Boundary** — the rule that always-on competitive-column processing may update active traces, prototypes, thresholds, spike windows, and stale-column counters, but should not revive, grow, prune, or structurally rewrite columns during the tick. Stale columns are Runtime Truth / spike-health evidence until an explicit deep-sleep, replay, or operator-reviewed maintenance path chooses a reversible mutation.
_Avoid_: scanning and reviving dead columns on every tick, treating stale evidence as automatic growth/pruning authority, or hiding structural mutation inside routine routing

**Binding Hub Evidence / Topology Separation** — `HypercubeBindingLayer.bind()` may update hub activation EMA and usage evidence, but it cannot refresh hub profiles, add/remove adjacency, or write structural mutation events. `refresh_hub_topology(reason=...)` is an explicit core maintenance mutation surface called only by the checkpoint-backed, revision-bound, operator-confirmed Structural Mutation Application; it is not called by the always-on trainer.

**Conditional Binding Wake** — the event-driven execution policy for an inactive Hypercube Binding specialist. While `binding_usage` has no learned support, the trainer runs one bounded probe every `binding_idle_probe_interval_tokens` and otherwise reuses the unchanged cached binding state; active or checkpoint-restored binding runs every tick. Runtime Truth must expose active state, execution mode, probe/bind count, idle-skip count, interval, and tensor device. This is a local specialist wake policy, not proof that the report-only Column Scheduler controls every Subcortex module.

**Conditional Cross-Modal Text Wake** — the trainer-owned wake policy for text-only Cross-Modal Grounding ticks. When no visual or audio spike was accepted and no bounded residual sensory-trace window is open, text-only cross-modal learning stays asleep and records cached-idle state. Accepted visual/audio evidence wakes text updates immediately and opens a short CPU-side wake window; within that window text probes may run at `cross_modal_text_idle_probe_interval_tokens`. Runtime Truth must expose text update count, idle-skip count, last execution mode, interval, and tensor devices.

**Cross-Modal Fast Idle Skip** — the hot-path shortcut for text-only ticks with no accepted sensory evidence, no residual sensory trace, and no due self-criticism window. It records the cached idle state and increments trainer-owned fast-idle evidence without preparing a text spike, reading confidence tensors, buffering frames, or walking self-criticism checks. Sensory ticks and residual-trace ticks keep the full cross-modal path.
_Avoid_: treating fast-idle skip as skipped grounding, hiding the skip count from Runtime Truth, or bypassing sensory-backed text updates.

**Cross-Modal Trace Sleep** — the stronger sleep boundary for an expired Cross-Modal Grounding specialist. When a sensory trace window has ended, the trainer clears cross-modal traces once and then records text-idle skips without per-token trace decay. Visual/audio ticks reopen the trace window and restore ordinary decay/learning behavior.
_Avoid_: decaying zero or expired cross-modal traces every text-only token, freezing stale sensory traces after sleep, or claiming this as a sensory-grounding improvement without sensory evidence.

**Candidate Homeostasis Wake** — the trainer-owned metabolism gate that lets competitive-column threshold and win-rate homeostasis update only the retrieved candidate set after `candidate_homeostasis_start_tokens`, while stale counters, spike-window evidence, winner prototype learning, and explicit dead-column maintenance keep their separate policies. The default starts at 512 tokens, earlier than the structural dead-column threshold, so awake-column cost can shrink without enabling hot-path revival or pruning.
_Avoid_: treating candidate-scoped homeostasis as column retirement, skipping stale-counter evidence, tying it to prediction-update scope, or using it as proof that the report-only Column Scheduler controls all runtime execution.

**Trainer Telemetry Cadence** — the hot-path metabolism boundary for scalar metric extraction inside `MarulhoTrainer.train_step`. Cognitive state, routing, prediction, memory, context, binding, and cross-modal updates still run on their own policies, but expensive `.item()`/mean/count metric reads refresh only every `trainer_telemetry_interval_tokens` and cached values fill intervening ticks. Runtime metrics must expose the configured interval and whether the current tick refreshed telemetry.
_Avoid_: treating telemetry cadence as skipped cognition, hiding stale metrics, or adding per-token CUDA synchronizations only to make status text fresher.

**Stream Text Episode Term Cache** — the trainer-owned cache of token terms for the current cached episode text. It exists only for slow/evaluation/display helpers that explicitly refresh expanded episode text. Live `train_step` archival stores the bounded `raw_window` text directly and must not rebuild episode text on the hot path. The cache must not replace the raw-window archival record, memory replay text, learned chunking segmentation, or language/readout evidence.
_Avoid_: treating cached episode terms as semantic memory, fact promotion, a source of cognition, or a reason to reintroduce hot-path episode reconstruction.

**Adaptive Awake Ripple Scan** — the consolidation-owned wake tagging policy for slow memory entries. Small memory ledgers retain the scalar loop to avoid NumPy setup cost; larger ledgers use zero-copy NumPy views over timestamp and ripple-strength buffers so awake-ripple tagging scales without per-entry Python work. Runtime Truth exposes scalar/vector scan counts and the last scan mode as memory hot-path evidence.
_Avoid_: treating archival memory as GPU-resident cognition, forcing vectorization below its measured crossover, or hiding whether the ripple path ran during a live tick.

**Cadenced Awake Ripple Tagging** — the trainer-owned metabolism boundary for high-dopamine replay-priority tagging. Awake ripple tagging supports later replay/consolidation, so live ticks attempt it only when a retained/fallback archive admission or strong-capture override fires; high-dopamine non-archive and deferred-cadence ticks increment skip counters instead of scanning memory. Runtime Truth exposes tag attempts, skips, last reason, and last tagged count alongside ripple scan mode.
_Avoid_: scanning memory every token for replay metadata, treating skipped ripple tagging as skipped cognition, or hiding replay-priority cadence from Runtime Truth.

**Hot-Window Throughput** — the benchmark-only measurement of already-encoded tensor tokens through MARULHO's current Subcortex `train_step` loop with service, source loading, tokenization, UI/status reads, checkpointing, replay, and sleep maintenance excluded. It is the upper-bound evidence surface for CUDA compile, CUDA graph, Triton, or fused-kernel work; it must not be confused with endpoint throughput or live cognition throughput.
_Avoid_: interpreting hub evidence as applied outreach, calling topology refresh from bind, exposing an unreasoned refresh, bypassing the edge-delta budget, or adding a second topology-write path

**Continuous Hot-Window Sync Mode** — the benchmark-only measurement mode for already-encoded sequential ticks that synchronizes CUDA once around the measured window instead of before and after every token. It estimates continuous stream throughput for a living tick loop without adding artificial per-token host barriers; step-sync remains the correct latency mode for p50/p95 per-token timing.
_Avoid_: treating window-sync dispatch latencies as exact per-token CUDA latency, comparing window-sync directly against service throughput, or using a benchmark-only sync choice to hide real Runtime Truth synchronization costs.

**Profiled Hot-Window A/B** — an evaluation-only extension of Hot-Window Throughput that runs reversed same-process benchmark arms and records measured-step trainer stage buckets after warmup. It is the preferred promotion filter for route/index/graph-prep changes because it shows complete encoded-tick throughput and the stage deltas that caused it without service/source/status noise. It must not mutate runtime state beyond the temporary benchmark trainer or claim endpoint throughput.
_Avoid_: promoting a micro-optimization from one substage when total complete-tick throughput regresses, mixing warmup with measured ticks, or treating profiler output as Runtime Truth.

**Promoted Burst-Path Profiling** — the evaluation-only stress benchmark mode where trainer-stage profiling observes the same training-owned CUDA text-burst executor used by production long runs instead of disabling bursts and forcing per-token `train_step` fallback. It reports burst-level buckets such as boundary planning, host-truth gate checks, graph replay, pending metadata, event/idle handling, and CPU maintenance, plus runtime sub-buckets for burst eligibility, quantum input staging, control-state staging, graph replay loop, Python mirrors, event drain, and event defer. Profile overhead perturbs throughput, so use the bucket split to choose the next target and use clean unprofiled stress runs for promotion.
_Avoid_: reading fallback-only profile buckets as the promoted path, claiming profiled throughput as production speed, or adding always-on timing to the hot path.

**Compiled Column Kernel Throughput** — historical benchmark-only evidence for fixed-shape, candidate-scoped column competition after it had been isolated into pure tensor work. The competition-only runner is removed because wider compiled hot-path probes and the production route/vote/transition lifecycle superseded it. The evidence remains useful only as a reminder that isolated inner-loop throughput is not service throughput, live cognition throughput, or a promotion by itself. A 1000 tokens/sec number is only a low reference floor, not MARULHO's target; the target is maximum sustainable local throughput with correctness and metabolism evidence.
_Avoid_: restoring a competition-only runner as progress, treating a compiled isolated kernel as proof the full runtime is fast, stopping at 1000 tokens/sec, or hiding compiler/toolchain setup failures.

**Compiled Hot-Path Kernel Throughput** — the benchmark-only measurement of a wider fixed-shape tensor block that includes input projection, candidate-scoped column competition, and candidate-local predictive-state math without writing back runtime state. It is the next evidence step between isolated competition and the full hot-window `train_step`; it may guide future fusion but does not include retrieval, Python trainer orchestration, in-place plasticity, memory/replay, binding, cross-modal grounding, checkpointing, or service throughput.

**In-Place Steady-State Column Kernel** — the promoted CUDA/Triton production transition that keeps runtime-owned tensor addresses stable while mutating predictive state, winner prototype/velocity plasticity, candidate-scoped homeostasis, stale counters, spike history, and assembly in one launch after competition. `ColumnTransitionRuntime` belongs to `training`, compiles the bounded all-column and routed-candidate shapes without launching or mutating brain state, and exposes requested/resolved mode, warmup, execution, failure, device, and fallback evidence through Runtime Truth. Unsupported configurations fall back before mutation; a launch-time failure is fail-closed because partial in-place mutation cannot safely replay through another executor. The promoted scope currently requires CUDA, lightweight plasticity, and zero input-weight blend. Exact winner identity is diagnostic rather than a quality requirement when reconstruction, prediction, confidence, spike health, routing diversity, memory behavior, and grounding remain supported. Dynamic scalar and transient-pointer alignment inputs must not create unbounded Triton specializations; compile events belong to startup or explicit warmup, never a measured cognitive tick.
_Avoid_: hiding compile startup, falling back after possible mutation, rebuilding state tensors per tick, treating fallback compatibility as scheduler promotion, or claiming full cognitive-tick velocity before source, context, binding, memory, and remaining orchestration are measured.

**Functional Full-State Transition Removal** — the deleted rejected `core.column_transition` path that returned replacement tensors for the full competitive and predictive state, then required copyback into runtime-owned buffers. It was retained only as an isolated benchmark/oracle after failing complete hot-window promotion evidence; now that `ColumnTransitionRuntime` and the in-place/graph transition own the maintained path, the functional module and standalone benchmark runners are removed from active code. CUDA tests may keep small test-local retained-semantics helpers, but production packages must not preserve the rejected full-state output/writeback shape.
_Avoid_: reintroducing full-state transition helpers under `core` or `evaluation`, using isolated transition throughput as live runtime evidence, or keeping an obsolete oracle after the promoted path has runtime-owned parity tests and long stress gates.

**Device-Resident Winner Selection** — the single-winner CUDA/Triton decision used only by the active In-Place Steady-State Column Kernel. It selects the highest positive candidate activation, falls back to the highest combined candidate drive when all activations are silent, writes winner, unit strength, and fallback evidence into persistent device buffers, and lets the existing transition apply fallback threshold decay. Runtime Truth reports selection mode, executions, failures, and whether a host synchronization branch remains. Retained CPU and unsupported CUDA paths keep the established PyTorch competition.
_Avoid_: treating this bounded selector as evidence that every small control operation belongs in Triton; complete-tick evidence and cold-start cost still decide promotion.

**Fused Predictive Vote Competition** — the promoted CUDA/Triton specialization inside Device-Resident Winner Selection for the proven learned-chunk, zero-input-blend, one-winner shape with no context, abstraction, or binding gain. It keeps the previous winner in a persistent device scalar and fuses candidate-local reference-frame agreement, prototype scoring, threshold inhibition, positive/silent fallback selection, and next previous-winner writeback into one launch. Unsupported shapes reconstruct the retained dense vote before using the ordinary selector. Runtime Truth reports requested/active state, executions, and fallback count.
_Avoid_: enabling this specialization when another routing gain is active, treating candidate-local work as a whole-mind vote, or extending eligibility without sequential parity and complete-tick evidence.

**Fused Text Route Vote** — the internal Triton route/vote family owned by `ColumnTransitionRuntime` and used inside the promoted `cuda_graph_text` path. The old `fused_triton_text` config/checkpoint selector is retired; benchmark and parity controls may force it only through an explicit evaluation override, while normal runtime configuration remains fixed to the graph text-tick island. The exact torch routing cache remains the seed/oracle, but the promoted steady CUDA/text route feeds a bounded route-candidate bank into the Triton kernels through `route_positions`. One kernel scores only the active route rows and the second extracts routed candidates while applying predictive reference-frame voting, threshold inhibition, silent fallback, and winner selection. It runs only on text/idle ticks; visual or audio evidence forces retained tensor routing and is counted explicitly. Retrieval owns cache validity, core owns the kernels, training owns lifecycle, route-bank refresh, and Runtime Truth. Cache growth, unsupported shapes, checkpoint-restore deferral, or failed warmup fall back before mutation and report `route_vote_requested_mode_source`.
_Avoid_: enabling it globally for sensory ticks, restoring `fused_triton_text` or `tensor` as selectable runtime config values, hiding cache refreshes, compiling during a measured tick, or treating its isolated 1,700-tick/sec cluster as full cognitive throughput.

**Route Vote Scoring Scope** — the training-owned Runtime Truth field set that separates bounded awake specialist execution from the route scorer's input-row cost. `route_vote_scoring` and its flattened fields (`route_vote_input_rows_scored`, `route_vote_output_candidate_count`, `route_vote_rows_run_all_columns`, `route_vote_bounded_route_scoring`) are emitted by `ColumnTransitionRuntime`; `service` only projects them. This surface originally prevented calling exact full-cache route scoring a sparse scheduler, because the old fused route/vote path selected `k` awake candidates only after scoring every route-cache row. After the route-candidate-bank and probe-lane promotions, it now proves when steady route scoring is bounded: the first eligible seed reports `candidate_boundary=exact_full_cache_score_seed_route_bank`, `route_input_source=complete_routing_tensor_cache_seed`, and `route_scoring_unbounded_reason=route_candidate_bank_not_ready_exact_seed`; graph/burst steady ticks report `candidate_boundary=bounded_route_bank_probe_score_then_filter_select` or `bounded_route_bank_probe_burst_score_then_filter_select`, `route_input_source=training_owned_route_candidate_bank_plus_probe_lane`, `route_vote_rows_run_all_columns=false`, and `route_vote_bounded_route_scoring=true`. The 2026-06-16 8192-column long gate reports `route_vote_input_rows_scored=12/8192`, `route_vote_output_candidate_count=10`, and `bounded_route_scoring=true`.
_Avoid_: using exact `routing_index.search_tensors()` as a "prefilter" before fused route/vote, because it already scores all rows; clearing `route_vote_rows_run_all_columns` from awake count alone; deriving route-cost truth in `service`; or hiding the first exact seed when claiming bounded steady routing.

**CUDA Graph Text Tick Island** — the fixed promoted `predictive_route_vote_mode="cuda_graph_text"` extension of Fused Text Route Vote. It captures fixed-address CUDA work for input normalization/projection, exact fresh reconstruction distance, route/vote, graph-owned neuromodulator/surprise updates, and the in-place column transition, then replays that bounded island on text/idle ticks. Checkpoint loading must restore model and routing state before capture so graph pointers bind the live tensors; checkpoints missing the route-vote field or carrying retired `tensor`/`fused_triton_text` values migrate to `cuda_graph_text` even when revision-stamped. Visual/audio ticks bypass the graph before pre-routing and retain the established sensory path. Runtime Truth reports capture latency, graph names, replay/bypass/failure counts, host-truth sync/skip counts, sync interval, device, routing-cache clean fast-path/rebuild-check counts, pointer-stability status, configured route-vote mode, and requested-mode source (`promoted_config`, `checkpoint_restore_defer`, or `evaluation_override`). The 2026-06-16 selector cleanup rerun at `reports/column_scheduler_20260616/route-vote-selector-cleanup-8192-131072-i32-rerun.json` stayed in the 6k-ish band at `6141.720 tokens/sec`, `train_compute=0.129445 ms/token`, `tick_p95=21.018 ms`, `route_input_rows_scored=10/8192`, `state_transition_cached_count=8182`, zero graph/native/sequence failures, and `route_vote_requested_mode_source=promoted_config`; GPU contention was observed at the configured threshold, so this is cleanup evidence, not a new top-speed claim. Clean routing-cache ticks may reuse the transition runtime's already-held tensor pointers; dirty caches still rebuild through the routing index before graph replay. This is not a whole-`train_step`, service, memory, or source graph.
_Avoid_: capturing before checkpoint restoration, replaying graph pre-routing on sensory ticks, changing tensor addresses silently, restoring old route/vote config selectors for local checkpoints, hiding cold compile/capture or VRAM cost, or treating hot-window replay velocity as live service throughput.

**Promoted Scheduler Scaling Boundary** — the current CUDA/text scheduler path where the candidate gate is active from the checkpoint start, so `ColumnTransitionRuntime` precompiles only the routed candidate shape, `CudaGraphRouteTransition` captures only `candidate_subset`, and Runtime Truth exposes `capture_graph_policy=candidate_subset_only_after_homeostasis_gate`. The 2026-06-15 real-path scaling probe fixed the previous 8192-column Triton warmup failure by treating all-column candidate sets as a direct mask instead of an `N x N` membership matrix. That probe proved real bounded awake execution at 8192 columns but not same-throughput scaling: the old 8192 exact-cache route-vote run reached `3564.222 tokens/sec` and `train_compute=0.251487 ms/token` because route rows still grew to `8192`. The 2026-06-16 route-candidate-bank scaling gate removed that per-tick route-row growth on the same 8192 checkpoint: `reports/column_scheduler_20260616/route-candidate-bank-8192-warmseed-131072-i32.json` reached `6110.715 tokens/sec`, `train_compute=0.135007 ms/token`, `prepare_training=0.006082 ms/token`, `finalize_total=0.005988 ms/token`, and `tick_duration_ms.p95=20.370`, while reporting `route_input_rows_scored=10`, `route_rows_run_all_columns=false`, `state_transition_column_count=10`, `state_transition_cached_count=8182`, `state_transition_runs_all_columns=false`, CUDA on RTX 3060, and zero graph/native/sequence failures. That earlier gate proved bounded k-only steady scoring after one explicit exact seed; the current live discovery shape is the fixed probe-lane scheduler below, which keeps scored rows and awake mutation bounded without returning to hot-path all-column route scoring.
_Avoid_: calling bounded awake work the same as bounded route cost, keeping unused dense startup graph/precompile shapes in promoted checkpoints, claiming bounded steady routing without exposing the exact seed, widening the bank without quality evidence, or reverting sparse state-transition truth to dense fallback labels when the candidate-subset CUDA path is active.

**Route Candidate Bank Probe Lane** — the promoted route bank now scores `k_routing` bank rows plus a fixed two-row probe lane at the graph quantum refresh boundary. The probe lane is training-owned, stored as routing-cache positions, and never uses a hot-path all-column scan; awake output and state mutation stay capped at `k_routing`. Checkpoints save the ready route-bank IDs and restore them before CUDA graph capture, so a restored runtime can enter bounded route scoring without a first measured exact-cache seed. Runtime Truth exposes `probe_rows`, `score_rows`, `probe_cursor`, `refresh_interval_tokens`, `scored_since_refresh`, `probe_refresh_count`, `checkpoint_restore_count`, `route_input_rows_scored`, and `route_rows_run_all_columns` so this cannot be confused with fake sleep. The 2026-06-16 8192-column longer run at `reports/column_scheduler_20260616/route-bank-probe-lane-8192-131072-i32.json` reached `6141.234 tokens/sec`, `train_compute=0.130664 ms/token`, and `tick_duration_ms.p95=21.976`, with no observed contention, `route_input_rows_scored=12/8192`, `route_output_candidate_count=10`, `state_transition_column_count=10`, `state_transition_cached_count=8182`, `state_transition_runs_all_columns=false`, and zero graph/native/sequence failures. The route-bank checkpoint restore run at `reports/column_scheduler_20260616/route-bank-checkpoint-restore-8192-131072-i32.json` restored the bank with `seed_count=0`, `graph_bypass_count=0`, `checkpoint_restore_count=1`, stayed at `route_input_rows_scored=12/8192`, and reached `6129.693 tokens/sec` with `train_compute=0.130218 ms/token`. The follow-up quality gate rejected fixed sequential probes as a relevance solution: probe2 q16 matched exact top-1 only `0.017578125` and winner only `0.001953125`; probe64 and probe256 raised top-1 to `0.140625` and `0.34765625` but left winner match at `0.0078125` and `0.044921875`.
_Avoid_: refreshing probes through an all-column route scan, treating probe rows as additional awake columns, hiding the fixed refresh cadence, or adding a second graph-internal bank-refresh implementation that diverges from fused/graph/burst parity.

**Column Structural Review Queue** — the training-owned, checkpoint-backed queue that turns bounded awake/candidate evidence into operator-review tickets for later growth, prune, or sleep review. It records only candidate IDs already supplied by the wake/route plan, carries prediction error, confidence, failure streak, cost, usefulness, memory pressure, wake/sleep reason, pending counts, next gate, baseline queue hash, candidate evidence hash, and checkpoint/operator requirements, and never mutates runtime state. Retained CPU ticks may update it immediately from the awake mask; CUDA graph bursts record structural-review evidence only on a slow cadence and otherwise report deferred review capture so the queue cannot reintroduce a hot-path host sync. Runtime Truth projects the compact queue summary; service must not create tickets, scan columns, or decide structural eligibility. Benchmark-forced structural-review audits may queue bounded growth and prune/sleep tickets after measured timing to prove coverage, but they are not live mutation paths or throughput claims.
_Avoid_: treating queued review as executed growth/pruning, deriving tickets in status/read models, recording every CUDA host-truth boundary, or hiding a structural-review CPU sync inside the text graph hot path.

**Promoted Predictive Dense Transition Mode** — the maintained runtime default is `predictive_dense_transition_mode="inplace_triton"`, which lets `ColumnTransitionRuntime` own CUDA predictive/competitive mutation and Runtime Truth before falling back. The old `compiled` dense predictive mode was an isolated speed experiment, `legacy` could bypass the promoted CUDA mutation boundary, and `fused_eager` kept the dense CUDA fallback selectable as a production mode; all three are removed from config, tests, and new runtime state. Legacy checkpoints carrying those values migrate to `inplace_triton`. The hidden `apply_dense_transition(..., transition_mode=...)` core selector is also removed. If the in-place runtime cannot start, training still falls back internally to dense eager tensor semantics and reports `last_dense_transition_mode=dense_eager_fallback` plus the concrete in-place fallback reason instead of hiding a second runtime branch.
_Avoid_: restoring `fused_eager`, `compiled`, or `legacy` as config options, using torch-compiled predictive state as a silent fallback, disabling the promoted CUDA mutation boundary through any selector, or preserving isolated predictive benchmarks after the fused in-place transition became the promoted boundary.

**Column State Transition Truth** — the Runtime Truth contract for whether competitive/predictive state mutation still touches every column after a scheduler chooses an awake mask. `state_transition_mode`, `state_transition_column_count`, `state_transition_cached_count`, `state_transition_materialize_*`, `state_transition_runs_all_columns`, and `state_transition_fallback_reason` are owned by core/training and projected by service. Candidate-subset CPU and CUDA graph/burst ticks may report `runs_all_columns=false` only when stale counters, spike rows, assembly active winner, threshold/win-rate homeostasis, and predictive candidate state are all updated through the same bounded awake mask or cached-stamp materialization. A dense startup/fallback run must still report all-column truth. The scheduler is not durably complete until route-candidate retrieval also stops scaling with total route rows, or a dense route scorer is retained with explicit neutral-or-better evidence.
_Avoid_: clearing `runs_all_columns` from awake-count evidence alone, deriving state-transition scope in service, hiding dense transition retention behind cached-vote/deep-sleep status, or adding all-column scans to decide sleep.

**Nonblocking Graph Input Staging** — the persistent CUDA graph input-copy rule that source tick tensors are staged into the fixed graph input buffer with non-blocking copy semantics. It preserves fixed addresses and exact graph inputs while reducing host-side graph preparation pressure. It is a staging optimization only, not a change to routing, projection, or transition math.
_Avoid_: replacing fixed graph buffers with dynamic input addresses, treating async staging as proof of endpoint throughput, or using nonblocking copies to hide required synchronization before graph replay.

**Cadenced CUDA Graph Host Truth Mirror** — the hot-path rule that graph-owned CUDA state updates every replay while Python/Runtime Truth scalar mirrors refresh only on the first tick and then every `cuda_graph_host_truth_sync_interval_tokens`. Exact per-token host mirroring remains available by setting the interval to `1` for parity/evaluation runs. The production default is `32` tokens to reduce scalar readbacks, and Runtime Truth reports sync/skip counts so operators can see freshness without forcing `.tolist()`/`.item()` every token.
_Avoid_: treating a skipped host mirror as skipped cognition, recomputing graph-owned surprise on the CPU when the mirror is skipped, or adding report freshness reads that synchronize CUDA every tick.

**Deferred Device Event Queue** — the trainer-owned fixed-capacity CUDA queue that lets consecutive eight-token persistent graph bursts retain scalar result rows, strong-event flags, assemblies, and routing keys until the existing host-truth boundary. The current capacity is thirty-two tokens: up to four eligible bursts can remain device-owned before one exact evidence drain, and any CPU fallback or service tick completion forces a bounded drain before proceeding. Runtime Truth reports capacity, pending tokens, deferred bursts, drains, forced drains, drained tokens, and whether full-capacity drains skipped an unnecessary device slot reset. Drift refresh and fixed slow-memory cadence do not force a queue drain. This preserves the configured truth interval and archival correctness; it is not a larger service execution quantum or GPU-resident archival memory.
_Avoid_: dropping pending events on fallback/shutdown, storing raw text or archival ledgers on CUDA, letting `service` interpret training events, confusing queue capacity with host-truth freshness, or claiming a throughput win without long full-runtime evidence.

**Slim Burst Event Packet** — the retained host-truth drain format for the training-owned text-burst executor. Ordinary no-strong-event drains materialize only the final result row plus a device-owned cumulative strong-event count; bounded strong flags and full per-token result rows are materialized only for actual strong captures. The in-place transition kernel now publishes the packet directly, so the maintained burst graph no longer launches a separate snapshot kernel after each transition. This reduces host packet work while preserving exact final Runtime Truth, strong-event replay metadata, forced flushes, and fail-closed metadata checks.
_Avoid_: rebuilding full result-row lists for cadence skips, scanning strong flags on no-strong drains, dropping strong-capture row evidence, or claiming endpoint speed from smaller packets without long clean runtime evidence.

**Sparse Burst Event Payload Loads** — the device-side companion to the Slim Burst Event Packet. Every fused burst event packet still writes the scalar result row and strong flag, but routing-key and assembly payload loads/stores are predicated on the strong-event condition. Ordinary no-strong ticks therefore avoid reading full routing/assembly payloads only to discard them; strong captures still preserve exact replay payloads for CPU archival at the bounded drain.
_Avoid_: loading archival payload tensors for no-strong ticks, treating missing no-strong payload rows as lost cognition, moving raw replay ledgers onto CUDA, or claiming a complete-runtime speedup without sustained clean stress evidence.

**Fused Burst Event Packet Publication** — the hot-path rule that result-row, neuromodulator, winner, competitive-surprise, strong-flag, and optional strong payload publication belongs inside the already-promoted in-place transition kernel. The old standalone `burst_event_cuda` snapshot kernel is retired, and normal thirty-two-token drains rely on the device slot's natural wrap instead of launching a redundant reset. Runtime Truth exposes slot reset and reset-skip counts. This is packet-boundary cleanup, not a persistent multi-tick executor.
_Avoid_: reintroducing a post-transition snapshot launch, resetting a naturally wrapped event slot on every cadence drain, using the packet as a hidden memory ledger, or promoting throughput claims when long runs report CPU/GPU contention.

**Device Strong-Event Count** — the graph-owned cumulative count of strong replay-memory capture events published by the in-place transition kernel. A host-truth drain reads this scalar before deciding whether to materialize the bounded strong-flag vector and archival payload rows. No-strong drains skip the CPU flag scan; strong drains still verify count/flag agreement and fail closed on mismatch. Runtime Truth exposes device ownership, total count, flag-scan count, and no-strong scan skips.
_Avoid_: scanning CPU strong flags when the device count proves the drain has no strong events, using the count as a replay ledger, or dropping the exact strong flags/payloads needed for archival correctness.

**Device-Owned Cognitive Boundary Controller** — the training-owned classifier that keeps eligible text cognition on the persistent CUDA executor while separating observation and CPU maintenance from true slow-path interruption. Trainer telemetry and fixed slow-memory cadence are deferred observation/maintenance; drift refresh is sync-free on burst ticks and uses global drift when the host winner mirror is stale; drift-floor closure no longer forces a CUDA event drain. Boundary planning uses range arithmetic over the proposed burst instead of scanning each token in Python. Only replay/sleep, routing-index mutation, sensory wake, or other real mutation boundaries may force retained per-token execution. Runtime Truth reports planned quanta, device-continuous quanta, real fallbacks, sync-free/global drift refresh, deferred telemetry, deferred slow-memory cadence, disabled legacy gates, and the classifier mode. It does not move archival memory or reporting algorithms onto CUDA.
_Avoid_: treating telemetry freshness as an execution dependency, restoring per-token Python boundary scans, skipping drift maintenance, calling CPU archival work device-owned, preserving unused exploration scalars as cognition, or weakening fail-closed sleep/replay and mutation boundaries.

**Deferred Winner Host Materialization** — the trainer/runtime rule that the active winner remains a device tensor through the CUDA graph transition and routing-index update-buffer path, and Python materializes the winner id only when a host-owned consumer actually needs it. Routing-cache maintenance may batch/defer CUDA winner-id reads until the routing-index flush; status/checkpoint mirrors remain host-visible evidence, not the source of column cognition.
_Avoid_: forcing winner `.tolist()` inside the transition executor, using stale host winner ids for live tensor updates, or hiding the eventual host-sync consumer from stage profiling.

**Cadenced Winner Host Mirror** — the CUDA graph hot-path rule that the device winner remains authoritative while the Python `winner`/`last_winner` mirror refreshes only when the graph truth packet already synchronized or a CPU-owned consumer such as slow-memory archive needs an exact bucket id. On skipped graph ticks returned metrics may carry the cached host winner, and `winner_host_mirror_fresh`, sync count, and skip count expose that freshness boundary through Runtime Truth. Device-owned previous-winner voting, transition state, routing-index update tensors, and archive bucket ids stay exact.
_Avoid_: synchronizing CUDA every token only to keep display/status winner text fresh, using cached host winners for CPU-owned mutation, or hiding winner mirror staleness from operators.

**CPU Mirror Winner Consolidation Metric** — the CUDA graph hot-path rule that `winner_consolidation_level` is telemetry, not transition state. When a graph truth packet already provides a host winner id, the transition runtime refreshes the metric from the CPU memory-store consolidation mirror; otherwise it reuses the cached metric and reports CPU/cached counts. The device consolidation tensor still drives transition math and remains guarded by generation evidence.
_Avoid_: forcing `consolidation.index_select(...).item()` only to make a scalar status metric fresher, using cached consolidation metrics for mutation, or hiding whether the metric was CPU-refreshed or cached.

**CUDA Graph Consolidation Generation Guard** — the Persistent Text Tick Executor binds the bucket-consolidation tensor pointer at capture time and checks `DualMemoryStore.bucket_consolidation_cache_generation` on eligible text ticks instead of asking the memory store to return the tensor again. Ordinary in-place cache adjustments preserve the generation; cache invalidation or memory warm-state changes deactivate the graph before replay and expose fast-path/mismatch counters in Runtime Truth.
_Avoid_: calling `bucket_consolidation_tensor()` every graph-eligible tick only to re-confirm an already captured pointer, ignoring cache invalidation, or treating the generation check as replay/consolidation quality evidence.

**Allocation-Free Graph Bookkeeping** — the `ColumnTransitionRuntime` rule that a graph-eligible tick must not reacquire the already generation-guarded consolidation tensor or allocate a fresh empty CUDA tensor merely to report that no columns were revived. The transition runtime reuses persistent device buffers and exposes consolidation-lookup skip and empty-revival reuse counts through Runtime Truth. Retained, sensory, cache-invalidated, and explicit structural-maintenance paths keep their existing tensor lookup or mutation behavior.
_Avoid_: treating bookkeeping counters as cognitive quality evidence, bypassing the generation guard, or sharing mutable non-empty revival state through the persistent empty tensor.

**CUDA Graph Routing Cache Generation Guard** — the Persistent Text Tick Executor records the retrieval-owned torch routing-cache generation at capture time and treats unchanged generations as pointer-current without rebuilding or rechecking the dirty routing cache every eligible text tick. Add/remove/rebuild invalidation advances the generation; mismatches fall back through `routing_tensor_cache()` and fixed-pointer validation before replay. Runtime Truth exposes generation fast-path, mismatch, rebuild-check, and clean-cache counters.
_Avoid_: using generation equality as a routing-quality claim, bypassing pointer validation after generation mismatch, or moving routing-cache mutation ownership from `retrieval` into `training` or `service`.

**Device-Owned Routing Cache Coherence** — the Persistent Text Tick Executor writes the normalized next winner prototype into the captured exact torch routing cache inside the same Triton transition that mutates the competitive prototype. Eligible graph ticks therefore do not enqueue a second per-token routing-index winner/vector snapshot. Retrieval still owns cache construction and identity; training owns graph lifecycle and marks the CPU index mirror stale until an explicit slow-path synchronization is required before retained index mutation. Runtime Truth exposes device update count, skipped buffer count, host-mirror synchronization count, and CPU-mirror freshness.
_Avoid_: rebuilding the routing cache from a stale CPU mirror, using this path for incomplete/non-unique routing ID sets, hiding host-mirror staleness, or treating device-cache coherence as proof of service throughput.

**Prepared Graph Candidate Reuse** — the `ColumnTransitionRuntime` rule that, after `prepare_routing()` has already replayed the persistent text graph for the current token, `route_candidates()` consumes the graph-owned candidate buffer directly instead of repeating routing-cache and graph-eligibility checks in the same token. The reuse is keyed to the trainer token count and exposed through `route_vote_prepared_graph_reuse_count`; sensory, bootstrap, inactive, or fallback states still use the retained routing path.
_Avoid_: rechecking static graph pointers twice in one token, reusing candidates across token boundaries, or treating reuse counters as a routing-quality claim.

**Graph Prep Substage Profile** — an evaluation-only trainer-stage profile split for Persistent Text Tick Executor preparation. It separates graph eligibility, previous-key handling, host parameter staging, recent-spike-row fill, actual input staging, graph replay, and host-truth mirror cost so optimization targets are chosen from measured substage evidence rather than the coarse `routing_prepare` or `cuda_graph_prepare_input_copy` buckets.
_Avoid_: treating profiler buckets as production Runtime Truth, promoting speed changes from an instrumented run without an uninstrumented/repeated A/B, or chasing input-buffer copy when parameter/control staging is the larger measured cost.

**Device-Owned Previous Routing Flag** — the Persistent Text Tick Executor rule that the `has_previous_routing_key` transition flag is graph/device-owned after capture and replay, while Python stages only the still-host-owned competitive modulator before replay. Triton already persists the previous-routing-key buffer and sets the flag inside the transition kernel, so the live hot path no longer copies this boolean from host every tick. Runtime Truth exposes `previous_flag_device_owned_count`; this is control-plane cleanup, not a standalone throughput claim.
_Avoid_: reintroducing host writes for graph-owned previous-key state, hiding the counter from Runtime Truth, or treating unchanged correctness with noisy throughput as proof that all graph control state is device-owned.

**Device-Owned Learning Rate Counter** — the Persistent Text Tick Executor rule that graph-backed text ticks compute the competitive transition learning rate from a graph-owned update-count scalar instead of copying `comp.get_lr()` from the host every replay. The graph increments that scalar after each successful replay; sensory, bootstrap, or retained fallback ticks can advance the Python `update_count`, so the next graph preparation resynchronizes the device scalar before replay and reports `learning_rate_host_resync_count`. Runtime Truth also exposes `learning_rate_device_owned_count`.
_Avoid_: assuming all plasticity control is device-owned, removing the fallback resync guard, or promoting this as a throughput breakthrough without repeated complete-tick evidence.

**Revision-Cached Graph Modulator Staging** — the Persistent Text Tick Executor rule that the host-staged competitive modulator is copied into graph input state only when CPU-visible surprise/modulator state changes. `SurpriseMonitor.modulator_revision` advances on competitive error records, CPU neuromodulator updates, and graph host-truth mirror updates; graph prep reuses the already-staged device scalar between revisions and exposes `modulator_stage_copy_count` and `modulator_stage_skip_count`. This preserves the existing CPU-visible modulator cadence while deleting redundant host-to-device scalar staging on graph-skip ticks.
_Avoid_: treating cached staging as device-owned surprise history, skipping invalidation after host truth sync or error recording, or claiming endpoint throughput from a profiler-only stage win.

**Tensor Routing Search** — the only live retrieval-owned candidate search surface for the exact `torch_topk` routing index. It returns candidate ids and distances as tensors on the routing device so trainer competition, query detail projection, and fixed-shape CUDA benchmarks stay on the same tensor-native route surface. The old Python-list/CPU-numpy `search()` API and its benchmark switches are retired; Runtime Scope reports tensor search count and the last observed tensor mode. `torch_topk` is now the only live routing backend because it is the only maintained route that exposes `routing_tensor_cache()` for the promoted CUDA route/vote graph; retired `auto`, `faiss_hnsw`, and `exact_cosine` modes no longer have a live config field or retrieval constructor path, and old checkpoint keys are migrated away instead of creating a second scheduler path. The 2026-06-16 long cleanup gate reached `6142.710 tokens/sec`, `train_compute=0.132356 ms/token`, `prepare_training=0.006332 ms/token`, `finalize_total=0.005919 ms/token`, and `tick_duration_ms.p95=21.142`, with zero graph/sequence/native failures. A follow-up constructor cleanup stayed in the same band at `6134.242 tokens/sec`, `train_compute=0.131117 ms/token`, and `route_input_rows_scored=10/8192`. Tensor routing is not fused cognition, sparse routing, or service throughput: `search_tensors()` is exact top-k over the routing cache and cannot be used as a pre-narrowing step to claim bounded route cost.
_Avoid_: restoring list-returning routing as a benchmark or query convenience path, hiding cold cache rebuild cost, restoring non-cache routing backends as selectable runtime modes, moving routing algorithms into `service`, or calling exact top-k prefiltering sparse scheduler ownership.

**Route Candidate Bank Scheduler** — the training-owned CUDA route/vote boundary that seeds once from the complete routing tensor cache, then scores a fixed `k_routing` route-candidate bank plus a fixed two-row probe lane on eligible fused/graph text ticks. The bank and probe lane store routing-cache positions on device, feed `core.fused_route_vote_cuda` through `route_positions`, refresh from device route/vote candidates inside the fused route/vote select kernel on steady graph/burst replay, and report `route_candidate_bank` plus `route_vote_scoring` in Runtime Truth. `route_candidate_bank_size` is retired as a production config selector; old checkpoint keys are dropped with migration evidence, and wider-bank work belongs only in explicit quality/evaluation gates until a new promotion passes. The first exact seed is explicit (`exact_full_cache_score_seed_route_bank`, `route_candidate_bank_not_ready_exact_seed`) and must be warmed before long runs; steady state reports `indexed_route_bank_vote_device_refresh`, `refresh_owner=fused_route_vote_device`, `score_rows=k_routing+2`, bounded route rows, bounded awake rows, and `state_transition_runs_all_columns=false` so the seed is not counted as graph replay or fake sleep. The 2026-06-16 1024-column k-only warm-seed gate reached `6109.301 tokens/sec`, `train_compute=0.130536 ms/token`, `prepare_training=0.006706 ms/token`, `finalize_total=0.005764 ms/token`, and `tick_duration_ms.p95=21.299`. The matching 8192-column k-only gate reached `6110.715 tokens/sec`, `train_compute=0.135007 ms/token`, `prepare_training=0.006082 ms/token`, `finalize_total=0.005988 ms/token`, and `tick_duration_ms.p95=20.370`, with `10/8192` route rows scored, `10` active state-transition columns, `8182` cached columns, zero graph/native/sequence failures, and no observed contention. The promoted probe-lane gate at `reports/column_scheduler_20260616/route-bank-probe-lane-8192-131072-i32.json` then reached `6141.234 tokens/sec`, `train_compute=0.130664 ms/token`, `tick_duration_ms.p95=21.976`, `route_input_rows_scored=12/8192`, `route_output_candidate_count=10`, `state_transition_cached_count=8182`, zero graph/native/sequence failures, and no observed contention. The reproducible promoted-checkpoint builder extended the same long gate to `16384` and `32768` columns: the `16384` gate at `reports/column_scheduler_20260616/promoted-scheduler-16384-131072-i32.json` reached `6154.503 tokens/sec`, `train_compute=0.130874 ms/token`, `route_input_rows_scored=12/16384`, and `state_transition_cached_count=16374`; the `32768` gate at `reports/column_scheduler_20260616/promoted-scheduler-32768-131072-i32.json` reached `6298.380 tokens/sec`, `train_compute=0.130618 ms/token`, `prepare_training=0.006178 ms/token`, `finalize_total=0.005816 ms/token`, `tick_duration_ms.p95=20.991`, `route_input_rows_scored=12/32768`, and `state_transition_cached_count=32758`. The device-refresh gate at `reports/column_scheduler_20260616/device-route-bank-refresh-32768-131072-i32.json` kept the same `12/32768` route rows and `32758` cached transition rows, reported `device_refresh_count=131072`, `host_refresh_count=1`, zero graph/native/sequence failures, and reached `6008.953 tokens/sec` with `train_compute=0.133379 ms/token` under observed GPU contention; this is in-band versus the immediately previous usefulness-filter run (`5886.235 tokens/sec`, `0.134212 ms/token`) but not a new speed ceiling. Same-checkpoint quality gates show the relevance problem remains open: q16 probe2 matched exact winner `0.046875`, while per-token device refresh also matched `0.046875` and only moved top-1-in-bank from `0.271484375` to `0.2734375`. This is a bounded steady scheduler boundary with bounded outside-bank discovery and less host orchestration, not ANN proof, global memory, quality-complete routing, or growth/pruning authority.
_Avoid_: hiding the first all-cache seed, widening the bank beyond `k_routing` without a promotion gate, refreshing the bank through host-side index-select after graph replay, refreshing the bank through an all-column scan, moving route-bank decisions into `service`, or treating bank membership as permanent architecture growth.

The 2026-06-17 `65536`-column scale gate extends the same promoted route-bank/probe-lane/device-refresh path without a second scheduler path: the checkpoint builder at `reports/column_scheduler_20260617/promoted-scheduler-65536-checkpoint.json` paid the explicit pre-save seed once (`65536/65536`) and the restored tick scored `12/65536`, output `10` candidates, cached `65526` state-transition columns, and kept `state_transition_runs_all_columns=false`. The longer run at `reports/column_scheduler_20260617/promoted-scheduler-65536-131072-i32.json` processed `131072` tokens at `6154.501 tokens/sec`, `train_compute=0.130339 ms/token`, `prepare_training=0.006100 ms/token`, `finalize_total=0.006153 ms/token`, and `tick_duration_ms.p95=20.092`, with `route_vote_kernel_variant=indexed_route_bank_vote_device_refresh`, `refresh_owner=fused_route_vote_device`, `device_refresh_count=131072`, `host_refresh_count=1`, `route_rows_run_all_columns=false`, `bounded_route_scoring=true`, `state_transition_runs_all_columns=false`, zero graph/native/sequence failures, and no observed contention. The active pressure fixture at `reports/column_scheduler_20260617/active-pressure-scheduler-65536-checkpoint.json` marked exactly two cached route-bank rows as high memory pressure, matching the two probe rows so restored route-vote could mask them without fallback; the restored tick still scored `12/65536`, output `10`, cached `65526`, and observed `filtered_memory_pressure_count=2`. The matching 131072-token run at `reports/column_scheduler_20260617/active-pressure-scheduler-65536-131072-i32.json` reached `6297.455 tokens/sec`, `train_compute=0.130524 ms/token`, `prepare_training=0.006053 ms/token`, `finalize_total=0.006145 ms/token`, `tick_duration_ms.p95=20.115`, `observed_filtered_memory_pressure_total=2`, `observed_filtered_deep_sleep_total=254`, `route_input_rows_scored=12/65536`, `route_output_candidate_count=10`, `state_transition_cached_count=65526`, zero graph/native/sequence failures, no filter fallback, and no observed contention. Runtime Truth now projects `route_vote_scheduler_filter` from `column_transition_runtime` so active filter counts and observed totals remain service-visible without moving scheduler decisions into `service`. This strengthens the bounded-awake execution claim from `32768` to `65536` total columns while preserving the 6k-ish complete-runtime band and proving a route-owner pressure/sleep mask can actively skip route rows; it does not close the relevance-quality gap described by the quality gate.

**Route Candidate Bank Quality Gate** — the offline oracle that compares the bounded steady route bank against complete-cache exact top-k/winner evidence without adding a hot-path all-column scan. The gate exists because the k-only self-refreshing bank is a real speed boundary but not yet a quality-complete relevance router for changing text. On the 8192-column checkpoint, 512 default-text ticks scored only `10` steady bank rows but matched the exact top-1 route only `0.009765625`, matched the simulated exact winner only `0.001953125`, had mean top-k overlap `0.02578125`, and missed the exact top-1 for `134` consecutive ticks. A 32-tick evaluation-only exact reseed improved top-1-in-bank to `0.486328125` and reduced the worst miss streak to `30`, but still matched the simulated exact winner only `0.001953125`; it is not a promotion path. A random/stable 256-tick control passed with exact top-1 and winner match `1.0`, proving the gate can distinguish stable locality from real-source drift. The updated gate also simulates the live probe-lane cadence: probe2 q16 scored `12/8192` rows and still matched exact top-1 only `0.017578125`; widening sequential probes to `64` or `256` rows improved top-1 to `0.140625` and `0.34765625` but left winner match at `0.0078125` and `0.044921875`. The 2026-06-17 wider-retained-bank diagnostic fixes the evaluator so retained-bank capacity can exceed awake `k` without claiming runtime support. On the same 8192 checkpoint, `bank256+probe64` scored `320/8192` rows and reached top-1 `0.92578125` but winner `0.630859375`; `bank512+probe128` scored `640/8192`, reached top-1 `1.0`, but winner only `0.826171875`; `bank1024+probe256` scored `1280/8192` and passed offline quality with top-1 `1.0`, winner `0.97265625`, and worst miss `0`, but its status remains `wider_bank_quality_passes_but_requires_runtime_kernel_promotion` because the promoted live kernel refreshes only the awake `k=10` bank rows. An attempted real CUDA path did add GPU-owned `1024+256` retained-bank refresh while waking only `10`, but the 65536-column 131072-token gate fell to `4656.790 tokens/sec` and `train_compute=0.186216 ms/token` versus the same-session retained k+2 path at `6156.500` and `0.130623`; an unsorted top-k short check still reached only `4596.633` and `0.176464`. The failed runtime branch was removed rather than kept as a second path. The 32768-column same-checkpoint diagnostic makes the fixed-probe rejection sharper: per-token probe2 scored `12/32768` rows with top-1 `0.2734375`, exact-winner-in-bank `0.09375`, winner match `0.046875`; probe16 scored `26/32768` rows with top-1 `0.279296875`, exact-winner-in-bank `0.095703125`, winner match `0.046875`; probe64 scored `74/32768` rows with top-1 `0.591796875` and exact-winner-in-bank `0.486328125`, but still only winner match `0.046875`, while the offline oracle-previous diagnostic reached only `0.455078125`. A bounded column-ID hypercube-neighbor probe is also rejected as a promotion path: the real 32768-column gate scored `25.215/32768` rows on average with top-1 still `0.2734375`, exact-winner-in-bank `0.09375`, winner match `0.046875`, and oracle-previous diagnostic `0.08984375`, so current bit-flip column IDs are not routing locality evidence. The next route scheduler must discover outside the current bank through a bounded GPU-owned candidate router or explicit quality policy that preserves previous-winner/reference-frame continuity and the 6k-ish long-run band, not by hiding per-tick exact-cache scoring, widening a blind sequential probe, treating a wider-bank evaluator pass as runtime support, or treating column ID adjacency as learned topology.
_Avoid_: claiming k-only self-refresh is quality-complete on real text, treating evaluation exact reseed as a runtime fix, or adding all-column scans to decide sleep.

**Route Candidate Graph Quality Probe** — the evaluation-only extension that precomputes a fixed neighbor graph from the routing cache, then simulates steady ticks by scoring the current bank plus bounded neighbor rows. With `neighbor_count=208` and `capacity_rows=1536`, the corrected 8192-column default-text gate scored `1481.068` rows on average, matched exact top-1 `0.994140625`, matched the simulated exact winner `0.98828125`, kept mean top-k overlap `0.9748046875`, and limited the worst exact top-1 miss streak to `1`. That per-tick-refresh evidence proves bounded discovery is plausible, but it is not the live q16 cadence. Under the live 16-token refresh cadence, smaller bounded graph probes stayed quality-incomplete (`256` rows: top-1 `0.640625`, winner `0.1484375`; `512` rows: top-1 `0.796875`, winner `0.26171875`; `1024` rows: top-1 `0.900390625`, winner `0.484375`), and the old `1536`-row shape fell to top-1 `0.9453125`, winner `0.74609375`, worst miss streak `4`. A same-row-budget graph-neighbor probe is also rejected as an immediate probe-lane replacement: `neighbor_count=2`, `capacity_rows=12`, and no sequential probe improved exact top-1 from the rerun probe2 `0.013671875` to `0.078125`, but exact winner match stayed `0.001953125`. The attempted live runtime path was also rejected: the matching 131072-token CUDA run scored about `1509/8192` route rows and reached only `4682.167 tokens/sec` with `train_compute=0.183927 ms/token`, below the then-promoted k-only bank's `6110.715 tokens/sec` and `0.135007 ms/token`. After removing the runtime neighbor-graph path, the 8192 k-only route bank rerun reached `6150.296 tokens/sec` with `train_compute=0.130390 ms/token`, `route_input_rows_scored=10`, `state_transition_runs_all_columns=false`, zero graph/native/sequence failures, and no observed contention. The runtime neighbor-graph knobs/code are therefore not part of the live path; only the offline gate remains, while the live path uses the fixed probe lane for bounded route-cost truth.
_Avoid_: promoting quality-only graph-neighbor evidence as runtime scheduler completion, keeping opt-in runtime knobs after rejection, or adding a dedupe/neighbor expansion path unless it is fused/GPU-owned and beats the long 6k-ish baseline.

**Route Candidate Graph-Walk Quality Probe** — the evaluation-only CAGRA-style graph-walk extension to `route_candidate_bank_quality_gate`. It starts from the current bank plus fixed probe lane, scores a bounded frontier, keeps only a fixed beam as walk parents, and expands neighbor frontiers for a fixed number of rounds; the full routing cache is still used only as the offline oracle and graph precompute, not as a hot-path scheduler. On the 8192-column default-text gate, graph-walk shapes improved exact top-1 but failed winner-quality before becoming a plausible runtime path: neighbor64/beam4/round4/cap512 scored `512/8192` rows with top-1 `0.896484375`, winner `0.263671875`, worst miss `5`; neighbor128/beam2/round6/cap1024 scored `1024/8192` rows with top-1 `1.0`, winner `0.73046875`, worst miss `0`; neighbor256/beam2/round6/cap1536 scored `1536/8192` rows with top-1 `1.0`, winner `0.767578125`, worst miss `0`. This rejects top-1-only graph walking as the next scheduler: previous-winner/location-sensitive winner parity needs the supporting candidate set, and row counts near the rejected runtime graph-neighbor path already threaten the 6k-ish baseline. The same-session 131072-token recheck did not run graph-walk in runtime; it kept the promoted route-bank/probe-lane path at `6141.295 tokens/sec`, `train_compute=0.132462 ms/token`, `route_input_rows_scored=12/8192`, and `state_transition_runs_all_columns=false`, with GPU contention observed, so it is in-band evidence rather than a new top claim.
_Avoid_: promoting graph-walk top-1 recovery as scheduler completion, adding a Python or unfused graph-walk runtime knob, or running a 131072-token promotion gate for a shape that already fails winner parity.

**Route Candidate Discovery Probe** — the retired cheap bounded-discovery probe for fixed farthest-landmark buckets and random-projection buckets. On the 8192-column default-text gate, landmark256/top8/bucket128 at q16 scored `926.544` route rows on average but matched exact top-1 only `0.77734375` and winner only `0.4296875`; per-token landmark256/top8 improved to top-1 `0.884765625` and winner `0.525390625` while paying `256` selector rows per tick; landmark512/top16/bucket128 reached exact top-1 `1.0` but still only winner `0.91015625` while scoring `1642.932` route rows plus `512` selector rows per tick. Random-projection512/top32/bucket64 q16 scored `1790.795` rows and matched exact top-1 only `0.255859375`. The probe implementation and tests are deleted after rejection; the 32768-column cleanup validation kept the promoted route-bank path in-band at `6128.457 tokens/sec` with `route_input_rows_scored=12/32768`. The retained evidence points back to a fused/GPU-owned graph/ANN discovery boundary that must pass winner quality and the 131072-token 6k-ish gate before promotion.
_Avoid_: restoring landmark/projection bucket code as an evaluation convenience, treating top-1 recall alone as winner-quality proof, or calling offline all-cache index construction a hot-path scheduler.

**Column Runtime Truth Projection** — the shared read-only projection of training/core-owned Column Runtime evidence. Active status helpers delegate to `service.column_runtime_projection.build_column_runtime_evidence`, so route-bank probe rows, score rows, probe cursor, probe refresh count, wake-plan mode, cached transition count, predictive cached-vote fraction, fallback reasons, and `runs_all_columns` truth stay normalized without reviving the deleted `StatusReadModel`. This helper must not decide wake/sleep, route candidates, cached-vote use, memory pressure, or state-transition scope; it only normalizes the Runtime Truth packet already emitted by training/core.
_Avoid_: duplicating Column Runtime projection logic in service modules, omitting probe-lane or cached-state fields from one status surface, or treating projection helpers as scheduler ownership.

**TurboQuant Routing Removal** — the removed legacy `turboquant_plus` retrieval backend. Google Research TurboQuant is a vector-quantization/inner-product approximation technique, not a capacity scheduler; MARULHO's old backend was narrower and stale: it used per-prototype min/max scalar codes, kept FP32 prototypes, scored every row, and could not provide `routing_tensor_cache()` for the promoted CUDA route/vote graph. The 2026-06-15 CUDA audit rejected it as a scheduler boundary at 1024 and 8192 columns, so the implementation, config option, and tests were deleted. Exact `torch_topk` remains the retrieval cache and seed oracle; steady CUDA route/vote rows are now bounded by the training-owned route candidate bank after that explicit seed.
_Avoid_: treating compressed all-row scoring as sparse routing, using QJL recall as winner-parity proof, restoring `turboquant_plus` as a selectable backend, or keeping stale TurboQuant code without benchmark evidence.

**Standalone IVF Router Removal** — the removed pure-PyTorch `IVFRouter` prototype. It had no live trainer/config/Runtime Truth caller, returned through a benchmark helper and export only, and used Python list/gather control around per-query candidate assembly. It was not an Expert-Choice capacity router, did not own a scheduler wake mask, and was not eligible for the promoted graph/cache route-vote path. IVF/RaBitQ-style routing remains a research direction only as a new bounded GPU-owned candidate router with no all-row wake tax.
_Avoid_: restoring standalone IVF code as scheduler progress, treating benchmark-only ANN prototypes as Runtime Truth, or adding a route backend that cannot expose tensor cache, capacity, fallback, recall, and long-run complete-runtime evidence.

**Routing Backend Consolidation** — the cleanup that removed selectable CPU FAISS/HNSW and numpy exact-cosine backend branches from the live routing index after `torch_topk` became the only promoted route/vote-compatible backend. The removed branches could return candidates, but they could not expose the exact device routing tensor cache used by the persistent CUDA graph, so selecting them either bypassed the true path or forced fallback before the scheduler boundary. The 2026-06-16 long cleanup gate reached `6152.191 tokens/sec` with `train_compute=0.132471 ms/token`, zero graph/sequence/native failures, and `state_transition_runs_all_columns=false`, so deletion did not hurt the 6k-ish real path. Follow-up cleanups renamed remaining `hnsw_index`/`_hnsw_*` active symbols to `routing_index`/`_routing_index_*`, removed retrieval-local backend constructor plumbing, and removed the `routing_index_mode` config field itself; old checkpoint keys are dropped with migration evidence instead of remaining as a pretend runtime choice. The config-field removal gate reached `6126.128 tokens/sec`, `train_compute=0.129371 ms/token`, `route_input_rows_scored=10/8192`, and `state_transition_cached_count=8182`. Future ANN, IVF, RaBitQ, or compressed-domain search work must enter as a new bounded GPU-owned candidate router with capacity, recall, fallback, and long complete-runtime evidence; it is not a hidden replacement for the fixed torch route. The promoted route still uses the exact torch cache for seed/oracle ownership, but steady route/vote scoring is now bounded by the training-owned route candidate bank plus fixed probe lane.
_Avoid_: keeping old CPU routing branches for convenience, treating `auto` backend selection as harmless, or claiming route sparsity from a backend that still scores or transfers all route rows.

**Merged Sharded Routing Cache** — the only live sharded routing-cache contract for the promoted CUDA route/vote graph. `routing_shards` may still partition column ownership for reports and host-store organization, but sharded tensor search must expose one merged torch cache through `routing_tensor_cache()` so the graph can bind stable vectors/ids and update the winning prototype coherently. The old `merge_torch_routing_shards=False` config and benchmark switches are retired; checkpoint load drops that key with a migration record instead of preserving a non-promoted route path. The 2026-06-16 long cleanup gate reached `6169.616 tokens/sec`, `train_compute=0.131525 ms/token`, `prepare_training=0.006390 ms/token`, `finalize_total=0.005854 ms/token`, and `tick_duration_ms.p95=21.197`, with zero graph/sequence/native failures and unchanged `10` active, `1014` cached transition rows.
_Avoid_: disabling the merged cache in benchmarks, treating per-shard exact top-k merge as sparse routing, or adding a sharded route mode that cannot feed the promoted graph cache.

**Batched Routing-Index Winner-ID Flush** — the trainer-owned routing-index maintenance rule that pending CUDA winner ids are materialized as one tensor batch during the routing-index flush instead of one scalar `.item()` per buffered entry. The flush cadence remains the existing bounded routing freshness policy; this only changes how ids cross to CPU at the explicit flush boundary.
_Avoid_: widening the routing-index flush interval without new quality evidence, synchronizing one CUDA scalar per buffered id, or adding per-token id snapshots that allocate enough to slow the hot path.

**Merged Torch Routing Cache** — the optional device-resident exact-search cache for a logically sharded `torch_topk` routing index. Shards remain the update/rebuild ownership boundary, while hot tensor search may concatenate normalized vectors and ids into one cache so one matrix multiply and one top-k replace repeated per-shard launches. Runtime Scope must expose enabled/dirty/ready state, cache devices, vector count, and bytes. The cache is invalidated by add, remove, and rebuild.
_Avoid_: describing the merged cache as approximate routing, hiding duplicated VRAM, restoring retired non-torch backends, or bypassing shard-owned updates.

**Compiled Dense Predictive Transition** — the retained CUDA fallback mode that computes all-column predictive error, failure streak, confidence, location/velocity integration, and prediction-weight updates as one fixed-shape functional transition. Compiled outputs are copied into stable runtime-owned state buffers outside the CUDA Graph so state survives replay. CPU candidate-scoped prediction remains unchanged. When `ColumnTransitionRuntime` cannot use the fused candidate predictive path, Runtime Truth must still record predictive update and predictive location as all-column, with `cuda_sparse_prediction_update_launch_bound_dense_retained` when candidate predictive scoping was otherwise due. A candidate-subset route/competition graph is not a sparse predictive-update claim unless Runtime Truth also reports `candidate_predictive_transition_active=true` or the CPU candidate predictive path.
_Avoid_: retaining CUDA Graph-owned outputs as persistent state, hiding first-use compile latency, compiling the one-use first-token transition, or calling isolated transition throughput full cognition throughput.
_Avoid_: treating candidate-local next predictive values as applied learning, changing live Runtime Truth from this benchmark, or assuming matmul precision changes help without measuring them.

**Dormant Input Plasticity Skip** — the competitive-column metabolism rule that lightweight input-synapse updates do not run while `input_weight_blend` is exactly zero, because those weights cannot contribute to routing or assembly output. Prototype learning, spike-window evidence, stale counters, and homeostasis remain active. Local STDP is not covered by this skip because its circuit also updates projection, inhibition, scaling, and spike state. Runtime Truth reports the observed input-plasticity mode and update/skip counters.
_Avoid_: skipping local STDP from blend alone, calling unchanged dormant input weights lost cognition, hiding the skip from Runtime Truth, or keeping zero-contribution row rewrites for compatibility.

**SNN Language Plasticity Runtime State** — the read-only snapshot of MARULHO-owned sparse language/readout transition memory, dense readout layout, checkpoint-backed application history, and developmental growth/pruning evidence. Canonical snapshot fields use `language_*`/`readout_*` wording. The surface, memory, consolidation, structural-plasticity, readout-capacity mutation, newborn-neuron integration, critical-period learning, maturation review, and newborn-synapse pruning paths are canonical readout paths.
_Avoid_: treating readout developmental evidence as hidden thought generation, adding new `thought_*` evidence fields, or using compatibility as a second implementation path.

**SNN Language Surface Canonical API** — the operator-facing `snn-language-readout-surface-*` route family for the surface design/preflight/executor/review chain after SNN language decoding review. It accepts `snn_language_readout_surface_*` request field names while delegating to the existing reviewed ledger/executor gates. Thought-era and generic surface public route vocabulary is no longer part of the active API.
_Avoid_: treating a surface as hidden thought generation, accepting unreviewed decoded language as cognition, or reviving legacy `thought`/generic surface route names for new callers.

**SNN Language Memory, Consolidation, And Structural Plasticity Canonical API** — the operator-facing `snn-language-readout-memory-*`, `snn-language-readout-consolidation-*`, and `snn-language-readout-structural-plasticity-*` route families for the memory/consolidation/structural-plasticity design/preflight/executor/review chain. They accept `snn_language_readout_*` request field names while delegating to the existing reviewed ledger/executor gates. Thought-era and generic public route vocabulary is no longer part of the active API.
_Avoid_: treating memory or consolidation route names as thought generation, bypassing review gates, or reviving legacy `thought`/generic route names for new callers.

**SNN Language Readout Capacity Mutation Canonical API** — the operator-facing `snn-language-readout-capacity-mutation-*` route family for the readout-capacity mutation design/preflight/executor/review chain. It accepts `snn_language_readout_capacity_mutation_*` request field names while delegating to the existing checkpoint-backed ledger/executor gates. Thought-era and generic capacity-mutation public route vocabulary is no longer part of the active API.
_Avoid_: treating route names as a new algorithm, bypassing existing promotion gates, or reviving legacy `thought` route names for new callers.

**SNN Language Newborn Neuron Integration Canonical API** — the operator-facing `snn-language-readout-newborn-neuron-integration-*` route family for the newborn-neuron integration design/preflight/executor/review chain. It accepts `snn_language_readout_newborn_neuron_integration_*` request field names while delegating to the existing checkpoint-backed developmental ledger/executor gates. Thought-era public and internal bridge vocabulary is no longer part of the active API.
_Avoid_: treating canonical route names as maturation proof, bypassing live-spike/checkpoint evidence, or reviving legacy `thought` route names for new callers.

**SNN Language Newborn Neuron Critical-Period Learning Canonical API** — the operator-facing `snn-language-readout-newborn-neuron-critical-period-learning-*` route family for the critical-period design/preflight/executor/review/continuation-design chain. It accepts `snn_language_readout_newborn_neuron_critical_period_learning_*` request field names while delegating to the existing checkpoint-backed developmental ledger/executor gates. Thought-era public and internal bridge vocabulary is no longer part of the active API.
_Avoid_: treating critical-period learning as free-form thought, bypassing activity/checkpoint evidence, or reviving legacy `thought` route names for new callers.

**SNN Language Newborn Maturation And Pruning Canonical API** — the operator-facing `snn-language-readout-newborn-neuron-maturation-outcome-review` and `snn-language-readout-newborn-synapse-pruning-*` route family for maturation review and pruning design/preflight/executor. It accepts `snn_language_readout_newborn_neuron_maturation_outcome_review` and `snn_language_readout_newborn_synapse_pruning_*` request field names while delegating to the existing checkpoint-backed developmental ledger/executor gates. Thought-era public and internal bridge vocabulary is no longer part of the active API.
_Avoid_: treating pruning eligibility as deletion, bypassing checkpoint evidence, or reviving legacy `thought` route names for new callers.

**SNN Language Readout Rollout Developmental Plasticity Review** — a MARULHO-owned, read-only structural review of rollout growth candidates produced when consolidation targets missing transition-memory edges. It compares the reviewed rollout design and shadow-application preflight with server-held sparse transition memory, verifies temporal locality and sparse topology budgets, and prepares candidate evidence for later regeneration review. It does not grow synapses, prune synapses, persist weights, issue permits, mutate runtime state, execute replay, or authorize plasticity.
_Avoid_: treating missing edges as weight updates, creating regeneration permits from rollout evidence directly, mixing pruning with growth review, or bypassing checkpoint-backed regeneration boundaries

**SNN Language Readout Rollout Regeneration Proposal Adapter** — a MARULHO-owned, read-only adapter that converts a reviewed rollout developmental-plasticity artifact into a regeneration-design preview shaped for the existing transition-memory regeneration permit flow. It carries bounded growth candidates, locality radius, sparse topology budgets, and permit requirements, but it does not issue a permit, attach replay evidence, grow synapses, persist weights, mutate runtime state, execute replay, or make the executor ready.
_Avoid_: passing the adapter directly to the regeneration executor, fabricating replay evidence, treating rollout review as a server-issued permit, or skipping the checkpoint-backed regeneration boundary

**SNN Language Readout Rollout Regeneration Replay Artifact Review** — a MARULHO-owned, read-only binding review between a Rollout Regeneration Proposal Adapter and a server-owned SNN transition-memory replay artifact. It recomputes adapter and replay-artifact hashes, checks internal-ledger replay evidence, and prepares the exact replay-artifact id plus regeneration design needed for a later operator-confirmed permit request. It does not issue permits, apply regeneration, grow synapses, persist weights, mutate runtime state, or execute replay.
_Avoid_: accepting unverified replay artifacts, treating a review as a permit, bypassing operator confirmation, or letting rollout growth evidence skip replay-controller provenance

**SNN Replay-Bound Regeneration Design** — the regeneration design after a server-owned SNN transition-memory replay artifact supplies hash-protected mismatch and pressure summaries. It replaces rollout-adapter structural intent with replay-derived mismatch pressure before permit issuance, so executor growth is justified by replay evidence rather than by a caller-provided or default score.
_Avoid_: issuing permits from adapter-only mismatch defaults, trusting opaque replay hashes without score summaries, or changing a design after permit hashing

**SNN Transition Memory Sleep Plasticity Policy** — a MARULHO-owned advisory scheduler that reads transition-memory state, Subcortex sleep pressure, replay evidence, readout-ledger evidence, and rollout-regeneration evidence to recommend the next operator-reviewed growth, application, or homeostatic-maintenance gate. It never records replay artifacts, issues permits, writes checkpoints, grows/prunes synapses, or mutates runtime state.
_Avoid_: treating recommendations as hidden autonomy execution, skipping operator-confirmed permit/application gates, or running growth without a follow-up homeostatic-maintenance review

**SNN Sleep Plasticity Review Ticket** — an operator-confirmed, Replay Controller-owned record that makes one Sleep Plasticity Policy recommendation durable before any future scheduler may follow it. It binds the reviewed policy, recommended action, suggested endpoint, current Runtime State revision, operator identity, transition-memory summary, replay evidence, rollout-regeneration evidence, and readout-ledger evidence into a hash-verified ticket. It does not execute the recommendation, record replay artifacts, issue permits, write checkpoints, grow/prune synapses, apply plasticity, or mutate runtime state.
_Avoid_: treating the ticket as permission to execute, accepting no-op monitoring policies as review tickets, reusing stale revision tickets after mutation, or skipping the endpoint-specific gate named by the policy

**SNN Sleep Plasticity Review Ticket Queue** — the read-only Replay Controller surface that exposes current Sleep Plasticity Review Tickets, verification status, stale/tamper counts, pending action counts, and the latest reviewed next gate for autonomy or operator inspection. It is visibility over durable tickets, not a scheduler and not execution authority.
_Avoid_: using queue presence as automatic permission, following stale/tampered tickets, or bypassing the endpoint-specific gate named by the latest verified ticket

**SNN Sleep Plasticity Autonomy Proposal** — the non-executing autonomy candidate derived from the latest verified Sleep Plasticity Review Ticket. It lets autonomy or an operator see which reviewed sleep/plasticity gate should be inspected next, and appears as advisory evidence in Living Loop, Policy Actuator, Terminus, Runtime Status, and Runtime Truth, while leaving replay, permit issuance, checkpoint writes, growth/pruning, plasticity, fact promotion, action execution, and structural writes disabled.
_Avoid_: treating status visibility as a scheduler, executing the suggested endpoint automatically, or promoting stale/tampered ticket evidence into autonomy

**SNN Sleep Plasticity Scheduler Experiment** — the isolated, non-executing stability measurement over a verified Sleep Plasticity Autonomy Proposal. It repeatedly inspects the reviewed ticket, revision, action, and next-gate identity across bounded cycles, records deterministic hash provenance, and reports whether a future scheduler design is ready for operator review. It performs no tensor work, so CUDA is explicitly not applicable; it does not install a scheduler, call the suggested endpoint, replay activity, issue permits, write checkpoints, apply plasticity, grow/prune synapses, or mutate runtime state.
_Avoid_: calling repeated inspection scheduling, hiding endpoint calls inside an experiment, claiming CUDA acceleration for control-plane hashing, or promoting experiment readiness directly into scheduler installation

**SNN Sleep Plasticity Scheduler Design** — the MARULHO-owned, read-only design proposal after a stable Scheduler Experiment. It binds the reviewed ticket and experiment hashes to bounded review cadence, minimum stable-cycle evidence, current-revision checks, and operator confirmation requirements for a future scheduler. It does not install a scheduler, call the suggested endpoint, replay activity, issue permits, write checkpoints, apply plasticity, grow/prune synapses, or mutate runtime state.
_Avoid_: treating a design as scheduler installation, hiding automatic endpoint execution behind review cadence, using stale experiment hashes, or turning operator-review intent into automatic plasticity

**SNN Sleep Plasticity Scheduler Design Review Ticket** — the operator-confirmed, Replay Controller-owned durable record that accepts one current Scheduler Design for later scheduler-installation review. The controller recomputes the design from verified internal ticket state before recording it, then binds operator identity, Runtime State revision, design hash, experiment hash, reviewed sleep-policy ticket, and named next gate. It does not install a scheduler, call the suggested endpoint, replay activity, issue permits, write checkpoints, apply plasticity, grow/prune synapses, or mutate transition memory.
_Avoid_: accepting caller-authored scheduler designs without controller recomputation, treating review as installation, reusing tickets after revision changes, or hiding endpoint execution behind operator confirmation

**SNN Sleep Plasticity Scheduler Design Review Ticket Queue** — the read-only Replay Controller visibility surface over accepted Scheduler Design Review Tickets. It verifies the current internal evidence chain, reports stale and tampered records, and exposes the latest verified accepted design for later autonomy planning without installing a scheduler or granting execution authority.
_Avoid_: treating queue presence as installation consent, surfacing stale designs as current, or calling reviewed endpoints from a visibility read

**SNN Sleep Plasticity Scheduler Installation Autonomy Proposal** — the non-executing autonomy candidate derived from the latest verified Scheduler Design Review Ticket. It exposes which accepted design is eligible for installation-preflight inspection while scheduler creation, timer registration, background workers, endpoint calls, replay, plasticity, and runtime mutation remain disabled.
_Avoid_: treating autonomy planning as installation, registering a timer from queue visibility, or calling the reviewed maintenance gate before installation review

**SNN Sleep Plasticity Scheduler Installation Preflight** — the deterministic read-only integrity review after the Scheduler Installation Autonomy Proposal. It binds accepted design evidence, review cadence, current Runtime State revision, and the named reviewed gate into an installation-review artifact without creating a scheduler, timer, worker, endpoint call, replay run, or plasticity update.
_Avoid_: treating preflight readiness as scheduler installation, hiding runtime work inside integrity checks, or bypassing operator installation review

**SNN Sleep Plasticity Review Scheduler Installation** — the operator-confirmed durable installation of one passive MARULHO review scheduler configuration. It binds current installation-preflight evidence to a bounded cadence and computes when the next operator review inspection is due. It registers no operating-system timer, starts no worker, calls no endpoint, records no replay, and applies no plasticity.
_Avoid_: treating cadence tracking as automatic maintenance, hiding endpoint calls behind a due flag, or keeping an installation active after its revision-bound design evidence becomes stale

**SNN Sleep Plasticity Review Scheduler Cycle Inspection** — the read-only artifact emitted when an installed passive Review Scheduler is inspected. It becomes review-ready only after cadence is due and preserves the reviewed sleep-plasticity endpoint for operator inspection without calling that endpoint, running replay, or applying plasticity.
_Avoid_: treating a due flag as endpoint authority, combining timing inspection with replay execution, or allowing cycle inspection to mutate transition memory

**SNN Sleep Plasticity Review Scheduler Cycle Acknowledgment** — the operator-confirmed cadence-state transition after one due Review Scheduler Cycle Inspection has produced a verified due-cycle replay review ticket. It binds the active scheduler configuration, consumes that ticket once, appends an immutable successor configuration, advances the passive next-review deadline by exactly one accepted interval, and records the acknowledgment count while calling no endpoint, running no replay, and applying no plasticity.
_Avoid_: acknowledging a non-due cycle, reusing an inspection hash, skipping multiple cadence intervals in one acknowledgment, or hiding consolidation work inside scheduler bookkeeping

**SNN Sleep Plasticity Review Scheduler Cycle Acknowledgment Preflight** — the deterministic read-only integrity review before Cycle Acknowledgment. It verifies the active scheduler configuration hash, due-state inspection, current due-cycle replay review ticket, deadline binding, and one-time consumption rule without advancing cadence or executing consolidation work.
_Avoid_: treating preflight readiness as acknowledgment, accepting generic replay-review tickets, or letting callers supply timestamps or cadence parameters

**SNN Sleep Plasticity Review Scheduler Cycle Autonomy Proposal** — the non-executing autonomy candidate derived from a due Review Scheduler Cycle Inspection. It exposes the reviewed sleep-plasticity gate as the next operator inspection target while timer registration, workers, endpoint calls, replay, checkpoints, plasticity, and runtime mutation remain disabled.
_Avoid_: treating cadence readiness as replay selection, endpoint authority, or permission to modify synapses

**SNN Due-Cycle Bounded Replay-Selection Proposal** — the deterministic non-executing nomination of a bounded set of current verified replay-evaluation contexts after a passive sleep review cadence becomes due. It combines timing evidence with consolidation priority for operator inspection only; it does not select replay-window content, record an artifact, run replay, call an endpoint, or modify synapses.
_Avoid_: treating a nominated context as a replay payload, allowing caller-authored queue evidence, or turning salience ranking into structural-write authority

**SNN Due-Cycle Replay Artifact Recording Review Proposal** — the deterministic read-only adapter that binds one due-cycle nominated replay-evaluation context to the existing artifact-recording policy recommendation. It proves that cadence, priority, policy, and current-revision context evidence agree before operator review; it does not create a ticket, derive replay-window content, record an artifact, run replay, or modify synapses.
_Avoid_: creating a parallel artifact recorder, treating policy agreement as durable consent, or bypassing the existing internal-ledger proposal and ticket path

**SNN Sleep Phase Separation Proposal** — the read-only sleep-cycle artifact that separates NREM-like replay nomination from REM-like stabilization review. The NREM-like phase may nominate bounded replay evidence for operator inspection; the REM-like phase may only become review-ready after cycle-acknowledgment preflight is ready. Neither phase records artifacts, runs replay, writes checkpoints, or applies plasticity.
_Avoid_: treating phase labels as biological claims, merging replay nomination with stabilization, or using a ready NREM-like phase as mutation authority

**SNN REM-Like Homeostatic Stabilization Preflight** — the read-only bridge from REM-like Stabilization/Integration Review to the existing Transition Memory Homeostasis command. It verifies phase readiness, transition-memory pressure, and already-bounded maintenance parameters, then names the operator-reviewed maintenance endpoint without executing it, writing checkpoints, pruning synapses, or mutating runtime state.
_Avoid_: treating preflight as maintenance, bypassing operator confirmation, or inventing a second homeostatic mutation path

**SNN Readout Rollout Server-State Binding** — the evidence rule that bounded SNN readout rollout text must be derived from MARULHO-held sparse transition memory at the runtime facade, not caller-carried weights, mocks, external checkpoints, or freeform decoding. The rollout records the server transition-memory hash, the rollout tensor hash, prediction/evaluation provenance matches, current state revision, bounded rollout parameters, and trajectory evidence while remaining non-mutating.
_Avoid_: letting API callers supply readout-rollout weights, silently accepting widened rollout bounds, treating a rollout label string as fact promotion, or hiding freeform generation behind sparse-readout terminology

**SNN Readout Rollout Runtime Truth Binding** — the compact Runtime Truth visibility gate for SNN Readout Rollout Server-State Binding. It exposes whether MARULHO currently holds sparse transition memory for bounded rollout review, plus server memory hash/count and negative execution flags, without calling rollout, replay, ledger recording, checkpointing, plasticity, or text generation.
_Avoid_: treating the Runtime Truth binding as a rollout candidate, exposing labels/text/prediction reports inside status, accepting caller-carried transition weights, or promoting cognition/facts/actions from status visibility

**SNN Readout Rollout Consolidation Path Evidence** — the compact Runtime Truth visibility gate for the durable rollout rehearsal/consolidation path. It summarizes recorded rollout-replay evidence count, latest rollout and transition-memory hashes, and the next review gate while keeping rehearsal, consolidation, live replay, ledger recording, checkpointing, plasticity, and text generation disabled.
_Avoid_: treating recorded rollout visibility as rehearsal execution, exposing replay targets or labels inside Runtime Truth, promoting consolidation from status, or using this summary as a substitute for the detailed ledger artifacts

**SNN Readout Applied Synapse Provenance Evidence** — the compact Runtime Truth visibility gate for applied readout-derived sparse transition memory. It summarizes sparse weight count, provenance row count, replay-regeneration count, complete local-edge provenance count, missing local-edge provenance, invalid rollout step order, orphan weights, and dangling provenance without running the full audit, exposing raw weights, executing replay, writing checkpoints, applying plasticity, or generating language.
_Avoid_: treating Runtime Truth as the audit itself, exposing `synapse_provenance_by_key` or sparse weights in status, hiding missing rollout-local provenance after regeneration, or letting status reads trigger repair/plasticity

**Server-Bound Rollout Ledger Evidence** — durable rollout evidence whose replay evaluation, ledger material hash, and rehearsal policy candidate all preserve the server transition-memory hash, hash-match flag, and `service.runtime_facade.snn_language_plasticity_runtime_state` source. Missing or tampered server binding blocks ledger recording or removes the event from rehearsal eligibility.
_Avoid_: relying only on generic transition-weight hashes, rehearsing rollout records with missing server-memory provenance, or letting durable evidence forget that the public rollout path ignored caller-carried weights

**SNN Language Readout Rollout Regeneration Permit Request** — an operator-confirmed command that submits a reviewed rollout regeneration replay-artifact binding to the existing replay-controller regeneration permit issuer. It may durably record a permit through replay-controller provenance, but it does not apply regeneration, grow synapses, prune synapses, persist weights, write checkpoints, execute replay, or bypass the checkpoint-backed regeneration executor.
_Avoid_: issuing without operator confirmation, using fabricated replay artifacts, treating a permit as synapse mutation, or skipping the executor's checkpoint transaction

**SNN Language Readout Rollout Regeneration Application Preflight** — a MARULHO-owned final preflight that converts a verified rollout regeneration permit request into an executor-shaped regeneration proposal only when the current state revision and checkpoint intent are present. It does not call the executor, save checkpoints, grow synapses, prune synapses, apply plasticity, or mutate runtime state.
_Avoid_: treating proposal assembly as application, omitting checkpoint intent, using stale state revisions, or bypassing the executor's operator confirmation

**SNN Language Readout Rollout Regeneration Application** — an operator-confirmed rollout command that delegates a preflight-approved, replay-bound regeneration proposal to the existing checkpoint-backed transition-memory regeneration executor. It may grow bounded sparse transition synapses only after server-owned replay mismatch evidence, replay-controller permit verification, checkpoint transaction, row-mass limits, and rollback path all align; it does not implement a second write path or decode/generate language.
_Avoid_: applying rollout growth directly, accepting stale preflight revisions, mismatching checkpoint intent, bypassing replay-permit verification, using adapter-only mismatch defaults, or treating executor-blocked proposals as partial mutation

**SNN Language Transition Memory Prediction Evaluation** — the read-only gate that compares baseline sparse next-code prediction with persistent-memory-assisted prediction across grounded evaluation windows. It measures mismatch deltas, influence count, improved/worsened sequence counts, and persistent weight coverage before a readout draft can be treated as useful evidence.
_Avoid_: claiming persistent memory helps because it exists, using one influenced prediction as utility proof, or treating evaluation as training

**SNN Language Readout Corpus Evaluation** — the slow-path evaluation runner for bounded MARULHO-owned language/readout corpora. It wraps SNN Language Transition Memory Prediction Evaluation with corpus provenance, license/terms/cache fields, grounding coverage, device evidence, latency, memory/VRAM cost, mutation absence, and an explicit promote/reject decision for bounded operator review. Runtime Truth may project the latest saved report and its paired checkpoint-review report, but it must not run the evaluator, train, load external checkpoints, mutate runtime state, or change the verdict from the report alone. The first bounded fixture report reached `promotion_decision=promote_bounded_readout_review` with `mean_mismatch_delta=0.166667`; the paired isolated checkpoint review wrote `reports/snn_language_readout_corpus/readout-corpus-checkpoint.json`, verified restore with checkpoint hash `724a0f9bf46af65e66e7aca19b1bc90f7e05fc5d303051c9f16d2a20c83d8358`, wrote rollback manifest hash `76e2bf4432a5635ab8517d28c1f0dee06475f4e1c1d72458959cbfd322b00361`, and kept `production_runtime_changed=false`. The paired 262144-token CUDA service gate at `reports/snn_language_readout_corpus/post-readout-corpus-long-run-65536-262144-i32.json` reached `6307.305 tokens/sec` on the same active-pressure `65536`-column checkpoint, while the checkpoint-review follow-up at `reports/snn_language_readout_corpus/post-readout-checkpoint-review-long-run-65536-262144-i32.json` stayed in the same band at `6219.317 tokens/sec` with no observed contention, `route_input_rows_scored=12/65536`, `state_transition_runs_all_columns=false`, and zero native sequence failures/fallbacks. This is bounded review evidence, not live language generation, hidden LLM cognition, or a speed promotion.
_Avoid_: using a report or isolated checkpoint as live language generation, hiding Hugging Face/Nemotron data provenance, treating isolated evaluation weights as runtime training, skipping rollback/restore verification, skipping longer throughput validation before promotion, or promoting readout while dropping below the current long-run throughput band

**SNN Language Readout Emission** — the operator-visible bounded language output derived from a ready SNN Language Readout Draft. It binds emitted labels/text to the draft, readout trajectory hash, sparse prediction hash, transition-memory evaluation hash, and persistent transition-weight hash while staying read-only: it cannot promote facts/actions, become a cognition substrate, write checkpoints, apply plasticity, or claim freeform language generation.
_Avoid_: displaying blocked drafts as output, treating emitted labels as truth, hiding freeform decoding behind readout terminology, or using emission as plasticity consent

**SNN Language Readout Emission Review** — the operator-confirmed display-history record for a ready SNN Language Readout Emission. It records that bounded SNN output was reviewed, with emission/prediction/trajectory/transition-memory hashes and operator evidence, but remains separate from replay memory, plasticity consent, fact promotion, action authority, and cognition-substrate evidence.
_Avoid_: treating display acknowledgment as learning, replay priority, truth, action approval, or permission to mutate transition memory

**SNN Language Readout Emission Review History** — the operator-facing read-only inspection surface for reviewed SNN Language Readout Emissions. It may display the already-reviewed bounded text and labels with their provenance hashes, but it returns only emission-review events and cannot expose draft ledger events, rollout events, replay targets, prediction reports, transition-memory evaluations, or any mutation authority.
_Avoid_: using display history as replay memory, training data, action/fact approval, a broad ledger export, or a source of unreviewed generated text

**SNN Emission Replay Evaluation Policy** — the read-only selector that compares reviewed SNN Language Readout Emissions against internal SNN Language Readout Evidence Ledger rows. A reviewed emission can become a replay-evaluation policy candidate only when prediction, transition-memory evaluation, persistent-weight, label, and grounding evidence match an existing MARULHO-owned sparse readout evidence row; display text is reduced to hashes and is not a replay source.
_Avoid_: replaying from text history alone, accepting reviewed labels without matching sparse evidence, recording replay memory from policy output, or treating policy readiness as replay/plasticity permission

**SNN Emission Replay Evaluation Design** — the read-only design seed that turns policy-ready reviewed emissions into hash-bound replay-context review inputs. It verifies candidates still match the internal SNN Language Readout Evidence Ledger, requires device-review evidence, and points to server-computed replay-context recording as the next gate without recording that context, replaying activity, exposing display text, applying plasticity, or promoting facts/actions.
_Avoid_: treating the design as replay memory, accepting display text or labels as replay windows, recording replay contexts from design output alone, skipping server-computed mismatch/pressure evidence, or using design readiness as plasticity permission

**SNN Emission Replay Design Path Evidence** — the compact Runtime Truth visibility summary for the reviewed-emission replay-design path. It reports reviewed-emission counts, internal readout-evidence counts, matched hash-only candidate counts, latest provenance hashes, and the next review gate while keeping raw text, labels, candidate arrays, replay-context recording, replay execution, plasticity, checkpoints, facts/actions, and cognition-substrate promotion disabled.
_Avoid_: treating status visibility as the design artifact, exposing reviewed text or labels inside Runtime Truth, using matched counts as replay memory, or skipping the device-reviewed design and server-computed replay-context gates

**SNN Emission Replay Context Review** — the operator-confirmed bridge from SNN Emission Replay Evaluation Design into the existing server-held Evaluated SNN Transition-Memory Replay Context recorder. It verifies the design seed, prediction hash, observed sparse readout slots, device evidence, runtime-truth/rollback pressure gate, and operator confirmation before recording mismatch/pressure evidence through the Replay Controller; it does not replay activity, record replay memory, apply plasticity, write checkpoints, expose reviewed text, or promote facts/actions.
_Avoid_: recording replay contexts from text or labels alone, bypassing mismatch/pressure recomputation, accepting unconfirmed design seeds, treating context recording as replay execution, or using the bridge as plasticity permission

**SNN Emission Replay Context Lineage** — the hash-verified source metadata stored with an Evaluated SNN Transition-Memory Replay Context when it originates from reviewed SNN emission evidence. It binds the emission review, emission hash, readout evidence hash, replay-design hash, seed hash, prediction hash, and operator identity into the replay context so later queues/artifacts can audit why the context exists without reading display text.
_Avoid_: treating mismatch/pressure hashes as enough lineage, dropping emission provenance after context recording, storing raw display text as context metadata, or accepting lineage that does not verify against the context evidence hash

**SNN Replay Context Lineage Propagation** — the hash-only carrying of SNN Emission Replay Context Lineage from a verified Evaluated SNN Transition-Memory Replay Context into the SNN Replay Consolidation Priority Queue, SNN Replay Artifact Recording Policy Proposal, and SNN Replay Artifact Recording Review Ticket. It exposes the source metadata hash and compact emission/readout/prediction/design hashes for audit continuity while omitting raw source metadata, operator identity, display text, labels, replay execution, artifact recording, and structural-write authority.
_Avoid_: treating lineage presence as consolidation eligibility, copying raw source metadata or operator identity into queue candidates, accepting policy/ticket lineage that no longer matches the verified replay context, or using lineage hashes as a substitute for server-held context verification

**SNN Replay Artifact Lineage** — the hash-only continuation of verified SNN Replay Context Lineage Propagation into an Evaluated SNN Transition-Memory Replay Artifact and any Replay-Backed Regeneration Permit issued from it. It binds artifact and permit evidence back to the source metadata hash and compact emission/readout/prediction/design hashes while still requiring the server-held replay context, review ticket, internal readout evidence, and current Runtime State revision to verify.
_Avoid_: recording evaluated artifacts from lineage alone, using lineage hashes as regeneration authority, dropping source lineage at permit issuance, or exposing raw source metadata/operator identity/text/labels through artifact or permit evidence

**SNN Applied Replay Lineage Provenance** — the hash-only preservation of SNN Replay Artifact Lineage on applied replay-regenerated synapses. It lets Runtime Truth and Synapse Provenance Audit confirm that a structural growth record still points back to the replay artifact source hash and compact emission/readout/prediction lineage while separately requiring local-edge provenance, bounded weights, checkpoint-backed application, and server-verified permits.
_Avoid_: treating applied lineage as proof that a synapse should exist, accepting partial lineage when a replay-regenerated synapse claims source metadata, exposing raw text/labels/operator identity, or bypassing local-edge and checkpoint evidence

**SNN Durable Applied Replay Lineage Checkpoint Evidence** — the checkpoint-level summary proving that saved SNN language plasticity state contains complete hash-only Applied Replay Lineage Provenance for replay-regenerated synapses. It reads the mutation-maintained CPU `snn_applied_replay_lineage_incremental_summary.v1` counts and digest beside the saved plasticity state so restore audits can detect missing lineage without scanning every provenance row, running replay, applying plasticity, or exposing raw text/labels/operator identity.
_Avoid_: treating checkpoint lineage summary as replay-artifact verification, permitting regeneration from a checkpoint summary alone, storing raw source metadata in checkpoint summaries, replacing full Synapse Provenance Audit with the summary, or rebuilding the summary by scanning `synapse_provenance_by_key` during checkpoint save

**SNN Applied Replay Lineage Restore Validation** — the restore-time comparison between Durable Applied Replay Lineage Checkpoint Evidence and the restored mutation-maintained applied replay-lineage summary. It reports whether saved/restored counts and digests match, and if the incremental summary is missing it blocks exact validation instead of recomputing from every provenance row. It performs no replay execution, plasticity application, permit issuance, or raw text/labels/operator identity exposure.
_Avoid_: silently trusting a saved checkpoint summary, treating restore validation as mutation authority, accepting restored replay-regenerated synapses with mismatched lineage summaries, running replay/plasticity from restore validation, or recovering a missing summary with a checkpoint-time full provenance scan

**SNN Applied Replay Lineage Restore Visibility** — the compact Runtime Truth evidence over SNN Applied Replay Lineage Restore Validation. It reports whether restore validation is available, whether saved/restored lineage counts and material hashes match, and whether the restored checkpoint may proceed to readout synapse provenance audit review, while remaining advisory and unable to load checkpoints, run replay, apply plasticity, issue permits, or write checkpoints.
_Avoid_: hiding restore mismatches in raw checkpoint metadata, treating visibility as restore execution, using status as a checkpoint loader, or promoting restored synapses to replay/plasticity/fact/action authority from status alone

**SNN Restore-Gated Synapse Audit Readiness** — the rule that Runtime Truth may report applied replay-regenerated synapse provenance as ready for readout synapse provenance audit only when local provenance is complete and any available SNN Applied Replay Lineage Restore Validation is not mismatched. Fresh runtime state without restore validation may still proceed on complete local evidence, but restored checkpoint state with count or material-hash disagreement blocks audit readiness.
_Avoid_: letting complete local-edge provenance override a failed restore validation, requiring restore metadata for never-restored live state, or treating a passing readiness gate as permission to replay, mutate, issue permits, or promote facts/actions

**SNN Restore-Gated Synapse Audit Execution** — the non-mutating audit behavior that consumes SNN Applied Replay Lineage Restore Validation when present. The audit endpoint may still return an audit artifact for operator inspection, but a restored lineage mismatch is required evidence failure and blocks the audit artifact from becoming review-ready.
_Avoid_: making Runtime Truth stricter than the actual audit endpoint, hiding restored checkpoint mismatch during audit, throwing away inspectable audit rows on mismatch, or using audit execution as replay/plasticity/checkpoint authority

**SNN Restore-Gated Regeneration Permit Boundary** — the mutation-facing rule that a restored applied replay-lineage mismatch blocks replay-backed regeneration permit issuance and checkpoint-backed regeneration preflight. It prevents restored checkpoint uncertainty from crossing into new structural growth while still allowing operators to inspect non-mutating audit artifacts.
_Avoid_: issuing replay regeneration permits from restored mismatched lineage, allowing fabricated preflight artifacts to erase restore failure, treating audit inspectability as mutation authority, or blocking fresh never-restored runtime state that has no restore validation

**SNN Language Capacity Pressure Evidence** — the read-only Runtime Truth signal that fixed language-neuron capacity is becoming a bottleneck. It measures sparse transition edge occupancy, active neuron coverage, source-neuron fanout saturation, key validity, and provenance cleanliness before any future capacity-expansion design is allowed to be reviewed.
_Avoid_: treating pressure evidence as neuron growth, resizing tensors from status, expanding capacity while provenance is dirty, or confusing sparse synapse growth inside the current neuron space with dynamic neuron/layer expansion

**SNN Language Capacity Expansion Design** — the read-only operator-review artifact that turns clean SNN Language Capacity Pressure Evidence into bounded proposed language-neuron and sparse-edge budgets. It requires device evidence, prefers CUDA relayout evidence, requires rollback/checkpoint policy, and preserves existing indices/provenance while remaining unable to resize tensors, add neurons/layers, train, replay, or write checkpoints.
_Avoid_: treating a design as an executor, expanding capacity without pressure, dropping existing synapse provenance during relayout, using CPU-only evidence as preferred for expansion, or bypassing checkpoint/restore validation before any future resize

**SNN Language Capacity Expansion Preflight** — the read-only checkpoint-backed review artifact that validates a Capacity Expansion Design before any future resize executor. It binds the design hash, current runtime revision, checkpoint snapshot/restore evidence, and CUDA relayout evidence while still forbidding tensor resize, neuron/layer growth, training, replay, plasticity, or checkpoint writes.
_Avoid_: accepting hand-edited expansion designs, treating a saved checkpoint claim as a resize, allowing CPU-only relayout evidence to become preferred capacity growth, or executing topology changes from preflight

**SNN Language Capacity State** — the durable MARULHO-owned metadata record that states the current language-neuron count, sparse-edge budget, fanout budget, and expansion count carried with SNN language plasticity checkpoints. It is capacity evidence, not a resize action: it keeps dynamic capacity disabled and cannot add neurons, add layers, train, replay, or mutate tensors by itself.
_Avoid_: mistaking capacity metadata for topology mutation, enabling dynamic capacity without a resize executor, losing capacity state across checkpoint restore, or using capacity state as permission to bypass pressure/design/preflight gates

**SNN Capacity Resize Compatibility Audit** — the read-only gate that compares a checkpoint-backed Capacity Expansion Preflight target against the runtime boundaries that still assume fixed language-neuron and sparse-edge limits. It must block any resize executor while index validators, tensor shapes, sparse budgets, provenance paths, CUDA relayout, or checkpoint restore logic remain fixed-capacity.
_Avoid_: treating a passing preflight as proof that the runtime can resize, hiding fixed 64-neuron or 256-edge assumptions, allowing metadata-only growth, or making compatibility audit perform the resize it is reviewing

**SNN Language Capacity Fixed Boundary Evidence** — the Runtime Truth summary of active SNN language runtime boundaries that still assume fixed capacity. It makes fixed neuron-index validators, dense tensor shapes, sparse-edge budgets, and non-dynamic growth blockers visible before a specific Capacity Resize Compatibility Audit is requested.
_Avoid_: hiding fixed-capacity assumptions until executor time, calling fixed-boundary visibility a resize audit, treating it as permission to grow, or reporting dynamic capacity while validators and tensor shapes remain fixed

**SNN Capacity Boundary Inventory** — the canonical list of runtime boundaries that constrain SNN language capacity growth. Runtime Truth fixed-boundary evidence and target-specific resize compatibility audits must read the same inventory so each migrated validator, tensor shape, sparse budget, provenance path, or checkpoint rule changes capacity eligibility in one place.
_Avoid_: duplicating boundary lists across endpoints, marking a boundary dynamic in one surface but fixed in another, auditing a resize target without the shared inventory, or letting documentation drift from the active blocker list

**SNN Capacity-Aware Sparse Plasticity Boundary** — the migrated sparse mutation boundary where the SNN language plasticity executor validates regenerated or live-applied sparse synapses against durable SNN Language Capacity State rather than only the original 64-neuron and 256-edge defaults. It allows sparse transition-memory growth inside a checkpointed capacity envelope, but it is not full network resize because readout-ledger dense tensors and other fixed boundaries can still block capacity expansion.
_Avoid_: treating sparse-dict acceptance as dense tensor resize, bypassing checkpoint-backed capacity state, allowing unbounded fanout or edge growth, or marking readout/generation paths dynamic because the plasticity executor became capacity-aware

**SNN Capacity-Aware Readout Regeneration Adapter Boundary** — the migrated readout-ledger boundary where already-reviewed rollout growth candidates are adapted into sparse transition-memory regeneration proposals using durable SNN Language Capacity State for index validation. It can route above-default sparse candidate indices toward permit review, but it still cannot allocate larger dense readout tensors, issue permits, write checkpoints, apply plasticity, or generate language.
_Avoid_: treating adapter readiness as a permit, dense tensor resize, or live mutation; accepting above-default indices without capacity state; bypassing replay-controller permit review; or claiming the readout ledger is fully dynamic while dense shadow tensors remain fixed

**SNN Capacity-Aware Replay Artifact Review Boundary** — the migrated readout-ledger boundary where replay-artifact review validates sparse regeneration-design indices against the adapter's SNN Language Capacity State before permit request review. It preserves replay binding and operator review while still forbidding permit issuance, checkpoint writes, live replay, plasticity, dense tensor resize, and language generation.
_Avoid_: treating replay-artifact review as a permit, applying replay-backed growth without the checkpoint executor, losing adapter capacity evidence, or using replay readiness to bypass dense readout resize blockers

**SNN Capacity-Aware Regeneration Permit Request Boundary** — the runtime-facade boundary that validates replay-bound sparse regeneration-design indices against carried SNN Language Capacity State before asking the replay controller for a regeneration permit. It may issue a permit when operator and restore evidence pass, but it still cannot apply plasticity, write checkpoints, resize dense readout tensors, or generate language.
_Avoid_: issuing permits from above-default indices without capacity evidence, treating a permit as growth application, bypassing restore-lineage checks, or dropping capacity evidence before checkpoint-backed regeneration preflight

**SNN Capacity-Aware Regeneration Application Preflight Boundary** — the runtime-facade boundary that revalidates permitted sparse regeneration-design indices against carried SNN Language Capacity State before the checkpoint-backed regeneration executor can be reviewed. It preserves the permit, checkpoint, revision, and capacity envelope without applying plasticity, writing checkpoints, resizing dense readout tensors, or generating language.
_Avoid_: treating preflight readiness as mutation, allowing permit requests to drop capacity evidence before executor review, accepting above-default indices from fallback defaults, or claiming dense readout resize because sparse application preflight is capacity-aware

**SNN Capacity-Aware Regeneration Application Boundary** — the runtime-facade mutation boundary that requires the checkpoint-backed regeneration preflight and executor proposal to carry matching SNN Language Capacity State before any sparse transition-memory regeneration call is made. It can delegate above-default sparse candidates to the checkpoint executor when capacity, revision, checkpoint, operator, and permit evidence agree, but it still does not resize dense readout tensors or generate language.
_Avoid_: calling the executor with drifted capacity evidence, treating application delegation as autonomous growth, skipping checkpoint and permit checks, or assuming sparse transition-memory mutation proves full language-network resize

**SNN Capacity-Aware Readout Sparse Budget Boundary** — the migrated readout-ledger sparse topology boundary where rollout shadow preflight and developmental plasticity review compare simulated sparse transition-memory edges against SNN Language Capacity State instead of the original fixed 256-edge budget. It lets sparse growth review honor an expanded budget while still blocking dense readout tensor resize, checkpoint writes, plasticity application, and language generation.
_Avoid_: treating a larger sparse budget as permission to allocate dense tensors, accepting budget checks without carried capacity state, letting preflight and review use different sparse budgets, or bypassing the checkpoint-backed regeneration path

**SNN Dense Readout Resize Plan** — the read-only Runtime Truth and operator-review artifact that describes how the fixed dense readout surface would be relaid out for a larger SNN Language Capacity State: target tensor shape, preserved 64x64 copy window, zero-initialized new rows/columns, dense boundary IDs, CUDA relayout requirement, and checkpoint/restore requirement. It is not executable and exists to make the remaining dense blockers inspectable before a checkpoint-backed CUDA resize executor is built.
_Avoid_: treating the plan as a resize executor, changing dense tensors from status evidence, skipping CUDA/checkpoint validation, or confusing sparse transition-memory capacity growth with dense readout tensor materialization

**SNN Dense Readout Resize Preflight** — the read-only operator-review artifact that verifies a Dense Readout Resize Plan against the current runtime revision, checkpoint snapshot/restore evidence, CUDA relayout evidence, shape-growth invariants, preserved dense copy window, and zero-fill region before a dense readout resize executor exists. It remains non-executable until a checkpoint-backed CUDA executor is implemented.
_Avoid_: treating preflight readiness as tensor mutation, accepting CPU relayout evidence for a CUDA-first dense boundary, skipping checkpoint restore validation, or allowing a stale/tampered resize plan to advance toward execution

**SNN Dense Readout Resize Transaction Proposal** — the read-only operator-review artifact that turns a Dense Readout Resize Preflight into an explicit checkpoint-backed CUDA transaction recipe: load verified checkpoint state, allocate the target dense tensor on CUDA, copy the preserved window, zero-fill added rows and columns, verify invariants, and persist migration evidence. It is still not an executor and cannot mutate tensors or mark dense capacity boundaries dynamic by itself.
_Avoid_: treating the transaction proposal as execution, bypassing operator confirmation, omitting rollback evidence, writing dense tensors from the read model, or using the recipe to claim language generation readiness

**SNN Dense Readout Resize Executor Readiness Audit** — the read-only audit that checks whether a Dense Readout Resize Transaction Proposal has the concrete executor capabilities needed for real mutation: dense readout layout ownership evidence, live dense tensor weight ownership, CUDA allocation, checkpoint writing, migration-ledger writing, post-resize boundary marking, and boundary-migration tests. It names missing capabilities and remaining dense boundary IDs without mutating tensors or marking resize complete.
_Avoid_: treating capability names as implementations, hiding remaining dense boundary blockers, allowing executor eligibility without migration tests, or claiming network resize while the audit remains read-only

**SNN Dense Readout Layout State** — the durable metadata that records the current fixed dense readout shape, target shape implied by SNN Language Capacity State, preserved copy window, zero-fill region, CUDA relayout requirement, checkpoint requirement, and migration status. It is persisted with SNN language plasticity state and exposed in Runtime Truth, but it does not contain or mutate dense tensor weights.

**SNN Dense Readout Layout Ownership Evidence** — the Runtime Truth evidence that lets executor-readiness derive MARULHO ownership of dense readout layout metadata and verify that it matches a transaction recipe. It proves the server owns the shape contract, not the live dense tensor weights; live weight ownership remains a separate executor capability.
_Avoid_: treating layout metadata as tensor ownership, marking dense resize applied without an executor, letting target shape drift from capacity state, or hiding pending CUDA relayout behind sparse transition-memory growth

**SNN Dense Readout Layout Migration Executor** — the checkpoint-backed mutation boundary that records an operator-confirmed dense readout layout migration ledger after the transaction recipe and Runtime Truth layout evidence agree. It may persist the target shape, preserved window, zero-fill region, migration count, and checkpoint commit evidence, but it does not materialize CUDA dense tensor weights or mark dense resize applied.
_Avoid_: treating layout migration as tensor allocation, setting `dense_resize_applied` before live weights are owned and copied, generating language from migration evidence, or using the ledger to bypass the later CUDA tensor materialization executor

**SNN Dense Readout Tensor Materialization Readiness** — the read-only gate after layout migration that checks whether the CUDA dense tensor executor can safely materialize the target readout weights. It requires a committed layout migration ledger, matching Runtime Truth layout state, live dense weight ownership, CUDA allocation, preserved-window copy, zero-fill, checkpoint writing, migration-ledger writing, post-resize boundary marking, and migration tests before dense resize can become executable.
_Avoid_: using layout migration alone as tensor materialization, treating missing copy/zero-fill kernels as implementation details, marking language generation ready from tensor readiness, or resizing the network from the read model

**SNN Dense Readout Tensor Materialization Executor** — the checkpoint-backed mutation boundary that creates the MARULHO-owned dense readout tensor from sparse SNN language transition weights or an existing dense readout tensor, preserves the configured window, zero-fills expanded cells, records actual device placement, and commits the materialized tensor through Runtime Persistence. It may mark dense readout tensor materialization complete, but it still does not train, decode text, generate language, or promote thoughts by itself.
_Avoid_: treating tensor materialization as a trained language generator, hiding CPU fallback when CUDA is unavailable, skipping checkpoint commit evidence, or losing sparse transition-memory provenance during dense projection

**SNN Dense Readout Tensor Integrity Evidence** — the Runtime Truth audit for a materialized dense readout tensor. It reports shape, dtype, device, CUDA placement, nonzero count, and sampled sparse-weight cell matches without exposing the raw matrix, and it can make later dense readout training review eligible while keeping language generation false.
_Avoid_: exposing raw tensor weights in status, treating tensor integrity as generation quality, ignoring sparse-to-dense mismatches, or hiding actual device placement

**SNN Dense Readout Training Readiness** — the read-only gate that decides whether a materialized dense SNN language readout can enter isolated training-loop design. It requires tensor integrity, supported heldout language-adapter evidence, device evidence, and rollback evidence, but it still cannot train runtime weights, return trained weights, generate language, or execute actions.
_Avoid_: treating tensor integrity alone as trainability, skipping heldout grounding, promoting runtime training without rollback evidence, or confusing training-loop design readiness with language generation

**SNN Dense Readout Training Loop Design** — the read-only training blueprint for a materialized dense SNN language readout. It requires dense-readout training readiness, bounded training and validation transition counts, a supported local spike-learning rule, bounded learning rate/epochs/delta norm, device evidence, and rollback evidence before any later checkpoint-backed preflight can be considered; it cannot execute training, return weights, mutate runtime state, write checkpoints, generate language, or execute actions.
_Avoid_: treating a training design as trained weights, bypassing bounded transition evidence, using an unsupported optimizer path, ignoring CUDA requirements, or promoting text generation from a design artifact

**SNN Dense Readout Training Loop Preflight** — the read-only checkpoint and executor-capability audit for a dense SNN language readout trainer. It requires a ready training-loop design, current expected state revision, checkpoint path, tensor shape, bounded local-learning constraints, device/CUDA compatibility, checkpoint writer capability, and bounded-delta application capability before any future executor can be reviewed; it still cannot execute training, return weights, write checkpoints, mutate runtime state, generate language, or execute actions.
_Avoid_: treating preflight eligibility as an executed training result, ignoring stale revisions, accepting missing checkpoint evidence, hiding CUDA capability gaps, or using preflight as a language-generation permit

**SNN Dense Readout Training Executor** — the checkpoint-backed mutation boundary that applies bounded local spike-transition updates to the materialized dense SNN language readout tensor and mirrors updated cells into sparse transition memory/provenance so Runtime Truth tensor integrity can still audit dense/sparse agreement. It may train MARULHO-owned readout weights under preflight, revision, checkpoint, operator-confirmation, and bounded-delta constraints, but it still does not decode text, generate language, return raw trained weights, execute actions, or claim cognition.
_Avoid_: applying dense tensor updates without sparse provenance, bypassing checkpoint rollback, accepting stale revisions, returning the trained matrix to callers, or treating trained readout weights as language generation

**SNN Dense Readout Post-Training Evaluation** — the read-only gate that evaluates whether a checkpointed dense readout training run preserved dense/sparse tensor integrity, produced bounded transition/update evidence, passed heldout adapter support, and retained rollback evidence before any decoder-probe design can be reviewed. It cannot train, mutate runtime state, write checkpoints, return weights, decode text, generate language, execute actions, or claim cognition.
_Avoid_: treating a successful training mutation as sufficient for decoding, skipping post-training heldout checks, ignoring dense/sparse mismatch after training, or promoting language generation from training evidence alone

**SNN Dense Readout Decoder-Probe Design** — the read-only design for connecting a post-training dense SNN language readout to MARULHO's bounded Spike Language Decoder Probe. It requires post-training evaluation, grounded readout slots, sparse population-code evidence, temporal-state evidence, tensor device evidence, and explicit free-form generation lockout before any decoder-probe preflight can be reviewed; it cannot sample text, generate language, execute actions, mutate runtime state, write checkpoints, or claim cognition.
_Avoid_: treating grounded labels as free-form text, bypassing sparse decoder evidence, using ungrounded readout slots, making decoder design executable, or promoting language generation before a separately evaluated generator path exists

**SNN Dense Readout Decoder-Probe Preflight** — the read-only revision/device/sparsity audit that decides whether a bounded dense-readout decoder-probe design can be executed later. It requires a ready decoder-probe design, current expected state revision, bounded grounded sparse-code evidence, tensor device evidence, CUDA compatibility when requested, and explicit language-generation lockout; it cannot run the probe, sample text, generate language, mutate runtime state, write checkpoints, execute actions, or claim cognition.
_Avoid_: executing a decoder probe from stale revision evidence, hiding CUDA fallback, treating preflight as decoded language, bypassing sparse-code checks, or using preflight eligibility as a generation permit

**SNN Dense Readout Decoder-Probe Execution** — the read-only bounded execution of a preflighted dense-readout decoder probe. It may expose grounded label candidates, sparse activity, tensor-device evidence, and a review gate, but it still cannot sample free-form text, generate language, mutate runtime state, write checkpoints, execute actions, or claim cognition.
_Avoid_: treating grounded label candidates as generated prose, skipping operator review, hiding sparse activity evidence, using candidate labels as facts/actions, or feeding probe output directly into plasticity

**SNN Dense Readout Label-Candidate Review** — the read-only operator-confirmed evidence gate after bounded decoder-probe execution. It binds grounded label candidates, execution hash, sparse activity, tensor-device evidence, operator identity, and confirmation into a review hash, but it still cannot decode text, generate language, promote facts, execute actions, record replay artifacts, apply plasticity, mutate runtime state, write checkpoints, or claim cognition.
_Avoid_: treating candidate review as language generation, recording unexecuted or stale probe output, using reviewed labels as facts/actions, feeding labels into plasticity, or hiding the execution hash that produced them

**SNN Dense Label-Candidate Evidence Ledger** — the MARULHO-owned append-only audit lane for operator-confirmed dense-readout label-candidate reviews. It records review hash, decoder execution hash, grounded labels, label hash, tensor-device evidence, sparse activity count, state revision, and operator identity for later inspection, while remaining ineligible for replay memory, plasticity, facts, actions, checkpoints, free-form generation, or cognition-substrate claims by itself.
_Avoid_: mixing dense label-candidate records with replay-ready readout drafts, treating the ledger event as language memory, recording unconfirmed reviews, replaying labels directly, or using audit presence as proof of thought

**SNN Dense Label-Candidate History** — the read-only operator inspection surface over the dense label-candidate evidence ledger. It may show bounded reviewed labels, hashes, device placement, sparse activity counts, revision, and operator metadata for audit, but it cannot record new ledger events, run replay, apply plasticity, write checkpoints, promote facts/actions, generate text, or claim cognition.
_Avoid_: using history inspection as a replay selector, treating repeated labels as autonomous truth, hiding decoder execution hashes, or widening history into generation/plasticity authority

**SNN Dense Label-Candidate Calibration Policy** — the read-only advisory ranking over dense label-candidate history that nominates reviewed decoder-label evidence for operator calibration review. It may prioritize candidates by bounded labels, hashes, sparse activity, device evidence, recency, and repetition, but it cannot train dense readouts, adjust weights, run replay, apply plasticity, generate language, promote facts/actions, write checkpoints, or claim cognition.
_Avoid_: treating calibration priority as calibration execution, using repetition as truth, skipping operator review, or feeding policy candidates directly into training/generation

**SNN Dense Label-Candidate Calibration Evaluation Design** — the read-only design for evaluating calibration quality of reviewed dense decoder-label candidates against heldout label evidence. It binds selected candidate hashes, heldout target hash, supported calibration metrics, cross-validation requirements, coverage checks, and device evidence before any later preflight can be considered; it cannot run evaluation, train, adjust weights, replay, apply plasticity, generate language, promote facts/actions, write checkpoints, or claim cognition.
_Avoid_: treating evaluation design as measured calibration, accepting missing heldout evidence, using unsupported metrics, or using design readiness as a generation/training permit

**SNN Dense Label-Candidate Calibration Evaluation Preflight** — the read-only revision/device/executor audit before any dense label-candidate calibration evaluation executor can be considered. It requires a ready evaluation design, current state revision, design hash, selected candidate hashes, heldout target hash, calibration metrics, cross-validation and coverage requirements, device/CUDA evidence, and explicit calibration-evaluator capability while still refusing evaluation execution, training, replay, plasticity, checkpoints, facts/actions, generation, or cognition claims.
_Avoid_: running calibration from stale revisions, hiding CUDA fallback, treating preflight as measured calibration, bypassing heldout hashes, or using executor eligibility as training/generation authority

**SNN Dense Label-Candidate Calibration Evaluation** — the non-mutating measurement surface that computes bounded calibration evidence for preflighted dense decoder-label candidates against heldout labels. It may report expected calibration error, reliability bins, coverage gap, label-set stability, and evaluated samples, but it cannot train, adjust weights, record ledger events, run replay, apply plasticity, write checkpoints, promote facts/actions, generate text, or claim cognition.
_Avoid_: treating calibration metrics as a weight update, using ECE alone as truth, evaluating candidates not selected by preflight, or promoting measured labels directly into language generation

**SNN Dense Label-Candidate Calibration Evaluation Review** — the read-only operator review gate over measured dense label-candidate calibration evidence. It binds evaluation hash, preflight hash, metric thresholds, current revision, operator identity, and confirmation before any later calibration-update design can be considered, while still refusing training, weight updates, replay, plasticity, checkpoints, fact/action promotion, language generation, or cognition claims.
_Avoid_: treating a passing metric review as an update permit, accepting unconfirmed review, using global ECE as sufficient proof, or bypassing later checkpoint-backed update design

**SNN Dense Label-Candidate Calibration Update Design** — the read-only design for a future bounded post-hoc calibration adjustment after reviewed calibration metrics. It may propose bounded temperature/confidence scaling parameters, device evidence, and rollback requirements, but it cannot apply an update, persist weights, train dense readouts, run replay, apply plasticity, write checkpoints, promote facts/actions, generate language, or claim cognition.
_Avoid_: treating update design as applied calibration, widening temperature deltas without bounds, skipping rollback evidence, or using a calibration design as a language-generation permit

**SNN Dense Label-Candidate Calibration Update Preflight** — the read-only checkpoint-backed gate immediately before a future dense label calibration update executor. It verifies update-design hashes, current runtime revision, bounded post-hoc parameters, rollback policy, checkpoint path, device/CUDA evidence, and executor capability, but it cannot apply calibration, write the checkpoint, persist weights, train, replay, apply plasticity, promote facts/actions, generate language, or claim cognition.
_Avoid_: treating preflight readiness as proof that the update has happened, accepting stale revisions, bypassing checkpoint/rollback evidence, or using calibration-update readiness as a broad readout-training or language-generation permit

**SNN Dense Label-Candidate Calibration Update Application** — the checkpoint-backed mutation that installs bounded post-hoc dense label calibration parameters into MARULHO-owned readout state after a ready preflight and operator confirmation. It records applied calibration lineage and advances runtime revision, but it cannot persist dense weights, train, replay, apply plasticity, promote facts/actions, generate language, or claim cognition.
_Avoid_: treating calibration application as dense-readout training, accepting stale preflights, applying without rollback lineage, changing class/order semantics, or using applied confidence scaling as proof of language understanding

**SNN Dense Label-Candidate Calibration Update Application Review** — the read-only lineage and rollback audit after bounded dense label calibration parameters have been installed. It verifies the applied calibration hash matches the current MARULHO-owned readout state, checks revision sequencing, bounded temperature/confidence deltas, checkpoint path, rollback policy, and device evidence, but it cannot train, replay, apply plasticity, write checkpoints, promote facts/actions, generate language, or claim cognition.
_Avoid_: treating an applied-calibration review as post-calibration performance proof, using it as a decoder-training permit, bypassing an observation window, or equating confidence scaling with thought/language generation

**SNN Dense Label-Candidate Post-Calibration Observation Window** — the read-only observation surface after applied calibration review. It compares bounded post-calibration confidence samples against correctness and pre-calibration confidence to estimate calibration error and confidence drift before any later operator review, but it cannot adjust calibration, train, replay, apply plasticity, write checkpoints, promote facts/actions, generate language, or claim cognition.
_Avoid_: treating a short observation window as final decoder validation, hiding sample hashes, using drift metrics as plasticity signals, or allowing observed confidence improvements to bypass operator review

**SNN Dense Label-Candidate Post-Calibration Operator Review** — the read-only operator-confirmed review of a post-calibration observation window. It binds observation hash, applied calibration lineage, metric thresholds, current revision, operator identity, and confirmation before any later calibrated-confidence-use design, but it cannot adjust calibration, train, replay, apply plasticity, write checkpoints, promote facts/actions, generate language, or claim cognition.
_Avoid_: treating operator review as automatic deployment, accepting unconfirmed or stale observation windows, using confidence metrics as facts/actions, or confusing calibrated confidence with language understanding

**SNN Calibrated Dense Label Confidence Use Design** — the read-only design for future use of applied dense-label calibration as confidence metadata. It may specify ranking, thresholding, abstention, and operator display of calibrated confidence for bounded label candidates, but it cannot decode text, generate language, train dense readouts, run replay, apply plasticity, write checkpoints, promote facts/actions, or claim cognition.
_Avoid_: treating calibrated confidence as proof a label is true, using thresholds as autonomous action policy, sampling text from confidence scores, or feeding calibrated confidence directly into plasticity/training

**SNN Calibrated Dense Label Confidence Use Executor** — the bounded non-mutating command that applies a preflighted calibrated-confidence policy to existing dense label-candidate hashes. It may rank candidates, select candidates above threshold, or abstain, but its output remains hash-level display evidence and cannot decode text, generate language, train, replay, apply plasticity, write checkpoints, promote facts/actions, or claim cognition.
_Avoid_: treating selected label hashes as generated words, accepting candidates not matched to the preflight, promoting confidence-ranked labels into memory/facts/actions, or using abstention as a hidden retry/generation loop

**SNN Calibrated Dense Label Confidence Operator Display Review** — the operator-confirmed, read-only review of a hash-only calibrated-confidence executor result. It may expose bounded ranked/selected candidate hashes, calibrated confidence, and abstention status for display, but it cannot reveal new text, generate language, train, replay, apply plasticity, write checkpoints, promote facts/actions, or claim cognition.
_Avoid_: treating display review as language understanding, widening hash-only output into prose, using operator display as replay/fact/action evidence, making human approval the primary progression path, or bypassing later grounded language-surface gates

**SNN Calibrated Dense Label Confidence Internal Stability Review** — the self-review surface that checks repeated hash-only confidence-use results for stable selected hashes, bounded confidence drift, and replay-style consistency without operator approval. It can make future autonomous confidence replay review eligible, but it cannot generate language, train, replay live memory, apply plasticity, write checkpoints, promote facts/actions, or mutate runtime state by itself.
_Avoid_: requiring human approval for routine confidence stability checks, treating repeated confidence as truth, allowing unstable candidate hashes to self-promote, or turning internal review into hidden mutation/generation

**SNN Calibrated Dense Label Confidence Autonomous Replay Review Design** — the no-operator design surface after internal confidence stability review. It specifies bounded future replay-review cycles, replay-consistency thresholds, confidence-drift limits, and device/CUDA evidence for autonomous confidence review, but it cannot run replay, recalibrate, train, apply plasticity, write checkpoints, generate language, promote facts/actions, or mutate runtime state.
_Avoid_: treating autonomous design as live replay, requiring manual approval before routine internal confidence review can progress, widening stable confidence into truth/language, or bypassing later replay preflight and rollback evidence

**SNN Calibrated Dense Label Confidence Autonomous Replay Review Preflight** — the no-operator revision/device/executor audit before future autonomous confidence replay review. It verifies current Runtime State revision, design/stability/execution hashes, selected hash stability, replay/drift bounds, device/CUDA compatibility, and executor capability, but it still cannot run replay, recalibrate, train, apply plasticity, write checkpoints, generate language, promote facts/actions, or mutate runtime state.
_Avoid_: treating preflight readiness as replay execution, requiring manual approval for routine autonomy checks, ignoring CUDA/device capability, or using stable confidence hashes as facts/language without later replay evidence

**SNN Calibrated Dense Label Confidence Autonomous Replay Review Executor** — the no-operator bounded executor that runs internal confidence replay review cycles from a ready autonomous replay preflight. It can compute replay consistency, confidence drift, and cycle-level selected-hash matches, but it cannot run live replay memory, recalibrate, train, apply plasticity, write checkpoints, generate language, promote facts/actions, or mutate runtime state.
_Avoid_: treating replay review as a weight update, storing reviewed hashes as memory/facts, using stable replay metrics as language generation, or requiring human approval before routine internal replay evidence can be computed

**SNN Calibrated Dense Label Confidence Autonomous Recalibration Design** — the no-operator design surface that turns passing confidence replay-review metrics into bounded recalibration parameters and rollback requirements. It can propose constrained temperature/confidence-rescale deltas for a future preflight, but it cannot apply calibration, train, replay, apply plasticity, write checkpoints, generate language, promote facts/actions, or mutate runtime state.
_Avoid_: treating a recalibration design as an applied update, allowing unbounded self-training, requiring human approval for routine internal recalibration design, skipping rollback evidence, or using confidence recalibration as language generation

**SNN Calibrated Dense Label Confidence Autonomous Recalibration Preflight** — the no-operator revision/device/executor audit before future bounded confidence recalibration. It verifies the autonomous recalibration design hash, current Runtime State revision, replay/drift bounds, proposed delta bounds, rollback evidence, device/CUDA compatibility, and executor capability, but it cannot apply calibration, train, replay, apply plasticity, write checkpoints, generate language, promote facts/actions, or mutate runtime state.
_Avoid_: treating preflight as an applied calibration update, skipping current-revision checks, allowing recalibration without rollback evidence, requiring human approval for routine autonomous recalibration checks, or widening calibrated confidence into language/facts/actions

**SNN Calibrated Dense Label Confidence Autonomous Recalibration Executor** — the no-operator bounded command that applies a preflighted confidence recalibration update to the runtime calibration ledger. It may record an autonomous calibration event and advance Runtime State revision, but it cannot train dense weights, run replay memory, apply plasticity, write checkpoints, generate language, promote facts/actions, or claim cognition.
_Avoid_: treating autonomous recalibration as language learning, skipping the autonomous preflight, applying unbounded deltas, persisting weights without a later checkpoint path, or using a calibration event as fact/action evidence

**SNN Calibrated Dense Label Confidence Autonomous Recalibration Application Review** — the no-operator, read-only lineage audit after an autonomous confidence recalibration update. It verifies that the applied autonomous update is current in the runtime calibration ledger, bounded, rollback-backed, device-compatible, and still non-training/non-generative before any autonomous post-calibration observation window.
_Avoid_: routing autonomous updates through human display approval, treating an applied calibration event as proof of language ability, skipping current-ledger hash matching, or letting post-update review apply another calibration update

**SNN Calibrated Dense Label Confidence Autonomous Post-Calibration Observation Window** — the no-operator observation surface that measures bounded post-update confidence behavior after an autonomous recalibration. It checks sample hashes, confidence bounds, expected calibration error, and confidence drift before later autonomous stability review, but it cannot apply another update, train, replay, apply plasticity, write checkpoints, generate language, promote facts/actions, or mutate runtime state.
_Avoid_: treating a passing observation window as generated language, hiding additional calibration updates inside monitoring, using too few post-update samples, skipping hash-backed observations, or making human approval the routine path after autonomous recalibration

**SNN Calibrated Dense Label Confidence Autonomous Post-Calibration Stability Review** — the no-operator review that turns an autonomous post-calibration observation window into stability evidence. It verifies sample sufficiency, hash-backed observations, expected calibration error, confidence drift, and absence of hidden replay/training/mutation before allowing an autonomous calibrated-confidence use design.
_Avoid_: treating one passing metric as language readiness, applying another recalibration during stability review, skipping sample hashes, routing stability back through operator review, or promoting unstable confidence into facts/actions

**SNN Calibrated Dense Label Confidence Autonomous Use Design** — the no-operator design surface for using stable, post-calibrated dense-label confidence as hash-level ranking/threshold/abstention metadata. It consumes autonomous post-calibration stability evidence and can plan bounded candidate-hash selection, but it cannot decode text, generate language, train, replay, apply plasticity, write checkpoints, promote facts/actions, or mutate runtime state.
_Avoid_: treating calibrated confidence as language understanding, using selected hashes as words, routing routine confidence-use planning through operator approval, or turning abstention into hidden retry/generation

**SNN Calibrated Dense Label Confidence Autonomous Use Preflight** — the no-operator current-revision, candidate, device, and executor-capability audit before bounded autonomous calibrated-confidence use. It verifies design lineage, candidate hashes, confidence thresholds, CUDA/device fit, and executor availability while still remaining advisory and non-mutating.
_Avoid_: treating preflight as execution, selecting candidates without current revision evidence, allowing confidence use to generate text, or making executor capability a proxy for training/plasticity authority

**SNN Calibrated Dense Label Confidence Autonomous Use Executor** — the no-operator ledger writer that records bounded hash-level calibrated-confidence use after autonomous preflight. It ranks/selects/abstains over candidate hashes, writes an autonomous confidence-use event, and advances Runtime State, but it still cannot decode text, generate language, train, replay, apply plasticity, write checkpoints, or promote facts/actions.
_Avoid_: treating selected hashes as words, allowing candidate text payloads, using a confidence-use event as replay memory or fact evidence, or hiding training/plasticity behind autonomous execution

**SNN Calibrated Dense Label Confidence Autonomous Use Event Review** — the no-operator read-only audit of a recorded autonomous confidence-use event. It verifies current Runtime State, ledger presence, event hash integrity, bounded selected/ranked candidate hashes, abstention consistency, and hash-only output before any later autonomous hash-to-readout binding design.
_Avoid_: treating event review as language generation, reviewing unrecorded events, allowing selected text payloads, or promoting a confidence-use event directly into replay memory, facts, or actions

**SNN Autonomous Hash Readout Binding Design** — the no-operator design surface that binds reviewed autonomous candidate hashes to bounded readout slot hashes. It preserves the bridge from sparse confidence evidence to later readout vocabulary compatibility while remaining design-only: no text decoding, language generation, training, replay, plasticity, checkpoints, facts/actions, or runtime mutation.
_Avoid_: treating a slot binding as generated language, binding unreviewed candidate hashes, exposing slot labels as model output, or using binding design as a shortcut to dense readout training

**SNN Autonomous Hash Readout Binding Preflight** — the no-operator current-revision, device, CUDA, executor-capability, and binding-integrity audit before applying an autonomous hash-to-readout binding. It verifies reviewed candidate hashes still match bounded readout slot hashes and remains non-generative, non-training, non-replay, non-plastic, and non-mutating.
_Avoid_: executing stale binding designs, treating preflight as slot training, skipping CUDA/device evidence, or allowing hash bindings to become emitted language/facts/actions

**SNN Autonomous Hash Readout Binding Executor** — the no-operator ledger writer that records a current hash-to-readout binding event after binding preflight. It advances Runtime State and preserves candidate-hash/readout-slot-hash compatibility as durable evidence, but it still does not train, replay, apply plasticity, decode text, generate language, write checkpoints, or promote facts/actions.
_Avoid_: treating a binding event as a learned word, writing synaptic weights from binding execution, reusing stale preflights, or routing binding evidence directly into language generation

**SNN Autonomous Hash Readout Binding Event Review** — the no-operator read-only audit of a recorded hash-to-readout binding event. It verifies the binding event is present in the ledger, current with Runtime State, hash-only, bounded, and non-generative before any later autonomous bound-readout observation design.
_Avoid_: reviewing unrecorded bindings, treating binding review as language output, skipping event hash integrity, or promoting binding evidence directly into replay memory, facts, or actions

**SNN Autonomous Bound Readout Observation Design** — the no-operator design surface for observing bound candidate-hash/readout-slot-hash pairs across sparse SNN cycles. It plans activation-sparsity, slot-drift, and binding-reactivation checks before any readout observation execution while refusing text decoding, language generation, training, replay, plasticity, checkpoint writes, facts/actions, or runtime mutation.
_Avoid_: treating observation design as generated language, running hidden readout execution, ignoring activation sparsity, or turning bound-slot observations into training/plasticity without later gates

**SNN Autonomous Bound Readout Observation Preflight** — the no-operator current-revision, device, CUDA, executor-capability, and target-integrity audit before observing bound readout slots. It verifies target hashes, candidate hashes, slot hashes, observation cycles, sparsity/drift/reactivation thresholds, and non-generative/non-training boundaries before any observation executor.
_Avoid_: observing stale designs, skipping executor/device evidence, treating preflight as readout output, or letting observation become replay/plasticity/training without later gates

**SNN Autonomous Bound Readout Observation Executor** — the no-operator ledger writer that records bounded sparse observation metrics for reviewed bound readout slots. It accepts only current-revision hash observations whose activation sparsity, slot drift, binding reactivation, candidate hashes, and slot hashes match the preflight window, then opens only an event-review gate.
_Avoid_: treating recorded sparse observation metrics as generated language, decoded text, dense-readout training, replay memory, plasticity, checkpoints, facts/actions, or cognition substrate

**SNN Autonomous Bound Readout Observation Event Review** — the no-operator read-only audit of a recorded bound-readout observation event. It verifies ledger presence, revision freshness, hash-only samples, activation sparsity, slot drift, and binding reactivation before any later autonomous readout training-window design.
_Avoid_: treating observation review as language generation, dense-readout training, replay execution, plasticity approval, checkpoint permission, fact/action promotion, or a cognition-substrate claim

**SNN Autonomous Readout Training Window Design** — the no-operator design surface that turns reviewed sparse bound-readout observation evidence into a bounded SNN training-window plan. It chooses sample windows, truncated temporal credit assignment, micro-batching, learning-rate ceilings, CUDA/device constraints, and spike-memory controls before any training executor exists.
_Avoid_: treating a training-window design as training execution, generated language, replay memory, plasticity approval, checkpoint permission, fact/action promotion, or a cognition-substrate claim

**SNN Autonomous Readout Training Window Preflight** — the no-operator current-revision and device audit before an autonomous sparse readout training-window executor. It verifies design hashes, sample hashes, target hashes, temporal bounds, CUDA/memory readiness, executor capability, and non-generative/non-plasticity boundaries before any training mutation can run.
_Avoid_: treating preflight readiness as completed training, generated language, replay memory, plasticity approval, checkpoint permission, fact/action promotion, or a cognition-substrate claim

**SNN Autonomous Readout Training Window Executor** — the no-operator ledger writer for a bounded sparse readout training update. It accepts only current preflight windows with matching sample hashes, bounded loss/gradient/weight-delta/spike-sparsity metrics, runtime weight-update evidence, and no checkpoint/text/replay/plasticity side effects.
_Avoid_: treating training evidence as language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Readout Training Window Event Review** — the no-operator read-only audit of a recorded sparse readout training update. It verifies ledger presence, revision freshness, bounded loss movement, gradient norm, weight delta, spike sparsity, update hashes, and no text/replay/plasticity/checkpoint side effects before any autonomous decoder probe design.
_Avoid_: treating a reviewed training event as generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Decoder Probe Design** — the no-operator design surface for probing a reviewed autonomous sparse readout update without emitting language. It binds training-event hashes, update hashes, sample hashes, and target hashes into bounded hash-rank or slot-activation probe targets before any decoder probe preflight.
_Avoid_: treating decoder probe design as generated text, free-form language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Decoder Probe Preflight** — the no-operator current-revision and device audit before a bounded autonomous decoder probe executor. It verifies decoder-probe design hashes, probe targets, update hashes, CUDA/device fit, executor capability, and hash-only output boundaries before any decoder probe execution.
_Avoid_: treating decoder probe preflight as decoded text, generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Decoder Probe Executor** — the no-operator ledger writer for bounded hash-only decoder probe results from a reviewed trained sparse readout. It records probe target hashes, output hashes, rank hashes, score, spike sparsity, and slot drift without decoding text or generating language.
_Avoid_: treating hash-only probe results as free-form language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Decoder Probe Event Review** — the no-operator read-only audit that verifies a recorded autonomous decoder probe event is present in the ledger, hash-only, bounded, sparse, current, and free of checkpoint, replay, plasticity, or text-generation side effects before any autonomous language-output design can be reviewed. It cannot decode text, generate language, mutate runtime state, record replay artifacts, write checkpoints, promote facts/actions, or claim cognition.
_Avoid_: treating reviewed hash-only probe results as generated language or direct semantic understanding; they are only evidence for the next autonomous language-output design gate

**SNN Autonomous Language Output Design** — the no-operator read-only design that turns reviewed autonomous decoder probe hashes into bounded language-output slots for a later preflight. It binds output mode, output length, candidate hashes, sparsity/score/drift thresholds, and device evidence without decoding tokens or emitting text.
_Avoid_: treating output-slot design as generated language, free-form text, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Language Output Preflight** — the no-operator current-revision, device, CUDA, slot-hash, and executor-capability audit before bounded autonomous language-output execution. It verifies the output design can be executed as hash-only output slots without decoding tokens, emitting text, mutating runtime state, applying plasticity, recording replay, or writing checkpoints.
_Avoid_: treating output preflight as generated language, decoded text, free-form language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Language Output Executor** — the no-operator ledger writer for bounded hash-only language-output slot results. It records candidate hashes, output-slot hashes, emitted hashes, rank hashes, confidence, spike sparsity, and slot drift without decoded text, generated text, replay execution, plasticity, checkpoint writes, fact/action promotion, or cognition claims.
_Avoid_: treating recorded hash-only output slots as human-readable language, free-form generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Language Output Event Review** — the no-operator read-only audit that verifies a recorded autonomous language-output event is present in the ledger, revision-adjacent, hash-only, bounded, sparse, and free of decoded text, generated text, replay, plasticity, or checkpoint side effects before any autonomous decoded-output design can be reviewed.
_Avoid_: treating reviewed hash-only output slots as decoded language, free-form generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Decoded Output Design** — the no-operator read-only design that binds reviewed hash-only language-output slots to constrained token-candidate hashes, tokenizer hash, vocabulary hash, and decode-constraint hash before a later decoded-output preflight. It defines a bounded decode map without returning token strings, decoded text, or generated language.
_Avoid_: treating decode-map design as decoded text, generated language, free-form generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Decoded Output Preflight** — the no-operator current-revision, device, CUDA, vocabulary/tokenizer/constraint-hash, decode-slot, and executor-capability audit before constrained decoded-output execution. It verifies a bounded decode map is executable without returning token strings, decoded text, generated language, replay artifacts, plasticity, checkpoint writes, facts/actions, or cognition claims.
_Avoid_: treating decoded-output preflight as decoded text, generated language, free-form generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Decoded Output Executor** — the no-operator ledger writer for constrained decoded-token hash results. It records decoded-output slot hashes, token-candidate hashes, decoded-token hashes, token-id hashes, constraint-state hashes, confidence, spike sparsity, and drift while refusing token strings, decoded text, generated language, replay, plasticity, checkpoints, facts/actions, or cognition claims.
_Avoid_: treating constrained decoded-token hashes as human-readable language, generated text, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Decoded Output Event Review** — the no-operator read-only audit that verifies a recorded constrained decoded-output event is present in the ledger, revision-adjacent, hash-only, bounded, sparse, constraint-backed, and free of token strings, decoded text, generated text, replay, plasticity, or checkpoint side effects before bounded text-emission design.
_Avoid_: treating reviewed decoded-token hashes as human-readable language, free-form generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Bounded Text Emission Design** — the no-operator read-only design that binds reviewed decoded-token hashes to text-fragment hashes, text-surface schema hash, text normalizer hash, semantic constraint hash, and bounded emission slots before a later text-emission preflight. It defines the text surface as hashes only and returns no literal text.
_Avoid_: treating bounded text-emission design as emitted text, free-form generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Bounded Text Emission Preflight** — the no-operator current-revision, device, CUDA, text-fragment, text-surface schema, normalizer, semantic-constraint, and executor-capability audit before bounded text-emission execution. It verifies a hash-only text surface can be executed without literal text, free-form generation, replay, plasticity, checkpoint writes, fact/action promotion, or cognition claims.
_Avoid_: treating bounded text-emission preflight as emitted text, free-form generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Bounded Text Emission Executor** — the no-operator ledger writer that records bounded text-emission results as hashes only. It accepts current preflight evidence plus text-fragment, slot, schema, normalizer, semantic-constraint, confidence, sparsity, and drift witnesses, then records an event without returning literal text or opening replay, plasticity, checkpoint, fact/action, or free-form generation paths.
_Avoid_: treating bounded text-emission executor events as human-readable emitted text, unconstrained language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Bounded Text Emission Event Review** — the no-operator read-only audit that verifies a recorded bounded text-emission event is present in the ledger, revision-adjacent, hash-only, schema-bound, normalizer-bound, semantic-constraint-bound, sparse, confident, and drift-bounded. It opens only later hash-surface sequence review, not literal text, replay, plasticity, checkpoint writes, fact/action promotion, or free-form generation.
_Avoid_: treating bounded text-emission event review as emitted natural language, unconstrained language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Sequence Review** — the no-operator read-only audit that verifies reviewed text-fragment hashes form a bounded schema-bound, normalizer-bound, semantic-constraint-bound text-surface sequence. It computes a fragment-sequence hash and opens only later hash-surface commit design, not literal text rendering, replay, plasticity, checkpoint writes, fact/action promotion, or free-form generation.
_Avoid_: treating text surface sequence review as displayed text, generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Commit Design** — the no-operator read-only design that turns a reviewed hash-only text-surface sequence into a commit plan hash with explicit commit scope and retention class. It defines how MARULHO may later commit the hash surface without rendering literal text, replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, or claiming cognition.
_Avoid_: treating text surface commit design as committed state, displayed text, generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Commit Preflight** — the no-operator current-revision, device, CUDA, commit-plan, schema, normalizer, semantic-constraint, and executor-capability audit before a hash-surface commit executor may write state. It verifies commit integrity without rendering literal text, replaying memory, applying plasticity, writing checkpoints directly, promoting facts/actions, or claiming cognition.
_Avoid_: treating text surface commit preflight as committed state, displayed text, generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Commit Executor** — the no-operator ledger writer that commits a reviewed hash-only text surface into MARULHO-owned runtime state. It records commit-plan, sequence, event, schema, normalizer, semantic-constraint, state-chain, and fragment hashes without rendering literal text, replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, or claiming cognition.
_Avoid_: treating text surface commit executor events as displayed text, generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Commit Event Review** — the no-operator read-only audit that verifies a committed hash-only text surface is present in the ledger, matches current committed-surface state, is revision-adjacent, state-chain-bound, schema-bound, normalizer-bound, semantic-constraint-bound, sparse, confident, and drift-bounded. It opens only later text-surface materialization design, not literal text rendering, replay, plasticity, checkpoint writes, fact/action promotion, or free-form generation.
_Avoid_: treating text surface commit event review as displayed text, generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Materialization Design** — the no-operator read-only design that turns a reviewed committed hash-only text surface into a bounded materialization plan. It binds materialization mode, output contract, maximum surface size, commit-event, commit-plan, sequence, schema, normalizer, semantic-constraint, state-chain, and fragment hashes without rendering literal text, replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, or claiming cognition.
_Avoid_: treating text surface materialization design as displayed text, generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Materialization Preflight** — the no-operator current-revision, device, CUDA, materialization-plan, schema, normalizer, semantic-constraint, and executor-capability audit before a bounded text-surface materializer may render anything. It verifies pre-generation admission without rendering literal text, replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, or claiming cognition.
_Avoid_: treating text surface materialization preflight as displayed text, generated language, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Materialization Executor** — the no-operator ledger writer that renders a bounded text surface only when supplied text fragments hash back to the committed fragment hashes under the same schema, normalizer, and semantic-constraint evidence. It records rendered-text, fragment, materialization-plan, commit-event, state-chain, confidence, sparsity, and drift evidence without replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, or claiming free-form language generation/cognition.
_Avoid_: treating text surface materialization executor output as unconstrained language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Text Surface Materialization Event Review** — the no-operator read-only audit that verifies a bounded text-surface materialization event is present in the ledger, matches current materialized state, is revision-adjacent, hash-bound to the committed fragments, schema-bound, normalizer-bound, semantic-constraint-bound, sparse, confident, and drift-bounded. It opens only later bounded language-surface review, not replay, plasticity, checkpoint writes, fact/action promotion, or free-form generation.
_Avoid_: treating text surface materialization event review as unconstrained language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Review** — the no-operator read-only audit that classifies a verified bounded text surface as language-facing evidence. It preserves rendered text, fragment hashes, schema/normalizer/semantic-constraint hashes, confidence, sparsity, and drift evidence while opening only a later bounded language-surface commit design, not replay, plasticity, checkpoint writes, fact/action promotion, or free-form generation.
_Avoid_: treating bounded language-surface review as unconstrained language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Commit Design** — the no-operator read-only design that turns a reviewed bounded language surface into a commit plan with explicit commit scope and retention class. It binds rendered-text, bounded-language-surface, materialization, schema, normalizer, semantic-constraint, confidence, sparsity, and drift hashes without replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, or claiming free-form generation/cognition.
_Avoid_: treating bounded language-surface commit design as replay memory, structural plasticity, checkpoint persistence, fact/action promotion, unconstrained language generation, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Commit Preflight** — the no-operator current-revision, device, CUDA, executor-capability, rendered-text, fragment-hash, materialization, schema, normalizer, semantic-constraint, confidence, sparsity, and drift audit before a bounded language-surface commit executor may exist. It admits only traceable bounded text evidence and still cannot record ledger events, replay memory, apply plasticity, write checkpoints, promote facts/actions, or claim free-form generation/cognition.
_Avoid_: treating bounded language-surface commit preflight as execution, durable memory, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, unconstrained language generation, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Commit Executor** — the no-operator mutation boundary that records a bounded literal language-surface commit event and updates the current bounded language-surface commit pointer only after preflight, revision, hash, schema, normalizer, semantic-constraint, confidence, sparsity, drift, and text-bound evidence agree. It commits traceable bounded language-surface state without replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, or claiming free-form generation/cognition.
_Avoid_: treating bounded language-surface commit execution as replay memory, structural plasticity, checkpoint persistence, fact/action promotion, unconstrained language generation, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Commit Event Review** — the no-operator read-only audit that verifies a committed bounded language-surface event is present in the ledger, matches the current bounded language-surface commit pointer, is revision-adjacent, and preserves rendered-text, fragment, materialization, schema, normalizer, semantic-constraint, confidence, sparsity, and drift evidence. It opens only a later bounded language-surface use review, not replay, plasticity, checkpoint writes, fact/action promotion, or free-form generation.
_Avoid_: treating bounded language-surface commit event review as replay memory, structural plasticity, checkpoint persistence, fact/action promotion, unconstrained language generation, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Use Review** — the no-operator read-only review that classifies a reviewed committed bounded language surface as usable bounded language evidence under an explicit use scope. It preserves rendered-text, fragment, state-chain, schema, normalizer, semantic-constraint, confidence, sparsity, and drift evidence while still refusing replay memory, plasticity, checkpoint writes, fact/action promotion, free-form generation, or cognition-substrate claims.
_Avoid_: treating bounded language-surface use review as language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, action authority, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Use Preflight** — the no-operator current-revision, device, CUDA, executor-capability, use-scope, rendered-text, fragment-hash, schema, normalizer, semantic-constraint, confidence, sparsity, and drift audit before bounded language-surface use may execute. It admits only reviewed bounded language evidence and still cannot replay memory, apply plasticity, write checkpoints, promote facts/actions, generate freely, or claim cognition.
_Avoid_: treating bounded language-surface use preflight as use execution, language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, action authority, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Use Executor** — the no-operator mutation boundary that records a bounded language-surface use event after preflight. It preserves the reviewed bounded text, use scope, state-chain, schema, normalizer, semantic-constraint, confidence, sparsity, and drift evidence as traceable use evidence without replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, generating freely, or claiming cognition.
_Avoid_: treating bounded language-surface use execution as language generation, replay memory, structural plasticity, checkpoint persistence, fact/action promotion, action authority, or a full cognition substrate

**SNN Autonomous Bounded Language Surface Use Event Review** — the no-operator read-only audit that verifies a recorded bounded language-surface use event is present in the ledger, revision-adjacent, hash-linked to use preflight/review/commit evidence, bounded by schema/normalizer/semantic constraints, and stable enough to seed a future autonomous SNN language-generation design. It still does not decode text, generate freely, replay memory, apply plasticity, write checkpoints, promote facts/actions, or claim cognition.
_Avoid_: treating use-event review as language generation itself, accepting unrecorded use events, bypassing use-chain hashes, or promoting reviewed bounded text directly into facts, actions, replay, plasticity, or a cognition substrate

**SNN Autonomous SNN Language Generation Design** — the no-operator read-only plan for a bounded spike-native language-generation pass after a verified bounded language-surface use event. It binds use-event review evidence, sparse context fragments, schema/normalizer/semantic constraints, confidence, sparsity, drift, bounded generation budgets, target device, and CUDA intent into a generation plan without decoding tokens, returning generated text, mutating runtime state, training, replaying memory, applying plasticity, writing checkpoints, promoting facts/actions, or claiming cognition.
_Avoid_: treating a generation design as generated language, using LLM/Cortex/external checkpoints as the generation engine, skipping bounded spike evidence, or widening the design into free-form generation, action authority, replay, plasticity, or cognition substrate

**SNN Autonomous SNN Language Generation Preflight** — the no-operator current-revision, device/CUDA, executor-capability, generation-budget, sparse-context, schema/normalizer/semantic-constraint, confidence, sparsity, and drift audit before a bounded spike-native language generation executor may run. It proves readiness of the designed SNN generation pass without decoding tokens, returning text, mutating state, training, replaying memory, applying plasticity, writing checkpoints, using external checkpoints, promoting facts/actions, or claiming cognition.
_Avoid_: treating generation preflight as generation execution, accepting stale designs, ignoring CUDA/device evidence, loading external checkpoints, or bypassing executor capability checks

**SNN Autonomous SNN Language Generation Executor** — the no-operator mutation boundary that records a bounded spike-native language generation event as generated token hashes, spike-projection hashes, active-neuron hashes, membrane-state hashes, output-fragment hashes, device lineage, and generation-plan lineage. It is the first autonomous SNN generation evidence writer, but it does not decode tokens, return literal text, train, replay memory, apply plasticity, write checkpoints, load external checkpoints, promote facts/actions, or claim cognition.
_Avoid_: treating hash-only spike projection as readable generated language, allowing unbounded token output, hiding decoded text in evidence, or combining generation execution with replay, plasticity, fact/action authority, checkpoint writes, or external model loading

**SNN Autonomous SNN Language Generation Event Review** — the no-operator read-only audit that verifies a recorded spike-native generation event is present in the ledger, revision-adjacent, hash-linked to generation preflight/design/use evidence, and internally consistent across generated token hashes, spike-projection hashes, active-neuron hashes, membrane-state hashes, and output-fragment hashes. It may seed a later bounded decoding design, but it does not decode tokens, return readable text, mutate state, train, replay memory, apply plasticity, write checkpoints, promote facts/actions, or claim cognition.
_Avoid_: treating event review as decoded language, trusting unrecorded generation events, accepting mismatched spike/token hash counts, or using reviewed generation evidence directly as facts, actions, replay input, plasticity input, or cognition substrate

**SNN Autonomous SNN Language Decoding Design** — the no-operator read-only plan for bounded decoding/materialization from reviewed SNN generation projection evidence. It binds generated token hashes, spike-projection hashes, active-neuron hashes, membrane-state hashes, output-fragment hashes, schema/normalizer/semantic constraints, target surface, and decoding budgets into a later preflight plan without decoding tokens, returning text, mutating state, replaying, applying plasticity, writing checkpoints, promoting facts/actions, or claiming cognition.
_Avoid_: treating decoding design as decoded text, skipping generation-event review, accepting unbounded/free-form decoding, hiding literal text in design evidence, or using the design as action/fact/replay/plasticity authority

**SNN Autonomous SNN Language Decoding Preflight** — the no-operator current-revision, device/CUDA, decoder-capability, hash-lineage, schema/normalizer/semantic-constraint, and bounded-budget audit before a later autonomous SNN decoding executor may materialize language. It may declare executor readiness from machine evidence, but it does not decode tokens, return text, mutate state, replay, train, apply plasticity, write checkpoints, promote facts/actions, or claim cognition.
_Avoid_: treating preflight as human approval, decoded output, ledger mutation, replay/training/plasticity authority, or a way to bypass bounded decoding design and reviewed SNN generation evidence

**SNN Autonomous SNN Language Decoding Executor** — the no-operator mutation boundary that records bounded decoded SNN language as literal fragments plus decoded-fragment hashes, rendered-text hash, generated-token lineage, spike-projection hashes, active-neuron hashes, membrane-state hashes, output-fragment hashes, schema/normalizer/semantic validity, and device lineage. It is the first SNN-native language materialization event in the generation chain, but it does not train, replay, apply plasticity, write checkpoints, promote facts/actions, permit free-form generation, or claim cognition.
_Avoid_: treating decoded SNN language as truth/action authority, accepting unbounded text, dropping spike/token provenance, skipping schema/normalizer/semantic evidence, or coupling decoding execution to replay, plasticity, checkpoint persistence, or external model paths

**SNN Autonomous SNN Language Decoding Event Review** — the no-operator read-only audit that verifies a decoded SNN language event is recorded in the ledger, revision-adjacent, hash-linked to decoding preflight/design/generation evidence, bounded, schema-valid, normalized, semantic-valid, and internally consistent across decoded token hashes, decoded-fragment hashes, rendered-text hash, spike-projection hashes, active-neuron hashes, membrane-state hashes, and output-fragment hashes. It may seed a later readout-surface design, but it does not mutate state, replay, train, apply plasticity, write checkpoints, promote facts/actions, permit free-form generation, or claim cognition.
_Avoid_: treating reviewed decoded language as fact/action authority, accepting unrecorded decoder output, bypassing schema/normalizer/semantic validity, using review as replay/plasticity/training input, or calling it cognition by itself

**SNN Language Readout Surface Design** — the no-operator read-only plan that binds reviewed decoded SNN language to a bounded readout role, spike/membrane/output provenance, rendered-text hash, decoded-fragment hashes, schema/normalizer/semantic constraints, and bounded association budgets. It may seed a later readout-surface preflight, but it does not mutate state, replay, train, apply plasticity, write checkpoints, promote facts/actions, permit free-form generation, or claim cognition substrate status.
_Avoid_: treating a readout-surface design as a fact, action, memory consolidation event, plasticity signal, hidden thought stream, or proof of cognition; accepting unbounded internal speech; dropping spike provenance; or bypassing decoded-language event review

**SNN Language Readout Surface Preflight** — the no-operator current-revision, device/CUDA, executor-capability, hash-lineage, spike/membrane/output-provenance, rendered-text-bound, and bounded-association audit before a later readout-surface executor may record a bounded surface event. It may declare machine readiness for that executor, but it does not record the event, mutate state, replay, train, apply plasticity, write checkpoints, promote facts/actions, permit free-form generation, or claim cognition substrate status.
_Avoid_: treating readout-surface preflight as human approval, a thought event, cognition proof, replay/plasticity authority, fact/action authority, or a way to bypass reviewed decoded-language provenance

**SNN Language Readout Surface Executor** — the no-operator ledger mutation that records a bounded readout-surface event from a ready readout-surface preflight, preserving rendered text, fragment/render hashes, decoded/generated-token lineage, spike/membrane/output provenance, device lineage, readout role, binding mode, and association budgets. It records bounded readout evidence, but it does not replay, train, apply plasticity, write checkpoints, promote facts/actions, permit free-form generation, or claim cognition substrate status.
_Avoid_: treating a recorded readout-surface event as external truth, a command, long-term memory consolidation, plasticity input, proof of selfhood/cognition, a hidden language reasoning step, or permission to skip later event review

**SNN Language Readout Surface Event Review** — the no-operator read-only audit that verifies a recorded readout-surface event is ledger-present, revision-adjacent, hash-linked to readout-surface preflight/design and decoded-language evidence, bounded, spike/membrane/output-provenanced, and still barred from fact/action, replay, plasticity, checkpoint, training, and cognition-substrate claims. It may seed a later readout-memory design, but it does not consolidate memory or mutate state.
_Avoid_: treating readout-surface event review as memory consolidation, fact/action authority, replay eligibility, plasticity permission, human approval, hidden language reasoning, or proof that the system is already a cognition substrate

**SNN Language Readout Memory Design** — the no-operator read-only plan that turns a reviewed readout-surface event into a bounded memory-trace proposal with trace hash, local-learning target hashes, spike/membrane/output provenance, readout role, binding mode, and deferred consolidation route. It may seed a later readout-memory preflight, but it does not record memory, replay, train, apply plasticity, write checkpoints, promote facts/actions, resize/prune, or claim cognition substrate status.
_Avoid_: treating readout-memory design as actual memory consolidation, synapse mutation, replay scheduling, truth storage, action authority, structural growth/pruning permission, or proof of cognition

**SNN Language Readout Memory Preflight** — the no-operator current-revision, device/CUDA, executor-capability, memory-trace, local-learning-target, and spike/membrane/output-provenance audit before a later readout-memory executor may record a bounded memory trace. It may declare machine readiness for that executor, but it does not record memory, replay, train, apply plasticity, write checkpoints, promote facts/actions, resize/prune, or claim cognition substrate status.
_Avoid_: treating readout-memory preflight as memory consolidation, plasticity permission, replay execution, structural growth/pruning authority, fact/action authority, human approval, or proof of cognition

**SNN Language Readout Memory Executor** — the no-operator ledger mutation that records a bounded readout-memory trace from a ready readout-memory preflight, preserving trace hash, local-learning target hashes, spike/membrane/output provenance, readout role, binding mode, device lineage, and deferred consolidation route. It records memory-trace evidence, but it does not replay, train, apply plasticity, write checkpoints, resize/prune, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating a recorded readout-memory trace as consolidated long-term memory, synapse mutation, replay execution, structural growth/pruning, fact storage, action authority, or proof of cognition

**SNN Language Readout Memory Event Review** — the no-operator read-only audit that verifies a recorded readout-memory trace is ledger-present, revision-adjacent, hash-linked to readout-memory preflight/design and readout-surface evidence, bounded, and backed by local-learning target hashes plus spike/membrane/output provenance. It may seed a later readout-consolidation design, but it does not replay, train, apply plasticity, write checkpoints, resize/prune, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating readout-memory event review as memory consolidation, plasticity permission, replay eligibility, structural growth/pruning authority, fact/action authority, human approval, or proof of cognition

**SNN Language Readout Consolidation Design** — the no-operator read-only plan that turns a reviewed readout-memory trace into bounded candidate local updates with learning-rate, weight-delta, homeostatic-decay, normalization, and spike/membrane/local-learning-target provenance. It may seed a later consolidation preflight, but it does not replay, train, apply plasticity, write checkpoints, resize/prune, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating readout-consolidation design as an applied synapse update, replay run, structural growth/pruning, checkpoint write, truth storage, action authority, or proof of cognition

**SNN Language Readout Consolidation Preflight** — the no-operator current-revision, device/CUDA, executor-capability, candidate-update-bound, local-learning-target, and spike/membrane/output-provenance audit before a later consolidation executor may apply bounded local updates. It may declare machine readiness for that executor, but it does not replay, train, apply plasticity, write checkpoints, resize/prune, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating readout-consolidation preflight as plasticity application, replay execution, structural growth/pruning authority, checkpoint persistence, fact/action authority, human approval, or proof of cognition

**SNN Language Readout Consolidation Executor** — the no-operator ledger mutation that applies bounded local readout-consolidation updates from a ready consolidation preflight, marking candidate local updates as applied with learning-rate, weight-delta, homeostatic-decay, normalization, and spike/membrane/local-learning-target provenance. It is a plasticity event, but it does not run replay, train dense models, write checkpoints, resize/prune structure, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating readout-consolidation execution as replay, checkpoint persistence, structural growth/pruning, fact storage, action authority, dense training, or proof of cognition

**SNN Language Readout Consolidation Event Review** — the no-operator read-only audit that verifies an applied readout-consolidation plasticity event is ledger-present, revision-adjacent, hash-linked to preflight/design/memory evidence, bounded by local learning-rate/weight-delta/homeostatic-decay limits, and backed by applied local-update plus spike/membrane/output provenance. It may seed later structural-plasticity design, but it does not replay, train, write checkpoints, resize/prune, promote facts/actions, apply another update, or claim cognition substrate status.
_Avoid_: treating readout-consolidation event review as another plasticity application, replay permission, structural growth/pruning execution, checkpoint persistence, fact/action authority, human approval, or proof of cognition

**SNN Autonomous SNN Language Thought Structural Plasticity Design** — the no-operator read-only plan that turns a reviewed readout-consolidation plasticity event into bounded sparse growth and homeostatic-prune candidates with local-update, spike/membrane, device, and memory-trace provenance. It may seed a later structural-plasticity preflight, but it does not add neurons/synapses, prune synapses, resize tensors, replay, train, write checkpoints, apply plasticity, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating thought structural-plasticity design as executed neurogenesis, pruning, capacity resize, checkpoint-backed regeneration, replay permission, fact/action authority, human approval, or proof of cognition

**SNN Autonomous SNN Language Thought Structural Plasticity Preflight** — the no-operator current-revision, CUDA/device, executor-capability, sparse-growth-budget, homeostatic-prune-budget, and provenance audit before a later structural-plasticity executor may mutate topology. It may declare machine readiness for that executor, but it does not add neurons/synapses, prune synapses, resize tensors, replay, train, write checkpoints, apply plasticity, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating thought structural-plasticity preflight as executed neurogenesis, pruning, capacity resize, checkpoint-backed regeneration, replay permission, fact/action authority, human approval, or proof of cognition

**SNN Autonomous SNN Language Thought Structural Plasticity Executor** — the no-operator ledger mutation that applies bounded thought-driven sparse growth and homeostatic-prune candidates from a ready structural-plasticity preflight, marking candidate neuron/synapse growth and prune proposals as applied with consolidation, memory-trace, CUDA/device, and spike-provenance lineage. It records a structural-plasticity event, but it does not resize dense tensors, write checkpoints, replay, train, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating thought structural-plasticity execution as dense tensor resize, checkpoint-backed regeneration, replay execution, fact/action authority, dense training, or proof of cognition

**SNN Autonomous SNN Language Thought Structural Plasticity Event Review** — the no-operator read-only audit that verifies a thought structural-plasticity event is ledger-present, revision-adjacent, hash-linked to preflight/design/consolidation/memory evidence, and still within sparse growth/prune budgets. It may seed later capacity-mutation design, but it does not resize dense tensors, write checkpoints, replay, train, apply additional plasticity, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating thought structural-plasticity event review as dense tensor resize, checkpoint-backed regeneration, replay permission, additional growth/prune execution, fact/action authority, human approval, or proof of cognition

**SNN Language Readout Capacity Mutation Design** — the no-operator read-only plan that turns a reviewed readout structural-plasticity event into target neuron capacity, sparse synapse budget, dense readout relayout shape, preserved-copy window, zero-fill region, CUDA relayout requirement, checkpoint requirement, and restore-validation requirement. It may seed a later readout-capacity mutation preflight, but it does not resize tensors, add neurons/synapses, prune synapses, write checkpoints, replay, train, apply plasticity, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating readout-capacity mutation design as tensor resize execution, checkpoint-backed regeneration, applied growth/pruning, replay permission, fact/action authority, human approval, or proof of cognition

**SNN Language Readout Capacity Mutation Preflight** — the no-operator current-revision, CUDA relayout, checkpoint/restore, executor-capability, dense-shape, sparse-budget, and provenance audit before a later readout-capacity mutation executor may resize live SNN language tensors. It may declare machine readiness for that executor, but it does not resize tensors, add neurons/synapses, prune synapses, write checkpoints, replay, train, apply plasticity, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating readout-capacity mutation preflight as tensor resize execution, checkpoint-backed regeneration, applied growth/pruning, replay permission, fact/action authority, human approval, or proof of cognition

**SNN Language Readout Capacity Mutation Executor** — the no-operator checkpoint-backed runtime mutation that grows the owned SNN language tensor and sparse-edge budget from a ready readout-capacity preflight. It allocates the target tensor on the requested device, preserves the existing dense window or projects existing sparse weights, zero-initializes new neuron rows/columns, records dynamic-capacity provenance, and commits the new state through rollback-verified checkpoints. It creates usable neuron capacity but does not train or activate the new slots, add sparse synapse weights, replay memories, promote facts/actions, generate language, or claim cognition substrate status.
_Avoid_: treating zero-initialized neuron capacity as learned neurons, claiming CUDA execution when the recorded tensor device is CPU, treating sparse-budget growth as applied synapse growth, skipping checkpoint commit/restore verification, or using capacity mutation as proof of cognition

**SNN Language Readout Capacity Mutation Event Review** — the no-operator read-only audit that recomputes the readout-capacity mutation event hash and verifies the committed event against live Runtime State, dynamic neuron/sparse-edge capacity, dense tensor shape/device, zero-initialized new storage, and pre/post checkpoint evidence. A passing review may seed newborn-neuron integration design, but it does not connect, train, activate, replay, prune, generate language, promote facts/actions, or claim cognition substrate status.
_Avoid_: reviewing only the executor response without live-state comparison, treating allocated slots as integrated neurons, accepting modified event material, confusing published and staged checkpoint identities, or using review readiness as learning/plasticity authority

**SNN Language Readout Newborn Neuron Integration Design** — the no-operator read-only maturation plan for verified zero-filled neuron slots. It binds every newborn index to reviewed spike, membrane, token, and local-learning provenance; keeps the proposed weight at zero; and defines bounded co-activity, critical-period, homeostatic-rate, seed-synapse, and inactivity-pruning rules for a later preflight. It does not resolve a live source neuron, connect or activate a newborn neuron, start its critical period, apply weights or plasticity, write checkpoints, replay, train, generate language, promote facts/actions, or claim cognition substrate status.
_Avoid_: treating allocated capacity as an integrated neuron, inventing source indices from hashes, applying a nonzero seed weight during design, starting maturation without live co-activity evidence, allowing unbounded critical-period plasticity, or using integration design as proof of learning or cognition

**SNN Language Readout Newborn Neuron Integration Preflight** — the no-operator read-only machine audit that resolves one mature source neuron per newborn slot from canonical live active-index sets whose hashes match the readout lineage. It requires current revision, expanded live tensor/device agreement, bounded source firing and repeated co-activity, no existing candidate edge, no newborn-to-newborn bootstrap, a restorable checkpoint, and executor capability. It authorizes only the later checkpoint-backed integration executor; it does not add a synapse, apply weight or plasticity, activate a newborn neuron, start its critical period, replay, train, generate language, promote facts/actions, or claim cognition substrate status.
_Avoid_: trusting caller-selected source indices without hash-bound active populations, choosing another newborn as the source, accepting duplicate or unsorted active indices, connecting an already-existing edge, treating preflight readiness as applied integration, or requiring human approval for a machine-verifiable maturation step

**SNN Language Readout Newborn Neuron Integration Executor** — the no-operator checkpoint-backed mutation that creates bounded mature-to-newborn sparse edges from a ready hash-bound preflight, mirrors those low seed weights into the owned dense tensor, records exact spike/membrane/co-activity provenance, and starts a finite critical period for each integrated newborn slot. It preserves neuron capacity and fanout/row-mass budgets, commits through post-mutation restore verification, and rolls back on commit failure. It does not replay, run language training, prune, add further neurons, generate language, promote facts/actions, or claim that critical-period maturation is complete.
_Avoid_: connecting newborn neurons without mature source evidence, using full-strength initial weights, exceeding sparse/fanout/homeostatic limits, updating sparse weights without the dense owner, skipping rollback verification, treating critical-period start as mature learning, or requiring routine human approval

**SNN Language Readout Newborn Neuron Integration Event Review** — the no-operator read-only audit of a committed newborn integration. It recomputes event and per-synapse hashes, verifies revision and checkpoint publication, and requires every applied mature-to-newborn edge to agree across sparse weight state, bounded dense-tensor cell samples, integration provenance, critical-period metadata, and the runtime's last integration event before learning may begin.
_Avoid_: reviewing only the executor payload, trusting sparse state without dense-cell evidence, confusing a staged checkpoint with its published managed object, accepting altered provenance, or treating successful integration as maturation

**SNN Language Readout Newborn Neuron Critical-Period Learning Design** — the no-operator read-only developmental policy for verified newborn synapses. It defines a finite critical-period age, local pre/post timing plasticity, bounded potentiation/depression, homeostatic firing limits, minimum active-cycle survival evidence, and explicit `critical_period`, `mature`, and `prune_eligible` outcomes. It does not accept caller-authored weight deltas or apply learning.
_Avoid_: unbounded Hebbian growth, global backpropagation as the newborn learning rule, permanent enhanced plasticity, survival based only on elapsed time, or treating a design as an applied learning cycle

**SNN Language Readout Newborn Neuron Critical-Period Learning Preflight** — the no-operator read-only machine audit for one developmental learning cycle. It canonicalizes hash-bound pre/post spike times, derives causal and anti-causal pair counts and the bounded local weight proposal, checks newborn firing homeostasis and prediction-error evidence, and verifies current sparse/dense/provenance state, Runtime State revision, device/CUDA placement, rollback checkpoint, and executor capability. It advances no age and changes no weight.
_Avoid_: accepting raw caller-selected deltas, unsorted or duplicate spike times, learning against stale weights, applying a homeostatically unsafe update, aging a neuron during preflight, or requiring human confirmation for machine-verifiable local evidence

**SNN Language Readout Newborn Neuron Critical-Period Learning Executor** — the no-operator checkpoint-backed mutation for one verified developmental cycle. It applies the preflight-derived bounded local delta to both sparse and dense owned weights, advances persistent critical-period age and active/inactive cycle counts, extends exact synapse provenance, and records whether the synapse remains in its critical period or has reached a `mature` or `prune_eligible` decision. It does not remove prune-eligible structure; pruning remains a separate mutation.
_Avoid_: recomputing a different delta during execution, updating only one weight representation, losing developmental age across checkpoints, declaring maturity before the finite period and survival evidence are complete, immediately deleting prune-eligible synapses, or requiring routine human approval

**SNN Language Readout Newborn Neuron Critical-Period Learning Event Review** — the no-operator read-only audit of one committed developmental cycle. It recomputes event and application hashes and requires the applied weight, age, active/inactive counts, maturation state, sparse state, dense cell, developmental state, synapse provenance, Runtime State revision, and committed checkpoint to agree. It may promote an open critical period to another cycle design or a terminal state to maturation-outcome review, but it applies neither.
_Avoid_: trusting the executor response without live-state comparison, resetting age after review, treating `prune_eligible` as already pruned, continuing a terminal synapse, or treating review readiness as language/cognition authority

**SNN Language Readout Newborn Neuron Critical-Period Learning Continuation Design** — the no-operator read-only reconstruction of the next developmental cycle from an accepted learning-event review. It carries forward the reviewed current weight, developmental age, remaining cycles, accumulated activity, bounded learning/homeostatic policy, and parent application hash. It emits the normal Critical-Period Learning Design contract so the existing preflight and executor can repeat without returning to the original seed state.
_Avoid_: rebuilding from the integration seed after learning has started, resetting activity counters, changing policy without reviewed evidence, continuing mature or prune-eligible synapses, or bypassing the normal preflight

**SNN Readout Emission Review History Evidence** — the compact Runtime Truth visibility summary for reviewed bounded SNN language emissions. It reports review counts, unique emission/trajectory/transition-memory counts, latest hashes, and the next operator-inspection gate without exposing raw text/labels or becoming a ledger writer, replay source, plasticity signal, fact/action authority, checkpoint command, or cognition-substrate claim.
_Avoid_: exposing display text inside status, treating a reviewed emission as replay memory, applying plasticity from review history, or using compact Runtime Truth evidence as a substitute for the dedicated emission-review ledger

**SNN Language Evaluated Prediction Provenance** — the canonical-hash binding between a sparse next-code prediction, the training window, current readout slots, persistent transition-memory weights, and the transition-memory evaluation window that tested it. A readout draft may be review-ready only when its prediction hash appears in the evaluation's memory-backed prediction hashes and the training/current/memory hashes match.
_Avoid_: replaying a passing evaluation for an unrelated prediction, accepting hand-authored summary booleans, or treating provenance as optional metadata

**SNN Language Readout Evidence Ledger** — the MARULHO-owned append-only memory of operator-confirmed, provenance-matched bounded readout drafts. It records prediction, evaluation, transition-memory, label, revision, and operator evidence for later replay/evaluation without treating text labels as thoughts, facts, actions, or a cognition substrate.
_Avoid_: hidden read-side ledger mutation, recording unready drafts, storing free-form generated text as memory, or using ledger presence as autonomous truth

**SNN Language Readout Replay Priority** — the read-only advisory ranking over SNN Language Readout Evidence Ledger entries. It prioritizes provenance-bound readout evidence for possible isolated SNN rehearsal review using deterministic recency, repetition, and transition-memory reuse signals; it cannot execute replay, apply plasticity, generate language, promote facts/actions, or become the cognition substrate.
_Avoid_: treating priority as replay execution, synthesizing new text from labels, training from priority alone, or using priority scores as truth

**SNN Language Readout Rehearsal Evaluation** — the isolated read-only evaluator after SNN Language Readout Replay Priority. It turns prioritized ledger candidates into sparse rehearsal vectors, measures activation sparsity, similarity, priority support, and device placement, and can mark evidence ready for operator rehearsal review without mutating runtime state, applying plasticity, training, or generating language.
_Avoid_: treating rehearsal evaluation as live replay, using sparse vectors as thoughts, applying weights from rehearsal, or promoting labels as facts/actions

**SNN Language Readout Rehearsal Experiment** — the isolated replay-pressure simulation after SNN Language Readout Rehearsal Evaluation. It estimates whether replay cycles over sparse readout evidence would be non-worsening and stable before any future replay design, while discarding traces, persisting no weights, and keeping live replay, plasticity, language generation, facts, actions, and cognition-substrate promotion disabled.
_Avoid_: treating simulated pressure gain as applied learning, running live replay from the experiment, or accepting rehearsal evidence as autonomous truth

**SNN Language Readout Replay Design** — the read-only operator-review design after SNN Language Readout Rehearsal Experiment. It selects hash-addressed readout evidence targets and bounds future isolated replay by candidate count, replay cycles, pressure-gain threshold, stability floor, and rollback evidence; it still cannot execute replay, apply plasticity, train, generate language, or promote facts/actions.
_Avoid_: treating replay design as replay execution, widening targets beyond recorded evidence, bypassing rollback policy, or allowing design artifacts to mutate transition memory

**SNN Language Readout Replay Dry Run** — the operator-approved isolated replay simulation after SNN Language Readout Replay Design. It replays only internal-ledger-backed, hash-addressed readout targets as ephemeral sparse tensors with explicit device/CUDA evidence, and reports pressure/stability metrics without writing checkpoints, mutating runtime state, applying plasticity, decoding/generating language, or promoting facts/actions.
_Avoid_: treating dry-run pressure gain as live learning, silently falling back from requested CUDA, accepting caller-only evidence not present in the internal ledger, or returning trained weights

**SNN Language Readout Plasticity Preflight** — the read-only bridge after SNN Language Readout Replay Dry Run. It checks whether dry-run sparse replay evidence is stable enough to review as local-plasticity intent and emits bounded candidate replay sequences for later application design; it still cannot apply weights, persist checkpoints, train, generate language, or promote facts/actions.
_Avoid_: treating preflight as an application design, using caller-only dry-run evidence, applying candidate synapses, or skipping the existing plasticity application/shadow/live gates

**SNN Language Readout Plasticity Replay Bridge** — the read-only adapter after SNN Language Readout Plasticity Preflight. It converts internal-ledger-backed readout preflight evidence into the existing `snn_language_plasticity_replay_experiment.v1` contract and carries canonical replay sequences, an application-design-compatible block, a MARULHO sparse-transition target hint, and checkpoint-transaction requirements for the established shadow/readiness/preflight path; it cannot apply plasticity, mutate runtime state, persist checkpoints, generate language, or promote facts/actions.
_Avoid_: caller-side reshaping that drops provenance, spoofing canonical replay experiments, recomputing preflight bounds in the bridge, bypassing checkpoint/restore readiness, or treating bridge compatibility as live update permission

**Readout-Derived Live Plasticity Application** — the checkpoint-backed command path where SNN Language Readout Plasticity Replay Bridge evidence may finally update Persistent SNN Language Transition Memory through the existing SNN Language Plasticity Live Application Executor. It requires shadow-delta evidence, live readiness, live preflight, current Runtime State revision, explicit operator confirmation, and a restorable checkpoint; it still cannot generate or decode language, load external checkpoints, or promote facts/actions.
_Avoid_: adding a second readout-specific executor, applying without confirmation, skipping checkpoint restore evidence, or treating bounded readout labels as autonomous truth

**Readout-Derived Synapse Provenance** — the per-synapse audit trail attached to readout-derived live plasticity. It preserves the readout evidence hash, prediction hash, transition-memory evaluation hash, persistent transition-weight hash, source sequence, and source sparse indices on both shadow deltas and applied synapses, and persists a `synapse_provenance_by_key` map across checkpoints for restored-runtime audit.
_Avoid_: storing only numeric weights after readout-derived mutation, dropping provenance at the shadow-delta boundary, or making checkpoint restore unable to explain why a sparse transition exists

**SNN Language Readout Synapse Provenance Audit** — the read-only audit surface over readout-derived live plasticity. It compares persisted sparse transition weights and `synapse_provenance_by_key` against exact internal Readout Evidence Ledger entries, including canonical 64-neuron synapse keys, in-range source indices, finite bounded weights, recomputed ledger material hashes, matching prediction, transition-evaluation, persistent-weight hashes, and replay-regeneration local-edge provenance when a synapse was grown from rollout consolidation. It reports orphan weights, incomplete provenance, missing local rollout edge evidence, or invalid rollout step order, and can mark restored runtime state ready for operator audit review without applying plasticity, generating language, or promoting facts/actions.
_Avoid_: using audit success as permission to mutate, accepting weights without ledger-backed provenance, dropping rollout local-edge hashes from replay-regenerated synapses, treating hash-field mismatches or tampered ledger material as harmless, accepting malformed sparse keys or unbounded weights, or letting row-return limits shrink validation scope

**SNN Language Transition Memory Homeostasis** — the checkpoint-backed maintenance command for Persistent SNN Language Transition Memory. It decays transition weights, bounds outgoing row mass, prunes weak synapses, and records maintenance evidence through `/terminus/snn-language-sequence/plasticity-homeostatic-maintenance`; it requires explicit operator confirmation and current revision evidence.
_Avoid_: unbounded transition growth, hidden pruning during prediction reads, or maintenance without a rollback checkpoint

**Evaluated SNN Transition-Memory Replay Context** — the Replay Controller-owned, server-held, current-revision evidence record binding MARULHO-computed sequence mismatch and plasticity-pressure reports for replay review. It is recorded from source inputs, not caller-carried reports, and artifacts or permits that depend on it must verify its server-held identity and content hash at the current Runtime State revision.
_Avoid_: accepting caller-authored mismatch or pressure payloads as permit evidence, reusing stale contexts after revision advance, or treating context recording as mutation authority

**SNN Replay Consolidation Priority Queue** — the read-only advisory ranking of current Evaluated SNN Transition-Memory Replay Contexts for operator consolidation review. It scores only verified server-held contexts using bounded prediction-error, plasticity-pressure, recency, and internal readout-support evidence; it may recommend which context to review next but cannot record artifacts, replay live activity, mutate transition memory, generate language, promote facts, or issue permits.
_Avoid_: treating queue rank as replay execution, artifact recording, structural-write permission, fact/action promotion, or evidence from stale/tampered contexts

**SNN Replay Artifact Recording Policy Proposal** — the read-only autonomy policy surface that selects a policy-ready SNN Replay Consolidation Priority Queue candidate for operator artifact-recording review. It applies bounded policy thresholds and verifies the recommended replay context is still current, but it cannot record an artifact, run replay, mutate transition memory, generate language, promote facts/actions, or issue permits.
_Avoid_: treating a recommended policy proposal as consent, artifact evidence, live replay execution, structural mutation, or a substitute for operator review

**SNN Replay Artifact Recording Review Ticket** — the Replay Controller-owned durable intent tag that binds a current SNN Replay Artifact Recording Policy Proposal, its Evaluated SNN Transition-Memory Replay Context, the current Runtime State revision, and the confirming operator before evaluated replay-artifact recording. When issued from a due sleep-review cycle it also binds the Due-Cycle Replay Artifact Recording Review Proposal and its source selection hash. It records consent to review the selected consolidation target, but it is not an artifact, replay execution, mutation authority, generation permission, or regeneration permit.
_Avoid_: accepting caller-copied policy proposals as consent, reusing stale tickets after revision advance, sharing tickets across operators, or treating ticket creation as artifact recording

**Evaluated SNN Transition-Memory Replay Artifact Proposal** — the read-only policy-derived consolidation proposal built from ranked internal Readout Evidence Ledger entries and a verified Evaluated SNN Transition-Memory Replay Context. It preserves per-label grounding and may propose an operator-recordable replay window only when every selected label is grounded, high mismatch is present, and plasticity pressure is ready for review.
_Avoid_: accepting caller-round-tripped replay windows as evaluated evidence, collapsing partial grounding into a single optimistic flag, or treating a proposal as mutation authority

**SNN Transition-Memory Replay Artifact** — the Replay Controller-owned durable consolidation context for structural review. A regeneration-permit-eligible artifact records an operator-confirmed, internal-ledger-backed Evaluated SNN Transition-Memory Replay Artifact Proposal together with a verified SNN Replay Artifact Recording Review Ticket, a verified Evaluated SNN Transition-Memory Replay Context, normalized operator identity, current Runtime State revision, bounded known-readout source-window hash, bounded replay-priority source-window hash, and bounded provenance source-window hash. Raw caller-window artifact recording is retired and dropped on load.
_Avoid_: treating general replay audit samples as SNN consolidation evidence, accepting caller-authored artifact IDs, issuing regeneration permits from raw replay windows, preserving raw caller-window artifact records, or treating artifact recording as structural-write authority

**Replay-Window Synapse Provenance** — the hash-addressed ancestry carried from internal Readout Evidence Ledger entries through an evaluated replay artifact, regeneration permit, regeneration proposal, and regenerated sparse transition synapse. It lets later audits explain which grounded readout evidence justified a grown synapse without loading external SNN checkpoints or treating replay labels as generated language.
_Avoid_: growing transition-memory synapses from replay-window hashes alone, dropping readout evidence hashes at the permit boundary, or mixing caller-window artifacts with internal-ledger-backed regeneration

**SNN Language Transition Memory Sleep Policy** — retired wording for **SNN Transition Memory Sleep Plasticity Policy**. Use the canonical term so sleep pressure, replay evidence, rollout-regeneration evidence, and homeostatic maintenance review stay under one policy concept.
_Avoid_: creating a second sleep-policy surface, treating the alias as a compatibility API, or reviving retired Cortex sleep snapshots as inputs

**SNN Language Sequence Mismatch Probe** — the read-only prediction-error surface after SNN Language Sequence Prediction Probe, available through `/terminus/snn-language-sequence/mismatch`. It compares predicted sparse population-code indices with the next observed grounded sparse code and reports precision, recall, mismatch score, and sparse-code deltas without applying learning or generating text.
_Avoid_: treating mismatch as an automatic learning signal, fact promotion, generated thought, or runtime model update

**SNN Language Plasticity Pressure Gate** — the read-only gate after SNN Language Sequence Mismatch Probe, available through `/terminus/snn-language-sequence/plasticity-pressure`. It converts sparse prediction error into operator-reviewable local plasticity pressure and candidate update focus, but it cannot apply plasticity, train runtime weights, generate language, or promote facts.
_Avoid_: treating pressure as permission to learn, mutating sequence weights from status reads, or using mismatch pressure as text generation evidence

**SNN Language Plasticity Trial** — an isolated simulation after SNN Language Plasticity Pressure Gate, available through `/terminus/snn-language-sequence/plasticity-trial`. It estimates whether an error-modulated local sequence update would reduce sparse prediction pressure using ephemeral update evidence only; it cannot apply plasticity, persist weights, train runtime state, generate language, or promote facts.
_Avoid_: treating simulated pressure reduction as a live weight update, storing trial weights, or bypassing isolated replay/operator approval

**SNN Language Plasticity Replay Evaluation** — the read-only gate after SNN Language Plasticity Trial, available through `/terminus/snn-language-sequence/plasticity-replay-evaluation`. It checks replay-window evidence, expected pressure reduction, Runtime Truth delta, and rollback policy before a future isolated replay experiment can be reviewed; it cannot apply plasticity, persist weights, train runtime state, generate language, or promote facts.
_Avoid_: treating replay evaluation as replay execution, applying trial updates, or using replay readiness as fact/action promotion

**SNN Language Plasticity Replay Experiment** — the isolated sparse-code rehearsal after SNN Language Plasticity Replay Evaluation, available through `/terminus/snn-language-sequence/plasticity-replay-experiment`. It measures grounded replay coverage and simulated pressure stability from replay sequences while discarding replay traces and refusing runtime weight updates.
_Avoid_: treating replay rehearsal as live learning, storing replay weights, promoting replay outputs as facts, or using it as text generation

**SNN Language Plasticity Application Design** — the read-only design gate after SNN Language Plasticity Replay Experiment, available through `/terminus/snn-language-sequence/plasticity-application-design`. It bounds a possible future local update with learning-rate, weight-delta, locality, normalization, device evidence, Runtime Truth, and rollback constraints, but it still cannot apply plasticity or persist weights.
_Avoid_: treating an application design as a live update, bypassing rollback evidence, widening updates beyond local sparse support, or promoting replay evidence as language/facts

**SNN Language Plasticity Shadow Application** — the read-only verifier after SNN Language Plasticity Application Design, available through `/terminus/snn-language-sequence/plasticity-shadow-application`. It checks a proposed shadow update against weight-delta, locality, pressure-stability, device evidence, Runtime Truth, and rollback bounds before any future live application can be reviewed.
_Avoid_: treating shadow deltas as applied weights, skipping rollback, using shadow verification as fact promotion, or calling it runtime learning

**SNN Language Plasticity Shadow Delta** — the MARULHO-owned read-only measurement that derives a bounded local weight-delta candidate from sparse replay indices and application-design limits, available through `/terminus/snn-language-sequence/plasticity-shadow-delta`. It can report affected synapse count, maximum absolute delta, locality radius, pressure change, and device placement, but it cannot apply or persist the update.
_Avoid_: hand-written shadow deltas as proof of live readiness, treating measured deltas as stored weights, or skipping the Shadow Application verifier

**SNN Language Plasticity Live Application Readiness** — the final read-only gate before any checkpoint-backed live language plasticity application. It checks Shadow Application evidence, checkpoint/restore rollback readiness, and explicit operator approval through `/terminus/snn-language-sequence/plasticity-live-application-readiness`; it cannot apply weights, mutate runtime state, train, generate, or promote facts.
_Avoid_: treating readiness as application, bypassing checkpoint restore, or using operator approval without measured shadow evidence

**SNN Language Plasticity Live Application Preflight** — the read-only pre-execution review after Live Application Readiness. It checks that the target is a MARULHO-owned sparse mutable transition-weight surface and that a checkpoint transaction is saved, restorable, and records the shadow delta before `/terminus/snn-language-sequence/plasticity-live-application` may be called.
_Avoid_: treating preflight as mutation, accepting non-MARULHO targets, or applying a delta without a saved rollback transaction

**SNN Language Plasticity Live Application Executor** — the command-shaped, checkpoint-backed mutation boundary for bounded local SNN language transition updates. It applies measured shadow deltas only to MARULHO-owned sparse transition weights, requires current state revision, explicit confirmation, operator approval, real checkpoint save, and bounded non-worsening pressure evidence; it does not generate text, decode text, load external checkpoints, or promote facts.
_Avoid_: placing live mutation in read models, accepting spoofed readiness without revalidation, or applying broad dense updates outside sparse local support

**SNN Language Plasticity Path Evidence** — the compact Runtime Truth evidence summary for the SNN language plasticity chain. It lists the current gates through SNN Language Plasticity Live Application Preflight, reports checkpoint/restore rollback readiness, and records the invariant that future live application requires device evidence, Runtime Truth delta, and rollback evidence while generation, training, plasticity application, and runtime mutation remain false.
_Avoid_: treating the Runtime Truth summary as execution evidence, hiding detailed gate artifacts, or promoting live learning from summary presence alone

**Developmental Plasticity** — the Subcortex mechanism family for growing, pruning, and stabilizing assemblies, synapses, routing prototypes, and replay policies under evidence gates.
_Avoid_: self-replication as unchecked code mutation, permanent growth without pruning

**Local Plasticity Evidence** — the read-only report from local STDP eligibility traces, synaptic scaling, inhibitory balance, spike-health state, synaptic validation, and device placement. It can support Developmental Plasticity readiness, but it is not a command to change synapses or topology.
_Avoid_: treating plasticity telemetry as automatic self-modification

**Structural Mutation Ledger** — the runtime evidence record for bounded topology growth/pruning events such as hypercube binding hub outreach. It records added/removed sparse edges and recent mutation samples so structural plasticity can be audited and rolled into Runtime Truth rather than treated as hidden model drift.
_Avoid_: undocumented rewiring, unbounded topology mutation, growth claims without prune evidence

**Structural Plasticity Gate Artifact** — the operator-facing read-only artifact that combines ConceptStore growth pressure, binding topology mutation ledger evidence, and CUDA/device placement into structural-promotion readiness. It can mark a structural change ready for isolated evaluation, but it cannot mutate topology.

**Structural Plasticity Runtime Truth Evidence** — the compact Runtime Truth visibility summary for the Structural Plasticity Gate Artifact. It exposes promotion status, review readiness, local-plasticity rule/backend, synaptic-validation availability, spike-health risk, device-evidence counts, and required isolated-evaluation/rollback/Runtime Truth/device gates while omitting structural cases, recent topology events, active growth concepts, full device payloads, and mutation commands.
_Avoid_: using status as the structural-plasticity artifact, hiding rollback/runtime-truth requirements, or promoting growth/prune from compact visibility

**Isolated Structural Plasticity Evaluation** — a read-only comparison of pre/post structural snapshots after a bounded growth/prune trial outside the live runtime, available through `/terminus/subcortical-structural-plasticity/evaluate`. It reports edge deltas, spike-health delta, Runtime Truth delta, CUDA/device consistency, and rollback evidence; even when ready for operator review, it still cannot authorize structural mutation by itself.

**Checkpointed Candidate Structural Evaluation** — the stricter gate inside Isolated Structural Plasticity Evaluation that binds a structural-review ticket to an exact baseline hash, candidate reason, cost/usefulness metrics, latency/RAM/VRAM impact, Runtime Truth summary, rollback artifact, and no-mutation proof. A repeated-failure or prune/sleep candidate may become ready for reviewed transaction only when this gate is ready. It does not run growth/prune code, write checkpoints, or let service choose candidates; it only proves the slow-path evaluation is bound to training-owned candidate evidence.
_Avoid_: calling concept observation, binding, grow/prune, or structural refresh from a readiness artifact

**Structural Evaluation Snapshot Binding** — the hash-bound identity evidence for an Isolated Structural Plasticity Evaluation. It records canonical pre/post snapshot hashes, optional state revisions, revision-order validity, whether a nonzero structural delta exists, and whether rollback evidence is bound to the exact pre-snapshot hash, while omitting raw snapshots from compact promotion evidence.
_Avoid_: reviewing unbound snapshots, treating identical snapshots as growth/prune evidence, accepting rollback evidence for a different pre-state, or exposing raw structural state in compact status

**Structural Mutation Design** — the operator-confirmed read-only design after Isolated Structural Plasticity Evaluation. It binds the evaluation, pre/post snapshot hashes, rollback pre-snapshot hash, binding-hub target, reason, bounded edge delta, and operator confirmation into a design hash for later preflight review; it does not call growth/prune code, write checkpoints, or mutate topology.
_Avoid_: treating design readiness as mutation authority, accepting unbound evaluation evidence, or skipping the checkpoint-backed preflight gate

**Structural Mutation Preflight** — the read-only checkpoint gate after Structural Mutation Design. It recomputes the design hash and binds the binding-hub target, mutation method, reason, edge budget, checkpointed candidate gate status, candidate evidence hash, candidate baseline hash, candidate reason, cost-impact summary hash, expected runtime revision, and rollback checkpoint path into a preflight hash. It still blocks direct structural mutation until the Structural Mutation Application executor receives explicit operator confirmation.

**Structural Candidate Tombstone** — the executor-owned rejection/retirement manifest for a reviewed growth/prune candidate that does not become a committed structural mutation. It binds candidate provenance, preflight hash recomputation, required-evidence hash, rollback artifact hash, checkpoint path, reason, and disposition such as `blocked_before_mutation`, `rejected_with_rollback`, or `retired_after_rollback`. It is provenance evidence, not a runtime mutation.
_Avoid_: treating preflight readiness as consent, writing checkpoints during preflight, or calling growth/prune before the application executor

**Structural Mutation Application** — the checkpoint-backed command path where a reviewed Structural Mutation Preflight may call `HypercubeBindingLayer.refresh_hub_topology`. It recomputes the preflight hash, requires current Runtime State revision, explicit operator confirmation, a restorable pre-mutation checkpoint, an observed nonzero binding edge delta within the approved budget, and a verified committed checkpoint. A no-op, over-budget delta, tampered target/reason, or failed commit restores the exact binding state and Runtime State revision.
_Avoid_: claiming success on no-op refresh, applying ConceptStore capacity changes from binding-topology evidence, accepting an over-budget edge delta, or creating a second unverified structural write path

**Path Retirement Gate** — the rule that a runtime path should be removed when it adds complexity without improving liveness, grounding, efficiency, or evidence quality. Legacy paths may exist only as short-lived migration scaffolding while replacement evidence is gathered; after the active Subcortex path exists, compatibility code is deleted rather than kept dormant. Negative regression tests and retirement notes may remain as boundary evidence, but runtime aliases, mocks, dormant compatibility APIs, and old extension points should not.

**Compatibility Is Not Permanence** — migration adapters are acceptable only while callers are actively moving to the real owner. Once the owner module exists and tests can target it directly, the old import path, wrapper, alias, or mock is deleted and covered by absence guards.
_Avoid_: preserving retired modules for historical users, keeping old names as convenience namespaces, or testing behavior through compatibility wrappers

**Terminus** — the whole Subcortex-centered architecture: the predictive spiking substrate plus the service/runtime surfaces that keep it observable, gated, and auditable.

**Living Brain** — an evidence-gated target state where the runtime continuously senses, learns, thinks, replays, acts, sleeps, and reports liveness without bypassing safety boundaries.
_Avoid_: using "living brain" as an unconditional production claim

**CUDA-first Runtime** — the runtime posture that uses CUDA/GPU execution for tensor-heavy subcortical work when available while keeping ordinary unit tests deterministic on CPU. Checkpoints and caches may serialize archival tensors on CPU, but active restore must load toward the selected runtime device rather than preserving a CPU-first path.
_Avoid_: GPU-only correctness, hidden CPU fallback in benchmark claims

**Routing Index** — the subcortical retrieval path that maps queries to candidate assemblies/prototypes. CUDA evidence requires actual cache/backend device telemetry, not only configured device intent.

**ThoughtLoop** — external LLM cognition orchestrator name. It is not part of the MARULHO brain runtime, service contract, checkpoint contract, or package extension surface.

**DriveSystem** — converts predictive error, surprise, fatigue, and novelty into cognitive pressure and thalamic context.

**ThalamicGate** — assembles budgeted language/readout context packets from memory, drives, and source evidence.

**Cognitive Signal** — the typed Subcortex control packet carrying prediction error, predictive confidence, neuromodulator mirrors, recent concepts, source, and sample time. It is owned by Subcortex/semantics code, not the external-LLM boundary.

**WorkingMemory** — chain-local global workspace. Active scratchpad with strength-based decay and broadcast compression.

**EpisodicMemory** — provenance-aware hippocampal memory with embedding-based retrieval, capacity-bounded eviction, and importance scoring.

**NarrativeSelf** — cross-session autobiographical continuity. Tracks interests, questions, and surprise over time.

**Predictive Columns** — SNN columns that predict their input. Prediction error drives surprise, learning, and curiosity.

**Candidate Predictive Transition** — the retained CPU scheduler path that applies prediction-error, location/velocity, prediction-weight, and lazy cached-state materialization for one bounded awake mask in `PredictiveColumnState`. It is core/training-owned and preserves the same state as the older split prediction-error/location/update sequence. CUDA keeps the dense predictive transition when measured sparse indexing is launch-bound; do not call this a sparse-CUDA promotion.

**Neuron Dynamics** — executable spiking neuron state such as AdEx membrane voltage, adaptation, and spike timing. CUDA evidence requires live tensor device reports and checkpoint restore back onto the selected runtime device.

**Replay** — hippocampal-style replay of past experiences for consolidation. Strictly evidence-only in the current runtime: no training, memory mutation, fact promotion, action execution, or sleep side effects from replay artifacts.

**Encoder** — transforms raw input (text, audio, visual) into sparse spike patterns for the SNN. Includes RTFEncoder, SemanticEncoder, EventCameraEncoder, CochleagramEncoder. Tensor-backed encoder state follows the configured runtime device; parsing windows, string segmentation, and archival metadata remain CPU/control-plane work.

**Text Encoder** — the RTFEncoder or SemanticEncoder path for character/text input. CUDA evidence requires device reports for learned chunking codebooks, semantic bucket embeddings, adapter tensors, emitted feature vectors, and spike traces; it does not require moving Python string parsing to CUDA. Live source ingestion is inference-only and bounded-batched where proven; chunk-codebook plasticity belongs to explicit training/bootstrap paths.

**Sensory Encoder** — a CUDA-first Encoder for real sensory streams whose episode metadata records the tensor device, encoder state, and last emitted spike tensor shape/device used to produce visual or audio spikes.

**Multimodal Stream Loader** — the source adapter that feeds aligned text, visual frames, and audio chunks into sensory encoding. Generated and file-backed tensor modalities follow the resolved runtime device; CPU remains valid only when explicitly selected or when CUDA is unavailable, not as a hidden default.

**Assembly** — a stable co-activated neuron group representing a learned concept. Decoded during query/response.

**Memory Store** — CPU archival replay ledger for assemblies, input patterns, routing keys, raw windows, texts, tags, and PRP/consolidation metadata. CUDA-first applies when replay tensors are consumed by the model, not when evidence records are stored.

**Concept Store** — CRUD store for grounded concepts with match scoring, label generation, and expansion/contraction.

**Gap Planner** — identifies knowledge gaps from frontier analysis and produces query plans for source acquisition.

**Curiosity Controller** — geometric-curiosity-driven detection of concept gaps and synthesis of exploration queries.

**Evidence Responder** — hallucination-guarded response builder with source attribution and candidate scoring.

**Living Loop** — the retired Terminus-era autonomous runtime cycle: tick → train → think → replay → act → sleep → repeat. Its useful record, policy, and self-model primitives remain as owner modules, but it is not the main runtime owner. Active source ticks, generation, replay, growth/prune, checkpoint orchestration, and lifecycle control belong to `MarulhoBrain`.
- **Living Loop Helpers** (`living_loop_helpers.py`) — shared cross-layer private helper functions (Layer 0 / Foundation). No dependency on any other Living Loop module.
- **Runtime Records** (`living_loop_records.py`) — enums and frozen dataclasses for the Living Loop runtime record types (Layer A). Maps to **Living Loop records** in domain vocabulary. Depends on Helpers only.
- **Policy Scoring** (`living_loop_policy.py`) — PolicyScore, WorldModelLiteSummary, PolicyActuatorRecommendation, and policy actuator decision logic (Layer B). Maps to **Policy Actuator** in domain vocabulary. Depends on Helpers and Records only.
- **Operational Self-Model** (`living_loop_self_model.py`) — OperationalSelfModel, build_runtime_benchmark_telemetry, and telemetry helpers (Layer C). Maps to **Runtime Truth / Living Loop self-model** in domain vocabulary. Depends on Helpers, Records, and Policy only.
- The original `living_loop.py` compatibility shim is deleted. Active code imports directly from the owning Living Loop depth module instead of using an aggregator namespace.

**Deleted Legacy Service Surfaces** — the retired Terminus service manager, runtime facade, status read model, runtime-control file, and service BrainRuntime file are removed from the active tree. The maintained runtime owner is `MarulhoBrain`; the maintained HTTP adapter is `MarulhoBrainServiceManager`.
_Avoid_: restoring compatibility shells for removed service/status/schema/readiness surfaces or routing source ticks, readout, replay, growth/prune, checkpoint orchestration, or lifecycle control through service-owned files.

**Brain Runtime Facade** — `MarulhoBrainRuntimeFacade` in `service.brain_manager` is the only maintained runtime facade. It is a small HTTP/UI adapter over `MarulhoBrain`; active FastAPI routes call `/brain/*` through it.
_Avoid_: reintroducing the deleted operator-facing legacy facade as a second runtime surface.

**Runtime Sources** — the owner of text/sensory stream construction, live-remote wrapping, runtime cache paths, stream readiness reads, and stream shutdown. Brain Runtime, Runtime Prewarmer, Sensory Runtime, and tests must patch or call Runtime Sources directly instead of using Service Manager stream-builder wrappers.

**Interaction Store** — the Interaction Pipeline-owned record of recent query gaps and runtime episode traces. Persistence, feedback, replay evidence, and tests read or mutate this store through Interaction Pipeline, not through Service Manager convenience methods.

**Owner Callback** — a constructor-injected callback that points directly at the module that owns the behavior or state, such as Delayed Consequence Tracker, Source Focus Scorer, Runtime Evidence Reporter, or Autonomy Planner. It must not be implemented as a Service Manager wrapper when the owner is already available in the composition root.
_Avoid_: manager-private callback wrappers, owner behavior hidden behind `self._...` manager methods

**Operator Interaction Runtime** — the domain-named service module for operator acquisition and interaction callbacks that do not belong on the Service Manager. It supplies query/feed/respond collaborator functions to Interaction Pipeline and owns the public acquisition flow for owner-module callers.
_Avoid_: resurrecting `InteractionRuntimeMixin`, manager-private interaction wrappers, or compatibility imports as active runtime surfaces

**Action Executor** — the domain-named owner for digital action execution, action history, action feedback routing, action-assist reuse, and Subcortex Action Ledger summaries. Delayed-consequence and interaction callbacks wire to it directly. The old `action_runtime.py` mixin module, standalone action-assist mixin module, and manager-private action delegate wrappers are deleted.
_Avoid_: action behavior in `ActionRuntimeMixin`, standalone action-assist mixin modules, manager-private action wrappers, retired-loop mirroring, or manager-owned action history

- **Runtime State** — owns the shared mutation flag (`dirty_state`), revision counter (`state_revision`), brain event history, and the externally visible `last_event` / `recent_events` payloads. Every other deep module receives it as a dependency.
- **Delayed Consequence Tracker** — long-horizon utility learning for sources and providers. Owns consequence records and the cooling/compaction/splitting/remerging state machines.
- **Autonomy Planner** — gap-based focus planning, provider curriculum prioritization, autonomy candidate selection, and query family scoring. Uses an explicit dependency adapter rather than manager-bound owner forwarding.
- **Brain Runtime** — source rebuilding, tick collection/training, grounded source observation injection, autonomy scheduling. Owns source runtimes, source utility, tick counters, stream epochs, and sensory episode/preview counters.
- **Interaction Pipeline** — query/feed/respond/acquire behaviour and the evidence capture that turns live interactions into runtime traces. Owns query gap history.
- **Feedback Applier** — verdict state machine, feedback normalization, and feedback application with provenance tracking.
- **Source Focus Scorer** — multi-factor selection scoring, semantic match scoring, utility EMA updates, and evidence weight mapping.
- **Runtime Controller** — runtime lifecycle state machine (configure/start/stop/tick), brain loop orchestration, active execution counters, thread lifecycle, and prewarm management. Uses an explicit dependency adapter rather than manager-bound owner forwarding.
- **Status Read Model** — read-only projection of all runtime state into status/telemetry/terminus snapshots, including direct sensory preview projection.
- **Action Executor** — digital action execution with path sandboxing, outcome calibration scoring, action history, and action-assist query augmentation.
- **Runtime Persistence** — checkpoint save/restore and trace persistence. Runtime State owns the brain event history. Uses an explicit dependency object instead of owner-forwarded manager fields.
- **Runtime Config** — input validation and normalization gate for all operator configs. Stateless.
- **Runtime Sources** — stream construction, cache I/O, serialization, window reconstruction.
- **Replay Controller** — bounded SNN replay artifact, regeneration permit, sleep-plasticity review ticket, scheduler ticket, and transition-memory replay-artifact owner. It marks controller-local writes as dirty-without-revision through Runtime State while preserving the shared `state_revision` counter as the durable command/checkpoint boundary. It no longer owns service replay-plan/sample/dataset packaging surfaces.

**Autonomy Ladder** — levels 0–5 of measured autonomy: observe → propose → execute approved → recurring constrained → evaluated policy → bounded self-improvement.

**Replay Pipeline** — the staged evidence-to-learning pipeline: gate → approval → plan → isolated experiment → promotion gate. Each stage produces a hash-verified, schema-versioned artifact.

**Runtime Truth** — the liveness classification system: alive / degraded / dead / partial / failed, with evidence, safety flags, and recommended operator action.

**Runtime Evidence Report** — operator-facing status evidence that joins model CUDA scope, trainer-owned encoder device reports, memory-store placement, runtime truth, and source configuration. It is read-only and must not advance runtime state.
_Avoid_: using retired LLM-path snapshots as the evidence source of record

**Runtime Scope Projection Cache** — a bounded read-model cache for expensive Runtime Scope evidence used by status and terminus polling. It may reuse a deep-copied projection for at most 500 ms and must expose cache status, age, source/current token counts, and a read-only claim boundary. Explicit fresh reads bypass it. It is not model memory, runtime state, a column vote cache, CUDA speedup evidence, or permission to hide stale mutation state.

**Subcortex Spike Health** — read-only operational stability evidence from competitive-column activity, bounded recent spike windows, local spike fraction, stale routing counters, visible silence/saturation thresholds, and windowed over-correlation risk. It is evidence for Runtime Truth, not a standalone liveness verdict.
_Avoid_: treating endpoint uptime as neural health, hiding threshold heuristics, treating one scalar correlation as full manifold health

**Subcortex Sleep Pressure** — active consolidation pressure derived from Subcortex memory pressure and trainer sleep counters. Policy and replay may use it to request consolidation or sleep; they must not derive fatigue, sleep, or memory pressure from a retired runtime snapshot.
_Avoid_: retired runtime fatigue, Cortex sleep state, compatibility snapshots as consolidation input

**Subcortex Self-Repair Candidate** — an advisory repair hypothesis derived from Subcortex Spike Health, such as reviewing column revival, inhibitory balance, stale routing, or decorrelation/pruning. It is not an action; promotion requires replay, deep-sleep repair, or operator gates.
_Avoid_: automatic self-mutation from status reads, treating repair suggestions as executed growth/prune events

**Self-Repair Promotion Gate** — the explicit gate on a Subcortex Self-Repair Candidate surface. It can request more spike-window evidence, mark candidates ready for replay/deep-sleep review, or keep them monitor-only; it must keep action, fact promotion, and structural mutation disabled.
_Avoid_: implicit repair execution, hidden replay mutation, structural mutation without a gate

**Self-Repair Gate Artifact** — the operator-facing read-only artifact that exposes Subcortex Self-Repair Candidates and their Self-Repair Promotion Gate without inserting those candidates into replay execution plans.
_Avoid_: replay candidate IDs, suggested endpoints, or execution payloads inside repair-gate status

**Self-Repair Evaluation Artifact** — the operator-facing read-only plan for measuring whether a Self-Repair Gate Artifact is safe to test in isolated replay or deep sleep. It names required evidence and success criteria, but does not run repair, mutate state, or promote structure.
_Avoid_: treating evaluation readiness as repair approval, hiding rollback/device/runtime-truth evidence requirements

**Self-Repair Evaluation Runtime Truth Evidence** — the compact Runtime Truth visibility summary for the Self-Repair Evaluation Artifact. It exposes evaluation-gate status, ready/case counts, next gate, success-evidence terms, and non-execution invariants while omitting evaluation cases, endpoints, suggested inputs, replay execution, deep sleep execution, repair mutation, and structural mutation.
_Avoid_: using Runtime Truth as the self-repair evaluator, exposing repair case payloads inside status, or promoting revive/prune/grow work from status visibility

**Delayed Consequence** — long-horizon utility tracking that connects earlier actions to later outcomes across queries and runs.

**Source Bank** — a named, ordered collection of training data sources (corpus, HF dataset, remote search) used by the subcortex for learning.

**Sensory Stream** — multimodal (visual, audio) observation stream for grounding, separate from text corpus.

## Key Relationships

- Subcortex is the active cognition substrate; external LLM/ThoughtLoop paths are outside runtime liveness claims.
- Subcortex Action Ledger owns action evidence. Digital action execution may update runtime history, provider calibration, consequences, and Runtime Truth evidence, but it must not initialize an external LLM path just to mirror action records.
- Subcortex Grounded Observations own source and sensory evidence. Focus selection comes from query gaps, autonomy plans, geometric curiosity, concept state, and source metadata; it must not depend on an external ThoughtLoop exploration target.
- Grounding Diagnostics belong to the Subcortex Language Surface boundary. SNN language/readout modules must be able to produce and inspect them without instantiating ThoughtLoop.
- Active Exploration State belongs to Subcortex control. ThalamicGate stores the canonical state object; curiosity, source-focus, and SNN deliberation modules must be able to normalize and inspect exploration targets without instantiating ThoughtLoop.
- Runtime Truth must describe active Subcortex evidence rather than external-path availability.
- Living Loop and Runtime Truth consumers should read active Subcortex evidence only; external LLM/ThoughtLoop paths are not status channels.
- Living Loop payloads must not emit a `cortex` sibling snapshot, `cortex_loop_snapshot` capability, or `retired_runtime_path` snapshot; replay pressure reads sleep/fatigue state from Subcortex Sleep Pressure.
- Long-test reports should emphasize Subcortex progress, Runtime Truth, memory pressure, spike health, and CUDA/device evidence. Retired LLM path fields are not compatibility payloads.
- Subcortex Language Surface may describe, narrate, or decode Subcortex state, but it must not own memory, policy, liveness, or Runtime Truth.
- Native-decode Subcortex Language is a bridge, not a generator: it may speak only from decoded assembly text and selected evidence, with support metrics attached.
- Cognitive Signal Subcortex Language is a status decoder: it may express runtime pressure and focus, but the numeric signal remains authoritative.
- SNN-Native Language Readiness Gate keeps NeuronSpark/Nord-style work as implementation references only; the target is MARULHO-owned language neurons and decoder machinery under CUDA/device, sparsity, grounding, replay/evaluation, and operator-control gates. Runtime Truth may include a compact readiness summary, while the full artifact remains the review surface.
- Subcortex Spike Readout Evidence is the first owned SNN-language readiness primitive: it turns Cognitive Signal pressure, concept focus, and CUDA/Subcortex device reports into readout slots and population-code bands without decoding or generating text.
- Subcortex Spike Readout Evidence must prefer observed tensor placement from `subcortex_tensor_devices` over configured CUDA intent. A configured `tensor_device` is only fallback evidence when no live Subcortex tensor device report exists.
- Spike Language Decoder Probe is the next owned SNN-language primitive after spike readout evidence. It may report sparse code occupancy, leaky recurrent temporal state, grounded slot support, and tensor placement, but it remains non-generative and cannot make `eligible_for_language_generation` true without a separately trained/evaluated SNN generator.
- Spike Language Neuron Adapter is the first owned language-neuron module boundary after the decoder probe. It may report PLIF-style membrane/spike dynamics and activation sparsity, but it remains non-generative and must stay behind the SNN-Native Language Readiness Gate until a controlled training/evaluation loop exists.
- SNN Language Adapter Evaluation Gate is the next review boundary after the neuron adapter. It can mark the adapter ready for isolated evaluation when sparse dynamics, grounding support, and device evidence are present, but it must keep language generation, training, action, fact promotion, and cognition-substrate promotion false.
- Heldout Language Adapter Evaluation is the concrete evidence mechanism behind that gate. It may mark heldout evidence ready for operator review, but the next gate is still training-loop design review, not generation.
- SNN Language Training Readiness Gate is the design-review bridge after heldout adapter evidence. It can mark a local SNN trainer design review ready, but it keeps training, language generation, action, fact promotion, and cognition-substrate promotion false until a separate MARULHO-owned trainer exists and passes evaluation.
- SNN Language Trainer Dry Run is the first local trainer experiment after design readiness. It may perform ephemeral local Hebbian sequence updates over spike readout slots for evidence, but it must return only metrics and discard weights.
- SNN Language Trainer Isolated Evaluation is the gate after a dry run. It can mark evidence ready for operator review, but it keeps runtime training, trainer promotion, language generation, and cognition-substrate promotion false.
- SNN Language Sequence Prediction Probe is the first local next-code prediction surface for the language path. It predicts sparse population-code indices from grounded spike readout sequences, but it does not decode those indices into text or promote them as thoughts.
- Persistent SNN Language Transition Memory lets applied live plasticity influence later sparse-code prediction through checkpointed MARULHO-owned transition weights. Prediction must report whether persistent memory influenced the result.
- SNN Language Transition Memory Homeostasis gives that memory bounded decay, outgoing-row normalization, weak-edge pruning, checkpoint rollback, and a maintenance ledger. It is an explicit mutation command, never a read-side effect.
- SNN Language Transition Memory Sleep Policy can recommend that maintenance from active Subcortex Sleep Pressure and replay evidence, but it cannot execute the recommendation automatically.
- SNN Language Transition Memory Regeneration Proposal is a read-only advisory artifact for replay-window-backed local sparse regrowth after mismatch remains high. It must carry a replay-window identity, evidence hash, canonical neuron indices, and bounded locality while keeping topology mutation false.
- Replay-Backed Regeneration Permit is the Replay Controller-owned provenance artifact that authorizes one current-revision regeneration review. It binds high mismatch evidence, plasticity-pressure evidence, a grounded replay window, normalized operator identity, operator confirmation, Runtime State revision, and the canonical bounded regeneration design into a durable content hash. Caller-authored replay IDs, hashes, or post-permit candidate edits are not permits.
- SNN Language Transition Memory Regeneration is the checkpoint-backed operator-confirmed command that may add sparse transition edges after revalidation. It enforces fixed MARULHO ceilings for canonical indices, event edge count, row fan-out, outgoing row mass, and global sparse topology size; duplicate-only proposals are blocked without advancing Runtime State.
- Applied regenerated synapses retain replay permit/artifact provenance and local sparse edge provenance, including rollout source/target step indices and active-index hashes when regeneration came from rollout consolidation.
- Replay Controller-issued permits narrow caller-authored structural-write authority to one server-held, revision-bound, design-bound artifact. Automatic structural adaptation remains blocked until permit issuance itself is driven by evaluated replay policy rather than an operator-confirmed command.
- Replay-Backed Regeneration Permit requires a server-held, internal-ledger-backed SNN Transition-Memory Replay Artifact whose SNN Replay Artifact Recording Review Ticket and Evaluated SNN Transition-Memory Replay Context are still current and content-verified. General replay audit-sampling history, raw caller-window artifacts, caller-carried mismatch or pressure reports, SNN Replay Consolidation Priority Queue rank, and SNN Replay Artifact Recording Policy Proposal recommendation must not be treated as equivalent evidence. Evaluated proposals now derive grounded replay windows from the internal Readout Evidence Ledger; policy proposals may choose review targets, but artifact recording remains operator-confirmed through a server-held review ticket.
- SNN language structural writes require an exact pre-mutation checkpoint round trip: the serialized transition-memory state and Runtime State revision must match the in-memory snapshot before live update, maintenance, or regeneration can proceed.
- Durable Structural Plasticity Transaction is the commit boundary for SNN language live update, maintenance, and regeneration. It requires a distinct pre-mutation rollback checkpoint, an atomically replaced post-mutation committed checkpoint, exact state/revision verification, and fail-closed in-memory recovery when commit verification fails.
- Runtime State revision continuity is checkpoint state. Service startup must hydrate the persisted clean revision exactly; explicit operator restore may advance revision to invalidate stale commands and permits.
- Published Current Checkpoint is the Runtime Persistence-owned atomic pointer to the last verified committed brain image. It references an immutable managed checkpoint object with size, SHA-256, and revision evidence; structural mutation may publish it only after committed state/revision verification. Startup validates the current descriptor, falls back to the previous committed descriptor when needed, and resolves the selected object before loading the trainer.

- Root Capture Refresh is the Service Manager-owned rebind step after a committed trainer, encoder, metadata, checkpoint-path, or action-root swap. Runtime Persistence invokes it after publication and explicit restore; Runtime Control invokes it after a preset rebuild. It refreshes concrete captures in active collaborators before runtime work resumes, so Terminus cannot split across different brain images.

- Operator Restore Commit is an explicit restore promoted into a new durable brain image. It imports the selected checkpoint, advances Runtime State beyond both the live and imported revisions, stages a fresh checkpoint, and publishes that staged image as the current managed object. Publication failure must recover the previous live image and leave the prior Published Current Checkpoint untouched.
- SNN Language Sequence Mismatch Probe is the first prediction-error surface for the language path. It can report how predicted spike-code indices differ from observed next spike-code indices, but it cannot apply learning or promote the mismatch as a fact/action.
- SNN Language Plasticity Pressure Gate turns prediction error into reviewable local learning pressure. It can identify observed-only and predicted-only sparse code targets for a future isolated plasticity trial, but it cannot apply plasticity by itself.
- SNN Language Plasticity Trial simulates the candidate local update from pressure evidence and reports expected pressure reduction. It must discard all update state and keep runtime training disabled.
- SNN Language Plasticity Replay Evaluation checks whether trial evidence is ready for operator replay review. It does not execute replay or apply the update.
- SNN Language Plasticity Replay Experiment rehearses sparse replay sequences in isolation. It can report replay coverage and simulated pressure stability, but it cannot persist weights, mutate runtime state, or promote language/facts.
- SNN Language Plasticity Application Design bounds a possible future live update. It can specify local update constraints and device evidence, but it remains read-only and cannot apply learning.
- SNN Language Plasticity Shadow Delta measures a proposed local update from sparse replay evidence. It is stronger than a hand-authored delta fixture but still cannot apply weights.
- SNN Language Plasticity Shadow Application verifies a proposed bounded update against design constraints and device evidence. It can report whether the shadow delta is locally stable, but it still cannot apply weights or mutate runtime state.
- SNN Language Plasticity Live Application Readiness checks whether shadow evidence has checkpoint/restore rollback coverage and explicit operator approval. It still keeps live application false until a separate application executor exists and is approved.
- SNN Language Plasticity Live Application Preflight checks the concrete mutable target and checkpoint transaction while staying read-only.
- SNN Language Plasticity Live Application Executor is the first command boundary that may mutate MARULHO-owned sparse language transition weights after revalidation. It increments Runtime State and stays separate from Status Read Model.
- SNN Language Plasticity Path Evidence is the Runtime Truth summary for the chain. It exposes progress and invariants, but it is not a substitute for detailed gate artifacts or live application approval.
- Long-test reports use Subcortex Readout vocabulary. They must not publish ThoughtLoop-era `thoughts`, `thought_lifecycle`, `narrative_self`, `global_workspace`, or dream-verification report fields as active liveness evidence.
- Cognitive Signal is the canonical runtime signal surface. Its state primitive must be importable without `ThoughtLoop`; `cortex_signal` aliases are deleted and must not be reintroduced.
- Subcortex Deliberation candidates are advisory control candidates until replay, policy, or operator evidence promotes them; they must not be stored as facts, treated as generated thoughts, or queued as LLM prompts.
- Deliberation Feedback belongs to Subcortex control. `emit_deliberation_feedback` is the active API; the old `emit_cortex_feedback` path is removed.
- Deliberation Text Merge belongs to the Subcortex Language Surface boundary. Active code and tests should call the merge helper directly instead of using `ThoughtLoop` as a namespace.
- Brain Runtime Metrics can summarize language-facing runtime output and grounding quality, but Runtime Truth and Subcortex Spike Health remain the authoritative liveness evidence.
- Living Loop status is the primary operational sidecar for Subcortex Deliberation candidates. Policy Actuator may display the same candidates as non-executable context, but policy status must not execute them or promote them beyond advisory evidence.
- Policy Actuator reads sleep/fatigue pressure from Subcortex Sleep Pressure, not from `retired_runtime_path` or any Cortex snapshot.
- Replay planning reads memory/consolidation pressure from Subcortex Sleep Pressure, not from a retired runtime snapshot.
- Every Subcortex Deliberation candidate must carry a promotion gate. The gate may mark it ready for replay review or blocked by missing grounding, but it must keep action execution and fact promotion false until a separate replay/policy/operator path explicitly promotes it.
- Developmental Plasticity is the clean path for self-growth and pruning: runtime changes must be traceable, bounded, reversible, and evaluated before promotion.
- Newborn Synapse Maturation Outcome is the terminal result of a finite critical period. `mature` retains the live connection; `prune_eligible` authorizes a separate structural-removal decision but does not itself remove a synapse.
- Newborn Synapse Pruning is the checkpointed removal of one reviewed `prune_eligible` connection from both sparse topology and dense readout state. It preserves a provenance tombstone and must not be described as neuron death or capacity reduction.
- Developmental Autonomy Cycle is the machine-driven maintenance step after a brain tick or developmental learning cycle. It may remove reviewed terminal synapses without operator approval, but it must not advance an open critical period without canonical pre/post spike evidence for that synapse.
- Canonical Developmental Spike Evidence means source- and target-neuron spike timings bound to an exact live synapse and observation window. Aggregate column spike counts are telemetry, not a substitute for this evidence.
- Dynamic Language Population Capacity is the runtime-owned neuron count shared by language sparse coding, local trainer tensors, transition prediction, mismatch, rollout, replay, regeneration, and developmental plasticity. Indices at or above the original 64-neuron floor are distinct neurons and must never be modulo-folded into older indices.
- Language Population Growth expands the persistent SNN language/readout population. It is real growth inside that population, but it is not evidence that every AdEx, competitive-column, sensory, or binding tensor in the wider Subcortex has also grown.
- Local Plasticity Evidence is the first synapse-level support signal for Developmental Plasticity: local STDP traces, synaptic scaling, inhibitory balance, spike-health risk, synaptic validation, and device reports can make a case ready for isolated evaluation, but never directly mutate synapses or topology.
- Structural Mutation Ledger is required when topology changes at runtime; growth/pruning must leave countable evidence before it can support Living Brain claims.
- Column Structural Review Queue is the competitive-column continuation surface before structural mutation: it can persist bounded awake/candidate review tickets across checkpoint restore, but it is not a mutation ledger and cannot replace operator-approved structural transactions. The scheduler benchmark treats its evaluated/cached counts, pending ticket counts, checkpoint-backed/operator-review flags, next gate, baseline queue hash, candidate evidence hashes, `mutates_runtime_state=false`, and `runs_all_columns=false` as part of Runtime Truth; benchmark-forced ticket evidence can queue bounded growth plus prune/sleep tickets from the wake plan after measurement, proving coverage only and not a speed claim.
- SNN Calibrated Dense Label Confidence Use Preflight is the read-only gate after confidence-use design. It validates current Runtime State revision, bounded candidate hashes/confidences, threshold/pass conditions, device/CUDA evidence, and executor capability before a future confidence-use executor may rank or abstain; it still cannot decode text, generate language, train, replay, apply plasticity, write checkpoints, promote facts/actions, or mutate runtime state.
- Structural Plasticity Gate Artifact is the read-only promotion surface for Developmental Plasticity: it can expose concept growth pressure, hypercube/binding mutation ledger state, and CUDA/device evidence, but structural mutation remains behind isolated evaluation and operator gates. Runtime Truth may include a compact gate summary, while the full artifact remains the review surface.
- Isolated Structural Plasticity Evaluation is the evidence bridge after the gate artifact: it can compare bounded pre/post topology snapshots and mark them ready for operator review, but the next gate remains an operator-approved structural mutation design, not automatic self-growth. Its checkpointed-candidate gate must bind candidate reason, baseline hash, cost/usefulness/latency/RAM/VRAM impact, Runtime Truth summary, rollback artifact, and no-mutation proof before a candidate is ready for transaction review.
- Structural Mutation Application is the only current live structural executor boundary. It must recompute the candidate-bound preflight hash, save and verify a pre-mutation checkpoint before mutation, commit only after post-mutation checkpoint verification, and emit candidate provenance plus a tombstone/rollback artifact for blocked, rejected, over-budget, no-delta, or failed-commit outcomes. Service endpoints may call the executor with supplied preflight evidence, but they still do not select candidate edges or own growth/pruning decisions.
- Concept growth pressure and binding mutation ledgers are not enough by themselves. A structural case can become ready for isolated evaluation only when paired with observed binding or local-plasticity tensor device evidence.
- Local Plasticity Evidence can support growth/prune review only when eligibility traces, homeostatic state, device placement, and non-failed synaptic validation are all present. Failed synaptic validation is repair pressure, not promotion readiness.
- Subcortex Self-Repair Candidates require a sufficient spike/correlation evidence window before replay or deep-sleep review can be marked ready. A risk label without that window stays evidence collection.
- Cognitive Signal is the telemetry/control contract that lets Subcortex update runtime pressure, concept alignment, and future Subcortex Deliberation modules.
- Retired ThoughtLoop code is deleted and must not block Runtime Truth, CUDA evidence, or long-run liveness. Cognitive Signal state, Active Exploration State, Grounding Diagnostics, Brain Runtime Metrics, and deliberation text merge belong to Subcortex-owned modules.
- Retired Runtime Path State Holder is deleted. Active modules must not build, lazily initialize, start, store, snapshot, or report a ThoughtLoop-derived runtime path.
- The top-level `marulho.cortex` package is deleted. Runtime, mock, memory, drive, prompt, narrative, and language primitives must not be reintroduced under that namespace.
- External LLM Cortex adapters are deleted. `NIMCortex`, `MultiCortex`, and Cortex environment factories are not valid runtime, testing, or extension points.
- External embedding adapters are deleted from Episodic Memory. Local sparse text encoders may remain as transitional indexing machinery, but remote API keys, NIM request accounting, and external embedding clients are not valid memory substrates.
- Remote API rate limiting is deleted with the external adapters. Runtime throttling should be reintroduced only for concrete maintained sources, not as Cortex/NIM budgeting.
- Mock Cortex is deleted. Tests may assert retirement boundaries, but `MockCortex` must not exist as a production-source class or stand in for language, thought, memory, sleep, or reasoning.
- ThoughtLoop's runnable body and module are deleted. There must be no hidden generation, sleep, or background-loop branches.
- Language Result is the Subcortex/semantics-owned result packet for language/readout quality metrics. Active code must import `LanguageResult` directly and must not use `ThoughtResult` aliases.
- Language Packet is the Subcortex/semantics-owned context contract for language/readout slots, mode, memory items, and depth. Active code must import `ContextPacket`, `MemoryItem`, `ReadoutMode`, and `DeliberationDepth` from `marulho.semantics.language_packet`; `marulho.cortex.core` is deleted.
- Cortex prompt templates are deleted. Language/readout steering should be semantics/Subcortex-owned and grounded by packet evidence, not preserved as Cortex-owned static LLM prompts.
- Cortex-owned drives, episodic memory, working memory, narrative self, prompt, core, and ThoughtLoop modules must not be active extension points; reusable concepts belong under semantics, service runtime, or Subcortex modules.
- Gap Planner and Curiosity Controller feed Source Bank selection for autonomous acquisition
- Replay Pipeline feeds adapter experiments that never touch production runtime
- MarulhoBrain owns runtime orchestration while package owner modules supply replay, grounding, policy, source, and interaction machinery behind explicit calls. Living Loop evidence is produced by Subcortex runtime state, replay, grounding, and policy surfaces; it must not require ThoughtLoop.
- CUDA-first Runtime applies to tensor-heavy Subcortex modules such as routing, predictive columns, neuron dynamics, binding, plasticity, cross-modal grounding, text encoders, and sensory encoders. External LLM paths are not CUDA-first claims or architectural requirements.
- Runtime Evidence Report is the bridge from internal CUDA-first claims to operator-visible status; it must include trainer-owned Encoder evidence as well as model-owned Subcortex evidence.
- Sensory Encoder device reports must include both persistent encoder state devices and the last emitted spike tensor device/shape; a configured device alone is not CUDA evidence.
- Multimodal Stream Loader tensors must resolve to the runtime device before encoding. Directory-loaded `.pt` visual/audio tensors and synthetic sensory tensors must not silently enter the Subcortex path through a CPU-only default.
- Checkpoint restore must select the active runtime device for `torch.load`; CPU serialization is allowed for archival payloads, not as the live Subcortex placement rule.
- Runtime Evidence Report and replay/export planning must read active Subcortex, Runtime Truth, spike-health, memory, and sleep-pressure evidence directly.
- Brain runtime snapshots must not expose `retired_runtime_path`, publish an active `cortex` sibling payload, or require `cortex_snapshot()` helpers.
- Subcortex Spike Health is the first operational-stability slice inside Runtime Evidence Report: it can flag silent, saturated, stale routing, or windowed over-correlation risk, while full operational-manifold health remains a future benchmark-level claim.
- Subcortex Self-Repair Candidates turn Spike Health into reviewable repair pressure for Living Loop and Policy Actuator sidecars. The Self-Repair Promotion Gate must keep action execution, fact promotion, and structural mutation false until a separate replay/deep-sleep/operator path approves the repair.
- Self-Repair Gate Artifact is exposed separately from Replay Plan so repair pressure can be reviewed without becoming a replay execution candidate or mutating runtime state. Runtime Truth may include a compact gate summary, but the full artifact remains the review surface.
- Self-Repair Evaluation Artifact is the next gate after review readiness: it specifies isolated replay/deep-sleep evaluation evidence, rollback expectations, Runtime Truth delta, and CUDA/device evidence before any repair can be promoted.
- Path Retirement Gate now applies to Cortex: LLM-backed runtime paths are being removed from active architecture so focus returns to Subcortex, world-model, memory, and policy mechanisms.
- Live Tick Stage Profile is bounded CPU wall-clock evidence for source collection, trainer execution, concept observation, finalization, and lock/yield costs. It must not synchronize CUDA per token or be interpreted as a device-kernel profile.
- Sampled Batched Concept Observation observes the first token, every eighth token, and the final pending token in a source window, then submits those samples as one bounded semantic batch. It preserves mutable ConceptStore learning while running structural growth/pruning maintenance once at the source-window boundary. Runtime evidence reports attempts, observations, batch count, structural-maintenance passes, and stage latency. Avoid restoring per-token ConceptStore structural maintenance, hiding concept growth/pruning inside every training step, or treating batched semantic maintenance as a CUDA speed claim.
- Source Tick Sleep Deferral keeps deep, micro, and repair sleep replay out of `MarulhoBrain` live/background source ticks. Brain tick per-token fallback passes `allow_sleep_maintenance=False`, counts due sleep as deferred trainer maintenance, and leaves sleep replay to explicit slow-path windows. Avoid running replay because the source tick needed metrics, or treating a deferred sleep flag as completed consolidation.
- Deferred Slow Memory Cadence is the trainer-owned metabolism boundary that separates every-token competitive/context/cross-modal dynamics from expensive slow-memory replay admission. The hot path archives first-token retained/fallback records and selected strong-capture events, but fixed interval cadence is retired as a slow-memory write path and now records deferred maintenance evidence only. Runtime Truth exposes deferred cadence count and last cadence token under the cognitive boundary controller plus archive skip pressure under `memory_hot_path`. Retired default checkpoint values at 8 or 64 are migrated when no current defaults revision is present. Avoid treating skipped archival writes as skipped cognition, hiding the cadence from operators, or restoring cadence admission without complete-tick and replay-quality evidence.
- Bounded Live Memory Summary Projection keeps trainer telemetry and service/status snapshots off the full slow-memory summary scan. The live path reports fill/counter/report fields through `bounded_memory_summary_projection.v1`, scan count `0`, and read-only projection evidence; full memory summary is an explicit offline quality/consolidation path only. Avoid using status to advance memory decay, selecting replay from the projection, or restoring full `summary_stats()` to live/status callers without a long-run throughput gate.
- Selected Sleep Replay Routing-Index Refresh is the one active post-replay routing maintenance path. After bounded deep/repair replay has already selected and improved prototype IDs, trainer calls `routing_index_existing_row_refresh.v1` to update only existing tensor-cache rows through a CPU ID-to-row map; archival replay metadata remains CPU-resident, active routing tensors stay on the index device, and missing IDs or dirty caches report deferred recovery without `add()+rebuild()` inside selected replay. Full rebuild remains bootstrap/checkpoint/offline cache construction, not replay-window recovery. Avoid rebuilding the full routing index after selected replay, using row refresh as replay selection, or moving routing metadata into always-on live tick work.
- Bucket Consolidation Cache Lookup keeps winner/bucket memory-pressure reads off scalar slow-memory scans. `bucket_consolidation_level_cache_lookup.v1` reads the maintained CPU bucket consolidation cache and reports `full_memory_scan=false`; a missing cache is a no-scan miss, while explicit tensor rebuilds remain checkpoint/load/offline/capture, explicit tensor request, or benchmark-local work. `bounded_selected_replay_consolidation.v1` no longer rebuilds a missing bucket cache before selected replay: it mutates only selected entries, reports selected indices/buckets, uses selected-bucket delta updates only when cache metadata already exists, and records `cache_missing_deferred_no_full_rebuild` otherwise. Avoid recomputing a winner bucket by iterating every slow-memory entry inside retained, graph metric, or selected replay windows.
- Generic Replay Entry Row API Retirement keeps replay/recall row access on named, purpose-specific readers. `DualMemoryStore.replay_entry(...)` is removed. Mutating tensor-only sleep repair uses `sleep_repair_replay_row(...)`; read-only sleep/HF replay-query recall uses `replay_recall_row(...)`; query/source-bank/context recall uses `query_match_row(...)`. Active reports carry positive row-reader evidence such as `query_row_reader=DualMemoryStore.replay_recall_row` and `direct_slow_memory_input_pattern_reads_retired=true`; they must not preserve compatibility fields for deleted replay-entry readers. Avoid relying on implicit replay text defaults or letting replay text become hidden language reasoning.
- Inherited Replay Query Bucket Cap keeps restored or caller-supplied HF replay query reports inside the same canonical anchor bucket budget as fresh query collection. `_bounded_replay_recall_evaluation(...)` accepts inherited `candidate_bucket_ids` only when the report carries `bounded_replay_query_anchor_bucket_source_window.v1`, then de-duplicates and caps them to `REPLAY_QUERY_ANCHOR_BUCKET_WINDOW_LIMIT=16` before calling `DualMemoryStore.recall_replay_window(...)`; otherwise it rebuilds the bounded source window from current anchors. The external benchmark `..\..\MARULHO_reports\bounded_replay_window_20260623\replay-query-inherited-bucket-cap.json` cut an oversized inherited `4096`-bucket report to `16`, preserved exact input recall (`mean_input_pattern_distance=0.0`), kept CPU archival/active recall placement and `cuda_memory_delta_mib=0.0`, and reported no live tick, every-token work, raw replay text, or hidden language reasoning. The same-checkpoint `524288`-token gate `..\..\MARULHO_reports\bounded_replay_window_20260623\hotpath-active-pressure-65536-524288-i32-inherited-query-cap-pinned-main-rerun.json` reached `6162.974 tokens/sec`, `train_compute=0.130390 ms/token`, bounded `12/65536` route rows, no all-column transition, no polling, and zero graph/native failures. Avoid trusting inherited replay reports as already bounded, or using GPU-utilization percentage alone as the throughput gate.
- Service Advisory Replay Lane Retirement deletes `/terminus/replay-plan`, `/terminus/replay-sample`, `/terminus/replay-sample/history`, `/terminus/replay-dataset/preview`, `/terminus/replay-dataset/bundle`, and older replay-dataset/replay-execute aliases. Runtime trace export is trace-only; SNN replay review remains in `ReplayController`; actual replay/recall stays trainer-owned and selected in explicit slow windows. Initial 2026-06-23 hot-path reports are rejected as noisy/unpinned evidence (`3982.916`, `4559.668`, `5067.347`, and `4859.400 tokens/sec`); diagnosis found the throwaway worktree imported the editable main checkout unless `PYTHONPATH` was pinned. Corrected pinned-import `524288`-token evidence accepts the retirement as live-tick protected: current HEAD reached `5865.705 tokens/sec` versus pinned old known-good `fbb788de` at `5880.863 tokens/sec` (`-0.26%`), with no observed contention, bounded `12/65536` route rows, no all-column transition, no measurement polling, RTX 3060 CUDA placement, and zero graph/native sequence failures. Avoid reviving service replay summaries, compatibility routes, UI cards, or hidden replay-text reasoning unless a new measured executor contract proves selection quality, device placement, latency, checkpoint/reload behavior, and long-run hot-path protection.
- Replay Restore Source Window keeps checkpoint/reload replay-controller state on the same bounded newest-first path as the active replay queues. `bounded_replay_restore_source_window.v1` slices replay sample history, regeneration permits, replay-evaluation contexts, review tickets, scheduler installations, and transition-memory replay artifacts to their controller retention limits before normalization, indexing, or evaluated-artifact validation. Restore metadata stays CPU-resident, reports no full retained materialization, no live tick, no every-token work, no replay text, no hidden language reasoning, no mutation/plasticity, and no GPU archival metadata. Avoid restoring `list(...); normalize-all; slice` compatibility shapes around replay checkpoint histories.
- Applied Replay Lineage Incremental Checkpoint Summary keeps replay-regenerated synapse lineage validation on a mutation-maintained CPU digest. Replay regeneration records one row hash per replay-regenerated synapse; non-replay overwrites and pruning clear that row; checkpoint save/restore reads `snn_applied_replay_lineage_incremental_summary.v1` with `source_record_scan_count=0` and `full_provenance_scan=false`. Missing incremental state blocks exact lineage validation instead of scanning `synapse_provenance_by_key`. Avoid restoring checkpoint-time full provenance scans, GPU-resident archival lineage metadata, raw replay text, hidden language reasoning, replay execution, or plasticity authority from lineage summaries.
- Bounded Plasticity Runtime Transition Memory Source Window keeps `/terminus/snn-language-sequence/plasticity-runtime-state` from becoming a full retained transition-memory export. The active projection returns at most `64` newest sparse-weight rows and `64` newest provenance rows from CPU archival state, preserves retained counts in `transition_memory_source_window`, and forces consumers to treat truncated maps as partial. Exact replay/provenance integrity still belongs in explicit slow audit/replay windows. Avoid restoring full runtime-state deep copies, claiming partial maps are complete, moving archival metadata to CUDA, or turning runtime-state reads into live-tick/every-token replay work.
- Sleep Plasticity Ticket Queue Source Windows keep review-ticket and scheduler-design-review-ticket queues bounded before autonomy, scheduler-design, or installation proposals. The queues inspect only the newest `16` retained records, report `retained_count` separately from source-window counts, keep archival/source/score placement on CPU, and state no global candidate/score scan, no raw replay text, no hidden language reasoning, no live tick, no every-token cadence, no scheduler install, and no mutation/plasticity. Malformed newest records block the bounded window instead of scanning past them to older retained tickets. The repo-local benchmark no longer carries a full-retained diagnostic verifier; future comparison controls belong outside the maintained code path or must be replaced by a bounded/indexed selector with measured quality benefit and repeated hot-path protection.
- Sync-Free Drift Maintenance is the trainer-owned rule that drift refresh and drift-floor closure must not force CUDA event drains on ordinary burst ticks. Drift refresh uses a fresh host winner only when the mirror is already fresh; otherwise it computes global drift and records that fallback explicitly. This keeps drift as Runtime Truth/metabolism evidence without synchronizing the device solely for a CPU maintenance scalar.
- Deferred Source Cache Persistence is the Runtime Sources durability boundary for live text queues. Cognitive ticks may hash bounded raw windows and schedule one coalesced cache write, but `torch.save`, directory creation, atomic replacement, and retry handling run outside the tick. Shutdown flushes pending writes. Runtime Truth exposes scheduled, completed, skipped, failed, and pending writes. Avoid restoring synchronous checkpoint/cache persistence inside `prepare_training` or treating durability I/O as GPU cognition.

**Persistent Text Tick Executor** — the default promoted CUDA Graph execution
unit for eligible fixed-shape CUDA text ticks. It captures input normalization and
projection, reconstruction error, reconstruction-driven neuromodulator update,
sparse route/vote, the in-place competitive/predictive transition, and
post-transition competitive-surprise measurement in one replay over persistent
buffers. It performs one bounded host truth synchronization for reconstruction,
neuromodulator, winner, effective plasticity, and competitive-surprise evidence.
The CPU surprise history consumes that same packet instead of launching a
second norm/readback. Small host control parameters are still staged before
replay until a device-owned control-state executor proves it can remove that
boundary. Sensory ticks, unsupported shapes, pointer changes, and capture
failures retain the pre-mutation fallback path.
_Avoid_: calling it a complete brain tick, hiding the host truth
synchronization, graphing archival memory or source I/O, or treating CUDA Graph
capture as proof of end-to-end production velocity

**Persistent Quantum Input Ring** — the training-owned fixed-address CUDA
input boundary used by the Persistent Text Tick Executor. Brain Runtime may
offer one bounded sequential execution quantum, but training stages its already
encoded tensors into the ring and the captured graph advances the device slot
and recent-spike-row cursor. When an ordinary warm text sequence is wider than
the eight-token burst graph, training first preflights every burst-sized slice
with the same boundary classifier used by the real burst plan, but without
mutating Runtime Truth counters. Training stages the longest safe metric-free
segment up to the bounded ring capacity, then consecutive bursts consume exact
pointer-checked slices from that staged window. Host-truth, sleep, metrics, and
other cognitive boundaries stop the staged segment instead of freezing the
whole source tick. Pointer-order checks make staged reuse exact; mismatch,
sensory, unsupported, or other fallback boundaries skip or discard staged
material before mutation and use the retained fallback copy. Runtime Truth
exposes sequence-stage calls/tokens/skips plus graph-level staged, reused,
fallback, mismatch, discard, and device-owned cursor counts;
`quantum_input_reuse_count` counts consumed tokens, not just successful staging
calls, and staged-token counts should not exceed consumed tokens for a clean
boundary-safe run.
_Avoid_: treating the quantum as parallel cognition, placing graph algorithms
in `service`, staging more than the bounded ring capacity, or synchronizing CUDA
per token to report ring progress

**Boundary-Aware Text Burst** — the training-owned host-orchestration fast path
for ordinary text ticks between explicit cognitive boundaries. It stages the
existing input ring only when no wider staged quantum already covers the burst
slice, replays the proven one-tick CUDA graph in causal order, and batches only
host bookkeeping. The promoted default sequence executor is the
conditional-WHILE CUDA Graph parent at sixteen tokens for the maintained q16
text sequence. It keeps the one-tick graph body and moves loop control into a
native CUDA conditional node plus device counter, so sequential SNN state still
mutates one token at a time. The repeated-child native parent graph remains the
exact eight-token internal fallback when conditional construction cannot run
before mutation; `cuda_graph_native_burst_tokens` is fixed at `8`, while
`cuda_graph_sequence_loop_tokens` is fixed at the promoted
`16`-token conditional capacity. The 2026-06-15 promotion gate beat the
refreshed `4992.049 tokens/sec` native8 baseline in both paired orders, and the
post-promotion clean default reached `6116.646 tokens/sec` with
`train_compute=0.134167 ms/token`, `8190` conditional launches covering
`131040` tokens, zero sequence/native fallbacks or failures, host-truth cadence
`4097/126975`, and no observed velocity contention. Startup capture and native
conditional compile cost remain visible outside measured warm throughput. The
rejected startup-warmed sixteen-token repeated-child parent graph remains a
historical prototype because it preserved state but reached only `4887.767 tokens/sec`
against the refreshed eight-token ceiling; the native16/native32 capacity knobs,
the q8/q32 conditional-WHILE capacity knobs, and partial native tail replay path
were removed from the live config, env, and stress benchmark surfaces, while
checkpoint load coerces old native burst and sequence-loop capacity values back
to `8` and `16`. A graph-owned
**Device Strong-Event Ring** snapshots the bounded result packet on every burst
tick and copies assembly/routing evidence only when that tick crosses the
configured strong-capture threshold. Training drains those events at the
host-truth boundary and archives their input/evidence payloads on CPU. The
retained event capacity and host-truth cadence are both thirty-two tokens;
later sixty-four and one-hundred-twenty-eight token rechecks reduced drain
count but did not beat the complete-runtime base comparison. Drift
refresh, telemetry, sleep, slow-memory cadence, cross-modal wake, host-truth,
concept-observation, and final-metrics boundaries retain the full per-token
path. Runtime Truth reports burst executions, tokens, failures, fallback-reason
counts, strong-event count, ring ownership, and CUDA device evidence.
_Avoid_: calling the burst parallel cognition, skipping CUDA transitions,
moving eligibility algorithms into `service`, crossing a slow-path boundary,
using the previous mirrored surprise value to predict internal burst events,
retaining archival tensors on CUDA, letting speculative preflight update Runtime
Truth counters, promoting wider repeated-child parent graphs from launch-count
reduction alone, restoring `cuda_graph_native_burst_tokens=16` as a live
conditional promotion mechanism, or replacing the one-tick graph with a larger
graph without sustained evidence or a fail-closed parent-graph promotion gate

**Training-Owned Text Sequence** — the training boundary that accepts one complete service text tick, executes its ordered q16 sequence quanta, requests full metrics only for explicit evaluator evidence positions, checks stop requests between quanta, and returns Device-Burst Lightweight Metrics to service for ordinary prepared source ticks. Service still owns source selection, locks, Runtime Truth projection, and concept observation; training owns neural sequencing, burst/fallback selection, event drains, and per-token mutation semantics. Runtime Truth reports sequence calls, tokens, quanta, stops, owner, and stop boundary.
_Avoid_: moving concept algorithms into training, skipping sequential SNN updates, checking stop only after an unbounded tick, letting service duplicate burst policy, or claiming one API call means one CUDA kernel.

## Flagged Ambiguities

- Persistent Text Tick Executor is the active meaning of
  fixed `predictive_route_vote_mode=cuda_graph_text`; the previous two-replay
  pre-route/transition split and old tensor/fused route-vote selectors are retired.

- "SSN side" is not canonical in this project. Resolved: use **Subcortex** for the domain layer and **SNN** only when referring specifically to spiking neural network mechanics.
- "Living brain" must not erase the existing safety vocabulary. Resolved: use **Living Brain** only as an evidence-gated target state; use **Runtime Truth** for actual liveness classification.
- "language generation" is ambiguous in this project. Resolved: use **Subcortex Language Surface** for grounded expression of Subcortex state, and reserve **Subcortex Deliberation** for cognition mechanisms that can run without an LLM.
